# v1 的範疇 —— 什麼准許出現在 `release/v1` 的樹上

> 這份文件給**維護 `release/v1` 這條線的人**。它只回答一個問題：
> **一個目錄該不該存在於 v1 的樹上。**
>
> 要用這個腳手架開案子的團隊，你要的是 [HANDOFF.md](HANDOFF.md)，不是這份。

---

## 兩份清單，兩條軸 —— 刻意不互相抄

| 文件                             | 軸       | 它回答的問題                               |
| -------------------------------- | -------- | ------------------------------------------ |
| HANDOFF〈v1.0.0 **不**涵蓋什麼〉 | **能力** | 「我的案子要無障礙／SBOM，該用哪條分支？」 |
| 本檔                             | **目錄** | 「這個目錄准不准出現在 v1 的樹上？」       |

⚠️ **兩邊不重述對方的內容，只互相指過去。** 同一件事抄成兩份、而沒有東西
斷言它們一致 —— 那正是 `v1.0.3` 才剛付過代價的病（HANDOFF〈已知的誠實缺口〉
第五條）。加一份清單而不加守它的機制，是在製造第五份手抄本。

---

## 判準：**看它守的東西給誰看**

- **面向交付物（含文件）→ 在內。** 受益者是**拉 v1 去做案子的團隊**。
- **面向開發流程 → 在外。** 受益者是維護者。

判定時**必須寫得出「受益者是拉 v1 的團隊」那一句**。寫不出來就不准進。

問法是：**「這東西紅了，是誰的問題被擋下來？」** 答案是拉 v1 的團隊 → 可以進；
是維護者 → 送 `main`。這條線怎麼定出來的、以及它為什麼不是「不服務五條承諾
就出局」，見 DECISIONS.md 的 C72。

---

## 事實來源是 **git**，不是工作樹

```bash
git ls-files -- tools/ platform/   # 這是 tools/scope-check 用的那一條
```

⚠️ **不要用 `ls`。** 切換分支不會刪掉被 ignore 的殘留目錄：`release/v1` 上
`ls tools/` 會列出一堆早已不在版控裡的目錄（`gate-roster` 就是其一，
`v1.0.3` 移除後它的 `node_modules` 還留在磁碟上）。用 `ls` 寫的檢查會把
那些殘骸當成違規，而報告出來的東西一個字都不對。

⚠️ **也不是 `git ls-tree HEAD`。** 那答的是「上一個 commit 裡有什麼」——
新增一支工具、`git add` 了、跑 `vpr ready`，**是綠的**，要等 commit 完才紅。
`ls-files` 讀的是 index，staged 的新目錄當場就看得見。

---

## `tools/` —— 准許存在的

每一支都要能填完最後一欄。填不出來的就不該在這裡。

| 路徑                  | 守什麼                                           | 為什麼受益者是拉 v1 的團隊                               |
| --------------------- | ------------------------------------------------ | -------------------------------------------------------- |
| `tools/conformance`   | 切片契約、分層邊界、相對路徑逃逸、幽靈依賴       | 多團隊並行時邊界被越過，痛的是他們                       |
| `tools/api-surface`   | `platform/*` 的型別形狀，改名或改形狀即失敗      | 平台一改就打斷每一個切片，這道閘門是他們的保險           |
| `tools/vue-typecheck` | `.vue` 的型別（`vp check` 的 tsgolint 不看 SFC） | 他們寫的就是 `.vue`                                      |
| `tools/theme-verify`  | 真的建置兩次，證明各案換得掉配色與形狀           | 「換配色」是他們每個案子的第一件事                       |
| `tools/slice-gen`     | 產生器與檢查器**讀同一份契約**                   | 他們每開一片新功能就用一次                               |
| `tools/codemods`      | 破壞性變更的遷移腳本，由提出者跑完全 repo        | 升級平台時不必逐處手改；`api-surface` 擋的就是沒附的那些 |
| `tools/doc-facts`     | `README`／`HANDOFF` 裡的數字 vs. repo 的事實來源 | 那兩份文件是交付物，他們照著做事                         |
| `tools/scope-check`   | **這份文件**與版控內容一致                       | 它紅了，擋下的是「v1 悄悄多了一個他們沒預期的東西」      |

⚠️ **`doc-facts` 不對應五條承諾中的任何一條** —— 它守的是那五條**寫下來的樣子
可不可信**。硬把它塞進某一條承諾表，會重演 C70 那個錯（同一份證據掛在兩條
承諾上，而空著的那一條沒有人發現）。它在這裡有自己一列，理由寫在同一格。

⚠️ **`scope-check` 守的是這份文件自己**，所以它必須登記自己 —— 這不是文字遊戲：
「守範疇的機制本身算不算範疇內」在 C72 有答案（**算，因為它守的是交付物有哪些**），
而它上線後的第一個紅燈，就是它自己還沒被寫進這張表。

---

## `platform/` —— 准許存在的

`platform/` 整層都是交付物本體，不是守門的工具，所以逐一寫受益者沒有意義 ——
**團隊寫的每一行程式碼都在用它。** 准許存在的是這幾個：

| 路徑                        | 是什麼                                                     |
| --------------------------- | ---------------------------------------------------------- |
| `platform/slice-kit`        | `defineFeature()` 切片契約，命名空間在執行期驗到底         |
| `platform/http-client`      | 唯一合法的 HTTP 出口；**沒有、也不會有 token 存取介面**    |
| `platform/config`           | 環境設定；`VITE_*` 白名單，像機密的變數讓建置失敗          |
| `platform/security-headers` | CSP 以**資料**定義，由 BFF／dev 中介層／測試三方共用       |
| `platform/bff-contract`     | 中間層必須做到什麼 ＋ 怎麼證明做到了                       |
| `platform/bff-mock`         | 契約的參考實作；**不是認證伺服器**                         |
| `platform/ui`               | 共用 UI 元件層。代幣兩層、具名槽、slot                     |
| `platform/pii`              | 個資欄位的標註與遮罩基礎設施（**在 v1 是慣例，不是機制**） |
| `platform/tsconfig`         | 共用 TypeScript 設定                                       |
| `platform/eslint-config`    | Tier 2 安全閘門的 ESLint 設定（與 oxlint 零重疊）          |

---

## 刻意在外的

**`tools/gate-roster`** —— 守的是 CI 閘門清單的內部一致性（`package.json`、
兩個 workflow、README 那張表）。它解決的問題是真的，但**漂移傷的是維護者，
不是拉 v1 的團隊** —— 填不出上面那一欄。`v1.0.2` 曾短暫帶著它，`v1.0.3` 移除，
它活在 `main`。這是這條判準第一次真的擋下東西，經過見 C72。

其餘不在 v1 的**能力**（以及哪一種案子會需要），見
HANDOFF〈v1.0.0 **不**涵蓋什麼〉—— 那是另一條軸，這裡不重抄。

---

## 這份文件**不**涵蓋什麼

- **`apps/` 與 `features/` 底下的東西。** 那些是示範切片，各案會整個換掉，
  不是治理對象。
- **根層的設定檔與文件。**
- **`.semgrep/`。** 承諾五的自寫 SAST 汙點傳遞規則住在那裡，由
  `.github/workflows/tier2-security.yml` 用釘住 digest 的容器跑。
  它在 v1 內，只是不在這兩個目錄底下。

---

## 這份文件由 `tools/scope-check` 守著（`v1.0.5` 起）

```bash
./node_modules/.bin/vpr scope-check
```

**兩個方向都會紅：**

| 情況                 | 為什麼要紅                                                                      |
| -------------------- | ------------------------------------------------------------------------------- |
| 版控裡有、上面沒列   | 範疇裡悄悄多了東西，而判準要求寫得出「受益者是拉 v1 的團隊」。沒寫就是沒判斷過  |
| 上面列了、版控裡沒有 | **清單在說謊** —— README 的目錄樹曾經列著 `tools/sast/`，而那個目錄從來沒存在過 |

它比對的是 **`git ls-files`**，不是磁碟上的目錄。理由見 `tools/scope-check/src/tree.ts`：
切分支會留下含 `node_modules` 殘骸的空目錄，用磁碟當事實來源的檢查**會在開發機紅、
在 CI 綠**。找不到 git 時它直接失敗，**刻意不退回去掃磁碟** —— 一道「找不到就換個
寬鬆判準」的閘門，會在最需要它的環境裡安靜地變成另一件事。

⚠️ **這份文件刻意一個導出的數字都沒有**（全部列名字，不寫「幾支」）。
`doc-facts` 的 `GUARDED` 只有 `README` 與 `HANDOFF` 兩份，加第三份的那一刻，
四個地方寫著「只守那兩份」的句子會同時變成假的 —— 所以這份文件不進 `GUARDED`，
而是寫成「就算沒有守衛也不會腐爛成假數字」的形狀。

⚠️ **它守的是清單，不是內容。** 第三欄那句「受益者是拉 v1 的團隊」有沒有寫、
寫得對不對，機器讀不出來 —— 那一格仍然只有人能判斷。這道閘門保證的是
**沒有人可以跳過那一格**。

### 先文件、後檢查的順序是刻意的

`v1.0.4` 先用人工把判準跑一遍，`v1.0.5` 才寫檢查。反過來做，補文件的動機會變成
「讓閘門轉綠」而不是「把判準真的跑一遍」—— 而人工那一遍掉出了兩件事：
`doc-facts` 的歸屬問題（見上面那格），以及 README 目錄樹裡那個從來不存在的
`tools/sast/`。
