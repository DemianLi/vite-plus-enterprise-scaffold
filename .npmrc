# ─────────────────────────────────────────────────────────────────────────────
# D1 / D6 — 供應鏈設定
# ─────────────────────────────────────────────────────────────────────────────

# R3：內部 registry。部署到受管制環境前必須解除註解並填入實際位址。
#
# ⚠️ **只設這一行是不夠的，而且不夠得很難察覺。** 實測（見 DECISIONS.md 的 C29）：
# 本檔的 registry= 只涵蓋「專案相依」；`vp` 為了啟動自己而下載 pnpm 的那一步
# **完全不看它**，照樣連 registry.npmjs.org。症狀是團隊設完之後所有東西看起來
# 都走內部 mirror，直到真的斷網那天才發現第一步就往外連。
#
# 要涵蓋套件管理器下載，必須改設在**映像檔／機器層級**（兩者實測皆有效）：
#     ~/.npmrc 的 registry=            或
#     npm_config_registry 環境變數（CI 用這個）
#
# 鏡像範圍不是「vite-plus 的 8 個平台包」—— 實際是 467 個套件、其中 121 個
# 平台限定的原生二進位、分屬 11 個家族。完整清單見：
#     vpr mirror-manifest      # 含 sha512，可直接餵給 mirror 工具
#     vpr airgap               # 封閉網路的前置條件與驗收方式
#
# registry=https://nexus.example.internal/repository/npm-group/

# 供應鏈：CI 必須以 lockfile 為準，不得靜默更新。
prefer-frozen-lockfile=true

# 保 SBOM 不失真：嚴格 node_modules，不產生 phantom dependency。
# （這是 D6 選 pnpm 而非 npm 的主要技術理由之一，勿改。）
node-linker=isolated
hoist=false

# build script 的允許清單在 pnpm-workspace.yaml 的 allowBuilds
#（pnpm 11 的欄位名。npm 生態多數文件仍寫 onlyBuiltDependencies，那是 pnpm 10 的寫法，
#  在 11 上會被靜默忽略 —— 也就是允許清單失效，見 DECISIONS.md 的 C4）。
# 此處刻意不設 ignore-scripts=true —— 那會連允許清單一併封鎖，使機制失效。

# R5：vp 會自動下載 pnpm。
#
# 原本這裡寫「須改 onFail 或讓內部 mirror 供應 pnpm」。前半句實測是**錯的**：
# onFail 設成 "error" 或 "ignore"，vp 照樣把 pnpm 抓下來並且 exit 0 ——
# 它不是「要不要下載」的開關。後半句要成立，前提是上面那段講的 registry
# 設在機器層級，否則 mirror 供應了也不會被用到。
#
# 這 19 個套件（pnpm、@pnpm/exe 與各平台變體、@reflink）住在 pnpm-lock.yaml 的
# **第一份 YAML 文件**裡，不在專案的相依樹中 —— 這是鏡像最常漏掉的一批。
# 漏掉的症狀不是某個套件裝不起來，是 vp 連跑都跑不起來。清單見 `vpr airgap`。
