# Vite+ 企業級腳手架 — 決策紀錄（第二卷）

> **第一卷是 [DECISIONS.md](DECISIONS.md)，C1–C138 全部在那裡，凍結於 2026-08-29。**
> 為什麼分卷、以及為什麼是「凍結」而不是「搬遷」，見 **第一卷的 C138**。
>
> ⚠️ **編號連續，不重新開始。** 下一則是 **C139**。D 與 R 兩串同理
> （第一卷凍結時是 D1–D16、R1–R9）。分的是檔案，不是編號 ——
> 一個 C-編號在整棵樹上仍然只指一件事。
>
> ⚠️ **引用寫法一個字不變**：仍然寫「C138」「D16」「R7」，不寫在哪一卷。
> 讀的人要找哪一卷，看編號落在哪一段。**這正是不改編號的理由** ——
> 樹上約 2,700 處交叉引用因此全部繼續成立。

## 這一卷收什麼

與第一卷同一件事：**有日期的決策日誌** —— 這個腳手架一路上做過的判斷與踩過的坑，
含原文與「不要照這裡做」標註。體例沿用第一卷：

- 一則一個 `### C<n> — 標題（日期，票號）` 標題，底下用 `#### 一、…` 分節。
- **數字一律標基準 commit**（C137 §五）。這一卷與第一卷一樣**沒有機制在守**
  裡面的數字 —— `tools/doc-facts` 刻意不守有日期的日誌，理由在
  `tools/doc-facts/src/cli.ts:25`：**守它等於要求回頭改寫歷史。**
- **推翻一則 C-條時，要寫明它產出過什麼、重跑了沒有**（C136 §八）。
  ⚠️ 這條規則沒有任何東西在守，只靠人讀。
- **要註記第一卷的某一則，寫在這裡**，第一卷最多加一行指標（C138 §四）。

## 條目

### C139 — 偵測器對同一個物件會隨時間翻面，而那個物件的本質從未改變（2026-08-29，#91）

⚠️ **量測基準 `313fe96`**（C138 合併的那一支）。本則所有數字凍結在那一天。

#### 一、這一則是本卷第一則，而它的題材剛好是本卷自己

`#91` 問「新長出的平行體系偵測得到嗎，還是只能靠人判斷」，並列了三個候選的
**結構代理**：內部出現連號的條目、被其他檔案大量交叉引用、自己宣稱是唯一事實來源。

C138 造了 `DECISIONS-2.md`（就是這個檔）。**它是同一套編號的續卷，不是平行體系** ——
所以它是那三個代理的一個現成測試案例。

#### 二、實測：今天兩個代理一個都不命中

| 代理                   | 今天（`313fe96` 實測）                         | 幾則之後（**推導，非量測**） |
| ---------------------- | ---------------------------------------------- | ---------------------------- |
| 內部出現連號的條目     | ❌ **0 則**（`### C\d+` 標題數為 0）           | ✅ 會命中                    |
| 被其他檔案大量交叉引用 | ❌ **2 個檔**（`SCOPE.md`、`DECISIONS.md`）    | ✅ 會命中                    |
| 自稱唯一事實來源       | ❌ 沒有 —— 開頭就寫「第一卷是 `DECISIONS.md`」 | ❌ 不會                      |
| **它實際上是什麼**     | **續卷**                                       | **續卷 —— 沒變**             |

⚠️ **右欄是從 `#91` 列的代理推導出來的，不是跑出來的 —— 目前沒有任何偵測器存在。**
這一格標明白，是因為這棵樹記過同一種錯：**判準套在東西的「描述」上，
而不是它的「實質」上**（C137 §四，一輪三次）。

#### 三、裁決：那兩個代理單獨用不夠 —— 而理由比「會誤判」更尖

**裁決：`#91` 的前兩個結構代理，單獨使用不足以判定平行體系。**

理由不是「它們會誤判」，是：

> **同一個物件，本質從未改變，而偵測器的答案會隨時間翻面。**

⚠️ **今天跑一個偵測器，會對 `DECISIONS-2.md` 回綠燈，而那個綠燈什麼都不代表** ——
它綠是因為這個檔還太新，不是因為它不是平行體系。**一個會自己變成對的答案，
與一個判斷過的答案，長得一模一樣。**

這正是 `#91` 自己寫下的那個危險，現在有了樹上的實例：

> 一個分不出好壞的偵測器比沒有偵測器更糟 —— 它會給出綠燈，而綠燈會被當成保證。

⚠️ 以前它靠 `release/v1` 的 C89 作類比（缺陷與非缺陷會紅同一組測試）；
**現在它有一個同一棵樹上、這個月發生的、而且答案會翻面的案例。**

#### 四、本則**不**裁的事

⚠️⚠️ **本則不裁「自動偵測不可能」。** 一個反例不證明不可能，而且 `#91` 還開著 ——
從一則 C-條裡替另一張票下結論，是把裁決藏在別人看不到的地方。

還缺的是「**有沒有一組代理合起來分得出來**」。C138 §五 有一句可以當起點的話：

> 平行體系與世代交替的差別，在於邊界有沒有日期、以及兩半是不是都活著。

⚠️ **而這句話裡機器讀得到的只有半句。** 「有沒有日期」勉強是結構的
（`DECISIONS.md` 的凍結告示寫著日期）；「**兩半是不是都活著**」不是 ——
它要判斷的是**有沒有人還在往舊的那一半寫**，那是意圖，不是形狀。

#### 五、處置：一條驗收條件，不設閘門

**`#91` 提出的任何偵測器，上線前要對 `DECISIONS-2.md` 跑兩次** ——
一次在它條目還少的時候，一次在它長起來之後。**兩次都必須判它不是平行體系。**

⚠️ **這是給那張票的驗收條件，不是一道閘門。** 與 C129、C136 §八 同一條理由：
明知不可靜態驗證的東西，寫成規則、寫明它敞著，不要假裝有機器在守。
⚠️ 而它與 C137 §一 不衝突：本則**沒有**增加或刪除任何閘門，也不拿任何比值論證。

⚠️ **這條驗收條件本身沒有東西在守**，包括「有沒有人真的跑第二次」。
到期條件也說不出口 —— 「等這個檔長起來」不是一個機器判得出的時點。
**寫下來是為了讓下一個人知道它敞著**（沿用 C109 §七、C135 §七、C136 §八）。

#### 六、起草時自己犯了一次，而它就是 §二 右欄的那個錯

⚠️ 這個反例第一次寫在 `#91` 上時，兩個代理都標了 **✅ 命中** ——
**把「幾則之後」寫成了「今天」**，而當時零量測。實測才發現是 0 則與 2 個檔。
那則留言已更正，原文以刪除線保留。

**更正之後這個反例更強，不是更弱**：它從「代理會誤判」升級成「答案會隨時間翻面」。
⚠️ 而值得記的是**錯誤的形狀**：我拿一個**推導出來的穩態**當成**量到的現況**寫下去。
那與 C137 §四 那三次是同一個病 —— 只是這一次錯在時間軸上，不是容器上。

### C140 — 三串三種體例、兩卷一個分界點，而入門序列的選法不能用引用數（2026-08-29，#189）

⚠️ **量測基準 `1ea808e`。** 本則所有數字凍結在那一天。

#### 一、`#189` 說的是「加一份路標，**不是**加第 161 則」，而這是一則

票面那句話針對的是**埋起來**：一份路標寫進 13,000 行的日誌中間，
讀者永遠走不到它。**那個顧慮成不成立，不看這一卷多長，看讀者從哪裡進來。**

⚠️ **而讀者不從這一卷進來。** 44 個檔提到 `DECISIONS.md`，包括 `README.md`、
`AGENTS.md`、`SCOPE.md`。**一份寫在第二卷、而第一卷不指過來的路標，
就是第 161 則** —— 第二卷再短都一樣。

**所以本則成立的條件是第一卷加一行指標**，而那一行**同時是它的上限**：
C138 §四 寫著「要註記 C1–C138 的某一則，寫在新一卷裡，**舊卷最多加一行指標**」。
在第一卷寫一段解釋會違反本卷前兩則剛立的規矩，而且是寫在被 C138 凍結的那個檔裡。

⚠️ **那一行沒有任何機制在守。** `doc-facts` 的 `GUARDED` 是
`README.md`／`HANDOFF.md`／`UI-SURVEY.md`，`DECISIONS.md` 刻意在外
（`tools/doc-facts/src/cli.ts:25`）。它爛掉的時候不會有東西說話。

#### 二、三串編號，三種體例 —— 這是路標的核心

| 系列   | 體例                                              | 洞                                     |
| ------ | ------------------------------------------------- | -------------------------------------- |
| `C<n>` | 每則一個 `### ` 標題                              | **C51 從未使用**                       |
| `D<n>` | 每則一個 `### ` 標題                              | **D14 佔兩個標題**（另有〈D14 下半〉） |
| `R<n>` | ⚠️ **一張九列的登記表**，只有需處置的七條另開專節 | **沒有洞，R1–R9 全在**                 |

⚠️⚠️ **`R` 那一列是這份路標存在的理由。** 「R7／R9 從未定義」這個結論
**已經獨立發生三次**（`#189` 票面有那張表），而三次的根源完全相同：
**拿 C／D 的體例（`### <編號>` 標題）當量尺去量 R。**
R7／R9 已解除、無需處置敘述，所以沒有專節 —— **那是體例，不是遺漏。**

⚠️ 數 R 要用 `grep -cE '^\| R[0-9] +\|'` → 9。寫成 `'^| R[0-9]'` 會回 14，
因為〈調查與處置〉還有一張 `| R2「…」` 開頭的對照表。

#### 三、哪一段編號在哪一卷 —— 寫成**分界點**，不是清單

> **C138 為界。C1–C138 在 `DECISIONS.md`，C139 起在 `DECISIONS-2.md`。**
> **D 與 R 兩串整串留在第一卷**（凍結時是 D1–D16、R1–R9）。

⚠️ **刻意不寫成「第二卷有 C139、C140」那種清單** —— 每加一則它就過期一次，
而它自己就會變成下一個過期的路標。**分界點只在分卷的時候才需要動。**

⚠️ **編號連續、引用寫法一個字不變**：寫「C138」不寫在哪一卷，
讀的人看編號落在分界點的哪一邊。那正是不改編號、也不改第一卷檔名的理由 ——
樹上約 2,699 處交叉引用因此全部繼續成立（C138 §三）。

⚠️ **而「`C<n>` 一定在 `DECISIONS.md` 裡」今天是假的。** 實測：
`grep '### C139' DECISIONS.md` 回零，而 C139 存在。
grep 編號、寫檢查、量引用**都要掃兩卷**。

#### 四、入門序列 —— 而量下去發現引用數是個壞代理

先講選法，因為**清單會過期，選法不會**。

##### 引用數量過了，它不能當主判準

實測各編號被引用次數（扣掉定義處，排除 `pnpm-lock.yaml`）：

| 高引用 | 次數   | 低引用但重複發生 | 次數  |
| ------ | ------ | ---------------- | ----- |
| `C41`  | **81** | `C105`           | **4** |
| `C136` | 69     | `C102`           | 7     |
| `C114` | 54     | `C107`           | 10    |
| `C72`  | 50     | `C122`           | 12    |

⚠️⚠️ **`C105` 記的是這棵樹重複最多次的失敗形狀**（判準套在東西的「描述」上
而不是「實質」上 —— 它自己那一輪犯三次，C106 第四次，C137 §四 又一次），
**而它只被引用 4 次。** 引用數量的是「後來的人回頭指它幾次」，
不是「同一個錯又發生了幾次」——**兩者不相關。**

##### 判準：人判為主，引用數只用來抓漏

- **主判準（人判）**：這一則記的是不是**一個重複發生過的失敗形狀**，
  而不是一次性的選型或實作細節。
- **輔判準（可量）**：引用數 —— 只拿來問「**高引用而我沒選的，是不是漏了**」。
- **結構必讀**：不看以上兩軸 —— 沒有它就走不動的那幾則。

##### 名單（11 則）

| 則       | 為什麼在名單上                                                                |
| -------- | ----------------------------------------------------------------------------- |
| **D16**  | 兩軸判準（交付／迭代）：一個東西留不留。每一次「要不要加／砍」都回到它        |
| **C72**  | 範疇判準：**看它守的東西給誰看**。⚠️ 問法還活著，**後果已被 C134／C136 收回** |
| **C93**  | **「沒有被治理」不等於「准許進入」** —— 沉默是舉證責任沒履行，不是預設通過    |
| **C136** | **判準的效力依附在它的後果上，而後果是環境給的**（同一件事已應驗三次）        |
| **C105** | **判準套在東西的「描述」上，不是「實質」上** —— 這棵樹重複最多次的錯          |
| **C41**  | 抄本漂移：產生器不知道 `D15` 存在，**而沒有任何一道閘門會說話**               |
| **C33**  | 假的綠的原型：SBOM 產出了、上傳了、全綠，**而裡面有 0 個 component**          |
| **C114** | 差一點交付一個**永遠不會執行**的檔案 —— 空清單與全綠長得一模一樣              |
| **C126** | **一條只守四支的絆線，與沒有絆線長得一樣**                                    |
| **C133** | 併線：**兩半從來沒見過面**，而天真的合併零衝突刪掉 71 個檔                    |
| **C139** | **答案會隨時間翻面，而對象沒變** —— 本卷第一則                                |

⚠️ **`D15`（引用 100，全樹最高）刻意不在名單上**：它是選型決策，
讀它學不到失敗形狀。**輔判準抓到了它，主判準把它擋掉了** —— 這正是兩軸分工的樣子。

⚠️ **C138 也不在名單上**，因為它已經寫在第一卷開頭的告示裡，讀者一定會撞到。

#### 五、這份路標沒有機制在守，而它會過期

⚠️ **三處會腐爛，而且沒有東西會說話**：

1. **第一卷那一行指標** —— `doc-facts` 刻意不守 `DECISIONS.md`。
2. **§三 的分界點** —— 下次分卷時要改，而沒有東西知道分卷發生了。
3. **§四 的名單** —— 新的失敗形狀出現時它不會自己長。

**處置：沒有機制，只有把選法寫下來**（§四）。名單會過期，
**而一個寫明選法的名單，下一個人可以自己重算一遍** —— 這是這一則
唯一真正交付的東西。與 C129、C136 §八、C139 §五 同一條理由：
明知不可靜態驗證，**寫明它敞著，不要假裝有機器在守**。

---

### C141 — 決策日誌的編號指得到，靠寬掃 ＋ 一個具名豁免（2026-08-30，#199）

#### 一、為什麼一份**刻意不被守**的日誌，可以被守這一件事

`doc-facts` 的 `GUARDED` 只有三份文件，而決策日誌**刻意在外**
（`tools/doc-facts/src/cli.ts:25`）：那是有日期的日誌，「C24 當時是 467 個套件」
陳述的是歷史，守它等於要求回頭改寫歷史。

**指不到的編號不是同一件事，而這個區分不是新發明的。** C99 為了同一個問題
逐個 commit 查過 50 個版本，得出的那句話一字不改地適用：

> 它們**不是**過期 —— `R3`／`R5`／`#25` 一次都沒有出現過。
> 也就是說：它們**從寫下的那一刻就是死的**。

條目從不被刪除，所以一則編號要嘛存在、要嘛從來沒有存在過。**沒有「當時是對的」
這種狀態** —— 而那正是 `GUARDED` 那條界線的判準。所以這一條的射程是決策日誌，
而 `GUARDED` 一格都沒動（`#199` 明文要求）。

⚠️ 失敗方向與 C99 同一個：**讀的人不會知道那一則不存在，只會以為自己漏讀了。**

#### 二、四個坑，三個寫進了規則，而第四個是量出來的

`#199` 開票時就帶著四個已知的坑。逐個處置：

| 坑                         | 處置                                                             | 守它的東西                    |
| -------------------------- | ---------------------------------------------------------------- | ----------------------------- |
| ① `R` 是登記表不是 `###`   | 定義集分兩種抽法：C／D 用 `^### `，**R 用 `^\| R<n> \|` 的表列** | 夾具斷言：R7／R9 必須在集合裡 |
| ② 要掃兩卷                 | **不掃「兩卷」，掃 `DECISIONS(-<n>)?.md` 這個檔名體例**          | 夾具斷言：卷數與檔名          |
| ③ 分界點是散文、沒有東西守 | **拆掉對它的依賴** —— 檢查器從頭到尾不知道 `C138` 是分界點       | 不需要（依賴不存在了）        |
| ④ 死鏈 4 處而全是對的      | 具名豁免 `ABSENT = {C51}`                                        | 夾具斷言：`C51` 仍未被使用    |

⚠️ **③ 是「拆掉」不是「解掉」。** C140 §五.2 記的那件事沒有被修好 ——
`DECISIONS-2.md` 開頭那句散文的分界點照樣會腐爛，照樣沒有東西在守它。
變的只有一件：**這支檢查器不再靠它**。

⚠️ **④ 那個豁免不是白名單的開頭，也不是一顆可以轉鬆的旋鈕。** ⚠️ 表上那個 4
是**基準 `d4c4925` 的數**（本則寫下之前，理由見 §八）；那 4 處全部是
「C51 從未使用」**那句話本身**
—— 記錄那個洞存在的句子，被守它的規則判成缺陷。豁免因此掛了一條夾具斷言
（C97 §三之二 那條教訓）：`C51` 哪天真的成為一則條目，豁免就從「陳述一個空號」
變成「放行一個真的引用」，而那會安靜地發生 —— 測試會紅並要求把它拿掉。

#### 三、兩種抽法都量過，選了寬的那個 —— 而窄的那個守備範圍剩 5.7%

C99 對「提到編號」與「叫人去看編號」的區分，用的代理是**指路動詞**
（見／參照／詳見／參見）。它可以直接搬過來，而且搬過來的偽陽性是零。量下去：

| 抽法                    | 掃到的引用      | 死鏈             | 代價                                 |
| ----------------------- | --------------- | ---------------- | ------------------------------------ |
| **寬掃**（任何 `C<n>`） | **2,975**       | 4（全是 `C51`）¹ | 需要一個具名豁免                     |
| 指路動詞（C99 的代理）  | 171（**5.7%**） | 0                | 94.3% 的引用不受守，而且代理繞得過去 |

¹ 兩欄都是基準 `d4c4925` 的數，而右欄那個 4 已被本則自己作廢（§八）。

**裁決：寬掃 ＋ 具名豁免。** 兩個理由：

1. **偽陽性有界而且已經數完了** —— 4 處、一個編號、一句話。豁免是一個**具體事實**
   （C51 從未使用），不是一條可以續加的規則。
2. **窄的那個代理，C99 自己的檔頭寫著它繞得過去**：「有人寫『HANDOFF 的 R7 有詳細
   說明』就繞過去了」。用一個承認有縫的代理，換掉 94.3% 的守備範圍，換不過來。

⚠️ **`\b` 與三位數上界不是樣式潔癖，是量出來的。** 拿掉任一個，`pnpm-lock.yaml`
的 sha512 會從 1 個命中灌到 145 個（實測）。所以那個檔**不需要排除** ——
`\b` 在 base64 區塊裡結構上就不成立。

#### 四、射程：`reports/` 在外，而**理由比看起來的弱**

唯一站得住的理由是 `reports/research/rigs/README.md:6` 那句**沒有修飾語**的
「這個目錄沒有任何機制在守」—— 掃它就讓那句話變成假的，而 `#199` 明文不動別人的
射程宣告。代價數得出來：**3 個檔、46 個引用不受守**（今天全部指得到）。

⚠️⚠️ **「那是凍結的研究稿，算歷史」不能拿來當理由，雖然它聽起來比上面那個強。**
那個論證對決策日誌本身一模一樣成立 —— 而 §一 整節正是在說它不成立。
起草時先寫了這個理由，發現它會**同時**論證掉本則自己，才換掉。
寫下來，是因為一個聽起來更強的錯理由，比一個承認自己弱的對理由危險。

**要把 `reports/` 收進來：改一個常數 ＋ 改那句 README，兩件事一起做。**

⚠️ **而 `ABSENT` 不是同一種東西 —— 它不得比照辦理。** 射程是設計選擇，豁免是
事實登記：放寬 `ABSENT` 就是改閘門來換綠燈（`AGENTS.md` 規則二）。所以那條規則
紅掉時的訊息只給一條路（**改那個引用**），並明寫「要再加一個空號，停下來告訴人」。

#### 五、D16 兩軸各報一格（`#187` 的填寫要求）

| 軸       | 這一支                                                          |
| -------- | --------------------------------------------------------------- |
| **交付** | **零。** 採購／資安／法務沒有一個收件人在意一則編號指不指得到。 |
| **迭代** | **有分**，三個具體的觸發寫在下面。                              |

迭代軸那一格，照 D16 自己給的具體程度（「vitest 4 移除 `--reporter=basic`，
於是失敗清單是空的 —— 而空清單和全綠長得一模一樣」）：

1. **有人寫下一則還沒存在的編號**（打錯字，或引用一則打算寫而沒寫的條目）——
   這是它每天在守的那一件。
2. **有人把 `R` 的抽法「簡化」成 `### R<n>`** —— 那是最直覺的寫法，而這個誤讀
   **已經獨立發生三次**（#189）。R7／R9 會掉出定義集，指向它們的引用全部變紅，
   **而紅燈的訊息會說「這個編號不存在」** —— 一個把體例誤讀報告成缺陷的紅燈。
3. **下一次分卷的檔名不合體例**（`DECISIONS-<n>.md`）—— 那一卷的條目整批不進
   定義集。⚠️ 反方向更危險：一個 `DECISIONS-DRAFT.md` 會**安靜地把定義集變寬**，
   而變寬的方向永遠不會有紅燈。夾具斷言擋的就是這一格。

⚠️ **必須寫下的一個誠實的地方：D16 迭代軸的原文是「上游變動時」，而這三個觸發
全部在內部。** `#187` 的措辭是「上游**或內部**發生什麼具體變動時」，所以這一格
填得出來 —— 但那是 `#187` 的措辭，不是 D16 的原文。**照 D16 逐字讀，這一支兩軸
皆零，該丟。** 本則不改 D16（`#187` 也明文不動它），把這個張力寫在這裡，
是因為下一支 meta 閘門會撞到同一格。

#### 六、它在 `ready` 不在 `gate` —— 這是一個性質，不是備註

做成**測試**不是 CLI（沿用 C99 的位置：`tools/doc-facts/tests/`）。後果具體：

- `vp run -r test` 會跑它 → `pnpm ready` 第 2 步 → CI。
- **`pnpm gate` 不會跑它。** 一個只改文件、加了死鏈的改動，`gate` 是綠的、
  `ready` 是紅的。

⚠️ 這與 C99 那條規則今天的處境完全相同，**而 `AGENTS.md` 要求的是 `vpr ready`
全綠，不是 `gate` 全綠**。記在這裡是因為「`gate` 是 `ready` 的子集」這件事
在這棵樹上已經被踩過兩次。

它也因此**不需要 `gate-roster` 的一列** —— 名冊登記的是 CLI，不是測試。

#### 七、本則不裁的事

⚠️ **不裁 C99 的射程要不要跟著改。** C99 守的是指向 `HANDOFF.md` 的引用，
它今天用指路動詞代理、只掃四份文件。本則量出了那個代理的守備範圍（5.7%），
但**沒有**回頭重判 C99 —— 那是另一張票的事，而在一則 C-條裡替它下結論，
就是 C139 §四 拒絕過的那個動作。

⚠️ **不裁 `GUARDED` 要不要加第四份。** `#199` 明文排除，`cli.ts:28` 的警告
（加第四份的那一刻，四個地方寫著「只守 README 與 HANDOFF」的句子同時變成假的）
原封不動。

#### 八、量測與驗證（基準 `d4c4925`，2026-08-30）

| 量的東西                               | 值                     |
| -------------------------------------- | ---------------------- |
| 掃描檔數（版控，排除 `reports/`）      | 379                    |
| 編號引用總數                           | 2,975                  |
| 死鏈                                   | **4，全部是 `C51`**    |
| C 定義集                               | 139 則（1–140，缺 51） |
| D／R 定義集                            | 16／9                  |
| 若用 `### R<n>` 抽 R                   | **7**（缺 R7／R9）     |
| `pnpm-lock.yaml` 的命中（有／無 `\b`） | 1／145                 |

⚠️⚠️ **上表那個 4 標著基準 commit，因為它已經被這一則自己作廢了。**
寫完本則之後，樹上提到 `C51` 的地方從 **4 處變成 18 處** —— 多出來的全部是
本則與那支測試在**談論這份豁免**。⚠️ 而這不是文字遊戲：哪天 `C51` 真的被使用、
要把豁免拿掉，**要處理的不是 4 處，是全部**，而夾具斷言只會告訴你「該拿掉了」，
不會告訴你有多少處要改。

⚠️ 起草時這個數字是寫死的「今天樹上 4 處」——**一個在講「引用要指得對」的條目，
自己寫了一個寫下當場就變假的數字**。這棵樹上同樣的形狀已經發生過
（C138 起草時的行數）。

⚠️ **差分探針跑過，而第一次跑出來的是假的紅燈。** 注入一句指向不存在 D 編號的
註解 → exit 1；但那個 1 來自 `npx` 的 `EBADDEVENGINES`，**不是測試失敗** ——
包在 shell script 裡的 `npx` 沒有被改寫。改成直接呼叫重跑，才拿到真的：
**那一條**紅、訊息正確、還原後全綠。

⚠️ **第二支探針驗的是「卷從哪裡來」，而它擋下的是 C73／C98 那個方向。** 起草時
`volumes()` 走 `readdirSync`，與同一個檔案上方三行引的規矩自相矛盾。實測：放一份
**未追蹤**的續卷、再從別處引用它裡面的編號 —— 改走 `git ls-files` 之後仍然紅
（正確：CI 的 checkout 沒有那個檔）。走 `readdirSync` 的話它會綠，
而**本機綠、CI 紅**正是 C73／C98 逐字寫下的失敗方向。

⚠️ 同一趟還看到一件事：**壓縮過的測試輸出會把死鏈清單省略成 `Array(1)`**。
紅燈本身看得見，**清單看不見** —— 要看是哪一條，得跑未壓縮的輸出。

### C142 — D16 迭代軸的「上游」是代理，判準是「壞法安靜」（2026-08-30，#187）

#### 一、選的是 A，而 A 的字面版本不能用

`#187` 的留言把 C141 §五 那個張力收成兩個選項問了人（`#199`／`#200` 之後）：

| 選項                                       | 代價                                                                                   |
| ------------------------------------------ | -------------------------------------------------------------------------------------- |
| **A（採用）** 明寫成「上游**或內部**」     | 實質動到 D16 的讀法，而 `#187` 說不動 D16                                              |
| B 維持逐字讀，承認 meta 閘門這一類答不出來 | 會把 `gate-roster`／`scope-check` 一起判成兩軸皆零 —— 而 `#187` 明講不主張砍掉任何一支 |

**人選 A。**

⚠️ **但字面照抄「上游或內部」會把這一格變成空的。** 那樣讀，
「有人改了它守的東西時它會紅」對**每一道**閘門都成立，一句話就能填滿 ——
而 `#187` 存在的理由正是那一句：**「它守得比較嚴」不是一個迭代軸的答案。**
A 的字面版本會親手做出這張票要擋的東西。

**所以本則做的是 A ＋ 一個限定條件，這比人選的多。** 寫在第一節是因為
它該被看見：人選的是「擴寫」，本則擴寫的同時收緊了。收緊的部分若判錯，
要推翻的是本則的 §二，不是那個選擇。

#### 二、裁決：迭代軸兩條，而「上游」是其中一條的常見來源

**D16 迭代軸讀作：有人為了別的目的做了一個變動，而「別的東西」因此壞了、
壞掉的樣子卻是綠的 —— 這個東西會不會把它變成紅的？**

要在這一格拿到分，兩條都要成立：

| 條件           | 內容                                         |
| -------------- | -------------------------------------------- |
| **① 對象在外** | 它報的是**別的東西**壞了，不是它自己被改壞了 |
| **② 壞法安靜** | 在**它不存在**的世界裡，那個壞法看起來是綠的 |

⚠️ **② 才是真正的門檻。** 填這一格的時候要說出的不是「違反規則時它會紅」，
而是**在它不存在的世界裡，那個壞法看起來是什麼樣子**。如果答案是
「看起來就是壞的、有人會發現」，這一格是零。

**這兩條不是新發明的，是從 D16 自己給的樣本讀出來的。** 逐項對照：

| D16 的樣本                       | 對應                                                |
| -------------------------------- | --------------------------------------------------- |
| vitest 4 移除 `--reporter=basic` | 做這件事的人不知道這棵樹存在 —— 「為了別的目的」    |
| 子行程啟動即失敗                 | 壞掉的是**測試有沒有真的跑**（對象在外）            |
| **空清單和全綠長得一模一樣**     | 壞法安靜 —— 而這一句是那個樣本被寫進 D16 的全部理由 |

**「上游」是條件 ①②同時成立最常見、也最容易查證的來源** —— 一個上游的變動
天生就不是為了對付你的閘門而做的。但它是**來源**，不是判準的內容；
判準的內容是「對象在外」加「壞法安靜」。

⚠️ 這棵樹踩過這個形狀：**把判準套在代理上，會給出漂亮而錯誤的答案**
（C140 §四 把引用數當「重要性」的代理，量得很乾淨而結論是錯的）。
「上游」對「壞法安靜」就是一個代理，而 meta 閘門這一整類正好落在代理的盲區裡。

#### 三、對照四支現存工具 —— 沒有一支掉出去（⚠️ 判讀，非量測）

**這一節的用途是擋 B 從後門進來。** 人選的是 A，而 `#187` 明文不主張砍掉任何
一支；一個收緊過的讀法如果順手把現存的工具判成該丟，那就是 B 換了個名字。
逐支問條件 ②（「它不在的話，壞法長什麼樣」）：

| 工具            | 對象在外                    | 它不在的話，那個壞法長什麼樣                                           |
| --------------- | --------------------------- | ---------------------------------------------------------------------- |
| `gate-roster`   | 四個消費端與名冊            | 一支閘門從某個消費端掉出去 —— **CI 照跑照綠，只是檢查得比較少**        |
| `scope-check`   | 樹與 `SCOPE.md`             | 頂層多一個目錄而沒登記，全綠；反方向（清單列了、樹上沒有）也沒人會發現 |
| `doc-facts`     | 文件裡的數字                | 數字與事實來源脫節，**文件照樣渲染**                                   |
| `promise-check` | `specs/` 的承諾有沒有被執行 | 一條承諾沒有被真的執行 —— **和全部通過長得一模一樣**                   |

**四支全部有分，而 `promise-check` 那一格是 D16 樣本的同構。**

⚠️ **這張表是判讀，不是量測** —— 沒有跑任何東西去驗證「它不在的話會綠」。
要驗，得逐支把它拿掉再跑一次；本則沒做。

**而在這一節，不量是可以接受的，理由要說出來**：表格右欄問的是**它不存在的話
會長什麼樣**，而每一列都只是把那支工具自己檔頭寫著的存在理由再讀一次
（四支的 `src/*.ts` 檔頭各自寫了它守什麼、刻意不守什麼）。**這是讀既有文件，
不是新的判斷。** ⚠️ 若哪天有人主張其中一列不成立，該做的是把那支拿掉跑一次，
**而不是換一把尺** —— D16 記過三次翻案，每次證據沒變、變的只有尺。

#### 四、這把尺對它的第一個受益者不利，而那是它不是為答案挑的證據

⚠️ **必須先說清楚立場**：本則的第一個受益者是 C141，寫在一天前，
而 C141 §五 自己寫著「照 D16 逐字讀，這一支兩軸皆零，該丟」。
**一個人寫了一道閘門，隔天寫一則裁決把判準改成讓它過關** ——
這正是 D16 §「為什麼需要一條明寫的判準」記下的病：
`exit-drill` 與 `ui-survey` 三次翻案，**證據沒變，變的只有尺**。

所以把不利的結果放在這裡：**照 §二 的兩條，C141 §五 報的三格只剩一格。**

| C141 §五 的觸發              | 條件 ① 對象在外 | 判                                                                   |
| ---------------------------- | --------------- | -------------------------------------------------------------------- |
| ① 有人寫下一則還沒存在的編號 | ✅ 日誌本身     | **有分** —— 而且壞法安靜（文件照樣渲染，讀的人以為是自己漏讀了一則） |
| ② 有人把 `R` 的抽法「簡化」  | ❌ 閘門自己     | **不算迭代軸的分**                                                   |
| ③ 下一次分卷的檔名不合體例   | ❌ 閘門自己     | **不算迭代軸的分**                                                   |

②③ 描述的是**這道閘門自己會怎麼被改壞**，而改它的人正在改的就是它 ——
兩條條件都不成立。它們是**自我防護**，那兩條夾具斷言該留著，
但它們填的不是 `#187` 那一格。

**C141 仍然有分（① 成立），仍然不該丟。** 而一把為了答案挑出來的尺，
不會在自己的第一個案例上砍掉三分之二。

⚠️ **依 C136 §八 交代產出**：本則**不推翻** C141 的結論，只判定它 §五 的三格
裡有兩格記錯了地方。那兩格的產出物是測試裡的夾具斷言，**它們仍然正確、
仍然該在**，本則改的是它們記在哪一格，不是它們該不該存在 ——
**沒有東西要重跑。**

#### 五、兩類東西分開寫 —— 這是 §二 的後果，不是一條新規定

§二 的兩條一旦成立，這件事就跟著成立，不需要另外的授權：**一道閘門守著自己
不被改壞的那些夾具斷言，兩條條件都不滿足**（對象是它自己、而改它的人正在改
的就是它），所以**它們填不了 `#187` 那一格**。

那些夾具**仍然要寫下來** —— 它們是這棵樹上最容易掉的東西 —— 只是寫在別處。

⚠️ 混寫的後果具體：一支兩軸皆零的工具，可以靠列出「我有三條夾具在守自己」
把迭代軸那一格填得滿滿的。**那是 §一 那個空格子的第二種寫法。**

⚠️ **這一節刻意寫成後果，不寫成「`#187` 新增一條填寫要求」。** 兩者的差別
不是措辭：後者是**替一張還開著的票加一條它沒裁過的規定**，而本則只被授權
處理 D16 迭代軸的讀法。要不要把它升格成票面上的一條，是 `#187` 的事
（同一個形狀見 C135 §一 —— 建議可以寫「新位置」，裁決不能）。

#### 六、D16 原文一個字不動，而舊卷那一行是 C138 §四 的第一次動用

兩個理由，第二個比第一個根本：

1. **C138 把第一卷凍結了**，D16 在第一卷。
2. 更根本的：D16 是**有日期的裁決**，改它等於回頭改寫歷史 ——
   `tools/doc-facts/src/cli.ts:25` 拒絕守 `DECISIONS.md` 用的就是這條理由。
   **這棵樹推翻舊裁決的做法是開新則**（C134／C136／C137 都是），本則照做。

C138 §四 把「C139 之後要註記舊則」的去處寫死了：**寫在新卷裡，舊卷最多加一行
指標。** 本則是那條規定的**第一次動用**，所以照字面只加**一行**——
`DECISIONS.md` 的 D16 兩軸表底下那一行。

⚠️ **代價寫清楚**：讀 D16 的人現在要多跳一次，而那一行是唯一的連結。
如果哪天有人把它刪掉，D16 會退回逐字讀，**而沒有任何東西會紅**。

#### 七、本則不裁的事

- **不動 D16 原文**，不動 C137，不動 `#187` 的票面措辭（票面本來就寫著
  「上游**或內部**」—— 本則是給那個措辭一個依據，不是改它）。
- **不重判任何現有工具，不增也不減任何一道閘門。** §三 那張表是拿來確認
  收緊沒有誤傷，不是拿來論證增減 —— C137 §一 明文禁止後者。
- **不預判 `#93`。** `#187` 建議把 `#93` 當這條要求的第一個案例；
  實際先撞到的是 C141，而那不改變 `#93` 自己要填。

#### 八、沒有機制在守，而唯一機械的那一點守錯了方向

D16 的讀法、`#187` 的填寫要求、本則的兩條條件 —— **全部靠人讀。**
本則不帶任何閘門，`gate-roster` 不加一列，`UNGATED` 也不加。

⚠️ 唯一機械的後果是反諷的：`C142` 這個**編號**現在被 C141 那道閘門守著 ——
指不到的話 `ready` 會紅。**而它守的是指得到，不是用得對。**
有人拿本則去論證增刪一道閘門（C137 §一 與 §七 都明文禁止），
不會有任何東西變紅。

### C143 — 正典分類進 `SCOPE.md` 根層表，而它不是新的一支閘門（2026-08-30，#93／#88／#187）

⚠️ **量測基準 `9659bb0`**（C137 §五）。本則所有數字凍結在那一個 commit。

#### 一、`#93` 問的四件事，答案先擺出來

| `#93` 要定的       | 本則的答案                                                               |
| ------------------ | ------------------------------------------------------------------------ |
| 閘門**斷言什麼**   | `SCOPE.md` 根層表的每一列，要指名四個桶之一（C135 §三 那四個）           |
| 它**住在哪**       | **不新造一支** —— 擴充 `tools/scope-check`，理由是擁有權（§五）          |
| 進不進 `gate` 鏈   | **已經在** —— `scope-check` 於 `#180`（`b745d6e`）進了 tier1，無需新決定 |
| **變異驗證**怎麼做 | §七 列了實作要交的四條                                                   |

#### 二、票面上兩處指向不成立，而其中一處是實測翻的

`#93` §六 寫著「新增一個 `tools/<name>` 會撞上 `scope-check` 的『樹上有、沒登記』」，
並由此推出「而 `main` 的範疇清單今天仍未定義（`#87` 動不了）」。**撞的對象錯了。**

實測（版控中的真 workspace 成員，未經管線、逐支取結束碼）：

| 探針                                           | `gate-roster`       | `scope-check` |
| ---------------------------------------------- | ------------------- | ------------- |
| 新增 `tools/probe-canon/`（含 `package.json`） | **1**（工具沒登記） | 0             |
| **對照組**：新增根層 `zz-root-probe/`          | —                   | **1**         |
| 兩者移除後                                     | 0                   | 0             |

C136／`#183` 之後 `scope-check` 的 `GOVERNED` 只有 `platform/` 與根層，
`tools/` 那一層交給了 `gate-roster`（`SCOPE.md` 那句「它一樣會擋你」是真的）。
**所以擋新工具的是登記程序，不是一份未定義的清單** —— `#87` 沒有擋著這件事。
`#86` 也已於 2026-08-26 關閉，「實作等解凍」那個條件不存在了。

⚠️ **起草時我自己先量出兩個假的綠，兩個都是夾具不具代表性：**

1. 第一支探針只有 `src/x.ts`、**沒有 `package.json`** —— 而 `gate-roster` 數的是
   workspace 成員，所以它根本不在母體裡。
2. 改好之後把它命名為 `zz-probe`，而 `tools/doc-facts/src/derive.ts:41` 的
   `TRANSIENT_PREFIX = "zz-"` **會刻意跳過**這個前綴的目錄。**又綠一次。**

換成中性名字才拿到那個 1。⚠️ 這是「絆線要掛在被守的對象上」的第三種形狀：
**夾具的名字本身可以讓它從母體裡消失。**

#### 三、被守的對象：C135 那 32 列不能當事實來源

C135 的表是**凍結卷裡有日期的快照**。守它有兩個各自獨立的擋路理由：
`tools/doc-facts/src/cli.ts:25`（守有日期的日誌等於要求回頭改寫歷史）、
以及 C138（第一卷凍結，本來就不能改）。

**而它已經過期了，三天。** 實測兩個 commit 的根層母體（`git ls-tree`／`git ls-files`）：

| 基準                          | 根層檔 | 頂層目錄 | 合計   |
| ----------------------------- | ------ | -------- | ------ |
| `1dc1227`（C135，2026-08-27） | 24     | 8        | **32** |
| `9659bb0`（今天）             | 25     | 9        | **34** |

多出來的兩列是 **`DECISIONS-2.md`**（C138 自己造的）與 **`reports/`**。
**沒有人把它們歸過類** —— 而 C135 那張表沒有任何機制會告訴你這件事。

所以四個選項連代價問了人（C138 §五 的形狀），**人選了「`SCOPE.md` 根層表加一欄」**。

⚠️ **最強的反對意見要正面回答：C135 §五 明文警告過不要繼承那張表的洞。**
那句話針對的是「**造第二張表、而它以 `SCOPE.md` 為準**」—— 繼承關係來自
「以它為準」。**本則不造第二張表**，那一欄長在同一張表上，母體仍然是
`git ls-files`（`scope-check` 兩個方向都在驗，§六的對照組就是它）。
沒有第二張表，就沒有可以被繼承的洞。

#### 四、裁決：第三欄「桶」，四選一，而機器只查填了沒有

`SCOPE.md` 的根層表今天是兩欄（名字／這是什麼）。**加第三欄，值域是 C135 §三
那四個桶**：正典／正交／過渡豁免／無關。

⚠️ **無關桶「不要求寫理由」與「不要求填桶名」是兩件事。** C135 §三 免掉的是
前者（替 `LICENSE` 寫受益者是儀式不是判斷），本則要求的是後者 ——
**那正是它成為一個有名字的逃生門、而不是一個沒人注意的逃生門的辦法。**

⚠️ **機器只查兩件事：填了沒有、填的是不是那四個之一。判斷仍然是人做的。**
這是刻意的，理由是 `#91` 自己寫的那句警告（C135 §五 引過）：
**「一個分不出好壞的偵測器比沒有偵測器更糟 —— 它會給出綠燈，而綠燈會被當成保證。」**
判「它承載哪一條軸」是語意判斷，正是 `#91` 剩下沒解的那一半。
**所以本則不卡 `#91`** —— 它把那一半留在人手上，沒有假裝機器答得了。

#### 五、不新造一支工具，而理由不是「省」

`#93` 的預設是產出一支新的 `tools/<name>`。**本則不那樣做**，理由是**擁有權**：
`SCOPE.md` 那張表已經有一個負責的閘門，兩支工具對同一份文件負責，
就製造了 C135 §五 定義的那個條件 —— **同一個問題有兩個答案，而沒有規則決定誰贏。**

⚠️ **這不是拿「過度設計」論證少加一支** —— C137 §一 明文禁止那種論證，
而本則沒有用任何比值、也沒有主張閘門太多。論的是一份文件一個守門人。

後果具體：不需要 `gate-roster` 的新一列、不需要 `UNGATED` 的新一列、
不需要新的 tier 決定，也不需要一份新的 README〈兩層檢查〉表列。

#### 六、D16 兩軸（`#187` 的填寫要求，照 C142 的讀法）

| 軸       | 這一條規則                                      |
| -------- | ----------------------------------------------- |
| **交付** | **零。** 沒有評審收件人在意根層表有沒有第三欄。 |
| **迭代** | **有分**，而 ② 是實測的。                       |

- **① 對象在外** ✅ 它報的是 `SCOPE.md` 的列 —— 人寫的文件，不是這道閘門自己。
- **② 壞法安靜** ✅ **實測**（逐支取結束碼，未經管線）：

| 步驟                                     | 結果                                              |
| ---------------------------------------- | ------------------------------------------------- |
| 新增根層 `NOTES.md`，**不**加列          | `scope-check` **1** —— 列是被逼出來的             |
| 加一列，「這是什麼」只寫**「專案筆記」** | `scope-check` 0、`doc-facts` 0、`gate-roster` 0   |
| 同一狀態跑 **`pnpm gate`**（14 段）      | **exit 0 —— 全綠**                                |
| 還原（列與檔案都移除）                   | 0                                                 |
| ⚠️ 中間態（列留著、檔案刪掉）            | `scope-check` **1** —— 那是「清單在說謊」那個方向 |

**所以今天樹上可以多一份承載專案紀錄的文件，而「它跟 `README`／`HANDOFF` 誰贏」
沒有任何人答過，全套閘門 exit 0。** 那正是 C135 §五 定義的平行體系，
**而它的壞法是全綠。**

⚠️ 這是 `#187` 那條填寫要求的**第一個正面案例**。C142 是第一個案例，
但它自己的迭代軸三格只剩一格（C142 §四）。

#### 七、實作要交什麼 —— 四條變異列

這個 repo 的規矩是每一條論證都要有變異列與紅的條數（`#93` 票面）。實作 PR 至少要交：

1. 某一列的桶欄**留空** → 紅。
2. 桶名**不在那四個裡**（例：`雜項`） → 紅。
3. 四個桶各至少一列、都填對 → 綠。
4. **把這條規則拿掉，第 1 條要變綠** —— 證明紅的是這條規則，不是別的東西。

⚠️ **第 4 條不是形式。** C94 記過同一支工具同一個地方的教訓：`SCOPE.md` 從
`v1.0.5` 起就宣稱「沒有人可以跳過那一格」，而實測把某一列後兩欄清空之後
**閘門全綠** —— 文件宣稱了一個保證，程式碼沒有交付它。

⚠️ **實作會動到 34 列的內容，而其中兩列是還沒有人做過的判斷**
（`DECISIONS-2.md`、`reports/`，見 §三）。C135 §二 明講這個判準機器判不了。
**那兩列的桶要由人判，實作 PR 要把它們單獨標出來**，不可以夾在 32 列的
照抄裡送出去 —— 照抄 C135 是有依據的（那是它做過的判斷），新判不是。

#### 八、本則不裁的事，以及沒有機制在守的

- **不裁 `docs/adr/`。** 它今天仍然沒有人裁過（C135 §一／C138 §五 都刻意留白）。
- **不動 C135**（凍結卷），本則是它的續集不是它的修正；C135 那張表仍然是
  2026-08-27 那天的正確快照。
- **不動 C137、不動 D16 原文、不增減任何一道閘門。**
- ⚠️ **`#93` 不關。** 本則是設計，實作是下一支 PR。

⚠️ **桶的判斷本身沒有機制在守** —— 機器查的是「填了沒有、是不是那四個之一」。
一列填了「正交」而它其實是正典，**不會有任何東西變紅**。這一條與
`AGENTS.md` 規則四（不得自己加 `@待辦`）是同一種：**靠人讀 diff。**

### C144 — 桶欄上線，而 34 列裡有兩列是這一支 PR 才判的（2026-08-30，#93）

⚠️ **量測基準 `b2e9798`**（C137 §五）。本則所有數字凍結在那一個 commit。

C143 是設計，本則是它的實作落地報告 —— 而它要單獨存在，是因為 C143 §七
要求「實作 PR 要把那兩列**單獨標出來**，不可以夾在 32 列的照抄裡送出去」。
**一份寫在 PR 內文裡的判斷不進版控**，下一個讀 `SCOPE.md` 的人看到的
只有兩個桶名。

#### 一、兩列新判 —— C135 §四 那張表沒有它們

| 名字             | 桶           | 依據                                                                                                                                                                                                 |
| ---------------- | ------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `DECISIONS-2.md` | **過渡豁免** | 與第一卷同桶。**終點未定**：C138 §三 只裁了「凍結不搬遷」、§五 只裁了「續卷放新的根層檔」，而 `#87`（搬去 ADR）仍然開著、`docs/adr/` 今天仍然沒有人裁過（C135 §二／C136 §十／C138 §五 三次刻意留白） |
| `reports/`       | **無關**     | 「既不是紀錄、也沒有自己的治理軸」兩半都成立 —— **`README.md` 與 `HANDOFF.md` 都 0 次提到它**（`doc-facts` 的入場判準兩半皆不成立），而 `SCOPE.md` 那一列自己寫著「沒有任何機制在守它們過不過期」    |

**`DECISIONS-2.md` 的到期條件**（比照 C135 §四 給第一卷的那一格）：
**`git ls-files DECISIONS-2.md` 為空。**
⚠️ **它沒有地方可以寫在表上** —— 桶欄只放桶名（C143 §四），所以它在這裡。
而它跟第一卷那一格一樣**是溫度計不是鬧鐘**（C135 §四）：只有人跑它的時候才被問到。

⚠️ **不選「正典」的理由要寫出來**：正典的意思是「**它本來就該在這裡**」，
而那比這棵樹今天撐得住的還強 —— 它會側面回答 `#87` 那個沒有人裁過的問題。
⚠️ **也不選「正交」**：C135 §六 已經裁過「有沒有閘門守」從來不是正交的判準，
正交是**不同的軸**，而決策日誌正是正典那條軸本身。

⚠️ **無關桶從 4 列變 5 列。** C135 §三 寫過「它小到可以合理質疑第四個桶值不值得」——
這一列讓那個質疑弱一點，但**不足以推翻**，記在這裡是為了讓下一個重數的人知道
數字動過。

#### 二、C143 §七 那四條變異列，逐條的實測

| §七                          | 落地形式                                                     | 實測                                             |
| ---------------------------- | ------------------------------------------------------------ | ------------------------------------------------ |
| 1. 桶欄留空 → 紅             | 兩種寫法各一條（格子是空的／整格不存在）                     | 各 1 條紅，**規則名唯一**                        |
| 2. 桶名不在四個裡 → 紅       | `雜項`                                                       | 1 條紅                                           |
| 3. 四個桶各一列、填對 → 綠   | 四列                                                         | 0                                                |
| 4. **拿掉規則，第 1 條變綠** | 把 `check.ts` 的桶那段用 `if (false && …)` 短路，只跑第 1 條 | **`expected [] to deeply equal ['沒有指名桶']`** |

第 4 條是真的跑過的，不是推論 —— 陣列**空的**，證明那條紅只由這條規則產生。

⚠️ **它能成立，靠的是一個設計決定**：桶那一格**被排除在 C94 的
「登記了、但那一格是空的」之外**（`check.ts` 的 `Layer.bucketColumn`）。
兩條規則都管同一格的話，把桶那條拿掉、空著的桶仍然會紅，
**第 4 條就紅零條** —— 而那正是 C94 記下的病（宣稱了一個保證、沒有交付），
只是這一次會發生在引用它的同一支 PR 裡。

⚠️ **排除不是放寬**：那一格從「非空」升級成「必須是那四個之一」，比 C94 嚴。
測試裡兩個方向各釘一條（桶空 → 不叫 C94 那條；「這是什麼」空 → 仍然叫）。

#### 三、絆線掛在真的那張表上，量過

變異列跑在假 repo 上，所以再對**真的 `SCOPE.md`** 量一次（C94 那次的教訓是
判準對得起夾具、對不起真樹）：清空 `TESTING.md` 的桶、把 `LICENSE` 改成
`雜項` → **2 條紅，兩條規則各 1**；還原 → **exit 0**。

#### 四、本則不裁的事

- **不裁 `#93` 關不關。** 票面的四件事都有答案了（C143 §一），但它的產出位置
  被 C143 §一 改過（`DECISIONS.md` 而非 `docs/adr/`），關不關是人判。
- **不裁 `docs/adr/`。** 第四次留白。
- **不動 C135**（凍結卷）。它那張表仍然是 2026-08-27 那天的正確快照，
  而**今天起有一份活的在 `SCOPE.md`**。
- ⚠️ **桶填得對不對仍然沒有機制在守**（C143 §八 一字不改）。本則自己就是例子：
  上面兩列的桶是人判的，而閘門對它們說的只有「填了、而且是那四個之一」。
