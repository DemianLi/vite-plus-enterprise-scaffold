# Vite+ 企業級腳手架 — 決策紀錄（第二卷）

> **第一卷是 [DECISIONS.md](DECISIONS.md)，C1–C138 全部在那裡，凍結於 2026-08-29。**
> 為什麼分卷、以及為什麼是「凍結」而不是「搬遷」，見 **第一卷的 C138**。
>
> ⚠️ **編號連續，不重新開始。** 下一則是 **C139**。D 與 R 兩串同理
> （第一卷凍結時是 D1–D16、R1–R9）。分的是檔案，不是編號 ——
> 一個 C-編號在整棵樹上仍然只指一件事。
>
> ⚠️ **引用寫法一個字不變**：仍然寫「C138」「D16」「R7」，不寫在哪一卷。
> 讀的人要找哪一卷，看編號落在哪一段。**這正是不改編號的理由** ——
> 樹上約 2,700 處交叉引用因此全部繼續成立。

## 這一卷收什麼

與第一卷同一件事：**有日期的決策日誌** —— 這個腳手架一路上做過的判斷與踩過的坑，
含原文與「不要照這裡做」標註。體例沿用第一卷：

- 一則一個 `### C<n> — 標題（日期，票號）` 標題，底下用 `#### 一、…` 分節。
- **數字一律標基準 commit**（C137 §五）。這一卷與第一卷一樣**沒有機制在守**
  裡面的數字 —— `tools/doc-facts` 刻意不守有日期的日誌，理由在
  `tools/doc-facts/src/cli.ts:25`：**守它等於要求回頭改寫歷史。**
- **推翻一則 C-條時，要寫明它產出過什麼、重跑了沒有**（C136 §八）。
  ⚠️ 這條規則沒有任何東西在守，只靠人讀。
- **要註記第一卷的某一則，寫在這裡**，第一卷最多加一行指標（C138 §四）。

## 條目

### C139 — 偵測器對同一個物件會隨時間翻面，而那個物件的本質從未改變（2026-08-29，#91）

⚠️ **量測基準 `313fe96`**（C138 合併的那一支）。本則所有數字凍結在那一天。

#### 一、這一則是本卷第一則，而它的題材剛好是本卷自己

`#91` 問「新長出的平行體系偵測得到嗎，還是只能靠人判斷」，並列了三個候選的
**結構代理**：內部出現連號的條目、被其他檔案大量交叉引用、自己宣稱是唯一事實來源。

C138 造了 `DECISIONS-2.md`（就是這個檔）。**它是同一套編號的續卷，不是平行體系** ——
所以它是那三個代理的一個現成測試案例。

#### 二、實測：今天兩個代理一個都不命中

| 代理                   | 今天（`313fe96` 實測）                         | 幾則之後（**推導，非量測**） |
| ---------------------- | ---------------------------------------------- | ---------------------------- |
| 內部出現連號的條目     | ❌ **0 則**（`### C\d+` 標題數為 0）           | ✅ 會命中                    |
| 被其他檔案大量交叉引用 | ❌ **2 個檔**（`SCOPE.md`、`DECISIONS.md`）    | ✅ 會命中                    |
| 自稱唯一事實來源       | ❌ 沒有 —— 開頭就寫「第一卷是 `DECISIONS.md`」 | ❌ 不會                      |
| **它實際上是什麼**     | **續卷**                                       | **續卷 —— 沒變**             |

⚠️ **右欄是從 `#91` 列的代理推導出來的，不是跑出來的 —— 目前沒有任何偵測器存在。**
這一格標明白，是因為這棵樹記過同一種錯：**判準套在東西的「描述」上，
而不是它的「實質」上**（C137 §四，一輪三次）。

#### 三、裁決：那兩個代理單獨用不夠 —— 而理由比「會誤判」更尖

**裁決：`#91` 的前兩個結構代理，單獨使用不足以判定平行體系。**

理由不是「它們會誤判」，是：

> **同一個物件，本質從未改變，而偵測器的答案會隨時間翻面。**

⚠️ **今天跑一個偵測器，會對 `DECISIONS-2.md` 回綠燈，而那個綠燈什麼都不代表** ——
它綠是因為這個檔還太新，不是因為它不是平行體系。**一個會自己變成對的答案，
與一個判斷過的答案，長得一模一樣。**

這正是 `#91` 自己寫下的那個危險，現在有了樹上的實例：

> 一個分不出好壞的偵測器比沒有偵測器更糟 —— 它會給出綠燈，而綠燈會被當成保證。

⚠️ 以前它靠 `release/v1` 的 C89 作類比（缺陷與非缺陷會紅同一組測試）；
**現在它有一個同一棵樹上、這個月發生的、而且答案會翻面的案例。**

#### 四、本則**不**裁的事

⚠️⚠️ **本則不裁「自動偵測不可能」。** 一個反例不證明不可能，而且 `#91` 還開著 ——
從一則 C-條裡替另一張票下結論，是把裁決藏在別人看不到的地方。

還缺的是「**有沒有一組代理合起來分得出來**」。C138 §五 有一句可以當起點的話：

> 平行體系與世代交替的差別，在於邊界有沒有日期、以及兩半是不是都活著。

⚠️ **而這句話裡機器讀得到的只有半句。** 「有沒有日期」勉強是結構的
（`DECISIONS.md` 的凍結告示寫著日期）；「**兩半是不是都活著**」不是 ——
它要判斷的是**有沒有人還在往舊的那一半寫**，那是意圖，不是形狀。

#### 五、處置：一條驗收條件，不設閘門

**`#91` 提出的任何偵測器，上線前要對 `DECISIONS-2.md` 跑兩次** ——
一次在它條目還少的時候，一次在它長起來之後。**兩次都必須判它不是平行體系。**

⚠️ **這是給那張票的驗收條件，不是一道閘門。** 與 C129、C136 §八 同一條理由：
明知不可靜態驗證的東西，寫成規則、寫明它敞著，不要假裝有機器在守。
⚠️ 而它與 C137 §一 不衝突：本則**沒有**增加或刪除任何閘門，也不拿任何比值論證。

⚠️ **這條驗收條件本身沒有東西在守**，包括「有沒有人真的跑第二次」。
到期條件也說不出口 —— 「等這個檔長起來」不是一個機器判得出的時點。
**寫下來是為了讓下一個人知道它敞著**（沿用 C109 §七、C135 §七、C136 §八）。

#### 六、起草時自己犯了一次，而它就是 §二 右欄的那個錯

⚠️ 這個反例第一次寫在 `#91` 上時，兩個代理都標了 **✅ 命中** ——
**把「幾則之後」寫成了「今天」**，而當時零量測。實測才發現是 0 則與 2 個檔。
那則留言已更正，原文以刪除線保留。

**更正之後這個反例更強，不是更弱**：它從「代理會誤判」升級成「答案會隨時間翻面」。
⚠️ 而值得記的是**錯誤的形狀**：我拿一個**推導出來的穩態**當成**量到的現況**寫下去。
那與 C137 §四 那三次是同一個病 —— 只是這一次錯在時間軸上，不是容器上。
