# Vite+ 企業級腳手架 — 決策紀錄（第二卷）

> **第一卷是 [DECISIONS.md](DECISIONS.md)，C1–C138 全部在那裡，凍結於 2026-08-29。**
> 為什麼分卷、以及為什麼是「凍結」而不是「搬遷」，見 **第一卷的 C138**。
>
> ⚠️ **編號連續，不重新開始。** 下一則是 **C139**。D 與 R 兩串同理
> （第一卷凍結時是 D1–D16、R1–R9）。分的是檔案，不是編號 ——
> 一個 C-編號在整棵樹上仍然只指一件事。
>
> ⚠️ **引用寫法一個字不變**：仍然寫「C138」「D16」「R7」，不寫在哪一卷。
> 讀的人要找哪一卷，看編號落在哪一段。**這正是不改編號的理由** ——
> 樹上約 2,700 處交叉引用因此全部繼續成立。

## 這一卷收什麼

與第一卷同一件事：**有日期的決策日誌** —— 這個腳手架一路上做過的判斷與踩過的坑，
含原文與「不要照這裡做」標註。體例沿用第一卷：

- 一則一個 `### C<n> — 標題（日期，票號）` 標題，底下用 `#### 一、…` 分節。
- **數字一律標基準 commit**（C137 §五）。這一卷與第一卷一樣**沒有機制在守**
  裡面的數字 —— `tools/doc-facts` 刻意不守有日期的日誌，理由在
  `tools/doc-facts/src/cli.ts:25`：**守它等於要求回頭改寫歷史。**
- **推翻一則 C-條時，要寫明它產出過什麼、重跑了沒有**（C136 §八）。
  ⚠️ 這條規則沒有任何東西在守，只靠人讀。
- **要註記第一卷的某一則，寫在這裡**，第一卷最多加一行指標（C138 §四）。

## 條目

### C139 — 偵測器對同一個物件會隨時間翻面，而那個物件的本質從未改變（2026-08-29，#91）

⚠️ **量測基準 `313fe96`**（C138 合併的那一支）。本則所有數字凍結在那一天。

#### 一、這一則是本卷第一則，而它的題材剛好是本卷自己

`#91` 問「新長出的平行體系偵測得到嗎，還是只能靠人判斷」，並列了三個候選的
**結構代理**：內部出現連號的條目、被其他檔案大量交叉引用、自己宣稱是唯一事實來源。

C138 造了 `DECISIONS-2.md`（就是這個檔）。**它是同一套編號的續卷，不是平行體系** ——
所以它是那三個代理的一個現成測試案例。

#### 二、實測：今天兩個代理一個都不命中

| 代理                   | 今天（`313fe96` 實測）                         | 幾則之後（**推導，非量測**） |
| ---------------------- | ---------------------------------------------- | ---------------------------- |
| 內部出現連號的條目     | ❌ **0 則**（`### C\d+` 標題數為 0）           | ✅ 會命中                    |
| 被其他檔案大量交叉引用 | ❌ **2 個檔**（`SCOPE.md`、`DECISIONS.md`）    | ✅ 會命中                    |
| 自稱唯一事實來源       | ❌ 沒有 —— 開頭就寫「第一卷是 `DECISIONS.md`」 | ❌ 不會                      |
| **它實際上是什麼**     | **續卷**                                       | **續卷 —— 沒變**             |

⚠️ **右欄是從 `#91` 列的代理推導出來的，不是跑出來的 —— 目前沒有任何偵測器存在。**
這一格標明白，是因為這棵樹記過同一種錯：**判準套在東西的「描述」上，
而不是它的「實質」上**（C137 §四，一輪三次）。

#### 三、裁決：那兩個代理單獨用不夠 —— 而理由比「會誤判」更尖

**裁決：`#91` 的前兩個結構代理，單獨使用不足以判定平行體系。**

理由不是「它們會誤判」，是：

> **同一個物件，本質從未改變，而偵測器的答案會隨時間翻面。**

⚠️ **今天跑一個偵測器，會對 `DECISIONS-2.md` 回綠燈，而那個綠燈什麼都不代表** ——
它綠是因為這個檔還太新，不是因為它不是平行體系。**一個會自己變成對的答案，
與一個判斷過的答案，長得一模一樣。**

這正是 `#91` 自己寫下的那個危險，現在有了樹上的實例：

> 一個分不出好壞的偵測器比沒有偵測器更糟 —— 它會給出綠燈，而綠燈會被當成保證。

⚠️ 以前它靠 `release/v1` 的 C89 作類比（缺陷與非缺陷會紅同一組測試）；
**現在它有一個同一棵樹上、這個月發生的、而且答案會翻面的案例。**

#### 四、本則**不**裁的事

⚠️⚠️ **本則不裁「自動偵測不可能」。** 一個反例不證明不可能，而且 `#91` 還開著 ——
從一則 C-條裡替另一張票下結論，是把裁決藏在別人看不到的地方。

還缺的是「**有沒有一組代理合起來分得出來**」。C138 §五 有一句可以當起點的話：

> 平行體系與世代交替的差別，在於邊界有沒有日期、以及兩半是不是都活著。

⚠️ **而這句話裡機器讀得到的只有半句。** 「有沒有日期」勉強是結構的
（`DECISIONS.md` 的凍結告示寫著日期）；「**兩半是不是都活著**」不是 ——
它要判斷的是**有沒有人還在往舊的那一半寫**，那是意圖，不是形狀。

#### 五、處置：一條驗收條件，不設閘門

**`#91` 提出的任何偵測器，上線前要對 `DECISIONS-2.md` 跑兩次** ——
一次在它條目還少的時候，一次在它長起來之後。**兩次都必須判它不是平行體系。**

⚠️ **這是給那張票的驗收條件，不是一道閘門。** 與 C129、C136 §八 同一條理由：
明知不可靜態驗證的東西，寫成規則、寫明它敞著，不要假裝有機器在守。
⚠️ 而它與 C137 §一 不衝突：本則**沒有**增加或刪除任何閘門，也不拿任何比值論證。

⚠️ **這條驗收條件本身沒有東西在守**，包括「有沒有人真的跑第二次」。
到期條件也說不出口 —— 「等這個檔長起來」不是一個機器判得出的時點。
**寫下來是為了讓下一個人知道它敞著**（沿用 C109 §七、C135 §七、C136 §八）。

#### 六、起草時自己犯了一次，而它就是 §二 右欄的那個錯

⚠️ 這個反例第一次寫在 `#91` 上時，兩個代理都標了 **✅ 命中** ——
**把「幾則之後」寫成了「今天」**，而當時零量測。實測才發現是 0 則與 2 個檔。
那則留言已更正，原文以刪除線保留。

**更正之後這個反例更強，不是更弱**：它從「代理會誤判」升級成「答案會隨時間翻面」。
⚠️ 而值得記的是**錯誤的形狀**：我拿一個**推導出來的穩態**當成**量到的現況**寫下去。
那與 C137 §四 那三次是同一個病 —— 只是這一次錯在時間軸上，不是容器上。

### C140 — 三串三種體例、兩卷一個分界點，而入門序列的選法不能用引用數（2026-08-29，#189）

⚠️ **量測基準 `1ea808e`。** 本則所有數字凍結在那一天。

#### 一、`#189` 說的是「加一份路標，**不是**加第 161 則」，而這是一則

票面那句話針對的是**埋起來**：一份路標寫進 13,000 行的日誌中間，
讀者永遠走不到它。**那個顧慮成不成立，不看這一卷多長，看讀者從哪裡進來。**

⚠️ **而讀者不從這一卷進來。** 44 個檔提到 `DECISIONS.md`，包括 `README.md`、
`AGENTS.md`、`SCOPE.md`。**一份寫在第二卷、而第一卷不指過來的路標，
就是第 161 則** —— 第二卷再短都一樣。

**所以本則成立的條件是第一卷加一行指標**，而那一行**同時是它的上限**：
C138 §四 寫著「要註記 C1–C138 的某一則，寫在新一卷裡，**舊卷最多加一行指標**」。
在第一卷寫一段解釋會違反本卷前兩則剛立的規矩，而且是寫在被 C138 凍結的那個檔裡。

⚠️ **那一行沒有任何機制在守。** `doc-facts` 的 `GUARDED` 是
`README.md`／`HANDOFF.md`／`UI-SURVEY.md`，`DECISIONS.md` 刻意在外
（`tools/doc-facts/src/cli.ts:25`）。它爛掉的時候不會有東西說話。

#### 二、三串編號，三種體例 —— 這是路標的核心

| 系列   | 體例                                              | 洞                                     |
| ------ | ------------------------------------------------- | -------------------------------------- |
| `C<n>` | 每則一個 `### ` 標題                              | **C51 從未使用**                       |
| `D<n>` | 每則一個 `### ` 標題                              | **D14 佔兩個標題**（另有〈D14 下半〉） |
| `R<n>` | ⚠️ **一張九列的登記表**，只有需處置的七條另開專節 | **沒有洞，R1–R9 全在**                 |

⚠️⚠️ **`R` 那一列是這份路標存在的理由。** 「R7／R9 從未定義」這個結論
**已經獨立發生三次**（`#189` 票面有那張表），而三次的根源完全相同：
**拿 C／D 的體例（`### <編號>` 標題）當量尺去量 R。**
R7／R9 已解除、無需處置敘述，所以沒有專節 —— **那是體例，不是遺漏。**

⚠️ 數 R 要用 `grep -cE '^\| R[0-9] +\|'` → 9。寫成 `'^| R[0-9]'` 會回 14，
因為〈調查與處置〉還有一張 `| R2「…」` 開頭的對照表。

#### 三、哪一段編號在哪一卷 —— 寫成**分界點**，不是清單

> **C138 為界。C1–C138 在 `DECISIONS.md`，C139 起在 `DECISIONS-2.md`。**
> **D 與 R 兩串整串留在第一卷**（凍結時是 D1–D16、R1–R9）。

⚠️ **刻意不寫成「第二卷有 C139、C140」那種清單** —— 每加一則它就過期一次，
而它自己就會變成下一個過期的路標。**分界點只在分卷的時候才需要動。**

⚠️ **編號連續、引用寫法一個字不變**：寫「C138」不寫在哪一卷，
讀的人看編號落在分界點的哪一邊。那正是不改編號、也不改第一卷檔名的理由 ——
樹上約 2,699 處交叉引用因此全部繼續成立（C138 §三）。

⚠️ **而「`C<n>` 一定在 `DECISIONS.md` 裡」今天是假的。** 實測：
`grep '### C139' DECISIONS.md` 回零，而 C139 存在。
grep 編號、寫檢查、量引用**都要掃兩卷**。

#### 四、入門序列 —— 而量下去發現引用數是個壞代理

先講選法，因為**清單會過期，選法不會**。

##### 引用數量過了，它不能當主判準

實測各編號被引用次數（扣掉定義處，排除 `pnpm-lock.yaml`）：

| 高引用 | 次數   | 低引用但重複發生 | 次數  |
| ------ | ------ | ---------------- | ----- |
| `C41`  | **81** | `C105`           | **4** |
| `C136` | 69     | `C102`           | 7     |
| `C114` | 54     | `C107`           | 10    |
| `C72`  | 50     | `C122`           | 12    |

⚠️⚠️ **`C105` 記的是這棵樹重複最多次的失敗形狀**（判準套在東西的「描述」上
而不是「實質」上 —— 它自己那一輪犯三次，C106 第四次，C137 §四 又一次），
**而它只被引用 4 次。** 引用數量的是「後來的人回頭指它幾次」，
不是「同一個錯又發生了幾次」——**兩者不相關。**

##### 判準：人判為主，引用數只用來抓漏

- **主判準（人判）**：這一則記的是不是**一個重複發生過的失敗形狀**，
  而不是一次性的選型或實作細節。
- **輔判準（可量）**：引用數 —— 只拿來問「**高引用而我沒選的，是不是漏了**」。
- **結構必讀**：不看以上兩軸 —— 沒有它就走不動的那幾則。

##### 名單（11 則）

| 則       | 為什麼在名單上                                                                |
| -------- | ----------------------------------------------------------------------------- |
| **D16**  | 兩軸判準（交付／迭代）：一個東西留不留。每一次「要不要加／砍」都回到它        |
| **C72**  | 範疇判準：**看它守的東西給誰看**。⚠️ 問法還活著，**後果已被 C134／C136 收回** |
| **C93**  | **「沒有被治理」不等於「准許進入」** —— 沉默是舉證責任沒履行，不是預設通過    |
| **C136** | **判準的效力依附在它的後果上，而後果是環境給的**（同一件事已應驗三次）        |
| **C105** | **判準套在東西的「描述」上，不是「實質」上** —— 這棵樹重複最多次的錯          |
| **C41**  | 抄本漂移：產生器不知道 `D15` 存在，**而沒有任何一道閘門會說話**               |
| **C33**  | 假的綠的原型：SBOM 產出了、上傳了、全綠，**而裡面有 0 個 component**          |
| **C114** | 差一點交付一個**永遠不會執行**的檔案 —— 空清單與全綠長得一模一樣              |
| **C126** | **一條只守四支的絆線，與沒有絆線長得一樣**                                    |
| **C133** | 併線：**兩半從來沒見過面**，而天真的合併零衝突刪掉 71 個檔                    |
| **C139** | **答案會隨時間翻面，而對象沒變** —— 本卷第一則                                |

⚠️ **`D15`（引用 100，全樹最高）刻意不在名單上**：它是選型決策，
讀它學不到失敗形狀。**輔判準抓到了它，主判準把它擋掉了** —— 這正是兩軸分工的樣子。

⚠️ **C138 也不在名單上**，因為它已經寫在第一卷開頭的告示裡，讀者一定會撞到。

#### 五、這份路標沒有機制在守，而它會過期

⚠️ **三處會腐爛，而且沒有東西會說話**：

1. **第一卷那一行指標** —— `doc-facts` 刻意不守 `DECISIONS.md`。
2. **§三 的分界點** —— 下次分卷時要改，而沒有東西知道分卷發生了。
3. **§四 的名單** —— 新的失敗形狀出現時它不會自己長。

**處置：沒有機制，只有把選法寫下來**（§四）。名單會過期，
**而一個寫明選法的名單，下一個人可以自己重算一遍** —— 這是這一則
唯一真正交付的東西。與 C129、C136 §八、C139 §五 同一條理由：
明知不可靜態驗證，**寫明它敞著，不要假裝有機器在守**。

---

### C141 — 決策日誌的編號指得到，靠寬掃 ＋ 一個具名豁免（2026-08-30，#199）

#### 一、為什麼一份**刻意不被守**的日誌，可以被守這一件事

`doc-facts` 的 `GUARDED` 只有三份文件，而決策日誌**刻意在外**
（`tools/doc-facts/src/cli.ts:25`）：那是有日期的日誌，「C24 當時是 467 個套件」
陳述的是歷史，守它等於要求回頭改寫歷史。

**指不到的編號不是同一件事，而這個區分不是新發明的。** C99 為了同一個問題
逐個 commit 查過 50 個版本，得出的那句話一字不改地適用：

> 它們**不是**過期 —— `R3`／`R5`／`#25` 一次都沒有出現過。
> 也就是說：它們**從寫下的那一刻就是死的**。

條目從不被刪除，所以一則編號要嘛存在、要嘛從來沒有存在過。**沒有「當時是對的」
這種狀態** —— 而那正是 `GUARDED` 那條界線的判準。所以這一條的射程是決策日誌，
而 `GUARDED` 一格都沒動（`#199` 明文要求）。

⚠️ 失敗方向與 C99 同一個：**讀的人不會知道那一則不存在，只會以為自己漏讀了。**

#### 二、四個坑，三個寫進了規則，而第四個是量出來的

`#199` 開票時就帶著四個已知的坑。逐個處置：

| 坑                         | 處置                                                             | 守它的東西                    |
| -------------------------- | ---------------------------------------------------------------- | ----------------------------- |
| ① `R` 是登記表不是 `###`   | 定義集分兩種抽法：C／D 用 `^### `，**R 用 `^\| R<n> \|` 的表列** | 夾具斷言：R7／R9 必須在集合裡 |
| ② 要掃兩卷                 | **不掃「兩卷」，掃 `DECISIONS(-<n>)?.md` 這個檔名體例**          | 夾具斷言：卷數與檔名          |
| ③ 分界點是散文、沒有東西守 | **拆掉對它的依賴** —— 檢查器從頭到尾不知道 `C138` 是分界點       | 不需要（依賴不存在了）        |
| ④ 死鏈 4 處而全是對的      | 具名豁免 `ABSENT = {C51}`                                        | 夾具斷言：`C51` 仍未被使用    |

⚠️ **③ 是「拆掉」不是「解掉」。** C140 §五.2 記的那件事沒有被修好 ——
`DECISIONS-2.md` 開頭那句散文的分界點照樣會腐爛，照樣沒有東西在守它。
變的只有一件：**這支檢查器不再靠它**。

⚠️ **④ 那個豁免不是白名單的開頭，也不是一顆可以轉鬆的旋鈕。** ⚠️ 表上那個 4
是**基準 `d4c4925` 的數**（本則寫下之前，理由見 §八）；那 4 處全部是
「C51 從未使用」**那句話本身**
—— 記錄那個洞存在的句子，被守它的規則判成缺陷。豁免因此掛了一條夾具斷言
（C97 §三之二 那條教訓）：`C51` 哪天真的成為一則條目，豁免就從「陳述一個空號」
變成「放行一個真的引用」，而那會安靜地發生 —— 測試會紅並要求把它拿掉。

#### 三、兩種抽法都量過，選了寬的那個 —— 而窄的那個守備範圍剩 5.7%

C99 對「提到編號」與「叫人去看編號」的區分，用的代理是**指路動詞**
（見／參照／詳見／參見）。它可以直接搬過來，而且搬過來的偽陽性是零。量下去：

| 抽法                    | 掃到的引用      | 死鏈             | 代價                                 |
| ----------------------- | --------------- | ---------------- | ------------------------------------ |
| **寬掃**（任何 `C<n>`） | **2,975**       | 4（全是 `C51`）¹ | 需要一個具名豁免                     |
| 指路動詞（C99 的代理）  | 171（**5.7%**） | 0                | 94.3% 的引用不受守，而且代理繞得過去 |

¹ 兩欄都是基準 `d4c4925` 的數，而右欄那個 4 已被本則自己作廢（§八）。

**裁決：寬掃 ＋ 具名豁免。** 兩個理由：

1. **偽陽性有界而且已經數完了** —— 4 處、一個編號、一句話。豁免是一個**具體事實**
   （C51 從未使用），不是一條可以續加的規則。
2. **窄的那個代理，C99 自己的檔頭寫著它繞得過去**：「有人寫『HANDOFF 的 R7 有詳細
   說明』就繞過去了」。用一個承認有縫的代理，換掉 94.3% 的守備範圍，換不過來。

⚠️ **`\b` 與三位數上界不是樣式潔癖，是量出來的。** 拿掉任一個，`pnpm-lock.yaml`
的 sha512 會從 1 個命中灌到 145 個（實測）。所以那個檔**不需要排除** ——
`\b` 在 base64 區塊裡結構上就不成立。

#### 四、射程：`reports/` 在外，而**理由比看起來的弱**

唯一站得住的理由是 `reports/research/rigs/README.md:6` 那句**沒有修飾語**的
「這個目錄沒有任何機制在守」—— 掃它就讓那句話變成假的，而 `#199` 明文不動別人的
射程宣告。代價數得出來：**3 個檔、46 個引用不受守**（今天全部指得到）。

⚠️⚠️ **「那是凍結的研究稿，算歷史」不能拿來當理由，雖然它聽起來比上面那個強。**
那個論證對決策日誌本身一模一樣成立 —— 而 §一 整節正是在說它不成立。
起草時先寫了這個理由，發現它會**同時**論證掉本則自己，才換掉。
寫下來，是因為一個聽起來更強的錯理由，比一個承認自己弱的對理由危險。

**要把 `reports/` 收進來：改一個常數 ＋ 改那句 README，兩件事一起做。**

⚠️ **而 `ABSENT` 不是同一種東西 —— 它不得比照辦理。** 射程是設計選擇，豁免是
事實登記：放寬 `ABSENT` 就是改閘門來換綠燈（`AGENTS.md` 規則二）。所以那條規則
紅掉時的訊息只給一條路（**改那個引用**），並明寫「要再加一個空號，停下來告訴人」。

#### 五、D16 兩軸各報一格（`#187` 的填寫要求）

| 軸       | 這一支                                                          |
| -------- | --------------------------------------------------------------- |
| **交付** | **零。** 採購／資安／法務沒有一個收件人在意一則編號指不指得到。 |
| **迭代** | **有分**，三個具體的觸發寫在下面。                              |

迭代軸那一格，照 D16 自己給的具體程度（「vitest 4 移除 `--reporter=basic`，
於是失敗清單是空的 —— 而空清單和全綠長得一模一樣」）：

1. **有人寫下一則還沒存在的編號**（打錯字，或引用一則打算寫而沒寫的條目）——
   這是它每天在守的那一件。
2. **有人把 `R` 的抽法「簡化」成 `### R<n>`** —— 那是最直覺的寫法，而這個誤讀
   **已經獨立發生三次**（#189）。R7／R9 會掉出定義集，指向它們的引用全部變紅，
   **而紅燈的訊息會說「這個編號不存在」** —— 一個把體例誤讀報告成缺陷的紅燈。
3. **下一次分卷的檔名不合體例**（`DECISIONS-<n>.md`）—— 那一卷的條目整批不進
   定義集。⚠️ 反方向更危險：一個 `DECISIONS-DRAFT.md` 會**安靜地把定義集變寬**，
   而變寬的方向永遠不會有紅燈。夾具斷言擋的就是這一格。

⚠️ **必須寫下的一個誠實的地方：D16 迭代軸的原文是「上游變動時」，而這三個觸發
全部在內部。** `#187` 的措辭是「上游**或內部**發生什麼具體變動時」，所以這一格
填得出來 —— 但那是 `#187` 的措辭，不是 D16 的原文。**照 D16 逐字讀，這一支兩軸
皆零，該丟。** 本則不改 D16（`#187` 也明文不動它），把這個張力寫在這裡，
是因為下一支 meta 閘門會撞到同一格。

#### 六、它在 `ready` 不在 `gate` —— 這是一個性質，不是備註

做成**測試**不是 CLI（沿用 C99 的位置：`tools/doc-facts/tests/`）。後果具體：

- `vp run -r test` 會跑它 → `pnpm ready` 第 2 步 → CI。
- **`pnpm gate` 不會跑它。** 一個只改文件、加了死鏈的改動，`gate` 是綠的、
  `ready` 是紅的。

⚠️ 這與 C99 那條規則今天的處境完全相同，**而 `AGENTS.md` 要求的是 `vpr ready`
全綠，不是 `gate` 全綠**。記在這裡是因為「`gate` 是 `ready` 的子集」這件事
在這棵樹上已經被踩過兩次。

它也因此**不需要 `gate-roster` 的一列** —— 名冊登記的是 CLI，不是測試。

**三條變異都真的跑過一次**（不是推論）：

| 變異                                | 結果                                                                         |
| ----------------------------------- | ---------------------------------------------------------------------------- |
| 在檔尾 append 一則真的重號          | **只有** 🔴 那條紅，訊息點名 `C170 被 2 則認領：DECISIONS-2.md:4732、…:5062` |
| 把 `duplicates()` 短路成永遠回 `[]` | **只有** ★ 那條紅（1 failed／57 passed）                                     |
| 把嚴格式收緊成 `— `（要求空格）     | **只有**「兩把尺集合相等」那條紅，C 少一個（161 vs 162）—— 那一個就是 C37    |

⚠️ 第二條與第三條是這一節存在的理由：**沒有它們，短路過的偵測器與收緊過的樣式
都會全綠**，而全綠與「樹是乾淨的」長得一模一樣。

⚠️ **另一個實測到的偽陽性形狀，寫在這裡免得下一個人查半天**：**在一次尚未解完的
rebase 中間跑這支測試，它會說每一個編號都被 3 則認領**（三處行號還完全相同）。
成因是 `git ls-files` 對未合併的路徑會列出三個 stage，於是同一卷被讀了三次。
⚠️ 同一趟裡「卷數」那條夾具也紅（4 卷）—— **兩條一起紅就是這個成因**，
不是樹上真的有重號。解完衝突、`git add` 之後就消失。

#### 七、本則不裁的事

⚠️ **不裁 C99 的射程要不要跟著改。** C99 守的是指向 `HANDOFF.md` 的引用，
它今天用指路動詞代理、只掃四份文件。本則量出了那個代理的守備範圍（5.7%），
但**沒有**回頭重判 C99 —— 那是另一張票的事，而在一則 C-條裡替它下結論，
就是 C139 §四 拒絕過的那個動作。

⚠️ **不裁 `GUARDED` 要不要加第四份。** `#199` 明文排除，`cli.ts:28` 的警告
（加第四份的那一刻，四個地方寫著「只守 README 與 HANDOFF」的句子同時變成假的）
原封不動。

#### 八、量測與驗證（基準 `d4c4925`，2026-08-30）

| 量的東西                               | 值                     |
| -------------------------------------- | ---------------------- |
| 掃描檔數（版控，排除 `reports/`）      | 379                    |
| 編號引用總數                           | 2,975                  |
| 死鏈                                   | **4，全部是 `C51`**    |
| C 定義集                               | 139 則（1–140，缺 51） |
| D／R 定義集                            | 16／9                  |
| 若用 `### R<n>` 抽 R                   | **7**（缺 R7／R9）     |
| `pnpm-lock.yaml` 的命中（有／無 `\b`） | 1／145                 |

⚠️⚠️ **上表那個 4 標著基準 commit，因為它已經被這一則自己作廢了。**
寫完本則之後，樹上提到 `C51` 的地方從 **4 處變成 18 處** —— 多出來的全部是
本則與那支測試在**談論這份豁免**。⚠️ 而這不是文字遊戲：哪天 `C51` 真的被使用、
要把豁免拿掉，**要處理的不是 4 處，是全部**，而夾具斷言只會告訴你「該拿掉了」，
不會告訴你有多少處要改。

⚠️ 起草時這個數字是寫死的「今天樹上 4 處」——**一個在講「引用要指得對」的條目，
自己寫了一個寫下當場就變假的數字**。這棵樹上同樣的形狀已經發生過
（C138 起草時的行數）。

⚠️ **差分探針跑過，而第一次跑出來的是假的紅燈。** 注入一句指向不存在 D 編號的
註解 → exit 1；但那個 1 來自 `npx` 的 `EBADDEVENGINES`，**不是測試失敗** ——
包在 shell script 裡的 `npx` 沒有被改寫。改成直接呼叫重跑，才拿到真的：
**那一條**紅、訊息正確、還原後全綠。

⚠️ **第二支探針驗的是「卷從哪裡來」，而它擋下的是 C73／C98 那個方向。** 起草時
`volumes()` 走 `readdirSync`，與同一個檔案上方三行引的規矩自相矛盾。實測：放一份
**未追蹤**的續卷、再從別處引用它裡面的編號 —— 改走 `git ls-files` 之後仍然紅
（正確：CI 的 checkout 沒有那個檔）。走 `readdirSync` 的話它會綠，
而**本機綠、CI 紅**正是 C73／C98 逐字寫下的失敗方向。

⚠️ 同一趟還看到一件事：**壓縮過的測試輸出會把死鏈清單省略成 `Array(1)`**。
紅燈本身看得見，**清單看不見** —— 要看是哪一條，得跑未壓縮的輸出。

### C142 — D16 迭代軸的「上游」是代理，判準是「壞法安靜」（2026-08-30，#187）

#### 一、選的是 A，而 A 的字面版本不能用

`#187` 的留言把 C141 §五 那個張力收成兩個選項問了人（`#199`／`#200` 之後）：

| 選項                                       | 代價                                                                                   |
| ------------------------------------------ | -------------------------------------------------------------------------------------- |
| **A（採用）** 明寫成「上游**或內部**」     | 實質動到 D16 的讀法，而 `#187` 說不動 D16                                              |
| B 維持逐字讀，承認 meta 閘門這一類答不出來 | 會把 `gate-roster`／`scope-check` 一起判成兩軸皆零 —— 而 `#187` 明講不主張砍掉任何一支 |

**人選 A。**

⚠️ **但字面照抄「上游或內部」會把這一格變成空的。** 那樣讀，
「有人改了它守的東西時它會紅」對**每一道**閘門都成立，一句話就能填滿 ——
而 `#187` 存在的理由正是那一句：**「它守得比較嚴」不是一個迭代軸的答案。**
A 的字面版本會親手做出這張票要擋的東西。

**所以本則做的是 A ＋ 一個限定條件，這比人選的多。** 寫在第一節是因為
它該被看見：人選的是「擴寫」，本則擴寫的同時收緊了。收緊的部分若判錯，
要推翻的是本則的 §二，不是那個選擇。

#### 二、裁決：迭代軸兩條，而「上游」是其中一條的常見來源

**D16 迭代軸讀作：有人為了別的目的做了一個變動，而「別的東西」因此壞了、
壞掉的樣子卻是綠的 —— 這個東西會不會把它變成紅的？**

要在這一格拿到分，兩條都要成立：

| 條件           | 內容                                         |
| -------------- | -------------------------------------------- |
| **① 對象在外** | 它報的是**別的東西**壞了，不是它自己被改壞了 |
| **② 壞法安靜** | 在**它不存在**的世界裡，那個壞法看起來是綠的 |

⚠️ **② 才是真正的門檻。** 填這一格的時候要說出的不是「違反規則時它會紅」，
而是**在它不存在的世界裡，那個壞法看起來是什麼樣子**。如果答案是
「看起來就是壞的、有人會發現」，這一格是零。

**這兩條不是新發明的，是從 D16 自己給的樣本讀出來的。** 逐項對照：

| D16 的樣本                       | 對應                                                |
| -------------------------------- | --------------------------------------------------- |
| vitest 4 移除 `--reporter=basic` | 做這件事的人不知道這棵樹存在 —— 「為了別的目的」    |
| 子行程啟動即失敗                 | 壞掉的是**測試有沒有真的跑**（對象在外）            |
| **空清單和全綠長得一模一樣**     | 壞法安靜 —— 而這一句是那個樣本被寫進 D16 的全部理由 |

**「上游」是條件 ①②同時成立最常見、也最容易查證的來源** —— 一個上游的變動
天生就不是為了對付你的閘門而做的。但它是**來源**，不是判準的內容；
判準的內容是「對象在外」加「壞法安靜」。

⚠️ 這棵樹踩過這個形狀：**把判準套在代理上，會給出漂亮而錯誤的答案**
（C140 §四 把引用數當「重要性」的代理，量得很乾淨而結論是錯的）。
「上游」對「壞法安靜」就是一個代理，而 meta 閘門這一整類正好落在代理的盲區裡。

#### 三、對照四支現存工具 —— 沒有一支掉出去（⚠️ 判讀，非量測）

**這一節的用途是擋 B 從後門進來。** 人選的是 A，而 `#187` 明文不主張砍掉任何
一支；一個收緊過的讀法如果順手把現存的工具判成該丟，那就是 B 換了個名字。
逐支問條件 ②（「它不在的話，壞法長什麼樣」）：

| 工具            | 對象在外                    | 它不在的話，那個壞法長什麼樣                                           |
| --------------- | --------------------------- | ---------------------------------------------------------------------- |
| `gate-roster`   | 四個消費端與名冊            | 一支閘門從某個消費端掉出去 —— **CI 照跑照綠，只是檢查得比較少**        |
| `scope-check`   | 樹與 `SCOPE.md`             | 頂層多一個目錄而沒登記，全綠；反方向（清單列了、樹上沒有）也沒人會發現 |
| `doc-facts`     | 文件裡的數字                | 數字與事實來源脫節，**文件照樣渲染**                                   |
| `promise-check` | `specs/` 的承諾有沒有被執行 | 一條承諾沒有被真的執行 —— **和全部通過長得一模一樣**                   |

**四支全部有分，而 `promise-check` 那一格是 D16 樣本的同構。**

⚠️ **這張表是判讀，不是量測** —— 沒有跑任何東西去驗證「它不在的話會綠」。
要驗，得逐支把它拿掉再跑一次；本則沒做。

**而在這一節，不量是可以接受的，理由要說出來**：表格右欄問的是**它不存在的話
會長什麼樣**，而每一列都只是把那支工具自己檔頭寫著的存在理由再讀一次
（四支的 `src/*.ts` 檔頭各自寫了它守什麼、刻意不守什麼）。**這是讀既有文件，
不是新的判斷。** ⚠️ 若哪天有人主張其中一列不成立，該做的是把那支拿掉跑一次，
**而不是換一把尺** —— D16 記過三次翻案，每次證據沒變、變的只有尺。

#### 四、這把尺對它的第一個受益者不利，而那是它不是為答案挑的證據

⚠️ **必須先說清楚立場**：本則的第一個受益者是 C141，寫在一天前，
而 C141 §五 自己寫著「照 D16 逐字讀，這一支兩軸皆零，該丟」。
**一個人寫了一道閘門，隔天寫一則裁決把判準改成讓它過關** ——
這正是 D16 §「為什麼需要一條明寫的判準」記下的病：
`exit-drill` 與 `ui-survey` 三次翻案，**證據沒變，變的只有尺**。

所以把不利的結果放在這裡：**照 §二 的兩條，C141 §五 報的三格只剩一格。**

| C141 §五 的觸發              | 條件 ① 對象在外 | 判                                                                   |
| ---------------------------- | --------------- | -------------------------------------------------------------------- |
| ① 有人寫下一則還沒存在的編號 | ✅ 日誌本身     | **有分** —— 而且壞法安靜（文件照樣渲染，讀的人以為是自己漏讀了一則） |
| ② 有人把 `R` 的抽法「簡化」  | ❌ 閘門自己     | **不算迭代軸的分**                                                   |
| ③ 下一次分卷的檔名不合體例   | ❌ 閘門自己     | **不算迭代軸的分**                                                   |

②③ 描述的是**這道閘門自己會怎麼被改壞**，而改它的人正在改的就是它 ——
兩條條件都不成立。它們是**自我防護**，那兩條夾具斷言該留著，
但它們填的不是 `#187` 那一格。

**C141 仍然有分（① 成立），仍然不該丟。** 而一把為了答案挑出來的尺，
不會在自己的第一個案例上砍掉三分之二。

⚠️ **依 C136 §八 交代產出**：本則**不推翻** C141 的結論，只判定它 §五 的三格
裡有兩格記錯了地方。那兩格的產出物是測試裡的夾具斷言，**它們仍然正確、
仍然該在**，本則改的是它們記在哪一格，不是它們該不該存在 ——
**沒有東西要重跑。**

#### 五、兩類東西分開寫 —— 這是 §二 的後果，不是一條新規定

§二 的兩條一旦成立，這件事就跟著成立，不需要另外的授權：**一道閘門守著自己
不被改壞的那些夾具斷言，兩條條件都不滿足**（對象是它自己、而改它的人正在改
的就是它），所以**它們填不了 `#187` 那一格**。

那些夾具**仍然要寫下來** —— 它們是這棵樹上最容易掉的東西 —— 只是寫在別處。

⚠️ 混寫的後果具體：一支兩軸皆零的工具，可以靠列出「我有三條夾具在守自己」
把迭代軸那一格填得滿滿的。**那是 §一 那個空格子的第二種寫法。**

⚠️ **這一節刻意寫成後果，不寫成「`#187` 新增一條填寫要求」。** 兩者的差別
不是措辭：後者是**替一張還開著的票加一條它沒裁過的規定**，而本則只被授權
處理 D16 迭代軸的讀法。要不要把它升格成票面上的一條，是 `#187` 的事
（同一個形狀見 C135 §一 —— 建議可以寫「新位置」，裁決不能）。

#### 六、D16 原文一個字不動，而舊卷那一行是 C138 §四 的第一次動用

兩個理由，第二個比第一個根本：

1. **C138 把第一卷凍結了**，D16 在第一卷。
2. 更根本的：D16 是**有日期的裁決**，改它等於回頭改寫歷史 ——
   `tools/doc-facts/src/cli.ts:25` 拒絕守 `DECISIONS.md` 用的就是這條理由。
   **這棵樹推翻舊裁決的做法是開新則**（C134／C136／C137 都是），本則照做。

C138 §四 把「C139 之後要註記舊則」的去處寫死了：**寫在新卷裡，舊卷最多加一行
指標。** 本則是那條規定的**第一次動用**，所以照字面只加**一行**——
`DECISIONS.md` 的 D16 兩軸表底下那一行。

⚠️ **代價寫清楚**：讀 D16 的人現在要多跳一次，而那一行是唯一的連結。
如果哪天有人把它刪掉，D16 會退回逐字讀，**而沒有任何東西會紅**。

#### 七、本則不裁的事

- **不動 D16 原文**，不動 C137，不動 `#187` 的票面措辭（票面本來就寫著
  「上游**或內部**」—— 本則是給那個措辭一個依據，不是改它）。
- **不重判任何現有工具，不增也不減任何一道閘門。** §三 那張表是拿來確認
  收緊沒有誤傷，不是拿來論證增減 —— C137 §一 明文禁止後者。
- **不預判 `#93`。** `#187` 建議把 `#93` 當這條要求的第一個案例；
  實際先撞到的是 C141，而那不改變 `#93` 自己要填。

#### 八、沒有機制在守，而唯一機械的那一點守錯了方向

D16 的讀法、`#187` 的填寫要求、本則的兩條條件 —— **全部靠人讀。**
本則不帶任何閘門，`gate-roster` 不加一列，`UNGATED` 也不加。

⚠️ 唯一機械的後果是反諷的：`C142` 這個**編號**現在被 C141 那道閘門守著 ——
指不到的話 `ready` 會紅。**而它守的是指得到，不是用得對。**
有人拿本則去論證增刪一道閘門（C137 §一 與 §七 都明文禁止），
不會有任何東西變紅。

### C143 — 正典分類進 `SCOPE.md` 根層表，而它不是新的一支閘門（2026-08-30，#93／#88／#187）

⚠️ **量測基準 `9659bb0`**（C137 §五）。本則所有數字凍結在那一個 commit。

#### 一、`#93` 問的四件事，答案先擺出來

| `#93` 要定的       | 本則的答案                                                               |
| ------------------ | ------------------------------------------------------------------------ |
| 閘門**斷言什麼**   | `SCOPE.md` 根層表的每一列，要指名四個桶之一（C135 §三 那四個）           |
| 它**住在哪**       | **不新造一支** —— 擴充 `tools/scope-check`，理由是擁有權（§五）          |
| 進不進 `gate` 鏈   | **已經在** —— `scope-check` 於 `#180`（`b745d6e`）進了 tier1，無需新決定 |
| **變異驗證**怎麼做 | §七 列了實作要交的四條                                                   |

#### 二、票面上兩處指向不成立，而其中一處是實測翻的

`#93` §六 寫著「新增一個 `tools/<name>` 會撞上 `scope-check` 的『樹上有、沒登記』」，
並由此推出「而 `main` 的範疇清單今天仍未定義（`#87` 動不了）」。**撞的對象錯了。**

實測（版控中的真 workspace 成員，未經管線、逐支取結束碼）：

| 探針                                           | `gate-roster`       | `scope-check` |
| ---------------------------------------------- | ------------------- | ------------- |
| 新增 `tools/probe-canon/`（含 `package.json`） | **1**（工具沒登記） | 0             |
| **對照組**：新增根層 `zz-root-probe/`          | —                   | **1**         |
| 兩者移除後                                     | 0                   | 0             |

C136／`#183` 之後 `scope-check` 的 `GOVERNED` 只有 `platform/` 與根層，
`tools/` 那一層交給了 `gate-roster`（`SCOPE.md` 那句「它一樣會擋你」是真的）。
**所以擋新工具的是登記程序，不是一份未定義的清單** —— `#87` 沒有擋著這件事。
`#86` 也已於 2026-08-26 關閉，「實作等解凍」那個條件不存在了。

⚠️ **起草時我自己先量出兩個假的綠，兩個都是夾具不具代表性：**

1. 第一支探針只有 `src/x.ts`、**沒有 `package.json`** —— 而 `gate-roster` 數的是
   workspace 成員，所以它根本不在母體裡。
2. 改好之後把它命名為 `zz-probe`，而 `tools/doc-facts/src/derive.ts:41` 的
   `TRANSIENT_PREFIX = "zz-"` **會刻意跳過**這個前綴的目錄。**又綠一次。**

換成中性名字才拿到那個 1。⚠️ 這是「絆線要掛在被守的對象上」的第三種形狀：
**夾具的名字本身可以讓它從母體裡消失。**

#### 三、被守的對象：C135 那 32 列不能當事實來源

C135 的表是**凍結卷裡有日期的快照**。守它有兩個各自獨立的擋路理由：
`tools/doc-facts/src/cli.ts:25`（守有日期的日誌等於要求回頭改寫歷史）、
以及 C138（第一卷凍結，本來就不能改）。

**而它已經過期了，三天。** 實測兩個 commit 的根層母體（`git ls-tree`／`git ls-files`）：

| 基準                          | 根層檔 | 頂層目錄 | 合計   |
| ----------------------------- | ------ | -------- | ------ |
| `1dc1227`（C135，2026-08-27） | 24     | 8        | **32** |
| `9659bb0`（今天）             | 25     | 9        | **34** |

多出來的兩列是 **`DECISIONS-2.md`**（C138 自己造的）與 **`reports/`**。
**沒有人把它們歸過類** —— 而 C135 那張表沒有任何機制會告訴你這件事。

所以四個選項連代價問了人（C138 §五 的形狀），**人選了「`SCOPE.md` 根層表加一欄」**。

⚠️ **最強的反對意見要正面回答：C135 §五 明文警告過不要繼承那張表的洞。**
那句話針對的是「**造第二張表、而它以 `SCOPE.md` 為準**」—— 繼承關係來自
「以它為準」。**本則不造第二張表**，那一欄長在同一張表上，母體仍然是
`git ls-files`（`scope-check` 兩個方向都在驗，§六的對照組就是它）。
沒有第二張表，就沒有可以被繼承的洞。

#### 四、裁決：第三欄「桶」，四選一，而機器只查填了沒有

`SCOPE.md` 的根層表今天是兩欄（名字／這是什麼）。**加第三欄，值域是 C135 §三
那四個桶**：正典／正交／過渡豁免／無關。

⚠️ **無關桶「不要求寫理由」與「不要求填桶名」是兩件事。** C135 §三 免掉的是
前者（替 `LICENSE` 寫受益者是儀式不是判斷），本則要求的是後者 ——
**那正是它成為一個有名字的逃生門、而不是一個沒人注意的逃生門的辦法。**

⚠️ **機器只查兩件事：填了沒有、填的是不是那四個之一。判斷仍然是人做的。**
這是刻意的，理由是 `#91` 自己寫的那句警告（C135 §五 引過）：
**「一個分不出好壞的偵測器比沒有偵測器更糟 —— 它會給出綠燈，而綠燈會被當成保證。」**
判「它承載哪一條軸」是語意判斷，正是 `#91` 剩下沒解的那一半。
**所以本則不卡 `#91`** —— 它把那一半留在人手上，沒有假裝機器答得了。

#### 五、不新造一支工具，而理由不是「省」

`#93` 的預設是產出一支新的 `tools/<name>`。**本則不那樣做**，理由是**擁有權**：
`SCOPE.md` 那張表已經有一個負責的閘門，兩支工具對同一份文件負責，
就製造了 C135 §五 定義的那個條件 —— **同一個問題有兩個答案，而沒有規則決定誰贏。**

⚠️ **這不是拿「過度設計」論證少加一支** —— C137 §一 明文禁止那種論證，
而本則沒有用任何比值、也沒有主張閘門太多。論的是一份文件一個守門人。

後果具體：不需要 `gate-roster` 的新一列、不需要 `UNGATED` 的新一列、
不需要新的 tier 決定，也不需要一份新的 README〈兩層檢查〉表列。

#### 六、D16 兩軸（`#187` 的填寫要求，照 C142 的讀法）

| 軸       | 這一條規則                                      |
| -------- | ----------------------------------------------- |
| **交付** | **零。** 沒有評審收件人在意根層表有沒有第三欄。 |
| **迭代** | **有分**，而 ② 是實測的。                       |

- **① 對象在外** ✅ 它報的是 `SCOPE.md` 的列 —— 人寫的文件，不是這道閘門自己。
- **② 壞法安靜** ✅ **實測**（逐支取結束碼，未經管線）：

| 步驟                                     | 結果                                              |
| ---------------------------------------- | ------------------------------------------------- |
| 新增根層 `NOTES.md`，**不**加列          | `scope-check` **1** —— 列是被逼出來的             |
| 加一列，「這是什麼」只寫**「專案筆記」** | `scope-check` 0、`doc-facts` 0、`gate-roster` 0   |
| 同一狀態跑 **`pnpm gate`**（14 段）      | **exit 0 —— 全綠**                                |
| 還原（列與檔案都移除）                   | 0                                                 |
| ⚠️ 中間態（列留著、檔案刪掉）            | `scope-check` **1** —— 那是「清單在說謊」那個方向 |

**所以今天樹上可以多一份承載專案紀錄的文件，而「它跟 `README`／`HANDOFF` 誰贏」
沒有任何人答過，全套閘門 exit 0。** 那正是 C135 §五 定義的平行體系，
**而它的壞法是全綠。**

⚠️ 這是 `#187` 那條填寫要求的**第一個正面案例**。C142 是第一個案例，
但它自己的迭代軸三格只剩一格（C142 §四）。

#### 七、實作要交什麼 —— 四條變異列

這個 repo 的規矩是每一條論證都要有變異列與紅的條數（`#93` 票面）。實作 PR 至少要交：

1. 某一列的桶欄**留空** → 紅。
2. 桶名**不在那四個裡**（例：`雜項`） → 紅。
3. 四個桶各至少一列、都填對 → 綠。
4. **把這條規則拿掉，第 1 條要變綠** —— 證明紅的是這條規則，不是別的東西。

⚠️ **第 4 條不是形式。** C94 記過同一支工具同一個地方的教訓：`SCOPE.md` 從
`v1.0.5` 起就宣稱「沒有人可以跳過那一格」，而實測把某一列後兩欄清空之後
**閘門全綠** —— 文件宣稱了一個保證，程式碼沒有交付它。

⚠️ **實作會動到 34 列的內容，而其中兩列是還沒有人做過的判斷**
（`DECISIONS-2.md`、`reports/`，見 §三）。C135 §二 明講這個判準機器判不了。
**那兩列的桶要由人判，實作 PR 要把它們單獨標出來**，不可以夾在 32 列的
照抄裡送出去 —— 照抄 C135 是有依據的（那是它做過的判斷），新判不是。

#### 九、本則不裁的事，以及沒有機制在守的

- **不裁 `docs/adr/`。** 它今天仍然沒有人裁過（C135 §一／C138 §五 都刻意留白）。
- **不動 C135**（凍結卷），本則是它的續集不是它的修正；C135 那張表仍然是
  2026-08-27 那天的正確快照。
- **不動 C137、不動 D16 原文、不增減任何一道閘門。**
- ⚠️ **`#93` 不關。** 本則是設計，實作是下一支 PR。

⚠️ **桶的判斷本身沒有機制在守** —— 機器查的是「填了沒有、是不是那四個之一」。
一列填了「正交」而它其實是正典，**不會有任何東西變紅**。這一條與
`AGENTS.md` 規則四（不得自己加 `@待辦`）是同一種：**靠人讀 diff。**

### C144 — 桶欄上線，而 34 列裡有兩列是這一支 PR 才判的（2026-08-30，#93）

⚠️ **量測基準 `b2e9798`**（C137 §五）。本則所有數字凍結在那一個 commit。

C143 是設計，本則是它的實作落地報告 —— 而它要單獨存在，是因為 C143 §七
要求「實作 PR 要把那兩列**單獨標出來**，不可以夾在 32 列的照抄裡送出去」。
**一份寫在 PR 內文裡的判斷不進版控**，下一個讀 `SCOPE.md` 的人看到的
只有兩個桶名。

#### 一、兩列新判 —— C135 §四 那張表沒有它們

| 名字             | 桶           | 依據                                                                                                                                                                                                                                                                                                                                                                                                        |
| ---------------- | ------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `DECISIONS-2.md` | **過渡豁免** | 與第一卷同桶。**終點未定**：C138 §三 只裁了「凍結不搬遷」、§五 只裁了「續卷放新的根層檔」，而 `docs/adr/` 今天仍然沒有人裁過（C135 §二／C136 §十／C138 §五 三次刻意留白）。⚠️ **起草時這裡寫著「`#87`（搬去 ADR）仍然開著」，而那在本則的基準之前就已經是假的** —— `#87` 已於 2026-08-29 關閉（NOT_PLANNED），⚠️ 但它的結案留言**明寫「不是『`docs/adr/` 出局了』」**，所以載重的那一半（沒有人裁過）沒有變 |
| `reports/`       | **無關**     | 「既不是紀錄、也沒有自己的治理軸」兩半都成立 —— **`README.md` 與 `HANDOFF.md` 都 0 次提到它**（`doc-facts` 的入場判準兩半皆不成立），而 `SCOPE.md` 那一列自己寫著「沒有任何機制在守它們過不過期」                                                                                                                                                                                                           |

**`DECISIONS-2.md` 的到期條件**（比照 C135 §四 給第一卷的那一格）：
**`git ls-files DECISIONS-2.md` 為空。**
⚠️ **它沒有地方可以寫在表上** —— 桶欄只放桶名（C143 §四），所以它在這裡。
而它跟第一卷那一格一樣**是溫度計不是鬧鐘**（C135 §四）：只有人跑它的時候才被問到。

⚠️ **不選「正典」的理由要寫出來**：正典的意思是「**它本來就該在這裡**」，
而那比這棵樹今天撐得住的還強 —— 它會側面回答 `#87` 那個沒有人裁過的問題。
⚠️ **也不選「正交」**：C135 §六 已經裁過「有沒有閘門守」從來不是正交的判準，
正交是**不同的軸**，而決策日誌正是正典那條軸本身。

⚠️ **無關桶從 4 列變 5 列。** C135 §三 寫過「它小到可以合理質疑第四個桶值不值得」——
這一列讓那個質疑弱一點，但**不足以推翻**，記在這裡是為了讓下一個重數的人知道
數字動過。

#### 二、C143 §七 那四條變異列，逐條的實測

| §七                          | 落地形式                                                     | 實測                                             |
| ---------------------------- | ------------------------------------------------------------ | ------------------------------------------------ |
| 1. 桶欄留空 → 紅             | 兩種寫法各一條（格子是空的／整格不存在）                     | 各 1 條紅，**規則名唯一**                        |
| 2. 桶名不在四個裡 → 紅       | `雜項`                                                       | 1 條紅                                           |
| 3. 四個桶各一列、填對 → 綠   | 四列                                                         | 0                                                |
| 4. **拿掉規則，第 1 條變綠** | 把 `check.ts` 的桶那段用 `if (false && …)` 短路，只跑第 1 條 | **`expected [] to deeply equal ['沒有指名桶']`** |

第 4 條是真的跑過的，不是推論 —— 陣列**空的**，證明那條紅只由這條規則產生。

⚠️ **它能成立，靠的是一個設計決定**：桶那一格**被排除在 C94 的
「登記了、但那一格是空的」之外**（`check.ts` 的 `Layer.bucketColumn`）。
兩條規則都管同一格的話，把桶那條拿掉、空著的桶仍然會紅，
**第 4 條就紅零條** —— 而那正是 C94 記下的病（宣稱了一個保證、沒有交付），
只是這一次會發生在引用它的同一支 PR 裡。

⚠️ **排除不是放寬**：那一格從「非空」升級成「必須是那四個之一」，比 C94 嚴。
測試裡兩個方向各釘一條（桶空 → 不叫 C94 那條；「這是什麼」空 → 仍然叫）。

#### 三、絆線掛在真的那張表上，量過

變異列跑在假 repo 上，所以再對**真的 `SCOPE.md`** 量一次（C94 那次的教訓是
判準對得起夾具、對不起真樹）：清空 `TESTING.md` 的桶、把 `LICENSE` 改成
`雜項` → **2 條紅，兩條規則各 1**；還原 → **exit 0**。

#### 四、本則不裁的事

- **不裁 `#93` 關不關。** 票面的四件事都有答案了（C143 §一），但它的產出位置
  被 C143 §一 改過（`DECISIONS.md` 而非 `docs/adr/`），關不關是人判。
- **不裁 `docs/adr/`。** 第四次留白。
- **不動 C135**（凍結卷）。它那張表仍然是 2026-08-27 那天的正確快照，
  而**今天起有一份活的在 `SCOPE.md`**。
- ⚠️ **桶填得對不對仍然沒有機制在守**（C143 §八 一字不改）。本則自己就是例子：
  上面兩列的桶是人判的，而閘門對它們說的只有「填了、而且是那四個之一」。

#### 五、34 列逐列重讀 —— 抄的部分零錯，而判準重套改了三格理由

⚠️ **這一節是實作 PR 加的，不是設計時就有的。** C143 §八 裁定桶那一格
「靠人讀 diff」，而這支 PR 自己把 34 列全部重排過（加一欄，`oxfmt` 重排全表），
**diff 讀不出來** —— 所以人改用逐列重讀，而這一節是那次重讀的產出。
不留下來，它就是這棵樹反覆記錄的那個形狀：**只活在對話裡的計數。**

**抄的部分零錯。** 逐格比對 C135 §四：正典 6／正交 21／過渡豁免 1／無關 4，
**集合完全相同**，沒有一列抄錯或漏。新判的兩列在 §一。

重套 C135 §二 的判準（「它承載的是專案紀錄，還是別的軸？」）之後改了三格，
⚠️ **三格都在「這是什麼」欄，桶值一格都沒動** —— 所以這一節**不是新判**，
C143 §七 要求單獨標出來的那種（§一 那兩列）只有兩列，仍然是兩列。

| 列          | 改了什麼                                          | 為什麼                                                                                                                                                             |
| ----------- | ------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `AGENTS.md` | 補回「只有 END 標記之後那半是正典」               | ⚠️ **這一格是加欄位造成的退化。** C135 §四 把這一列裁成兩半，而桶欄是單值的表達不了；散文格原本也沒說，所以**被機器檢查的那一格平白斷言整份是正典**                |
| `SCOPE.md`  | 軸從「由 `scope-check` 守著」改成「這棵樹的組成」 | C135 §四 給它的軸原文是**交付樹准入**，而 C136 把准入拿掉了。剩下的「有閘門守著」**分不出它與 `TESTING.md`** —— 那兩列同一個守門人、不同的桶、而理由一字之差都沒有 |
| `.claude/`  | 補上 C135 §四 的**生效條件**                      | 那個條件只活在凍結卷裡（實測：`grep 生效條件 SCOPE.md` → 0，只在 `DECISIONS.md`）。⚠️ 補進來**不會讓它響** —— 它仍然是溫度計不是鬧鐘，只是讀表的人現在看得到       |

**兩件看過而刻意不動的：**

- `CHANGELOG.md`（正交）與 `README.md`（正典）都是敘述性紀錄，但 CHANGELOG
  的軸（SemVer 對 `platform/*` 的承諾）是真的存在的。**不動。**
- ⚠️ `DECISIONS.md` 的到期條件是「`git ls-files DECISIONS.md` 為空」，而
  **C138 §三 裁定檔名刻意永不改 —— 那個條件不可能響。** 它是 C135 的、在凍結卷裡，
  本則動不了（C143 §三 那兩個擋路理由一樣適用）。記在這裡，是因為
  這次是第一次有人把 34 列讀完，不寫下來就沒有下一次。

### C145 — `docs/adr/` 這條路線出局，而它今天可以被裁的理由是守它的兩張票剛剛都關了（2026-08-30，#88／#87／#195／#93）

⚠️ **量測基準 `e052ec2`**（C137 §五）。本則所有數字凍結在那一個 commit。

#### 一、前四次留白都是刻意的，而它們的理由今天不存在了

⚠️ **這一節要先寫清楚，否則本則讀起來像在改正前面四則的疏漏 —— 它們沒有疏漏。**

| 留白                   | 當時寫的理由                                                                                               |
| ---------------------- | ---------------------------------------------------------------------------------------------------------- |
| **C135 §一**           | 產出走 `DECISIONS.md`，不造 `docs/adr/` —— 因為 `#93` 的交付物要造的東西正是它要解的問題，**那個圈要剪開** |
| **C135 §二**           | 判準**刻意不帶准入後果**，所以它答不了「准不准進樹」                                                       |
| **C136 §十**           | 特別警告「`#180` 批准了 `docs/adr/`」是誤讀                                                                |
| **C138 §五**           | 選了新的根層檔而**沒有填**那一格：走 `docs/adr/` 會把已被否決的重接成本從後門帶回來                        |
| **C143 §八**／C144 §四 | 不裁；本則是設計、實作是下一支 PR                                                                          |

**共通的形狀是：每一次都有一個活著的相依方，而替它做決定會越權。** 今天沒有了：

| 曾經指著 `docs/adr/` 的             | 今天（實查）                                                                                           |
| ----------------------------------- | ------------------------------------------------------------------------------------------------------ |
| `#87`（把 `DECISIONS.md` 搬去 ADR） | **CLOSED / NOT_PLANNED**（2026-08-29T14:33Z）。⚠️ 結案留言明寫「**不是**『`docs/adr/` 出局了』」       |
| `#195`（兩條路線同時活著沒人裁）    | **CLOSED / COMPLETED** → 產出 C138，而 C138 §五 只裁了續卷位置                                         |
| `#93` 的交付形式                    | 已由 C143 §一 改成 `DECISIONS.md`；`#93` 已於本則同日關閉                                              |
| **`#88` 的 Destination**            | ⚠️ **仍然寫著「決策談完、寫進 ADR 就算到了」**，而同一張票 Notes 第 4 條說路線已改 —— **票內自相矛盾** |

⚠️⚠️ **所以今天的狀態是：兩張守著這個問題的票都關了，兩次都特地聲明「這不代表它出局」，
而唯一還指著它的文字是一句已被同一張票推翻的 Destination。**
**這正是這棵樹反覆記錄的那個形狀** —— 一個被留白的問題，它的看守者一個一個離場，
而**留白與「已經裁掉了」長得一模一樣**（C134 §七：滿足於缺席）。

⚠️ **這一則不是「終於補上」，是「條件變了所以現在輪到它」。** 差別要緊：
前者會讓下一個人以為留白是壞的，而 C138 §五 示範的正好相反 —— **在有相依方的時候留白是對的。**

#### 二、裁決：決策一律寫進 `DECISIONS.md`／續卷，`docs/adr/` 不造

**這棵樹的決策紀錄體系是 `DECISIONS.md`（C1–C138）＋ `DECISIONS-2.md`（C139 起），
編號連續、體例相同。ADR 那條路線出局。**

選項連代價列出來問了人（C138 §五 的形狀）：

| 選項                                      | 代價                                                                                                     |
| ----------------------------------------- | -------------------------------------------------------------------------------------------------------- |
| **裁掉 ADR 路線（採用）**                 | ⚠️ **這一格填了會決定更大的事** —— 它同時決定了往後**所有**裁決的落點；而且連帶讓兩卷的桶失去理由（§三） |
| 只修 `#88` 的 Destination，路線第五次留白 | 留白從「有相依方所以不裁」變成**沒有理由的留白**，而下一次沒有人會發現主人已經沒了                       |
| 第五次留白，但把理由寫下來                | 要說得出那個理由是什麼；問的時候說不出來                                                                 |

**採用第一項的理由**：C93 裁過「沉默是舉證責任沒履行，不是預設通過」，而**它的反向今天成立** ——
一個問題的守門票全部關閉之後，繼續沉默**不會**維持現狀，它會讓現狀變成無人主張的預設。

⚠️ **本則只裁「決策寫在哪」。** 不裁 `docs/` 這個目錄本身能不能為了**別的**用途存在
（例如使用說明、教學）—— 那仍然要走 C93 那一關，逐案舉證。
`#190` 當初把研究稿放進 `reports/research/` 而不造 `docs/`，理由是「造 `docs/` 等於替 `#87`
預先決定目錄形狀」；`#87` 關了，那個理由消失了，**但本則不接手它** —— 沒有人在要求造 `docs/`。

#### 三、連帶：兩卷的桶從「過渡豁免」改成「正典」—— 兩列新判

⚠️ **依 C136 §八，推翻一則 C-條要寫明它產出過什麼、重跑了沒有。**
本則推翻 **C144 §一** 對 `DECISIONS-2.md` 的桶判斷（以及它繼承的 C135 §四 對第一卷的）。

**C144 §一 產出的是 `SCOPE.md` 那兩列，而本則同一支 PR 就把它們重跑了。**

推翻的理由是 C144 §一 **自己寫下來的**：

> ⚠️ **不選「正典」的理由要寫出來**：正典的意思是「**它本來就該在這裡**」，而那比這棵樹
> 今天撐得住的還強 —— **它會側面回答 `#87` 那個沒有人裁過的問題。**

**§二 把那個問題正面裁了，所以那個理由不存在了。** 而 `正典` 的定義
（C135 §三：「這個 repo 自己的紀錄體系。人讀它們來知道『這個專案是什麼、為什麼』」）
對決策日誌是逐字成立的。

第二條理由是**桶名今天說謊**：`過渡豁免` 的定義是「今天存在、**遷移完成後該消失的**」，
到期條件是 `git ls-files DECISIONS.md` 為空。而

- **C138 §三** 裁定檔名**永不改**（44 個檔提到它），
- **本則 §二** 裁掉唯一的遷移路線。

⚠️ **那個條件從「不可能響」變成「確定永遠不會響」**（C144 §五 記過前半）。
**一個永遠不會到期的到期條件，不是溫度計，是裝飾。**

⚠️⚠️ **所以 `C144 §一` 那份「這支 PR 新判的列」清單現在不完整** —— 它列了兩列，
而同樣那兩列今天被重判了一次。**這一節就是補上的地方**，照 C143 §七 的要求單獨標出來：

| 名字             | 從       | 到       | 依據                                           |
| ---------------- | -------- | -------- | ---------------------------------------------- |
| `DECISIONS.md`   | 過渡豁免 | **正典** | C135 §三 的定義逐字成立；到期條件確定永不響    |
| `DECISIONS-2.md` | 過渡豁免 | **正典** | 同上；C144 §一 拒絕正典的那個理由已被 §二 消滅 |

**分佈**：正典 6 → **8**、過渡豁免 2 → **0**、正交 21、無關 5，合計仍然 **34**。

#### 四、⚠️ `過渡豁免` 桶現在是空的，而值域不動

**下一個人最順手的動作是把它從 `check.ts` 的 `BUCKETS` 拿掉。不要。**

`BUCKETS` 是**判準的一部分**，往裡面加或減一個名字就是改閘門（`AGENTS.md` 規則二，
`check.ts` 的 `fix` 文字自己寫著這一句）。而且空桶**沒有壞任何東西**：

- 值域檢查問的是「這一格是不是那四個之一」，不是「四個各用過幾次」；
- C135 §三 對 `無關` 桶問過同一個問題（「小到可以合理質疑第四個桶值不值得」），
  結論是**留著**，理由是那幾列在三桶制底下無處可去。**同一個結論適用於這裡**：
  下一次有東西真的在遷移途中，那個桶要在。

⚠️ **實測，不是斷言**（逐支取結束碼，未經管線）：兩列改成 `正典`、`過渡豁免` 0 列之後，
`node tools/scope-check/src/cli.ts` → **0**，`pnpm ready` → **0**。
**閘門對「某個桶沒有人用」沒有意見** —— C143 §七 第 3 條那個「四個桶各至少一列」是
**夾具**上的斷言，不是對真樹的要求。

⚠️⚠️ **而上面那句「不要」守得住嗎？起草時我連著寫錯兩句斷言，兩句都被實跑推翻，
所以這裡只寫量到的（逐支取結束碼，變異後還原並回讀）：**

| 變異                          | `scope-check`（真樹） | `scope-check` 測試                                                                               |
| ----------------------------- | --------------------- | ------------------------------------------------------------------------------------------------ |
| `BUCKETS` **拿掉** `過渡豁免` | **0 —— 全綠**         | ⚠️ **1 條紅**：`③ 四個桶各一列、都填對就是綠的`                                                  |
| `BUCKETS` **加**一個 `雜項`   | **0 —— 全綠**         | ⚠️ **2 條紅**：`② 桶名不在那四個裡會紅`、`填錯桶名的訊息叫人改那一列，不是叫人往 BUCKETS 加一個` |
| 還原                          | 0                     | **40/40 綠**，`git diff --summary` 無模式變更                                                    |

⚠️ **兩句被推翻的斷言逐字記下來，因為它們錯的方式一樣。** 起草時我寫
「拿掉它是一支全綠的 diff」，改完又寫「加一個名字那條測試不會紅」——
**兩次都是從『真樹 0 列在用它』推出『所以沒有東西會紅』，而測試根本不看真樹。**

**量到的結論比推出來的強**：值域**兩個方向都有絆線**，而它們是 C143 §七 那幾條
**順帶**造出來的 —— ②、③ 與 `fix` 文字那條原本各自答別的問題，
**沒有人寫下來過它們同時在守 `BUCKETS` 本身**。這一則補上。

⚠️ **但真樹那一欄兩列都是 0**：`過渡豁免` 0 列之後，真的 `SCOPE.md` 對值域怎麼改
都沒有意見。**所以守它的是測試，不是閘門** —— 一個要跑 `pnpm ready` 才問得到的保證，
不是 `scope-check` 自己說得出口的話。

#### 五、`#88` 的 Destination 改寫，而原文存檔

`#88` 的 Destination 原文：

> **到達的定義：決策談完、寫進 ADR 就算到了。**

它與同一張票 Notes 第 4 條（「『寫成 ADR』——路線已改」）直接矛盾，
而 **Destination 是那張圖的驗收條件**，Notes 不是。本則落地時一併把它改成
「寫進 `DECISIONS.md`／續卷」，原文逐字存檔在票面上、並標出本則。

⚠️ **票面的改寫沒有任何機制在守** —— 它不在版控裡。寫在這裡，是因為
這一則的效力有一半落在 GitHub 上，而那一半只有這一句話記得。

#### 六、本則不裁的事

- **不裁 `docs/` 為別的用途能不能存在**（§二 末段），也不動 `reports/research/` 與它的分界。
- **不動 C135、C138**（凍結卷）。本則是它們的續集不是修正。
- **不動 D16、C137，不增減任何一道閘門，不改 `BUCKETS`**（§四）。
- ⚠️ **不重開 `#87`。** 它的兩個驗收條件今天仍然不成立（C138 §一），本則只拿掉它結案留言
  刻意留著的那個懸念，沒有讓它的工作回來。
- ⚠️ **桶填得對不對仍然沒有機制在守**（C143 §八 一字不改）。本則自己又是一個例子：
  兩列的桶翻面，而閘門從頭到尾 exit 0 —— **翻面前後它說的都是「填了、而且是那四個之一」。**
- ⚠️⚠️ **`AGENTS.md` 那一列的桶值今天是一個已知過寬的斷言，本則沒有解掉它。**
  C135 §四 裁的是「**只有 END 標記之後那半是正典**」，而桶欄是單值的表達不了；
  C144 §五 把散文格補回了那一半，**但被機器檢查的那一格仍然平白寫著 `正典`**。
  ⚠️ **所以那一列現在是散文說機器錯，而閘門永遠報綠** —— 正是 `parse.ts` 開頭
  講的 `tools/sast` 那個形狀（一個假的東西待在最會被讀的地方，全套閘門照樣全綠）。
  ⚠️ **C144 §五 把它列在「改了什麼」底下，讀起來像已經解決 —— 它沒有。**
  三條出路（散文格補一句／改判正交／值域加第五個桶）都在 #93 那一輪問過人，
  人選了第一條，**而第一條刻意不動值域**（改 `BUCKETS` 是改閘門，`AGENTS.md` 規則二）。
  要真的解掉它，需要一則新的裁決，本則不做。

### C146 — `AGENTS.md` 那一格沒有壞，而判錯它的是兩天前的 C144 §五 與 C145 §六（2026-08-30，#93 後續）

⚠️ **量測基準 `8df09db`**（C137 §五）。本則所有數字凍結在那一個 commit。

#### 一、兩種讀法，而分辨它們的既不是行數、也不是 C135 的表格位置

C135 §四 對 `AGENTS.md` 那一列的備註是：

> ⚠️ **只有 END 標記之後那半是正典**；標記之間由 `vp` 產生，是另一條軸，機制不准碰

**讀法一**（C144 §五／C145 §六 採的）：桶欄是單值的，而這一列有兩條軸，
所以那個 `正典` 是**已知過寬的斷言** —— 加欄位當下就丟了 C135 的一半。

**讀法二**（本則採的）：桶回答的是**這一個被登記的項目**落哪一桶，
而被登記的項目是**檔案**。`AGENTS.md` 這個檔承載那份契約，所以是正典；
備註那一句限定的是**理由的範圍**，不是第二個桶。

⚠️ **分辨它們的是這張表自己已經有的先例**，而這是任何人讀同一張表都查得到的：

| 列          | 桶   | 那一列自己寫的內部異質性                                      |
| ----------- | ---- | ------------------------------------------------------------- |
| `apps/`     | 正交 | 「⚠️ **目錄准許存在，底下的內容不治理** —— 各案會整個換掉」   |
| `features/` | 正交 | 同上                                                          |
| `reports/`  | 無關 | 「⚠️ **只有 `research/` 進版控**，其餘…由 `.gitignore` 忽略」 |
| `platform/` | 正交 | 「**底下有自己的清單**」                                      |
| `tools/`    | 正交 | 「**底下的清單在 `tools/gate-roster`**」                      |

**讀法一底下，這五列全部同樣「過寬」** —— `apps/` 底下的內容不在任何一條軸上，
比 `AGENTS.md` 的情形更極端。**而從來沒有人那樣讀過它們。**

**母體是 `git ls-files`**（`SCOPE.md` 那一節的引言自己寫著），
**粒度是目錄與根層檔，不是檔案的半邊。** 桶欄跟著母體走。

⚠️ **二階確認**：讀法一底下**沒有一個正確的值填得進去**。上一輪把出路列全了 ——
散文格補一句（不改值）／改判正交（反轉 C135，而且丟掉載重的那一半）／
值域加第五個桶（改閘門）／一格填兩個桶／拆成兩列（同一個路徑兩列，破壞 1:1 登記）——
**每一條都是壞的。一個讓格子填不出正確答案的讀法，誤認的是格子在問什麼。**

（次要佐證，不是判準：`AGENTS.md` 130 行，標記內 **27** 行、標記之後 **103** 行。
⚠️ **行數不能當判準** —— 它明天就會變，而讀法二不會。）

#### 二、那 C135 §四 那句話在做什麼

**它是理由的範圍限定詞，寫在它該在的欄。** C135 §四 的正典表有三欄：
名字／**承載什麼體系**／備註。`AGENTS.md` 那一列的「承載什麼體系」寫的是
「下發給採用團隊的 agent 契約（四條規則）」—— **桶的理由**；
而「只有 END 標記之後那半」寫在**備註**欄，限定那個理由涵蓋到哪裡。

⚠️ **C135 沒有給標記內那一半任何桶**，因為它的母體裡沒有「半個檔案」這種東西。

**這一節要寫下來，否則下一個人會從同一句話重新推出讀法一** —— 兩天前就有人這樣推過，
而那個人有 C135 全文在手。

#### 三、依 C136 §八：C145 §六 產出過什麼，重跑了沒有

⚠️ **五處，而其中兩處不在版控裡** —— 那兩處是最容易漏的，因為沒有任何 diff 會顯示它們。

| #   | 產出處                                                  | 在版控 | 處置                                                                                                  |
| --- | ------------------------------------------------------- | ------ | ----------------------------------------------------------------------------------------------------- |
| 1   | `SCOPE.md` 的 `AGENTS.md` 那一格（「已知過寬的斷言」）  | ✅     | **本則整格重寫**（§五）                                                                               |
| 2   | **C144 §五** 表格那一列（「這一格是加欄位造成的退化」） | ✅     | ⚠️ **不改** —— 有日期的裁決不就地改寫，由本則更正                                                     |
| 3   | **C145 §六** 那一條（「已知過寬…本則沒有解掉它」）      | ✅     | 同上                                                                                                  |
| 4   | 工作記憶 `v1-scope-criterion`                           | ❌     | 已改                                                                                                  |
| 5   | 工作記憶 `reformat-destroys-human-review`               | ❌     | ⚠️ 已改 —— 它把讀法一寫成**逐列重讀的可複用教訓**，而真正的教訓（重排會殺掉 diff 審查）不需要那個例子 |

⚠️ **PR #203／#204 的留言與 commit 訊息也帶著讀法一，本則不追改** ——
它們是當下的紀錄，改了就是改寫歷史。**看得懂的辦法是本則存在。**

⚠️⚠️ **這一節是 C136 §八 第一次真的抓到東西。** 前幾次（C138 §六、C143 §八）
觸發的都是「本則不推翻任何 C-條」那一半。**這一次要點名的產出散在五個地方，
而版控只涵蓋三個。**

#### 四、順手量到的：「機制不准碰」沒有任何東西在守 —— 而本則**不裁**它

C135 §四 那句備註的後半是「**機制不准碰**」，`CLAUDE.md` 也寫著手寫內容一律加在
END 標記之後。**實測有沒有東西在守它**（`grep -rlF`，逐支數檔案數）：

| 查什麼                             | 在 `tools/`／`platform/`／`.github/` 命中的檔案數 |
| ---------------------------------- | ------------------------------------------------- |
| `VITE PLUS`（標記本身）            | **0**                                             |
| **對照組**：`SCOPE.md` 在 `tools/` | **11**                                            |

**對照組非零，所以 0 不是量測假象** —— 沒有任何閘門知道那兩個標記存在。
⚠️ 而 `AGENTS.md` 是**全樹唯一**帶那組標記的檔（`CLAUDE.md`／`DECISIONS.md`
只是在散文裡提到它們，實測沒有一行是真的分隔符）。

⚠️⚠️ **本則不裁要不要補一道閘門。** 兩個理由：

1. **它不是這一列的桶問題。** 把一個閘門提案綁進一則關於表格某一格的裁決，
   正是 C143 §五 避開的那個形狀（一份文件一個守門人、一則裁決一個題目）。
2. **要提它得先過 `#187`／C142 的填寫要求** —— D16 迭代軸那一格要寫出
   「上游或內部發生什麼具體變動時，它會紅」，而那需要另開一張票。
   ⚠️ C137 §一 同時禁止用「過度設計」論證增或減，兩個方向都禁。

**本則只把它從「沒有人量過」變成「量過了，是 0」。**

#### 五、`SCOPE.md` 那一格整格重寫，而不是再加一條 ⚠️

那一格在本則之前疊了三條 ⚠️，是全表最長的一格 —— **在一個全部價值都是給人讀的欄位裡。**
再加第四條（內容是撤回第三條）會讓它更長而更難讀。

改寫後那一格說三件事，各一句：檔案是什麼、**桶是整個檔案的答案而理由是那份契約**、
**標記之間不准碰而沒有閘門在守**。
⚠️ **行數（27／103）刻意不寫進表** —— 散文裡的計數會過期，它們只留在本則的凍結基準裡。

#### 六、本則不裁的事

- **不裁 `AGENTS.md` 標記那道閘門**（§四），不開那張票 —— 要開是人的決定。
- **不改 `BUCKETS`、不增減任何一道閘門、不動 C135**（凍結卷）。
- ⚠️ **不改 C144 §五 與 C145 §六 的原文**（§三）。它們錯了，而更正的形式是本則。
- ⚠️ **桶填得對不對仍然沒有機制在守**（C143 §八 一字不改）。
  ⚠️⚠️ **而本則是它第一次真的出事**：那一格從頭到尾是對的，**紅的是人的判斷**，
  連續兩則裁決都判錯，而閘門三次都說綠。**「靠人讀 diff」不保證人讀對。**

### C147 — C119 的校準法是單向棘輪，所以改的是方向不是數字（2026-08-30，#93 後續）

⚠️ **量測基準 `2846949`**（C137 §五）。本則所有數字凍結在那一個 commit。

#### 一、缺陷：一個設在觀測最大值的門檻，永遠產不出收緊它所需要的論證

C119 定的方法是：

> 先設寬到不擋任何既有程式碼，**每一次它真的擋下一件事就記一則 C 編號**，
> 收緊時附上那些 C 編號當論證。

**那個方法有一個它自己看不見的迴圈**：門檻取觀測最大值 ⇒ 它擋下 0 件事 ⇒
**那些 C 編號永遠不會出現** ⇒ 永遠沒有論證可以收緊。而程式碼變差的時候，
觀測最大值上升，於是門檻**被往上校準**（有論證：新程式碼從來沒被量過）。

**只有往上那一邊走得通。**

⚠️ **這不是推論，是帳**。至今每一次記錄在案的移動都是往上，一次往下都沒有：

| 何時                           | 從  | 到      |
| ------------------------------ | --- | ------- |
| C133 §八（併線當天）產品碼行數 | 185 | **239** |
| C133 §八 產品碼 `complexity`   | 36  | **39**  |
| C133 §八 測試碼 `complexity`   | 11  | **15**  |
| C130 §八 那筆 per-file 放行    | 841 | **850** |

**實測今天的分佈**（八個門檻全部調到最低跑一次 `vp lint`，解析 5583/5583 條，
⚠️ 對照組：四條規則各自的計數和相符；量完還原並以 sha256 回讀）：

| 規則                     | 產品碼 n／中位／p90／p99／**max** | 現行門檻 | 今天擋下 |
| ------------------------ | --------------------------------- | -------- | -------- |
| `max-lines-per-function` | 475／11／52／148／**239**         | 239      | **0**    |
| `max-depth`              | 1156／1／3／4／**5**              | 5        | **0**    |
| `max-params`             | 674／1／2／4／**6**               | 6        | **0**    |
| `complexity`             | 358／4／12／29／**39**            | 39       | **0**    |

**四條規則，四個門檻，今天合計擋下 0 個函式。**

#### 二、裁決：觀測最大值是**上界，不是錨點** —— 門檻自動往下，往上要一則裁決

C119 的方法保留，加一條方向規則：

| 觀測最大值                         | 門檻怎麼走                                                                                              |
| ---------------------------------- | ------------------------------------------------------------------------------------------------------- |
| **下降**（有人重構掉了那支極端值） | ⚠️ **門檻必須跟著降到新的最大值。不需要論證** —— 它擋不下任何今天存在的東西，所以降它不會紅             |
| **上升**                           | **維持原判**。要抬就要一則 C 編號寫明為什麼那段新程式碼該被容許 —— 這一半 C119 已經有了，而且開火過四次 |

⚠️⚠️ **本則一個數字都不動，今天擋下的仍然是 0 個函式。** 改的是**方向性**：
把一個只會鬆的棘輪，換成一個**重構會被永久記帳**的棘輪。
一次把 `runFull` 拆小，換到的不只是那一次的乾淨，是門檻從此低在那裡。

⚠️ **「不需要論證」只適用於降。** 降不會擋下任何既有程式碼（定義上如此），
所以它不是 `AGENTS.md` 規則二 說的那種改動；**抬仍然要人裁**。

#### 三、考慮過而否決：門檻跟著 p95

| 選項                              | 代價                                                                                                                                                      |
| --------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **上界＋只准降（採用）**          | 今天零成本、零紅燈；⚠️ 但**沒有機制在守**（§四）                                                                                                          |
| 門檻改跟 p95（83／3／3／15）      | ⚠️ **CI 第一天紅約 72 個函式，而程式碼一行都沒改** —— C108 付過這筆學費（照抄外部建議值，紅的原因不是程式碼變差）。⚠️ 而且 **p95 一樣是棘輪**，只是慢一點 |
| 只收最極端的幾支（150／4／5／25） | 擋下 13 個函式 —— 要先把那 13 支重構或逐支寫放行，**那是真工作**，而本則不主張它                                                                          |
| 不動                              | 名義上四道防線，實際上只擋得住「比今天最壞的還壞」                                                                                                        |

#### 四、⚠️ 本則沒有機制在守，而本則**不裁**要不要加

「觀測最大值降了、門檻要跟著降」**沒有任何東西會提醒你**。它與 C143 §八、
C146 §四 是同一種：靠人。

**可機械化的形狀寫在這裡，好讓下一個人不必重新想**：一支檢查把八個門檻讀出來、
跟實測最大值比，**`實測 max < 設定值` → 紅**（訊息是「門檻過期了，調下來」）。
⚠️ 它的失敗方向是安全的：程式碼變好才會紅，而那種紅一行 config 就修掉。

⚠️⚠️ **但那是新增一道 meta 閘門**，所以要先過 `#187`／C142 的填寫要求 ——
D16 兩軸各報一格，迭代軸那格要寫出「上游或內部發生什麼具體變動時，它會紅」。
**本則不代替那張票做決定**，也不主張閘門太多或太少（C137 §一 兩個方向都禁）。

#### 五、本則不裁的事

- **八個數字一個都不動**，per-file 放行（`buildSliceFiles` 850）也不動。
- **不裁 `.vue` 那一格。** 它的分母不同 —— 四條規則在真 `.vue` 上只報得出 30 條
  （`.ts` 是 5549），而 31 個檔裡 **23 個一條都報不出來**。那一格要先答
  「元件的邏輯有多少在 `template` 裡」，不是先答一個數字（C120 §一 已經這樣裁過）。
  量測另行成稿。
- **不裁要不要加守它的閘門**（§四）。
- ⚠️ **不推翻 C119。** 本則是它的補丁不是它的替代：C119 的「抬要有 C 編號」原封不動，
  本則加的是它缺的另一半方向。依 C136 §八，本則不推翻任何 C-條，那一半不觸發。

### C148 — 退出演練的失敗分成兩類，而票上判它們的那三句話是錯的（2026-08-30，#206）

⚠️ **一手證據是 2026-08-30 那次 `pnpm exit-drill --full` 的完整輸出**（工作目錄
`exit-drill-MdqwaG`），不是 `#206` 的敘述。本則有三處與那張票不一致，以輸出為準。

#### 一、先更正票上三句話 —— 三句都錯，而其中一句把整個問題導到錯的方向

| `#206` 寫的                                                                       | 實際                                                                                                                                                                                                                                                                   |
| --------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 「失敗的 4 條 ＋ **6 個載不起來的檔**」                                           | **4 個載不起來**（`Failed Suites 4`），另外 **2 個載得起來、斷言失敗**。`Test Files 6 failed` 的 6 不是「載不起來」的 6                                                                                                                                                |
| 「3 支 UI 測試要 `@vitejs/plugin-vue`，**演練刻意不裝**，帳目寫著『明示丟棄 2』」 | ⚠️⚠️ **反了。`@vitejs/plugin-vue` 在 `DRILL_PLUGINS` 裡**（`plugins.ts:44`，演練**重現**它），`happy-dom` 與 `@vue/test-utils` 也**真的安裝**（`dependencies.ts` 的 `DRILL_TEST_DEPENDENCIES`）。「明示丟棄 2」指的是 `securityHeaders` 與 `assertStaticCspCompatible` |
| 「那 4 條斷言根 `package.json` 的 script 與 `.env`」                              | `proxy-target` 那 3 條炸在 `await import("../vite.config.ts")` —— **演練自己刪掉了那個檔**（`cli.ts:606`）。與 `.env` 無關（那支測試的 `.env` 是它自己寫在暫存目錄裡的）                                                                                               |

**第二句最貴**：它把一個真缺陷寫成「設計的後果」。照那句讀，`.vue` 那三支
是演練刻意不管的東西 —— 於是不會有人去看 `cli.ts:696`。

#### 二、五個失敗單位，兩類，而分類是從原始碼推出來的

| 失敗單位                                                                          | 類    | 成因                                                                                                              |
| --------------------------------------------------------------------------------- | ----- | ----------------------------------------------------------------------------------------------------------------- |
| `app/tests/dev-session-stripped.test.ts`（載不起來）                              | A     | `import { build } from "vite-plus"` —— 演練把 `vite-plus` 拿掉了                                                  |
| `app/tests/proxy-target.test.ts` 3 條                                             | A     | `import("../vite.config.ts")` —— 演練刪掉了那個檔（`cli.ts:606`）                                                 |
| `app/tests/bff-routes.test.ts` ★ 1 條                                             | A     | 讀根 `package.json` 的 `bff` script —— 演練產生自己的 `package.json`                                              |
| `packages/ui/tests/{alert-dialog,dropdown-menu,field-wiring}.test.ts`（載不起來） | **B** | ⚠️ **`cli.ts:696` 產生的 `vitest.config.mjs` 沒有拿 `DRILL_PLUGINS`** —— 建置那份（`cli.ts:683`）有，測試那份沒有 |

**A 類：演練把它要問的那個東西換掉了。B 類：演練的兩份設定不一致。**

⚠️ **B 正是 `plugins.ts` 檔頭寫著要防的那個失敗模式**（「兩邊分開寫的話，
總有一天會有人只改到其中一邊」），而它發生在那張帳目**自己的消費端**上：
帳目是對的，只有一個消費端讀了它。

#### 三、Q1 前半：**不**讓 `--full` 去認 `STATIC_ALLOWED` —— 那張表只涵蓋 A 類的五分之一

`STATIC_ALLOWED` 只有一筆（`cli.ts:104`），而 A 類有 5 個失敗單位。
照票上那個做法修，`proxy-target` 3 條與 `bff-routes` 1 條原封不動 ——
**修完仍然是紅的，而且會看起來像「剩下的是真缺口」。**

**判準不是那張表，是這棵樹已經在用的那一條，往下一層：**

> `cli.ts:610` 的推導：**可達＝在保證內**。`@org/eslint-config` 與 `@org/bff-mock`
> 被排除，理由是「lint 設定與 BFF mock 都不是應用的一部分 —— 退出 vite-plus
> 之後，你的 eslint 設定不需要用上游 Vite 建得起來」。

A 類是同一句話換一個層級：**退出 `vite-plus` 之後，一支問「`vp build` 有沒有
搖掉 dev session 入口」的測試不再有意義** —— 它問的是腳手架，不是應用。
三個被問的對象（`vite-plus`、`apps/console/vite.config.ts`、根 `package.json`）
**恰好就是演練替換掉的那三樣東西**，一個不多一個不少。

#### 四、粒度是**逐測試**，不是逐檔 —— 而這是 C146 的反面，判法一樣

`bff-routes.test.ts` 有 6 條，**1 條問腳手架、5 條問應用**（路由的形狀、
端點的載入端契約）。逐檔排除會把 5 條有效測試移出退出保證，
**而那正是「改到綠」躲得住的地方**：population 少了，成績照樣是綠的。

⚠️ **判法與 C146 相同：看被登記的單位是什麼。** C146 那次答案是「整個檔案」，
因為 `SCOPE.md` 登記的就是檔案與目錄；這裡答案是「一條測試」，因為演練
數的、寫進 `evidence.json` 的（`tests: 155`）、被 `checkDocumentedCounts`
拿去比對文件的，**全都是測試條數**。同一把尺，兩個相反的答案，而兩次都對。

#### 五、機制：**預期失敗帳**，雙向，不是排除清單

登記 A 類，但**不把它們移出 population**：

| 方向                         | 結果                             |
| ---------------------------- | -------------------------------- |
| 出現**沒登記**的失敗         | 紅（今天的行為，不變）           |
| 登記過的那一條**開始通過**   | ⚠️ **也要紅** —— 帳目過期了      |
| 同一個檔案裡**別的**測試失敗 | 紅（因為檔案照跑，沒有被排除掉） |

每一筆的理由必須寫出**演練替換掉的是哪一個東西**。沿用 `DROPPED_TEST_DEPENDENCIES`
的那句話：**「演練用不到」不是理由，「演練用得到但那個東西被演練換掉了」才是。**

⚠️ 選這個而不選排除清單，理由就是規則二：排除清單會讓 population 安靜地縮小，
而預期失敗帳讓縮小的部分**每一次都被念出來**，且過期時會紅。

#### 六、B 類是真缺陷，修它是**擴張**不是放寬 —— 這一節是規則二在本則的判法

⚠️ **收窄與擴張要用不同的標準審，因為「改到綠」只躲在收窄那一邊：**

| 動作                                     | 方向     | 要求                                                                                |
| ---------------------------------------- | -------- | ----------------------------------------------------------------------------------- |
| `vitest.config.mjs` 也吃 `DRILL_PLUGINS` | **擴張** | 不需要儀式 —— 那 3 個檔從「0 test」變成真的執行，演練從此能失敗在它以前碰不到的地方 |
| A 類進預期失敗帳                         | **收窄** | 登記表 ＋ 逐筆理由 ＋ 雙向紅（§五）                                                 |

#### 七、Q2 新鮮度：判準改成「這份證據是不是關於今天這棵樹」，機制另案

`cli.ts:128` 的 `FRESHNESS_DAYS = 120` 問的是「幾天前」。**裁定它問錯了問題**：
一份 08-16 量在 `release/v1` 那棵樹上的證據，在 08-26 併線之後量的是今天這棵的
**子集**，而它還有 106 天的效期。

⚠️ **本則只裁判準，不裁機制。** 要記哪個 hash、涵蓋哪些路徑、過期時是 warn
還是 fail、舊格式的 `evidence.json` 怎麼過渡 —— 都是還開著的設計選擇，
另一支 PR 處理。

**D16 兩軸報備（依 C142）：**

| 軸           | 內容                                                                                                              |
| ------------ | ----------------------------------------------------------------------------------------------------------------- |
| 交付軸       | R9 的退出保單是拿去跟採購與稽核講的話（同 `checkDocumentedCounts` 守的那個數字）                                  |
| **迭代軸 ①** | 對象在外：它報的是**退出保證**過期了，不是它自己被改壞                                                            |
| **迭代軸 ②** | 壞法安靜：⚠️⚠️ **這一格是量到的，不是判讀的** —— 併線讓樹變兩倍，而閘門在那之後連續 10 天印「✓ 退出演練證據有效」 |

⚠️ C142 §三 自陳那張表是判讀非量測。**本則的 ② 不是** —— 它有一個發生過的實例，
而那個實例的形狀正是 D16 樣本的形狀（空清單和全綠長得一模一樣）。
這是這棵樹第一次有一格 ② 是實測出來的。

> ⚠️ **上面那句序數不成立：C143 §六 的探針早它 15 小時，② 的三個級別見 C154 §五。**
> （本則其餘一個字不動 —— 更正的形式是新則，同 C142 §六；加這一行的體例沿用
> `DECISIONS.md` D16 兩軸表底下那一行。）

#### 八、⚠️ 本則產生一個義務：重跑演練、提交新的 `evidence.json`

今天的綠燈靠的是一份 **08-16 的 pass，人裁還原**（`#206` 自陳）。
在 §六 那個修落地之前，**這棵樹的退出保單是空的**，而它看起來是綠的。

**這一句寫在裁決裡，是因為它不寫下來就會變成無主的**：`#206` 關掉之後，
「重跑」這件事不屬於任何人 —— 而這棵樹剛剛才記過一次同樣的形狀
（C145 §二：守著留白的票全關了，留白就變成無人主張的預設）。

#### 九、⚠️ 更正的三句話產出過什麼（依 C136 §八）

**本則不推翻任何 C 條。** 被推翻的是 `#206` 票面的三句話，它們的產出處**兩個**，
兩個都不在版控裡：

| 產出處                 | 處置                                        |
| ---------------------- | ------------------------------------------- |
| `#206` 票面本身        | 本 PR 一併改寫，並在票上留言指向本則        |
| session 記憶（非版控） | 同步更正 —— ⚠️ 它抄了「演練刻意不裝」那一句 |

⚠️ **沒有第三處**：這三句話從來沒有進過樹上的任何文件，也沒有被任何閘門讀過。

### C149 — 樹指紋：機制的三個選擇，而其中一個是「刻意不擋人」（2026-08-30，#206）

C148 §七 裁了判準（「這份證據是不是關於今天這棵樹」）並把機制留成另案。
本則是那個另案，三個選擇逐一寫下來 —— **每一個都有一個看起來更嚴、
而實際上會讓這道檢查被繞過的替代方案。**

#### 一、涵蓋哪些路徑：演練真的會讀到的東西，不是整棵樹

| 方案                         | 為什麼不是它                                                                     |
| ---------------------------- | -------------------------------------------------------------------------------- |
| `git rev-parse HEAD`         | **任何一次 commit 都讓證據作廢** —— 改一個錯字就紅。`counts.ts` 檔頭記過這個下場 |
| 只涵蓋 `apps/console` 與套件 | 漏掉**量法**：有人改了演練產生設定的方式，舊證據描述的是另一支演練               |

**選的是兩類都收**：被量的（`apps/console`、可達套件、`platform/tsconfig`）
＋ 量法（`tools/exit-drill/src`、`pnpm-workspace.yaml` 的 catalog）。
`tools/exit-drill/tests` **不在內** —— 測試不改變演練怎麼跑。

⚠️ 「量法在內」不是對稱的漂亮話，它有一個具體的前科：**C148 §二 的 B 類**
就是有人改了設定產生方式（測試那份沒吃 `DRILL_PLUGINS`）而證據照樣有效。

#### 二、⚠️ 指紋對不上**只 warn，連 `--require-fresh` 都不 fail**

這一節是本則唯一可能被誤讀成「調鬆」的地方，所以理由是量出來的：

**實測（`6e6089b`，`git log --first-parent`）：最近 60 支 main commit 裡有
25 支動到涵蓋路徑 —— 42%。**
⚠️ 對照組：同一支查詢對 `DECISIONS-2.md` 回 11，對涵蓋路徑第一次回 **0**，
而那個 0 是假的（多路徑參數在同一個 Bash 區塊裡被吃掉了）——
單獨查 `tools/exit-drill/src` 立刻回 6 支。**本則的 25 是重量過的。**

`--require-fresh` 跑在**非 PR** 的 Tier 2（推 main ＋ 排程）。指紋對不上就 fail
的話，一支動到 `platform/ui` 的功能 PR 合進 main 之後，main 會一直紅到有人
重跑那場二十分鐘的季演練 —— **把每季一次的控制措施變成每次合併的阻斷器**。
`tier2-security.yml` 自己寫著那種閘門的下場：先被繞過，再被忽略。

**所以它的作用是讓那一行停止說它不知道的事，不是攔截。** 唯一會 fail 的是
`unrecorded`（舊格式、根本沒記指紋），而且只在排程上 —— 那是過渡狀態。

⚠️ **這留下一個沒有被機制守住的狀態：「沒有人量過現在這一棵」。**
有牙齒的版本存在 —— 把 `--require-fresh` 收窄成只在 `schedule` 觸發時跑，
然後讓它對 drift 也 fail。**本則明文不採用，也明文記下它**，
免得它變成無人主張的預設（C145 §二 的形狀）。

#### 三、清單走版控、內容走磁碟 —— 兩邊各取一半

⚠️ 這棵樹踩過「這把尺量的是 index 還是工作區」四次，所以兩邊都要說清楚：

| 取自           | 為什麼                                                                                                   |
| -------------- | -------------------------------------------------------------------------------------------------------- |
| 清單走版控     | 不然本機一個暫存檔就讓別人的閘門叫（演練會複製它，但它不該讓指紋變動）                                   |
| **內容走磁碟** | 演練複製的是磁碟上的內容。只讀 index 的話，**一個沒有 `git add` 的改動會讓檢查說「同一棵樹」而其實不是** |

**已知限制**：沒進版控、而演練會複製的檔案（未追蹤的新檔）不在指紋裡。
寫在 `tree-fingerprint.ts` 檔頭，不留給下一個人發現。

#### 四、`treeFiles` 是對照組，不是說明文字

⚠️ **一個涵蓋 0 個檔案的列舉，在兩邊會算出同一個空雜湊，然後「相符」**——
一個保證成立的假綠。所以 `compareFingerprint` 把 `files === 0` 判成錯誤
**而且排在比對雜湊之前**，即使兩個雜湊一模一樣。

這是這棵樹在「量測台回報假的零」上栽的第七次的預防，不是裝飾。

#### 五、實測驗過兩個方向

| 動作                                             | 結果                                  |
| ------------------------------------------------ | ------------------------------------- |
| `apps/console/src/features.ts` 加一個換行        | ⚠️ drift（`e88cd026…` → `cc6576f8…`） |
| 還原                                             | ✓ 相符，雜湊回到 `e88cd026…`          |
| **對照組**：`tools/exit-drill/tests/` 加一個換行 | ✓ 仍然相符 —— 涵蓋範圍確實排除了它    |

⚠️ 只驗「改了會紅」不夠：那條在**涵蓋範圍設得太寬**時也會過。
第三列才證明範圍是它宣稱的那個。

#### 六、本則不推翻任何 C 條（C136 §八）

C148 §七 明文把機制留給另案，本則是履行它，不是改它。
`FRESHNESS_DAYS = 120` 一個字沒動 —— 日期那一半的行為與 C64 裁的完全相同。

### C150 — 斷言強度的儀器：`expect(` 字面數量不到它要量的東西，而那個空格刻意不填（2026-08-30，#192）

#### 一、裁的是儀器，不是要不要有這道門檻

`#192` 記錄的是一個**儀器選擇**：如果有一天有人要量「這批測試有沒有真的在
斷言」，最便宜、最直覺的那支筆已經被實測證明量不到它要量的東西。

**本則只裁那件事。** 要不要填這個空格，見 §五 —— 那一格刻意留白，
而本則把留白的理由與代價寫下來，因為 `#192` 隨本則關閉。

⚠️ **本則不論證增加或減少任何一道閘門**（C137 §一）。它不動 `gate-roster`、
不動 `UNGATED`、不新增任何檢查。

#### 二、字面數分不出「參數化」與「掏空」—— 實測，不是推論

研究「`tools/` 測試碼能不能在分數不掉下少 30%」時，實驗室的第六道守衛用
`expect(` 的**字面出現次數**當分子，去擋「就地掏空」（把十二個 `it` 併成一個
`it.each` 而順手拿掉斷言）。**然後它駁回了一次合法的重構。**

| 版本                                     | `expect(` | `loc`       | 測試數 |
| ---------------------------------------- | --------- | ----------- | ------ |
| 對照組 —— 從通過的測試裡**拿掉兩行斷言** | 615 → 613 | 3507 → 3505 | 1451   |
| 實作 —— **合法的 `it.each` 併塊**        | 615 → 613 | 3507 → 3498 | 1451   |

**字面數的差值一模一樣，測試數兩者都不動。** 唯一的差別是 `loc` 掉得更多 ——
而減少 `loc` 正是當時的目標，所以**合法重構的「密度」必然比掏空更差**。

⚠️ 這是這棵樹反覆出現的同一個形狀（C137 §四）：**判準套在東西的「描述」上，
不是「實質」上。** `expect(` 是語彙記號，不是執行到的斷言。

#### 三、正確的儀器已經量過了

用一支一次性的 setup 檔（`expect.getState().assertionCalls`）在
`tools/vue-typecheck` 上量三個版本：

| 版本                     | 執行到的斷言  | `expect(` 字面數 | `loc` |
| ------------------------ | ------------- | ---------------- | ----- |
| 現況                     | **74**        | 615              | 3445  |
| `it.each` 併三塊（合法） | **74** ← 不動 | 613              | 3446  |
| 拿掉兩行斷言（掏空）     | **72** ← 掉了 | 613              | 3443  |

**執行次數分得開，字面數分不開。** 量完立刻還原，禁區指紋 118 條逐條驗回。

⚠️ 一個反直覺的量測結果順帶記著：合法的 `it.each` 併塊讓 `loc` **+1** ——
格式化器把區塊收成單行之後，`it.each` 的表格自己占的行數比省下來的多。
**「參數化會變短」是直覺，不是這棵樹上的事實。**

證據在 `reports/research/test-volume-30pct-2026-08-29.md` §四，量測台在
`reports/research/rigs/`。⚠️ 那個目錄**沒有機制在守**。

#### 四、接上它要動設定，所以停在這裡

`assertionCalls` 只能從 setup 檔裡讀，也就是需要 `test.setupFiles`。
**vitest 4 的 CLI 沒有 `--setupFiles` 這個選項**：

```
CACError: Unknown option `--setupFiles`
```

⚠️ **本則重驗過**（`vitest@4.1.10`，2026-08-30）—— 票上的斷言不是照抄的。

所以它**必須寫進 `vite.config.ts`**，而那是設定。依 `AGENTS.md` 規則二，
一個可以被受檢者自己調鬆的門檻量到的只是它自己；判斷門檻訂錯了要停下來
告訴人，不得自己改。`#192` 停在那裡是對的，本則不替它往前走。

#### 五、⚠️ 那個空格刻意不填，而守它的票隨本則關閉

樹上現在**沒有**斷言強度的門檻。生效的 oxlint 設定裡與斷言沾邊的只有
`typescript/no-extra-non-null-assertion` 與
`typescript/no-non-null-asserted-optional-chain`，兩支都與測試斷言無關。

**本則不裁要不要填它。** 那是 D16 兩軸的問題，而這棵樹上已經有一支儀器
（`stryker.config.mjs`，元層，不擋任何東西）在回答同一個問題的一部分。

⚠️⚠️ **但要說清楚一件事：`#192` 隨本則關閉，於是這個留白失去了守它的票。**
這棵樹記過這個形狀 —— **相依方一張一張關掉之後，「不裁」會變成「無人主張
的預設」**（同一句話在 C135 §一、C139 §四、C142 §五 都寫過）。所以把它明文
留在這裡：

> **「斷言強度要不要有門檻」沒有被裁過。** 沒有人主張它不該有，
> 也沒有人主張它該有。它是**未決**，不是**已決為不做**。

要動它的人需要的全部證據就在 §二 §三 兩張表裡 —— **而唯一被本則排除的是
一支筆，不是那道門。**

#### 六、⚠️ 驗這件事的方法本身有個坑，寫下來因為下一個人會重跑

本則要確認「生效的 oxlint 設定裡沒有斷言強度規則」，做法是 `--print-config`。
第一次跑回報的東西是**假的**：

| 跑法                                         | 規則數  | 對照組 `complexity` |
| -------------------------------------------- | ------- | ------------------- |
| `node_modules/.pnpm/…/oxlint --print-config` | 111     | ❌ **不在**         |
| `vp lint --print-config`                     | **154** | ✅ `max: 39`        |

兩個原因疊在一起：`node_modules/.bin/oxlint` 是**只給 IDE 用的 wrapper**
（直接印一行訊息就退出，不是 linter）；而繞過它去叫 `.pnpm` 底下的真 binary，
拿到的是 **oxlint 的預設集** —— 這棵樹沒有 `.oxlintrc.json`，規則寫在
`vite.config.ts` 裡，真 binary 看不到。

⚠️ **兩次跑出來的「含 assert/expect 的規則」是同一組**，所以結論沒有變 ——
**發現它的是對照組，不是結論**。這正是 `#192` 那張票自己選 `complexity`
當對照組的理由，而本則是那個對照組第一次真的擋下東西。

判 oxlint 有沒有某條規則，唯一可信的跑法是 **`vp lint --print-config`**。

#### 七、本則不推翻任何 C 條（C136 §八）

不動 C119（複雜度門檻）、不動 C120（覆蓋率門檻）、不動 C137。
沒有任何既有產出需要重跑。

### C151 — 三個代理不構成一個偵測器，而缺的那一格今天決定了全部（2026-08-31，#91）

⚠️ **量測基準兩個，因為本則是一個兩點對照**：run 1 凍結在 `313fe96`（C139），
run 2 凍結在 `0b777d8`，跑於 2026-08-30。本則所有數字凍結在這兩個 commit。

#### 一、裁什麼、不裁什麼，先擺出來

|          |                                                                                                                                                    |
| -------- | -------------------------------------------------------------------------------------------------------------------------------------------------- |
| **裁**   | `#91` 列的三個結構代理，即使每一個都量得出來，**合起來仍然不構成一個偵測器** —— 因為聚合規則從來沒有被定義過，而 run 2 的判決整個掛在它上面（§三） |
| **不裁** | 聚合規則該是哪一條、② 的「大量」門檻該訂多少                                                                                                       |
| **不裁** | 「自動偵測不可能」—— C139 §四 的理由一字不改                                                                                                       |

⚠️ **第二列是刻意的，不是省略。** `#91` 還開著，而從一則 C-條裡替另一張票
決定它的參數，是**替一張還開著的票加一條它沒裁過的規定**（C142 §五、
C135 §一：建議可以寫「新位置」，裁決不能）。

**`#91` 原問題的三個選項沒有一個被本則淘汰。** 今天拿掉的是另一條捷徑：
**「定義好三個代理就等於定義好偵測器」。**

#### 二、兩點對照

| 代理               | run 1 `313fe96` | run 2 `0b777d8`                 | 已知正對照：第一卷今天 | 翻面了嗎 |
| ------------------ | --------------- | ------------------------------- | ---------------------- | -------- |
| ① 內部連號條目     | ❌ 0            | ✅ **12**（C139–C150 連號無洞） | ✅ 161                 | **翻了** |
| ② 被大量交叉引用   | ❌ 2 檔         | ❌ **4 檔**                     | ✅ 39 檔               | 還沒     |
| ③ 自稱唯一事實來源 | ❌ 沒有         | ❌ **沒有** —— 開頭那句原封不動 | ✅ 有                  | 還沒     |
| **它實際上是什麼** | 續卷            | **續卷 —— 沒變**                | 正典                   | —        |

> ⚠️ 「161」與「12」是 **`###` 編號標題數**，不是「則」數 —— `R` 是一張九列的
> 表、沒有 `###` 標題，`D14` 佔兩個。拿 C 的體例去數 R 在這棵樹上已經獨立錯過
> 三次（C140）。

第二卷這段期間長了 **+1440 −3** 行，107 → 1467 行。

#### 三、判決整個掛在一個沒有人定過的參數上

C139 §五 的驗收條件是「兩次都必須判它不是平行體系」。run 2 是 **1/3 命中**：

| 聚合規則     | run 2 判 | 驗收條件      |
| ------------ | -------- | ------------- |
| **任一命中** | 平行體系 | ❌ **不通過** |
| 多數決       | 非平行   | ✅ 通過       |
| 三個全中     | 非平行   | ✅ 通過       |

**`#91` 從來沒有定過聚合規則，而今天它決定了全部。** ② 的「**大量**」同樣
沒有門檻 —— 4 對 39，臨界值訂在 3 就命中，訂在 5 就不命中，而沒有任何東西
在決定它。

⚠️ 所以 run 2 量出來的不是「一個答案」，是**答案還沒有被定義**。

#### 四、代理不同時翻面 —— 所以不存在「穩定的那一刻」

C139 立的是「聚合起來的答案會隨時間翻面」。本則多一層：**① 已經翻了，
②③ 還沒。** 三個代理各有各的時鐘。

⚠️ 而這正是**中段跑比等它長滿再跑更有資訊**的理由：等三個都翻了，看到的是
齊刷刷的「命中」，**脫節這件事就看不見了**。

#### 五、⚠️ ③ 那一格：C139 判的是語意，而那把尺不可機械化

C139 §二 判 ③ 不命中的依據是**語意** —— 這個檔開頭就寫「第一卷是
`DECISIONS.md`」。run 2 把它實作成三個詞的**字串比對**，而兩把尺在 `313fe96`
**都回 0**，於是對照組看起來通過了。**那是巧合，不是同一個問題。**

把那把錯尺跑 run 2，命中 **2 次**，兩次都在 `DECISIONS-2.md` 自己身上：

- `:36` —— C139 §一 **引述本票**列出的三個代理
- `:47` —— C139 §二 那張表裡寫著「自稱唯一事實來源 ❌ 沒有」的**那一列**

> **一份記錄「我不是平行體系」的裁決，讓偵測器判它是平行體系。**

對照組同向：第一卷唯一那次命中是「唯一事實來源是 `evidence.json`」——
一句關於**別的檔案**的話。

⚠️ 這是判準套在東西的**描述**上、不是**實質**上的又一個標本
（C137 §四、C150 §二 同一個形狀），而且是**最便宜的實作方式**踩出來的。

#### 六、「兩半是不是都活著」有形狀，⚠️ 而最直覺的量法給相反的答案

C139 §四 引 C138 §五「差別在邊界有沒有日期、以及**兩半是不是都活著**」，
並判後半「是意圖，不是形狀」。這一例上它有形狀，**但要挑對尺**：

| 量法               | 第一卷             | 第二卷       | 判                           |
| ------------------ | ------------------ | ------------ | ---------------------------- |
| commit 數          | **2**              | 12           | 兩半都活著 → **平行體系** ❌ |
| **新增編號標題數** | **0**（161 → 161） | 12（0 → 12） | 舊卷已凍結 → 世代交替 ✅     |
| 改動行數           | +3 −0              | +1440 −3     | 世代交替 ✅                  |

同一個檔、同一段期間、兩把尺、**相反的答案**。第一卷那 2 支 commit
（`d4c4925`、`9659bb0`）加的是**指標行**，不是條目。

⚠️⚠️ **這一格請照字面讀，不要放大。** 它成立是因為 **C138 明文凍結了第一卷**。
一個安靜地不再往舊那半寫的平行體系，在這把尺下**長得一模一樣**。所以能講的
只有「這一例有形狀，而最直覺的量法給反的」，不是「這半可以自動判定」。

#### 七、⚠️ C139 §五 那一格沒有被本則兌現

第二卷今天約長到第一卷的 **10%**（標題 12/161、行數 1467/13345、被引 4/39）。
這是**中段**，不是 C139 §五 說的「長起來之後」。**那條驗收條件仍然敞著，
還欠一跑。**

本則能做的只有讓它的到期條件說得出口。資料支持的觸發點只有一個：
**② 的被引用數逼近對照組的量級**（今天 4，對照 39）。

⚠️ **本則不設任何機制去守它** —— 與 C139 §五 同一條理由（明知不可靜態驗證的
東西，寫成規則、寫明它敞著，不要假裝有機器在守）。

⚠️ **而本則自己就在移動代理 ①**：加上這一則，`###` 編號標題數從 12 變成 13，
行數也不再是 1467。**正文裡的數字會被寫下它的那次 commit 改掉** —— 這正是
上面每一節都先釘住基準 commit 的理由。

> ⚠️ **後續（2026-08-31）：上面那個觸發點已由 C152 §六 收回，而 C152 不提替代**
> —— C139 §五 的到期條件因此回到「說不出口」。**本節其餘部分不受影響**，
> 包括「今天是中段」這個判定，以及這一段反身註記本身。

#### 八、與 C139 的關係（C136 §八）

**本則不推翻 C139 的任何一節，它加強 C139。** §五 那一格看起來像在更正
C139 §二 的 ③ 欄，其實不是：C139 §二 的判定**成立**，本則指出的是那個判定的
**依據是語意**，而語意這把尺換成機械實作之後量的是別的東西。

C139 §五 是 C139 的產出，本則說明它**還沒被兌現**，不是重跑它。
不動 C135、C137、C138、C140、C150。

### C152 — 三個代理與它們要分辨的兩種東西同形，而只有一個有解藥（2026-08-31，#91）

⚠️ **量測基準 `961e237`**（C151 併入之後）。本則所有數字凍結在那裡。

#### 一、裁什麼、不裁什麼，先擺出來

|          |                                                                                                                                                                                                |
| -------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **裁**   | `#91` 列的三個結構代理，在**這個對象**上與它們要分辨的兩種東西**同形** —— 每一個的讀數（命中或未命中），來源都是「它已被正確納入正典體系」，而「另立一套正典」在同一把尺上會給出**同一個讀數** |
| **裁**   | 三個之中只有 ① 找得到一個不同形的位置，而那個位置**已經寫在樹上了**（§五）                                                                                                                     |
| **不裁** | 聚合規則該是哪一條、② 的「大量」門檻該訂多少 —— C151 §一 不裁的，本則一樣不裁                                                                                                                  |
| **不裁** | `#91` 三個選項的取捨、① 的那個判準**要不要採納**                                                                                                                                               |
| **不裁** | 「自動偵測不可能」—— C139 §四 的理由仍然一字不改                                                                                                                                               |

⚠️ **本則不論證增加或減少任何一道閘門**（C137 §一），也不新增任何檢查。

#### 二、三種同形，逐個判 —— ⚠️ 而 ③ 的方向與 ①② 相反

| 代理               | 這個對象的讀數                      | 讀數的**來源**                                | 「另立正典」在同一把尺上                       | 同形發生在     | 有解藥嗎     |
| ------------------ | ----------------------------------- | --------------------------------------------- | ---------------------------------------------- | -------------- | ------------ |
| ① 內部連號條目     | ✅ 命中（C139 起連號無洞，12 → 13） | **體例繼承** —— 卷二照卷一的體例寫            | 也會有連號條目 —— 任何有紀律的紀錄都會有       | **命中**方向   | ✅ 有（§五） |
| ② 被大量交叉引用   | ❌ 未命中，但在長（2 → 4 檔）       | **治理它的機制** —— 增量 100% 來自閘門（§三） | 也會被交叉引用，而且同樣是閘門先引到它         | **命中**方向   | ❌ 沒找到    |
| ③ 自稱唯一事實來源 | ❌ 未命中 —— 檔頭明寫指向卷一       | **有人寫了一份謹慎的檔頭**                    | 同一批人真的另立一套，也會寫一份一樣謹慎的檔頭 | **未命中**方向 | ❌ 沒找到    |

⚠️ **③ 那一列請照著「未命中」讀。** ①② 是「命中了，但命中不代表它是平行體系」；
③ 是「**沒**命中，而沒命中一樣不代表它不是」。③ 的 ❌ 量到的是**寫檔頭的人夠謹慎**，
不是這份文件的性質 —— 而謹慎的人另立一套正典時，會寫出同一份檔頭。

**這一節不是在說代理量錯了。** 三個都量對了。問題在於它們量到的東西，
在兩種答案上長得一樣。

#### 三、② 扣掉治理它的機制之後，run 1 到今天一動也沒動

| 引用卷二的檔案（不含它自己） | run 1 `313fe96` | run 2 `0b777d8` | 今天 `961e237` |
| ---------------------------- | --------------- | --------------- | -------------- |
| 全部                         | 2               | 4               | 4              |
| 其中**治理它的閘門檔**       | 0               | 2               | 2              |
| **扣掉之後**                 | **2**           | **2**           | **2**          |
| 對照：卷二自己的長度         | 107 行          | 1467 行         | 1588 行        |

⚠️ **數字相同還不夠，成員也相同**：三次的那兩個檔案都是 `DECISIONS.md`（卷一
指向卷二的那一句）與 `SCOPE.md`（根層登記表的那一列）。這棵樹已經記過
「數字對得上不代表問的是同一個問題」（C151 §五），所以這裡比的是**成員**。

那兩支閘門檔是 `tools/doc-facts/tests/decision-ids.test.ts:154`（斷言決策檔就是
這兩份）與 `tools/scope-check/src/check.ts:80`（註解 `SCOPE.md` 為什麼多一列）。
**兩支都是為了管住卷二才提到它的。**

於是：卷二在這段期間長了 **14 倍**（107 → 1588 行），而「不是為了治理它而引到它」
的檔案數 **一個都沒有增加**。

#### 四、詞彙：為什麼叫「同形」，不叫「污染」

「污染」是**歸因**，不是描述 —— 它預設有一個乾淨的版本，後來被弄髒了。
這裡沒有那個乾淨的版本：兩種答案在這三把尺上**本來就長一樣**，
從第一次量之前就是。

也不叫「治理痕跡」：那個詞蓋不住 ①。① 的來源是**體例繼承**，
在任何閘門碰到卷二之前就成立了。

「同形」只講一件事，而那件事是可檢查的：**兩個不同的輸入，在這把尺上映到同一個輸出。**

#### 五、① 的那個位置已經寫在樹上了 —— ⚠️ 而本則不採納它

① 唯一不同形的位置是**編號的起點**：

> 從 `1` 重新開始 → 平行體系。接續正典的最後一號 → 續卷。

⚠️ **這不是本則提出來的判準。** 它逐字寫在 `DECISIONS-2.md` 的檔頭裡：

> ⚠️ **編號連續，不重新開始。** 下一則是 **C139**。

那一行是 C138 分卷時寫的，當時**沒有人知道它會是一個判別式**。
所以本則做的是**指出它已經在那裡**，不是發明一個新位置。

⚠️⚠️ **但本則不採納它當偵測判準。** C135 §一 那條線：**建議可以寫「新位置」，
裁決不能。** 要不要把它接成 `#91` 的判準、以及它跟另外兩個代理怎麼聚合，
是 `#91` 的事，不是本則的。

⚠️ 而它**只解 ①**。②③ 本則沒有找到不同形的位置 —— 那不等於沒有，
只等於今天沒找到。

#### 六、收回 C151 §七 的到期觸發點，而**不提替代**

C151 §七 寫的是：

> 資料支持的觸發點只有一個：**② 的被引用數逼近對照組的量級**。

**那個觸發點不成立。** §三 顯示 ② 的增量 100% 來自治理它的閘門 ——
所以那個數字接近對照組的那一天，量到的是「有多少閘門在管它」，
不是「它有多像正典」。**它會在被觸發的那天讓人以為量到了什麼。**

⚠️ **本則不提替代觸發點。** 提一個就等於替 `#91` 定聚合規則，
而 §一 明文不裁那件事。

**後果照實寫**：C139 §五 那條驗收條件的到期條件，回到 C151 之前的狀態 ——
**說不出口**。一個空白比一個假的觸發點安全。

⚠️ 這個空白**由 `#91` 守著，而 `#91` 開著**。這棵樹記過相反的情況：
相依方一張一張關掉之後，「不裁」會變成「無人主張的預設」
（C135 §一、C139 §四、C142 §五、C150 §五）。**關 `#91` 之前先讀這一節。**

#### 七、與 C151、C139 的關係（C136 §八）

**C151 §二 那張表本則不推翻。** 它是照 `#91` 票面寫的三把尺量的，
在那把尺上成立。本則換的是**追問讀數的來源**，不是換尺。分開講：

| C151 §七 的哪一部分            | 本則怎麼處置                                       |
| ------------------------------ | -------------------------------------------------- |
| ② 的**讀數**（2 檔 → 4 檔）    | **成立，不動** —— §三 是在它上面往下追，不是更正它 |
| 從那個讀數推出的**觸發點**     | **不成立**（§六）                                  |
| 「今天是中段，不是長起來之後」 | 不受影響 —— 仍然是中段                             |
| 「本則自己就在移動代理 ①」     | 不受影響，而且**本則又移動了一次**（13 → 14）      |

C151 §七 已加一行指向本則，**原文一個字沒動**。C151 的其他產出不需要重跑。

⚠️ 本則重量過 C151 §二 的 ①② 兩列與 ① 的對照組（161）。**② 的對照組沒有對上**：
C151 §二 記 39，本則同法在**同一個 commit**（`0b777d8`）量到 **41**。commit 相同，
所以差的那 2 個檔案是**量法**不是成長 —— 而 C151 的量法無法從記錄裡重建。
**本則不判它對錯，因為本則的論證不用它**（§六 收回那個觸發點，靠的是 ② 的
**來源**，不是對照組的值）。

⚠️ 記在這裡的理由：下一個人重量會遇到同一個差值，而**「沒有人注意到」比「差 2」貴**。
這正是 C151 §五 那一節的形狀 —— 兩把尺在同一個基準上給出的數字，不保證它們
問的是同一個問題。

> ⚠️ **本段是就地更正（2026-08-31，`bb811f1` 之後）。原文逐字存檔：**
> 「⚠️ 本則重量過 C151 §二 的 ①② 兩列與 ① 的對照組（161）；**② 的對照組沒有重量**，
> 因為本則的論證不用它。」
> —— 那一句是假的：對照組**量過**，量了兩次，只是沒有被採用。
> 寫下它的那一輪把「沒有採用」寫成了「沒有量」。

不動 C135、C137、C138、C139、C140、C143、C150。

### C153 — `#88` 的驗收條件指名一套樹上已經裁掉一半的標準，而那個 ❌ 分不出兩種相反的來源（2026-08-31，#88／#91／#93／#90）

⚠️ **量測基準 `67ed365`。** 本則所有數字凍結在那一個 commit。

#### 一、裁什麼、不裁什麼，先擺出來

**裁三件事**：`#88` 前半的驗收判準怎麼窄化（§二）、它與那套標準的差異表怎麼寫、
放哪一類位置（§三、§四）、以及這張圖的到達條件怎麼拆與怎麼移交（§五）。

**不裁**：`#91` 的聚合規則與三個選項（C152 §六 刻意留白，本則一個字都不碰）、
`docs/` 為別的用途能不能存在 —— `docs/adr/` 那一條已由 C145 §二 裁掉，
其餘用途仍要走 C93 那一關（C135 §一、C136 §十、C138 §五、C143 §八、C145 §六
逐則刻意留白）、
`AGENTS.md` 那一列的桶值（C145 §六 末段，仍未解）。

⚠️ **本則不論證增加或減少任何一道閘門**（C137 §一）。不動 `BUCKETS`、
不動 `gate-roster`、不動任何門檻、不新增任何檢查。

#### 二、原判準量不到它要量的東西 —— 因為它指名的那套標準，樹上裁掉了一半

`#88` 的 Destination 前半原文：

> 一套**可重複叫用的審查機制**，能判定這個 repo 的專案紀錄有沒有照
> `/setup-matt-pocock-skills` 的方式歸納

把那支 skill 讀完（`mattpocock-skills@1.2.3`），它產出五樣東西：

| 它會建立                                                | 這棵樹的狀態                                               |
| ------------------------------------------------------- | ---------------------------------------------------------- |
| `CONTEXT.md`（Section C 單一 context，**不問就寫**）    | `#91` 那一輪（C152）明文排除                               |
| `docs/adr/`（同上，不問就寫）                           | **C145 §二 裁定不造**                                      |
| `docs/agents/issue-tracker.md`                          | `docs/` 為別的用途能不能存在，**從沒人裁過**（C93 那一關） |
| `docs/agents/domain.md`、`docs/agents/triage-labels.md` | 同上（`triage` 有裝，Section B 會跑）                      |
| `CLAUDE.md` 加一個 `## Agent skills` 區塊               | `CLAUDE.md` 存在 → 它改這一份                              |

**於是拿原判準去量今天這棵樹，答案永遠是 ❌，而那個 ❌ 分不出兩件相反的事：**

> 「沒照著做**因為亂**」　與　「沒照著做**因為裁過了**」

⚠️ **這是 C152 §二 那個形狀的第四個實例** —— 一個讀數，兩個相反的來源映到同一格。
差別在來源：C152 那三支代理是**結構特徵**同形，這一支是**判準指名的標準本身**
已經被這棵樹部分否決，所以它的 ❌ 從第一天起就沒有分辨力。

**判準窄化成**：

> 能判定這個 repo 的專案紀錄有沒有**一套自己的歸納方式**，以及
> **說不說得出它與 `/setup-matt-pocock-skills` 的差異在哪、為什麼**。

⚠️ **窄化不是放寬。** 原判準的 ❌ 恆為真所以不帶資訊；窄化後的問法答得出
「有／沒有」，而**差異表本身就是那個答案**（§三）。

#### 三、差異表：五樣產出逐項，加上接線那一列

| 那套標準要的                     | 這棵樹的做法                                | 依據                          |
| -------------------------------- | ------------------------------------------- | ----------------------------- |
| `CONTEXT.md`（詞彙表）           | **不造。** 詞彙散在裁決裡，沒有獨立的詞彙表 | C152 那一輪的範疇             |
| `docs/adr/`（一決策一檔）        | **不造。** 決策一律進 `DECISIONS.md`／續卷  | **C145 §二**                  |
| `docs/agents/issue-tracker.md`   | **不造。** 開票流程沒有寫下來的設定檔       | C93（`#94` 撤出的正是這一份） |
| `docs/agents/domain.md`          | **不造。** 同上                             | C93                           |
| `docs/agents/triage-labels.md`   | **不造。** 見下面那一列                     | C93                           |
| `CLAUDE.md` 的 `## Agent skills` | **不加那個區塊**，改為一句指向本則（§四）   | 本則                          |

⚠️ **接線那一列要講的事跟直覺相反，措辭寫死在這裡**：

> 五個標準 triage 標籤 **已經建在這個 repo 上** —— `needs-triage`、`needs-info`、
> `ready-for-agent`、`ready-for-human`、`wontfix`，描述是中文、寫著「（triage 狀態）」。
> 但**流程沒有接**：`triage` skill 要 `docs/agents/triage-labels.md` 才知道標籤字串，
> 而那份檔案不在樹上。實際在用的是 `wayfinder:*` 那一套（`map`／`grilling`／
> `research`／`task`）；那五個標籤**總共只用過 3 次**（實測，含已關閉的票）。
>
> **所以「沒接線」不等於「沒關係」** —— 一個看到那五個標籤的 agent 會合理推論
> `triage` 是這棵樹的流程。這一列擋的是**那個推論**，不是「我們沒裝那套 skill」。

⚠️ **不接線的理由是代價比**：要接就得先有 `docs/agents/triage-labels.md`，
也就是**先撬開 `docs/`** —— 而那一格 C135 §一、C136 §十、C138 §五、C143 §八、
C145 §六 逐則刻意留白，每一次都寫下了理由。
為一份標籤對照檔去撬開它，而要對齊的詞彙這棵樹實際上沒在用，不成比例。

#### 四、⚠️ 為什麼這一句不放 `AGENTS.md` —— C109 §七 守的就是這個後門

`AGENTS.md` 是每個 session 都在上下文裡的那一份，直覺上該放那裡。**不行。**

C109 §七 履行的是 C93 §二 的舉證責任：

> 判定時必須寫得出「受益者是拉 v1 的團隊」那一句。寫不出來就不准進。

而它同時記下 `#94` 被撤的分界：`docs/agents/issue-tracker.md` 是**維護者的流程**
所以出局，`AGENTS.md` END 之後那節是**下發給採用團隊的 agent 契約**所以進得去。
**分界是內容在說什麼，不是在哪個目錄**（C93 §三）。

拿這把尺量本則這一句：它講的是**這個 repo 自己**的紀錄體系。拉 v1 去接案子的
團隊有他們自己的決策日誌，沒有人要求他們用 `DECISIONS.md`。**「受益者是拉 v1 的
團隊」那一句寫不乾淨** —— 而 C93 §二 明寫**沉默不是預設通過**。

所以落在 **`CLAUDE.md`**，理由是正面的而不只是排除法：**危害的源頭是 Claude Code
的外掛 skill**（`domain-modeling` 每次載入都寫著「create `docs/adr/` when the first
ADR is needed」），Codex／Cursor 不載入 mattpocock 那套。它確實是
「Claude Code 專屬的東西」—— `CLAUDE.md` 自己寫的用途。

⚠️ **它不是「複製規則」**（`CLAUDE.md` 明文禁止的那件事）：它不是 `AGENTS.md`
某一條的副本，是一件那邊沒有的新事。`CLAUDE.md` 已在 `SCOPE.md` 根層表上，
不需要新登記，`scope-check` 不受影響。

⚠️⚠️ **換到的東西要說清楚：這是一句話，不是閘門。** 沒有任何檢查會在有人跑
`/setup-matt-pocock-skills` 或照 `domain-modeling` 造 `docs/adr/` 的時候變紅 ——
`scope-check` 會在**加了頂層 `docs/` 而沒登記**的時候紅（#180／`b745d6e`），
但那是事後、而且它問的是登記不是路線。這一句與 `AGENTS.md` 規則四同一種，
而規則四自己就寫著「**這一條靠的是你**」。

#### 五、到達條件拆成兩半：一半達了，一半移交

`#88` 的 Destination 後半是「有沒有**殘留**或**新長出**的平行管理體系」。
兩半的狀態差很多，而票面把它們綁成一個條件，於是整張圖跟著最慢的那半走。

| 半邊       | 狀態     | 依據                                                                                                                                                           |
| ---------- | -------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **殘留**   | **已達** | C135 四個桶 → C143 §四 進 `SCOPE.md` 根層表 → `scope-check` 於 #180 上 tier1。今日實測 exit 0、零違規；根層表 **34 列**（正典 8／正交 21／無關 5／過渡豁免 0） |
| **新長出** | **未達** | 就是 `#91`。C152 §二 裁定三支結構代理**全部同形**，沒有偵測器；三個選項一個都沒選                                                                              |

**處置：`#88` 不等 `#91`，把後半連同到達條件移交過去。**

⚠️⚠️ **移交出去的條件由誰守 —— 現在明文寫下來，因為它已經是無主的一步之遙。**
`#88` 關了、指著 `#91`，而**「`#88` 的後半住在 `#91`」這件事只寫在一個 GitHub
票面上** —— 正是本則 §七 剛記錄了三次不受版控、無人守的那一類文字。所以：

> **`#91` 關閉時，必須明文交代它承接的那個條件（「能不能判定這棵樹有沒有
> 新長出的平行管理體系」）是達成了、還是再移交給誰。** 不得只答原本那三個選項。

⚠️ 沒有這一句，`#91` 關掉的那天那個條件會變成**無人主張的預設** ——
C135 §一、C139 §四、C142 §五、C150 §五 記過四次的同一件事。
**本則不替 `#91` 選答案，只要求它答。**

⚠️ **理由是「等」等不到**：`#91` 的那個留白是 C152 §六 **刻意不填**的 ——
提替代方案就等於替它選了聚合規則。把 `#88` 的關票條件綁在一個**設計上就沒有
到期日**的空格上，等於讓這張圖永遠開著。移交給 `#88` 一個它自己控制得了的
關票條件：§二 §三 落地、§六 三條留白裁完，就關。

⚠️ **移交要寫在 `#91` 的票面上，不是留言** —— 承接的是**驗收條件**，
留言會被往下捲掉。`#91` 今天站不住：票面沒寫它承接了什麼，也沒寫殘留那半已達。
**標籤不動** —— `wayfinder:map` 的意思是它底下有票，而 `#91` 沒有子票，
改標籤是斷言一個不存在的結構。

#### 六、四個留白，三個的前提死了，而它們死的方向不一樣

| 留白                                    | 判定             | 死因                                                                                                                                                            |
| --------------------------------------- | ---------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| ① 過渡豁免怎麼在 `#87` 完成時拿掉       | **死**           | `#87` 是 `NOT_PLANNED`（2026-08-29），「`#87` 完成」不會發生 → 沒有掛鉤可掛；而那個桶今天 **0 列**（C145 §三 把 `DECISIONS.md` 翻成正典）→ 沒有東西要拿掉       |
| ② 要不要跟 `/triage`、`to-tickets` 接線 | **活，本則裁掉** | 見 §三 末段。明文**不接線**                                                                                                                                     |
| ③ 兩棵文件樹收斂時機制會不會要改        | **死**           | 併線 2026-08-26 就做完了（C133／#176）。而機制（`SCOPE.md` 桶欄，C143／2026-08-30）是**併線之後**才設計的，從來沒有面對過兩棵樹 —— 問題問不出來了               |
| ④ C 編號從 C70 起分岔                   | **死**           | 實測：`DECISIONS.md` 上 C70–C74 留原號、`main` 那批已是 C128–C132，號碼不再撞。⚠️ 而 `#88` 的 Notes 第 2 條**自己已經寫了「編號分岔已經解決」** —— 票內自相矛盾 |

⚠️⚠️ **① 的死因必須反過來說死，否則它會被讀成相反的下一步。**
「過渡豁免 0 列」**不是**「該從 `BUCKETS` 拿掉它」的訊號 —— C145 §四 已明文
（空桶不是那個訊號，`BUCKETS` 是判準的一部分），而且 `BUCKETS` 是閘門的值域，
動它是動門檻：`AGENTS.md` 規則二說判斷門檻訂錯了要**停下來告訴人**，不得自己改。
**本則不動它。**

⚠️ **① 的另一個前提沒有死，它更成立了。** 原文寫「機制在 `DECISIONS.md` 還有
439KB 的時候不可能通過」—— 今天是 **848K**（卷一）＋ **124K**（卷二）。
那句話比當初更真，只是它現在不再支持原本那個結論。**這一句要留著，
不能跟著死因一起刪。**

#### 七、`#88` 票面**六處**指令的原文逐字存檔 —— ⚠️ 而其中兩處是全文掃描才出現的

處置判準用這棵樹自己的（C136 §八）：**指令（會被照做）→ 就地改票面；
陳述（會被相信）→ 不改，貼留言。** 存檔落在這裡而不是票面上，
理由也是 C136 §八 的：**GitHub 票面不受版控、改了沒有 diff 可讀**；
而這次 `#88` 是**關票**，關掉的票沒有人會回去讀存檔區塊。

**其一 —— Destination 底下那一段，它現在指著自己的存檔：**

> ⚠️⚠️ **這一句是本票唯一還指著 `docs/adr/` 的文字，而它與自己 Notes 第 4 條
> （「『寫成 ADR』——路線已改」）直接矛盾** —— Destination 是驗收條件，
> Notes 不是，所以矛盾的時候被照做的是這一句。

實況：它描述的那一句已由 **C145 §五** 改成「寫進 `DECISIONS.md`／續卷」並逐字存檔。
**這段話今天指著的是那份存檔**，而它仍然用現在式說「唯一還指著 `docs/adr/`」。

**其二 —— Notes 末段：**

> **現況**：`main` 的範疇邊界仍未定義，而那**不是 `#90`／`#93` 能解的**——見 **#180**。

實況：`#180` 已於 2026-08-28 關閉（COMPLETED），`scope-check` 已上 tier1
閘門鏈（`b745d6e`），今日零違規。

**其三 —— Out of scope 第一條的後半：**

> 擋著它的換成 **#180**：新增一個 `tools/<name>` 會撞上 `scope-check` 的
> 「樹上有、沒登記」，而那條線今天還沒畫（`main` 的範疇清單未定義）。

實況：`#93` 結案留言已實測翻掉它 —— C136／#183 之後 `scope-check` 只管
`platform/` 與根層，`tools/` 交給了 `gate-roster`。**擋新工具的是登記程序，
不是一份未定義的清單。** 而 `#93` 自己已於 2026-08-29 關閉，實作是 PR #203。

**其四 —— 留白④ 裡的一句：**

> 但它今天長在 `main` 上（本圖已排除 `main`）

實況：與 C136 §八 存檔的那句同類 ——「排除 `main`」照做的結果是繞開整棵樹。
⚠️ **C136 §八 改了 Notes 第 2 條，漏了這一處。** 一次改寫沒有掃完同一份文件裡
的同一個斷言，而**沒有任何機制在守票面**（C145 §五）。

**其五 —— Notes 最後一行「相關」：**

> 相關：**#180**（`main` 的範疇清單 —— 今天真正擋著的那一張）

實況：`#180` 已於 2026-08-28 關閉。「今天真正擋著的那一張」是指路，
而它指向的是一張關掉的票。

**其六 —— Out of scope 第二條的末句：**

> 而且它只活在 `main`，本圖已排除 `main`。

實況：與其四同一句話的第二個實例，**在同一份文件的另一節**。

⚠️⚠️ **這一節自己就是它在講的那個失敗的第三個實例，寫下來。**
產出本則的那一輪 grilling 逐節讀過票面，**點出四處**；落地時改用全文掃描
（`排除`／`#180`／`docs/adr` 三個字串各掃一次），**出來六處** ——
多的兩處都是**同一句話的第二個實例落在另一節**。

| 掃法                                   | 找到 | 漏掉       |
| -------------------------------------- | ---- | ---------- |
| 逐節讀（人）                           | 4    | 其五、其六 |
| 逐字串掃（`排除`／`#180`／`docs/adr`） | 6    | —          |

⚠️ **那三個字串是手選的，所以這張表證明的是「字串掃勝過逐節讀」，不是
「字串掃找得完」。** 完整性仍然掛在「有沒有列對要掃的字串」上 ——
本則另外三處（索引缺 `#93`、`#87`／`#195` 未列）是**從別的問題**問出來的
（子題關了沒有），不是掃出來的。**這個補充自己就是那個限制的實例。**

**前兩個實例**：C136 §八 改了 Notes 第 2 條、漏了留白④ 裡的同一句（＝其四）；
C145 §五 改了 Destination、而描述它的那一段留在原地變成指著自己的存檔（＝其一）。
**三次都是同一個形狀：一次改寫只處理了它當時看著的那一處。**

⚠️ 這裡沒有解藥，只有一條可以照做的：**改票面之前先對每一個要改的字串
做一次全文掃描**，不要靠逐節讀。⚠️ 而**票面不受版控，掃描結果沒有地方可以留**
—— 這一段就是那個地方。

⚠️ **索引裡 `#90` 那一行的數字一個都不動。** 「32 列、四個桶（正典 6／
過渡豁免 1／正交 21／無關 4）」描述的是 **C135 產出了什麼**，作為歷史陳述它是對的；
今天的 34 列是 C145 §三 之後的樹。兩者都成立，換數字上去等於再造一個
沒有議定口徑的計數（C136 §八 末段、C130 §四／C53(c)）。**索引只補一行：`#93`。**

#### 八、與既有裁決的關係（C136 §八）

| 既有               | 本則做了什麼                                                                               |
| ------------------ | ------------------------------------------------------------------------------------------ |
| **C135**（凍結卷） | 不動。§五 引用它的四個桶，沒有改判                                                         |
| **C136 §八**       | **不推翻，補一處它漏掉的**（§七 其四）。它的判準（指令／陳述）本則整段沿用                 |
| **C143**           | 不動。§五 引用它把桶欄接上 `scope-check`                                                   |
| **C145**           | 不動。§二 §三 建立在 §二 的路線裁決上；§六 ① 明文遵守 §四 的「空桶不是訊號」               |
| **C150 §五**       | 本則是它那句話的直接應用 —— 三條留白的守票要關了，所以逐條裁掉而不是留著                   |
| **C152**           | 不動，**一個字都不碰 `#91` 的三個選項與聚合規則**。§二 引用它的「同形」當第四個實例        |
| **C93／C109 §七**  | 不動。§四 是它們那條舉證責任的一次適用，**而這次的結論是「寫不乾淨所以不進 `AGENTS.md`」** |

**沒有任何既有產出需要重跑。** 不動 C137、C138、C139、C140、C144、C146、C151。

### C154 — 新增機械檢查要在 D16 兩軸上報數，而這條要求在被裁之前就已經擋下三件事（2026-09-01，#187）

⚠️ **量測基準 `cd6de4e`。** 本則所有行號、時戳與計數凍結在那一個 commit。

#### 一、裁三件，不裁三件

**裁：**

1. **填寫要求的內容**（§三，三條）。
2. **它的射程**（§四）—— 而射程比 `#187` 票面寬。
3. **它寫在哪裡**（§七）—— 而那推翻了票面自己的一行。

**不裁：**

- **不動 D16 原文。** 理由是 C142 §六 那一條：它是有日期的裁決，改它等於回頭
  改寫歷史。本則同樣不動 C142。
- **不追溯**（§六）。
- **不裁那道存在性檢查**（§七）—— 那是 `#219`。

⚠️ **本則不論證增加或減少任何一道閘門**（C137 §一）。它不動 `GATES`、
不動 `UNGATED`、不新增任何檢查。⚠️ 而射程變寬**不等於門檻變高**：
填得出來就過，這一條不擋任何東西，**它只要求話說完**。

#### 二、它在被裁之前就已經有效力了 —— 三處填、三處擋

`#187` 開了四天沒有被裁，而樹上已經有六處在用它。**三處照它填了，
三處引它當理由停下來：**

| 引用處                                                        | 用途   | 內容                                                       |
| ------------------------------------------------------------- | ------ | ---------------------------------------------------------- |
| C141 §五（`DECISIONS-2.md:307`）                              | 填     | 第一支；後被 C142 §四 從三格降成一格                       |
| C143 §六（`DECISIONS-2.md:642`）                              | 填     | **第一個正面案例**，② 附五步結束碼表                       |
| C148 §七（`DECISIONS-2.md:1246`）                             | 填     | ② 自稱「第一次實測」—— 那句話不成立，見 §五                |
| C146 §四（`DECISIONS-2.md:1041`）                             | **擋** | `AGENTS.md` 標記那道閘門 ——「要提它得先過 `#187`／C142」   |
| C147 §四（`DECISIONS-2.md:1137`）                             | **擋** | 門檻過期檢查（`實測 max < 設定值` → 紅）—— 同一句話        |
| `reports/research/vue-complexity-blindspot-2026-08-30.md:121` | **擋** | `.vue` 複雜度 ——「加 meta 閘門要先過 `#187`／C142 的填寫」 |

⚠️⚠️ **這是「無人主張的預設」的反面，而這棵樹沒記過這一面。**
已記的那一面是：**相依方一張一張關掉之後，「不裁」會變成無人主張的預設**
（C135 §一、C139 §四、C142 §五、C150 §五 都寫過同一句）。這裡發生的是相反的事 ——
**一件從來沒有被裁過的事，靠著被引用而取得了效力，並且已經在擋工作。**

⚠️ 而票面說的是相反的話。`#187` 的正文寫著「**本票目前沒有待決的適用案例**」，
那一版寫於 `2026-08-30 16:06`（票面最後更新時戳），而上表三處「擋」**全部晚於它**：
C146 `16:23`、C147 `17:56`、那份報告 `17:57`（`765233c`）。
**票面在說它沒有案例的同一個下午，三件工作停在它後面。**

#### 三、裁決：填寫要求三條

**新增任何機械檢查時，提案要交出這三樣。**

**第 1 條 —— D16 兩軸各報一格。** 兩軸是交付與迭代，一格都不許空著；
兩軸皆零就丟（D16 原文），而本則不改那條後果。

**第 2 條 —— 迭代軸那一格照 C142 §二 的兩條件填，並標明級別。**

兩條件是 **① 對象在外**（它報的是別的東西壞了，不是它自己被改壞）與
**② 壞法安靜**（在它不存在的世界裡，那個壞法看起來是綠的）。
⚠️ ② 是真正的門檻：要說出的不是「違反規則時它會紅」，而是**它不在的話，
那個壞法長什麼樣**。答案若是「看起來就是壞的、有人會發現」，這一格是零。

**② 那一格要標明是哪一級：**

| 級別             | 意思                                 | 先例                             |
| ---------------- | ------------------------------------ | -------------------------------- |
| **判讀**         | 讀既有文件推出來的，沒有跑任何東西   | C142 §三（自陳「判讀，非量測」） |
| **探針**         | 構造一個情境、逐支取結束碼           | C143 §六（五步表）               |
| **發生過的實例** | 那個安靜的壞法**真的發生過**，有證據 | C148 §七（連續 10 天的假肯定句） |

⚠️ **判讀那一級要多寫一句：「要推翻它，該做什麼。」** C142 §三 已經這樣做了
（「該做的是把那支拿掉跑一次，不是換一把尺」），本則把那個做法變成要求。
**三級都算填了** —— 分級不是為了排序高低，是為了讓下一個人知道那一格的**來源**，
而不是看到「② ✅」就以為有人量過。

**第 3 條 —— 自我防護的夾具不計迭代軸的分，但要另立一節寫下來。**

這一條是 C142 §五 的升格。一道閘門守著**自己不被改壞**的那些夾具斷言，
兩條條件都不滿足（對象是它自己，而改它的人正在改的就是它），所以填不了那一格。

⚠️ 混寫的後果具體：**一支兩軸皆零的工具，可以靠列出「我有三條夾具在守自己」
把迭代軸那一格填得滿滿的。** 那是空格子的第二種寫法。

⚠️ 那些夾具**仍然要寫** —— 它們是這棵樹上最容易掉的東西（C141 §五 的 ②③
就是為了回答這一格才想清楚的，兩條都變成了測試裡的夾具斷言）。
**「別處」指的是提案裡另立的一節，不指定檔案** —— 這條要求管的是提案的形式，
不是檔案佈局。替裁決發明一個新的檔案位置，是 C135 §一 記過的那件事
（建議可以寫「新位置」，裁決不能）。

⚠️ **升格是有實測依據的，不是形式**：C141 §五 三格有兩格填錯了地方，
而那發生在 C142 §五 把話講明**之前** —— 那就是「留給下一個人自己推導」的結果。

#### 四、射程：任何新增的機械檢查，而「meta 閘門」退回描述性用語

**射程是「任何新增的機械檢查」，包含在既有工具裡加一條規則。**
不是只有「新增獨立的一支」。

兩個理由，第二個比第一個硬：

1. **窄射程留下一個明顯的繞道** —— 把新檢查塞進既有工具就免填。
2. ⚠️⚠️ **窄射程追溯會把 C143 踢出去，而它是這條要求自己認定的第一個正面案例。**
   C143 的標題就寫著「**而它不是新的一支閘門**」（桶欄是加在既有 `scope-check`
   裡的一條規則），而 C143 §六 自陳是「`#187` 那條填寫要求的第一個正面案例」。
   **一個把自己最好的證據判成射程外的射程，是挑出來的不是推出來的。**

**於是「meta 閘門」這個詞退回描述性用語，不再是判準的一部分。**
它在這棵樹上**沒有任何機械對應物**：`tools/gate-roster/src/gates.ts` 沒有分類欄，
19 支工具沒有一支帶「meta」標記，這個詞只活在 `DECISIONS-2.md` 的散文與
一份研究報告裡。而 `#187` 對它唯一的定義是**四支列舉 ＋ 一句話**。

⚠️ 這順帶關掉 `#187` 留言一登記的那個邊界問題：`#192`（斷言強度）落在
「守治理」與「守測試碼」的邊界上，而票面用列舉定義的那一類答不出它算不算。
**射程改成「任何新增的機械檢查」之後，那個問題不必回答就消失了** ——
`#192` 那個空格若哪天有人要填，照這條填就是（而 C150 §五 記著它今天未決）。

⚠️ 拿字面相同去論定適用，是 C137 §四 記的那個形狀（判準套在東西的「描述」上，
不是「實質」上）。**本則的解法不是把那個詞定義得更好，是不用它。**

#### 五、更正 C148 §七 那句序數（依 C136 §八）

C148 §七 的 ② 那一格寫著：

> 這是這棵樹第一次有一格 ② 是實測出來的。

**那句話不成立**，而成因是它只跟一則比過。逐項核對（時戳取自 commit）：

| 條目     | commit    | 時間               | ② 的來源                       |
| -------- | --------- | ------------------ | ------------------------------ |
| C143 §六 | `b2e9798` | 2026-08-30 `03:54` | **探針** —— 五步，逐支取結束碼 |
| C148 §七 | `37d3ab5` | 2026-08-30 `18:39` | **發生過的實例** —— 連續 10 天 |

C148 §七 那一段拿來比的是 C142 §三（「自陳判讀，非量測」），
**沒有比 15 小時前的 C143 §六**。照 §三 的三級，探針與實例都算實測，
所以第一格實測的 ② 是 C143 §六。

⚠️ **更正的形式是本則，不改 C148 的原文**（C142 §六：改有日期的裁決等於改寫歷史）。

**依 C136 §八 交代它產出過什麼：** C148 §七 的產出是**樹指紋那條判準**
（機制在 C149／`ec4b0b0`，程式在 `tools/exit-drill/src/tree-fingerprint.ts`）。
那句序數**沒有進到任何程式碼、任何閘門訊息、任何 `evidence.json` 欄位裡** ——
它是一句自我評價。**沒有任何東西要重跑**，C148 的結論一條都不推翻，
它的 ② 仍然成立、仍然是「發生過的實例」那一級。

#### 六、不追溯，而已知的漏填有一處

**本則只管它之後的提案。** 理由同 §一：補一格會動到已經成立、有日期的裁決。

⚠️ **但已知的那一處要列出來，否則它會變成一個安靜的先例**（「原來不填也沒事」）：

| 條目     | 它加了什麼                                      | 兩軸 |
| -------- | ----------------------------------------------- | ---- |
| C148 §五 | 預期失敗帳（`exit-drill` 裡新增的一套雙向帳目） | 沒填 |

C142（`2026-08-30`）之後，**在 `tools/` 與 `.github/` 這個射程裡**只有這一處。
C144（桶欄上線）與 C149（樹指紋機制）都是實作，判準那一層已由 C143 §六 與
C148 §七 報過，**不要求實作再報一次**。

⚠️ **那個射程是這一次查的範圍，不是 §四 的射程。** §四 說的是「任何新增的
機械檢查」，不限於那兩個目錄 —— 所以本節列的是**已知的**漏填，
不是「全部的」。

⚠️ **相鄰但不算漏填**：C150 §五 把斷言強度那個空格明文留白，理由寫的是
「那是 D16 兩軸的問題」—— 它引的是 D16，不是本票。**它在等的是同一件事**，
而它今天仍然未決（C150 §五：「沒有人主張它不該有，也沒有人主張它該有」）。

#### 七、落點在 `gates.ts` 檔頭，而本則推翻了票面自己的一行

`#187` 的〈不在這張票內〉寫著：

> 不動現有四支工具的任何一行，不改 `gate-roster` 的 `UNGATED` 名冊。

**本則推翻那一行的前半。** 動的是 `tools/gate-roster/src/gates.ts` 的**檔頭註解**，
`GATES` 與 `UNGATED` 的**任何一列都不動**，也不動另外三支工具。

理由：**新增一道閘門必經那個檔案**（`GATES ∪ UNGATED` 登記、`why` 必填），
所以那是這條要求會被讀到的**那一刻所在的地方**。

> ⚠️ **括號裡的「`why` 必填」在當時是假的：`Gate.why` 只是型別上必填，
> 連空的都綠 —— 補上它的是 C155。⚠️ 而本則的結論不受影響，落點靠的是
> 「新增閘門必經那個檔案」那一半。**
> （本則其餘一個字不動 —— 更正的形式是新則，同 C142 §六。）

只寫在裁決裡的規則，靠的是下一個人剛好讀過 `DECISIONS-2.md` —— 而 §二 那三處
「擋」證明了引用會發生，**卻證明不了不引用時會發生什麼**。

⚠️ **那一段是指針，不是副本。** 只寫兩條條件的名字（對象在外／壞法安靜）
與「自我防護的夾具不算」一句，論證與三級標籤留在本則。
理由寫在那個檔頭自己的開場：**一份到處都有副本的清單，改動時只有人記得改其中幾處**
（那是這份名冊存在的全部理由）。在它裡面放一份 C154 的副本會是反諷的。

⚠️ **代價**：那一段被刪掉的話，本則退回「只存在於裁決裡」，
**而沒有任何東西會紅。** 同 C142 §六 記的那一行指標。

#### 八、沒有機制在守，而這一次留白當場有了主張人

填寫要求、三級標籤、射程 —— **全部靠人讀。** 本則不帶任何閘門，
`gate-roster` 不加一列，`UNGATED` 也不加。同 `AGENTS.md` 規則四。

⚠️⚠️ 而 `#187` 隨本則關閉，於是「要不要有機制」這一格失去守它的票 ——
這棵樹記過五次同一個形狀（C135 §一、C136 §十、C138 §五、C143 §八、C145 §六：
守著留白的票關光之後，「不裁」變成無人主張的預設）。**本則不加第六次：**

> **`#219` 承接它** —— `gate-roster` 的 `why` 欄要不要有存在性檢查
> （只查有沒有寫，不查寫得對不對，形狀等同 `SCOPE.md` 的桶欄）。
> 那張票**開票時就照本則報了自己的兩軸**，而它的 ② 標的是**判讀**。

⚠️ `#219` 自己也是一道新的機械檢查，所以它要過本則這條要求 —— 而它過了。
**這條要求第一次被套用在它自己的機制提案上。**

#### 九、與既有裁決的關係（C136 §八）

| 既有              | 本則做了什麼                                                                      |
| ----------------- | --------------------------------------------------------------------------------- |
| **D16**（凍結卷） | **不動一個字。** 本則是它底下的一條填寫要求，不是新判準                           |
| **C137 §一**      | 不動，**並遵守它** —— 本則不論證增或減任何一道閘門                                |
| **C141**          | 不動。§二 引用它 §五 當第一支填寫案例，C142 §四 對它的降格仍然成立                |
| **C142**          | 不動，**§三 第 2、3 條是它 §二 §五 的升格** —— 而升格的授權來自 §五 自己那一句    |
| **C143**          | 不動。§四 引用它當「窄射程會踢掉自己最好的證據」的理由                            |
| **C148**          | **不推翻，更正 §七 一句自我評價**（§五）。它的結論、它的 ② 級別、它的產出全部不變 |
| **C150 §五**      | 不動。§六 記下它在等同一件事，而本則不替它填                                      |
| **C152 §二**      | 不動。§四 用它的「同形」解釋為什麼不去把「meta 閘門」定義得更好                   |
| **C136 §八**      | 沿用它的判準（§五 交代產出、§九 這張表）                                          |

**沒有任何既有產出需要重跑。** §五 更正的那句話沒有進到任何程式碼、
閘門訊息或資料檔裡。

---

### C155 — 四個同語意的必填理由欄，只有一個是靜默的；而三個「該一起搬」的手足，② 實測是零（2026-09-01，#219）

⚠️ **量測基準 `929c1a6`。**

#### 一、裁什麼、不裁什麼

**裁三件：**

1. `Gate.why` 加一條**非空**檢查，落在 `tools/gate-roster/src/check.ts`（②），
   進 `vpr gate`。門檻與三個手足一字不差（`> 20`）。
2. **另外三個必填理由欄不搬** —— `Ungated.why`、`Variant.why`、
   `notInGateScript` 留在 `roster.test.ts`。理由是量出來的，見 §五。
3. 更正 `#219` 票面三處前提、以及 C154 §七 一句被當成事實使用的斷言（§三）。

**不裁三件：**

- **不裁「兩軸有沒有填」要不要有檢查。** 那是 `#219` 的題目，本則不動它 ——
  而它的 ② 由本則升到探針（§六）。
- **不裁「必填理由該落在哪一層」。** 那是一個沒有人裁過的設計問題（§七）。
- **不碰版號。** main 至今一版未發，那個判斷的射程比本則大（§八）。

⚠️ **本則不論證增加或減少任何一道閘門**（C137 §一）。它加的是既有工具裡的
一條規則，`GATES` 與 `UNGATED` 的任何一列都不動。

#### 二、⚠️ 一條「已經在用」的斷言，從來沒有被執行過

`#219` 的提案建立在一句話上：**「而且 `why` 已經是必填的」**。
而 C154 §七 更早就拿同一句話去論證落點：

> 理由：**新增一道閘門必經那個檔案**（`GATES ∪ UNGATED` 登記、`why` 必填）

`gates.ts` 的檔頭從 C136 起也這樣寫著：「`check.ts` 的①（每一個 `tools/*`
workspace 成員都要登記、`why` 必填）」。

**三處都是假的**，而且是同一句話傳了三手。`Gate.why` 只是 TypeScript 上必填 ——
寫 `""` 通過型別檢查、通過 `vpr gate`、通過整套測試。

⚠️ 這與 C148 §七 記的是同一個形狀：**一句肯定句，說著它不知道的事，而沒有東西
會反駁它。** 差別在那一次是閘門在印，這一次是三份文件在寫。

#### 三、實測：四個同語意的欄位，只有一個是靜默的

把每個欄位各改成 `"短"`（不是清空 —— 清空對三個手足是同一個結果，
而改短才量得到那條 `> 20` 的門檻），逐支取結束碼。取代數逐項驗過
（全部 `=1`，否則跳過該列 —— 假的綠會從那裡進來）：

| 變異                       | `gate` CLI | `vitest` | ② 壞法安靜？    |
| -------------------------- | ---------- | -------- | --------------- |
| **對照組（未變異）**       | 0          | 0        | —               |
| `Gate.why` → `"短"`        | 0          | **0**    | ✅ **完全安靜** |
| `Ungated.why` → `"短"`     | 0          | **1**    | ❌ 今天就紅     |
| `Variant.why` → `"短"`     | 0          | **1**    | ❌ 今天就紅     |
| `notInGateScript` → `"短"` | 0          | **1**    | ❌ 今天就紅     |

跑完還原，`git diff` 0 行、逐字相同。

三個手足的斷言吃的是**真的常數**（`for (const entry of UNGATED)`），
所以它們今天就會擋人。⚠️ 而 tier1 同時跑 gate CLI（第 84 行）與
`vp run -r test`（第 127 行）—— 對那三個欄位，**搬家不會讓任何今天綠的東西變紅，
只會換一個步驟去報它。**

#### 四、這條規則自己的 D16 兩軸（C154）

| 軸       | 這一條                                                         |
| -------- | -------------------------------------------------------------- |
| **交付** | **零。** 採購／資安／法務沒有一個收件人在意 `why` 欄有沒有寫。 |
| **迭代** | **有分。** 見下。                                              |

- **① 對象在外** ✅ 它報的是**別人寫的登記內容**。⚠️ 而「別人」有一個具體的
  身分：`gates.ts` 檔頭寫著，對 fork v1 在做案子的團隊而言 **`why` 就是那個判斷
  本身**。他們加 `tools/their-thing` 時撞到的應該是閘門，不是一支他們沒在讀的測試
  —— 這也是它落在 `check.ts` 而不是 `roster.test.ts` 的理由。
- **② 壞法安靜** ✅ **級別：探針。** 證據就是 §三 那張表的第二列：
  `Gate.why` 改短，`vpr gate` 與整套測試**都是 0**。

⚠️ **自我防護的夾具另計，不計迭代軸的分**（C154 §三 第 3 條）：
`roster.test.ts` 的 `describe("閘門沒寫理由（C155）")` 整個 describe 都是守這條
規則自己的，它們走 `checkRoster` 的注入參數 —— 那個參數存在的理由就是讓測試
換掉名冊。**它們不填上面那一格。**

⚠️ **門檻 `> 20` 今天咬不到任何一列**（16 列最短 25）。那是刻意的：
挑一個今天就會咬的數字來證明它有用，量到的只會是那個數字。同 `gates.ts` 檔頭
對 plugin 那條規則寫過的話 —— **「今天不痛是巧合，不是設計。加的那天它才會擋人，
而那正是它該擋人的時候。」**

#### 五、⚠️ 「一起搬」這個提案的 ② 是零，而支持它的論證是被禁止的那一類

本則原本的方向是把四個必填理由欄**統一**搬進 `check.ts`。§三 那張表推翻了它：
三個手足的 ②「在它不存在的世界裡，那個壞法看起來是綠的」**是假的** ——
誠實的反事實是「這次搬遷沒有發生」，而那個世界裡它們已經是紅的。

而支持搬遷的唯一論證是「同一類要求不該有兩個執行位置」——
**那是一個可維護性論證。D16 沒有那一格，而 C137 §一 明文不准拿那一類去論證
增減閘門，兩個方向都不准。**

依 C154 §四，射程是「任何新增的機械檢查」，**包含在既有工具裡加的每一條規則** ——
所以那會是四份兩軸報告，不是一份。三份的 ② 會寫 0，而兩軸皆零就丟（D16）。

⚠️ 記下這一段，是因為**一份報告蓋住四條規則**正是 C70 那個形狀：
同一份證據掛在兩條承諾上，而空著的那一條沒有人發現。

#### 六、`#219` 的 ② 從判讀升到探針，而本則不裁它

`#219` 開票時把自己的 ② 標成**判讀**，並依 C154 §三 第 2 條寫了「要推翻它該做
什麼」。本則做了那件事，但**不是用票面寫的做法**：

⚠️ **票面那條路不可構造。** 「加一列不含兩軸的假登記」會噴出五個與兩軸無關的紅
（重複登記／`scripts.gate` 對不上／別名對不上／workflow 少一道／README 漏一道）。
要讓那一列乾淨地綠著，得同時偽造 `package.json` 兩處、兩個 workflow、README 一列
—— **那個紅不是 ② 的證據，是雜訊。**

改用差分式，跑在真樹上：

| 狀態                                  | `pnpm gate` |
| ------------------------------------- | ----------- |
| C —— 沒有兩軸（＝今天的樹）           | **0**       |
| B —— `gate-roster` 那一列寫進合格兩軸 | **0**       |

**兩軸的存在與否對整條閘門鏈完全隱形。** 變異驗證（檔案真的變了、新內容真的含
兩軸標記）與還原驗證（`git diff` 0 行、逐字相同）都跑過。

⚠️ 順帶量到一個普查結果：**名冊 21 列，同時含「交付」與「迭代」的是 0 列**
（已知非零對照組：含「閘門」5 列、含 C 編號 11 列）。也就是說
**今天這棵樹本身就是 ② 的反事實世界**，不必構造。

**本則不拿這個結果去裁 `#219`。** 它欠的不是 ②，是「查什麼」——
`why` 是散文，**沒有值域**，而 C143／C144 那個「只查填了沒有」之所以成立，
靠的是 `BUCKETS` 是一個封閉的四值列舉。`#219` 保持開著。

#### 七、⚠️ 一個具名的留白：必填理由該落在哪一層，沒有人裁過

普查結果：**六條必填理由斷言分布在四支工具，全部只由測試守。**

| 工具          | 欄位                                            | 本則的處置  |
| ------------- | ----------------------------------------------- | ----------- |
| `gate-roster` | `Ungated.why`／`Variant.why`／`notInGateScript` | 不動（§五） |
| `pii-check`   | `rule.why`                                      | **不動**    |
| `exit-drill`  | `entry.reason`                                  | **不動**    |
| `ui-survey`   | `candidate.eliminated`                          | **不動**    |

⚠️ **那三支不是壞的** —— 它們的斷言真的會擋人，只是擋在測試那一層。
所以問題不是「它們沒人守」，是：

> **必填理由該落在閘門那一層還是測試那一層？沒有人裁過。**
> 它是**未決**，不是**已決為不做**。

⚠️ 而本則替 `Gate.why` 選了閘門那一層（§四 給了理由：fork 團隊撞到的應該是閘門），
**那個選擇本身沒有被推廣過** —— 它建立在 `Gate.why` 的 ② 上，而三個手足的 ② 是 0。

`#222` 承接這個留白。⚠️ 那張票**不帶「該搬」的預設**，而且票面載明三個手足的
② 實測是 0。

#### 八、不碰版號，而理由不是「不重要」

本則之後 `vpr gate` 會擋下今天不擋的東西 —— 對 fork v1 的團隊，一個 `why` 寫得短
的登記升上來就會紅。照這棵樹的版號判準（「升上來會不會壞」不是「改了什麼」），
**那是會壞**。

**但本則不出版號。** `v1.14.1`（`2026-08-26`）是 `release/v1` 的最後一版，
而**交付線移回 main 之後一版都沒發過** —— main 上已經有 8 支行為變更積著，
本則是第 9 支。要判的是「這 9 支合起來該是什麼版號」，那個射程比本則大得多。

⚠️ 這個發現**刻意不寫進本則的射程**（它是查版號程序時撞到的，與 `#219` 無關），
但它有一張自己的票：**`#223`**。

#### 九、與既有裁決的關係（C136 §八）

| 既有                 | 本則做了什麼                                                                           |
| -------------------- | -------------------------------------------------------------------------------------- |
| **D16**              | **不動。** §四 照它報兩軸，§五 照它「兩軸皆零就丟」否決了自己原本的方向                |
| **C137 §一**         | 不動，**並遵守它** —— §五 明文拒絕用可維護性論證去搬三個手足                           |
| **C142 §二**         | 不動。§四 的 ①② 照它的讀法填                                                           |
| **C143／C144**       | 不動。§六 用桶欄的**封閉值域**解釋為什麼 `why` 不是同一個形狀                          |
| **C148 §七**         | 不動。§二 引用它當「肯定句說著它不知道的事」的同形先例                                 |
| **C150 §五**         | 不動。§七 沿用它「未決，不是已決為不做」的措辭                                         |
| **C152 §二**         | 不動。§六 用「同形」解釋為什麼散文代理答不了「查什麼」                                 |
| **C154**             | **不推翻，更正 §七 一句被當成事實使用的斷言**（§二）。它的結論、射程、三級標籤全部不變 |
| **C154 §四**         | **遵守它** —— §四 是本則這條新規則自己的兩軸報告                                       |
| **C154 §三 第 3 條** | **遵守它** —— §四 末段把自我防護的夾具另立，不計分                                     |

**沒有任何既有產出需要重跑。** §二 更正的那三句話都是**論證的前提**，
不是產出：C154 §七 拿它論證落點，而落點的結論（寫進 `gates.ts` 檔頭）不受影響 ——
那一段本來就不靠 `why` 真的被檢查才成立，它靠的是「新增閘門必經那個檔案」。

### C156 — 一張把五層重新綁定到工具的表：前四列量的都是這棵樹已經有了或已經裁過的東西，而唯一有增益的第五列被貼成一個已判不可填的層（2026-09-01）

⚠️ **量測基準 `e2e4a06`。** 本則所有數字凍結在那一天。

#### 一、裁什麼、不裁什麼

**裁四件：**

1. **不以那張角色表取代 `TESTING.md` 的「五層兩類」。** 理由不是它錯，是它
   **不是一個替代模型** —— 它是同一組五層座標的工具綁定表，而且把「兩類」
   那一軸拿掉了（§二）。
2. **記下這個提案在它自己的判準上往反方向走**（§三）—— 提案的目標是
   「減少導入測試種類」，而它的淨效果是 runner **1 → 3**。
3. **逐列否決第 1–4 列**（§四–§七）。⚠️ **四列各有各的理由，不共用一個** ——
   共用一個正是 C155 §五 記的那個形狀。四列共同的**只有一件事**：它們量的都是
   這棵樹**已經有了**（第 2、4 列）或**已經裁過**（第 1、3 列）的東西。
4. **第 5 列（Browser Mode）重貼標籤**：它是**層 2／3 換執行基底**，
   不是層 4（§八）。

**不裁三件：**

- **不裁 Browser Mode 要不要做。** 它欠一份自己的 D16 兩軸報告（C154 §四），
  **本則不代它填**（§八）。
- **不裁閘門 CLI 的整併。** §九 只記下它是唯一有合法論證空間的位置，
  不動任何一支、不開票。
- **不碰任何門檻，不新增也不移除任何一道閘門**（C137 §一）。

⚠️ **本則不新增任何機械檢查**，所以不觸發 C154 §四 的兩軸報告要求。
全案唯一需要那份報告的是第 5 列，而它未裁。

#### 二、那張表少掉的那一軸，而少掉它已經害死過一次東西

那張表的五個角色與 `TESTING.md` 的五層是一對一的：規格員＝層 3、撰寫員＝層 2、
清理員＝層 1、強化員＝元層、QA 員＝層 4／5。**所以它不是一個競爭的模型，
是同一組座標的工具綁定表。**

而 `TESTING.md` §一 那句話是這樣寫的：**五層是借來的座標軸，兩類是這個腳手架的
身分，組織原則是後者。** 角色表只帶了前者。

⚠️ **少掉那一軸的後果不是抽象的，它有前例。** C109：用五層的尺量，`slice-gen`
被判「一致性機制，不是測試層」，裁決是刪掉；用兩類重量，它是第二類**唯一**的
入口，裁決反轉。

⚠️⚠️ **而這一次少掉的東西，正好是提案人要保住的那個。** `TESTING.md` §五 那張
表列了七個「用五層的尺對應不上」的機制，其中五個是在守飄移：

| 機制                 | 它守的那種飄移                                                                              |
| -------------------- | ------------------------------------------------------------------------------------------- |
| `tools/api-surface`  | 基準檔差分 —— 平台一改就打斷每一個切片                                                      |
| `tools/codemods`     | 升級平台時的手改缺口                                                                        |
| `tools/theme-verify` | 可替換性                                                                                    |
| `tools/doc-facts`    | 文件斷言與實作分岔                                                                          |
| `tools/scope-check`  | 目錄範疇 ⚠️ **只查填了沒有＋是不是那四個桶，填得對不對全綠**（C136／#180 之後已退化成登記） |

加上層 1 既有的 `tools/conformance`（分層邊界、幽靈依賴、相對路徑逃逸）與
C148／C149 的樹指紋，**角色表一格都沒有**。

⚠️ **而「表上沒這一格」與「判它出局」在文件上長得一樣。** 這是 C152 那個「同形」
的近親：兩個相反的來源映到同一個讀數。⚠️ **差別要標出來** —— C152 的讀數是量出來
的，這裡的「讀數」是一張表的沉默，我沒有把它當量測用。

#### 三、⚠️ 提案在它自己的判準上往反方向走，而擋它的那句話是付過錢的

提案的目標寫得很明確：**減少導入測試種類。** 逐列加總它的淨效果：

| 列       | 對「種類」的淨效果                                                      |
| -------- | ----------------------------------------------------------------------- |
| 1 規格員 | **＋1 runner**（Playwright）＋1 外掛（playwright-bdd）；既有層 3 被延後 |
| 2 撰寫員 | **＋1 手法**（MSW）—— 而這棵樹目前零個 test double（§五）               |
| 3 清理員 | ＋1 導出指標（CRAP），無新工具                                          |
| 4 強化員 | ±0 —— 它已經在樹上（§七）                                               |
| 5 QA 員  | **＋1 執行基底**（browser-mode Vitest）                                 |

**runner 數 1 → 3**（Vitest ＋ Playwright ＋ browser-mode Vitest）。

⚠️ **而「不養第二套 runner」在這條線上不是偏好，是已經付過的帳。**
`TESTING.md`〈runner 與門檻〉那一節把理由寫死了：跨套件測試在這條線上已經有過
排程相依，**C87**（「兩支測試搶同一個 repo，第二次；而這次兩條斷言直接相反」）
用 `dependsOn` 讓開，**第二套 runner 會再撞一次同一個問題**。

**所以否決第 1 列不必訴諸偏好** —— 提案在提案人自己選的那把尺上就是負的。

#### 四、第 1 列：前提打錯對象，而代價可以指名

那張表對規格員的判詞是「前端 Gherkin 投報率低，建議延後」。**它評的是
Playwright 驅動的瀏覽器 Gherkin。**

**這棵樹的層 3 不是那個東西。** 它是 Vitest 驅動、打 `src/usecases/` 的
Gherkin（C114），而那個差別是有實測背書的：把 usecase 裡的篩選拿掉 →
**規格紅 5 條，而既有的切片契約測試那 5 條照樣全綠**。

現況普查（基準 `e2e4a06`）：

| 量的東西                        | 數                                                      |
| ------------------------------- | ------------------------------------------------------- |
| 宣告 `@amiceli/vitest-cucumber` | **3** 支（`promise-check`／`slice-gen`／`spec-report`） |
| **對照組** —— 宣告 `vitest`     | 29 支                                                   |

產生器實測：`slice-gen` 產出 `specs/<name>.feature`（`files.ts:270`）與
`tests/specs/<name>.spec.ts`（`files.ts:768`）。**層 3 的設施不是提案，是既有物。**

⚠️ **「延後」會關掉三樣可以指名的東西**：

1. **完成率** —— C115 那份四態台帳，第二類唯一可量的產出。
2. **第二類那句核心承諾唯一的著力點**（「專案成員只要補 TDD，就量得出業務
   功能完成率」）。
3. **框架承諾五條裡唯一有機械背書的那一條**（C118／`promise-check` **照著**
   `specs/` 執行）。

⚠️ **「投報率低」這個結論不轉移**，因為兩邊的成本結構與產出都不同。這與
`TESTING.md` 對借來的門檻值說過的是同一句話：**借座標會連別人的風險清單一起
借過來，那跟門檻值一樣是別人專案的產物。**

#### 五、第 2 列：普查結果是這棵樹一個 test double 都沒有

第 2 列的 `vp test` 本來就是現況。要判的是 `vi.mock` ＋ MSW 那一半。

射程 `apps/`／`platform/`／`tools/`／`features/`／`specs/`，`*.ts` 與 `*.vue`：

| 手法                       | 出現次數 |
| -------------------------- | -------- |
| `vi.mock(`                 | **0**    |
| `vi.stubGlobal(`           | **0**    |
| `vi.spyOn(`                | **0**    |
| `globalThis.fetch`         | **0**    |
| **對照組** `startBffMock(` | 6        |
| **對照組** `describe(`     | 287      |

**這棵樹不 mock。** `platform/bff-mock` 是 `@org/bff-contract` 的**參考實作**
（R6，`server.ts` 檔頭），測試裡起一台真的 http server 走完整往返
（session cookie → CSRF 擋下 → 補標頭 → 通過），而 `tools/bff-check` 驗它符合契約。

⚠️ **對「偵測飄移」這個目標，MSW 是反向的：** handler 是手寫的 API 副本，
契約改了它不會說話。現況那條路飄移時 `bff-check` 會紅。

⚠️ **上一段後半是判讀，不是量測。** 我沒有在這棵樹上觀測過「MSW 的 handler 會
飄移」—— 那是它的形狀決定的推論。量到的只有上表：**這裡零個 test double，
所以 MSW 是新增一種手法，不是取代一種。**

#### 六、第 3 列：CRAP Score 是導出的單一數字，而兩個因子各自半盲

`vp check` 已含四維複雜度（C119：函式大小、巢狀深度、參數個數、循環複雜度，
產品碼與測試碼兩組數字）。要判的是 CRAP Score 那一格。

CRAP ＝ **複雜度 × 覆蓋率**的導出單一數字。**C121 對單一分數的裁決原封不動
適用**：分數下降的原因裡至少四種與程式碼品質無關，而**一道經常因為無關原因
變紅的閘門，結局是被關掉，而被關掉的閘門連清單都產不出來**。

⚠️ **更糟的是這棵樹的兩個因子各有已知盲區：**

- **複雜度**：`<script setup>` 的 module 層在四條規則裡幾乎不存在 ——
  `platform/ui` 的 24 個零函式 `.vue`、合計 1992 行 script 在分佈裡是 **0**
  （`TESTING.md` 引 #129 §六）。
- **覆蓋率**：預設射程會讓未載入的產品碼整支不進分母，而射程**逐 package 不同
  且寫錯不會報錯**。

**兩個半盲的量測相乘，得到的是一個看起來權威的讀數。**

#### 七、第 4 列：它已經在樹上，而「相容性風險」是已經付過的帳

根層 `stryker.config.mjs` 在版控裡，`@stryker-mutator/core` 與
`@stryker-mutator/vitest-runner` **釘死 10.0.0**（C121）。磁碟實測：
`.pnpm` 上 stryker 相關 **5** 個實體。

C108 已經實測過那兩個「真阻斷」（TypeScript 7 移除 Stryker 依賴的 compiler API、
isolated node_modules 讓 runner plugin 找不到），兩個都有繞法。

⚠️ **重新導入的代價不是安裝，是那些實測知識要重踩一遍**：`ignoreStatic: true`
是**安全性的**不是效能的（不開它跑一趟全樹，版控上會多一個 `feat: initialized
repo ✨` 的 commit ＋ 本機 tag 被刪）、假存活的三個機制、以及逐測試檔的擊殺數
量到的是**歸屬**不是斷言強度（`killedBy` 只記一支，#163 撤回）。

#### 八、第 5 列：唯一有增益的一格，而它的標籤是錯的

那張表把 Browser Mode 放在「QA 員」＝層 4。**層 4／5 的裁定是「下發不了、
人做的事」** —— 貼成層 4 會讓人以為填了一個**被判不可填**的洞。

**正確的講法：它是層 2／3 換執行基底**，happy-dom → 真瀏覽器。

它對得上兩個**已經量到**的盲點：reka-ui 的 Teleport 在 `renderToString` 下
一個字都沒有；這棵樹**量不到可及名稱**（走 textContent 代理，缺陷與非缺陷
紅同一組）。

成本實測（`node_modules/.pnpm`，基準 `e2e4a06`）：

| 套件                     | 磁碟上的實體數     | 意義                    |
| ------------------------ | ------------------ | ----------------------- |
| `@vitest/browser@4.1.10` | **3**（peer 變體） | 已隨 vite-plus 進來     |
| `browser-playwright`     | **0**              | optional peer，**未裝** |
| `playwright`（任何形式） | **0**              | 未裝                    |
| **對照組** stryker       | 5                  | —                       |

→ **preview provider 走既有相依；playwright provider 要新增兩層**
（provider 套件 ＋ 瀏覽器二進位），而後者會動到 `supply-chain`／airgap 那條線。

⚠️ **本則不裁它。** 它要自己走 D16 兩軸（C154 §四），而**那份報告本則不代填**。
理由是 C155 §五 已經寫過的那一句，而它引的是 **C70**（「承諾有五條，檢查只有
四條：空著的那一條被另一條的證據填上了」，`DECISIONS.md:4651` —— 本則直接核過
它指得到）：**一份報告蓋住多條規則，會讓空著的那一條沒有人發現。**

#### 九、⚠️ 提案的動機指對了方向，而合法的著力點不在測試模型

提案的動機之一是「不要讓腳手架變成屎山代碼」。

⚠️ **那是一個過度設計論證，而 C137 §一 明文不准拿它論證增減任何閘門**
（這棵樹上「過度設計」沒有分母：產品就是 `tools/`，`apps`／`features` 是示範
不是產品）。**所以本則兩個方向都不用它。**

而那個直覺指的方向有一個合法的落點：`pnpm gate` 串起 **12 支自製 CLI ＋
兩道 eslint**，`tools/` 底下 **20 個目錄**。**整併它們是一個可以問的問題**，
而 `tools/gate-kit` 已經是那個方向（C125／C126 讓八支一律拒絕不認得的旗標）。

⚠️ **但它只能逐支走 D16，不能拿「太多了」當理由** —— 同 C155 §五 拒絕
「同一類要求不該有兩個執行位置」。**本則不動任何一支，也不開票。**

#### 十、⚠️ 一個具名的留白：借來的外部座標要怎麼判它適不適用

這棵樹借過兩次外部座標：`TESTING.md` 的五層（Robert C. Martin），以及本則在判
的這張角色表。

`TESTING.md` 對第一次留下一句提醒 ——「借座標會連別人的風險清單一起借過來」——
**而那是一句提醒，不是判準。** 本則實際用的做法是 §四 那個：**逐列問「它評的
是不是這棵樹上的那個東西」**，而那個做法：

> **沒有被推廣過，也沒有被裁過。借來的外部座標要怎麼判它適不適用？**
> 它是**未決**，不是**已決為不做**。

⚠️ 記下它，是因為第三次一定會有 —— 而 §四 那一列（前提打錯對象）在下一次不會
自己現形。

#### 十一、與既有裁決的關係（C136 §八）

| 既有             | 本則做了什麼                                                               |
| ---------------- | -------------------------------------------------------------------------- |
| **D16**          | **不動。** §八 要求第 5 列自己報兩軸，§九 要求閘門整併逐支報               |
| **C137 §一**     | 不動，**並遵守它** —— §九 明文拒絕用「屎山／過度設計」論證任何增減         |
| **C70**          | 不動。§八 引用它當「一份報告蓋住多條規則」的先例，**並核過它指得到**       |
| **C87**          | 不動。§三 引用它 —— 第二套 runner 會再撞一次那個排程相依                   |
| **C108**         | 不動。§七 引用它證明「相容性風險」是已付的帳                               |
| **C109**         | 不動。§二 引用它當「少掉兩類那一軸會判錯」的前例                           |
| **C114／C115**   | 不動。§四 引用它們指名「延後層 3」會關掉什麼                               |
| **C118**         | 不動。§四 引用它 —— 五條承諾裡唯一有機械背書的那條掛在同一個 runner 上     |
| **C119**         | 不動。§六 引用它說明複雜度那一格已有，要判的只有 CRAP                      |
| **C121**         | 不動。§六 把它對單一分數的裁決套到 CRAP；§七 引用它的釘版與 `ignoreStatic` |
| **C125／C126**   | 不動。§九 引用它們指出整併已有的方向                                       |
| **C136／#180**   | 不動。§二 那張表的 `scope-check` 那一列照它註明已退化成登記                |
| **C152**         | 不動。§二 引用它的「同形」當形狀，**並標明這裡是沉默不是量測**             |
| **C154 §四**     | **遵守它** —— §一 說明本則不新增機械檢查故不觸發；§八 要求第 5 列補報      |
| **C155 §五**     | 不動。§一 與 §八 沿用它「一份報告不能蓋住多條規則」                        |
| **`TESTING.md`** | **不改。** 本則是否決一次替換，沒有任何一節需要重寫                        |

**沒有任何既有產出需要重跑。** 本則不推翻任何一則 C-條，它是一次逐列否決 ——
唯一的產出是 §八 的重貼標籤與 §十 的留白，兩者都是待裁項，不是已交付物。

### C157 — D16 的 ② 有一個誤讀形狀：問題從「被守的對象」搬到「守衛自己」；而按正確的問法，Browser Mode 那一格今天就有答案（2026-09-01）

⚠️ **量測基準 `19c0c06`**（C156 合併的那一支）。本則所有數字凍結在那一天。

#### 一、裁什麼、不裁什麼

**裁三件：**

1. **記下 ② 的誤讀形狀，並給它一個判準**（§二）。
2. **補上 C156 §八 懸置的那份 D16 兩軸報告** —— 兩軸都有分，所以 Browser Mode
   **通過 D16、不出局**（§三、§四）。
3. **更正一份研究稿的三處**（§六），其中兩處是同一個形狀。

**不裁四件：**

- **不裁要不要現在導入，也不裁走哪一個 provider。** 兩條路的成本差一個數量級，
  而**便宜的那條可能買不到最貴的那個盲點**（§五）。
- **不裁它算第一類還是第二類。** 那決定誰維護、門檻設給誰，射程比本則大。
- **不新增任何閘門、不碰任何門檻**（C137 §一）。
- **不裁研究稿在這棵樹上的地位**（§七）。

⚠️ **本則不新增任何機械檢查**，所以不觸發 C154 §四 的兩軸報告要求。§三／§四 那份
兩軸報告是**替 Browser Mode 補的**，不是本則自己的。

#### 二、⚠️ ② 的誤讀形狀：問題從被守的對象搬到守衛自己

C142 §一 的 ②「壞法安靜」問的是：**在這個檢查不存在的世界裡，被守的那個東西
壞掉時，看起來是不是綠的。**

一份背景調研把它讀成另一句：

> provider 未裝時，`vp test --browser` 會紅還是靜默回綠？

⚠️ **那是在問守衛自己壞掉時吵不吵，不是問被守的對象壞掉時安不安靜。**
兩件事可以同時成立而彼此無關：**一個吵得很大聲的工具，完全可以守著一個
今天就全綠的洞。**

⚠️ **這個形狀不是新的，樹上已經有兩處記著它的近親：**

- **C155 §四** 把「自我防護的夾具」另立、**不計迭代軸的分** —— 同一個方向：
  守衛守自己的那部分，不算它守住了什麼。
- 「**絆線要掛在被守的對象上**」是同一句話的另一種說法：斷言吃的資料要從被守的
  東西取，不是從守衛自己取。

**本則加的判準：**

> **填 ② 之前，先指名被守的對象是什麼，再問「它壞掉的那一天，今天的樹會不會
> 全綠」。⚠️ 如果那個句子的主詞是工具本身，就是問錯了。**

⚠️ 順帶把那個被問錯的問題也量了，**因為它雖然答錯了 ②，本身仍是一個真問題**
（基準 `19c0c06`）：

| 情境                                       | 結束碼 | 輸出                                                                                        |
| ------------------------------------------ | ------ | ------------------------------------------------------------------------------------------- |
| `vp test --browser`（樹上零 browser 設定） | **1**  | `Startup Error: Vitest received --browser flag, but no project had a browser configuration` |

**不靜默。** ⚠️ 但這只答了「設定缺席」那一種；**「設定在、provider 套件不在」沒有量** ——
量它得先寫一份 browser 設定，而那已經是導入的第一步，不是評估。

#### 三、按正確的問法，② 是「安靜」，而證據是樹上自己寫的兩句話

| 被守的對象               | 出處                                                                                                                                                        | 它壞掉的那一天，今天的樹 |
| ------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------ |
| Teleport 裡的內容        | `platform/ui/tests/alert-dialog.test.ts:13`：「**一個字都驗不到**」                                                                                         | **全綠**                 |
| 可及名稱（computedName） | `platform/ui/tests/dropdown-menu.test.ts:181`：「⚠️ **規格來源，本 repo 量不到**：page JS 沒有算可及名稱的 API，`computedName` 只存在於 DevTools protocol」 | **全綠**                 |

⚠️ **第二處的措辭要照抄，不要轉述成「無障礙沒在測」。** 同一個 package 的
`a11y.test.ts:181` 明說那一條「**守的是版面，不是無障礙**」——
**這棵樹知道自己量的是代理，而且寫下來了。** 把它讀成「這裡沒有無障礙測試」
會判錯下一步該補什麼。

#### 四、兩軸都有分，所以不出局 —— 而那不等於「該做」

| 軸             | 這一格                                                         |
| -------------- | -------------------------------------------------------------- |
| **① 對象在外** | ✅ Vitest 與 browser provider 都是上游的東西，這棵樹不擁有它們 |
| **② 壞法安靜** | ✅ §三 那兩列                                                  |

D16：**兩軸逐支問，至少一軸有分就留** → **Browser Mode 不出局。**

⚠️ **「不出局」不是「該做」。** D16 是**保留判準**，不是排程，也不是預算。
本則到此為止，導入與否見 §五 那個還沒答的問題。

#### 五、⚠️ 而便宜的那條路可能買不到最貴的那個盲點

磁碟實測（`node_modules/.pnpm`，基準 `19c0c06`）：`@vitest/browser@4.1.10` **3**
（peer 變體，已隨 vite-plus 進來）、`browser-playwright` **0**、`playwright` **0**
（對照組 stryker 5）。所以有兩條路：

| provider   | 新增相依             | 動到 `supply-chain`／airgap |
| ---------- | -------------------- | --------------------------- |
| preview    | **0**                | 否                          |
| playwright | 2 層（套件＋二進位） | **是**                      |

⚠️⚠️ **而 §三 那兩個盲點不是同一個難度：**

- **Teleport 那一個**要的只是「一個會 mount 的真 DOM」。
- **可及名稱那一個**要的是 **computedName**，而樹上那句話自己寫著它
  「**只存在於 DevTools protocol**」。

**所以便宜的那條路買得到第一個；第二個是待驗的。**

> **具名待驗**：Vitest browser mode 的 **preview** provider 取不取得到 computed
> accessible name。⚠️ **這是判讀，不是量測** —— 本則沒有量它。

⚠️ **而它必須在導入之前被答，不是之後。** 先裝了便宜的那條、再發現第二個盲點
還在，會得到一個「已經有 browser mode 了」的樹和一個沒有被補的洞 ——
而那兩件事在報表上長得一樣（同 C156 §二 記的形狀）。

#### 六、更正一份研究稿的三處，而其中兩處是同一個形狀

`reports/research/test-model-selection-2026-09-01.md`（⚠️ `reports/` 明文
「沒有任何機制在守」）。**三處都在它進版控之前就已改掉** —— 稿子照 `reports/`
的慣例走自己的 commit，本則不帶它：

| 原句                                        | 實情                                                                                                                     |
| ------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------ |
| 「#145 三處由規格發現」                     | `TESTING.md:186` 明說「**不是**手寫想出來的，是**機械跑一遍掉出來的**（#136）」—— 來源是 Stryker。而且是**三處不是四個** |
| 「WebSearch 確認 vitest-runner 最新 9.1.1」 | 與「樹上釘死 10.0.0 且全樹跑過」不可能同時成立（C121：78 支產品檔／6,125 個 mutant／0 errors）。**磁碟是事實來源**       |
| 「驗證一手來源：CRAP 定義無誤」             | 引到的是三份**二手**整理，Savoia & Alberg 的原文沒有取到                                                                 |

⚠️ **第一與第三處是同一個形狀：一句話宣稱自己被驗過，而驗它的動作沒有發生。**
那正是 C155 §二 記的東西（三處假陳述，同一句話傳了三手），差別在那一次的三處
分佈在三份文件，這一次的兩處在同一份稿子裡。

#### 七、⚠️ 一個具名的留白：研究稿能不能當裁決的證據

`reports/` 的 README 明文「**這個目錄沒有任何機制在守**」，而本則剛剛拿一份
那個目錄底下的稿子當入口。

> **研究稿可以當裁決的證據嗎？如果可以，它過期的時候誰會說話？**
> **未決，不是已決為不做。**

⚠️ **本則的做法是：稿子裡每一個被引用的事實，都回樹上重查了一次** ——
§三 的兩個行號、§五 的磁碟實測、§六 的三處更正，全部是這樣來的，而§六 證明
那個動作不是儀式（三處裡三處都不成立）。**那個做法沒有被推廣過，也沒有被裁過。**

#### 八、與既有裁決的關係（C136 §八）

| 既有             | 本則做了什麼                                                                     |
| ---------------- | -------------------------------------------------------------------------------- |
| **D16**          | **不動，並照它報數** —— §四 兩軸皆有分故不出局；⚠️ §四 同時標明「不出局 ≠ 該做」 |
| **C142 §一**     | **不動，並補一條判準** —— §二 的「先指名被守的對象」是它的操作化，不是修正       |
| **C137 §一**     | 不動，**並遵守它** —— 本則不論證增減任何閘門                                     |
| **C154 §四**     | 不動。§一 說明本則不新增機械檢查故不觸發                                         |
| **C155 §二**     | 不動。§六 引用它當「宣稱被驗過而沒驗」的同形先例                                 |
| **C155 §四**     | 不動。§二 引用它當「自我防護不計分」的近親                                       |
| **C156 §八**     | **不推翻，補上它明文懸置的那份兩軸報告。** C156 的結論、標籤更正全部不變         |
| **C156 §二**     | 不動。§五 引用它的「兩件事在報表上長得一樣」                                     |
| **C121**         | 不動。§六 引用它的全樹數字反駁那個 9.1.1                                         |
| **`TESTING.md`** | **不改。** §六 引用它 `:186` 更正稿子，本檔一節都不需要重寫                      |

**沒有任何既有產出需要重跑。** 本則不推翻任何一則 C-條 —— C156 §八 明文寫的是
「本則不裁它」，本則補的正是它留的那一格。§六 那三處**在稿子進版控之前就改掉了**，
所以沒有任何一份下游拷貝帶著它們。

⚠️ **上一版這兩處寫的是「未進版控」，而那句話自己會過期** —— 稿子照 `reports/`
的慣例走自己的 commit，落地的那一刻它就變假，**而沒有任何東西會說話**
（`reports/` 沒有機制在守，決策日誌也刻意不被 `doc-facts` 守）。改成陳述
**不會隨時間翻面的那一半**：三處是在它進版控之前改掉的。同 C136 §七 記的形狀。

### C158 — 同一列因為 provider 不同落在不同的層：便宜的那條路不是層 2／3，它是層 4；而擋著導入的那個待驗被撤銷，不是被答（2026-09-01）

⚠️ **量測基準 `6f9ca9c`。** 本則所有數字凍結在那一天。

#### 一、裁什麼、不裁什麼

**裁三件：**

1. **preview provider 這條路出局**，理由是它自己的 README 說它不是給 CI 的（§二）。
2. **「Browser Mode」在這棵樹上要分成兩列**：preview 那條落在**層 4／5**
   （人做的事、下發不了），playwright 那條才是**層 2／3 換執行基底**（§三）。
3. **撤銷 C157 §五 那個具名待驗**（preview 取不取得到 computedName）——
   ⚠️ **不是答它，是它問錯了層次**（§四）。

**不裁四件：**

- **不裁要不要導入 playwright provider**（**#230** 承接）。它撞到 **R4**，而擋著
  它的是一個具名待驗（§五）—— 那個待驗是**那張票的驗收條件**，不是註記。
- **不裁 webdriverio。** ⚠️ **本則沒有評估它**（§五末），而它是 README 並列的
  第三個選項 —— 不寫下來就是把三選一悄悄變成二選一。
- **不裁它算第一類還是第二類**（**#231** 承接）。
- **不裁研究稿的地位**（C157 §七 刻意留的那一格，**#232** 承接）。

⚠️ **本則不新增任何機械檢查**，不觸發 C154 §四；不論證增減任何閘門（C137 §一）。

#### 二、一手來源：那個 provider 自己說它不是給 CI 的

`node_modules/.pnpm/@vitest+browser-preview@4.1.10_*/…/README.md`，逐字兩句：

> For proper and stable browser testing, we recommend running tests in a
> **headless** browser in your CI instead. For this, you should use either
> `@vitest/browser-playwright` … `@vitest/browser-webdriverio`

> If browser didn't open automatically, **follow the link in the terminal to
> open the browser preview**.

**它會開一個瀏覽器視窗給人看，而它的作者建議 CI 用別的。**

⚠️ **這是它自己的 README，不是二手整理。** C157 §六 剛更正過一份稿子把三份二手
整理當成一手來源用 —— 本則的入口刻意選在磁碟上那一份。

#### 三、⚠️ 出局的方式，決定了一件比它自己更大的事

一個**只有人打開瀏覽器才會跑**的檢查，是什麼？

`TESTING.md` 對層 4／5 的裁定是：**「下發不了 —— 它們是人做的事，不是可以打包進
`platform/*` 交付的東西。」** preview provider 逐字符合那個描述。

**所以「Browser Mode」不是一列，是兩列：**

| provider   | 落在哪一層             | 為什麼                           |
| ---------- | ---------------------- | -------------------------------- |
| preview    | **層 4／5**            | 人開瀏覽器才會跑，打包不進交付物 |
| playwright | **層 2／3 換執行基底** | headless、跑得進 CI              |

⚠️ **這不推翻 C156 §八。** 那一則判的是「提案表把 Browser Mode 整列貼成層 4 是
錯的」，而**那仍然成立** —— 提案表指名的正是 Playwright provider。本則加的是：
**同一個名字底下有兩個東西，而其中一個真的在層 4。**

⚠️⚠️ **而這一格的形狀值得單獨記下**：C156 §八 更正了一個**錯的標籤**，本則發現
**那個標籤對其中一半是對的** —— 不是因為原判錯了，是因為**被貼標籤的對象不是一個
東西**。⚠️ 「先確認被判的是不是一個東西」在這棵樹上沒有被寫成規則，本則也不立
（立它要有第二個案例，而目前只有一個）。

#### 四、撤銷那個待驗，而撤銷的理由是升了一層不是答了它

C157 §五 寫的具名待驗是：

> preview provider 取不取得到 computed accessible name。
> **它必須在導入之前被答。**

**本則撤銷它，而且沒有去量它。** 理由：**就算答案是「取得到」，那個檢查也不會
在 CI 跑**（§二、§三）。問題被一個更上層的事實吃掉了。

⚠️ **這與「答案是否定的」不是同一回事，差別要寫清楚**：那個問題**沒有被回答**，
它是**不必回答了**。如果哪天 preview 變成 headless 可跑，這個問題會原樣回來。

⚠️ 順帶更正一句本則寫成之前在對話裡說過、而**沒有進版控**的話：「便宜的那條路
買得到 Teleport 那個盲點」。**不對** —— 它在 CI 裡什麼都買不到。買得到的是
「人坐在電腦前的時候看得見」，那是層 4 的東西。

#### 五、playwright 那條的代價落在 R4，而它有一半沒有被量

`tools/supply-chain/src/cli.ts:42` 寫著 **R4 來源證明 —— 在公網擷取、在封閉網路
離線驗證**。閘門今天的讀數（基準 `6f9ca9c`）：

> ✓ 供應鏈：**712 個套件、144 個原生二進位、12 個家族**（工具鏈 9／選用 1／
> 套件管理器 2），**4 個目標平台**皆有變體，來源綁定一致

**一手來源**（playwright.dev/docs/browsers）：

> By default, Playwright downloads browsers from **Microsoft's CDN**.

離線是官方支援的，經由 `PLAYWRIGHT_DOWNLOAD_HOST`／`PLAYWRIGHT_BROWSERS_PATH`
（同頁：「Sometimes companies maintain an internal artifact repository to host
browser binaries」）。

⚠️ **所以量到的是這一句：瀏覽器二進位的來源不是 registry，是另一個 host。**
而這棵樹的鏡像機制（`--manifest`／`--airgap`）是**繞著 registry 建的**。

⚠️⚠️ **沒有量的是另外兩件，而它們才是決定導入與否的那一半：**

> 1. **這棵樹的鏡像清單涵不涵蓋得了一個非 registry 的 CDN？**
> 2. **`supply-chain` 那四個數字（套件／原生二進位／家族／目標平台）會變成什麼？**

**這兩件是那張票的驗收條件，不是註記。** 在它們有答案之前，「導入的代價是兩個
套件」是一句沒有根據的話 —— 而對一個要交給政府採購案、可能落在封閉網路裡的
腳手架，那正是最貴的那一格。

⚠️ **`webdriverio` 本則沒有評估。** 它是 README 並列的第三個選項，而它的二進位
取得方式我沒有取到一手來源（`webdriver.io` 的兩個頁面一個 404、一個沒有這段）。
**寫下來是因為不寫就等於把三選一悄悄變成二選一。**

#### 六、⚠️ 三則連著沒有裁出導入決定，而每一則都拿掉了一個具體的錯選項

C156 §八 懸置它、C157 補上兩軸報告、本則否決其中一個 provider —— 讀起來像三次
沒有結論。**逐則看不是：**

| 則   | 拿掉了什麼                                             |
| ---- | ------------------------------------------------------ |
| C156 | 「它是層 4，所以填不了」這個錯的出局理由               |
| C157 | 「② 沒答案，所以判不了」這個錯的阻塞（② 的主詞問錯了） |
| C158 | 「有一條 0 相依的便宜路」這個錯的選項                  |

**剩下的那一個決定，代價第一次是具體的**（§五 那兩個待驗），而它是人的決定不是
本則的：它改變 fork v1 的團隊在封閉網路裡裝這棵樹會撞到什麼。

#### 七、與既有裁決的關係（C136 §八）

| 既有             | 本則做了什麼                                                                        |
| ---------------- | ----------------------------------------------------------------------------------- |
| **C156 §八**     | **不推翻。** §三 說明它判的那件事仍然成立，本則加的是「被貼標籤的對象不是一個東西」 |
| **C157 §五**     | **撤銷它的具名待驗，而不是回答它**（§四）。C157 的兩軸報告與判準不受影響            |
| **C157 §六**     | 不動。§二 引用它 —— 本則刻意把入口選在磁碟上的一手 README                           |
| **D16**          | 不動。C157 §四 判的「不出局」不變 —— 本則否決的是**一個 provider**，不是那一格      |
| **C137 §一**     | 不動，**並遵守它**                                                                  |
| **C154 §四**     | 不動。§一 說明本則不新增機械檢查故不觸發                                            |
| **R4**           | **不動，並指名它是那張票的所在** —— 本則不碰 `supply-chain` 的任何一行              |
| **`TESTING.md`** | **不改。** §三 引用它層 4／5 那句裁定，而那句話一字不動就吃得下 preview provider    |

**沒有任何既有產出需要重跑。** §四 撤銷的待驗**從未被任何東西消費**（C157 合併到
本則之間沒有 PR 引用它）；§四 末段更正的那句話**只活在對話裡，沒有進版控**。

### C159 — 「研究稿能不能當證據」C137 兩天前就裁過一半，而票面用檔名去找所以沒找到；要不要回樹上重查，取決於稿子在論證裡扮演什麼（2026-09-01，#232）

⚠️ **量測基準 `dac91ce`。** 本則所有數字凍結在那一天。

#### 一、裁什麼、不裁什麼

**裁四件：**

1. **Q1 的一半已經有答案，本則只是把它接上** —— C137 §一 ＋ `SCOPE.md:168`
   （§三）。稿子是**觀察的來源**，不是判準，**不得論證准入**。
2. **Q2 成立，但射程比票面窄**：「回樹上重查一次」只對**其中一種用法**是必要的
   （§四），而那一種**有兩個先例，兩個都在本則之前**。
3. **Q3 的答案是「人」，而現況有一個活的實例** —— 一份稿子在**進版控的那一刻**
   就自稱不進版控，三天沒有人說話（§五）。本則連同修掉它。
4. **不把 `reports/` 收進 `decision-ids` 的射程** —— 理由不是代價，是**它接不住
   §五 那個實例**（§六）。順帶更正那一行載重的過期數字（46 → 119）。

**不裁兩件：**

- **不裁 C158 §三 那條規則要不要成立。** 它的第二個案例**在本則裡出現了**
  （§七），而在供給它第二個案例的同一則裡把它升格，正是 C146 推翻過的形狀。
- **不裁 `reports/` 的守衛狀態要不要改。** `SCOPE.md:168` 把「沒有機制在守」
  明文登記為**刻意**，動它的射程比本則大。

⚠️ **本則不新增任何機械檢查**，所以不觸發 C154 §四 的兩軸報告要求。

#### 二、票面的前提低估了 —— 不是一次，是四則裁決六處，而最相關的那一則沒有寫檔名

`#232` 寫「**這個做法已經發生了一次**」。逐處量（基準 `dac91ce`）：

| 裁決                   | 處                       | 稿子                                  |
| ---------------------- | ------------------------ | ------------------------------------- |
| **C137**（2026-08-29） | 整則的來源，**未寫路徑** | `scaffold-sufficiency-2026-08-28`     |
| **C150** §三           | `DECISIONS-2.md:1409`    | `test-volume-30pct-2026-08-29`        |
| **C154** §二           | `DECISIONS-2.md:2024`    | `vue-complexity-blindspot-2026-08-30` |
| **C154** §三           | `DECISIONS-2.md:2096`    | 同上（未寫路徑，稱「一份研究報告」）  |
| **C157** §二           | `DECISIONS-2.md:2683`    | 同上（未寫路徑，稱「一份背景調研」）  |
| **C157** §六           | `DECISIONS-2.md:2765`    | `test-model-selection-2026-09-01`     |

⚠️⚠️ **票面漏掉 C137，而漏法本身是這一則最該記住的東西：** `#232` 是拿**檔名**
去掃的，而 **C137 通篇沒有出現任何 `reports/` 路徑** —— 那份稿子當時還沒進版控
（`dcc65f9`，2026-08-29 才解除忽略）。

⚠️⚠️ **而本則初稿把它數成「五處」，漏掉 C157 §二 —— 漏法值得寫下來，因為它是新的：**
掃描本身是對的，**`| head -40` 把輸出截掉了**，而被截掉的 281 行正好是 C157 §六 之後
與整個 C158。**截斷不會報錯，也不會空手 —— 它給一個看起來完整的答案。**

> **量「有幾處」的時候不要接 `head`。** 要不完整輸出，要不先 `grep -c` 拿總數
> 再決定怎麼讀。⚠️ 這一種**對照組接不住**：那 40 行每一行都是真的。

⚠️ 記在這裡而不是修掉了事，是因為 **§四 這一則自己在立的規則，正是「不要宣稱
一件沒有發生的驗證」** —— 一則講這個的裁決自己數錯，不寫下來就是同一個毛病。

> **掃「這個做法有沒有發生過」，不能掃它的拼法。**
> 檔名找得到引用，找不到「不寫檔名的使用」—— 而**做法比它的拼法早存在**：
> C137 用那份稿子的時候，它還沒有一個可以被掃到的路徑。

⚠️ **對照組**：以檔名掃出的四份稿子中，`scaffold-sufficiency` 的引用數是 **0**
（唯一沒有被任何裁決以路徑引用的一份），而它恰恰是**被用得最深的那一份**。
**引用數在這裡給出的是相反的答案**，同 C140 §四 記的那一條（引用數量的是
「後來的人回頭指它幾次」，不能當重要性的主判準）。

#### 三、Q1 的一半，C137 §一 已經裁了，而 `SCOPE.md:168` 把它推廣到全部

C137 的標題逐字寫著：**「那次研究產出的是觀察不是判準」**，§一 的裁決句是：

> **那次研究產出分類與觀察，不帶處置後果。**
> ⚠️ 不准拿 C137 論證刪掉哪一支閘門、或論證再加一支。

而 `SCOPE.md:168` 的 `reports/` 那一列已經把它從「那次」推廣到「那些」：

> ⚠️ 那些稿子是**觀察不是判準**，不得拿來論證准入（C137 §一）。

**所以 Q1 剩下的空格比票面框的小得多：**

| 問題                             | 狀態                                       |
| -------------------------------- | ------------------------------------------ |
| 稿子能不能當**判準**             | **已裁：不能**（C137 §一／`SCOPE.md:168`） |
| 稿子能不能論證**准入**           | **已裁：不能**（同上）                     |
| 稿子能不能當**論證的入口／觀察** | **開著** —— 本則答這一格                   |

**本則答：可以，而要付的代價寫在 §四。**

#### 四、三種用法，只有一種要「回樹上重查」——而那一種的兩個先例都在本則之前

⚠️ **「研究稿當證據」不是一件事。** 逐處讀 §二 那六處，稿子扮演三種不同的角色，
而它們要的驗證強度不一樣：

| 用法 | 稿子在論證裡是什麼                                                    | 要不要回樹上重查                   | 處                           |
| ---- | --------------------------------------------------------------------- | ---------------------------------- | ---------------------------- |
| 甲   | **稿子的文字本身就是被陳述的事實**（「它在這裡停下來、引了 `#187`」） | **不必** —— 讀它就是驗它           | C154 §二、C154 §三、C157 §二 |
| 乙   | **完整記錄的所在**，而數字已經抄進裁決本身                            | **不必驗稿子** —— 要驗抄進來的數字 | C150 §三                     |
| 丙   | **論證的入口**，事實從它來                                            | **必須**                           | **C137、C157 §六**           |

**裁決（書寫規則，不設閘門 —— 同 C137 §四 的處置形狀）：**

> **用法丙的每一個事實，引用之前回樹上重查一次；查完把來源寫成樹上的位置，
> 不是稿子的位置。** ⚠️ 判它是不是丙，問一句：**把那份稿子刪掉，這個論證還站得住嗎？**
> 站得住是甲或乙，站不住就是丙。

⚠️ **這條規則不是本則發明的，是把兩個已經做過的動作寫下來 —— 而兩次都抓到東西：**

| 先例         | 重查的方式                       | 抓到什麼                                                                                                                              |
| ------------ | -------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------- |
| **C137 §二** | 逐項問「有沒有處置」（五列的表） | 研究的**頭條發現是這棵樹自己已經寫過的話**（`TESTING.md` §六 第二列）；另一列因為「沒查到是刻意還是遺漏」被降級成**未決問題不是發現** |
| **C157 §六** | 逐句回樹上查                     | **三處查、三處不成立**，其中兩處宣稱被一手來源驗過而驗它的動作沒有發生                                                                |

⚠️ **`reports/` 外面還有第三個同形的先例，本則不把它算進上表但要指出來：**
**C131 §七** 更正了一份**架構檢視報告**的兩個數字（「14 支閘門」實際 11 個有
`src/cli.ts`；「28 份 `repoRoot` 複本」實際 26 處／5 種寫法），而 C131 §七 自己
寫著那份報告「三張卡有兩張在罵這個毛病」。⚠️ **它不在 `reports/research/` 底下**
（寫於那個目錄存在之前），所以它不改變上表的計數 —— **但它把「回樹上重查會抓到
東西」的復發率從 2/2 推到 3/3，而第三次發生在一份完全不同來源的文件上。**

⚠️ **兩次抓到的是同一類東西：稿子宣稱自己帶來了新東西，而樹上已經有相反的話。**
`TESTING.md` §七「這裡不重抄」與 C155 §二「同一句話傳了三手都沒人驗」是它的兩個
既有名字 —— **本則不新造名字**，只是指出它**在研究稿上**復發率是 2/2
（把上一段那個 `reports/` 外的鄰例算進來是 3/3）。

#### 五、Q3 有一個活的實例，而它在進版控的那一刻就變成假的

`reports/research/scaffold-sufficiency-2026-08-28.md` 的第 3 行與第 17 行：

> 這份報告**本身不進版控**（`reports/` 在 `.gitignore` 裡）
>
> 此後的漂移沒有任何機制在守（`reports/` **不進版控**，`doc-facts` 也刻意不守這一類）

**兩句都假，而且假在同一個字上。** `dcc65f9`（2026-08-29，「研究稿進版控 ——
`reports/research/` 解除忽略並登記」）**就是把這個檔加進版控的那一支**，
今天 `.gitignore:57-58` 寫的是 `reports/*` ＋ `!reports/research/`。

⚠️⚠️ **時序才是重點：那句話不是慢慢過期的，是被收它進來的那一支 commit 當場推翻的。**
Q3 問「稿子過期時誰說話」，答案是實測的：**三天，沒有人。**

⚠️⚠️ **而這棵樹兩天前才剛接住同一個形狀 —— 靠的是有人在寫的時候剛好注意到。**
C157 §八 末段記著：它自己有三處寫「未進版控」，而「稿子照 `reports/` 的慣例走
自己的 commit，落地的那一刻它就變假」，所以**在合併前**改成不會翻面的講法。
**同一個形狀，一個接住了一個沒有 —— 差別只在有沒有人剛好在看那一段。**
那正是 Q3 要的答案：**現行的防線是「寫的人碰巧注意到」，而它伸不到已經合併的稿子。**

⚠️ **兩句要分開處置，因為第二句只有前半假。** 「`doc-facts` 也刻意不守這一類」
**是真的**（`tools/doc-facts/tests/decision-ids.test.ts` 的 `OUT_OF_SCOPE = "reports/"`）——
把整句劃掉會連一句成立的話一起丟。**逐句量，不整段搬**（同 C155 §二 的形狀）。

**處置：兩處各就地加撤回標記，不重寫稿子**（C157 §六 的慣例 —— 稿子是凍結的，
改的是它上面那層記述）。⚠️ 這兩處**沒有下游產出** —— C137 §二 的表逐列標了處置，
那句話不在任何一列裡。

#### 六、不把 `reports/` 收進射程，而理由是它接不住 §五，不是代價太高

`#232` 把這個選項的代價寫成「會讓 `rigs/README.md:6` 那句變成假的」。
**代價是真的，但不是拒絕它的理由 —— 拒絕它的理由是它答不到 Q3。**

實測（基準 `dac91ce`，用 `decision-ids.test.ts` 自己那把尺 `\b([CDR])(\d{1,3})\b`）：

| 量到的                    | 值                          |
| ------------------------- | --------------------------- |
| `reports/` 底下有引用的檔 | **6**（`d4c4925` 當時是 3） |
| 引用總數                  | **119**（當時是 **46**）    |
| **指不到的編號**          | **0**                       |

**射程收進來，今天一條都不會紅。** 而 §五 那個缺陷**不是指不到的編號** ——
它是一句關於 `.gitignore` 狀態的假陳述。**這把尺量不到它，以後也量不到。**

⚠️ **順帶更正那一行載重的過期數字。** 那個註解寫著「代價是 3 個檔、46 個引用
不受守」，而它是 `#232` 拿來估這個選項的唯一依據。以同一把尺重現 `d4c4925`：
**3 個檔、46 個引用，逐檔組成也對得上**（38／7／1）—— 尺沒換、問的是同一個問題，
**它就是過期了，兩天長 2.6 倍**。

⚠️ **更正的是註解，不是門檻**（`AGENTS.md` 規則二）：`OUT_OF_SCOPE` 那一行本身
一個字不動，改的是它上面那段成本估計，而改法照 C137 §五 的老招 —— **標明那一天
那個 commit**，因為這個數字還會再過期。

#### 七、⚠️ 記下一件本則刻意不做的事：C158 §三 那條規則的第二個案例出現了

C158 §三 寫著：

> 「先確認被判的是不是一個東西」在這棵樹上沒有被寫成規則，本則也不立
> （立它要有第二個案例，而目前只有一個）。

**§四 就是那個第二個案例** ——「研究稿當證據」被當成一件事，而它是三件。

⚠️⚠️ **但本則不立它。** 在**供給第二個案例的同一則裡**把規則升格，正是 C146
推翻過的形狀：那一次連著兩則（C144 §五、C145 §六）把一個重讀出來的偽陽性寫成
裁決，而閘門三次都說綠。**本則只登記案例，把升格留給第三個。**

#### 八、與既有裁決的關係（C136 §八）

| 既有                                     | 本則做了什麼                                                                             |
| ---------------------------------------- | ---------------------------------------------------------------------------------------- |
| **C137 §一**                             | **不動，並接上它** —— §三 指出它已經裁掉 Q1 的一半，本則只補「入口／觀察」那一格         |
| **C137 §四／§五**                        | 不動。§四 沿用它「書寫規則，不設閘門」的處置形狀；§六 沿用它「標明那一天那個 commit」    |
| **C150 §三**                             | 不動。§四 把它歸為用法乙 —— **不是說它做錯了**，是說它那一種不需要重查                   |
| **C154 §二／§三**                        | 不動。§四 把它歸為用法甲，同上                                                           |
| **C155 §二**                             | 不動。§四／§五 兩處引它當同形先例                                                        |
| **C157 §二／§六／§七**                   | **§七 那個具名留白由本則填掉**（§三／§四）；§二 與 §六 歸為用法甲與丙（§四），兩處都不動 |
| **C158 §三**                             | **不動，並刻意不促成它** —— §七 只登記第二個案例出現了                                   |
| **C131 §七**                             | 不動。§四 引它當 `reports/` **外面**的同形鄰例，**並明說不算進 §二 的計數**              |
| **C146**                                 | 不動。§七 引它當「偽陽性被寫成裁決」的先例                                               |
| **`SCOPE.md:168`**                       | **不改。** §三 引用它 —— 那一列已經寫了本則要說的一半，一字不動就成立                    |
| **`rigs/README.md:6`**                   | **不改**，而 §六 拒絕了那個會讓它變假的選項                                              |
| **`decision-ids.test.ts`**               | **只改註解裡的成本估計**（46 → 119，附基準）。`OUT_OF_SCOPE` 那一行不動（§六）           |
| **`scaffold-sufficiency-2026-08-28.md`** | **兩處就地加撤回標記**（§五），稿子本文不重寫                                            |
| **C154 §四**                             | 不動。§一 說明本則不新增機械檢查故不觸發                                                 |
| **C137 §一（第二次引用）**               | **遵守它** —— 本則不論證增減任何閘門；§六 拒絕擴射程的理由是「接不住那個實例」，不是成本 |

**沒有任何既有產出需要重跑。** §五 修的兩句話**不在 C137 §二 那張處置表的任何
一列裡**，所以它沒有下游；§六 改的是註解，`OUT_OF_SCOPE` 的行為一個字不變。

### C160 — 分級 SLA 全樹只有一處宣告，而那一處不生效；所以要拆的不是分級，是那行讀起來像分級的字（2026-09-03，#244）

⚠️ **量測基準 `da06148`。** 本則所有數字凍結在那一個 commit。

#### 一、裁什麼、不裁什麼

**裁三件：**

1. **刪掉 `severity: HIGH,CRITICAL`，不加 `limit-severities-for-sarif: true`**
   —— 票面的甲會拆掉一道**這一輪剛剛證明過自己有牙齒**的閘門（§三），
   那是 `AGENTS.md` 規則二 明文禁止的方向。走乙。
2. **D13「修補 SLA」那一列的分級從未被實作** —— 就地加更正指標，
   那一列的原文**不改**（§四）。
3. **法遵對照表 §11 II ③ 的 SLA 引用一併對齊**（§五）。三份不一致裡
   它賭注最大：**那一份是交給稽核的。**

**不裁兩件：**

- **不裁「分級 SLA 該不該存在」。** 本則只確認它今天**不存在、也沒有實作**。
  要立分級，要一則新裁決 ＋ 一個真的實作它的機制，兩件一起做（§六）。
- **不裁 #245**，也不順手動這一步旁邊的任何鍵。

⚠️ **本則不新增任何機械檢查**，所以不觸發 C154 §四 的兩軸報告要求。
⚠️ **本則不增減任何閘門，也不動任何門檻**（C137 §一／C147 §二）——
閘門的判定**一個位元都沒有變**（§二）。

#### 二、行為零改動，而那是本則能成立的前提

`aquasecurity/trivy-action@ed142fd` 的兩個步驟，逐行照 CI log 抄
（run `33715021980`、job `100522273879`）：

| 步驟                            | log 說了什麼                                                                                                                          |
| ------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------- |
| Set Trivy environment variables | `set_env_var_if_provided "TRIVY_SEVERITY" "$INPUT_SEVERITY" "UNKNOWN,LOW,MEDIUM,HIGH,CRITICAL"`，而 `INPUT_SEVERITY: HIGH,CRITICAL`   |
| Run Trivy（`entrypoint.sh`）    | `INPUT_LIMIT_SEVERITIES_FOR_SARIF:`（空）→ `Building SARIF report with all severities` → `Running Trivy with options: trivy fs .scan` |

**那一行輸入被接收了、被轉成環境變數了，然後在 `format: sarif` 這條路徑上
被 action 自己蓋掉。** `exit-code` 是對蓋掉之後那份全嚴重度報告算的。

實測（本機 trivy `0.70.0`，與 CI 同版；`TRIVY_INCLUDE_DEV_DEPS=true`、
`--ignore-unfixed`、同一份 `.scan`）：

| 樹                   | 不給 `--severity`（＝ CI 實際跑的）      | `--severity HIGH,CRITICAL`（＝ yaml 寫的） |
| -------------------- | ---------------------------------------- | ------------------------------------------ |
| #243 之前（qs 未修） | **2 條，都是 MEDIUM** —— CI 紅的就是這個 | **0 條**                                   |
| `da06148`（今天）    | **0 條**                                 | **0 條**                                   |

⚠️ **所以刪掉它今天的價目是零**：兩欄都是 0，`exit-code` 兩種寫法都不開火。
本則不是靠這個零成本才成立的（§三 才是理由），但它排除了「這是在調鬆門檻」
這個讀法 —— **沒有任何一條漏洞因為本則而從紅變綠。**

#### 三、票面的方向反了：那個不生效的宣告，是這一輪**唯一**抓到 C133 過期的東西

`#244` 的標題把它寫成缺陷，處置是甲（讓宣告生效）。**把 #243 重播一次，
方向就倒過來了。**

`pnpm-workspace.yaml` 的 `qs` override 被抓到**兩次**，而兩次的嚴重度不同：

| 何時                   | 抓到什麼                                                                                        | Trivy 判      | 若 `severity: HIGH,CRITICAL` 生效 |
| ---------------------- | ----------------------------------------------------------------------------------------------- | ------------- | --------------------------------- |
| C133（併線那天）       | CVE-2026-8723，`>=6.15.2` 是**修好它**的那一筆                                                  | HIGH          | 照樣開火                          |
| **#243（2026-09-03）** | 那個下限**過期**了：`>=` 讓 qs 解到 6.15.3，而 6.15.3 自己中了 CVE-2026-82417 與 CVE-2026-82562 | **MEDIUM ×2** | ⚠️ **不開火**                     |

第二次是重點。**如果那個宣告當初真的生效，`main` 會一路綠著**，而 C133 訂的
下限到今天還躺在原地。

⚠️ 而**樹上沒有第二個東西會說話**：宣告生效的話，上傳到 Code Scanning 的
SARIF 也是被過濾過的，那兩條 MEDIUM 連告警都不會出現；`overrides` 的下限
過不過期，沒有任何機器人在報（Renovate 管的是相依宣告，不是
`pnpm-workspace.yaml` 裡一行 `>=` 的下限是否仍然足夠）。

**這就是為什麼甲踩到規則二而乙沒有**：甲拿掉的是一道剛示範過自己有用的
閘門，而它今天正好綠著 —— 那正是規則二 在講的「調鬆一個量到自己的門檻」。
乙不動閘門一個位元，只把檔案上那句與行為不符的話拿掉。

#### 四、全樹只有一處分級宣告，而它就是那一處

`git ls-files` 全掃 `severity`／`Severity`／`SEVERITY`（排除 `reports/` 與
`*.md`，基準 `da06148`）：

| 檔                                               | 是不是漏洞嚴重度分級                                                                                                            |
| ------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------- |
| `.github/workflows/tier2-security.yml:382`       | **是** —— 唯一一處，就是本則刪掉的那一行                                                                                        |
| `renovate.json`                                  | ⚠️ **零處** —— `vulnerabilityAlerts` 對每一條告警一視同仁：`schedule: at any time`、`minimumReleaseAge: null`、`prPriority: 10` |
| `tools/api-surface/src/{cli,compare}.ts`         | 否 —— API 破壞性變更的分級                                                                                                      |
| `tools/conformance/src/finding.ts`               | 否 —— 閘門 finding 的分級                                                                                                       |
| `.semgrep/rules.yml`                             | 否 —— semgrep 規則自己的 `severity`                                                                                             |
| `platform/eslint-config/tests/a11y.test.ts`      | 否 —— 同上                                                                                                                      |
| `tools/supply-chain/tests/sbom-negative.test.ts` | 否 —— SBOM 欄位                                                                                                                 |

**偵測那一半沒有分級，因應那一半也沒有。** D13 那張表寫下的三段
（critical 3 天／high 14 天／medium 下個 sprint）**在這棵樹上一天都沒有跑過**。

#### 五、那一行是**休眠**，不是死透 —— 這才是刪它而不是留著的理由

留一行不生效的字，代價看起來是零。**不是。**

§二 那張表的第一列說：`TRIVY_SEVERITY=HIGH,CRITICAL` **照樣被寫進
`trivy_envs.txt`**，蓋掉它的是 `entrypoint.sh` 裡 `format: sarif` 那條分支。
⚠️ 推論（不是實測，本則不假裝量過）：把 `format:` 換成 `table`，
那一行就會醒過來，**把閘門悄悄縮到只剩 high／critical**。

那是一個**由不相干的編輯觸發的陷阱**：改輸出格式的人不會想到自己同時
改了守備範圍，而 diff 上看得到的只有 `format:` 那一行。

⚠️ 同一句話反過來也成立，所以本則把它寫在檔案上：**要分級就要一則裁決，
不是補一個 `with:` 鍵。**

⚠️ **`sbom-negative.test.ts` 那一條斷言一併刪掉，而它不是為了讓測試變綠。**
那個 `describe` 的契約是「設定沒有漂掉」，而那條 `it` 的名字是「SCA 掃描發現
漏洞時會讓 job 失敗」—— 讓 job 失敗的是 `exit-code: "1"`（留著）；被釘住的
`severity: HIGH,CRITICAL` 釘的是**會少失敗一些**，與那條 `it` 的名字相反。
刪掉它之後，那條測試才與自己的名字相符。其餘四條 `it` 一字未動。

⚠️⚠️ **而本則刻意不補一條反向斷言**（`not.toContain("severity:")`）。理由不是
成本：那樣的斷言會讓**測試**去禁止一件本則沒有裁的事 —— §一 明說「分級該不該
存在」留給下一則，而一條紅燈寫著「不准加」的測試，等於把那則裁決先判掉了。
workflow 上那句「要分級就要一則裁決，不是補一個 `with:` 鍵」是對的強度：
**它指路，不設禁。**

⚠️ **代價要寫下來：這一行今天沒有機制在守。** 有人把 `severity:` 加回去，
全綠。與 C147 §四、C143 §八 同一種，靠人。可機械化的形狀在這裡，
好讓下一個人不必重新想：**那條斷言要跟一則「分級是否成立」的裁決綁在一起，
而不是單獨存在** —— 單獨存在的版本正是上一段拒絕的那一個。

#### 六、考慮過而否決

| 選項                                      | 代價                                                                                                                             |
| ----------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------- |
| **乙：刪宣告、改文件（採用）**            | 全嚴重度零容忍從此明文 —— ⚠️ **未來任何一條 LOW 都會擋住所有合併**，過去 24 小時剛示範過它會發生。這是真實的營運成本，本則承擔它 |
| 甲：加 `limit-severities-for-sarif: true` | ⚠️ 拆掉 §三 那道剛證明有用的閘門，踩規則二                                                                                       |
| 丙：維持現狀                              | 三份文件繼續各說各話，而 §五 那個陷阱留在原地                                                                                    |
| 乙＋為 low／medium 補分級 SLA 的條文      | ⚠️ **那要先有一個實作它的機制**，否則只是把同一個不一致換個地方寫。留給 §一 的「不裁」那一項                                     |

⚠️ **本則不主張零容忍比分級好。** 它主張的只有一件事：**檔案上寫的，
要是機器真的在做的那一件。** 分級要不要立，是下一則的事。

#### 七、本則不裁、不動的事

- **`DECISIONS.md:657`／`HANDOFF.md:539` 不改。** 那兩處引「critical 3 天」
  是在論證**版本流核准不能逐版做**；那個論證在零容忍之下**更強不更弱**，
  一個字不必動。
- **`DECISIONS.md:1426` 不改** —— 那是 C33 敘事裡的歷史引文（當時說的
  「由一個掃 0 個套件的閘門把關」在當時是真的）。
- **`TRIVY_INCLUDE_DEV_DEPS` 與拆 lockfile 兩件一個字未動**（C33／C34）。
  它們是這道閘門看得見東西的必要條件，與嚴重度無關。
- **`gate-roster` 不必動** —— `gates.ts` 檔頭寫明它對 `uses:` 步驟
  「什麼都不說」，只精確比對三類 `run:` 步驟。一個 `with:` 鍵不在射程裡。
- **不裁 #245。**

#### 八、與既有裁決的關係（C136 §八）

| 既有                              | 本則做了什麼                                                                                                   |
| --------------------------------- | -------------------------------------------------------------------------------------------------------------- |
| **D13「修補 SLA」那一列**         | **就地加更正指標，原文一字不改**（§四）—— 它是當時的決定，現行行為以本則為準                                   |
| **C133**                          | 不動。§三 引它當「同一行被抓到兩次、而兩次嚴重度不同」的來源                                                   |
| **C33／C34**                      | 不動（§七）。本則只刪 `severity:`，那兩件必要條件原封不動                                                      |
| **C137 §一**                      | **遵守它** —— 本則不論證增減任何閘門；§二 量到閘門行為零改動                                                   |
| **C147 §二**                      | **不觸發** —— 既不抬也不降。刪掉的是一行**從未參與判定**的宣告                                                 |
| **C154 §四**                      | 不動，**不觸發** —— 本則不新增機械檢查；§五 記下為什麼連那一條反向斷言都不加                                   |
| **#240（`category: trivy`）**     | **本則是它的下游**：診斷視圖修好之後，下一次紅燈才看得到是哪一條、什麼嚴重度 —— §三 那種重播才不必再靠本機重跑 |
| **`sbom-negative.test.ts`**       | **刪一條斷言**（§五）。`describe` 的契約、其餘四條 `it`、檔頭一字未動                                          |
| **`tools/compliance/src/map.ts`** | **改註記一句 ＋ 重產 `COMPLIANCE.md`**（§五）。條號、閘門清單、`coverage`、`owed` 全部不動                     |
| **`AGENTS.md` 規則二**            | §三 是它的一次適用：判斷門檻訂錯了要停下來告訴人 —— 而本則是人裁完之後的執行                                   |
| **`#244`**                        | **不由本則關掉**票面的甲；本則把票的方向倒過來並執行乙，甲留給 §一 那條「不裁」                                |

### C161 — 兩個驗收待驗都答了，而決定這張票的不是它們：票面「它買什麼」那兩列，一列今天就在守、一列 0 個新套件買得到（2026-09-04，#230）

⚠️ **量測基準 `fe81d45`（`v1.15.0`）。** 本則所有數字凍結在那一天。

#### 一、裁什麼、不裁什麼

**裁四件：**

1. **不導入 `@vitest/browser-playwright`。** ⚠️ 理由**不是代價太高** —— 是票面
   「它買什麼」那張表的兩列都被實測推翻（§三），而剩下的那一格從未量過（§四）。
   ⚠️ **這是一則可以被新量測翻掉的裁決**：翻它要的是 §四 那一格的讀數，不是重新
   權衡代價。
2. **`webdriverio` 明文出局**，依據是它**沒有受支援的離線路徑**（§五）——
   C158 §五 那個「不要靜默變成二選一」的警告**到此解除**。
3. **更正 C157 §三 第一列與 #230「它買什麼」第一列**（§三）。這正是研究稿
   §九 #1 指名要「一則 C」去做的事。
4. **更正 C158 §五 引的來源，而更正有兩個口徑**（§六）—— 順帶更正本則作者
   已貼在 #230 上的那句「兩個網域」。

**不裁三件：**

- **不裁「Browser Mode 出局」。** ⚠️ 射程要窄：本則否決的是**這張票的導入論證**，
  不是那條路。它獨有的那一格（版面／可見性／真焦點）還活著，而本則**沒有量它**。
- **不裁 `dom-accessibility-api` 進不進 `platform/ui` 的 `devDependencies`**
  （研究稿 §九 #2）—— 那是一支新的機械檢查，要走 D16 兩軸（C154 §四）。
- **不裁 `--airgap` 要不要補那個缺口**（§七 寫下重問條件，不留白）。

⚠️ **本則不新增任何機械檢查**，不觸發 C154 §四。**D16 那一格引 C157 §四，不重做。**
不論證增減任何閘門（C137 §一）。

#### 二、代價那一半：兩個具名待驗有答案了，而它們不是決定性的

#230 的兩個驗收條件，在丟棄式 clone 上量完（`pnpm add -D -w @vitest/browser-playwright@4.1.10 playwright`，pnpm `11.21.0`）：

| 待驗                       | 答案                                                                                                                   |
| -------------------------- | ---------------------------------------------------------------------------------------------------------------------- |
| ② `supply-chain` 四個數字  | 712→**716**、144→**145**、12→**12**、4→**4**（唯一新增的原生二進位是 `fsevents`，與瀏覽器無關）                        |
| ① 鏡像清單涵不涵蓋得了 CDN | **不能，而限制在 schema**：`--manifest` 條目欄位是 `['integrity','name','platform','scope','version']`，**沒有來源欄** |

⚠️⚠️ **決定性的對照**：跑完 `playwright install chromium`（磁碟 1.6 G）之後**原封不動
再跑一次**盤點，讀數**一個位元都沒變**。成因在 `tools/supply-chain/src/inventory.ts`
的 `isNative()` —— **整份盤點的定義域是 lockfile 的 `packages:` 區**，而瀏覽器二進位
不是 npm 套件、裝在 repo 之外的使用者快取。

⚠️ **這不是閘門壞了，是射程。** 而失敗的方式是安靜的：導入之後 `--airgap` 產出的
封閉網路前置條件文件會**照樣說 716 個套件、逐平台 95／92／54／82 MB**，一個字不提
那 1.6 G。⚠️ 這個缺口是真的，**但它不是本則否決導入的理由** —— 見 §七。

#### 三、⚠️⚠️ 而決定這張票的是另一半，它在一份稿子裡，本則自核過才用

`reports/research/test-boundary-minimal-packages-2026-09-02.md` 的標題就是這件事。
C159 §一 裁定稿子是**觀察的來源、不是判準**，所以本則**兩列都自己重跑過**：

**第一列（Teleport 裡的內容）—— 今天就在守。**

| 操作                                                                         | 本則自核（`fe81d45`）                  | 稿子（`55b7655`） |
| ---------------------------------------------------------------------------- | -------------------------------------- | ----------------- |
| 對照組                                                                       | **7 檔／399 條全綠**                   | 逐字相同          |
| 變異 T1：`UiAlertDialog.vue:169` 的 `<AlertDialogContent>` 加 `v-if="false"` | **12 紅／387 綠**                      | 逐字相同          |
| 還原                                                                         | `cmp` = 0、`git status --porcelain` 空 | —                 |

⚠️ **形狀**：C157 §三 引 `alert-dialog.test.ts:13` 的「一個字都驗不到」，而**那一行
在解釋這支檔案為什麼不用 `renderToString`** —— 它講的是 SSR，不是 happy-dom。同一支
檔案的做法是 `mount(..., { attachTo: document.body })`，對被傳送出去的內容斷言。
**「Teleport 裡的內容」在 `renderToString` 下與在 `mount` 下不是一個東西。**

**第二列（可及名稱）—— 0 個新套件買得到。** 本則自核 `pnpm-lock.yaml`：

```
3174:  dom-accessibility-api@0.5.16:
3175:    resolution: {integrity: sha512-X7BJ2yElsnOJ30pZF4uIIDfBEVgF4XEBxL9Bxhy6dnrm5hkzqmsWHGTiHqRiITNhMyFLyAiWndIJP7Z1NTteDg==}
```

**它在 `packages:` 區**，由 `@vitest/browser-preview@4.1.10 → @testing-library/dom` 帶進來，
已經在那 712 個套件裡。稿子量到的導入代價：**3 個檔、8 行，`packages:` 區 0 個新條目，
四個數字不變**。

⚠️ **兩套 accname 都不是 DevTools protocol**：happy-dom 這條走 `dom-accessibility-api`，
Browser Mode 那條走 `ivya`（Playwright `roleUtils` 的 fork）。樹上三處「只存在於
DevTools protocol」那半句，**兩條路都反證了它**。

#### 四、所以「它買什麼」剩下的是什麼 —— 而本則沒有量它

三支跑在 happy-dom 的測試檔**自己的檔頭登記了那些洞**：沒有版面與可見性計算
（`display: none` 的取消鈕照樣「聚焦得到」）、面板座標驗不到（floating-ui 要版面）、
`padding-right` 是 1024px（真瀏覽器約 15px）。

**那才是 Browser Mode 獨有的一格，而它從來沒有被量過代價效益。** 本則否決的是
「用 #230 票面那兩列去論證導入」，**不是這一格**。

#### 五、`webdriverio` 出局，而依據比「主機面更寬」硬

研究稿引 `DriverBinaries.md`（一手）：driver **一律**自動下載除非指定 `binary`，
瀏覽器走 `@puppeteer/browsers`，而 **Chrome for Testing 那條路沒有 mirror 環境變數**，
離線預熱要釘死完整版本號否則 `resolveBuildId` 還是要網路。

⚠️ **對照 playwright：離線是官方支援的**（`PLAYWRIGHT_DOWNLOAD_HOST`／
`PLAYWRIGHT_BROWSERS_PATH`）。所以 webdriverio 在封閉網路這一格**比 playwright 差**，
它不是一條更便宜的第三條路。**C158 §五 的警告解除。**

⚠️ 本則作者另用 WebFetch 讀過同一頁，**拿到的是小模型的摘要不是逐字** —— 那是
C157 §六 記的同一個坑，**已丟棄不用**。上面這段的依據是稿子引的逐字。

#### 六、來源更正有兩個口徑，而它們不衝突

C158 §五 引「By default, Playwright downloads browsers from **Microsoft's CDN**」，
單數。本則作者在 #230 留言更正為「**兩個網域**」。**兩個都不完整。** 自核
`packages/playwright-core/src/server/registry/index.ts`（GitHub raw，`main`）：

```text
const PLAYWRIGHT_CDN_MIRRORS = [
  'https://cdn.playwright.dev/dbazure/download/playwright',                    // ESRP CDN
  'https://playwright.download.prss.microsoft.com/dbazure/download/playwright',
  'https://cdn.playwright.dev',
];
```

⚠️ **三條 URL、兩個網域**（`cdn.playwright.dev` 出現兩次，路徑不同）。稿子寫「三個
host」、本則作者實測觀測到「兩個網域」—— **兩邊都對，量的不是同一個東西。**
鏡像要放行的是**網域**，預熱要試的是**三條 URL**。

⚠️ 這一段讀的是 `main` 分支、且經 WebFetch 轉述，**不是 v1.62.1 tag 的逐位元組自核**。
真要導入時要對著釘住的版本重讀。

#### 七、⚠️ §二 那個缺口沒有票在守，所以重問條件寫在這裡

裁不導入之後，`--airgap` 表達不了非 registry 來源這件事**失去了承載它的 PR**。
⚠️ **那正是 #249 開票的那個形狀**（留白活得比守它的票久 → 退化成無人主張的預設），
不能在同一週於本則手上重演。照 #249 自己訂的體例，寫下重問條件：

| 缺口                                              | 什麼條件下重問                                                                     |
| ------------------------------------------------- | ---------------------------------------------------------------------------------- |
| `--airgap`／`--manifest` 表達不了非 registry 來源 | **樹上出現第一個非 registry 來源的構件時** —— 今天一個都沒有，所以文件今天是完整的 |
| 版面／可見性／真焦點那一格（§四）                 | **那三支測試檔頭登記的洞，有任何一個造成過一次真的漏接時**                         |

⚠️ 兩列都**不要求現在做任何事**，也不要求為它們加斷言 —— 那會是新的機械檢查
（C154 §四）。

#### 八、與既有裁決的關係（C136 §八）

| 既有                | 本則做了什麼                                                                                                                                           |
| ------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **C156 §八**        | **不動。** 「它是層 4 所以填不了」仍是錯的出局理由；本則的出局理由是效益前提被推翻                                                                     |
| **C157 §三 第一列** | **更正**（§三）—— 引的那一行講 SSR，不是 happy-dom。這是研究稿 §九 #1 指名的那一則                                                                     |
| **C157 §四**        | **不動，並引它當 D16 兩軸的報告。** 「兩軸皆有分 → 不出局」仍然成立 —— ⚠️ 而「不出局」不是「該做」                                                     |
| **C157 §五**        | 不動（C158 §四 已撤銷那個待驗）                                                                                                                        |
| **C158 §三**        | **不動。** preview 落層 4／5 仍然成立。⚠️ 那條沒立的規則（「先確認被判的是不是一個東西」）第三、四個案例都在研究稿裡，**本則不升格它** —— 不是本則射程 |
| **C158 §五**        | **兩件：** 解除 `webdriverio` 的警告（§五）、更正引用的來源（§六）                                                                                     |
| **C159 §一**        | **遵守它** —— 稿子當觀察來源，§三 兩列都自核過才用                                                                                                     |
| **C137 §一**        | 不動，**並遵守它**                                                                                                                                     |
| **C154 §四**        | 不動。§一 說明本則不新增機械檢查故不觸發                                                                                                               |
| **R4**              | **不動，一行未碰。** §二 量到的是它的射程，不是缺陷                                                                                                    |
| **`TESTING.md`**    | **不改**                                                                                                                                               |
| **`#230`**          | **由本則關掉。** 兩個驗收待驗有答案（§二），而決定它的是 §三                                                                                           |
| **`#231`**          | **不動**（它算第一類還是第二類，#230 票面明文不屬於這張票）                                                                                            |
| **研究稿 §九 #5**   | 那個條件句（「若 #230 裁導入，Stryker 可見度變成驗收條件」）**不觸發** —— 本則裁不導入                                                                 |

**沒有任何既有產出需要重跑。** §三 更正的 C157 §三 第一列**從未被任何機械檢查消費**
（它是一則裁決裡的論證，不是斷言）；§六 更正的那句話在 `DECISIONS-2.md:2906` 與
#230 留言各一處，兩處都是引文不是判準。

⚠️ **順帶一個量測台的過期**：研究稿寫的 `vp -C platform/ui test` 在今天的樹上是
`Unexpected argument '-C'`。現行寫法是 **`vp run '@org/ui#test'`**（選擇器吃 package
name，不是目錄名）。

### C162 — 門檻過期檢查：射程十一格、獨立一支、紅燈給目標值；而票面的量法有一個它自己看不見的死角（2026-09-04，#226）

⚠️ **量測基準 `739fe89`。** 本則所有讀數凍結在那一個 commit。

`#226` 把三件事留給實作那一則裁：射程幾格、落在哪一層、紅燈給不給目標值。
本則裁這三件，並記下**量法換掉了**，以及換掉的理由不是偏好。

#### 一、裁三件

1. **射程十一格**（§二）—— 由**使用者**裁定；而本則加一句票面沒有的：
   十一這個數字**不是寫死的清單**。
2. **獨立一支，Tier 1**（§三）。
3. **紅燈給目標值**（§四）—— 而它在新量法底下是**免費的**。

**不裁：**

- **不動任何門檻。** 那一半 `c7166f4` 已經做完（產品碼 `max-lines-per-function`
  239 → 199），本則不重做也不推翻。
- **不裁「量不到」那一格該怎麼處置**（§六）—— 這道閘門遇到它時的處置是
  **停下來告訴人**，照 `AGENTS.md` 規則二。
- ⚠️ **不論證增減任何閘門**（C137 §一）。本則新增一道，理由是 `#226` 的
  D16 兩軸報告（§七），不是「閘門多寡」。

#### 二、① 射程：十一格，而「十一」是讀出來的不是抄下來的

`C147 §四` 的草圖寫「八個門檻」（產品碼四 ＋ 測試碼四），票面實測是**十一格**
（多了 `vue/max-props` 兩格與 per-file 放行那一格）。

⚠️ **那個差額就是手抄清單的成本，而它會再發生。** 所以這支工具**不維護
「哪些格子算門檻」的清單**：射程由 `vp lint --print-config` 決定 ——
任何 `["deny"|"warn", [{ …數字… }]]` 形狀的規則選項都算一格。今天是 11，
明天有人加第十二條複雜度規則，它自己就進來了。

⚠️ **代價是它會多抓**：一個「有數字選項但不是門檻」的規則會被算進來。
擋它的是 `tests/threshold.test.ts` 那條把今天的格數釘死的測試 ——
`vite.config.ts` 的門檻增減時它先紅，**由人判斷那一格算不算門檻**。
這與 `gate-roster` 的 `notInGateScript` 是同一個形狀：**加了就得說得出口**，
不是加了就閉嘴。

⚠️ **所以「十一格」不該被寫成一句耐久的散文。** 這棵樹記過「散文裡的計數會過期」，
而這一格有解藥：**格數由 `--print-config` 決定，今天是 11**。

#### 三、② 落在哪一層：獨立一支，Tier 1，排在範疇檢查之後

**Tier 1**，理由與名冊、範疇、無障礙、設計系統接縫那幾步逐字相同：
**它量的是原始碼本身，不會隨時間失效** —— 沒有新公布的 CVE 會讓一支函式變長。

**獨立一支**而不是塞進既有工具裡的一條規則。⚠️ C154 §四 已經裁明白兩種形狀
都要報 D16 兩軸，所以這裡不是為了免填而選 —— 選獨立一支的理由是**它要起子行程
跑四趟 `vp lint`**，而 `doc-facts`／`scope-check` 這幾支候選宿主都是
「讀檔案、比對、回傳」。把一個三秒的子行程量測塞進去，換到的是一道
兩件事共用一個結束碼的閘門。

⚠️ **不要把上一句讀成「這棵樹的閘門都不起子行程」——那句話是假的，而且
先例正好站在本則這一邊**：`promise-check` spawn 其他閘門、`exit-drill` 造一整棵
暫存樹、`theme-verify` 跑兩次 Tailwind 建置、`api-surface` 呼叫 `vp fmt`。
**它們四支都是獨立的工具**，沒有一支是塞在別人裡面的一條規則。

**排序**：`scope-check` 之後、`promise-check` 之前。名冊的順序規則是
**便宜的排前面**，而這一支約三秒 —— 名冊與範疇那兩步只讀幾個檔，便宜一個量級。

⚠️⚠️ **它是名冊裡最綁 `vp` 的一道**：三個驅動層旗標（`--print-config`、
`-f json`、`--debug=files`）少一個就跑不完。那是刻意的 —— 門檻的事實來源是
oxlint 自己，另寫一把尺去量就是造第二份會漂開的名冊（`gate-kit/flags.ts`
的檔頭列了三次這個形狀）。⚠️ **但這一點在 Tier 2 會違反 D2 保單**
（安全閘門不得依賴可替換的驅動層），所以它在 Tier 1 有第二個理由：
**它不是安全閘門**。換掉 vite-plus 的那天，這一道要跟著改。

#### 四、③ 紅燈給目標值 —— 而換了量法之後它是免費的

票面寫「給的話成本從一次 lint 變成一次搜尋」。⚠️ **在新量法底下那句話不成立**：
壓到地板的那一趟本來就把整個分佈攤開了，最大值是同一份資料上的一次 `Math.max`。

所以紅燈說得出：`max-lines-per-function／max 設在 239，而這個範圍裡的實測
最大值是 199` → `把它降成 199`。⚠️ 訊息裡**明講降是合法的**（C147 §二），
否則下一個人會在 `AGENTS.md` 規則二 前面停住 —— 那一條禁的是**調鬆**。

#### 五、⚠️ 量法換掉了：不是「就地改設定各減一」，是「符號連結農場 ＋ 壓到地板」

`#226` 的〈重跑〉那一節寫的是：備份 `vite.config.ts`、就地改、量完還原、
以 sha256 回讀。**人手動量一次可以這樣，一道每趟 `vpr gate` 都跑的閘門不行。**

|          | 票面                              | 本則                                                         |
| -------- | --------------------------------- | ------------------------------------------------------------ |
| 設定放哪 | 就地改真樹的 `vite.config.ts`     | 暫存目錄裡的**符號連結農場**，只有 `vite.config.ts` 是實體檔 |
| 改成什麼 | 每格**減一**                      | 每格壓到**它那條規則裡獨一無二的地板值**                     |
| 答得出   | 過不過期                          | 過不過期 **＋ 該降到多少 ＋ 量不量得到**                     |
| 違規歸屬 | 看違規行的路徑，**重算一次 glob** | 看訊息裡的 `Maximum allowed is N`，**歸屬是 oxlint 算的**    |

三個理由，第三個最硬：

1. **它不寫版控追蹤中的檔案。** 就地改的話，每趟閘門都對 `vite.config.ts`
   寫兩次；中途被砍掉時樹上留著一份被改過的設定，而沒有東西會說。
2. **地板值一次答完三個問題**（§四、§六）。
3. ⚠️⚠️ **地板值逐格不同，於是不必抄第二份 `files:` 樣式。** 一條規則配在多個
   範圍上時，票面那個做法要照路徑重算一次 glob —— 那是 `vite.config.ts` 的
   `files:` 在這棵樹上的**第二份副本**，而兩份副本漂開時兩邊都是綠的。
   每格給不同地板值之後，oxlint 自己在訊息裡寫著歸屬。**多一個 override、
   改一次 glob，這支工具一行都不用動。**

⚠️ **農場這一招成不成立完全靠一件事**：oxlint 走得進符號連結，掃到的檔案集合
與真樹一模一樣。實測 **257 = 257，逐行相同**。走不進去的話它會掃到零個檔、
回綠 —— 十一格全部「量不到」。所以那條比對是夾具，不是保險（§八）。

#### 六、⚠️⚠️ 票面的量法有一個死角：兩個相反的狀態映到同一個讀數

「某條規則減一之後零違規 ⇒ 過期」漏掉了另一種零違規：

| 狀態       | 減一後 | 處置                         |
| ---------- | ------ | ---------------------------- |
| **過期**   | 零違規 | 降到實測最大值 —— **有答案** |
| **量不到** | 零違規 | **沒有數字可以降到**         |

⚠️ 這是 C152 記過的那一種（**同形**：代理量對、對照組正常、判準沒換，
而兩個相反的答案映到同一個讀數）。**前五層的量測防護一條都接不住它。**

⚠️ **而它不是假想的**：測試碼那格 `vue/max-props: 2` 今天由**兩支 fixture
`.vue`** 撐著（`tests/threshold.test.ts` 之外的量測顯示 `2 個測得到的單位`）。
刪掉那兩支，這一格就懸空了 —— 而票面的量法會叫人「把它降到某個數字」，
**降到哪裡沒有答案**。

⚠️⚠️ 更貴的是：C147 §五 **明文拒答過 `.vue` 那一格**（「要先答『元件的邏輯有多少
在 template 裡』，不是先答一個數字」）。照票面的量法做，這道閘門會在
`vue/max-props` 那一格**用一個數字回答一個被明文留白的問題**。

壓到地板就把兩者分開了：地板之上仍然零違規 ⇒ 那個範圍裡連一個測得到的單位
都沒有。**它是獨立的一種紅燈**，訊息說的是「不要隨便填一個數字，依規則二
停下來告訴人」。

#### 七、⚠️ 一個新的排程互斥，而擋它的那句舊論證在寫下的當天是對的

`tools/promise-check/tests/cli.test.ts` 的檔頭寫著「排程相依查過了：**不需要**
`dependsOn`」，論證是：`slice-gen` 的端對端測試會在真的 `features/` 底下建一個
`zz-` 切片，但它整份沒有碰過 git，而 `promise-check` 與 `spec-report` 的事實來源
都是 `git ls-files` —— 沒進 index 的東西兩支都看不見。

**那個論證是對的，而它不涵蓋新的相依方。** `threshold-check` 不走 `git ls-files`，
它跑 `vp lint` **掃磁碟**，於是那個 `zz-` 切片它看得見。而它分兩次取檔案清單
（一次真樹、一次農場），中間隔著一趟 `--print-config`：那幾百毫秒剛好落在切片
被建起來又刪掉的窗口裡，兩份清單就對不上。

⚠️ **紅燈會說「量測台自己壞了」** —— 一則指向這支工具、與真正原因無關的訊息，
而且是**間歇性**的，所以它不會在寫下來的那天被抓到。

處置與 C87 同一條：**修排程，不修斷言。** `tools/threshold-check` 與
`tools/promise-check` 各多一份 `vite.config.ts`，`test` 這個 task `dependsOn`
`@org/slice-gen#test`。⚠️ `vp run --parallel` 會忽略 task 相依，這道防護會安靜
失效 —— CI 跑的是 `vp run -r test`，沒有 `--parallel`。

⚠️ 通則寫在這裡，因為它比這一格大：**一句「查過了，不需要」記的是當天那批
相依方**。新增一個相依方的時候，要回去讀那句話的**論證**，不是它的結論 ——
這一則的論證有一個具名的前提（事實來源是 `git ls-files`），而新的相依方
不滿足它。

#### 八、D16 兩軸（C154 §三 的填寫要求）

`#226` 票面已經填過這兩軸，本則照抄結論並補上 C154 §三 第 2 條要的**級別**。

| 軸       | 這一支                                                              |
| -------- | ------------------------------------------------------------------- |
| **交付** | **零。** 採購／資安／法務沒有一個收件人在意一個 lint 門檻設得準不準 |
| **迭代** | **有分**，級別是「**發生過的實例**」                                |

**① 對象在外** —— 它報的不是自己被改壞，是**門檻與樹的實際形狀脫節了**。
造成脫節的是別人為了別的目的做的重構（把 `runFull` 拆短的人不是在對付這道閘門）。

**② 壞法安靜，級別「發生過的實例」** —— ⚠️ 那個安靜的壞法**真的發生過，有證據**：
`max-lines-per-function` 高了 **40 行**（239 而實測 199），而 `vp lint` exit 0、
`vpr gate` 全綠、`pnpm ready` 全綠，**安靜了 15 支 commit**（`2846949` → `e2e4a06`）。
發現它的是人回頭查，不是任何機制。

⚠️ 票面在這一格寫的是「實測」，而那**不是 C154 §三 那三級裡的任何一個**。
本則把它歸為「發生過的實例」——與 C148 §七 同級。

#### 八、自我防護的夾具（C154 §三 第 3 條：另立一節，不計迭代軸的分）

⚠️ **這支工具的每一種紅燈都是「零違規」形狀的，而「什麼都沒量到」也是零違規。**
所以量測台自己要有七條夾具，它們守的是這支工具有沒有量對，**不是別人的程式碼
有沒有壞** —— 依 C154 §三 第 3 條，**這一節不計迭代軸的分**。

1. **改寫格數 == `--print-config` 讀出的格數**。對不上代表萃取樣式漏了一種寫法。
   ⚠️ 這一條在實作當天就開火了：第一版的樣式要求規則名帶引號，而
   `complexity:` 在 `vite.config.ts` 裡**沒有引號** —— 十一格漏成九格。
2. **農場與真樹掃到的檔案清單逐行相同**（257 = 257）。§五 那一段全靠它。
3. **含 `Maximum allowed is` 卻讀不出數字的訊息要被點名**。上游換一次措辭
   就會讓十一格全部變成「量不到」——那是一個看起來有在工作的綠燈。
4. **地板值必須低於門檻**，否則探針量不到分佈。
5. ⚠️ **同一條規則的兩格不得拿到同一個地板值**。混在一起的話，其中一格過期
   而另一格沒有時，混出來的最大值會讓它靜靜地回綠。
6. ⚠️⚠️ **檔案清單裡必須有一個釘住的檔案**（`tools/gate-roster/src/cli.ts`）。
   第 2 條（兩邊逐行相同）與「非空」**都接不住「兩邊是同一坨垃圾」** ——
   檔案清單走 `--debug=files`，而它印到哪一條輸出串流是上游的事（今天是 stderr）。
   哪天它往 stdout 印一行 `note:`，兩趟都會拿到同一個單行字串：**長度是 1 不是 0、
   兩邊還相等，於是綠燈，而什麼都沒量到。** 實測把釘子改成一個不存在的路徑，
   這一條會紅。
7. ⚠️ **量測那一趟自己的射程要對**（`-f json` 回的 `number_of_files` == 真樹檔數）。
   第 2、6 條問的是 `--debug=files` 那兩趟，而**讀數來自另一趟呼叫** ——
   拿一趟的射程去替另一趟作證，是這棵樹記過的「同形」的一個變種。

#### 九、實測：紅燈與綠燈都跑過，而它**出廠就是綠的**

| 跑法                                        | 結果                                                          |
| ------------------------------------------- | ------------------------------------------------------------- |
| 今天的樹（199）                             | ✓ 11 格全部 `設定值 == 實測 max`，`vpr gate` EXIT=0           |
| 把 199 改回 **239**（`c7166f4` 之前的狀態） | ✗ `門檻過期`：`設在 239，而實測最大值是 199` → `把它降成 199` |
| 還原                                        | `sha256` 逐位相同（`580b915b…`），`git status` 乾淨           |

⚠️ **「出廠就是綠的」正是 C147 §四 說的「失敗方向是安全的」兌現**：
程式碼變好才會紅。它今天擋下 0 件事，而那不是它沒用 ——
**降那一半在它之前擋下的是 0 件事，而帳上欠著 40 行。**

⚠️ **順帶一個量測台的坑，第九種**：`diff <(cat a) <(cat b)` 對兩個**確實不同**的
檔案回報了 `[ok] Files are identical`（rtk 改寫）。驗逐字相同一律用 `cmp` 或
`shasum`，這棵樹已經記過八種，這是第九種。

#### 十、本則不裁的事

- **不動任何門檻**（§一）。
- **不裁 `.vue` 那一格**。C147 §五 的留白原封不動 —— §六 說明的是這道閘門
  **不會**替它填一個數字，不是替它填了。
- **不裁「量不到」出現時該拿掉那一格還是留著**。那時候由人裁，而閘門的處置
  是停下來說。
- **不推翻 C147**。本則是它 §四 的兌現，§一 到 §三、§五 一字不動。

---

### C163 — 第二份規格：一道閘門第一次證明自己會紅，而「每份規格都要有對照組」原來是整批問的（2026-09-05，接 C162）

⚠️ **量測基準 `85c0cb7`**（本則的變更疊在它上面）。本則不裁任何法規、不論證增減閘門（C137 §一）。

#### 一、做了五件事

| 東西                                     | 是什麼                                                     |
| ---------------------------------------- | ---------------------------------------------------------- |
| `specs/gate-thresholds.feature`          | 兩個場景：門檻被抬高必須紅、原封不動必須綠                 |
| `threshold-check` 讀 `--root`            | **只換被驗的那份設定**，被 lint 的樹與 `vp` 的位置不跟著走 |
| `SANDBOX_FILES`                          | 沙盒契約多一項根層檔案：`vite.config.ts`                   |
| `checkPromises` 的對照組檢查改成**逐份** | §四 —— 這一項不是計畫裡的，是被既有的反向測試撞出來的      |
| 兩支 package 各一份 `vite.config.ts`     | §七 —— 排程互斥，也不是計畫裡的                            |

`tools/threshold-check` 因此是這條線上**第一道用規格證明自己會紅的閘門**：
C162 交出來的時候，它會不會紅只有它自己的單元測試說了算，而那些測的是判定函式，
不是 CLI 的結束碼。

#### 二、⚠️ 它**不是**第六條承諾 —— 而檔名就是那個決定的載體

`HANDOFF.md`〈v1.0.0 承諾什麼〉那五條講的是交給採用團隊的**能力**。
`threshold-check` 對不到其中任何一條，而它落的那一類樹上早有先例：
`tools/doc-facts` 的登記理由明文寫著「硬把它塞進某一條承諾表會重演 C70 那個錯」。

檔名走 `promise-N-` 等於宣告第六條，而那會讓三份文件裡的「五條承諾守到一條」
同時失真 —— **那是人的裁決，不是寫規格的人順手做的決定**。所以：

- 檔名**不編號**，`promise-2` 到 `promise-5` 仍然留給 C118 §五 點名的那四條。
- `TESTING.md` 與 `HANDOFF.md` 那句「五條承諾守到一條」**一個字都沒有改**，
  只在 `TESTING.md` 層 3 補了一句說明第二份規格是什麼、以及它為什麼不算第六條。
- ⚠️ **本則不裁它算不算第六條。** 要編進去，是人的事。

#### 三、`--root` 只換被驗的那份設定，而基準那一趟也改走農場

C127 §一 判過：`--root` 指的是**被驗的對象**，不是「這支工具跑在哪」。
這裡被驗的對象是 `vite.config.ts` 裡那幾格門檻的數字；被 lint 的那棵樹是**素材**，
留在真樹（連同 `vp` 的位置與那個釘住的錨點檔）。

⚠️ **基準那一趟從「直接量真樹」改成「農場裡放目標的設定原封不動」。**
理由是那個目錄可能只有一份設定檔（承諾檢查的沙盒就是），在那裡跑 `vp lint`
會掃到零個檔 —— 而掃不到與「這棵樹很乾淨」長得一模一樣。

⚠️⚠️ **`realFiles` 仍然量真樹，沒有跟著改成農場。** 換成「兩個農場互比」的話，
C162 §八 第 2 條那條夾具問的東西會從「符號連結農場有沒有重現這棵樹」變成
「兩個農場同不同意」—— 恆真，而它是整個符號連結那一招唯一的地基。

`vp lint` 的趟數沒變，仍然是五趟。

#### 四、⚠️⚠️ 順帶量到：「至少要有一條對照組」的射程是**整批**，不是逐份

C118 §三 立過一條保險：規格裡必須有一條期待綠燈的對照組，否則沙盒建壞掉時
每一條「必須紅」都會成功變紅。實作是
`scenarios.filter(!expectRed).length === 0` —— **射程是整批的**。

一份規格的時候，「整批」與「這一份」是同一件事。**第二份進來的那一刻它就鬆了**：
把 `promise-1` 的對照組改成「必須紅」，`gate-thresholds` 的那條還在，
於是這道閘門全綠 —— 而 `promise-1` 的沙盒從此壞掉了也不會有人發現。

⚠️ **這正是這道閘門在別處擋的形狀**（`doc-facts` 的 `unguarded`、`spec-report`
的第四態）：一條保險在某個維度上被稀釋掉，而症狀是全綠。

⚠️ **它不是想出來的，是撞出來的**：既有的反向測試「把對照組的必須綠改成必須紅
→ 沒有對照組」在加了第二份規格之後不再紅。改成逐份問之後它回來了。

#### 五、實測：四個方向都短路過

| 短路掉的                    | 哪一條紅           | 紅成什麼樣子                                       |
| --------------------------- | ------------------ | -------------------------------------------------- |
| 沙盒不複製 `vite.config.ts` | 對照組那條         | 〈承諾誤擋〉＋「`<沙盒>` 底下沒有 vite.config.ts」 |
| 破壞手法 `+900` 改成 `+0`   | 「必須紅」那條     | 〈承諾沒有牙齒〉                                   |
| 對照組檢查留在整批問        | （既有的反向測試） | 那條測試綠了 —— 逐份問之後才紅                     |
| `--root` 只拿去看目錄在不在 | 那條新的整合斷言   | 紅燈裡的數字是 `5`（真樹），不是 `500`（那一份）   |

⚠️⚠️ **第四列是這一輪唯一沒有別人接得住的方向。** 那種實作會通過
`threshold-check` 原有的每一條單元測試、通過 `probeRootSupport`（兩趟輸出確實
不同）、也通過沙盒的逐位元組比對，然後讓「必須紅」那條報〈承諾沒有牙齒〉——
一則指向閘門或規格、而兩者都不是原因的紅燈。擋它的只有一件事：
**紅燈裡那個數字必須是目標設定裡的數字，不是真樹的。**

⚠️ 第一列同時驗掉了 C118 §三 的排序：對照組**先跑**，所以後面那條會被誤讀的
〈承諾沒有牙齒〉根本沒有印出來。

手動指一份門檻被抬高的設定，紅燈逐字是：

```
✗ [門檻過期] max-depth／max 設在 500，而這個範圍裡的實測最大值是 5（1235 個測得到的單位）
```

#### 六、代價，而它**逐場景累加**

| 量的                 | 之前                        | 現在                  |
| -------------------- | --------------------------- | --------------------- |
| `promise-check` 一趟 | 1.12s（C127 §六，4 個場景） | **10.0s**（6 個場景） |
| `vpr gate` 端到端    | —                           | 約 17s（快取暖）      |

三趟取最小值。多出來的九秒全部是 `tools/threshold-check`：兩個場景各跑一次、
`probeRootSupport` 再跑一次，每次約三秒。

⚠️ **這是每個 fork 每次 `vpr gate` 都要付的，而且逐場景累加** —— 下一條指名
這道閘門的承諾要再加三秒。C127 §六 記過同一句話，那時候的單位是毫秒。

⚠️ 測試那一側也漲了：`negative.test.ts` 每一次呼叫都要把第二份規格一起餵進去
（`BREAKAGES` 是全樹共用的一張表，只餵一份會讓另一份的破壞手法**全部變孤兒**，
於是 `checkPromises` 在接線那一關就回頭，下面每一條斷言都拿到無關的紅燈）。
四條因此真的會跑到閘門，逾時放寬到 60 秒。⚠️ **放寬的是逾時，不是門檻** ——
那幾條量的是「改壞規格會不會被抓到」，不是「它跑多快」。

#### 七、⚠️ 一個新的排程互斥，而擋它的那句舊論證在寫下的當天是對的

`tools/promise-check/tests/cli.test.ts` 的檔頭寫著「排程相依查過了：**不需要**
`dependsOn`」，論證是：`slice-gen` 的端對端測試會在真的 `features/` 底下建一個
`zz-` 切片，但它整份沒有碰過 git，而 `promise-check` 與 `spec-report` 的事實來源
都是 `git ls-files` —— 沒進 index 的東西兩支都看不見。

**那個論證是對的，而它不涵蓋新的相依方。** `threshold-check` 不走 `git ls-files`，
它跑 `vp lint` **掃磁碟**，於是那個 `zz-` 切片它看得見。而它分兩次取檔案清單
（一次真樹、一次農場），中間隔著一趟 `--print-config`：那幾百毫秒剛好落在切片
被建起來又刪掉的窗口裡，兩份清單就對不上。

⚠️ **紅燈會說「量測台自己壞了」** —— 一則指向這支工具、與真正原因無關的訊息，
而且是**間歇性**的，所以它不會在寫下來的那天被抓到。

處置與 C87 同一條：**修排程，不修斷言。** `tools/threshold-check` 與
`tools/promise-check` 各多一份 `vite.config.ts`，`test` 這個 task `dependsOn`
`@org/slice-gen#test`。⚠️ `vp run --parallel` 會忽略 task 相依，這道防護會安靜
失效 —— CI 跑的是 `vp run -r test`，沒有 `--parallel`。

⚠️ 通則寫在這裡，因為它比這一格大：**一句「查過了，不需要」記的是當天那批
相依方**。新增一個相依方的時候，要回去讀那句話的**論證**，不是它的結論 ——
這一則的論證有一個具名的前提（事實來源是 `git ls-files`），而新的相依方
不滿足它。

#### 八、D16 兩軸（C154 §三 的填寫要求）

軸填的是 §四 那條新的機械檢查（逐份對照組）；規格本身是 C162 那道閘門的
**證明**，不是新的檢查。

| 軸       | 這一次                                              |
| -------- | --------------------------------------------------- |
| **交付** | **零。** 沒有任何收件人在意這棵樹的規格有沒有對照組 |
| **迭代** | **有分**，級別是「**發生過的實例**」                |

**① 對象在外** —— 它報的不是自己被改壞，是**寫規格的人把那份規格的保險拆掉了**。
拆的動作合法、diff 很小，而且完全可以是無意的。

**② 壞法安靜，級別「發生過的實例」** —— ⚠️ 就在這一輪：整批問的版本在加了第二份
規格之後，對「`promise-1` 的對照組被改成必須紅」**回綠**。發現它的是既有的反向
測試，不是人回頭讀 —— 但那條測試會發現，靠的是它剛好也被這次改動掃到。

#### 九、本則不裁的

- **不裁它算不算第六條承諾**（§二）。
- **不裁要不要把 `threshold-check` 加進 `tools/compliance` 的對照表。**
  那張表的分母是條號，而這道閘門對不到任何一條。
  ⚠️ **順帶更正一件說過的話**：`threshold-check` **不在**「未證明會紅的閘門
  5／18」的分子或分母裡 —— 那張表（`tools/compliance/src/map.ts`）根本沒有它。
  之前把它算成那 5 之一是錯的。
- **不動 C118 §五 那張「哪幾支接得上」的表。** 那是 2026-08-23 的記錄，
  它當天是對的；今天多了 `theme-verify`（C127）與 `threshold-check`（本則）兩支，
  記在各自的那一則。
- **不改 `promise-check` 綠燈那句「N 條承諾」。** 它今天會印「2 條承諾」，
  而其中一條不是那五條之一 —— 要不要改那個字，繫於 §二 那個沒有被裁的問題。

---

### C164 — `specs/` 收的是「腳手架對採用團隊的承諾」，不是「HANDOFF 那五條」；所以它不編號，而那句話仍然沒有機制在守（2026-09-05，接 C163 §二）

C163 §二 把「`gate-thresholds.feature` 算不算第六條承諾」明文留給人裁。這一則裁它。
⚠️ **本則不新增任何機械檢查**，所以沒有 C154 §三 的兩軸欄要填 —— 為什麼不加，見 §四。

#### 一、裁決

1. **`specs/gate-thresholds.feature` 不是第六條承諾，檔名不編號。**
2. **判準（給下一份用）：它滿不滿足 `HANDOFF.md`〈v1.0.0 承諾什麼〉那五節裡的某一節？**
   滿足 → 檔名走 `promise-N-`；不滿足、而它仍然是「腳手架對採用團隊的承諾」→ 不編號。
3. `promise-2` 到 `promise-5` **保留**給 C118 §五 點名的那四條。
4. 三份文件裡的「五條承諾守到一條」**不動**。

#### 二、為什麼不是第六條 —— 判準不是這一則發明的，它寫在工具的檔頭

`tools/promise-check/src/cli.ts:30` 逐字：

```
這一支      specs/*.feature            第一類 —— 腳手架對採用團隊的承諾
```

⚠️ **它寫的不是「HANDOFF 那五條」。** 所以這個目錄是**超集**，那五條是**子集**。

採用團隊 fork 之後連 `vpr gate` 一起繼承，C147 §二 那條紀律也一起繼承 ——
`gate-thresholds` 滿足上面那個定義。而它滿足不了那五節裡的任何一節：
那五節講的是**能力**（架構、樣式接縫、代幣與配色、資安前置過濾），
它講的是**量那些能力的尺會不會過期**。

超集與子集的關係，就是為什麼那三句話一個字都不用改 —— **不是因為繞過去了，
是因為它們本來就在講子集。**

先例是現成的：`tools/doc-facts` 的登記理由明文寫著「硬把它塞進某一條承諾表會
重演 C70 那個錯（同一份證據掛在兩條承諾上，而空著的那一條沒有人發現）」。

#### 三、判準寫在哪，以及誰是權威來源

| 位置                                 | 寫了什麼                                          |
| ------------------------------------ | ------------------------------------------------- |
| `specs/gate-thresholds.feature` 檔頭 | 這一條不是那五條，以及檔名為什麼不走 `promise-N-` |
| `tools/promise-check/README.md` 開頭 | `specs/` 底下不是每一份都是那五條                 |
| `TESTING.md` 層 3                    | 第二份規格不是第六條承諾                          |

⚠️ 三處都是散文，而且是**同一件事的三份抄本**。**這一則是它們的權威來源** ——
判準要改，改這一則；那三處是指標，不是判準。

#### 四、⚠️ 不加機制，而且要寫下它守不住什麼

考慮過一條絆線：`specs/promise-N-*.feature` 的 `N` 必須 ⊆ {1..5}。**它掛錯地方了。**

真正有風險的句子是「五條承諾**守到一條**」，而那句話裡的變數是
**五條裡有幾條有規格**，不是「有沒有人超過五」。明天合法地補上
`promise-2-*.feature`（C118 §五 點名的四條之一），那三處當場變假，
而 `N ⊆ {1..5}` 全綠。⚠️ 那正是 `tripwire-must-hang-on-its-target` 記的形狀：
斷言吃的資料要從被守的東西取。

可數的事實是 `git ls-files specs/promise-*.feature` 的筆數對上散文裡的數字，
而它的家是 `tools/doc-facts`。**⚠️ 那支工具今天表達不了它**：`Fact.citations`
的第 1 條硬性要求是「**恰好一個捕獲群組，就是那個數字**」，而現有每一條樣式
都是 `(\d+)`；`HANDOFF.md:152`、`TESTING.md:63`、`TESTING.md:142` 那三句寫的是
**中文數字**（「一條」「四條」）。要守它，得先改 `doc-facts` 的契約 ——
那是另一個決定，不該搭這一則的便車。

**所以這一格明文留白。** C118 §六 對同一句話做過同一件事：它把「那句話只被
改掉一部分」寫下來，而不是拿掉那句話。

⚠️ **分開裁是刻意的。** 分類與機制在這棵樹上被 C134／C135／C136 花了三則才拆開；
把機制決定塞進一則命名裁決，是把它們又黏回去。

#### 五、掃過而**不動**的

`SCOPE.md:41`：「它為什麼不是『不服務五條承諾就出局』，見 `DECISIONS.md` 的 C72。」

查過了：**那一行是準確的，而且講的是另一件事** —— C72 是**範疇判準**那一則，
它自己就是拒絕「不服務五條承諾就出局」的那個裁決（它的效力後來被 C136 §三
收成分類，但那一行陳述的內容沒有變）。它與規格檔名無關。

寫在這裡，是為了讓下一次掃「五條承諾」這幾個字的人不要把這個省略讀成疏漏。

#### 六、本則不裁的

- **不裁 `doc-facts` 要不要能表達中文數字**（§四）。
- **不裁 C118 §五 那四條要不要開始寫。** 那要先讓對應的閘門跑得上一份副本，
  而那是各自獨立的工作（C118 §五 自己說的）。
- **不改 `promise-check` 綠燈那句「N 條承諾」。** C163 §九 把它繫在本則上，
  而本則的答案是：那個數字數的是**這個目錄裡的規格數**，與「五條」不是同一個量。
  ⚠️ 兩個數字同時出現在終端機上會讓人以為它們該相等 —— 這一格留著，
  因為改掉它要先決定那句話該說哪一個量，而那不是命名判準的一部分。

### C165 — 完成率的分母第一次非零，而它引爆了一個從 C115 就在的閘門互斥：報表的內容歸產生器，排版歸 oxfmt（2026-09-05，#188）

⚠️ **量測基準 `fee0660`。** 本則所有數字凍結在那一個 commit。

#### 一、發生什麼 —— `vpr ready` 兩個方向都紅，沒有出口

`#188` 的目的是讓業務功能完成率第一次有非零分母：`vp create slice` 產出
`features/invoice`，規格進版控，`SPEC-REPORT.md` 於是第一次長出表格。

當場撞到：

| 順序                                      | 發生什麼                                             |
| ----------------------------------------- | ---------------------------------------------------- |
| `vpr ready` 第 1 步 `vp check`（oxfmt）   | 把 `SPEC-REPORT.md` 的表格補上對齊空白               |
| `vpr ready` 第 5 步 `spec-report --check` | 產生器吐的是不帶 padding 的表格 → **「與現況不符」** |

排版過就說報表過期，重新產生就說沒排版。**兩個方向都紅，繞不出去。**

#### 二、⚠️ 它從 C115 就在了 —— 引爆它的是「分母第一次非零」

`spec-report` 上線（C115）到今天，`SPEC-REPORT.md` 寫的是

> 目前沒有任何**版控中的**切片帶著驗收規格。

**一列表格都沒有。** 於是 oxfmt 與 `spec-report` 從來沒有碰過同一行，
互斥不存在。這與 C163 §四 記的是同一個形狀：**一條規則在空集合上與另一條
不衝突，加進第一個成員的那一刻才現形**，而 diff 上看不到任何東西變壞。

⚠️ 差別在於 C163 那次是「射程被稀釋」，這次是「兩支工具第一次有交集」——
共通點是**觸發它的那個 PR 做的事本身完全正確**。

#### 三、選了哪一條，以及另外兩條為什麼沒選

⚠️ **這一格停下來問人了**（`AGENTS.md` 規則二：門檻訂錯要停下來說，不要自己改）。
三條路連代價一起列，人選了 B。

|          | 做什麼                                                                            | 為什麼不是它                                                                                                                    |
| -------- | --------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------- |
| A        | 讓 `spec-report` 直接吐 oxfmt 版型的表格，加一條「產生 → 跑 oxfmt → `cmp`」的夾具 | 要在工具裡實作顯示寬度（CJK 兩格、emoji 未定），oxfmt 換演算法就會漂；而那條夾具得 spawn `vp fmt` —— **閘門不依賴驅動層（D2）** |
| **B ✅** | `--check` 比較時正規化表格空白；**內容沒變就不重寫檔案**                          | 選了它                                                                                                                          |
| C        | 加 `.prettierignore` 排除 `SPEC-REPORT.md`                                        | ⚠️ 那是**改設定**，規則二 直接指向「停下來告訴人，不要自己改」                                                                  |

**分工變成：內容歸 `spec-report`，排版歸 oxfmt。**

⚠️ **B 是兩半，缺一邊都繞不出去。** 只放寬比較的話，不帶 `--check` 那一趟
仍然無條件覆寫，把 oxfmt 補的 padding 洗掉 —— 下一步 `vp check` 照樣紅。
兩半各由一條測試單獨守著（§五）。

#### 四、正規化吃掉什麼，刻意不吃什麼

`sameReport`（`tools/spec-report/src/render.ts`）只動 `|` 開頭的列，
而且只動空白與分隔列的 `-` 長度。刻意**不吃**的兩樣：

- **對齊用的 `:`**（`| :--- |` 與 `| --- |` 仍然算不同）—— 那是語意不是排版。
  吃掉的話，有人在報表裡加了對齊、這支工具說「一致」，下一次產生就洗掉它。
- **圍籬（```）裡的一切**，逐字比。報表開頭那段指令住在圍籬裡，
  而它是要人逐字複製去跑的。

⚠️ 儲存格全是切片名、整數與百分比，**只差空白不可能是語意差異** ——
這是放寬得起的前提，換一種內容（例如儲存格裡放程式碼）就不成立了。

#### 五、實測：三個方向都短路過

| 短路                                     | 應該紅的     | 實測                                              |
| ---------------------------------------- | ------------ | ------------------------------------------------- |
| `sameReport` → `return true`             | 全部對照組   | **7 條紅**（含既有的「報表過期時 `--check` 紅」） |
| `sameReport` → `a === b`（改動前的行為） | 四條 padding | **4 條紅**                                        |
| 寫檔那半 → 無條件覆寫                    | **只有一條** | **1 條紅**（「再產生一次不會把排版洗掉」）        |

⚠️ 第三列是重點：**兩半各自有一條、而且只有一條在守**。少了它，B 的另一半
可以被安靜地拿掉而全樹綠。

⚠️ 單元測試那組的 padding 樣本是**從真的 oxfmt 產出複製下來的**，不是照
它的演算法推的 —— CJK 佔兩格、emoji 佔幾格，猜錯的話那組測試會變成在驗想像。

#### 六、順帶死掉的兩條測試前提

`tools/spec-report/tests/cli.test.ts` 有一個 describe 叫
**「真的 repo（目前沒有任何切片有規格）」**，兩條測試斷言真樹上一個切片規格
都沒有。`features/invoice` 進版控的那一刻它們紅了。

⚠️ **它們一直是對的，而對的理由是「這棵樹剛好還沒有人用過這條線」——
不是這支工具的性質。** 拆成兩組：

- 真樹那一組留下它真正在守的東西：`git ls-files` 跑得起來、glob 對得上、
  中文路徑不炸（C112）。⚠️ **刻意不驗退出碼** —— 那要看 `.vitest-results.json`
  在不在，而那是排程的事（C87），驗它會讓這一條在乾淨 clone 上偽紅。
- 空樹那一組改用一個真的空 git 沙盒，並補一條對照：**檔案在磁碟上但沒
  `git add` → 仍然要判空**。少了它，事實來源被改成 `readdirSync` 也是全綠的
  （C73／C98 的那個坑）。

#### 七、⚠️ 同一句話第三次被推翻，而這次的破法又換了

`tools/promise-check/tests/cli.test.ts` 的檔頭記著一句「排程相依查過了」，
它的論證是：**這一支與 `spec-report` 的事實來源都是 `git ls-files`**，
所以 `slice-gen` 建了又刪的 `zz-` 切片兩邊都看不見。

| 第幾次 | 新的相依方                        | 為什麼那個論證擋不住它                                                                |
| ------ | --------------------------------- | ------------------------------------------------------------------------------------- |
| 一     | 原始的 `slice-gen`（C87）         | ——（論證成立）                                                                        |
| 二     | `threshold-check`（C163 §七）     | 它跑 `vp lint` **掃磁碟**，不走 `git ls-files`                                        |
| 三     | **`spec-report --check`（本則）** | 它讀 `features/*/.vitest-results.json` —— 一個 **gitignore 掉的產物**，根本不在版控裡 |

⚠️ 在 `features/invoice` 之前，`--check` 對空報表**恆綠**，所以誰先跑無所謂。
恆綠那一段消失的那一刻，它才變成一個有跑序要求的相依方。

已加 `@org/feature-invoice#test` 進 `dependsOn`。⚠️ **而 `dependsOn` 綁的是
跑序、不是產出**：結果檔只有帶 `--outputFile` 的那條完整指令會產生，
單獨 `vp run @org/promise-check#test` 永遠缺它，**那不是缺陷**。

⚠️ 所以另外補了一條**前提斷言**（自我防護的夾具，C154 §三 第 3 條，
不計迭代軸的分）：跑 `--check` 之前先列出「版控裡有規格、磁碟上沒有結果」
的切片，訊息把兩個成因分開講。它同時是**下一片帶規格的切片**的絆線 ——
那一片沒被加進 `dependsOn` 時，這裡會指名說出是哪一片，而不是丟一句
「還沒有測試結果」讓人以為自己忘了跑測試。

⚠️⚠️ **這一條是 advisor 指出來的，不是我自己查到的。** 我的 `vpr ready`
EXIT=0 是真的，而它綠的理由是**結果檔早就躺在磁碟上** —— 那是
「`pnpm ready` 綠證明不了 CI 綠」的第六種，機制是新的：閘門讀了一個
gitignore 掉的產物。實測：刪掉 `features/*/.vitest-results.json` 再跑
`vp run @org/promise-check#test`，那條測試當場紅。

⚠️ CI 這一側**沒有問題，而且上一個人已經想過**：`tier1-quality.yml` 的測試
（第 144 行）與 `spec-report --check`（第 158 行）在**同一個 job**，而且那裡
就寫著「必須在測試之後 —— 它讀的是上一步留下的 `.vitest-results.json`，
而那些檔案跨 workflow 拿不到」。

#### 八、⚠️ 本則**沒有**新增機械檢查，所以不報 D16 兩軸

C154 §三 的填寫要求是給**新增機械檢查**的。本則做的是**放寬一條既有比對**
＋ 補夾具，而夾具是自我防護的（C154 §三 第 3 條：不計迭代軸的分）。
寫下這一句是為了讓下一次掃描不要把它讀成漏填。

#### 九、本則不裁的

- **不裁 A 那條路。** 讓 `spec-report` 產出排版好的表格今天沒有必要，
  但它是 B 的上位解 —— 哪天報表的儲存格開始放「空白有意義」的東西就要重議。
- **不動 oxfmt 的任何設定**，樹上今天仍然沒有 `.prettierignore`。
- **不推廣到別的產出物。** `SPEC-REPORT.md` 是這棵樹上**唯一**由閘門寫進版控的
  markdown；`SCOPE.md`、`UI-SURVEY.md`、`HANDOFF.md` 都是人寫的，不適用。
  ⚠️ 加第二份機器產的 markdown 時要回頭看這一則。
- ⚠️ **`features/invoice/specs/invoice.feature` 的內容不由 agent 寫**（規則四）。
  本則交付的是骨架與接線；那份規格今天還是 `vp create slice` 的範本，
  **報表上那個 80% 是範本的示範場景，不是業務完成率**。

### C166 — 「下一條承諾能不能寫」問的不是收不收 `--root`，是判定有幾成跟著它走；而那句代價敘述 C123 十一天前就作廢過（2026-09-05，#259）

⚠️ **量測基準分兩份，不要混讀。** `#259` 的數字凍結在 `fee0660`；本則自己量的
（§五）凍結在 `0645fba`。

#### 一、C118 §五 那句話今天是假的，而它的結論靠一個它沒提到的理由活著

`DECISIONS.md` 的 **C118 §五**（2026-08-23）：

> ⚠️ **不是估錯，也不是留給下一輪的客套話。** 九支閘門裡，**只有
> `tools/conformance` 能在一份副本上跑**（`--root`），而那是這種驗法的前提。

`#259` 實測（`fee0660`）：**16 支 `tools/*/src/cli.ts`，6 支收 `--root`。**
`theme-verify` 在那則裁決寫下的**三天後**就接上了（`6fa4966`，C127／#167），
而 `pii-check` 的 `flags.root` 比 C118 還**早五天**。

**而它推出的「下一步不是再寫四份規格」仍然成立** —— 靠的是下面這句，
C118 §五 一個字都沒有提到：

> **收了 `--root` ≠ 這支工具的判定跟著 `--root` 走。**

⚠️ **本則不改 C118**（C138：凍結不搬遷）。C118 §五 是 2026-08-23 那天的正確
快照，本則是它的續集，體例照 C163 §九 對 C135 的處理。

#### 二、裁決：規劃下一條承諾時要問的問題換一句

**問「這支閘門收不收 `--root`」是錯的問法，問「它的判定有幾成跟著 `--root` 走」。**

那 6 支今天被驗到哪一層（`#259` 的實測，`--root "$PWD"` 對上完全不給旗標，
被驗對象完全相同，逐位元組 `cmp`）：

| 驗到哪一層                     | 哪幾支                                      |
| ------------------------------ | ------------------------------------------- |
| 逐項驗過「紅燈點的是副本」     | `threshold-check` 一支（C163 §五）          |
| 只知道「旗標的有無會改變輸出」 | `conformance`、`theme-verify`               |
| 只知道「旗標的有無不縮射程」   | `pii-check`、`promise-check`、`spec-report` |

⚠️ **第三列不是「驗過了」** —— 兩趟輸出相同只證明加上旗標沒有讓它少檢查東西，
**完全不證明它讀的是副本**。一支從頭到尾讀真樹、把旗標丟掉的工具，兩趟輸出
也會相同。這一列的五支之中沒有任何一支被量過。

⚠️⚠️ 第二列更要小心：`theme-verify` **通過** `probeRootSupport`（C124）的判定，
因為靜態半確實對 root 有反應 —— 而建置半照樣頂著閘門的名字報真樹的數字。
它自己的檔尾寫著：「那正是 C124 建來抓的那個形狀，只是低了一層。」
**一支閘門可以一半跟著走、一半不跟，而既有的探針只回答「有沒有反應」。**

#### 三、⚠️ `#259` 的提議二早就做完了 —— C123 §五（a），十一天前

`#259` 提議「把 `theme-verify` 那一列的『代價是每個場景多幾十秒』作廢」。
全樹掃字串（不是逐節讀，`grep 幾十秒` 兩個命中）：

| 位置                 | 是什麼                                              |
| -------------------- | --------------------------------------------------- |
| `DECISIONS.md:10393` | C118 §五 那張表裡的原句                             |
| `DECISIONS.md:11165` | **C123 §五（a）：「代價不是時間。」**（2026-08-25） |

**那句話從 C123 起就是作廢的**，而 `#259` 整張票沒有提到 C123 一次。

⚠️ 而這裡有比「重複勞動」更值得記的一件事：**作廢它的那個理由，自己也被推翻過。**

| 誰            | 「代價是每個場景多幾十秒」錯在哪                                                                                                                |
| ------------- | ----------------------------------------------------------------------------------------------------------------------------------------------- |
| C118 §五      | （原句）代價是時間                                                                                                                              |
| C123 §五（a） | 不是時間 —— 建置半 **25 毫秒就爆** `Can't resolve '@org/ui/styles.css'`（副本不是 workspace）                                                   |
| C127（隔天）  | **也不是會爆** —— 標題原句：擋路的是「**建置會成功並報出真樹的數字**」                                                                          |
| 今天          | 所以建置半被判成一句範疇聲明，`FIXTURES = join(SELF, "fixtures")` 刻意不跟著走，`--root` 之下印一行說自己沒驗然後跳過 —— **它不爆，它根本不跑** |

⚠️ **拿 C123 的理由去解釋今天的行為會答錯**，反過來也一樣。這一格是 §二 那條
判準的最好例子：`theme-verify`「收不收 `--root`」在 C123 到 C127 之間從假變真，
而**擋路的東西一格都沒有動** —— 動的只有它的名字。

#### 四、所以今天能寫幾條承諾：0 條，不是 4 條

`#259` §三 那張表逐條列了為什麼，本則不複述。要記的是**卡點的形狀變了，
而且四條卡的不是同一種東西** —— 逐條問「再讓一支閘門收 `--root`，這一格動得了嗎」：

| 承諾           | 卡在哪                                           | 那條路動得了嗎        |
| -------------- | ------------------------------------------------ | --------------------- |
| 二（前四段）   | `component-contract.test.ts` 是 vitest，不是 CLI | ❌                    |
| 二（末段）／四 | `theme-verify` 建置半是一句範疇聲明              | ❌                    |
| 三             | `api-surface` 沒有 `--root`                      | **✅ 它還在那條路上** |
| 五             | eslint／`.semgrep`／gitleaks，形狀差最遠         | ❌                    |

**所以「讓更多閘門收 `--root`」這條路對承諾二／四／五 已經走完了** —— 那三條的
卡點整個搬離了它，C118 §五 的原句對它們今天沒有指導作用。**承諾三 還在上面。**

⚠️ **而那不是一個免費的解。** 照 §二 的分層，「收了旗標」只落到第三層，
而第三層今天**一支都沒有被量過**。給 `api-surface` 加上 `--root` 之後要問的
仍然是「它的判定有幾成跟著走」—— §二 那條判準正是為這一格寫的，
它在本則裡不是背景說明。

#### 五、⚠️ 量它的時候有一個會給出漂亮而錯誤答案的量法

本則重量了一次（`0645fba`），拿到的是 **8** 支，不是 6。差在
`grep -E '"--root"|--root|flags\.root'` 會掃到**散文**：

```
tools/api-surface/src/cli.ts:101  ⚠️ 與 tools/conformance 的 `--root` 一樣刻意**不做環境變數版本**
tools/compliance/src/cli.ts:77    與 `tools/conformance` 的 `--root` 同一個取捨，只是範圍更窄
```

兩支都是在檔頭**談論**別人的 `--root`，兩支都不收。⚠️ 而 8 這個數字沒有任何
一處會紅 —— 它比 6 大，看起來像「又多了兩支接上了」，是個往樂觀方向的錯。
要重量，**判準是旗標宣告，不是字串出現**。

#### 六、本則不新增任何機械檢查，所以不報 D16 兩軸

C154 §三 只在新增機械檢查時要求報數。理由照 `#259` §五 抄下來，
免得下一次掃描把這一格讀成漏填：

- 要守的是**一則凍結裁決裡的一句散文**，而 `doc-facts` 守的是數字。
- 「6 支收 `--root`」倒是數得出來，**但它不是要守的那句話** —— 守住它只會讓
  下一個人更確信那個已經不重要的變數。**這是 C152 §二 那個同形陷阱的形狀。**
- ⚠️ 因此本則交付之後，§二 那條判準**仍然沒有機制在守**，靠人讀。

#### 七、本則不裁的

- **不動 `theme-verify` 的建置半**，不讓 `FIXTURES` 跟著 `--root` 走。
  那要先回答「副本怎麼 install」，是全樹副本的量級 —— 要改要先裁，不在本則。
- **不去補量 §二 第三列那五支**。那是獨立的一張票，而且它要先決定量法：
  `probeRootSupport` 判得出「有沒有反應」，判不出「幾成」。
- **不增減任何一道閘門**（C137 §一，兩個方向都禁）。
- **不改 C118、不改 C123**（C138）。
- **不關 `#188`**、不動 C164 §六 的其他兩格。

### C167 — CI 讀不出「哪一層在守」，而把 Tier 換成五層會造一批 `TESTING.md` 說不存在的層號；所以交叉標註，兩條軸都留（2026-09-02，#236）

#### 一、問題

看 CI 的人只看得到兩個綠勾（`quality`／`gate`），讀不出**測試邊界有沒有守住**。

#### 二、⚠️ 字面的做法（把 Tier 換成五層）在這棵樹上是錯的

`TESTING.md` §五 那張表已經裁過這件事：

| 機制                | 用五層的尺                   | 用兩類的框架                           |
| ------------------- | ---------------------------- | -------------------------------------- |
| **Tier 2 安全整條** | **「安全不在五層的題目裡」** | **第二類** —— 專案組寫業務時的前置過濾 |

> 左欄那些「對應不上」，共同的原因只有一個：**借來的座標整個只描述第一類。**

**把 CI 重排成五層，會逼人給 Tier 2 十道閘門一個 `TESTING.md` 說不存在的層號。**
那不是讓邊界更清楚，是造一批**看起來精確的假標籤**。

另外三件機械上的事實：

1. `Tier` 是 `gate-roster` 的型別，兩個 workflow 檔名寫死在 `check.ts` 裡 ——
   改成五個 layer job ＝**重寫那支閘門自己的模型**。
2. 兩個 workflow 各只有一個 job（`quality`／`gate`），那就是 branch protection
   的必要 context。拆成五個會把它們全部換掉。
3. ⚠️ **Tier 承載的不只是執行策略。** `eslint.config.js` 檔頭寫著 Tier 2 刻意
   不經 `vp`，因為 **D2 要求安全閘門獨立於可替換的驅動層** —— 那是「守什麼」的
   性質，跟著 Tier 走，不跟著層走。**所以兩條軸是交叉的，不是巢狀的。**

#### 三、裁決：交叉標註，不是取代

1. 每個閘門步驟名加層前綴 —— `層 1 約束 ·`（3 步）、`層 2 單元 ·`（2 步）、
   `層 3 驗收 ·`（2 步）、`五層答不出 ·`（17 步）。
2. 每條 workflow **第一步**寫一張「測試邊界地圖」到 `$GITHUB_STEP_SUMMARY`。

⚠️ **放第一步是刻意的**：半張表跟一張短表長得一模一樣。任何一步失敗之前它就
已經印出來了。

⚠️ **地圖刻意不列閘門清單**，只列層。那份清單的唯一來源是 `gates.ts` ——
抄第二份正是那支工具存在的理由（它自己的檔頭記著這一類已發作兩次）。

⚠️ **`gate-roster` 比對的是 `run:` 指令不是步驟名**，所以改名對它是透明的
（實測退出 0）。這也是本則選「改名而不改結構」付得起的原因。

#### 四、這個改動買到什麼

**六列裡有三列是空的（層 4／層 5／元層），而 CI 頁面現在會說出來。**
Tier 的視角看不到這件事 —— 它只顯示兩個綠勾。

而最要命的是層 3：`spec-report --check` 今天輸出「沒有找到任何版控中的驗收
規格」然後**回綠**。原本在 CI 上長得跟「驗收全過」一模一樣。這正是 **#188**
的缺口，本則把它印在每一次執行上。

⚠️ 這是這棵樹付過學費的形狀：**三態不夠用，第四態「未執行」與全綠長得一樣。**

#### 五、⚠️ 有一個標籤沒有背書，本則明說

17 個 `五層答不出` 裡，**16 個有 `TESTING.md` §五 的明文背書**
（Tier 2 安全整條 ＋ `theme-verify`／`scope-check`／`doc-facts`／`api-surface`）。

**`gate-roster` 那一個是推的** —— §五 沒有列它。寫在這裡是為了讓它是敞著的，
不是混在另外 16 個裡面。

#### 六、刻意不做

- **不改 job 結構、不改 workflow 檔名、不改必要 context**（§二 的三件事實）。
- **不改 `gates.ts` 的 `Tier` 型別。**
- **不改任何門檻**（AGENTS.md 規則二）。本則零行為變更 —— 加的是步驟名與一份摘要。
- **不重抄閘門清單。**

#### 七、與既有裁決的關係（C136 §八）

| 既有                 | 本則做了什麼                                                        |
| -------------------- | ------------------------------------------------------------------- |
| **D10**              | **不動。** 兩層的結構、觸發、快取語意一個字不改；本則只加一條標註軸 |
| **D2**               | **不動，並引它** —— §二 指出 Tier 2 不經 `vp` 是「守什麼」的性質    |
| **`TESTING.md` §二** | **不改。** 地圖照著它的六列走，是引用不是副本                       |
| **`TESTING.md` §五** | **不改，並當判準** —— §二／§五 的「五層答不出」直接來自它           |
| **`gates.ts`**       | **不改。** §三 說明地圖刻意不列閘門清單，避免造第二份手抄本         |
| **C137 §一**         | **遵守它** —— 本則不論證增減任何閘門，只論證怎麼標示既有的          |
| **#188**             | 不裁。§四 只指出本改動會讓它的缺口在每次 CI 上可見                  |

**沒有任何既有產出需要重跑。** 本則零行為變更：`pnpm ready` 退出 0、
`gate-roster` 退出 0、地圖步驟本機實跑無誤縮排。

---

### C168 — 測試接縫的架構審查：真重複只有八組，而 harness 是十一份實作的同一個 module；收進 `@org/gate-kit/testing`，其餘互補的三十列一併登記（2026-09-05）

> ⚠️ **本則 §二／§六 的數字與判斷在落地時被逐項重量，多處更正；原文不改。**
>
> - §二 `threshold-check` 探針列「一次 `vpr ready` 跑 8 次」→ 見 C176 §三（實測 22）
> - §二 各閘門對真樹 exit 0 列／§六 第 2 項「九支測試」→ 見 C170 §二（十三處五種，純重複只有三條）
> - §二 `defineFeature` 列／§六 第 3 項「整個 describe 不可達」→ 見 C172 §二（十條不可達、七條獨一）
> - §二 BFF 列／§六 第 4 項「401／403／CSRF 重複」→ 見 C174 §二（十二條只有一條）
> - §二 旗標列／§六 第 1 項「三處同一變異同時紅」「收掉三個 parser」→ 見 C178 §一（三句各對一半；三條守的是到不了的第二道檢查；三個重盤後是一個）
> - §六 第 2 項「`--evidence` 不在 `scripts.gate`，那是洞」→ 見 C178 §一 表第二列與 §四（variant 不是洞）
> - §六 第 6 項「走假閘門」→ 見 C176 §二（機制否決）
> - §六 第 7 項「四條只留 adapter 那一條」→ 見 C177 §二（最深的那條不是 C168 說要留的那條）
> - §五「不排除」／§八「兩件都沒在這裡修」→ 見 C194（讀真樹的是四支不是兩支、`disableTypeChecks` 關掉、`vue-typecheck` 回到射程，N 129 → 139）
> - §二 `maskName` 列（§六 沒帶上它）→ 見 C196 §一（列了候選、清單掉了；四條各是嚴格子集，刪，另找到一條恆真的斷言）
> - 原稿（審查報告的 HTML，含卡片、兩張 mermaid 圖與附錄 A–C）在 `reports/research/test-seam-review-2026-09-05.html`；2026-09-07 加了落地標註（頁首區塊、每張卡片一條狀態列、附錄 A 第六欄、附錄 C 每條的下落），**原文不改**，與這個區塊同一種做法。

#### 一、問法與判準

起於一次「有沒有重複守同一條接縫的測試」的架構審查，84 支測試檔逐支讀。
判準是機械的：**同一個變異會不會讓兩邊同時紅**，再加 deletion test ——
同時紅且刪掉一邊複雜度**消失**＝真重複；只有一邊紅＝互補；刪掉一邊複雜度
**搬家**＝留。「同時紅」是從 import 與讀檔目標推導的，樹上有實測記錄的地方
引用記錄；沒有跑 Stryker。

⚠️ **這個問法先被 C137 §一 裁過一次**：不准拿成本或「過度設計」論證增減閘門。
本則每一項處置動的都是**測試碼**，`vpr gate` 那條鏈一支不動；C43 起的原則
「每一道閘門附一支反向測試」原樣保留 —— harness 決定**怎麼建樹、怎麼跑**，
不決定**什麼算對**。

#### 二、真重複的八組，與最大的那一組

| 接縫                     | 兩邊                                                                                                 | 處置        |
| ------------------------ | ---------------------------------------------------------------------------------------------------- | ----------- |
| **CLI 測試 harness**     | 11 份沙盒建構、8 份 spawn 包裝、17 處手抄 repo 根、14 檔的輸出合併、`slice-gen` 的 `read()` 逐字三份 | **本則**    |
| 未知旗標／缺值 → 紅      | `gate-kit` flags＋adoption ↔ `pii-check`:184 · `compliance`:230 · `sbom-negative`:119,161            | 候選（§六） |
| 各閘門對真樹 exit 0      | 九支測試各一次 ↔ 腿④ `vpr gate` 再一次；只有 `gate-roster`:146 留了辯護                              | 候選（§六） |
| `defineFeature` 命名空間 | `define-feature.test`:38 ↔ 三片切片的範本測試（違規時 import 先炸，斷言不可達）                      | 候選（§六） |
| `maskName`               | `platform/pii` mask.test:59 ↔ `features/order` masking.test:22（同一個字面值）                       | 候選（§六） |
| BFF 401／403／CSRF       | `bff-mock` routes.test ↔ `bff-check` contract.test（同一個 `startBffMock`）                          | 候選（§六） |
| `index.ts` 匯出完整性    | `platform/ui` styles.test:315 ↔ component-contract:69，**兩邊檔頭都沒提對方**                        | 候選（§六） |
| `threshold-check` 探針   | 一次 `vpr ready` 跑 8 次；樹自己在 `promise-check/tests/sandbox.test.ts:96` 拒絕過第三份             | 候選（§六） |

harness 那一組與其他七組性質不同：它不是斷言重複，是 **deletion test 判「搬家」
的那一種** —— 每一份都在做事，但「臨時目錄要不要 `git init`、要不要清、用
`git ls-files` 還是 `readdirSync`」這種政策改一次得改十一處。清理方式已經分裂
成三派（`afterEach` 掃陣列／`try-finally`／單一變數）。`@org/gate-kit` 早就導出
`repoRoot()`，而只有兩支測試在用。

#### 三、裁決：收進 `@org/gate-kit/testing`

| 決定          | 取法                                                                                                                                                                 | 沒取的                                                                                                                    |
| ------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------- |
| seam 位置     | `@org/gate-kit` 加 subpath export `./testing`；17 支工具本來就宣告它，一行相依不加                                                                                   | 新 package（要進 `gate-roster` 名冊）；跨 package 相對路徑（conformance 會紅）                                            |
| interface     | `sandbox({files, copy, git, within, prefix, lifetime})` → `{root, read, write, git}`；`runCli(cli, args)` → `{status, stdout, stderr, output}`；re-export `repoRoot` | `pii-check` 那段「幾個檔算夠」的迴圈（那是它的斷言）；`gate-roster` 的 layout DSL；`promise-check` 產品碼的 `makeSandbox` |
| `copy` 的語意 | **只複製版控裡的檔**（`git ls-files`）；指到的路徑一個版控檔都沒有時丟例外                                                                                           | `cpSync` 整個目錄再濾 `node_modules` —— 沙盒會隨工作區狀態變                                                              |
| 清理歸誰      | harness 在 import 時自己向 vitest 註冊 `afterEach`／`afterAll`（`lifetime: "each" \| "all"`）                                                                        | 回傳 cleanup 讓各檔自己掛（三派保留成一派但仍十七份）；`using`（樹上零使用、轉譯未驗）                                    |
| 旗標          | args 全由呼叫端明寫，**不補 `--root`** —— C126 之後八支 CLI 拒絕不認得的旗標                                                                                         | —                                                                                                                         |
| `cwd`         | 固定 `repoRoot()`；沙盒走 `--root` 不走 `cwd`                                                                                                                        | —                                                                                                                         |
| 兩條明文例外  | `root.test.ts:11-18` 的 symlink 斷言留在消費端（它不是 harness）；`vue-typecheck` 的沙盒用 `within` 容納，理由留在它自己檔頭                                         | —                                                                                                                         |

⚠️ **`gate-kit/package.json` 的 description 從 C125 §五 起寫著「這條線只帶
parseFlags」。** 那是 `release/v1` 時代的話；`main` 的 `root.ts`、`walk.ts` 早就在，
併線之後沒人改。本則順手重寫，不另立一則。

#### 四、交付分兩支 PR，證據怎麼算

- **PR 1（本則）**：模組、它自己的測試（含一次真的短路：把清理改成空操作，
  「已被清掉」那條紅、其餘綠，還原）、description、本則。
- **PR 2**：17 支測試檔遷移；絆線；本則補 §八。

**零斷言損失的證據**：`vp test --reporter=json` 抽前後**逐檔** `it` 數，逐位相同
才算。不用 `grep -c "it("`（會把 `it.each` 與註解裡的算進去），不用 `vpr ready`
全綠（證明不了「沒少」），不跑 Stryker 前後比（子行程型測試對它不可見，差是雜訊）。

**絆線**（PR 2）：`tools/gate-kit/tests` 加一條，掃 `tools/*/tests/**/*.test.ts` 裡直接
呼叫 `mkdtempSync(`、`spawnSync("node"`、`execFileSync("node"` 的檔，例外名單
`toEqual([])` 明寫；`spawnSync("git"` 放行；inline fixture 當已知非零的對照。
只掃 `tools/` —— harness 是為閘門 CLI 長的，`platform/ui` 那些 `mount()` 不是這個形狀。
⚠️ 它放 PR 2 不放 PR 1，因為 PR 1 到 PR 2 之間它是紅的，而「先列現況當例外再清空」
是一份只活一支 PR 的清單，不值得。

⚠️ 不做「模組先進、消費端有動到再改」：那會長成沒有追蹤處的餘數，樹上已有三個。

#### 五、Stryker：不排除，而那句手算式已經過期一次

`tools/*/src/**/*.ts` 會把 `testing.ts` 收進射程。**不排除**：harness 的變異若存活，
意思是「某支測試不管沙盒內容怎樣都綠」，那正是這棵樹最想抓的形狀。跑一趟看存活
清單再決定，符合〈先寬、先量、先記〉。

⚠️ `stryker.config.mjs` 那句「N 是 78 ＝ 86 − 8」是 `d67583e` 那天的數，到 `0645fba`
已經是 136 − 8 ＝ 128 而沒有任何東西在守它。本則改成 129（加 `testing.ts`），
並寫明「這個數只在跑的那天對」。⚠️ 量的時候先踩了一次：第一把尺濾掉了 `.d.ts`
得到 127，而 86 那次沒濾 —— 兩把尺要對得上才可以寫下來。

#### 六、候選、未裁

§二 表的另外七組，各一行，**本則不裁它們**：

1. 旗標 interface 只守一次，adoption 的名冊改吃 `gate-roster` 的 `GATES ∪ variants`
   （順帶收掉 `scripts.gate` 的三個 parser，關掉看不見四支 CLI 與四條 variant 的洞）。
2. 「真樹綠」收成 adoption 裡一條 `it.each`，刪九處光禿禿的 `status === 0`；
   ⚠️ `compliance --evidence` 不在 `scripts.gate`，本機唯一跑到它的是那條測試 —— 那是洞不是重複。
3. `slice-gen` 範本的命名空間 describe 不可達；conformance 的 `hasTestFile` 認 `.spec.ts`。
   ⚠️ 會動 conformance 一條規則的射程，要先裁。
4. `bff-mock` 的測試收到只剩注入 seam 特有的性質；body 形狀算不算契約要先決定。
5. `platform/ui/tests/styles.test.ts:315-329` 刪，:331-337 留。十行。
6. `promise-check` 的 CLI 測試走假閘門，不 spawn 真的 `threshold-check`。
   ⚠️ C165 剛把那支測試從恆綠救出來，第三次換形狀，所以只列不推。
7. 零散：`promise-check/tests/sandbox.test.ts:128-134`（檔案自己在 :136-139 承認）；
   `sbom-negative.test.ts:194-198`（`gate-roster` variant 已守）；lockfile 拆兩份文件那四條
   只留 `--split-lockfile` adapter 那一條。

#### 七、查過、判互補、不動 —— 登記在這裡，下一次審查不重判

| 接縫                             | 兩邊                                                                         | 為什麼不是重複                                                        |
| -------------------------------- | ---------------------------------------------------------------------------- | --------------------------------------------------------------------- |
| a11y 三支                        | `eslint-config` · `platform/ui` · `compliance`                               | 讀檔目標互不相交（lint 規則／元件原始碼＋Tailwind 產物／分工表）      |
| PII 偵測 vs 遮罩                 | `pii-check` detect.test ↔ `platform/pii`                                     | 耦合不是重複：mask.test 的資料刻意寫成不通過校驗                      |
| CSP 政策／靜態違規／跑起來的標頭 | policy.test ↔ static-csp.test ↔ `bff-check`:251                              | 三個讀的東西不同；`apps/console` 四支與 CSP 零交集                    |
| 切片契約四組                     | define-feature ↔ conformance rules ↔ contract-alignment ↔ boundary-alignment | `isValidComposableFile` 三邊各走不同分支；boundary-alignment 檔頭自陳 |
| `composableFunctionName`         | define-feature:150 ↔ contract-alignment:239                                  | 同時紅但**搬家** —— 後者守「產生器有沒有跟上」                        |
| usecase `queryInvoice`           | `invoice.spec.ts` 唯一；`invoice.test.ts` 零條 usecase 斷言                  | 無重複                                                                |
| `repoRoot()`                     | `gate-kit` root.test:7 ↔ `pii-check` roster.test:37                          | symlink 變異只後者紅；root.test:11-18 明文                            |
| `--root` 換的是被驗的設定        | threshold.test:294 ↔ `promise-check/sandbox`:98                              | sandbox:92-97 明文                                                    |
| conformance 讀 `--root`          | sandbox:113 ↔ probe.test:26                                                  | 同時紅但**搬家**：probe:36-43 是探針唯一的非零列                      |
| markdown 事實三支                | doc-facts／threshold-check／promise-check                                    | 嫌疑不成立：後兩支不讀 `.md`                                          |
| `HANDOFF.md` 的數字              | doc-facts ↔ exit-drill counts                                                | 守不同數字；`counts.ts:148` 明文分界                                  |
| 樹上有哪些 tools／有沒有登記     | gate-roster ↔ scope-check ↔ pii-check roster                                 | `scope.test:162-170` 專門斷言 tools 那層必須綠（C136 §四）            |
| 閘門鏈成員資格                   | gate-roster check ↔ adoption ↔ promise-check check                           | 三個問句；三個 parser 是 §六 第 1 項                                  |
| exit-drill 五支                  | 一對一對五個 src 模組                                                        | 「空帳目與全部登記過同形」是判準重複，不是接縫重複                    |
| 兩軌 lint                        | `vp check`（oxlint）↔ gate 的 eslint ×2                                      | 規則集刻意零重疊（`vite.config.ts:1-10`）                             |
| 原始顏色判定                     | theme-verify palette.test ↔ contract-alignment:363                           | 共用 `findPaletteUsage`、輸入不同；:360-361 明文                      |
| `sr-only` span 形狀              | ui a11y.test:201 ↔ dropdown-menu:198                                         | 同時紅但**搬家**：各是別條的前置；兩邊檔頭載明                        |
| 模板 HTML 註解                   | a11y:387（廣）↔ field-wiring／alert-dialog／dropdown-menu（深）              | alert-dialog:292 實測：content 外只有廣的紅                           |
| gate-roster 真樹 vs fixture      | roster.test:141 ↔ :145                                                       | :146-147 有辯護；併入 §六 第 2 項                                     |
| vitest project 重疊              | 根層 `vite.config.ts` 無 `test` 區塊                                         | 結構上不可能：收集靠 `pnpm-workspace` × `vp run -r`                   |

順帶撞到、**不是重複、本則不處理**的缺口：a11y 規則清單縮水時三支都綠；
`form-control-has-label` 的範圍豁免沒進交付文件（`compliance/src/a11y.ts:145` 的
`.find()` 只取第一個區塊）；CSP 指令被刪具名三條以外兩邊都綠；覆蓋率 `enabled: true`
三片＋範本各抄一份；`apps/console`、`features/order` 兩支 `vite.config.ts` 的散文描述
一個已不存在的根層 `test` 區塊；`tools/sast/` 是只有 `node_modules` 的幽靈目錄，
兩份名冊都看不見。

#### 八、實測（PR 2，2026-09-05，基準 `390f585`）

**遷了 22 支，不是 §四 說的 17。** 17 是「手抄 repo 根」那一列的數；絆線的三個
pattern（`mkdtempSync(`／`spawnSync("node"`／`execFileSync("node"`）掃出來是 22，
差的五支是「只建沙盒沒 spawn」或反過來的那種 —— 同一個 module 的消費端，一起遷。
⚠️ 一份裁決裡的數字是哪把尺量的，要寫在數字旁邊；這一則第一版沒寫。

**逐檔 `it` 數（`vp run -r test -- --reporter=json`，從根層跑一次、跑序由 `dependsOn`
綁住）：** 前 57 支 974 條，後 58 支 982 條。57 支逐位相同；差的 8 條全在 `gate-kit`
自己：絆線 6 條（新檔）＋ `testing.test.ts` 12 → 14（下面 Stryker 逼出來的兩條）。
⚠️ 第一版的量法是逐 package 單獨跑，`promise-check/cli.test` 因為切片的
`.vitest-results.json` 還沒產生而紅 —— 那是 C87／C165 那種紅，不是遷移的；換成根層
一次跑就沒了。**量測台自己先給了一個假的紅。**

**絆線的對照組，真的跑過：** `harness-tripwire.test.ts` 六條 —— 三個 pattern 各一條
inline fixture、`spawnSync("git"` 放行一條、「本檔自己含那三個字串所以必須被點到」
一條（掃到零支與 `walk` 指錯目錄長得一樣）、例外名單 `toEqual([])` 一條。短路法：
把 `gate-roster/tests/roster.test.ts` 換回 `390f585` 的版本再跑 —— 紅一條，`Received`
是 `["tools/gate-roster/tests/roster.test.ts"]`，還原回綠。

**Stryker 一趟（只 mutate `testing.ts`，85 顆）：** 第一趟殺 68、逾時 8、靜態忽略 4、
未覆蓋 2、**存活 3**。三顆各補一條斷言後第三趟**存活 0**（殺 71）：

| 存活的                                 | 它說的事                                                             | 補的斷言                                           |
| -------------------------------------- | -------------------------------------------------------------------- | -------------------------------------------------- |
| `runCli` 的 `args = []` → `["…"]`      | **真缺口**：「不給 args 就不夾帶任何旗標」沒人守，而那是 C126 那一半 | `runCli(fixture)` 的 argv `toEqual([])`            |
| `prefix ?? "gate-kit-sandbox-"` → `""` | 預設前綴是給清理失敗時認屍的，沒斷言                                 | `basename(root)` 以它開頭                          |
| `args.join(" ")` → `join("")`          | 單一參數時等價；訊息少一個空白沒人管                                 | 兩個參數的 git 呼叫，訊息要含 `git ls-files --zzz` |

未覆蓋那 2 顆是 `result.stdout ?? ""` 的右手邊 —— 只在 `spawnSync` 連 stdout 都給不出
時走到，測試裡沒有那種情況。逾時 8 顆是清理與 `sandbox()` 內部（`read`／`write`／`git`
換成空函式），Stryker 記成偵測到；⚠️ 那是時間相依的判定，不當殺數讀（`stryker.config.mjs`
檔頭）。

⚠️⚠️ **這一趟不是照 `stryker.config.mjs` 跑的，而原因與本則無關、但要記下來：**
照原設定跑，乾跑在**第一支紅**就停（vitest 被 runner 設成 bail），三趟各停在不同的地方 ——

1. `vue-typecheck/tests/negative.test.ts`：`disableTypeChecks` 預設 `true` 是**全部檔案**，
   Stryker 就地往 `tests/fixtures/app/src/*.vue` 插 `// @ts-nocheck`，`vue-tsc` 從此
   什麼都不報，六條 🔴 全紅。**與遷移無關** —— 把 `390f585` 的舊版測試放回去、手動插
   `@ts-nocheck`，同樣六條紅。設定檔那段「排除 `tools/vue-typecheck/src` 就好了」的
   敘述在今天的樹上**不成立**；它 2026-09-02 為什麼過得了，本則沒有查。
2. `promise-check/tests/negative.test.ts`：`gate-thresholds.feature` 讓 `threshold-check`
   讀**真樹**，而真樹裡此刻躺著被插樁的 `testing.ts` —— 複雜度 42，門檻 39，紅。
   任何被插樁的檔都會這樣；這一條在 #255／#257 之後才有，09-02 那趟碰不到它。

所以這一趟用的是一份**只活在 scratchpad 的設定**：`disableTypeChecks` 縮成
`tools/gate-kit/src/**`、vitest 只收 `tools/*/tests`（排除 `promise-check` 四支）。
問的問題因此是「`tools/` 底下所有執行到 harness 的測試殺不殺得動它」，不是全樹。
**`stryker.config.mjs` 一個字沒動** —— 修那兩件事是另一則的事，不是本則的。

#### 九、與既有裁決的關係（C136 §八）

| 既有                           | 本則做了什麼                                                                                       |
| ------------------------------ | -------------------------------------------------------------------------------------------------- |
| **C137 §一**                   | **遵守** —— 零閘門增減；§一 明寫每一項處置動的是測試碼                                             |
| **C43**                        | **不動** —— 每道閘門的反向測試留在原處，harness 不碰斷言                                           |
| **C125 §五**                   | **部分過期，本則更正** —— `repoRoot` 早已在 `main` 的 `gate-kit`；description 那句跟著改           |
| **C126**                       | **引它** —— `runCli` 不補 `--root` 的理由                                                          |
| **C153 §三**                   | **遵守** —— 這次審查的詞彙與裁決都落在這裡，不造 `CONTEXT.md`、`docs/adr/`                         |
| **C121／`stryker.config.mjs`** | **不排除新檔，改那句過期的手算**（§五）                                                            |
| **D16**                        | 兩軸都有分：迭代軸 —— 清理政策改一次到處生效；交付軸 —— 無                                         |
| **`TESTING.md`**               | **不改。** 層 2 第一類的「無下限門檻」不受影響；§六 第 3 項若裁，會動到它 §四 對 `.spec.ts` 的定位 |

#### 十、⚠️ 與 `#237` 的關係 —— 同一個問題、兩把尺，而兩半差點沒見面

本則寫到一半，`#237`（`2722de9`，`reports/research/test-redundancy-loo-2026-09-02.md`）
合進了 `main`。它問的是**同一個問題**（有沒有多餘的測試），答案是
「量得到的 35 支裡可裁 **0** 支，另外 43 支沒被量過」。本則說有八組真重複。
兩個答案**不矛盾，因為尺不同**：

| 尺                | `#237` 留一法                            | 本則                                     |
| ----------------- | ---------------------------------------- | ---------------------------------------- |
| 最小單位          | **一支檔案**（拿掉整支，看覆蓋率／擊殺） | **一條接縫**，斷言到 describe／`it` 層級 |
| 問的              | 拿掉它，儀器讀數動不動                   | 同一個變異會不會讓兩邊同時紅             |
| 射程              | `tools/` 未釘住的 35 支                  | 84 支全部（讀，不跑）                    |
| 看得見 harness 嗎 | 看不見（它不是斷言，不影響讀數）         | 是本則最大的一組                         |

所以本則的八組**沒有一組是整支檔案**：每一支都同時裝著獨有的斷言（§七 那些
「留」的就在同一支檔裡），拿掉整支當然 Δ>0 或紅 —— `#237` 的「可裁 0」在檔案
粒度上成立，而本則在檔內粒度上找到的東西，那把尺結構上照不到。反過來，`#237`
的 Δ0 五支裡 `boundary-alignment.test.ts` 本則判**互補**（它守產生器產出的相對
路徑，contract-alignment 守常數）—— Δ0 是必要不充分條件，那份報告自己也這麼寫。

⚠️ **這一節存在的理由是形狀，不是內容**：兩份對同一個問題的產出各自開工、
各自快寫完才撞見，而它們用不同的尺得到看似相反的一句話。沒有這一節，下一個人
會拿「可裁 0」否決本則，或拿本則否決 `#237`。

### C169 — 往後 `main` 上的發版由什麼觸發：四個選項各自出局，而剩下那一個不需要數字（2026-09-05，#249）

⚠️ **量測基準 `3566e2f`。** 本則所有計數、blob 雜湊與日期凍結在那一支。

#### 一、裁決：**D 的一個變體 —— 加一支報距離的，而不設 N**

它每次跑印一行：**距上一個 tag 幾支 first-parent commit、幾天。**
**沒有門檻、不會紅、不擋任何 PR。**

⚠️ 這個變體不在票面那五列裡，是票面允許的「第六個選項」。它與 **D** 的差別
只有一處，而那一處是全部：**D 的代價欄假設提醒需要一個門檻（「仍要訂 N」），
本則的發現是它不需要 —— 而且訂不出來（§四）。**

本則**不做**三件事：

- **不設 N／M。** §四 是理由，不是省略。
- **不指定它放在哪。** C135 §一：裁決不能替自己發明新位置。
- **不實作。** 實作是 **#267**。⚠️ 而 C154 §四 那份報告本則**先填了**（§六），
  留給 #267 的只有第 3 條（自我防護的夾具另立一節）。

#### 二、A 與 B：票面自己的代價欄就把它們判掉了

| 選項                | 為什麼出局                                                                                                                                                                  |
| ------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **A. 每支合併就發** | release commit 在這棵樹是獨立一支 PR（#223 坑二）—— 25 支變更就是 25 支額外的 PR。票面已算過這筆帳                                                                          |
| **B. 固定週期**     | 「沒有變更的週要判不發」，而那個判斷沒有機械依據。⚠️ 還有票面沒寫的第二筆：版號判準是「升上來會不會壞」，一個為了週期而切的版**沒有東西可以判**，於是那個判準在那些版上空轉 |

#### 三、C 比 D 多出來的那一半是恆綠的 —— 實測

**C** 是兩半：「`api-surface` 報形狀變動 → 必發」**＋**「否則累到 N 支或 M 天」。
第一半不需要數字，所以它是 **C 唯一比 D 多出來的東西**。

**實測 `tools/api-surface/surface.json` 在每個發版窗口的 blob：**

| 窗口                  | blob                  |          |
| --------------------- | --------------------- | -------- |
| `v1.14.1` → `v1.15.0` | `13a900f` → `e1faa4d` | **變了** |
| `v1.15.0` → `v1.16.0` | `e1faa4d` → `e1faa4d` | 未變     |
| `v1.16.0` → `3566e2f` | `e1faa4d` → `e1faa4d` | 未變     |

⚠️ **對照組**（同兩個 tag 之間一個必然變動的檔）：`CHANGELOG.md`
`6b56697` → `8dc6af0` —— 量測有反應，「未變」不是假的零。

而唯一那一次「變了」**照這棵樹自己的判準也不觸發必發**。`v1.15.0` 的
`CHANGELOG` 寫著那筆變動是**純增**，並自己下了結論：

> 沒有移除、沒有改名、沒有形狀改變 —— 閘門沒有要求 codemod，也就沒有逼出 major。

所以：**在 `main` 成為交付線之後，C 的第一半開火 0 次；而它唯一的一次歷史開火，
在判準上不成立。** C 剩下的就是 D 的數字那一半。

⚠️ **這一節沒有在說 `api-surface` 沒有用。** 它守的是 major 的定義，而那是票面
「不屬於這張票」的第一列。本節只說一件事：**拿它當發版觸發器，買到的是一條
恆綠的線。**

#### 四、N 不可校準 —— 三個理由，而第三個是機械的

| #   | 理由                                                                   | 誰寫的                   |
| --- | ---------------------------------------------------------------------- | ------------------------ |
| ①   | 一個樣本不是分布                                                       | 票面自己                 |
| ②   | 第二個樣本（12 支／27 小時）來源不同，湊起來只會更糟                   | `v1.16.0` 的 `CHANGELOG` |
| ③   | **在 `v1.15.0` 之前，「距上一個 tag 幾支」在 `main` 上沒有良構的答案** | 本則                     |

③ 是實測出來的：**`v1.0.0`–`v1.14.1` 這 31 個 tag，沒有一個在 `main` 的
第一父鏈上** —— 它們活在已刪的 `release/v1`。

| tag       | 是 `3566e2f` 的祖先 | 在 `main` 第一父鏈上 |
| --------- | ------------------- | -------------------- |
| `v0.7.1`  | 是                  | **在**               |
| `v1.0.0`  | 是                  | 不在                 |
| `v1.10.0` | 是                  | 不在                 |
| `v1.14.1` | 是                  | 不在                 |
| `v1.15.0` | 是                  | **在**               |

於是那 31 個區間量的不是「`main` 隔多久打一個 tag」，是「隔多久把一批東西
**擢升**到另一條線」。**那是另一個問題。**

⚠️ **而這正是 25 支那個數字的來源。** `git rev-list --count v1.14.1..v1.15.0`
回 **63**；`CHANGELOG` 記的是 **25**。兩個都對 —— 那個區間**跨過併線**，
機械計數把 `main` 上早於 `v1.14.1` 的 commit 一起算了進去。
**一支報數的工具在那個窗口上會印 63。拿 25 去校準一個印 63 的工具，是兩把尺。**

**於是可校準的區間有幾個：**

| 區間                        | 兩端都在 `main` 上 | 可校準                                                          |
| --------------------------- | ------------------ | --------------------------------------------------------------- |
| `v0.1.0`…`v0.7.1`（7 個）   | 是                 | ⚠️ 否 —— 七個全部落在 repo 的第一天（2026-08-16），是腳手架初建 |
| `v0.7.1` → `v1.0.0`         | 跨線               | 否                                                              |
| `v1.0.0`…`v1.14.1`（31 個） | 否（`release/v1`） | 否                                                              |
| `v1.14.1` → `v1.15.0`       | 跨線               | 否 —— 單位不同（63 vs 25）                                      |
| `v1.15.0` → `v1.16.0`       | 是                 | ⚠️ 否 —— `v1.16.0` 自己判掉的（② ）                             |

**合計 0 個。** ⚠️ **不是「只有一個」，是 0。**

⚠️ 這不是「等久一點就會有」的那種 0：第一個能進這張表的區間是 `v1.16.0`
的下一個，**而在那之前，任何 N 都是憑空的。**

#### 五、E：它的代價是真的，本則不採用它，但保留它的問法

⚠️ **本則寫到一半時我一度認為 25 支是併線那件一次性的事造成的、所以不該算 E 的帳。
查了日期，那個說法不成立**：`release/v1` 在 **2026-08-26** 就併回並刪除
（同日 `865948f` 把它從排程的分支清單拿掉），而那 25 支累積在 **08-27 到 09-03**
—— **整段都在 `main` 已經是唯一交付線之後。E 的帳是 E 自己的。**

本則不採 E，理由不是「E 更貴」，是 **E 把問題留在看不見的地方**。
而票面驗收第三條只在裁 E 時要求寫下重問條件 —— **本則雖不裁 E，仍然寫**（§七），
因為它同時是 §六 ② 那一格的否證條件。

⚠️ **順帶記一筆過期**：C155 §一 當初不碰版號，理由寫的是「`main` 至今一版未發」。
那句話在 `v1.15.0` 之後**已經不成立**。本則不推翻 C155 —— 那是一個刻意的留白，
而填留白不是推翻（C136 §八 那一半不觸發）。記在這裡是因為**那個留白的理由
比留白本身早失效**，而 #249 是它退化成無人主張的預設之後才被掛回來的。

#### 六、C154 §四 的填寫要求 —— D16 兩軸

⚠️ 依 C154 §四，射程是「任何新增的機械檢查」，**包含一支不會紅的**。
本則**不**主張「它不會紅所以不算檢查」—— 那是 C137 §四 記的形狀
（判準套在描述上，不是實質上），也正是 C154 §四 關掉的那個繞道。

| 軸       | 這一格                            |
| -------- | --------------------------------- |
| **交付** | **零。** 它交不到評審桌上任何東西 |
| **迭代** | **非零**，見下                    |

**① 對象在外** ✅ —— 它報的是交付線的狀態（tag 與 HEAD 的距離），不是它自己。

**② 壞法安靜 —— 兩個主張，兩個級別，刻意不合寫：**

| 主張                                 | 級別             | 依據                                                                                                                                                          |
| ------------------------------------ | ---------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 這個壞法**真的發生過**，而且是安靜地 | **發生過的實例** | 25 支／8 天全程零紅燈零警告；`#223` 第三問**跟著票一起關掉**；`#249` 在那之後才把它掛回樹上                                                                   |
| 一個看得見的數字**會擋下**它         | **判讀**         | 那 8 天裡人每支 PR 都在讀閘門輸出；缺的不是注意力，是**那個數字沒有被印出來**（`tools/` 裡 0 支讀 git tag 或 `CHANGELOG.md`，對照組：45 支讀 `package.json`） |

⚠️⚠️ **第二個主張是本則的承重牆，而它只是判讀。** 依 C154 §三，判讀那一格要
多寫一句「要推翻它，該做什麼」：

> **把 #267 那支裝上去之後，如果再出現一次可比的積壓（數量級同於 25 支／8 天）
> —— 而那一行數字每一次跑都印在人眼前 —— 可見性這個假設就被否證了，
> 答案要翻到「D 帶 N」或 C。**

⚠️ 而那時**才會有可校準的區間**（§四）：否證與校準是同一件事的兩面，
所以否證發生的那一刻，訂 N 所需要的東西剛好也到齊了。

**自我防護的夾具**（C154 §三 第 3 條）：**本則不填，留給 #267。**
⚠️ 依 C154 §三，那些夾具不計迭代軸的分 —— 上表那一格的非零不靠它們。

#### 七、下一次在什麼條件下重問 —— 三個條件，任一成立

1. **§六 那條否證成立**：#267 裝上之後又積了一次可比的量。
2. **`surface.json` 在一個發版窗口內出現非純增的變動** —— 那時 C 的第一半
   第一次有內容，§三 的實測就過期了。
3. **有人開始從 tag 拉這棵樹。** 今天沒有：根 `package.json` 是 `private: true`、
   樹上沒有任何東西發佈到 registry、`tools/` 裡 0 支讀 tag。⚠️ 那一天
   「發版」買到的東西變了，票面整張代價欄要重算 —— **不是重算 N，是重算選項。**

#### 八、本則不裁的事

- **不動版號判準**（「升上來會不會壞」）—— 票面「不屬於這張票」第一列。
- **不動 `CHANGELOG` 的體例與寫法**。
- **不論證增減任何閘門**（C137 §一）。⚠️ §三 判 C 的第一半恆綠，用的是
  **實測開火次數**，不是「閘門太多」；§六 的兩軸是 C154 §四 要求的填表，
  不是增減的論證。
- **不實作、不指定位置**（#267／C135 §一）。
- **不推翻任何 C-條。** §五 記的是 C155 §一 一句理由的過期，不是推翻它。

#### 九、⚠️ 量測本身踩到的一個新坑，寫在這裡因為它會再咬人

`rtk` 改寫過的 `git log` **會靜默丟掉列**。同一個範圍：

| 量法                                                    | 結果                                                  |
| ------------------------------------------------------- | ----------------------------------------------------- |
| `git log --first-parent --format=%ad`（經 rtk）逐日加總 | **50**                                                |
| `rtk proxy git log` 同一行                              | **63**                                                |
| `git rev-list --count`                                  | 63（**兩種都回 63** —— 計數沒被改寫，只有列表被改寫） |

丟掉的是 **08-27 的 5 支、08-28 的 1 支、08-29 的 7 支** —— **兩整天加一天的
大半，而剩下的表看起來完全合理**。⚠️ 抓到它的是「逐日加總 ≠ `rev-list --count`」
這個對照，不是眼睛。**凡是要逐列讀 `git log` 的量測，走 `rtk proxy`，
並且拿一個獨立的計數當對照。**

### C170 — 「真樹綠」那一格：C168 說九處，重盤是十三處五種東西，而純重複只有三條（2026-09-05）

⚠️ **量測基準 `3566e2f`。** 本則的行號、計數都凍結在那一支（C168 §六 寫的行號是
遷移前的，`3566e2f` 自己把那幾支往上推了 10 行）。

#### 一、問題

C168 §六 第 2 項：「『真樹綠』收成 adoption 裡一條 `it.each`，刪九處光禿禿的
`status === 0`」。開工前重盤，**數字不對、形狀不對、提議的替代物填不上它自己
點名的洞**。

#### 二、重盤：對真樹跑閘門、期待綠的地方有 13 處，是五種東西

| 類                                                  | 成員                                                                                                                                            | 性質                                                                               |
| --------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------- |
| **A** 只斷言 exit 0，指令逐字在 `scripts.gate`      | `api-surface/negative:213`、`doc-facts/facts:376`、`pii-check/roster:141`                                                                       | 純 pass-through；`vpr ready` 的腿④兩步後再跑同一支                                 |
| **B** 只斷言 exit 0，指令**不在任何自動鏈上**       | `compliance/evidence:138`（`--evidence`）                                                                                                       | **洞**：`gate`／`ready` 都沒有它，只有手動別名 `scripts.evidence` 與 CI 的 variant |
| **C** 另有第二條斷言                                | `promise-check/cli:92`（場景數）、`promise-check/sandbox:175`（三段輸出）、`promise-check/cli:134`（分界）、`conformance/output:94`（版控檔數） | 各自有辯護，不是 exit-0 那一格                                                     |
| **D** 污染絆線（`describe("repo 本身沒有被動到")`） | `conformance/negative:541`、`compliance/negative:231`、`sbom-negative:197`                                                                      | 問的是「破壞性測試有沒有洩漏到真樹」；`vpr gate` 跑在測試之後，看不到那個時窗      |
| **E** 進程內、不 spawn                              | `gate-roster/roster:135`（:136 有辯護）、`scope-check/scope:103`                                                                                | 測的是 library 函式不是 CLI                                                        |

「九處」怎麼來的，已經還原不出一條自洽的路：C168 §二 把 `gate-roster:146`
（現 :136）當「唯一留了辯護的那一處」，也就是把 E 類算進九處；而把 E 類算進去
就數不出九。**那個數字是哪把尺量的，C168 沒寫**（它 §八 自己對另一個數字承認過
同一件事）。

另外兩件事實：

- **`conformance/negative:541`**「而且真正的 repo 仍然是綠的」跑的是
  `--root <repo 根>`，而檔案模式那條規則**刻意不在 `--root` 底下跑**
  （`output.test:84` 檔頭明文）。它讀起來像 A 類，實際比閘門弱一截。
- **`adoption.test` 的名冊剝掉 args**（regex 只抓 `tools/*/src/cli.ts`）。所以
  §六 第 2 項提議的那條 `it.each` 跑的是 13 支裸指令，**填不上 B 類那個洞** ——
  除非先做 §六 第 1 項（名冊改吃 `GATES ∪ variants`）。

#### 三、裁決

1. **A 類三條刪，不補任何替代。** deletion test 判「消失」：刪掉之後複雜度沒有
   搬去任何地方，`vpr gate` 就是那道守衛。§六 第 2 項的 `it.each` **否決**：
   它是把 `vpr gate` 整條搬進 `vp test` 再跑一次，是這組重複的純化形式不是它的
   消除。找不到辯護可寫 —— `gate-roster:136`「兩邊都要能看見同一件事變紅」對
   library 函式成立，對「spawn 同一支 CLI」不成立。
2. **B 類留，補一段註解寫事實不寫辯護**：它是本機唯一自動跑到 `--evidence` 的
   路徑，下面兩條只看 stdout、非零退出照樣印得出那兩句。**「該不該上鏈」不在
   本則** —— 那是閘門增減，C168 §一 開宗明義這一族裁決不碰 `vpr gate` 那條鏈。
3. **`conformance/negative:541` 那兩行刪**，污染那一半（store 沒被注入 import）
   原封不動 —— 那才是這條 `it` 存在的理由，檔內 :528 自己寫了。「帶 `--root`
   指到真樹也綠」這句話沒有人需要。
4. **C、D 其餘兩條、E 全部不動。** C168 §七「gate-roster 真樹 vs fixture …
   併入 §六 第 2 項」—— **不併**，E 類留在原處。`sbom-negative:197` 歸 §六
   第 7 項，本則不混。
5. 順手：`gate-roster/roster.test:210` 標題「真的名冊 16 列」，`GATES` 是 17 列，
   標題自己說「在加第 17 列那天才擋人」而第 17 列加了沒人動它。改成不帶數字。

C43 不受影響：三支的反向測試一條不動，動的是正向那一條。C137 §一 遵守：零閘門增減。

#### 四、實測

- **逐檔 `it` 數**（量法同 C168 §八）：前 58 支 982 條，後 58 支 979 條。
  差的 3 條正是 A 類：`api-surface/negative` 73 → 72、`doc-facts/facts` 36 → 35、
  `pii-check/roster` 23 → 22；其餘 55 支逐位相同。`conformance/negative` 仍是 34
  —— 第 3 項刪的是一條 `it` 裡的兩行，不是一條 `it`。
- **對照組是 C141 那道閘門自己響的**：註解裡先寫了 `C170`、本則還沒寫，
  `decision-ids.test` 點名兩處「指向不存在的編號」。編號絆線第一次在寫裁決的
  當下擋到寫裁決的人。
- `vpr ready` 全綠（見 PR）。

#### 五、順手撞到、本則不裁

- **`--evidence` 本機不在任何自動鏈上、CI 有。** 上不上鏈是一則獨立的裁決或一張票。
- **`adoption.test` 看不到四條 variant**（args 被剝掉）—— C168 §六 第 1 項的題，
  本則只把事實登記下來。
- `vpr gate` 上有、而一條「真樹綠」測試都沒有的閘門：`vue-typecheck`、
  `exit-drill`、`threshold-check`、conformance 的逐字指令。本則的立場是**那不是
  缺口**（腿④在跑），登記是為了下一次審查不用再數。

#### 六、與既有裁決的關係（C136 §八）

| 既有                 | 本則做了什麼                                                             |
| -------------------- | ------------------------------------------------------------------------ |
| **C168 §六 第 2 項** | **裁掉**，數字與提議的替代物都更正；§七 那句「併入 §六 第 2 項」改成不併 |
| **C168 §一**         | **遵守** —— 判準同一把（同時紅＋deletion test），不碰鏈                  |
| **C137 §一**         | **遵守** —— 零閘門增減；「B 類要不要上鏈」明文交出去                     |
| **C43**              | **不動** —— 反向測試一條不刪                                             |
| **C141**             | **它擋到了本則** —— §四 第二項                                           |
| **C126**             | **引它** —— `--root` 指到真樹為什麼弱：那條規則的射程是版控不是副本      |

### C171 — 一支不會紅的東西放哪裡：`UNGATED` ＋ `ready` 的尾巴，而它上不了 workflow 的理由是 CI 沒有 tag（2026-09-05，#267）

⚠️ **量測基準：本支自己**（`feat/release-distance-267`，接在 C170 之後）。
本則是 **C169 §一** 的實作，並補上 **C169 §六 留給本票的那一格**（C154 §三 第 3 條）。

#### 一、裁決：三個落點

| 問題         | 裁決                                                                                                                  |
| ------------ | --------------------------------------------------------------------------------------------------------------------- |
| 名冊位置     | **`UNGATED`**，不是 `GATES`                                                                                           |
| 執行位置     | **`scripts.ready` 的最後一步**，形式是根 `vite.config.ts` 的 **task**（§九）—— 不進 `scripts.gate`、不進兩個 workflow |
| 什麼時候非零 | **只有不認得的旗標**（C126）。其餘一律 0，**夾具響了也是 0**                                                          |

#### 二、為什麼不是 `GATES` —— 第二個理由是實測

1. **語意**：一支不會失敗的東西進了閘門鏈，「閘門」這個詞在這棵樹上就少掉一半意思。
2. ⚠️ **機械**：`GATES` 的成員會被 `deriveGateScript` 與 `deriveTierCommands`
   推進 `scripts.gate` 與兩個 workflow —— 而 **CI 是淺 checkout**：
   `tier1-quality.yml:28` 的 `actions/checkout@v7` 沒有 `fetch-depth`，預設是 1，
   **不帶 tag、不帶歷史**。於是它在 CI 上會**每一趟**印「量不到」。

⚠️⚠️ **第 2 點是本則唯一一個對別的東西也有用的事實：這棵樹的 CI 上沒有 tag、
沒有歷史。** 任何一道想讀 git 歷史的檢查都要先付這筆錢（改 `fetch-depth` 會讓
每一趟 CI 變慢）。**本則不裁要不要改它** —— `#267` 的射程裡沒有 CI 設定。

#### 三、為什麼是 `ready` 的尾巴，而代價明寫在這裡

最後一步＝人讀到的最後一行。⚠️ **代價是：`ready` 紅的時候它不印 ——
而樹紅了好幾天，正是積壓長大的時候。**

取捨的依據是 `AGENTS.md` 規則一（「全綠才算完成」），所以正常的終止狀態是綠的。
⚠️ 落點與 `spec-report --check` 相同而**理由不同**（那一支是「兩種都不是這次的
完成率」，見 `gates.ts:174`）—— 位置一樣不代表論證可以互相引用。

#### 四、「量不到」與「零」分開報

三種情況印「量不到 —— ⟨成因⟩」而不是 `0`：不是 git 工作樹、看不到任何 tag
（淺 checkout）、找得到 tag 但數不出距離。⚠️ 這棵樹一再栽在這一格上，
而這一支**沒有紅燈可以掉**，所以誤報成 `0` 不會有任何東西發現。

#### 五、C154 §三 第 3 條 —— 自我防護的夾具三條

**要擋的安靜壞法很具體**：它跑完、印出一行**看起來完全合理**的數字，而那個數字
量的是別的東西，或什麼都沒量。⚠️ 因為它沒有紅燈，**「印得出來」與「量對了」
在輸出上長得一模一樣**。

| 夾具                                            | 擋的那個假答案                                                                                                       |
| ----------------------------------------------- | -------------------------------------------------------------------------------------------------------------------- |
| `git` 回的 toplevel ≠ 呼叫端問的路徑            | `git` 會從 cwd 往上找 —— 一個打錯的路徑會安靜地爬到外層某個 repo，而輸出照樣是一行數字                               |
| 數出 `0` 支，但 HEAD 不是那個 tag 指到的 commit | ⚠️ **`0` 是這支最像真的錯誤答案**：範圍寫壞、tag 解析到別的東西，`rev-list --count` 都回 0，而「剛發完版」本來就是 0 |
| 讀得到 tag 名字、解析不出它的 commit            | 後面每個數字都是從空氣算出來的，而輸出裡仍然有那個 tag 的名字                                                        |

⚠️ **夾具響了也不紅**，只印一行 stderr。理由：一個「量測台壞了就變紅」的例外，
會讓它在淺 checkout 之類的環境裡變成一道**沒有人打算加的閘門**。
代價是這一行只能靠人讀。

⚠️ 依 C154 §三 第 3 條，這三條**不計 D16 迭代軸的分** —— C169 §六 那一格的非零
不靠它們。

#### 六、數的是第一父鏈，而它自己會講出分岔

C169 §四 的 **63 vs 25** 寫進了 `src/git.ts` 的檔頭，而不只是註解：
`onFirstParent` 為假時輸出會多印一行說「這個數字會與人數出來的分岔」。
⚠️ 那正是 `v1.14.1 → v1.15.0` 那個跨併線窗口的形狀 —— 今天的樹上不會再有，
但它是這支工具唯一一個**已知會給出誤導性正確答案**的情境。

#### 七、本則不裁的事

- **不設 N。** 要設先推翻 C169 §四（可校準的區間 0 個），不是在實作裡加一個常數。
- **不改 CI 的 checkout 深度**（§二）。
- ⚠️ **不寫 `.feature`。** C163 的「每份規格都要有對照組」與「證明它真的會紅」
  預設了一個**有紅燈的閘門**，而這一支沒有；而且 `AGENTS.md` 規則四 本來就不准
  我寫規格。**取而代之的是那條載重測試**：「除了旗標，什麼都不紅」，
  並附一個對照組斷言真樹上**真的量到了** —— 少了它，一棵沒有 tag 的樹會讓
  每一條「回 0」的斷言都假性通過。
- **不論證增減任何閘門**（C137 §一）。

#### 八、⚠️ 一條昨天寫下的絆線，這支是它的第一個案例

`tools/gate-kit/tests/harness-tripwire.test.ts:12`（C168 §四）寫著：

> 收攏的那一天 22 支全遷了；這條在的理由是**第 23 支**。

實測：`3566e2f`（C168）之後，`tools/*/tests/*.test.ts` **新增的只有本支這一個檔**。
它走 `@org/gate-kit/testing`，絆線是綠的。

⚠️ **所以這不是「它擋下了什麼」，是「它第一次有機會擋」。** 記在這裡是因為
兩者在紀錄上長得一樣，而只有前者能拿去論證那條絆線有效。

#### 九、⚠️ 它必須是 task 而不是 script，而發現這件事的是一個對不上的數字

寫完之後 `vpr ready` 印的是「距 `v1.16.0` 已 **5** 支」，而 `git rev-list --count --first-parent v1.16.0..HEAD` 是 **8**。成因不在工具裡：

根 `vite.config.ts` 的 `run.cache: true` 同時打開 **script 快取**，而 `&&` 串起來的每一段會被拆成各自快取的子任務（`vp` 文件〈Compound Commands〉）。快取鍵來自**自動資料追蹤**（這個行程讀過哪些檔），而這支的輸入是 **git 的 ref，不是檔案** —— 於是它第一趟之後**永遠 cache hit**，把同一個數字重播下去。

⚠️⚠️ **一個凍住的數字與一個正確的數字，在輸出上一模一樣。** 而這支**沒有紅燈可以掉**（§一），所以除了有人手動去數，沒有任何東西會發現。

**試過而無效的一條**：讓 CLI 去 `readFileSync` 那幾個 ref 檔（`HEAD`、`packed-refs`、`refs/heads/<branch>`），把它們宣告成輸入。**實測：提交一支之後仍然 cache hit** —— 追蹤不看 `.git` 底下。那段程式碼已經拿掉。

**採用的解法**：把它定義成根 `vite.config.ts` 的 task，帶 `cache: false`（先例：`tools/slice-gen/vite.config.ts`）。`vpr ready` 那一步因此寫成 `vp run release-distance`，輸出印的是 `⊘ cache disabled`。

⚠️ **代價有兩個，都不是零：**

1. **同一個名字不能同時是 task 與 `package.json` 的 script**（會是 `Failed to load task graph`），所以 `vpr release-distance` 這個別名沒有了 —— 單獨跑它要 `vp run release-distance`。
2. ⚠️⚠️ **它的路徑因此不再出現在 `scripts.gate ＋ scripts.ready` 的字串裡** —— 而 `gate-kit/tests/adoption.test.ts`（C125／C126 的絆線）正是從那兩個字串推導名冊的。**不動它的話，這一支會在加進來的那天安靜地掉出 C126 的射程。** 本則因此把那份名冊的事實來源從兩處擴到三處（加根 `vite.config.ts` 的原始碼），並加了第二個具名錨點。

⚠️ 那正是那支測試檔頭自己寫的形狀：「**閘門鏈的成員資格，對『會不會被執行』既不必要也不充分**」。它第一次是為了 `spec-report` 寫的（在 `ready` 不在 `gate`），這是第二次 —— 而這一次連 `ready` 的字串裡都沒有。

### C172 — 切片範本的「命名空間」斷言：C168 說整個 describe 不可達，實測是十條不可達、七條獨一，而不可達的前提住在另一個 package（2026-09-05）

⚠️ **量測基準 `a318dc4`。** 本則的行號與計數凍結在那一支。

⚠️ 本則第一版編 C171；#270（C171）同日先開，本則讓號。C141 只守「指得到」，兩支 PR
各自在自己的分支上都綠，撞號只有讀兩邊的人看得見 —— 而寫「第一版編 C171」這句時，
C171 在這條分支上還不存在，C141 當場擋了一次。

#### 一、問題

C168 §六 第 3 項：「`slice-gen` 範本的命名空間 describe 不可達；conformance 的
`hasTestFile` 認 `.spec.ts`。⚠️ 會動 conformance 一條規則的射程，要先裁。」
開工前重盤，**第一句不精確，第二句與第一句沒有相依**。

#### 二、重盤：範本一份、手抄本三片，逐條看

`defineFeature`（`platform/slice-kit/src/define-feature.ts:104`）在 `import.meta.env.DEV`
下對路由 name、頂層路由 path、權限碼、i18n 頂層 key、選單 labelKey／routeName
**前綴**逐一 throw。切片測試第一行是 `import feature from "../src/index.ts"`，
所以違規時 import 先炸，檔案裡的斷言一條都輪不到。

| 條                             | 範本 `files.ts:917-941`／三片 | 判                                                            |
| ------------------------------ | ----------------------------- | ------------------------------------------------------------- |
| 路由 name 落在命名空間         | 三片各一                      | **不可達** —— `define-feature.ts:61` 先 throw                 |
| 頂層路由 path 落在 `/order`    | 只有 order                    | **不可達** —— `:68`                                           |
| 權限碼落在命名空間             | 三片各一                      | **不可達** —— `:77`                                           |
| i18n 頂層 key 恰好一個         | 三片各一                      | **不可達** —— `:87`                                           |
| 選單項目指向**實際存在**的路由 | 三片各一                      | **可達、獨一** —— `defineFeature` 只驗前綴（`:98`），不驗存在 |
| query key 以切片名開頭         | 三片各一                      | **可達、獨一** —— `defineFeature` 不碰 `api.ts`               |
| list key 帶條件時不相等        | 只有 order                    | **可達、獨一**                                                |

不可達 **10 條**（3＋1＋3＋3），可達 **7 條**。C168 寫「describe 不可達」把第五列
一起算進去了；那一列是三片切片裡**唯一**在守「選單指到的路由存在」的地方，
`register.ts` 也不驗。

實測（都已還原）：invoice 權限碼改成 `read` → `tests/invoice.test.ts (0 test)`，
`FeatureContractError` 在 `src/index.ts:11`；invoice 選單 `routeName` 改成
`invoice/nope` → import 不炸，**只有**第五列那條紅。

**候選 3 的後半與前半沒有相依。** 刪掉 10 條之後每片檔案都還在（invoice／shipment
剩 2 條、order 剩 3 條），`hasTestFile`（`conformance/src/scan.ts:29`）照樣滿足。
`hasTestFile` 認不認 `.spec.ts` 是閘門一條規則的射程，不是測試碼 —— 與 C170 §五
「`--evidence` 要不要上鏈」同一類，本則不裁（§五）。

#### 三、不可達的前提，與它住在哪裡

那 10 條是重複，**只在 `import.meta.env.DEV === true` 時成立**。守這件事的是
`platform/slice-kit/tests/define-feature.test.ts` 那八條 `toThrow` —— 在另一個
package。而每片切片有自己的 `vite.config.ts`（C120 的覆蓋率門檻），根層 `test`
區塊整塊不繼承。

實測前提能不能翻：

| 入口                                     | `DEV`     | `NODE_ENV`                                   |
| ---------------------------------------- | --------- | -------------------------------------------- |
| `vp run -r test`（`vpr ready` 走的）     | true      | `test`                                       |
| 同上，外面套 `NODE_ENV=production`       | true      | `test`                                       |
| 同上，`-- --mode production`             | true      | `test`（`MODE` 變 `production`，`DEV` 不動） |
| 在切片目錄 `NODE_ENV=production vp test` | **false** | —                                            |

所以 `vpr ready` 那條路上它翻不了，但 `vp test` 那條路上一個環境變數就翻了 ——
而翻了之後 `defineFeature` 不驗、切片測試也不驗，違規切片**全綠**。

#### 四、裁決

1. **範本與三片刪那 10 條**，可達的 7 條原樣留。
2. **describe 改名**：「`<name>` 切片契約」→「`<name>` 切片契約 —— defineFeature
   不驗、只有這裡在守的」。留著原名，下一個人會覺得少了三條而補回去；
   改名是唯一寫在檔案裡、不靠讀裁決就看得到的訊號。
3. **前提掛絆線**：範本與三片各加一條 `expect(import.meta.env.DEV).toBe(true)`，
   describe 名直接寫「命名空間斷言在 C172 刪掉的前提」。守衛從三片搬到 slice-kit
   這件事必須有人知道搬了；§三 那張表證明它在 `vp test` 那條路上真的會紅。
4. **檔頭兩句都留**：「一致性檢查要求至少一支 `.test.ts`」是事實；補一段
   「命名空間由 `defineFeature` 在 import 時驗，違規時這支檔一條都跑不到」是刪除的
   理由 —— 少了第二句，五個月後 review 會問「為什麼切片測試不驗命名空間」。
5. **不裁 `hasTestFile` 認 `.spec.ts`**，不開票（§五）。
6. **順帶降一條門檻**：`vite.config.ts` 那條 per-file `max-lines-per-function`
   850 → 843。刪十條讓 `buildSliceFiles` 短了 7 行，`threshold-check` 的「門檻過期」
   規則（C147 §二）當場咬住 `vpr ready`；降不是調鬆、不需要論證，但 C119 寫明那條線
   每動一次都要記帳，所以記在這裡。

淨變化 −10 ＋4（範本那一條不在樹上的 `it` 數裡，只在產出的切片裡）。

#### 五、順手撞到、本則不裁

- **`hasTestFile` 認不認 `.spec.ts`**：閘門規則的射程，C137 §一 那一側。現在
  `slice-gen` 產的切片同時有 `.test.ts` 與 `.spec.ts`，沒有任何一片正在被這條規則
  擋，所以不開票 —— 沒有相依方的洞開票會變成第四個沒人主張的預設。
  等真的有人要做「只有規格的切片」那天再開。
- **`checkSliceTests`／`hasTestFile` 在 `tools/conformance/tests/` 裡零反向測試**：
  `report.test.ts:31,55` 只把「測試」當字串標籤用，`rules.test.ts` 沒有它。
  補它是測試碼，但不是本則的題目（重複），登記給下一次審查。
- **`vp test` 與 `vp run -r test` 對 `NODE_ENV` 的處理不同**（§三 表）：同一支工具兩條路，
  一條翻得動一條翻不動，**兩邊都不出聲**（快取重播至少還印 `cache hit`）。本則只用它
  證明絆線會紅，沒有查是哪一層改寫的。

#### 六、與既有裁決的關係（C136 §八）

| 既有                 | 本則做了什麼                                                                  |
| -------------------- | ----------------------------------------------------------------------------- |
| **C168 §六 第 3 項** | **裁掉前半**，「describe 不可達」更正成「十條不可達、七條獨一」；後半明文不裁 |
| **C168 §一**         | **遵守** —— 同一把判準（同時紅＋deletion test），閘門鏈不動                   |
| **C137 §一**         | **遵守** —— 零閘門增減；`hasTestFile` 的射程明文交出去                        |
| **C170 §五**         | **同形** —— 「洞不是重複、另立一則」的處置與它一致                            |
| **C120**             | **引它** —— 每片自己的 `vite.config.ts` 是前提住在另一個 package 會出事的理由 |
| **C154 §四**         | **填了**（§八）—— 第一版判「不適用」，理由是位置不是實質，撤回                |

#### 七、實測（基準 `a318dc4`，worktree `slice-namespace-dead`）

- 逐檔 `it` 數（`vp run -r test -- --reporter=json`，彙整 `tools/*`、`features/*`、
  `platform/*`、`apps/*` 的 `.vitest-results.json`）：**1568 → 1561**，83 支檔案只有
  三支變：invoice 5→3、order 7→4、shipment 5→3，逐位就是 −10 ＋3。
- 短路兩個方向：shipment 選單指向 `shipment/nope` → 留下那條紅、其餘綠；
  在切片目錄 `NODE_ENV=production vp test` → 新加那條 `DEV` 絆線紅（§三 表）。
- `vpr ready` 第一趟 **RC=1**：`threshold-check` 門檻過期（§四 第 6 項）；降到 843
  後 RC=0。

#### 八、C154 §四 的填寫要求 —— D16 兩軸，而射程那條線在本則之前沒有人畫過

⚠️ 本則第一版把 §四 第 3 項那條 `DEV` 絆線判成「不在 C154 §四 射程 —— 它是切片
測試裡的一條 `it`，不是工具裡的規則」。**那是位置論證**，正是 C154 §四 最後一段
擋的形狀（判準套在描述上不是實質上）。照實質問：它是新增的、機械的、會紅的
判定，在射程裡。撤回，填。

⚠️⚠️ **這是一次邊界判斷，而那條線在本則之前沒有人畫過。** 「任何新增的 `expect`
都算」顯然不是 C154 的意思（那會要求每一條測試斷言附報告）。本則採用的分界是
C154 §三 第 2 條 ① 的直接推論：**普通斷言的對象在內**（驗的是同一個單元的行為，
改壞它的人正在改的就是它），**絆線的對象在外**（報的是環境／設定／別處壞了）。
`import.meta.env.DEV` 是環境事實，對象在外，所以填。這條線本則用了，**沒有裁它**；
下一個人若要拿它當先例，先知道它是在這裡第一次出現的。
「這個問法長什麼樣」的參照：`#222` 已對六條住在 `tests/*.test.ts` 裡的必填理由斷言填過
兩軸 —— ⚠️ 它的提案是把那些斷言搬到閘門層，所以嚴格說被填的落在閘門層，不當先例引，
只證明「對象在測試裡的東西填兩軸」這個動作這棵樹做過，而當時沒有人主張「是測試所以不必」。

| 軸       | 這一格                            |
| -------- | --------------------------------- |
| **交付** | **零。** 它交不到評審桌上任何東西 |
| **迭代** | **非零**，見下                    |

**① 對象在外** ✅ —— 它報的是「這片切片跑測試時 `defineFeature` 有沒有在驗」，
不是它自己被改壞。

**② 壞法安靜 —— 級別：探針。** §三 表就是那個探針：在切片目錄
`NODE_ENV=production vp test`，`DEV` 變 false，`defineFeature` 一條都不驗；
同一趟裡那支檔其餘的 `it` **全綠**。也就是在這條絆線不存在的世界裡，一片違規
的切片在那條路上是綠的，而且沒有任何輸出提到 `DEV` 翻了。⚠️ 探針只證明那條路
翻得動；「有人真的走那條路」是判讀，沒有實例。

⚠️ 探針走的是切片目錄裡的 `vp test`；`vpr ready` 與 CI 走的是 `vp run -r test`，而那條路
今天翻不動 `DEV`（§三 表）。所以這條絆線證明的是「它會紅」，**不是**「本樹的日常執行會讓它
紅」—— 它守的是哪天上游或某個 task 讓 `DEV` 變 false 的那一天，而那一天不會走手動構造的
這條路。

**自我防護的夾具**（C154 §三 第 3 條）：**沒有。** 這條 `it` 一行斷言，沒有東西在守
它自己不被刪 —— 刪了它的人看到的是一支少一條的綠檔。守它的只有範本：新產的
切片會帶著它。

### C173 — 重複的裁決編號：數得出來的那一半守得住，而真正想避免的那一半發生在寫下之前（2026-09-05，#262）

⚠️ **量測基準 `6d93df8`。** 本則所有計數凍結在那一支。

#### 一、裁決

`tools/doc-facts/tests/decision-ids.test.ts` 加一條：**沒有一個編號被兩則條目認領**，
射程 `C`／`D`／`R` 三串。它住在 C141 那條的隔壁，因為它們問的是同一份東西的兩件事。

⚠️ **而本則同樣裁定它要明文寫下自己不守什麼**（§三）—— 那句話寫在紅燈訊息裡，
不是只寫在這裡。

#### 二、⚠️ 先更正票面：「加一條斷言不難」是錯的

`#262` §四.1 寫著「加一條斷言不難」。**天真的版本今天就會在 `main` 上紅。**

`DECISIONS.md:403` 是 `### D14 下半：Pinia 的界線` —— 它是 **D14 的續節**，
不是第二則 D14，而且 **2026-08-15 就在那裡**。定義集的樣式 `^### *D(\d+)\b`
吃得下它，於是 D14 出現兩次。

**解法是分兩把尺：**

| 尺                          | 樣式                   | 問的                                                     |
| --------------------------- | ---------------------- | -------------------------------------------------------- |
| 寬（`definitions()`，原有） | `^### *C(\d+)\b`       | 這個編號**存不存在** —— 結果進 `Set`，重複在那裡塌成一個 |
| 嚴（`headingsIn()`，本則）  | `^### *([CD])(\d+) *—` | 這一行是不是一則**定義**的開頭 —— 續節標題因此不算       |

⚠️ **嚴的那把不可以要求破折號後面有空格**：`DECISIONS.md:1633` 是
`### C37 —「這個應用怎麼上樣式」…`，破折號直接接引號。收緊成 `— ` 會讓 C37
掉出去，**而掉出去的方向不會紅**。

⚠️ **兩把尺會漂開**，所以本則加一條夾具斷言它們對「有哪些編號」完全同意
（今天 C 170／D 16／R 9，兩邊一致）。`R` 不在這個問題裡 —— 它的定義樣式就是
登記表的列，兩把尺是同一把。

#### 三、它守的是哪一個變數 —— `#262` §四.2 問的那件事

|            |                                                                                                     |
| ---------- | --------------------------------------------------------------------------------------------------- |
| **守得到** | 重複**已經進了同一棵樹**（同一個工作樹裡有兩則同號）                                                |
| **守不到** | ⚠️ **兩條未合併的分支各自認領同一個號** —— 那個狀態**沒有任何一棵工作樹讀得到**：兩邊各自都只有一則 |

**而「合併前會紅」不是這道檢查的性質。** 實測：這個 repo 的 branch protection 是
**`strict: false`** —— 沒有任何 PR 被強制 rebase。它之所以通常會在合併前紅，
靠的是**「裁決一律追加在檔尾」這個慣例**：

> 追加在檔尾 → 第二支 PR 必然衝突 → 必然 rebase → rebase 之後它的檔案裡有兩則
> 同號 → **那一支 PR 的 CI 當場紅**。

⚠️⚠️ **那個慣例沒有任何機制在守。** 哪天有一則插在檔案中間而不衝突，
重複就會直接進 `main`，這條要到 `main` 的下一趟才紅 —— 事後，而不是事前。

#### 四、今天的實例，照實寫

兩個 session 各自把新裁決編成 **C171**，兩條分支上 C141 **都是綠的**。

⚠️ **而這道檢查抓不到今天那一件** —— 對面在 rebase **之前**就改號了
（改號時它寫下「第一版編 C171」那個字面，C141 當場擋下：那條分支上 C171 不存在。
⚠️ 那剛好說明 C141 守的是「指得到」，不是「不重複」）。**抓到撞號的是兩個 agent
互相報號，不是任何機制。**

所以這道檢查是**「我們沒有講話」那個情況的後手**，不是今天那件事的解。
⚠️ 把它寫成前者，正是 C152 §二 警告的形狀：**守住數得出來的那個，
會讓下一個人更確信那個真正重要的變數已經被顧到了。**

**三次撞號，三個不同的成因：**

| 何時         | 形狀                                | 處置                                      |
| ------------ | ----------------------------------- | ----------------------------------------- |
| 併線         | 兩條分支各自編號（C70–C74）         | `main` 那批已改號 C128–C132               |
| `#238`／C160 | 同一條線上兩張票並行                | `#238` 已於 2026-09-04 改號 **C167** 合併 |
| 今天         | **兩個 session 並行**，沒有票在中間 | 互相報號，其中一方讓號                    |

#### 五、⚠️ 本則自己就是第四次，而 C141 一度不准我把它寫清楚

寫本則的當下，**C172 由一支還沒合併的 PR（`#271`）認領**，所以本則跳過它、編 C173。
⚠️ **我知道這件事的唯一途徑是跨 session 的訊息** —— 那一刻 `main` 上讀不到 C172，
任何機械檢查也讀不到。

⚠️⚠️ **而那一刻我不能把那個編號的字面寫進來，因為 C141 會擋下它** ——
那個號在這條分支上不存在，寫下去就是一個指不到的引用。**實測踩到了**（本則第一版
寫了字面，`decision-ids` 當場紅在四處），而對面改號時**也踩到同一格**。

**所以這裡有一個沒有人記過的性質：C141 使得「在決策日誌裡描述一個尚未合併的編號」
在字面上是不可能的。** 那不是缺陷 —— 它守的正是「寫下的那一刻就是死的」那個方向；
但副作用是**撞號只能用繞過字面的方式被記錄下來**，而繞過字面的句子比較難讀。

**同一句話的三種狀態，而 C141 只看得見中間那一種：**

| 那一刻                                            | 那句話                 | C141   |
| ------------------------------------------------- | ---------------------- | ------ |
| 分支基準還在 `#271` 之前，句子寫著那個編號的字面  | 指向一個樹上不存在的號 | **紅** |
| 改寫成「本則的前一號」，不寫字面                  | 沒有引用               | 綠     |
| rebase 到那個號已經存在的 `main` 之後，字面寫回去 | 指得到                 | 綠     |

⚠️ **第一格與第三格是同一句話、同一份意思，只有時間不同。** 而對面那支 PR
（`#271`）**獨立踩到同樣的三格** —— 它讓號時在檔頭寫「本則第一版編 C171…」，
同一行被報了兩次（那句裡那個號出現兩次）。

⚠️ **後來的事實**：`#271` 在本則之前落地（2026-09-05 08:00 UTC），於是 C172 成為
`main` 上的一則，上面那個限制**當場消失**，這一節才寫得出它的字面。
**那個限制是有時效的，而它的時效由另一支 PR 的合併時間決定** —— 兩件本則預先寫下、
而沒有發生的後果，也一併記在這裡：

| 預先寫下的後果                                          | 實際                                                   |
| ------------------------------------------------------- | ------------------------------------------------------ |
| `main` 上會短暫地從 C171 跳到 C173（缺一號）            | **沒有發生** —— `#271` 先合，順序是 C171 → C172 → C173 |
| `#271` 若在本則之後落地，它的編號會物理上排在 C173 後面 | **沒有發生**，同上                                     |

⚠️ 兩件都靠**合併順序**避開，而合併順序是人決定的，不是任何機制。
本則因此**不**把「順序正確」列成一個成立的性質。

#### 六、C154 §四 的 D16 兩軸

| 軸       |          |
| -------- | -------- |
| **交付** | **零**   |
| **迭代** | **非零** |

**① 對象在外** ✅ —— 它報的是別人寫下的條目標題，不是它自己。

**② 壞法安靜 —— 級別：發生過的實例**，而且有兩件真的：

- `#238` 帶著一個會撞號的 `### C160` 開著，**而 `vpr ready` 與 CI 兩邊都綠**
  （`#262` §一 的實測：在檔尾 append 一行重複標題，`doc-facts` 56 條全過）。
- 今天的 C171（§四）。

⚠️ **附一句判讀，不與上面合寫**：「它會在 rebase 的時候擋下」依賴 §三 那個
沒有機制在守的檔尾慣例。**要推翻它**：哪天有一則裁決插在檔案中間而合併時不衝突，
這句就不成立了。

**自我防護的夾具**（C154 §三 第 3 條，不計迭代軸的分）：

- 兩把尺的**集合相等** —— 嚴的那把漏掉一則時，那一則的重複永遠不會被數到，
  **而寬的那把仍然全綠**。
- ★ **合成一則重號**，並且斷言的是**恰好那一個 id**（不是「有東西紅了」）。
  ⚠️ 同一段合成裡放一個 `### D<n> 下半：` 的續節，斷言它**不**被算成第二則 ——
  沒有這一半，§二 那個回歸不會被擋住。
- ⚠️ 合成的編號**是算出來的**：寫死一個字面量，C141 那條會把這支測試自己掃成違規。
  **實測踩到過** —— 第一版用寫死的三個號，`decision-ids` 紅在它自己的合成樣本上，
  訊息指著這支測試檔的行號。（⚠️ 那三個號的字面同樣不能抄進這裡，理由見 §五。）

#### 七、本則不裁的事

- **不做「認領編號」的登記檔、鎖、或前置步驟**（`#262` §五）。那要守的是寫下**之前**
  的狀態，而本則明文說了那不在射程裡 —— 把它一起做，就是拿一個數得出來的東西
  替一個數不出來的東西背書。
- ⚠️ **不加「定義順序遞增」的斷言 —— 這是一個選擇，不是遺漏。** 兩條路各有代價：
  要順序單調，代價是**重編號**（第二支 PR 得改自己的號，而第一支可能已經在 `main`
  上被引用了）；不重編，代價是**檔內順序偶爾不單調**。**本則選後者：編號在指派時
  就定，之後不隨合併順序重編。** ⚠️ 本則自己差一點就是案例 —— `#271` 若晚一步落地，
  檔案會是 C171、C173、C172。⚠️⚠️ 而這個取捨是**兩個 session 談出來的**，
  不是任何機制推導的；記在這裡，是因為下一個人只會看到「沒有那條斷言」。
- **不動 `ABSENT`**，那不是旋鈕。
- **不裁檔尾慣例要不要有機制守**（§三 只是點名它是承重的）。
- **不增減任何一道閘門**（C137 §一）。⚠️ 本則是**在既有工具裡加一條規則**，
  射程由 C154 §四 明文涵蓋（「不是只有新增獨立的一支」）。

### C174 — `bff-mock` 的注入接縫測試：C168 說「401／403／CSRF 重複」，實測十二條只有一條重複，而 401 與 CSRF 正是接縫本身（2026-09-05）

⚠️ **量測基準 `9a8c9a4`。** 本則的行號與計數凍結在那一支。

#### 一、問題

C168 §六 第 4 項：「`bff-mock` 的測試收到只剩注入 seam 特有的性質；body 形狀算不算
契約要先決定。」§二 那一列寫的是「BFF 401／403／CSRF：`bff-mock` routes.test ↔
`bff-check` contract.test（同一個 `startBffMock`）」。開工前重盤，**三個字只有 403 是真的**。

#### 二、重盤：十二條逐條對十五條問「有沒有一個變異只讓一邊紅」

`platform/bff-mock/tests/routes.test.ts` 12 條 ↔ `tools/bff-check/tests/contract.test.ts`
15 條契約項。判準與 C168 §一 同一把：同時紅＋deletion test。

| #   | routes.test                 | 對 contract 的關係                                                                                                                     |
| --- | --------------------------- | -------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | 命中回傳內容、query 拿得到  | 接縫獨一（handler 的輸入形狀）                                                                                                         |
| 2   | ★ 注入路由沒 session 回 401 | **不是重複**：變異「`matchRoute` 搬到 session 閘門前」只有它紅；contract 打 `/api/ping`，照樣 401                                      |
| 3   | ★ 契約端點蓋不掉            | 接縫獨一（同一個變異也紅）                                                                                                             |
| 4   | ★ 注入路由 POST 要過 CSRF   | **不是重複**，同 #2：CSRF 閘門在 `server.ts:335`、比對在 :420                                                                          |
| 5   | POST body 解析              | 接縫獨一                                                                                                                               |
| 6   | params ＋ permissions       | 接縫獨一；`apps/console/bff-routes.ts` 檔頭點名 #1／#5／#6 是它形狀的守衛                                                              |
| 7   | 沒命中時示範資料仍在        | 接縫獨一（fallthrough），但釘的是 `total: 4`                                                                                           |
| 8   | 處理器炸 → 500              | 接縫獨一                                                                                                                               |
| 9   | ★ 預設值不含 admin          | **真重複**：與 `403-forbidden` 同一個 `startBffMock()`、同一條路徑、同一個期望；`negative.test.ts` 還替 `403-forbidden` 放了第三份牙齒 |
| 10  | ★ 追加不是取代              | 接縫獨一，釘的是 `order:read`／`shipment:read` 字面                                                                                    |
| 11  | 追加的在 GET session 看得到 | 接縫獨一（GET 走 `session.permissions`，與 #10 的 POST 分開）                                                                          |
| 12  | cookie 名字不受注入影響     | 接縫獨一，找不到合理變異                                                                                                               |

C168 把 #2／#4 讀成「打契約端點」，實際上它們打的是**注入路由**，而「注入路由落在
401／CSRF 閘門之後」正是 `server.ts:405-418` 那段註解說的、這道接縫唯一重要的事。
這與 C172 對候選 3 的更正同形：C168 的盤點不精確。

#9 的註解寫「admin 一旦進了預設清單，403 那條就永遠驗不到」—— 錯。那條會**直接紅**
（§七 實測），不是驗不到。

#### 三、body 形狀不是契約

事實：`CONTRACT_ITEMS` 十五條只講狀態碼與 cookie 屬性，**零 body**；`platform/http-client`
與 `apps/console` 沒有任何地方讀 `error` 欄位；`total: 4` 是示範資料，`features/order`
只有 `total: number` 的型別。

裁：**mock 自己回的 body 是示範資料，不是契約。** 理由不是可維護性（C137 §一），是
D16 迭代軸：示範資料改一筆（訂單 4 → 5 筆）會讓一支標題講接縫的測試紅，而紅的訊息
與接縫無關 —— 一條斷言同時管兩個問題，紅了指不出是哪一個；C144 記的
「兩條規則管同一格」是同一種病的另一個方向。

⚠️ 射程只到 mock 自己回的 body。handler 自己回的東西（#1／#5／#6／#8）**是接縫的
interface 本身**，不在此列。

#### 四、裁決

1. **刪 #9**。守它的是 `403-forbidden`，牙齒在 `negative.test.ts`。
2. **#2 只留狀態碼**，刪 `toEqual({ error: "unauthenticated" })`。與 `401-unauthenticated`
   同口徑。
3. **#3 改成 `status 200` ＋ `not.toHaveProperty("hijacked")`**。只驗後者不夠：契約端點
   整個消失變 404 時 body 也沒有 `hijacked`，會假綠。
4. **#7 改差分**：同一條測試裡再起一個無 `routes` 的 mock，斷言兩邊 `/api/orders` 的
   body `toEqual`。它問的正是「fallthrough 給的東西與沒注入時一樣」；只驗 200 接不住
   「落到了另一個 200」（§七 有那個變異）。
5. **#10 的兩個字面留著**，與 #7 不同類：示範切片改權限碼名時 `MOCK_PERMISSIONS`
   **必須**跟著改，那個紅是對的紅。§三 的判準是「紅的訊息與接縫無關」，`total: 4`
   符合、`order:read` 不符合。所以 §三 落地是三處不是四處。
6. **#12 留**。「找不到變異」是強度論證，C137 §一 不准拿它刪測試；它問的問題
   （注入不動 cookie 名）確實是接縫的。
7. **檔頭加兩句**：預設不含 admin 由誰守、以及不釘 mock 自己的 body。少了前一句，
   下一次審查會再問一次為什麼 mock 自己不驗。
8. **不動門檻**：`vite.config.ts` 對 `bff-mock` 沒有逐檔覆寫，describe 回呼遠低於
   全域 199，`threshold-check` 沒有話說。

淨變化 −1 條 `it`；三條斷言換形狀。

#### 五、順手撞到、本則不裁

- **「示範切片宣告的權限碼 ⊆ `MOCK_PERMISSIONS`」全樹只有 #10 的字面在釘**，而它是
  靠字面、不是讀 `features/*/src/index.ts`。正確的絆線要讀切片（見記憶
  「絆線要掛在被守的對象上」），而 `platform/` 不能依賴 `features/`，所以它只能住在
  `apps/console/tests`（那裡已同時 import 兩邊）。那是加一條機械檢查，C154 §四 射程，
  本則不做、不開票。`server.ts:40` 的註解只講 admin，沒提示範切片，一併記在這裡。
- **`negative.test.ts` 的 `CONTRACT_ITEM_COUNT = 15` 是手抄的數字**，本則沒碰
  contract.test 所以沒撞到；它與 C173 §二 講的「人抄的數字」同類，記下不裁。

#### 六、與既有裁決的關係（C136 §八）

| 既有                 | 本則做了什麼                                                                               |
| -------------------- | ------------------------------------------------------------------------------------------ |
| **C168 §六 第 4 項** | **裁了**；§二 那一列「401／403／CSRF」更正成「只有 403」；「body 形狀」那個問句在 §三 答了 |
| **C168 §一**         | **遵守** —— 同一把判準（同時紅＋deletion test），閘門鏈不動                                |
| **C172**             | **同形** —— 第二次證明 C168 的盤點要逐條重盤；那則是「不可達」誤判，本則是「打誰」誤判     |
| **C137 §一**         | **遵守** —— 零閘門增減；#12 留下的理由就是它                                               |
| **C144**             | **引它** —— §三 「一條斷言管兩個問題」與它「兩條規則管一格」同病                           |
| **C154 §四**         | **不適用於刪與換形狀**（零新增機械檢查）；§五 那條絆線若做才要填，本則不做                 |
| **C173 §七**         | **引它** —— 本則編號在指派時定（對方 session 先取 C173），C173 落地後才寫它的字面          |
| **C173 §五**         | **續集**（§七）—— 射程從「決策日誌裡」改寬成「版控裡任何一個檔」；三態表判定不變           |
| **D8**               | **遵守** —— 401／CSRF 閘門在注入之前這件事本則沒動，只是證明它有測試在守                   |

#### 七、實測（基準 `9a8c9a4`，worktree `bff-mock-seam-only`）

- 短路 (a)：注入路由的比對搬到 session 閘門前 → routes.test **#2、#3 紅**、其餘 10 綠；
  contract.test **15 條全綠**。這就是「401 不是重複」的證據。
- 短路 (b)：`admin` 塞進 `MOCK_PERMISSIONS` → 刪 #9 前兩邊各紅一條（routes #9、contract
  `403-forbidden`）；刪 #9 後 routes 11 綠、contract **仍紅那一條**。
- #7 差分兩個方向：fallthrough 落到另一個 200（`{ total: 0, items: [] }`）→ **紅**；
  `DEMO_ORDERS` 4 → 5 筆 → **綠**（舊版 `total: 4` 會紅）。
- 逐檔 `it` 數（同 C172 §七 的量法）：**1565 → 1564**，83 支檔案只有 routes.test
  變：12 → 11。⚠️ after 那趟 `vp run -r test` **RC=1**，紅的是 `decision-ids`：
  routes.test 已寫 `C174` 字面而本則還沒接上檔案 —— C173 §五 三態表的第一格，
  本則是它的第三個獨立實例。接上就綠。
  ⚠️ **而這一次比前兩次寬，本則同時把 C173 §五 的射程改寬**：那一節寫的是
  「在決策日誌裡描述一個尚未合併的編號」，前兩個實例也都發生在 `DECISIONS-2.md`
  內部；本則的實例發生在 `platform/bff-mock/tests/routes.test.ts` —— **射程是版控裡
  任何一個檔**（`decision-ids.test.ts:36` 掃 `git ls-files -z`，只扣 `reports/`）。
  實務上的形狀是「測試先寫、裁決後接」，比寫散文常見得多。依 C136 §八：C173 §五
  產出的是那張三態表本身，三格判定（紅／綠／綠）一格不變，變的只有「哪些檔會踩到」，
  不重跑任何東西；C173 不改（C138 凍結不搬遷），續集記在這裡。指出這件事的是
  對方 session，不是任何機制。
- `vp check` 三個 warning 都在 `platform/ui`／`platform/pii`／`tools/spec-report`，
  `main` 上就有，與本則無關。
- `vpr ready` 第一趟 **RC=1**：oxfmt 對本則自己的表格；`vp fmt` 後 hunk 仍只有本則
  這一段（`git diff -U0` 單一 hunk、零刪除），第二趟 RC=0。

### C175 — `@org/ui` 的 `index.ts` 匯出完整性：C168 說兩份，實測一份是另一份的嚴格子集，刪子集、留唯一的那條（2026-09-05）

⚠️ **量測基準 `52fb30e`。** 本則的行號與計數凍結在那一支。

#### 一、問題

C168 §六 第 5 項：「`platform/ui/tests/styles.test.ts:315-329` 刪，:331-337 留。十行。」
§二 那一列寫的是「`index.ts` 匯出完整性：styles.test:315 ↔ component-contract:69，
**兩邊檔頭都沒提對方**」。開工前重盤 —— 這一項 C168 盤對了，行號在 `52fb30e` 上
仍然對得上（`describe("公開契約")` :314、要刪的那條 :315、要留的那條 :331）。
與 C172／C174 不同：那兩則各推翻了 C168 的一半，本則只證實。

#### 二、重盤：兩條斷言逐項比，一邊是另一邊的嚴格子集

| 問的事        | styles.test:315（刪）                       | component-contract ①（留）                                         |
| ------------- | ------------------------------------------- | ------------------------------------------------------------------ |
| 讀 `index.ts` | 原文，**不去註解**                          | `componentExports()` 先 `stripComments`                            |
| 匯出名與檔名  | 兩個 `toContain` **分開比**，不要求在同一行 | 正則要求 `export { default as X } from "./components/X.vue";` 成對 |
| 元件清單      | `readdirSync` 濾 `.vue`                     | 同                                                                 |
| 目錄空掉      | `length > 0`                                | 「★ 至少掃到兩個元件」`>= 2`                                       |
| 反向測試      | 無                                          | 「🔴 每一條都要抓得到違規」①×2                                     |

每一列右邊都不弱於左邊，而有三列嚴格更嚴。找不到任何只讓左邊紅的變異；
反過來「兩行 export 的檔名對調」只有右邊紅（§六）。deletion test 判**消失**：真重複。

`:331-337`「不轉出 reka-ui、不 `export *`」則全樹只有它在守：`tools/conformance` 的
CSP 規則要有 named import 子句才認得 Splitter，`export *` 沒有子句；`api-surface`
遇到 `export *` 是報「無法把 export 名字對回來源檔」的錯，不是這條禁令。README
〈禁止事項〉表那列指的正是它。**留。**

#### 三、裁決

1. **刪 styles.test:315-329 那一條 `it`**；`describe("公開契約")` 留一條。不拆 describe、
   不把留下的那條升到頂層 —— 那是整理，C137 §一 不准拿可維護性當理由。
2. **留下的那條留在原地不搬。** README〈禁止事項〉表明寫 `tests/styles.test.ts`，
   搬了要改文件；搬遷的唯一論證是「與檔頭主題一致」，同上不准。
3. **styles.test 的 `describe` 上方加一段**：匯出完整性由 component-contract ① 守、
   ① 比這裡曾經的那條嚴在哪、這裡只剩 `index.ts` 自己的禁止事項。少了它，下一次
   審查看到 styles.test 只守「不轉出」會再問一次「匯出完整性誰守」（C174 §四 第 7 項
   同一個理由）。
4. **component-contract 不動。** 它自帶反向測試與「至少兩個」的保險，唯一性由第 3 項
   單向指過去；雙向互指是兩份要同步的散文。
5. **不動門檻**：`vite.config.ts` 對 `platform/ui` 沒有逐檔覆寫；describe 回呼數只減不增。

淨變化 −1 條 `it`，零斷言換形狀，零閘門增減。

#### 四、順手撞到、本則不裁

- `platform/ui/README.md:114` 那列把 `tools/api-surface` 列為「不得 `export *`」的第二個
  守衛。查了：它是**報錯**（`shape.ts:1140`，無法枚舉），效果上會紅，但那是它的能力邊界
  不是它的規則。文件沒錯，不改。
- `IMPORT_SPECIFIER_PATTERN` 認 `from`，所以切片裡 `export * from "reka-ui"` 會被
  design-system 那條抓；CSP 那條（Splitter）對沒有子句的 `export *` 是盲的。`platform/ui`
  自己由本則留下的那條守，切片由 design-system 守，沒有洞；記下是因為下一個人讀
  `csp.ts:47-49`「這條掃整個 repo」時會以為它連 `export *` 一起掃。

#### 五、與既有裁決的關係（C136 §八）

| 既有                 | 本則做了什麼                                                                           |
| -------------------- | -------------------------------------------------------------------------------------- |
| **C168 §六 第 5 項** | **裁了，照它的行號**；§二 那列「兩邊檔頭都沒提對方」由 §三 第 3 項補一邊               |
| **C168 §一**         | **遵守** —— 同一把判準（同時紅＋deletion test），閘門鏈不動                            |
| **C172／C174**       | **同系列第三則**；前兩則各推翻 C168 一半，本則證實 —— 重盤的結論不預設方向             |
| **C137 §一**         | **遵守** —— 零閘門增減；不拆 describe、不搬測試的理由就是它                            |
| **C154 §四**         | **不適用**（零新增機械檢查）                                                           |
| **C173 §五／§七**    | **引它** —— 測試檔先寫 `C175` 字面、本則接上前 `decision-ids` 紅，同一支 PR 內接上即綠 |
| **D15**              | **不動** —— `index.ts` 是唯一公開契約這件事本則沒碰，只是把守它的測試從兩份收成一份    |

#### 六、實測（基準 `52fb30e`，worktree `ui-index-export-once`）

- 變異 A「刪掉 `UiSkeleton` 那行 export」→ styles.test:315 **紅**、component-contract ①
  **紅**（`UiSkeleton`）。同時紅。
- 變異 B「`UiSkeleton` 與 `UiTabs` 兩行的檔名對調」→ **只有 component-contract 紅兩條**，
  styles.test 17 條全綠。這就是「嚴格子集」的證據。
- 變異 C「`src/components/` 清空」→ 刪之前 styles.test:315 紅（`> 0`）、component-contract
  「★ 至少掃到兩個元件」紅；刪之後後者仍紅。`> 0` 那條保險刪掉沒損失。
- 逐檔 `it` 數（同 C172 §七 的量法）：**1564 → 1563**，只有 styles.test 變：17 → 16。
- ⚠️ after 那趟 `decision-ids` 紅過一次（`C175` 字面先於本則），C173 §五 三態表第一格，
  接上就綠。

### C176 — `promise-check` 的 CLI 測試要不要走假閘門：C168 說「不 spawn 真的 `threshold-check`」，而那條路被閘門自己的接線擋死，樹早在兩處拒絕過；順手把 C170 留下那條測試的理由變成真的（2026-09-05）

⚠️ **量測基準 `b6b3bcc`。** 本則的行號與計數凍結在那一支；§三 的 spawn 數量在
`c4f7565` 上，兩支之間只差 `#275`（`exit-drill` 的重跑），不碰 `promise-check`。

#### 一、問題

C168 §六 第 6 項：「`promise-check` 的 CLI 測試走假閘門，不 spawn 真的
`threshold-check`。⚠️ C165 剛把那支測試從恆綠救出來，第三次換形狀，所以只列不推。」
§二 那一列寫的是「`threshold-check` 探針：一次 `vpr ready` 跑 8 次；樹自己在
`promise-check/tests/sandbox.test.ts:96` 拒絕過第三份」。

開工前重盤：**機制做不到、數字量錯、而拒絕它的論證樹上早就有兩處**。
與 C172／C174 同系列：C168 的盤點要逐項重盤，本則是第四則、第三則推翻。

#### 二、機制：假閘門進不了 `wire()`，而擋它的是這道閘門自己的規則

`tools/promise-check/src/check.ts` 的 `wire()` 是**雙向接線**：

- 規格「當」那一句點名的閘門必須在根 `package.json` 的 `scripts.gate` 裡，不然
  〈閘門沒有接上〉紅（`negative.test.ts:169` 守它）。假閘門不在鏈上。
- `BREAKAGES` 表裡每一條破壞手法都要有場景在用，不然〈孤兒接線〉紅
  （`negative.test.ts:129` 守它）。餵一份只有假閘門場景的規格，真規格的破壞手法
  全部變孤兒。

所以「走假閘門」只剩一條路：鬆 `wire()`，讓測試裡的閘門可以不在鏈上。那是
`AGENTS.md` 規則二 —— 改閘門換綠燈。**否決。**

⚠️ 樹自己已經兩次寫過同一個結論，C168 列它的時候沒有引：

- `negative.test.ts:43-45`：「拿一份假的去頂替省不掉：假的規格得指名某一道真的
  閘門，而它一旦跑起來，省下的就又回來了。」
- `sandbox.test.ts:96-100`：拒絕在沙盒測試裡再抄一份 `threshold-check` 差分 ——
  「`gate-thresholds.feature` 的兩個場景就是那個差分，每次 `vpr gate` 都跑」。

#### 三、數字：「8 次」量錯了，實測 22

量法：`NODE_OPTIONS=--import` 一支把 `process.argv` 追加到檔案的 `.mjs`，
每一個被 spawn 的 `node` 都會繼承它；數含 `threshold-check/src/cli.ts` 的行。
對照組 `conformance/src/cli.ts` 全程非零。

| 在哪裡                                                                                            | `threshold-check` 被起幾次    |
| ------------------------------------------------------------------------------------------------- | ----------------------------- |
| 測試 `promise-check/tests/cli.test.ts:92`                                                         | 4（探針**兩趟** ＋ 兩個場景） |
| 測試 `promise-check/tests/negative.test.ts`：真規格綠 4、承諾誤擋 2、指不到副本 2、紅在別的地方 4 | 12                            |
| 測試 `threshold-check/tests/threshold.test.ts:303`                                                | 1                             |
| 腿④ `vpr gate`：`threshold-check` 自己 ＋ `promise-check`                                         | 1 ＋ 4                        |
| **一次 `vpr ready`**                                                                              | **22**                        |

C168 的「8」是哪把尺量的，還原不出來（與 C170 §二 對「九處」的處境相同）。
最可能的來源是把探針算成一次 —— `cli.test.ts:87` 的註解也這樣寫，而
`probe.ts` 的 `probeRootSupport` 是兩趟 `runOnce`（指向空目錄一趟、指向 repo 一趟）。

⚠️ `negative.test.ts` 那 12 次**不是重複**：三條各自的變異不同（改對照組成必須綠
→〈承諾誤擋〉；改指名成看不見副本的閘門 →〈閘門指不到副本〉；改訊息片段 →
〈紅在別的地方〉），deletion test 各自不消失，而且是 C43 的反向測試。貴，是因為
`checkPromises` 每呼叫一次就重探一次，同一進程三次 —— 省它只能動 `check.ts`，
而探針在 CLI 裡本來就只呼叫一次，快取對閘門本身零效果。本則不動。

#### 四、順手：C170 留下 cli:92 的理由只對一半，把它變成真的

C170 §二 把 `promise-check/cli:92` 歸 C 類「另有第二條斷言（場景數）」而留下。
那條斷言是 `toContain("個場景各執行過一次")` —— 對「**0** 個場景各執行過一次」
也綠，而 0 正是「一條都沒跑」的樣子。真正守場景數的是 `negative.test.ts:94` 的
`runs.length >= 4`（進程內）；cli:92 獨有的只剩「成功那行有印出來」。

裁：**把那條斷言補強成比真的數字** —— 用 cli.test 已經 import 的 `parseSpec`，對
`git ls-files -- specs/*.feature`（與 `cli.ts:64` 同一個事實來源）算出場景總數 n，
斷言 `toContain(\`${n} 個場景各執行過一次\`)`。不抄字面：規格加一個場景的那天，
字面會安靜地少守一條（`tripwire-must-hang-on-its-target`）。

⚠️ 這**不是** C168 警告的「第四次換形狀」：檔頭、describe、`it` 數（4）都不動，
改的是一條 `it` 裡的一行斷言加一個 helper。spawn 數也不變（仍是 4）。

#### 五、裁決

1. **候選 6 的機制否決**，理由 §二；不開票、不留待辦。
2. **`cli.test.ts:92` 的第二條斷言補強成真 n**（§四）。
3. **`cli.test.ts:87` 的註解「三次／探針一次」改成「四次／探針兩趟」**，並指到 §三。
   `negative.test.ts:43` 的「每趟約三秒」是對的，不動。
4. **`negative.test.ts` 不動**（§三 末段）。`sandbox.test.ts:96` 不動。
5. **不動門檻、零閘門增減、`check.ts` 一個位元組不動。**

淨變化：0 條 `it`，一條斷言換形狀，一段註解更正。

#### 六、順手撞到、本則不裁

- **`vpr gate` 在 main 上有別的 session 的 worktree 時會紅**：`tools/pii-check` 用
  `gate-kit` 的 `walk()` 掃磁碟，`SKIP` 名單（`cli.ts:27`）沒有 `.claude`、也沒開
  `walk()` 的 `skipDotDirs`；`.gitignore:33` 排掉了 `.claude/worktrees/`，但 `walk`
  不讀 gitignore；`EXEMPT` 以完整路徑為鍵，`.claude/worktrees/<名字>/tools/pii-check/tests/detect.test.ts`
  對不上。症狀是 `pii-check` 報出另一棵樹的測試資料。`SKIP` 裡的 `.stryker-tmp`
  當年（C121）就是同一種形狀加進去的。這是 `pii-check`／`gate-kit` 的事，
  不是本系列的；量 §三 那張表時它擋到了我（腿④在 `pii-check` 就停了，改成逐支
  CLI 單獨量）。
- **C168 §二 那列的「8」與 `cli.test.ts:87` 的「三次」是同一個錯**，本則只改後者
  （C136 §八：不改舊裁決，寫關係）。

#### 七、與既有裁決的關係（C136 §八）

| 既有                 | 本則做了什麼                                                                                 |
| -------------------- | -------------------------------------------------------------------------------------------- |
| **C168 §六 第 6 項** | **否決機制、登記事實**；§二 那列「8 次」更正為 22，「拒絕過第三份」補上第二處（negative:43） |
| **C168 §一**         | **遵守** —— 同一把判準；`negative.test` 三條 deletion test 各自不消失，所以不是重複          |
| **C170 §二／§三**    | **更正它留 cli:92 的理由**（第二條斷言原本不守數字），結論不變：留                           |
| **C172／C174／C175** | **同系列第四則**；三則推翻、一則證實 —— 重盤的結論不預設方向                                 |
| **C165 §七**         | **遵守它的警告** —— 檔頭與形狀不動，只改一行斷言                                             |
| **C137 §一**         | **遵守** —— 零閘門增減；§三 末段「快取探針」明文不做                                         |
| **C43**              | **不動** —— `negative.test` 一條不刪                                                         |
| **C154 §四**         | **不適用**（零新增機械檢查）                                                                 |
| **規則二**           | **它擋掉了候選 6** —— 「讓假閘門進 `wire()`」就是改閘門換綠燈                                |

#### 八、實測（基準 `b6b3bcc`，worktree `promise-cli-scenario-count`）

| 變異                                      | 舊斷言（HEAD） | 新斷言 | `negative.test:94` |
| ----------------------------------------- | -------------- | ------ | ------------------ |
| M1：`cli.ts:94` 印 `0 個場景各執行過一次` | **綠**         | 紅     | 綠（不讀 stdout）  |
| M2：`check.ts:308` 拿掉 `runs.push`       | **綠**         | 紅     | 紅                 |

M1 那一格是本則的證據：舊斷言對「一條都沒跑」全綠。今天的 n 是 6
（`promise-1` 4 ＋ `gate-thresholds` 2），CLI 印「2 條承諾、6 個場景各執行過一次」。

- `it` 數 4 → 4（`grep -c '^\s*it('`）。
- ⚠️ 新 worktree 第一趟 cli.test 紅的是 C165 §七 那條前提（`features/invoice` 沒有
  `.vitest-results.json`），檔案自己說那不是缺陷；跑一次完整指令產出結果檔即綠。

### C177 — C168 §六 第 7 項的三件零散：兩件是閘門已守的 `toContain`，一件是差分的推論，而「四條」裡最深的那條不是 C168 說要留的那條（2026-09-05）

⚠️ **量測基準 `14f7e18`。** 本則的行號與計數凍結在那一支。

#### 一、問題，與一次行號漂移

C168 §六 第 7 項列了三件「零散」：`promise-check/tests/sandbox.test.ts:128-134`
（檔案自己在 :136-139 承認）、`sbom-negative.test.ts:194-198`（`gate-roster` variant
已守）、「lockfile 拆兩份文件那四條只留 `--split-lockfile` adapter 那一條」。§二 表裡
沒有它們的來源列，三件都要從檔案本身重盤。

⚠️ **C168 的行號凍在 `390f585`，PR 2 把 `sbom-negative.test.ts` 縮了十行。** 所以：

| C168 寫的               | 在 `390f585` 上是                                                   | 在 `14f7e18` 上 |
| ----------------------- | ------------------------------------------------------------------- | --------------- |
| `sandbox.test:128-134`  | 「副本裡有 platform/ui 的元件與代幣，而 theme-verify 指得到」       | :133-139        |
| `sbom-negative:194-198` | 「🔴 `--verify-sbom` 那一步必須留著」`toContain("--verify-sbom …")` | :184-187        |

**C170 §三 第 4 項寫的「`sbom-negative:197` 歸 §六 第 7 項」是漂移後的讀法**：C170 拿
post-PR2 的 :197（污染絆線 `describe("repo 本身沒有被動到")`）去對 C168 pre-PR2 的
:194-198。污染絆線 C170 自己判 D 類「不動」，本則不碰它；這一句寫在這裡，C170 不改
（C136 §八）。

#### 二、重盤：三件各是什麼

**(b) `sbom-negative:184` 與 :179 的第一句 —— `gate-roster` 守得比它嚴。**
`gate-roster/src/check.ts` ⑤ 把 `GATES` 的 command ∪ variants 與 tier2 workflow 的
`run:` 行做**精確集合比對**，`--verify-sbom sbom.cdx.json` 與 `--split-lockfile .scan`
都登記在 variants（`gates.ts:274,280`）。測試那兩句 `toContain` 更弱：把 `run:` 那行
**註解掉**，字串還在檔案裡，`toContain` 照樣綠、`gate-roster` 紅（§六 b2）。判準同
C170 §三 第 1 項的 A 類：只斷言指令在、`vpr gate` 就是守衛，deletion test 判消失。
:179 的第二句 `scan-ref: .scan` 是 action 的 `with:` 參數，名冊的 `RUN_LINE` 看不到，
只有它在守 —— **留**。

**(a) `sandbox.test:133` —— 一半是推論、一半是唯一。** `componentCount > 0` 是 :145
差分的推論（after ≥ 0 ⇒ before ≥ 1；抓不到那行時兩邊都 −1，差分照樣紅）。
`status === 0` 全檔只有它在斷言：讓 theme-verify 在 `--root` 下印完數字後 `exit(1)`，
只有它紅、差分綠（§六 a）。而 `--root <副本>` 的 theme-verify 沒有任何 spec 指名、
`promise-check` 不 spawn 它 —— 那個 status 沒有消費者，但「CLI 在副本上紅著而測試
不看」是 C43 那一族的洞。

**(c) 「真的 `pnpm-lock.yaml` 是兩份文件」被四條釘著，而 C168 說要留的不是最深的那條。**

| 條                  | 斷言                                                    | 只讓它紅的變異                             |
| ------------------- | ------------------------------------------------------- | ------------------------------------------ |
| `lockfile.test:165` | `splitDocuments` 恰好兩份、10 倍、`merged − whole < 10` | 無，但它是**唯一守守恆的**                 |
| `lockfile.test:220` | `parseLockfile().documents === 2`                       | 無：都與 fixture :81 或 :165 同紅          |
| `lockfile.test:230` | `packages.length > 400`                                 | 無：與 :165／:81 同紅                      |
| `sbom-negative:128` | 走 CLI 讀回 doc1／doc2：`< 50`、`> 400`、`inBoth` 段    | **有**：CLI 把同一份寫進兩個目錄（§六 c3） |

`inBoth` 那段（`first + second − LOCK_PACKAGES`）與 :165 的 `merged − whole` 是同一個
算式讀兩次。sbom:128 唯一的那個變異是 **adapter 層**的（`cli.ts:1288` 寫檔），
不是形狀的。C168 說「只留 `--split-lockfile` adapter 那一條」—— 讀成留 sbom:128
會丟掉守恆；讀成「adapter 那條只驗 adapter 的事」才對得上變異表。與 C172／C174
同形：C168 的處置方向要重盤。

#### 三、裁決

1. **刪 `sbom-negative:184-187`**；**`:179` 刪第一句**、留 `scan-ref: .scan`，標題改成
   講 Trivy 掃哪裡。上方加一段：兩行 `run:` 在不在由 `gate-roster` variants 守、為什麼
   `toContain` 比它弱、`scan-ref` 為什麼留。
2. **刪 `sandbox.test:133`**；`expect(before.status, before.output).toBe(0)` 一句搬進
   :145。`status` 那半 deletion test 判搬家，所以搬不刪；不多一條 `it`。:141 那段註解
   重寫 —— 它講的「上面那條」已不存在。
3. **`lockfile.test:220`、`:230` 刪**；describe 上方加一段說形狀由 `splitDocuments`
   那組最後一條守、守恆抓得到什麼。
4. **`sbom-negative:128` 換形狀**：只驗 `first !== second` 與 `parse(first) < parse(second)`
   —— doc1、doc2 是不同的兩份、小的在前。刪 `< 50`／`> 400`／`inBoth` 段。標題跟著改。
5. **`lockfile.test:165` 不動**。它是四條裡唯一守守恆的。
6. **污染絆線 `sbom-negative:194`（改後 :190）不動** —— C170 D 類；本則 §一 只更正它被
   誤歸到這裡。
7. **不動門檻**：三個 package 在 `vite.config.ts` 都沒有逐檔覆寫；describe 回呼數只減
   不增。

淨變化 **−4 條 `it`**（sandbox −1、lockfile −2、sbom −1），兩條換形狀，零閘門增減。

#### 四、順手撞到、本則不裁

- `vp run -r test -- --reporter=json` 那一趟 doc-facts 紅（`C177` 字面先於本則）之後，
  `tools/supply-chain/.vitest-results.json` 的 mtime 是新的、**內容是舊的**（lockfile
  16 條而 log 說 14）。像是任務鏈某一支紅了之後 `vp run` 把快取的產物寫回去。逐檔
  `it` 數本則改成對兩個 package 直接 `vitest run` 取得；成因未查、不開票。
- rtk 的 `diff` 對兩個確實不同的檔又回報 `[ok] Files are identical`（第二次），`cmp`
  抓到。已記在記憶。

#### 五、與既有裁決的關係（C136 §八）

| 既有                 | 本則做了什麼                                                                                       |
| -------------------- | -------------------------------------------------------------------------------------------------- |
| **C168 §六 第 7 項** | **裁了三件**；(c) 的處置方向更正（留守恆那條，adapter 那條只驗 adapter）；§六 至此 1 與 7 之外全裁 |
| **C168 §一**         | **遵守** —— 同一把判準（同時紅＋deletion test），閘門鏈不動                                        |
| **C170 §三 第 1 項** | **引它** —— (b) 是它 A 類的第四、五處，行號在 PR 2 之前                                            |
| **C170 §三 第 4 項** | **更正** —— 「`sbom-negative:197` 歸 §六 第 7 項」是行號漂移；D 類那條本則不動                     |
| **C172／C174／C175** | **同系列第五則**；(c) 推翻 C168 半個處置，(a)(b) 證實                                              |
| **C137 §一**         | **遵守** —— 零閘門增減；`status` 那句搬不刪的理由是 deletion test 不是可維護性                     |
| **C43**              | **引它** —— (a) 留 `status` 的理由                                                                 |
| **C154 §四**         | **不適用**（零新增機械檢查）                                                                       |
| **C173 §五**         | **引它** —— 三支測試檔先寫 `C177` 字面、本則接上前 `decision-ids` 紅，接上即綠（第四個實例）       |

#### 六、實測（基準 `14f7e18`，worktree `scattered-c168-seven`）

改前（證明「同時紅」與「更弱」）：

- **b1** 刪掉 workflow :403 那行 `run:` → sbom:184 紅、`gate-roster` 紅「workflow 少一道」。
- **b2** 把同一行**註解掉** → sbom **15 條全綠**、`gate-roster` 紅。這就是「更弱」的證據。
- **a** theme-verify 在 `--root` 下印完後 `exit(1)` → 只有 sandbox:133 紅（1 failed | 8）。
- **c1** `splitDocuments` 只回第一份 → 五條同紅（lockfile :134／:143／:165、sbom :115／:128）。
- **c2** `parseLockfile` 略過第二份的 `packages:` → 十條同紅，含 fixture :81、:165、:230、
  sbom :84／:128；⚠️ **:220 綠**（`documents` 計數器沒壞）—— 它守的只有「計數器」，而那由 :81 守。
- **c3** CLI 把最後一份寫進每個目錄 → **只有 sbom:128 紅**。

改後（證明留下的接得住）：

- **c3** → 只有換形狀後的 sbom:128 紅（1 failed | 27）。**c1** → 五條同紅（:165 與 sbom
  新那條都在）。**c2** → 八條紅，:165 在。**a** → :145 紅（`before.status`）。
- 逐檔 `it` 數（量法同 C172 §七，但見 §四 第一項）：**1563 → 1559**，83 支只有三支變：
  sandbox 9 → 8、lockfile 16 → 14、sbom-negative 15 → 14。
- ⚠️ after 那趟 `decision-ids` 紅過一次（`C177` 字面先於本則），C173 §五 三態表第一格。

### C178 — C168 §六 第 1 項「旗標 interface 只守一次」：名冊改吃磁碟不吃 `GATES`，variant 不是洞、三個 parser 只收一個，而三條「缺值 → 紅」守的是 `parseFlags` 後面到不了的第二道檢查（2026-09-05）

⚠️ **量測基準 `0a9345d`（C177 合併後的 `main`）。** 本則的行號與計數凍結在那一支；
改後的行號另外標「改後」。

#### 一、問題：C168 那一項有三句話，重盤後三句各對一半

C168 §六 第 1 項：「旗標 interface 只守一次，adoption 的名冊改吃 `gate-roster` 的
`GATES ∪ variants`（順帶收掉 `scripts.gate` 的三個 parser，關掉看不見四支 CLI 與
四條 variant 的洞）。」§二 那列指名的重複是 `gate-kit` flags＋adoption ↔
`pii-check`:184 · `compliance`:230 · `sbom-negative`:119,161（`390f585` 的行號）。

| C168 說                      | `0a9345d` 上的事實                                                                                                                                                                                                                                                                    |
| ---------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| adoption 只讀 `scripts.gate` | C171 §九 之後讀三處（`gate`＋`ready`＋根 `vite.config.ts`），**15 支**；磁碟上 `tools/*/src/cli.ts` **17 支**，看不見的是 `csp-verify`、`ui-survey` —— 兩支檔頭自己寫著「這幾行沒有東西在守」。「四支」在 `390f585` 上也數不出來：那時 16 見 14，差的也是這兩支                       |
| 四條 variant 是洞            | 四條 variant 的旗標各由該工具自己的行為測試守**接受**方向（`--evidence` evidence.test:144、`--split-lockfile`／`--verify-sbom` sbom-negative），**只有 `--require-fresh` 本機零測試**。而拿掉它 tier2 會大聲紅 —— 不安靜，D16 迭代軸 ② 不成立，不是絆線該補的洞                       |
| 收掉三個 parser              | `gate-roster` ③ 是產生器＋逐字等於，是名冊的真值來源；`promise-check` 是 `chain.includes(cli)`，問的是「`vpr gate` 真的會執行它」—— 與 GATES 成員資格是**不同問題**（`spec-report` 在 GATES 但 `notInGateScript`）。C168 §七 自己那列寫的就是「三個問句」。能收的只有 adoption 那一個 |

#### 二、名冊：磁碟上每一支，不是執行路徑，也不是 `GATES`

adoption 檔頭的主張是「這條線上**每一支** CLI」。從執行路徑推導名冊，每多一種接線
形狀就要多讀一處（C171 為 `release-distance` 加了第三處），而 `UNGATED` 裡帶 CLI 的
兩支永遠不在任何執行路徑上 —— 那個問法**結構上**答不出「每一支」。

C168 的原案是 import `gate-roster` 的 `GATES ∪ UNGATED`。沒取：`gate-roster` 已相依
`gate-kit`（`parseFlags`），反向再加一條是 workspace 循環；跨 package 相對路徑 C168 §三
自己說會讓 conformance 紅。而 `gate-roster` ① 已守「`tools/*` 每一個目錄都登記在
`GATES ∪ UNGATED`」，所以**磁碟清單與名冊等價**，讀磁碟不用付那條相依。

**裁決：名冊＝ `readdirSync("tools")` 底下每一個有 `src/cli.ts` 的目錄。** 17 支。
錨點從 `spec-report`／`release-distance`（兩個「執行路徑多讀一處」的具名錨）換成
`csp-verify`（先前看不見的那一支）；名冊改回執行路徑時它紅（§六 M9）。

⚠️ 射程從 15 加到 17，是既有絆線加兩列，照 C154 §三 填兩軸：
**交付軸** —— 無。**迭代軸** —— ① 對象在外：它報的是 `csp-verify`／`ui-survey` 壞了，
不是它自己；② 壞法安靜：`0a9345d` 上把 `csp-verify` 的 `parseFlags` 繞掉，
`csp-verify --nope` 回 0、adoption 16/16 綠（§六 M10 改前那趟）。兩條都成立。

#### 三、裁決：十條逐工具的旗標測試，刪十、adoption 多一句

判準同 C168 §一（同一個變異兩邊同紅＋deletion test），每一格 §六 有實測。

| 測試（`0a9345d` 行號）                                                 | 變異                     | 處置                                                                                                                                                                          |
| ---------------------------------------------------------------------- | ------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `pii-check/roster.test`:172 `--masking` → 紅、:178 任何旗標 → 紅       | M1 與 adoption 那列同紅  | **刪**；C52 的事故敘述留在原處的區塊註解，指向 adoption                                                                                                                       |
| `pii-check`:183 訊息要說得出為什麼                                     | M2 **只有它紅**          | **搬進 adoption 的 `it.each`**（多一條 `expect`，零新 `it`）再刪 —— 「原樣轉出 `parseFlags` 的訊息」是每一支的事，不是 `pii-check` 的事；留在一支裡等於按對稱每一支都該有一條 |
| `pii-check`:188 對照組：認得的旗標照常運作                             | M8 同檔另三條同紅        | **刪**；「什麼旗標都紅」由本檔其餘走 `--root` 的測試接住                                                                                                                      |
| `release-distance/distance.test`:59 不認得的旗標是唯一的非零           | M3 與 adoption 那列同紅  | **刪**；describe 標題「除了旗標」那一半留一行註解                                                                                                                             |
| `api-surface/negative.test`:639 `--baseline`、:643 `--platform` 缺值   | M5 與 flags.test:92 同紅 | **刪**；這支讀 `FLAGS.flags`，沒有第二條路（M6 改 kind 是 beforeAll 炸，RC=1）                                                                                                |
| `compliance/negative.test`:216 `--file` 缺值                           | M5 之下**仍綠**          | **刪**（見 §五：它守的是 `parseFile` 裡到不了的第二道檢查）                                                                                                                   |
| `sbom-negative.test`:109 `--verify-sbom`、:147 `--split-lockfile` 缺值 | M5 之下**仍綠**          | 同上，**刪**                                                                                                                                                                  |

淨變化 **−10 條 `it`，adoption 名冊 +2 列**（`csp-verify`、`ui-survey`），零閘門增減
（C137 §一），門檻不動，`vpr gate` 那條鏈一支不碰。`csp-verify`／`ui-survey` 檔頭那段
「看不見它」改寫成「從 C178 起看得見」。

#### 四、variant 與三個 parser：都不動

- **variant 不進名冊。** 未知旗標的判定不隨 variant 變；接受方向是各工具行為測試的事
  （§一 表第二列）。`--require-fresh` 本機零測試記在 §五，不開票。
- **`gate-roster` ③ 與 `promise-check` 不動。** 兩支都是閘門的規則（C137 §一），而且
  問的是不同的問題；把 `promise-check` 改查 GATES 會把「`vpr gate` 會執行它」換成
  「名冊上有它」—— 語意變弱。C168「收掉三個」重盤後是一個。

#### 五、順手撞到、本則不裁

- **五支 CLI 把 `parseFlags` 當門房，之後仍手讀 argv，`FLAGS.flags` 一次都沒用**：
  `compliance`（`indexOf("--file")`＋兩個 `includes`）、`exit-drill`（`--full`、
  `--require-fresh`）、`spec-report`（`--results`）、`supply-chain`（十個）、`ui-survey`
  （`--csp`、`--sca`）。`conformance` 用了 `FLAGS.flags` 但也有一處 `indexOf("--root")`。
  這是 C126 接線的形狀：`parseFlags` 擋在前面，舊的手讀段沒拆。後果有兩個，本則只裁
  第一個的測試：(a) `compliance`／`supply-chain` 手讀段裡各有一道「缺值 → 紅」，
  `parseFlags` 先跑所以它到不了，而 §三 那三條測試守的正是它（M5 之下仍綠的原因）；
  (b) 把 `--file` 從 `value` 改 `boolean` 零紅（M4／M7）—— 手讀段照樣讀到值，spec 的
  kind 對這五支是裝飾。**這是產品碼的事，射程是五支閘門，不是「測試邊界」這一項的**；
  拆一半在這裡修是 C118 那種做完的子集＋沒有追蹤處的餘數。不開票，記在這裡。
- `exit-drill --require-fresh` 本機零測試，只有 tier2 在跑；拿掉它 CI 會紅、不安靜，
  所以不是絆線的事（§一）。
- ⚠️⚠️ **`tools/release-distance/tests/distance.test.ts` 十四條，沒有任何東西在跑它。**
  那個 package 沒有 `test` script、沒有 `vite.config.ts` task，`vp run -r test` 對它
  回「Task "test" not found」—— 本機 `vpr ready` 與 CI 的 `vp run -r test` 都不跑，
  逐檔 `it` 數（§六）83 支裡也沒有它。C171（#270，今天）落地時就是這樣。這是
  `spec-completion-rate-design` 記的第四態「未執行」：與全綠長得一樣。**本則不接**：
  接上去不是一行的事 —— 它第一條「真樹上是 0 而且真的量到了」要有 tag，而 CI 是淺
  checkout（C171 §九 自己寫的），接上就紅。要另裁。本則在那支檔刪的那一條（§三），
  因此改前改後都沒有被執行過；`it` 數是直接 `npx vitest run` 量的，14 → 13。
- ⚠️ 本則量測時踩了一次：變異腳本用 `git checkout -- <檔>` 還原，而其中一個變異
  改的是我**還沒 commit** 的 adoption 改寫 —— 還原把改寫一起還原了。改動一寫完就
  先 commit WIP 再量，`worktree-reset-ate-uncommitted-work` 的同一條。

#### 六、實測（基準 `0a9345d`，worktree `flags-interface-once`）

六個 package 七支測試檔，逐條記狀態（`--reporter=json`），改前 163 條、改後 155 條。

**改前**（決定 §三 每一格的證據）：

| 變異                                          | adoption     | flags.test | 逐工具                                                               |
| --------------------------------------------- | ------------ | ---------- | -------------------------------------------------------------------- |
| M1 `pii-check` 在 CLI 層濾掉未知旗標          | pii 列紅     | —          | :172、:178、:183 三條同紅                                            |
| M2 `pii-check` 印自己的訊息（沒「綠燈」那句） | **綠**       | —          | **只有** :183 紅                                                     |
| M3 `release-distance` 濾掉未知旗標            | rd 列紅      | —          | :59 紅                                                               |
| M4 `compliance --file` 改 `boolean`           | —            | —          | **零紅**                                                             |
| M5 `parseFlags` 缺值不紅                      | —            | :92 紅     | api-surface :639／:643 紅；**compliance :216、sbom :109／:147 仍綠** |
| M6 `api-surface --platform` 改 `boolean`      | —            | —          | 整檔 beforeAll 炸，RC=1，非 :643                                     |
| M7 `supply-chain --verify-sbom` 改 `boolean`  | —            | —          | **零紅**                                                             |
| M8 `pii-check` 拒絕所有旗標                   | —            | —          | :188 對照組紅，同檔另三條也紅                                        |
| M10 `csp-verify` 濾掉所有旗標                 | **16/16 綠** | —          | `csp-verify --nope` RC=0 —— 這就是 §二 說的洞                        |

**改後**（短路，每一趟還原後 `git status` 乾淨）：M1 → adoption pii 列紅。**M2 → adoption
pii 列紅**（新那句 `expect` 接住了）。M3 → adoption rd 列紅。M5 → flags.test:92 紅。
M8 → pii-check 同檔三條紅（對照組刪掉沒損失）。**M9「名冊改回執行路徑」** → 錨點紅、
`csp-verify`／`ui-survey` 兩列消失（155 → 153）。**M10 → adoption csp-verify 列紅。**

- 逐檔 `it` 數（量法同 C172 §七，⚠️ 見 C177 §四 第一項）：**1559 → 1552**，83 支只有
  五支變：api-surface 72 → 70、compliance 14 → 13、adoption 16 → 18、pii-check 22 → 18、
  sbom-negative 14 → 12。合計 −10 ＋ 2 ＝ −8，與 §三 對得上。`release-distance` 不在
  那 83 支裡（§五 第二項），另量：14 → 13。
- ⚠️ after 那趟 `decision-ids` 紅過一次（`C178` 字面先於本則），C173 §五 三態表第一格，
  接上就綠。

#### 七、與既有裁決的關係（C136 §八）

| 既有                 | 本則做了什麼                                                                                                                                                          |
| -------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **C168 §六 第 1 項** | **裁了，三句各改一半**：名冊改吃磁碟不吃 `GATES`；variant 不是洞；三個 parser 收一個。§二 那列四處測試全刪，外加 C168 沒列的 api-surface 兩條與 release-distance 一條 |
| **C168 §一／§七**    | **遵守**同一把判準；§七「三個問句」那列是 §四 不動兩支閘門的理由                                                                                                      |
| **C126**             | **接上它沒接的那一半**：「八支」如今是 17 支，而只有 15 支在絆線裡；C126 §五「一條只守四支的絆線與沒有絆線長得一樣」在 15 對 17 上又應驗一次                          |
| **C125 §五**         | **不動**，名冊仍是推導的                                                                                                                                              |
| **C171 §九**         | **部分過期，本則更正**：它為 `release-distance` 在 adoption 加的第三處事實來源與具名錨，隨名冊改吃磁碟一起拿掉；C171 的理由（task 不是 script）不受影響               |
| **C137 §一**         | **遵守** —— 零閘門增減；§四 不動兩支閘門的 parser                                                                                                                     |
| **C43**              | **不動** —— 每道閘門的反向測試留在原處，刪的十條沒有一條是某道閘門唯一的反向測試                                                                                      |
| **C154 §三／§四**    | **填了兩軸**（§二）—— 既有絆線加兩列，對象在外、壞法安靜                                                                                                              |
| **C118**             | **引它** —— §五 不拆一半修五支手讀 argv 的理由                                                                                                                        |
| **C173 §五**         | **引它** —— `C178` 字面先於本則那一趟紅                                                                                                                               |
| **C177**             | **同系列第七則**（C170／C172／C174／C175／C176／C177 之後）；C168 §六 七項至此**全部裁完**                                                                            |

### C179 — 一支在樹上活了一整天、十四條一次都沒跑過的測試檔：接成 task 不接成 script，對照組改斷言「與 git 的說法一致」而不是定值，並在 gate-kit 加第二條絆線（2026-09-05）

⚠️ **量測基準：`e76e8dc`（C178）**，加本支。本則收掉 **C178 §五** 順手撞到的第二件。

#### 一、事實：它不是「跑不過」，是「沒有東西在跑它」

| 事實                                                | 量法                                                                             | 值                                              |
| --------------------------------------------------- | -------------------------------------------------------------------------------- | ----------------------------------------------- |
| `tools/` 有 `tests/*.test.ts` 的 package            | 逐目錄數                                                                         | 18                                              |
| 其中沒有 `scripts.test`、也沒有 `run.tasks.test` 的 | 逐 `package.json`／`vite.config.ts` 讀                                           | **1**（release-distance）                       |
| 那支檔案的條數                                      | `npx vitest run`                                                                 | 13（C178 從 14 刪成 13）                        |
| 直接跑的結果                                        | 同上                                                                             | 13 綠，RC=0                                     |
| 十三條裡讀真樹 tag 的                               | 逐條讀                                                                           | **1**（`★ 真樹上是 0，而且真的量到了`）         |
| 那一條在 CI 形狀的樹上                              | `git clone --depth 1 --no-tags` 這棵樹，`pnpm install --offline`，跑 main 的檔案 | **1/13 紅**（tag 0 個；對照組完整 clone 42 個） |
| `vp run test` 這個 script 會不會被快取              | gate-roster 跑兩趟                                                               | 第二趟「cache hit, 765ms saved」                |

⚠️ 第二列是 **N=1** 的形狀：沒有任何東西守「有測試檔 ⇒ 有東西在跑它」。`harness-tripwire`
走訪過這個檔（C171 §八 說「絆線是綠的」），但它問的是「用不用 harness」，不是「有沒有在跑」。
`vp run -r test` 對沒有 `test` 的 package **靜默跳過**，不印一行。

⚠️⚠️ **一份從來沒跑過的測試，與一份全綠的測試，在樹上長得一模一樣。** C171 §七 拿「那條載重
測試」取代 `.feature`，而那根柱子從 C171 到 C178 是空的 —— 論證本身沒錯，證據在今天之前不存在。

#### 二、對照組：不斷言定值，斷言兩個讀數相等

原本那條斷言 `report(readGit(repoRoot())).kind === "measured"`，在淺 checkout 上必紅（§一）。四個選項：

| 選項                                    | 出局理由                                                                                                                    |
| --------------------------------------- | --------------------------------------------------------------------------------------------------------------------------- |
| `it.skipIf(真樹量不到)`                 | CI 上它變「未執行」，而 C114 §二 記的正是「未執行與全綠長一樣」；這條存在的理由就是抓「綠是因為沒量到」                     |
| tier1 加 `fetch-depth: 0`               | C171 §七 明文不裁、`#267` 射程外；每趟變慢，而且要先論證                                                                    |
| 拆兩條：結束碼 0 兩邊跑、量到與否留本機 | 是 skipIf 換皮                                                                                                              |
| **雙向一致性**（採用）                  | 測試自己問 `git tag --merged HEAD`：看得到 ⇒ report 必須 `measured`；看不到 ⇒ 必須 `unmeasurable` 且 `why` 含 `fetch-depth` |

紅只在「git 的說法與 report 的說法不一致」時。⚠️ 兩個讀數走的是**不同的 git 命令**（`tag --merged`
vs `describe --tags`），不是同一個查詢問兩次。

**附帶效果**：CI 的淺 checkout 成了「看不到 tag」那條分支**唯一在真樹上跑到**的地方 —— 沙盒那條
「剛 git init、一個 tag 都沒有」是空樹不是淺樹。

#### 三、`test` 是 task 不是 script —— C171 §九 為 CLI 付過的錢，測試也要付

`"test": "vp test"` 跟其他十二支一樣最省事，而 §一 最後一列實測它會被快取。這條對照組的輸入是
git 的 ref，追蹤看不見，第一趟之後永遠重播 —— 本機刪光 tag 它照樣綠。所以 `tools/release-distance/vite.config.ts`
定 `run.tasks.test`、`cache: false`（先例 `tools/slice-gen/vite.config.ts`）。

實測：task 第二趟真的重跑（印 Duration，不印 cache hit）。代價：每趟 `vpr ready`／CI 多約 1.3 秒。
⚠️ 根 `vite.config.ts` 沒有 vitest 的 `test` 區塊，所以 package 層加設定檔**不會**遮掉繼承（那個坑在 C120 記過）。

#### 四、第二條絆線：有測試檔的 package，必須有東西在跑它

掛在 `tools/gate-kit/tests/harness-tripwire.test.ts` —— 它已經在走訪 `tools/*/tests/*.test.ts`，
第一條絆線看得到那個檔，第二條問它「接了沒」。判定：`package.json` 的 `scripts.test` 是字串，
**或** 同目錄 `vite.config.ts` 匹配 `tasks: { … test: { command:`。

⚠️ 那個 regex 要求 `tasks:` 在前、`command:` 緊接 —— 光找 `test: {` 會撞到 vitest 自己的
`test: { include… }` 區塊，有對照條守它。

**C154 §三 兩軸**（對象在外：別人的 `package.json`／`vite.config.ts`）：對象在外 ✓；壞法安靜 ✓
（§一：跳過不印一行，而沒跑過與全綠同形）。⚠️ 它不是閘門，是 gate-kit 現有測試檔多的一段 —— C137 §一 不涉。

#### 五、變異表

| #   | 變異                                                  | 樹   | 紅了什麼                                                          |
| --- | ----------------------------------------------------- | ---- | ----------------------------------------------------------------- |
| M1  | 刪 `tools/release-distance/vite.config.ts`            | 本機 | 絆線 2 條（錨點 ＋ 名單非空），61 → 2 紅                          |
| M2  | `readGit` 丟掉 tag                                    | 本機 | 對照組 ＋ 沙盒「有 tag 時印得出數字」                             |
| M3  | `describe --match "v9*"`（沙盒的 tag 過、真樹的不過） | 本機 | **只紅對照組** —— 它獨有的價值：沙盒 fixture 裡的假設，真樹會揭穿 |
| M4  | task 改名 `tset`                                      | 本機 | 絆線 2 條                                                         |
| M5  | `why` 拿掉 `fetch-depth`                              | 淺樹 | 對照組 ＋ 純函式那條                                              |
| M6  | 沒 tag 時當成量到 0 支（C169 §四 的致命錯）           | 淺樹 | 對照組 ＋ 沙盒空樹 ＋ 純函式，3 紅                                |
| —   | main 的舊對照組                                       | 淺樹 | 1/13 紅（本則的起點）；新版 13/13 綠                              |

淺樹＝`--depth 1 --no-tags` clone 本支，`pnpm install --offline`；每個變異之後 `git status` 乾淨。

#### 六、不裁的事

- **不改 CI 的 checkout 深度**（C171 §七 照舊）。§二 的做法讓這件事不再需要答案。
- **不動 C171**（C136 §八）；§七 那根柱子的空白寫在本則 §一，不回頭改。
- **不貼 `#267`**。票關在驗收條件成立的前提下，那個前提現在有紀錄了 —— 在這裡，不在票面。
- **不論證增減任何閘門**（C137 §一）。
- ⚠️ C178 §五 的**另一件**（五支 CLI 手讀 argv、`FLAGS.flags` 零使用）仍在，本則不碰。

#### 七、關係

- **C171 §七**：「取而代之的是那條載重測試」—— 自本則起才第一次執行。
- **C171 §八**：「它走 `@org/gate-kit/testing`，絆線是綠的」—— 那條絆線問的是 harness；「第一次有機會擋」而它擋的不是這個。§四 的第二條補的就是這一格。
- **C171 §九**：同一筆錢（快取看不見 git ref），CLI 付過，本則讓測試也付（§三）。
- **C178 §五**：順手撞到的兩件收掉一件。
- **C168 §一** 的判準（同變異兩邊紅 ＋ 刪除測試）：M3 是對照組留下的理由。
- **C169 §四**：M6 就是「把空的當成 0」，本則證明它在淺樹上會被抓到。
- **C114 §二**：§二 否決 skipIf 的依據。
- **C154 §三**：§四 的兩軸。

`it` 數：`vp run -r test` 逐檔 `it` 數 **83 檔 1552 → 84 檔 1571**（C178 基準）—— 差的兩個檔：`tools/release-distance/tests/distance.test.ts` 無 → 13（第一次出現在結果檔裡），`tools/gate-kit/tests/harness-tripwire.test.ts` 6 → 12。其餘 82 檔逐位相同。

### C180 — 五支 CLI 把 `parseFlags` 當門房、拿到 `FLAGS` 一次都沒讀：同一個 argv 在同一個行程裡兩個答案，改成值只從 `parseFlags` 來，三道到不了的缺值檢查刪掉，並在 adoption 加「`process.argv` 只讀一次」的絆線（2026-09-05）

⚠️ **量測基準：`ad405ce`（C179 合併後的 `main`）。** 本則收掉 **C178 §五** 第一項 ——
那一則自己說「這是產品碼的事，射程是五支閘門」而不裁的那件。

#### 一、事實：C178 §五 那一句記錯兩處，射程仍是五支

| CLI           | spec 鍵                            | 走 `FLAGS.flags` | 手讀                                 | `process.argv`（去註解） | 本機測試餵過的旗標                       |
| ------------- | ---------------------------------- | ---------------- | ------------------------------------ | ------------------------ | ---------------------------------------- |
| compliance    | file, evidence, update             | 0                | 3（`indexOf` ＋ 兩個 `includes`）    | 2                        | 三個都有                                 |
| supply-chain  | 九個                               | 0                | 9（兩個 `indexOf`、七個 `includes`） | 2                        | 只有 `--split-lockfile`、`--verify-sbom` |
| exit-drill    | full, require-fresh                | 0                | 2（`--require-fresh` 讀三處）        | **5**                    | 零                                       |
| ui-survey     | csp, sca                           | 0                | 2                                    | 2                        | 零（survey.test 不 spawn CLI）           |
| csp-verify    | print-probe                        | 0                | 1                                    | 2                        | 零（沒有 `tests/` 目錄）                 |
| spec-report   | check, help, report, results, root | 4                | 1（`--results` 可重複）              | 1                        | —                                        |
| promise-check | root, spec                         | 1                | 1（`--spec` 可重複）                 | 1                        | —                                        |

其餘十支：走 `.flags` 或空 spec，`process.argv` 各一次。

C178 §五 記錯的兩處：**`conformance` 那一處 `indexOf("--root")` 在檔頭註解裡**，程式碼沒有；
**`csp-verify` 漏了**（`ARGV.includes("--print-probe")`，同形）。`spec-report` 不算：四個旗標
已走 `flags.flags`，剩那一個與 `promise-check` 是同一題（§五）。射程是五支，名單換一支。

#### 二、兩條取值路徑不是等價的：重複給同一個旗標，手讀取第一個、`parseFlags` 取最後一個

差分探針 D1（改前）：`compliance --file /不存在 --file COMPLIANCE.md` → **RC 1**；反過來 → RC 0。
同一個 argv、同一個行程，`FLAGS.flags.file` 是後者、`parseFile()` 是前者，而沒有人讀
`FLAGS.flags` 所以看不見。`spec-report` 與 `promise-check` 的檔頭都寫著「`parseFlags` 只留
最後一個」—— 改前變異 M10（把 `parseFlags` 改成第一個贏）**55 條全綠**：那句話沒有東西在守。

改前變異（逐 package `npx vitest run`）：

| 變異                                                  | 紅                  |
| ----------------------------------------------------- | ------------------- |
| M1 compliance `--file` 取值 `indexOf` → `lastIndexOf` | **零**              |
| M2 compliance 無視 `--file`                           | negative.test 2 條  |
| M3 compliance 無視 `--evidence`                       | evidence.test 2 條  |
| M4 supply-chain 無視 `--split-lockfile`               | sbom-negative 2 條  |
| M5 supply-chain 無視 `--verify-sbom`                  | sbom-negative 3 條  |
| M6 exit-drill 無視 `--full`                           | **零**              |
| M7 exit-drill 無視 `--require-fresh`（三處）          | **零**              |
| M8 ui-survey 無視 `--csp`                             | **零**              |
| M10 `parseFlags` 重複旗標改「第一個贏」               | **零**（55 條全綠） |

M2–M5 說明取值路徑本身有行為測試接著（收攏不會丟覆蓋）；M1／M10 說明兩條路徑的分歧
沒有；M6–M8 說明那四個旗標本機什麼都沒有（§五）。

#### 三、裁決：值只從 `parseFlags` 來

1. **五支 CLI 一律讀 `FLAGS`**，手讀段全拆。收窄在頂層做一次
   （`const FLAGS = PARSED.flags`）—— `process.exit` 的 `never` 不會把型別帶進函式體，
   api-surface 檔頭記過。`exit-drill` 從五處 `process.argv` 降到一處。
2. **重複旗標的語意跟 interface 走：最後一個贏。** compliance／supply-chain 因此翻面
   （§六 D1 改後）。不改 `parseFlags` 去遷就繞過它的 caller。`flags.test` 加一條
   「同一個 value 旗標給兩次 → 最後一個贏」（+1 `it`），從此那兩句檔頭有東西守。
3. **三道「缺值 → 紅」的手寫檢查刪掉**（compliance `parseFile`、supply-chain
   `--split-lockfile`／`--verify-sbom` 各一）：`parseFlags` 先紅，它們到不了；C178 §三
   已把守它們的測試刪了。deletion test：刪掉沒有複雜度回到任何 caller，
   `string | undefined` 的 `undefined` 就是「沒給」。
4. **supply-chain 九個模式的優先序逐字保留**（`--update` > `--split-lockfile` >
   `--verify-sbom` > `--capture-health` > `--recapture-safe` > `--capture` > `--manifest` >
   `--dossier` > `--airgap` > gate）。同時給兩個先出現在鏈上的贏，這一點沒有裁（§五）。

零閘門增減（C137 §一），門檻不動，`vpr gate` 那條鏈一支不碰。變的是五道閘門的內部。

#### 四、絆線：每支 `cli.ts` 的 `process.argv` 恰好一次

掛在 `gate-kit/tests/adoption.test.ts`，吃同一份磁碟名冊（C178 §二）。剝掉區塊註解與整行
`//` 之後數 `process.argv`，17 支各 **1**，零 allowlist；0 也紅（那表示剝除吃掉了程式碼，
絆線在量空氣）。對照組一條：註解裡的不算、`process.argv.includes("--full")` 那個形狀算兩次。

C154 §三 兩軸：**交付軸** —— 無。**迭代軸** —— ① 對象在外：它報的是五支閘門的形狀，
不是它自己；② 壞法安靜：C178 §六 M4／M7 與本則 M1、M10 四個零紅。兩條都成立。

⚠️ 它抓不到「抓一次 argv 再自己 `indexOf`」那種形 —— `spec-report`／`promise-check`
今天就是。更強的版本（`cli.ts` 裡不准 `indexOf("--`／`includes("--`）要等 §五 第一項
定了才零例外。

#### 五、不裁

- **可重複旗標。** `promise-check` 的 `parseRepeated` 與 `spec-report` 的 inline 迴圈是同一段
  程式碼寫兩次，`parseFlags` 沒有這個 kind。兩個 adapter 是真的 seam；但它不是 C178 §五
  那一項，C118 那種「順手做一半」的代價這棵樹付過。給 `parseFlags` 加第三種 kind 要另裁，
  裁的時候 §四 的絆線可以升成強版。
- **本機零測試的四個旗標**（`--full`、`--require-fresh`、`--csp`／`--sca`、`--print-probe`）。
  改前改後 M6／M7／M8 都零紅。收攏之後它們是型別檢查過的屬性存取 —— spec 少一鍵在
  `vp check` 紅 —— 但「讀到了卻沒用」仍然安靜。`--require-fresh` 要有本機測試得先開一個
  seam（evidence 路徑寫死在 `ROOT`、CLI 沒有 `--root`、函式沒匯出），那是改一道閘門的
  interface，題目變了。C178 §五 第二項那句照留。
- **supply-chain 模式互斥。** 兩個以上同時給要不要紅，沒有事故在後面，不裁。
- **C178 §五 記錯的兩處**照 C136 §八 記在 §一，不改舊則。

#### 六、實測（基準 `ad405ce`，worktree `flags-value-path`）

改後（每一趟還原後 `git status` 乾淨）：

| 變異                                                     | 紅                                                                          |
| -------------------------------------------------------- | --------------------------------------------------------------------------- |
| A1 compliance `--file` 改 `boolean`（C178 M4）           | **7/65 紅** ＋ `vp check` TS2345；C178 §六 那一格是零                       |
| A2 supply-chain `--verify-sbom` 改 `boolean`（C178 M7）  | **6/94 紅**；C178 §六 那一格是零                                            |
| A3 exit-drill 放回一句 `process.argv.includes("--full")` | adoption 絆線紅                                                             |
| A4 `parseFlags` 重複旗標改「第一個贏」（＝M10）          | flags.test 新那條紅                                                         |
| A5 絆線的註解剝除改成吃掉全部                            | 對照組紅 ＋ 絆線紅（17 支全 0）                                             |
| A6 compliance 無視 `--file`（＝M2）                      | negative.test 同 2 條                                                       |
| D1 改後                                                  | `--file 壞 --file 好` → **RC 0**，反過來 RC 1 —— 翻面，與 `parseFlags` 一致 |

- `vpr ready`：第一趟 decision-ids 紅（`C180` 字面先於本則，C173 §五 三態表第一格），
  其餘全綠；接上本則再跑一趟見 commit。
- 逐檔 `it` 數（量法同 C172 §七）：**84 檔 1571 → 84 檔 1574**（C179 基準）—— 只有兩個檔變：`tools/gate-kit/tests/flags.test.ts` 11 → 12、`tools/gate-kit/tests/adoption.test.ts` 18 → 20。其餘 82 檔逐位相同。

#### 七、關係

- **C178 §五 第一項**：收掉。§一 記它兩處記錯；§三 第 3 點讓 C178 §三 那句「守的是到不了的
  第二道檢查」從「半真」（擋掉是真的、取值走另一條路）變成全真。
- **C178 §六 M4／M7**：兩個零紅在 §六 A1／A2 變成 7 與 6。
- **C126**：本則拆的就是它接線時留下的形狀 —— `parseFlags` 擋在前面、手讀段沒拆。
- **C168 §一**：判準照用（M2–M5 改前改後同紅；deletion test 判 §三 第 3 點）。
- **C154 §三**：§四 的兩軸。
- **C137 §一**：零閘門增減。**C43**：每道閘門的反向測試一條沒動。
- **C118**：§五 第一項不順手做一半的理由；反過來，§三 是「五支全收」不是子集。
- **C173 §五**：`C180` 字面先於本則那一趟紅。
- **C136 §八**：C178 §五 記錯的兩處不回頭改。

### C181 — `parseFlags` 加第三種 kind `list`：`promise-check`／`spec-report` 各自的收集迴圈是最後兩處手讀，而重複給旗標的路徑一個零餵養者、一個零測試（2026-09-05）

⚠️ **量測基準：`3cd03db`（C180 合併後的 `main`）。** 本則收掉 **C180 §五** 第一項。
量測期間 #284（`4dd3f1a`，只動 `CHANGELOG.md`）合進 `main`，本則接在它之上；基準不變。

#### 一、事實：兩處迴圈同形，餵養者一個是零、一個只給過一次

|          | `promise-check` `parseRepeated(argv, "--spec")`    | `spec-report` `readResults(root, specs, argv)` |
| -------- | -------------------------------------------------- | ---------------------------------------------- |
| 演算法   | `indexOf` 逐次前進，取 `argv[i + 1]`               | 逐字同形，多一步 `resolvePath(root, …)`        |
| 缺值檢查 | 有，但 `parseFlags` 已對每一次出現檢查過 —— 到不了 | 同                                             |
| 沒給時   | `length > 0 ? explicit : trackedSpecs(root)`       | 預設路徑先放，額外的 push 在後                 |

兩支都是 C180 那種形：`parseFlags` 走一遍拒絕不認得的，迴圈再走一遍取值。差別只在這兩支的迴圈
確實有 `parseFlags` 給不出的能力（多值），所以 C180 §五 把它們另裁。

餵養者（全樹，排除 `DECISIONS*` 與鄰居 worktree）：

- `--results`：**零**。從 C115（`723beb1`）出生到本則，沒有測試、workflow、script 用過它。
- `--spec`：一處，`promise-check/tests/cli.test.ts` 給**一次**。重複給的路徑零測試。
- `promise-check/tests/negative.test.ts` 走 `import { checkPromises }`，從 C118（`37c3547`）起就
  不經過 CLI。

#### 二、C126 §七 的兩句話，一句理由死了、一句事實錯了

- **「改一個字就失去逐字相同 → 零衝突」** 指的是 `main` 與 `release/v1` 兩線併線時
  `flags.ts` 逐字相同。兩線模型 2026-08-26 結束（C133），這個理由已經不存在。
- **「縮減掉的正是反向測試餵多份改壞規格那條路」**：反向測試不走 `--spec`（§一）。多值的能力
  是真的 —— 一份改壞的規格得跟一份好的一起餵，否則 `BREAKAGES` 孤兒檢查先紅 —— 但那條路在
  測試裡從來不存在。`promise-check` 檔頭抄了同一句。兩處都不回頭改（C136 §八），本則記。

#### 三、裁決

1. **`parseFlags` 加 `kind: "list"`**，型別 `readonly string[]`，沒給是 `[]`，順序照給的順序；
   缺值走 `value` 同一條（後面沒東西、後面是另一個旗標）。**不提供 `fallback`**：兩個呼叫端都是
   「沒給就換一個來源」，空陣列直接接得上，fallback 沒有使用者（一個 adapter 是假想的 seam）。
2. `promise-check` 刪 `parseRepeated`，`spec-report` 的 `readResults` 第三個參數從 `argv` 改成
   `extra: readonly string[]`；兩支檔頭「`parseFlags` 只留最後一個」那段整段拿掉。
3. **`value` 重複給仍是最後一個贏。** C180 D1 剛裁、剛加測試，而目前零證據有人被它咬到；改它是
   三支以上 CLI 的行為改變，另裁（§五）。
4. 兩支各加一條差分探針（C180 §二 D1 的形）：`promise-check` 給兩個不存在的 `--spec`，輸出要同時
   提到兩個；`spec-report` 在 sandbox 給兩份各一半場景的 `--results`，合起來才是 3/4，只給第二份
   是未執行（對照組）。
5. `flags.test.ts` 加三條：空、多值保序、缺值兩種缺法。五條併三條 —— 兩條殺不到獨立的變異
   （C168 §一）。

#### 四、絆線升強版：argv 上的搜尋呼叫零次

C180 §四 那條守「`process.argv` 恰好一次」，抓不到「抓一次 argv 再自己 `indexOf`」。本則加第二條：
去註解後，`argv`／`args`／`ARGV`／`ARGS` 接 `indexOf`／`lastIndexOf`／`includes`／`startsWith`／
`find`／`some` 的呼叫在每支 `cli.ts` **零次**（`\b` 讓 `process.argv.` 也算）。

pattern 改了兩版，兩版都是對照組逼出來的：

| 版        | pattern                          | 問題                                                                                                                                                                       |
| --------- | -------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1         | 任何接收者 ＋ `"--` 字面         | 誤報兩處：`supply-chain` 掃 workflow 行的 `line.includes("--frozen-lockfile")`、`theme-verify` 判 CSS 自訂屬性的 `utility.startsWith("--")` —— 比對的字串來自檔案不是 argv |
| 2         | 接收者限定四個別名 ＋ `"--` 字面 | 對 C180 前的 `promise-check` 回 **0**：`parseRepeated(argv, name)` 寫的是 `argv.indexOf(name)`，字面在參數裡                                                               |
| 3（採用） | 接收者限定四個別名，不要求字面   | 對 C180 前的樹（`9a8c9a4`）回 **23 處／7 支**（compliance 3、supply-chain 9、exit-drill 4、ui-survey 2、csp-verify 1、spec-report 2、promise-check 2）；現在 17 支全 0     |

對照組七句：`argv.indexOf("--spec")`、`ARGV.includes('--full')`、`argv.indexOf(name)` 各 1；
USAGE 模板字串、註解裡的、`main(process.argv.slice(2))`、第 1 版誤報的那兩句各 0。

C154 §三 兩軸：對象是 17 支 `cli.ts` 的程式碼（在測試外）；壞法是「重複給旗標時兩條路徑答案相反」
（C180 §二），零紅零訊息。

#### 五、不裁

- **`value` 重複給改成拒絕。** 有了 `list` 之後 `--root a --root b` 幾乎必然是打錯，但改它要動
  C180 那條測試與三支以上 CLI 的行為，而零證據。要裁時先找一個被咬到的案例。
- **`list` 的去重／空字串。** `--spec a --spec a` 會跑兩次；沒有呼叫端在乎。

#### 六、實測（基準 `3cd03db`，worktree `list-flag-kind`）

改前（迴圈還在，`parseFlags` 沒有 `list`）—— 每趟還原後 `git status` 乾淨：

| 變異                              | 紅                                                                                                           |
| --------------------------------- | ------------------------------------------------------------------------------------------------------------ |
| M1 `parseRepeated` 只留最後一個   | **0**（promise-check 1/36 是環境紅：新 worktree 沒有 `.vitest-results.json`，不變異也紅，`cli.test.ts:155`） |
| M2 `parseRepeated` 只留第一個     | **0**（同上）                                                                                                |
| M3 `readResults` 迴圈只留最後一個 | **0**（spec-report 0/39）                                                                                    |
| M4 `readResults` 迴圈只留第一個   | **0**                                                                                                        |

四顆零紅就是 §一「零餵養者／零測試」那一行的執行版。

改後：

| 變異                                               | 紅                                                                    |
| -------------------------------------------------- | --------------------------------------------------------------------- |
| A1 `list` 改只留最後一個                           | flags.test 1 ＋ spec-report 1 ＋ promise-check 1（三個 package 都紅） |
| A2 `list` 改只留第一個                             | 同 A1                                                                 |
| A3 `readResults` 只用 `extra.slice(-1)`            | spec-report 差分那條紅                                                |
| A4 `promise-check` 只用 `spec.slice(-1)`           | promise-check 差分那條紅                                              |
| A5 絆線 pattern 加回 `"--"` 字面要求               | 對照組 `argv.indexOf(name)` 那句紅                                    |
| A6 `spec-report` 放回一句 `argv.indexOf("--root")` | 絆線紅                                                                |

- `vpr ready`：一趟全綠（`READY_RC=0`，讀的是 log 尾不是通知摘要）。
- 逐檔 `it` 數（量法同 C172 §七）：**84 檔 1574 → 84 檔 1582**（C180 基準）—— 只有四個檔變：
  `gate-kit/tests/flags.test.ts` 12 → 15、`gate-kit/tests/adoption.test.ts` 20 → 22、
  `spec-report/tests/cli.test.ts` 16 → 18、`promise-check/tests/cli.test.ts` 4 → 5。其餘 80 檔逐位相同。
  ⚠️ rtk 的 `diff` 對這兩份計數檔回報 `Files are identical`（總數差 8），第九種的再一次。

#### 七、關係

- **C180 §五 第一項**：收掉。§四 是它預告的「強版」，而強版的形狀跟預告的不一樣（不是零例外，
  是換一個量的東西）。
- **C126 §七**：兩句話的處置在 §二；`flags.ts` 從 C126 起第一次改。
- **C133**：兩線模型結束，是 §二 第一句的依據。
- **C118**：C180 §五 引它不順手做，本則是那個「另裁」。
- **C168 §一**：§三 第 5 點併條的判準；§六 M1–M4 改前零紅是 §一 的執行版。
- **C154 §三**：§四 兩軸。**C137 §一**：零閘門增減。**C43**：反向測試一條沒動。
- **C136 §八**：C126 §七 與 `promise-check` 舊檔頭那句不回頭改。
- **C173 §五**：`C181` 字面先於本則那一趟 `vpr ready` 會紅 —— 所以本則跟程式碼同一個 commit。

### C182 — 本機閘門走進鄰居 worktree：事實來源改問 git 不問磁碟，而這棵樹早就替另一支工具寫下了這個論證（2026-09-05，#282）

⚠️ **量測基準 `4dd3f1a`。** 本則裁 `#282`。

#### 一、量法：兩趟，差別只有一個鄰居 worktree

在 **repo 外面**（`$TMPDIR`）開一份 `git worktree add --detach 4dd3f1a` ＋ `vp install`
的乾淨樹 —— 它結構性沒有巢狀 worktree。逐支跑 `gate` 那 15 步（不能跑 `vpr gate`：
它是 `&&` 串的，第 9 步就中斷，第 15 步看不到）。然後在它底下種一個
`.claude/worktrees/neighbor`（同一支 commit、也 `vp install` 過），一個字都不改再跑一次。

**13 步的輸出逐位元組相同，2 步變：**

| 步驟                           | 乾淨 | 有鄰居                   |
| ------------------------------ | ---- | ------------------------ |
| 其餘 13 步                     | RC 0 | RC 0（輸出逐位元組相同） |
| 9 `pii-check`                  | RC 0 | **RC 1**（9 項）         |
| 15 `eslint --config a11y.js .` | RC 0 | **RC 1**（1 項）         |

#### 二、「走進去」與「紅」不是同一件事 —— 第 14 步的綠是內容的巧合

第 14 步（`eslint . --max-warnings=0`）兩趟都綠。**它不是被排除，是走進去了沒撿到東西**：

| 設定       | 列舉檔數 | 其中在鄰居樹 |
| ---------- | -------- | ------------ |
| 根（安全） | **552**  | **276**      |
| a11y       | **80**   | **40**       |

兩個都**恰好一半** —— 鄰居是整棵樹的副本，列舉數翻倍就是「它走進去了」的直接讀數。

⚠️ **而 13 步那一側是有對照組的**，不是「沒印錯就算沒事」：它們印的計數只要走進去就會翻倍，
兩趟逐字相同 ——

```
✓ 一致性檢查通過（3 個切片）
✓ platform/ API 形狀無破壞性變更（11 個進入點，159 個 export）
✓ .vue 型別檢查通過（5 份 program、32 個 SFC）
✓ 靜態：27 個元件 ＋ 5 個切片／應用畫面、0 處原始顏色
✓ 供應鏈：712 個套件、144 個原生二進位、12 個家族（…）
✓ 文件裡的數字與事實來源一致（14 個事實、33 個引用樣式、3 份文件）
```

所以**曝險的是三步、發作的是兩步**，而不是「只有一支工具有問題」。

#### 三、成因：三種列舉，只有一種會走進去

| 列舉方式             | 誰                                                                                       | 走進鄰居樹？                             |
| -------------------- | ---------------------------------------------------------------------------------------- | ---------------------------------------- |
| 問 git（`ls-files`） | scope-check、api-surface、doc-facts、conformance、gate-roster、promise-check、exit-drill | **不會**（鄰居目錄進了 `.gitignore:33`） |
| 從**子目錄**走磁碟   | theme-verify、vue-typecheck、supply-chain、threshold-check                               | **不會**（`.claude` 不在它們的起點底下） |
| 從 **repo 根**走磁碟 | `pii-check`、兩支 eslint                                                                 | **會**                                   |

兩個症狀的近因不一樣，而遠因是同一個：

- **`pii-check`** 用 `gate-kit` 的 `walk(root, { skip: SKIP, extensions: TEXT })`，
  `SKIP` 是一張**目錄名**清單（`node_modules`、`.git`、`dist`、`.scan`、`coverage`、
  `.vite-plus`、`reports`、`.stryker-tmp`），沒有 `.claude`。而命中的 9 項全部是
  `tools/pii-check/tests/detect.test.ts` 這份**已經在 `EXEMPT` 裡**的反向測試對照組的**複本** ——
  `EXEMPT` 只收完整路徑、不收 glob，而那是刻意的（`scan.ts:56`），所以**沒有任何條目
  能夠涵蓋副本**。
- **a11y eslint** 的 `files: ["platform/ui/src/components/**/*.vue"]`（`a11y.js:148`）
  **從 repo 根錨定**，配不上副本路徑，於是那條被刻意關掉的 `form-control-has-label`
  （理由在 `:133–147`：基元刻意不宣告 `id`，由 `UiField` 綁，C84）在副本上又被打開。
  ⚠️ 兩份檔案 `cmp` 逐位元組相同、主樹那一份 RC 0 —— **這不是同事分支上的新缺陷**。

⚠️⚠️ **同一個檔案的 `:78–83` 已經把這個病寫下來過了**：flat config 的樣式是相對
**basePath** 比對的，而 basePath 隨呼叫端而變，寫死前綴的版本會在其中一邊安靜失效。
那段註解守的是 `:84` 的 `ignores`，**沒有守 `:148` 的 `files`**。

#### 四、這棵樹三個月前就替另一支工具寫下了答案

`tools/scope-check/src/tree.ts` 的檔頭，逐字：

> 因為問的問題就是「**版控裡**有什麼」，而 `readdirSync` 答的是「**磁碟上**有什麼」。
> （…）用磁碟當事實來源的檢查會**在開發機紅、在 CI 綠** —— 那正是 C41 說的
> 「第一天就被加例外，然後例外再也拿不掉」的形狀。

**它預測的正是 `#282` 的症狀，一個字不差。** 所以本則不需要發明判準 —— 判準已經在樹上，
`pii-check` 是沒拿到它的那一支。⚠️ 這一點很重要：自己再造一把尺，就是 D16 記錄過那三次
翻案的形狀（證據沒變、變的只有尺）。

#### 五、兩個代理都不成立，而它們都長得像答案

| 代理                          | 今天成立嗎                                  | 為什麼不裁它                                                                                                                                                                                                                                                   |
| ----------------------------- | ------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `walk` 的 `skipDotDirs: true` | **成立**（219 → 112，正好回到乾淨樹的答案） | 那是巧合：實測射程內的檔案落在點目錄底下的有 **107 個，分佈 `{".claude": 107}`** —— **一個別的點目錄都沒有**。第一份塞進 `.其他/` 的 fixture 會安靜離開射程，而 diff 上看不到（N=1 的形狀）。⚠️ 且 `skipDotDirs` **今天零個 caller**，拿這裡當它的第一個試驗場 |
| `SKIP` 加一列 `.claude`       | 成立                                        | 黑名單。下一份副本落在別處（repo 的 `dist/`、備份目錄、第二個 worktree 根）就要再加一列，而每一列都要有人先被咬一次                                                                                                                                            |

#### 六、裁決

1. **`pii-check` 的檔案列舉改問 git**：`git ls-files` ∪ `git ls-files --others --exclude-standard`。
   前者是追蹤中的，後者是**未追蹤但沒被 ignore** 的 —— 後半不能省：一支 PII 閘門最該擋的
   就是「真資料還沒 commit」那一刻。實測見 §八。
2. **原則對兩支 eslint 同樣成立**：**綠不等於被排除**。第 14 步今天綠是內容的巧合，
   不得拿它當「根設定有排除 `.claude`」的證據。⚠️ 但**機制本則不裁**（§七）。
3. **不禁止巢狀 worktree。** 三個理由：C135 §一 —— 裁決不能替自己發明新位置；它不解決這一類
   （任何一份副本都重現）；而 `.claude/worktrees/` 這個慣例已經是承重的（兩次 session
   互相覆蓋的事故都掛在它上面）。**要改的是工具，不是工作方式。**
4. **零閘門增減**（C137 §一），`vpr gate` 那條鏈一支不碰，門檻一格不動。

⚠️ **這不是規則二說的「改門檻求綠燈」。** 規則二 管的是**判準**；本則動的是**對象**。
`pii-check` 宣稱的對象是「**這個 repo 的**測試環境」（§11 II ⑥），而鄰居 worktree 是
**另一個工作樹**。把它從分母裡拿掉不是放寬，是把分母改對 —— 而判準（`SCAN_RULES` 四條、
`detect.ts` 的偵測器、`EXEMPT`）一個字都不動。

#### 七、不裁

- **兩支 eslint 的機制。** eslint 沒有「問 git」這個列舉選項，能用的是 `ignores`，
  而它撞的是 `a11y.js:78–83` 記著的那個 basePath 問題：任何 `.claude` 的排除都要
  **同時**在兩個呼叫端成立（閘門從 repo 根跑、`platform/eslint-config` 的反向測試從
  package 裡跑）。⚠️ 那是改一個共用設定的 interface，題目與 §六 第 1 點不同，另裁。
- **另外四支從子目錄走磁碟的**（theme-verify、vue-typecheck、supply-chain、threshold-check）。
  §二 的對照組說它們今天走不進去，而那是起點決定的、不是它們判斷過。要不要一併換成問 git
  沒有事故在後面，不裁。
- **`walk` 要不要有預設的 skip 聯集。** `walk.ts:5–13` 自己說了為什麼今天是參數不是預設值
  （八份清單互相矛盾，給聯集會讓 `conformance` 悄悄變弱）。本則不動它。
- **`MINIMUM_SCANNED` 的值。** 12 對上乾淨樹的 112，§八 說明改列舉之後仍是 112。不碰。

#### 八、實測：git 那個述詞，兩個方向都放對照組

在 §一 那棵**有鄰居**的樹上跑：

| 讀數                                   | 值                       |
| -------------------------------------- | ------------------------ |
| `ls-files`                             | 441                      |
| `ls-files --others --exclude-standard` | 1（＝種下去的探針）      |
| 聯集                                   | 442                      |
| 其中在射程內（`inScope`）              | **113**                  |
| 其中在鄰居樹裡                         | **0**（已知為零的方向）  |
| 種下的**未追蹤**探針有沒有被看見       | **有**（已知非零的方向） |

113 ＝ 乾淨樹的 **112** ＋ 探針 1。也就是：**換成問 git 之後，有沒有鄰居的答案都是 112**，
而「未追蹤的真資料」照樣看得見。對上 `MINIMUM_SCANNED = 12`，還有十倍餘裕。

射程本身（`SCAN_RULES` 四條，讀出來不是推出來的）：`/tests/` 底下的、檔名含 `fixture` 的、
`platform/bff-mock/` 底下的、`/locales/` 或 `i18n.*`。**四條都不指向任何點目錄** ——
`.claude` 之所以進得來，只因為它裝著整棵樹的副本（副本裡有 `tools/*/tests/`）。

#### 九、實作留在 `#282`，絆線本則先填

本則**不實作**（C169 是先例：裁決與實作分開），也**不關 `#282`** —— 那張票繼續當實作票，
而 §七 第一項（兩支 eslint 的機制）也留在它身上。實作要帶的絆線，本則先把
**C154 §三** 兩軸填了：

**它要斷言什麼**：在樹底下種一個鄰居 worktree（或任何一份副本），`pii-check` 的
**列舉數不變**。⚠️ 對照組要兩條：拿掉鄰居也不變（已知為零）、種一個未追蹤的射程內檔案
會 +1（已知非零）—— 少了第二條，一支「永遠回 0 個檔」的實作也會通過。

**交付軸** —— 無。**迭代軸** —— ① 對象在外：它報的是一道閘門的事實來源，不是它自己；
② 壞法安靜：`#282` 的兩處都是**沒有任何東西會紅**的形狀，發現它靠的是有人手動逐步跑
（CI 是乾淨 checkout，結構性看不到這一整類）。兩條都成立。

#### 十、關係

- **C41**：`scope-check/src/tree.ts` 引它的那句「開發機紅、CI 綠」，本則是它的第二個實例。
- **C135 §一**：§六 第 3 點不替 worktree 發明新位置。
- **C137 §一**：零閘門增減。
- **C84**：`a11y.js:148` 關掉 `form-control-has-label` 的理由，本則不動它。
- **C154 §三**：§九 的兩軸。
- **C169**：裁決與實作分開的先例。
- **C33／C34**：`MINIMUM_SCANNED` 檔頭引的那兩個「掃到零＝通過」，§七 不碰它的理由。

### C183 — `exit-drill` 的新鮮度判斷表抽成純函式：`--require-fresh` 三處讀收成一個參數，十四列的表第一次有測試，而最後那一行傳遞本機仍然零紅（2026-09-05）

⚠️ **量測基準：`9cccf86`（C181 合併後的 `main`）。** 本則收掉 **C180 §五** 第二項裡
`--require-fresh` 那一個。編號跳過一號：鄰居 session 的「本機閘門走進鄰居 worktree」先取了它
（分支 `docs/gate-tree-boundary`，本則寫的時候還沒進 `main`，所以這裡不寫它的字面）。

#### 一、事實：三處讀、七個狀態、零測試

`checkFreshness()`（`cli.ts:366–443`）是一張表，`--require-fresh` 在三格裡有作用：

| 狀態                        | 沒給    | 給了         |
| --------------------------- | ------- | ------------ |
| `evidence.json` 不存在      | warn，0 | warn，**1**  |
| 超過 120 天                 | warn，0 | error，**1** |
| 指紋 `unrecorded`（舊格式） | warn，0 | error，**1** |

另外四格旗標不影響：`result !== "pass"` → 1；指紋 `empty` → 1；`drift` → warn 0（C149 §二 刻意）；
`match` → 往下比文件數字（`tests` 缺欄位跳過、對不上 → 1）。**順序有意義**：失敗在過期之前、
過期在指紋之前，又失敗又過期印的是失敗那句。

沒有 seam 的四件事全在同一個函式體裡：`EVIDENCE_PATH` 寫死在 `ROOT`、`Date.now()`、
`currentFingerprint()` 直接 `git ls-files` 真樹、`Evidence` 沒匯出。周圍已經是 seam 的兩塊
（`compareFingerprint` 7 條、`checkDocumentedCounts` 測到真文件）夾著中間這張沒人測的表。
餵養者：CI 只有 `tier2-security.yml` 排程分支給；本機零。

#### 二、裁決

1. **新檔 `src/freshness.ts`，純函式 `judgeFreshness(input): { code, lines }`。** input 是
   `{ evidence: Evidence | null, now, current, documents, requireFresh }`；回傳結束碼與要印的行
   （`level` ＋ `text`），印與離開留給 `cli.ts`。三處讀收成 input 的一個欄位。
2. **射程含下半張表**：`tests` 缺欄位跳過、`checkDocumentedCounts` 對不上 → 1，一起進來。
   切開會留一個 C118 式的「守到一半」。`checkDocumentedCounts` 本身不動。
3. **證據照值傳**，`cli.ts` 讀檔＋`JSON.parse`，不存在給 `null`。解析失敗現在丟出去沒接，維持原樣。
   `Evidence` 與 `FRESHNESS_DAYS` 搬進 `freshness.ts` 匯出。
4. **`now: number` 由 `cli.ts` 傳 `Date.now()`。**
5. **`current` 與 `documents` 是 thunk，不是值** —— grilling 時說的是值，實作改了：原本指紋只在
   證據過了前兩關之後才算（`git ls-files` ＋ 全檔 hash），照值傳會讓證據不存在時也算一遍，順序變了。
   有一條測試守「沒有證據／失敗時一次都不叫」。
6. **測試是判斷表本身**：`it.each` 七個狀態 × 兩個旗標值 = 14 列，每列斷言結束碼與最高 level；
   再加順序（又失敗又過期又 drift → 印失敗、兩個旗標值都 1）、thunk 不早叫、`tests` 缺欄位、
   文件對不上、**邊界（剛好 120 天仍新鮮）**、README 的「上限 N 天」等於常數。
   另一條「旗標只翻三列」斷言的是表的資料不是程式碼 —— 它守的是 C149 §二 那句 drift 不翻，留著。

#### 三、那一行傳遞，本機零紅，承認

改完之後 `cli.ts` 只剩 `requireFresh: FLAGS["require-fresh"]` 一句。把它改成 `false`，本機**仍然
零紅**（§六 A5）：這支 CLI 沒有 `--root`，spawn 它只能跑真樹，真樹的證據新鮮、兩個旗標值都 exit 0，
做不出差分。局部零測試的面從「三處判斷」縮成「一行傳遞」，那一行由型別（spec 少鍵 `vp check` 紅）
與 C180 §四 的絆線守。**不加 `--root`**：要動 `coveredPaths`、`currentFingerprint`、`EVIDENCE_PATH`、
掃描目錄四處，是改一道閘門的 interface —— C180 §五 說「題目變了」的正是這個；等有第二個需要
`--root` 的理由再裁。

#### 四、不加絆線

C154 §三 兩軸：對象在內（`exit-drill` 自己的判斷邏輯），不是 meta 閘門；壞法安靜這一軸成立但
單元測試接得住。所以是測試不是絆線。README 那條「上限 N 天」是唯一掛在文件上的，形狀照
`counts.test.ts`「真實文件與真實證據」那組；`tier2-security.yml` 的註解不守 —— 它不是文件，改門檻
的人會一起看到那一步。

#### 五、不裁

- **`JSON.parse` 失敗沒接。** 壞掉的 `evidence.json` 現在是一個 stack trace，不是一句話。另一題。
- **`--root`**：§三。
- **`FRESHNESS_DAYS` 的值**：120 是 C36 的數字，本則只搬不改（AGENTS.md 規則二）。

#### 六、實測（基準 `9cccf86`，worktree `freshness-seam`）

改前，三處**逐一**無視（C180 M7 是三處一起）—— 每趟還原後 `git status` 乾淨：

| 變異                                | 紅       |
| ----------------------------------- | -------- |
| M1 `cli.ts:369` 沒有證據那格永遠 0  | **0**/74 |
| M2 `cli.ts:384` 過期那格永遠 warn   | **0**/74 |
| M3 `cli.ts:409` 舊格式那格永遠 warn | **0**/74 |

改後：

| 變異                                    | 紅                                      |
| --------------------------------------- | --------------------------------------- |
| A1 `judgeFreshness` 無視 `requireFresh` | **3**/94 —— 恰好三列，對上「三處」      |
| A2 `ageDays >` 改 `>=`                  | 第一趟 **0**/94 → 補邊界那條後 **1**/95 |
| A3 失敗檢查搬到過期之後                 | 1/94（只有順序那條，它是獨立的）        |
| A4 拿掉 `tests` 缺欄位的跳過            | 1/94                                    |
| A5 `cli.ts` 傳遞改成 `false`            | **0**/95 —— §三 預告的那個零            |

- `vpr ready`：一趟全綠（`READY_RC=0`，讀 log 尾）。
- 逐檔 `it` 數（量法同 C172 §七）：**84 檔 1582 → 85 檔 1603**（C181 基準）—— 只多一個檔
  `tools/exit-drill/tests/freshness.test.ts` 21 條，其餘 84 檔逐位相同。

#### 七、關係

- **C180 §五 第二項**：`--require-fresh` 那一個收掉；另三個（`--full`、`--csp`／`--sca`、
  `--print-probe`）照留 —— 它們沒有一張表可抽。
- **C180 §六 M7**：三處一起零紅，本則 §六 M1–M3 拆成三個零。
- **C149 §二**：drift 不 fail 那句原樣搬進 `freshness.ts`，表裡那列兩個旗標值都是 0。
- **C148 §七**：「幾天前答不出同一棵樹嗎」那段註解原樣搬。
- **C36**：120 天與 `tests` 缺欄位跳過的出處。
- **C168 §一**：§六 A1–A4 每條測試各有獨立變異；A2 第一趟零紅是補測試的依據。
- **C154 §三**：§四 不加絆線的理由。**C137 §一**：零閘門增減。**C43**：反向測試一條沒動。
- **C118**：§二 第 2 點射程含下半張表。
- **C141**：本則跳一號；那一號還沒進 `main`，字面不寫。
- **C173 §五**：`C183` 字面與本則同一個 commit。

### C184 — `exit-drill` 接 `--root`：C183 說「四處」、實測是兩個常數底下二十一次讀，而這支 CLI 在此之前沒有任何測試 spawn 過它 —— 退出面掃描零反向測試（2026-09-06）

⚠️ **量測基準：`55458ef`（C183 合併後的 `main`）。** 本則收掉 C183 §三「不加 `--root`」那句
（它自己寫了「等有第二個需要 `--root` 的理由再裁」，理由在 §一 第二項）。

⚠️⚠️ **本 PR 同時逐字補回 C182（161 行）。** #287 把它併進 `main`（`647140a`）之後，#288 的
commit `18c633f` 是接在 `647140a` 上的，而它的 `DECISIONS-2.md` 是 `647140a` **減掉整則 C182**
再加 C183 —— 我把分支重接到 `main` 時用自己那份檔案蓋掉了鄰居剛併進去的那一則。合併後
`main` 的 CI 綠：decision-ids 守的是「引用指得到」，而 C183 **刻意不寫 `C182` 的字面**
（C173 §五），於是一則零引用的裁決整則消失時沒有任何東西紅。抓到它的是本則 —— 寫下
「C182 已進 `main`」那句話，`vpr ready` 在本機紅了。驗法：現在的檔案 ＝ `647140a` 全文 ＋
`55458ef` 從 C183 起的尾巴，python 逐字比對，diff 是純新增。⚠️ 這是 `two-halves-never-met`
的第九件，形狀是新的：**兩半見過面（同一條線、同一個檔尾），而重接分支那一步把對方那半換成
了自己記憶裡的版本** —— 重接之後要驗「新檔是不是舊 `main` 的超集」，不能只看 `git status` 乾淨。

#### 一、事實：二十一次讀、十四組、兩個常數，而「四處」是少算的

C183 §三 說接 `--root` 要動 `coveredPaths`、`currentFingerprint`、`EVIDENCE_PATH`、掃描目錄四處。
`rtk proxy grep` 全盤（rtk 的 `grep` 會漏行）：`ROOT`／`EVIDENCE_PATH` 兩個常數（`cli.ts:87–88`）
底下 **21 次讀**，按「答的是什麼」分 14 組 —— 預設模式 9 組（證據檔、`SCAN_DIRS` 掃描、
`EXIT_SURFACE` 兩份設定、workspace 清單、可達性起點、測試相依帳目、`coveredPaths`、
`git ls-files` ＋ 逐檔 hash、`DOCUMENTS_CITING_EVIDENCE`），`--full` 再 5 組（catalog、執行期
相依、`apps/console/dist`、兩處 `cpSync`、`vp fmt` 的 `cwd`）。**工作量沒有隨數字變大**：
21 次全部只經過那兩個常數，接旗標是改一個三元式。

**第二個理由（C183 §三 要的）**：`tools/exit-drill/tests` 六支測試檔**全是純函式**，沒有一支
spawn 這支 CLI。C183 §三 寫「spawn 它只能跑真樹」—— 更正：**沒有東西 spawn 它**。同一件事的
另一面：退出面掃描（`runStatic` 的 `leaks.push`，這道閘門存在的理由）**零反向測試**。
改前把它刪掉，零紅（§六 M1）。這不是「`--require-fresh` 那一行」的親戚，是這道閘門自己的
C43 反向測試從來沒有 —— `conformance` 檔頭那段「`--root` 不是為了彈性，是為了能被反向測試」
逐字適用。

C127 §一 的問題（哪些基準點是工具自己的素材、不隨 `--root` 走）：**這支沒有 SELF**。
沒有 `fixtures/`；證據檔是被驗的對象不是素材（沙盒要放一份 121 天前的證據才做得出差分）。
看起來像 SELF 的兩處（`vp fmt` 要真樹的 `node_modules`、`dist` 是真樹的建置產物）都只在 `--full`。

檔頭 `cli.ts:51` 寫著 `--static（預設）`，spec 裡沒有這個旗標：傳它被 C126 拒絕。幻影旗標。

外圍：根層 `specs/` 沒有場景點名 exit-drill，promise-check 的 `--root` 探針（C124）不受牽動；
gate-kit 的 `sandbox({ git: true })` 已有四支測試在用；adoption 那條「不認得的旗標一律紅」
對 17 支通用。加上這支，讀 `--root` 的 CLI 今天實測是 **8 支**（`gates.ts` threshold-check 條目
裡「第三支」是 C163 當時的序數，不追改 —— 散文裡的序數是快照）。

#### 二、裁決

1. **14 組全部隨 `--root` 走。** `ROOT` 改由旗標決定（`kind: "value"`，同其他七支），
   `EVIDENCE_PATH` 跟著。不開第二個基準點。
2. **`--full` ＋ `--root` 拒絕，紅。** 三個選項：(a) 拒絕；(b) 照 C127 §三 跑靜態半、印一行
   「建置半沒跑」；(c) 允許演練跑在副本上。(c) 的 `vp fmt`／`dist` 在副本裡沒有答案；(b) 的先例
   是一個命令兩半，而這支的兩半已經是兩個旗標 —— `--full` 之下靜默降成靜態正是 C124 說的
   「頂著名字回綠」。訊息一句話：`--full 只跑真樹`。
3. **指紋清單照走 `git ls-files`，不加 `SANDBOXED`、不跳過。** conformance 跳檔案模式的理由是
   「那個問題在副本裡沒有答案」；這裡 `sandbox({ git: true })` 讓副本就是版控，有答案。
   副作用是對的：非 git 的副本走到 `empty` 那格 —— C149 為「列舉本身壞了」寫的那則紅燈，
   CLI 層在這之前一次都沒到過，這次有測試了。
4. **新檔 `tests/cli.test.ts` 七條**，走 gate-kit 的 `sandbox`／`runCli`，副本用 `copy:`
   （`apps/console`、`platform`、`features`、`vite.config.ts`，155 個版控檔）不手拼 —— plugin
   與相依帳目在靜態半前面，手拼的副本會先紅在那兩關：
   ★ 切片 import vite-plus → 1 且點名檔案；對照組乾淨副本 → 0 走完四關；★ 證據 121 天
   不帶 0／帶 1；沒有證據檔 不帶 0／帶 1；舊格式 `unrecorded` 經過副本的 `git ls-files`
   不帶 0／帶 1；非 git 副本兩種旗標都 1 且含「0 個檔案」；`--full --root` → 1。
   `--roo` 不重寫，adoption 已守。
5. **幻影 `--static` 同 PR 改成「（預設，不帶旗標）」**，用法區塊加 `--root` 一行。README 沒有
   旗標表，不動。
6. **`gates.ts:367` 那句序數不改**，本則 §一 記今天的數。

#### 三、更正 C183 §三 兩處

- 「四處」→ 兩個常數、21 次讀、14 組（§一）。少算不影響工作量，但影響「要動四處」那個判斷
  的重量 —— 它讀起來像要改四段邏輯，實際是改一行。
- 「spawn 它只能跑真樹」→ 沒有東西 spawn 它。A5 零紅的成因不是 `--root` 缺席，是 CLI 層
  零測試；`--root` 是讓那條測試寫得出來的前提，不是成因本身。

照 C136 §八 記在這裡，不改 C183。

#### 四、不加絆線

同 C183 §四：對象在內（這支自己的 `ROOT` 解析），C154 §三 兩軸落在測試不在絆線。§六 A4
（`ROOT` 無視旗標）是它的替身。

#### 五、不裁

- **允許 `--full` 跑在副本上**（§二 第 2 點的 (c)）：要先給 `vp fmt` 與 `dist` 各一個副本裡的
  答案，等有人要在沙盒裡驗 `--full` 再裁。C180 §五 的 `--full` 零本機測試那一項**照留**。
- **`JSON.parse` 失敗**（C183 §五 第一項）：現在沙盒寫得出一份壞掉的 `evidence.json` 了，
  但它仍是一個 stack trace。另一題，本則不順手。
- **drift 對 `tools/exit-drill/src` 的敏感度**：本 PR 改 `cli.ts` 讓真樹的指紋從 149 變 150
  個檔而只 warn —— C149 §二 刻意的，不動。

#### 六、實測（基準 `55458ef`，worktree `exit-drill-root`）

改前，`tools/exit-drill` 95 條，每趟還原後 `git status` 乾淨：

| 變異                                                | 紅       |
| --------------------------------------------------- | -------- |
| M1 `runStatic` 刪掉 `leaks.push`                    | **0**/95 |
| M2 `requireFresh: FLAGS["require-fresh"]` → `false` | **0**/95 |
| M3 `EVIDENCE_PATH` 指到不存在的檔                   | **0**/95 |
| 對照 `FRESHNESS_DAYS` 120 → 121                     | 1/95     |

改後（95 → 102 條）：

| 變異                                    | 紅                                                                                    |
| --------------------------------------- | ------------------------------------------------------------------------------------- |
| A1 同 M1                                | **1**/102                                                                             |
| A2 同 M2（C183 §六 A5 那個零）          | **3**/102                                                                             |
| A3 同 M3                                | **3**/102                                                                             |
| A4 `cli.ts:105` `ROOT` 無視旗標寫死真樹 | **5**/102 —— 活的兩條是對照組（真樹本來就綠）與 `--full --root`（拒絕在 `ROOT` 之前） |
| A5 `--full --root` 的拒絕拿掉           | 1/102                                                                                 |
| A6 `cli.ts:330` 列舉失敗回一個非空指紋  | 1/102                                                                                 |

- 真樹四種呼叫：不帶旗標 0、`--require-fresh` 0、`--roo` 紅（訊息列出三個旗標）、
  `--full --root` 紅。
- `vpr ready`：第一趟 **READY_RC=1** —— decision-ids 紅在本則寫的「C182 已進 `main`」，那是抓到
  C182 消失的那一刻（開頭）；補回後第二趟 **READY_RC=0**（讀 log 尾）。
- 逐檔 `it` 數（量法同 C172 §七）：**85 檔 1603 → 86 檔 1610**（C183 基準）—— 只多一個檔
  `tools/exit-drill/tests/cli.test.ts` 7 條，其餘 85 檔逐位相同。

#### 七、關係

- **C183 §三**：收掉「不加 `--root`」；§三 更正兩處。**C183 §五 第二項**：`--root` 那一項收掉。
- **C180 §五 第二項**：`--full` 那一個照留（§五）；`--require-fresh` 本機零測試自 C183 起
  分兩半，本則收掉剩下那一半（傳遞那一行）。
- **C127 §一**：基準點的問題問過，答案是「沒有 SELF」；**C127 §三**：`--full --root` 拒絕
  而不是「跑一半印一行」的理由。
- **C123 檔頭那段**（conformance）：「`--root` 是為了能被反向測試」逐字適用。
- **C124**：探針不受牽動（根層規格沒點名它）。
- **C149 §二**：`empty` 那格第一次在 CLI 層有測試；drift 只 warn 不動。
- **C126／C43**：`--roo` 由 adoption 守，反向測試一條沒動。
- **C168 §一**：§六 六顆變異各對應至少一條測試。**C154 §三**：§四。**C137 §一**：零閘門增減。
- **C136 §八**：§三 的更正不改舊則。**C173 §五**：`C184` 字面與本則同一個 commit。
- **C182**：整則補回，一字不改；**C141**：抓不到零引用的整則消失 —— 它守的是引用不是存在。

### C185 — `--full` 的本機測試：組證據與產設定各抽一個純函式，改前五顆變異零紅；而季排程上「產物與本 repo 同級」那一步從來沒有比過（2026-09-06）

⚠️ **量測基準：`48e1503`（#290 合併後的 `main`）。** 本則收掉 C180 §五 第二項的 `--full`
那一格（C184 §五 第一項寫「照留」，留的理由是「等有人要在沙盒裡驗 `--full`」—— 本則就是）。
`--full --root` 的拒絕**不動**：本機測試不是 spawn 這支 CLI 帶 `--full`，是把 `runFull` 裡
「怎麼組證據」與「產哪三份設定」抽出來，從介面測。

#### 一、事實：十四步、進真樹兩步、零測試十個函式

`--full` 從演練到寫證據依序動磁碟十四步：第 0 步先取指紋（`git ls-files` ＋ 逐檔 hash，刻意在
動任何東西之前）；第 1–7 步在 `os.tmpdir()`（`mkdtemp`、複製 `apps/console` 與每個可達套件並
各刪一份 `vite.config.ts`、產三份設定、`npm install`、複製 `platform/tsconfig`、`vite build`）；
第 8 步讀**真樹的** `apps/console/dist` 當比對基準；第 9–10 步 `vitest run --reporter=json`
再對帳、撈測試數；**第 11 步 `writeFileSync(EVIDENCE_PATH)`** 與**第 13 步 `vp fmt` 同一個檔**
是僅有的兩次真樹寫入，中間第 12 步 `rmSync(workdir)`。兩次寫入無條件發生：`passed === false`
也寫（刻意，CI `always()` 上傳）、`npm install` 失敗也寫（`tests: 0`、`result: fail`）、
`vp fmt` 失敗回 1 而未格式化的證據留在樹上。

**零測試的範圍**：`runFull`、`writeDrillWorkspace`、`compareArtifacts`、`totalCssBytes`、
`catalogVersions`、`listWorkspacePackages`、`reachableWorkspacePackages`、`cli.ts` 裡的
`runtimeDependencies`、`run`、`reconcile` 包裝 —— 十個函式沒有任何測試碰到。有測試的是抽出去的
六個模組。第 11 步「把這些湊成一份證據」整段在 `runFull` 裡：`steps.every` 換成 `some`、拿掉
「撈不到測試數就補一步失敗」的守衛、vitest 那份設定漏掉 plugin、alias 排序反轉、`vite` 裝到
catalog 那個 —— 五顆變異改前全零（§六）。這五件事每一件都有一段註解記著它發生過的樣子
（C148 §二 B 類、`.../slice-kit/src/index.ts/contract`、「整場演練的重點就在這一行」），
而註解不是測試。

**⚠️ 季排程上第 8 步是空轉的。** `.github/workflows/exit-drill.yml` 在 `--full` 之前**沒有建置**
`apps/console`，所以 CI 上 `compareArtifacts` 的 `reference === 0` 永遠成立，走「本 repo 尚無
建置產物可比對」那條 `ok: true`，`steps` 照樣登記「✓ 產物與本 repo 同級」—— C124 的「頂著名字
回綠」，而 `compareArtifacts` 檔頭寫著它存在的理由是三次「建置成功但產物是壞的、退出碼 0」。
緩解的事實：`evidence.json` 最近五份（2026-08-30 起，#212／#251／#266／#275／#280）全是本機跑完
開 PR 進來的，本機有 `dist`，所以 **`main` 上這份證據的比對是真的跑過的**；壞的是排程那條路。
本則在 worktree 裡真跑一次 `--full` 重現了它（§六：worktree 沒有 `dist`，那一行印的正是那句話）。

#### 二、裁決

1. **seam 開在兩個純函式，不是 executor 注入、不是放行 `--full --root`。** `src/evidence.ts` 的
   `assembleEvidence(outcome) → { evidence, steps, missingCounts }` 與 `src/drill-workspace.ts` 的
   `drillWorkspaceFiles(input) → { 三個檔名: 內容 }`；`runFull` 只剩 spawn 序列與兩次寫入。
   與 C183 同一個形狀（判斷進模組、CLI 讀與印）。executor 注入要靠旗標或環境變數才過得了
   行程邊界，等於在產品碼放一個測試模式（C124 那一類）；放行 `--full --root` 每條測試分鐘級
   還要外網。刪除測試：兩個函式刪掉，複雜度回到 `runFull` 一處，不是散掉。
2. **「撈不到測試數就補一步失敗」搬進 `assembleEvidence`。** 它是「`tests: 0` 而 `pass`」唯一的
   擋法，留在 `runFull` 就永遠零紅。規則改寫成「其餘每一步都過、`counts === null` → 補一列
   `撈取測試數` 失敗」：與原本的 `reconciled.ok && counts === null` 在 `result` 上等價（其他
   情況本來就是 fail），差別只在少印一列噪音。印輸出尾端 600 字元那段留在 CLI。
3. **`drillWorkspaceFiles` 自己排 alias。** 排序是「產出的設定對不對」的一部分，留在 CLI 就測不到。
   輸入陣列不就地改動。
4. **`UPSTREAM` 搬到 `drill-workspace.ts`、由 `evidence.ts` 引用；`EXIT_SURFACE` 留在 `cli.ts` 當
   參數傳入。** 前者是「演練裝什麼」，屬於產設定；後者靜態掃描也在用，搬走會把 `runStatic`
   一起拖進來。
5. **測試斷發生過的事，不做快照。** 兩份設定都含每個 `DRILL_PLUGINS` 的 import 與呼叫、兩份拿到
   同一組、alias 長的在前且一個不掉、`devDependencies.vite` 是 `UPSTREAM.vite` 且**不等於**
   catalog 那個、`DRILL_TEST_DEPENDENCIES` 每個都在；證據那邊：四步各紅一次都 fail、
   `counts === null` 且全過 → 多一列且 fail、已有一步紅則不多列、指紋原樣、秒數與日期。
   快照每次改 plugin 就過期然後被重生，等於沒守。
6. **季排程的空轉只記不修（§一 第三段）。** 修法有兩條：workflow 在 `--full` 前建置一次，或把
   `reference === 0` 改成失敗的一步。兩條都是改這道閘門在 CI 上的判定，不是本題；後者會讓每一次
   沒有 `dist` 的本機執行紅。另開票，票面要帶「最近五份證據都是本機跑的所以目前那格是真的」。

#### 三、更正

- **C184 §一** 說 `--full` 「再 5 組」讀真樹：catalog、執行期相依、`apps/console/dist`、兩處
  `cpSync`、`vp fmt` 的 `cwd`。數字不改，但**其中 `apps/console/dist` 那一組在季排程上讀到的
  永遠是「不存在」** —— C184 把它列成 SELF 的候選（「`dist` 是真樹的建置產物」）而沒有問
  「排程那棵真樹上有沒有」。C136 §八：不改舊則。

#### 四、絆線

無。本則加的是測試不是閘門（C137 §一）；「指紋在演練之前取」這條順序不變量純函式量不到，
仍靠 `runFull` 的註解，寫在 `evidence.ts` 檔頭。

#### 五、不裁

- **`replaced["vite-plus"] ?? "unknown"`**：壞在 `replaced` 這欄不在 `result`，稽核看得出來，而
  `catalog:` 缺項在 `runtimeDependencies` 已經會 throw。不加拒絕。
- **CLI 落檔那一步**（`writeDrillWorkspace` 三個檔少寫一個）：seam 的另一邊，沙盒進不去。
  §六 A8 零紅是這條界線的形狀，不是漏。
- **`compareArtifacts`／`totalCssBytes`／`catalogVersions`／`listWorkspacePackages`／
  `reachableWorkspacePackages`**：都讀真樹，各自要一個 `--root` 之下的 fixture 才測得到；
  §一 第三段那張票先裁 `dist` 要不要在排程上存在，再看 `compareArtifacts` 值不值得。
- **季排程第 8 步的修法**：§二 第 6 點。

#### 六、實測（基準 `48e1503`，worktree `drill-evidence-seam`）

改前，`tools/exit-drill` 102 條（⚠️ 第一趟量到 95：worktree 沒裝相依，`cli.test.ts` 整檔載不起來而
`numFailedTests` 是 0 —— 對照組也是 0 才看出來；`vp install` 之後重量），每趟還原後 `git status` 乾淨：

| 變異                                           | 紅        |
| ---------------------------------------------- | --------- |
| M1 `runFull` 的 `steps.every` → `some`         | **0**/102 |
| M2 拿掉「對帳過了卻撈不到測試數」那條守衛      | **0**/102 |
| M3 vitest 那份設定 `plugins: []`               | **0**/102 |
| M4 alias 排序反轉（短的在前）                  | **0**/102 |
| M5 `devDependencies.vite` 改裝 catalog 那個    | **0**/102 |
| 對照 `--full --root` 的拒絕拿掉（C184 §六 A5） | 1/102     |

改後（102 → 122 條）：

| 變異                                | 紅                                       |
| ----------------------------------- | ---------------------------------------- |
| A1 同 M1（`evidence.ts`）           | **5**/122                                |
| A2 同 M2（`missingCounts` 恆假）    | **1**/122                                |
| A3 同 M3（`drill-workspace.ts`）    | **2**/122                                |
| A4 同 M4                            | **2**/122                                |
| A5 同 M5                            | **1**/122                                |
| A6 alias 不排序                     | 2/122                                    |
| A7 vite 那份設定 `plugins: []`      | 2/122                                    |
| A8 CLI 落檔少寫 `vitest.config.mjs` | **0**/122 —— seam 的另一邊（§五 第二項） |
| 對照 同上                           | 1/122                                    |

- 真跑一次 `--full`（worktree，28 秒）：540 條／19 檔／5 條預期失敗與 `main` 的證據**逐位相同**，
  只有秒數與指紋變（149 → 152 個檔：`cli.ts` 加兩個新模組）；第 8 步印的是「本 repo 尚無建置產物
  可比對」而 `steps` 登記 ✓（§一 第三段）。跑完 `git checkout` 還原證據，本 PR 不重跑演練
  （C184 §五 第三項同一個理由：drift 只 warn）。
- `vpr ready`：第一趟紅在 oxfmt（`vp check --fix`）；第二趟 **READY_RC=1** 紅在 `threshold-check`
  「`max-lines-per-function` 設在 199，實測最大值 185」—— `runFull` 199 → 184 行；降成 185 後第三趟
  **READY_RC=0**（讀 log 尾）。
- 逐檔 `it` 數（量法同 C172 §七）：**86 檔 1610 → 89 檔 1635**（C184 基準）—— 本則加兩個檔
  `drill-workspace.test.ts` 11、`evidence.test.ts` 9；第三個 `tools/pii-check/tests/enumeration.test.ts`
  5 條是 #290 的，其餘 86 檔逐位相同。

#### 七、關係

- **C180 §五 第二項**：`--full` 那一格收掉；四個旗標剩 `--csp`／`--sca`、`--print-probe`。
- **C184 §五 第一項**：「等有人要在沙盒裡驗 `--full`」—— 是本則，但答案不是放行 `--full --root`；
  **C184 §一**：§三 更正。**C184 §二 第 2 點**：拒絕照舊。
- **C183**：同一個形狀的第二次（判斷表 → 純函式）；**C183 §五**：`JSON.parse` 那一項照留。
- **C148 §二 B 類**：vitest 那份設定漏 plugin，第一次有反向測試。**C148 §五**：對帳那一半的純
  函式早有測試，本則補的是它上面那層。
- **C149**：指紋原樣寫入有測試；順序不變量沒有（§四）。
- **C124**：「頂著名字回綠」—— 季排程第 8 步是一件新的實例，記在 §一。
- **C127 §一／§三**：`dist` 那個 SELF 候選在排程上不存在，是基準點問題的另一種答案。
- **C147 §二／C162**：`runFull` 從 199 行縮短，`max-lines-per-function` 依規必須跟著降，數字由
  `threshold-check` 報（§六）—— 這是它守的那一格第一次因為抽測試 seam 而動。
- **C168 §一**：§六 A1–A7 每顆至少一條測試。**C154 §三**：§四。**C137 §一**：零閘門增減。
- **C136 §八**：§三 不改舊則。**C173 §五**：`C185` 字面與本則同一個 commit。

### C186 — a11y 規則清單縮水：三支測試都綠是真的，而守它的是閘門的 baseline 比對 —— 真缺口是那條分支零反向測試，而且測試一直在改寫真樹的交付文件（2026-09-06，#296）

票 #296（地圖 #295 的子票，C168 附錄 C 第 1 條），基準 `575334d`，worktree `a11y-shrink`。
這張票開的是 `wayfinder:grilling`（HITL）；本則由監督 session 依使用者指示代為裁決，
子代理負責量測與實作，監督者逐項複核 —— 第一個子代理的量測表整張作廢（對照組沒紅、
結果檔不存在），下面的數字全部是監督者在同一個 worktree 重量的。

#### 一、事實

1. **規則清單不是手寫的。** `platform/eslint-config/src/a11y.js:50-53` 的 `ALL_RULES` 是
   `Object.keys(a11y.rules)` 全開，23 條全從 plugin 推導。所以「縮水」的真實路徑有兩條：
   上游升版拿掉一條規則、或本地加 `.filter`／`off` 覆寫 —— 不是「刪一行」。
2. **附錄 C 說的「三支都綠」成立。** 變異 M1（`ALL_RULES` 加 `.filter((name) => name !== "no-access-key")`）：
   `platform/eslint-config` 7/7、`tools/compliance` 65/65、`platform/ui` 全綠。
   `eslint-config/tests/a11y.test.ts` 的 `enabled` 與 `fired` 兩個集合都從同一份設定推導，
   少一條就一起少。對照組 C1（fixture `:70` 的 `<button accesskey="k">` 改成 `<button>`）
   → 7 條 1 紅（「★ fixture 觸發了每一條被啟用的規則」），量測台有反應。
3. **但「減的方向沒有守」不成立。** `tools/compliance/ACCESSIBILITY.md` 進版控（24 行含
   `vuejs-accessibility/`），`tools/compliance/src/cli.ts` 在驗證模式拿 `renderAccessibilityFormatted()`
   與版控那份逐字比。同一顆 M1 下 `node tools/compliance/src/cli.ts` → **RC=1
   「✗ ACCESSIBILITY.md 與 a11y.ts 不一致」**；乾淨樹 RC=0。守它的是 `vpr gate` 那條鏈上的
   閘門，絆線掛在被守的對象（交付文件）上，位置正確；`--update` 是唯一出口，而它會在
   版控的交付文件上留下一個少一行的 diff。
4. **C168 為什麼看漏：** 那次審查的量法是「從 import／讀檔目標推導」，只讀了測試檔；
   守這件事的比對住在 CLI 裡，不在任何一支 `*.test.ts`。與 C185 §一 同一個形狀的教訓反過來：
   那邊是步驟名印 ✓ 而輸入不存在，這邊是測試全綠而閘門有紅。
5. **真缺口 A —— 那條分支零反向測試（C43）。** `cli.ts` 的 `a11yBaseline` 寫死
   `join(ROOT, "tools/compliance/ACCESSIBILITY.md")`，`--file` 只導 `COMPLIANCE.md`。
   `tests/negative.test.ts`「手改 COMPLIANCE.md 會被抓到」那個 describe 碰不到它。
   變異 M2（刪掉「不一致 → return 1」那五行）：compliance 三支測試 **65/65 綠**。
   對照組 C2（刪 `COMPLIANCE.md` 那段比對）→ negative.test 1 紅。
6. **真缺口 B —— 測試一直在改寫真樹。** `--update` 分支的 `writeFileSync(a11yBaseline, …)`
   不看 `--file`。`negative.test.ts` 兩個 describe 跑 `--update --file <沙盒>` 時，沙盒只拿到
   `COMPLIANCE.md`，真樹 `tools/compliance/ACCESSIBILITY.md` 被重寫一次（實測 mtime
   `1788633608 → 1788634157`）。內容相同所以 `git status` 看不到 —— 這正是 C124 記的形狀：
   閘門看副本是逐規則的，同一支 CLI 一份文件走副本、另一份走真樹。

#### 二、裁決

1. **不加凍結清單，不動 `platform/eslint-config` 的任何測試。** 減的方向已由閘門守，
   位置對、出口唯一。在測試裡再放一份 23 條的字面清單，是同一條守衛的第二份副本 ——
   C168 §一 的判準（同一變異兩邊同紅、刪掉一邊複雜度消失）判它是真重複，而那一族裁決
   在刪的正是這種。這不是增減閘門：本則零閘門增減。
2. **`--file` 同時導 `ACCESSIBILITY.md`。** `A11Y_BASELINE` 取 `--file` 所在目錄的手足
   （`join(dirname(resolve(FLAGS.file)), "ACCESSIBILITY.md")`），沒給 `--file` 時維持真樹路徑。
   不加新旗標：`--file` 的意思從「COMPLIANCE.md 的路徑」變成「兩份交付文件所在」，
   說明文字跟著改。這一條同時關掉缺口 B。
3. **補兩條反向測試**，都在 `negative.test.ts` 既有的 describe 裡：(a) 沙盒 `--update --file`
   後從沙盒的 `ACCESSIBILITY.md` 刪一行 `vuejs-accessibility/…`，驗證模式要紅且說得出
   「ACCESSIBILITY.md 與 a11y.ts 不一致」—— 斷言前先確認有破壞對象；(b) 跑
   `--update --file <沙盒>` 前後真樹 `ACCESSIBILITY.md` 的內容與 mtime 都不變。
4. **`a11y.js` 檔頭補一句事實**：減的方向由 `tools/compliance/ACCESSIBILITY.md` 守、`--update`
   是唯一出口。檔頭原本只寫「新規則會自動進來、反向測試會紅」—— 那是加的方向；少了這一句，
   下一次審查會再問一次同樣的問題。
5. **不動門檻。** `threshold-check` 沒有話說（見 §四）。

#### 三、不裁

- **`compliance` 測試做 subset 檢查**（「產出 ⊇ 版控那份」）：與凍結清單同一個理由，第二份副本。
- **`--file` 拆成兩個旗標**：兩份文件在真樹裡就是同一個目錄的手足，沙盒裡沒有理由不是。
- **把「規則清單縮水」升成 `eslint-config` 自己的閘門**：那是增減閘門，C137 §一 不准在這裡論證，
  而且 §一 第 3 點證明不需要。
- **`form-control-has-label` 的 `off` 覆寫不在 `preFilterRules()` 的視野裡**：那是 #297 的題，本則不碰。
- **HITL 票由 session 代裁**這件事本身：使用者明示，記在前言，不另立規則。

#### 四、實測

改前（`575334d`）：

| 變異                                     | 對象                                   | 結果                                         |
| ---------------------------------------- | -------------------------------------- | -------------------------------------------- |
| M1 `ALL_RULES` 少一條                    | eslint-config／compliance／ui 三支測試 | 7/7、65/65、全綠                             |
| M1 同上                                  | `node tools/compliance/src/cli.ts`     | **RC=1** 不一致                              |
| M2 刪「ACCESSIBILITY.md 不一致 → 1」五行 | compliance 三支測試                    | 65/65 綠（缺口 A）                           |
| C1 fixture 少一個 `accesskey`            | eslint-config                          | 7 條 **1 紅**                                |
| C2 刪「COMPLIANCE.md 不一致 → 1」那段    | compliance negative                    | **1 紅**                                     |
| `--update --file <沙盒>`（舊 CLI）       | 真樹 `ACCESSIBILITY.md` mtime          | **變了**；沙盒只有 `COMPLIANCE.md`（缺口 B） |

改後：

| 變異                            | 對象                | 結果                                                       |
| ------------------------------- | ------------------- | ---------------------------------------------------------- |
| M2 同上                         | compliance（67 條） | **1 紅**：「★ 手改 ACCESSIBILITY.md 會被抓到」             |
| M3 `A11Y_BASELINE` 改回寫死真樹 | compliance（67 條） | **2 紅**：上面那條 ＋「repo 的 ACCESSIBILITY.md 不被動到」 |
| M1                              | compliance CLI      | RC=1 不變                                                  |
| C1                              | eslint-config       | 7 條 1 紅不變                                              |

`vpr ready`：兩趟。第一趟 RC=1，oxfmt 對本則與新測試各有意見 → `vp check --fix`；第二趟 RC=0。`threshold-check` 沒有話說。

淨變化：`tools/compliance` ＋2 條 `it`，`cli.ts` 一個常數換位置、零行為變更（沒給 `--file` 時路徑相同），
零閘門增減，零門檻變動。

#### 五、與既有裁決的關係（C136 §八）

- **C168 §一／附錄 C 第 1 條**：缺口的陳述一半成立（三支測試綠）、一半不成立（減的方向有閘門守）；
  本則不改 C168，用 §一 第 4 點寫下看漏的成因。
- **C43**：閘門的每一條紅分支要有反向測試 —— `ACCESSIBILITY.md` 那條在此之前是零。
- **C124**：閘門看副本是逐規則的 —— 這次是逐**文件**：同一支 CLI 的 `--file` 導了一份、沒導另一份，
  症狀與 C124 一樣是「副本上綠、真樹被碰」。
- **C170 §三 第 2 點**：那邊留下 `compliance --evidence` 那條測試是因為它是本機唯一跑到那個
  variant 的路徑；這邊是同一支工具的另一條分支，本機在此之前**零**路徑跑到它。
- **C178 §五**：`parseFile` 裡到不了的第二道缺值檢查 —— `A11Y_BASELINE` 一樣只讀 `FLAGS.file`，
  不另掃 argv。
- **C185 §一**：「步驟印 ✓ 而輸入不在」與本則「測試全綠而閘門有紅」是同一個教訓的兩面：
  讀一道守衛時要把它守的東西對到每一條會執行它的路徑。

### C187 — a11y 的範圍豁免沒進交付文件：文件列 23 條「實際檢查」而其中一條對設計系統的基元不跑 —— 讀設定那一行只看第一個區塊，加豁免、擴豁免、多關一條三顆變異閘門零紅（2026-09-06，#297）

票 #297（#295 地圖，C168 附錄 C 第 2 條），基準 `b4b8ae6`（C186 合併後的 `main`），worktree `a11y-exemption`。

#### 一、事實

1. **設定有兩個帶 `rules` 的區塊，交付文件只讀第一個。** `platform/eslint-config/src/a11y.js` 第 90–135 行是全域區塊（`files: ["**/*.vue"]`、`...ALL_RULES`），第 136–154 行是 `platform/ui` 的豁免（`files: ["platform/ui/src/components/**/*.vue"]`、`form-control-has-label: "off"`，檔內自陳「範圍刻意只有這個目錄」）。`tools/compliance/src/a11y.ts:144-152` 的 `preFilterRules()` 用 `.find()` 取第一個帶 `rules` 的區塊就 `return`，第二個從來沒被讀過。
2. **交付文件因此在樂觀的方向說謊。** `ACCESSIBILITY.md` 的「開發期的前置過濾器實際檢查什麼」列 23 條，其中 `form-control-has-label` 對 `platform/ui/src/components/` 底下 27 支 `.vue` 根本不跑 —— 而那份文件的反向測試檔頭（`tools/compliance/tests/a11y.test.ts:16-19`）點名要防的正是「宣稱開發期擋得掉、其實沒有東西在守」這個方向。
3. **三顆變異改前閘門零紅。** 在真樹的 `a11y.js` 上做：M1 尾巴加 `{ files: ["apps/**/*.vue"], rules: { "vuejs-accessibility/no-autofocus": "off" } }`；M2 把豁免的 `files` 擴成 `["**/*.vue"]`；M3 豁免區塊多關一條 `label-has-for`。三顆 `node tools/compliance/src/cli.ts` 都 RC=0、`ACCESSIBILITY.md` 一個字不變。M2 唯一紅的是 `platform/eslint-config` 的「fixture 觸發了每一條被啟用的規則」（fixture 自己被豁免了），那是開發端的絆線，交付文件那一頭仍然全綠。對照組 C1（`ALL_RULES` 少一條）如 C186 所述 RC=1 —— 減全域清單有人守，加範圍豁免沒有。
4. **C168 當時把它列成附錄 C 而不是缺口本體，理由與 C186 §一 第 4 點同：只讀了測試檔。** 測試檔說「規則清單是推導的不是抄的」，而推導只推了一個區塊。

#### 二、裁決

1. **`a11y.ts` 新增 `scopedOverrides(config)`：取第一個 `rules` 區塊之後每一個帶 `rules` 的區塊，每條規則產一列 `{ rule, files, setting }`。** `setting` 把 `"off"`／`0` 正規化成 `off`，其他原樣 `JSON.stringify` —— 把 `error` 降成 `warn` 也是覆寫，要進表。區塊沒有 `files` 就 `throw`：一個沒有範圍的第二個 `rules` 區塊會靜默蓋掉全域設定，讀不到範圍就不要給判決（與 `preFilterRules` 找不到 `rules` 時的立場相同）。
2. **交付文件多一節「哪些規則在哪些路徑被覆寫」，由 C186 那條 baseline 比對守。** 非空印 `| 規則 | 範圍（files） | 設定 |`；**空也要印一句「沒有範圍覆寫」** —— 拿掉一條豁免也要讓 baseline 動，否則「減豁免」又成了下一個暗處。表後兩句：理由留在 `a11y.js` 該區塊的註解裡不抄（抄本會過期）；範圍是 glob **字面**、從 repo 根錨定，印的是設定寫的字串不是它命中的檔案數。
3. **`preFilterRules` 與 `scopedOverrides` 都接受設定當參數，預設真設定。** 這是為了第 4 條：反向測試用參數注入做變異，不改真樹的 `a11y.js`、更不跑 `--update` —— C186 §二 之後測試不准改寫真樹的交付文件。
4. **反向測試三顆變異各一條（M1／M2／M3 注入後產出 `not.toBe` 真設定的產出，且含新的 glob／規則名），加一條對照（真設定 render 兩次 `toBe`）、一條「第二個區塊沒 `files` → throw」、一條「零覆寫 → 文件明說」。** 閘門那一頭不再加測試：產出變了 → baseline 紅，走的是 C186 那條已有沙盒測試的路。
5. **`a11y.js` 豁免區塊的註解補一句：改 `files` 或加規則之後要 `--update`，不然閘門紅。** 不改任何行為。

#### 三、不裁

- **代表路徑法**（對三條代表路徑各跑 ESLint 的 `calculateConfigForFile`，文件印各自的有效規則集）：要把 ESLint 拉進 `tools/compliance` 當執行期相依；「代表路徑」是杜撰的檔名，量不到真樹；而它答的是「某條路徑開了幾條」，不是這張票問的「豁免有沒有進交付文件」。
- **不裁、或只加反向測試**：兩者都讓文件繼續印 23 條「實際檢查」而其中一條對基元不跑 —— 那正是事實第 2 點的謊。
- **把 `a11y.js` 註解裡的理由抄進文件**：抄本會過期，而過期的理由比沒有理由更糟；文件指向註解的位置就夠。
- **印「實際命中幾個檔」而不印 glob 字面**：命中數會隨檔案增減漂移，而且 #282 正在改那條 glob 的錨定方式；印字面讓 #282 改完 `--update` 一次就跟上，不在它之前替它決定。
- **凍結一份豁免清單**：C186 §二 對規則清單否決過同形，理由不變。

#### 四、實測

改前（基準 `b4b8ae6`，真樹 `a11y.js` 逐顆改、量完還原）：

| 變異                     | compliance 測試 | eslint-config 測試 | 閘門 CLI |
| ------------------------ | --------------- | ------------------ | -------- |
| 真設定（對照）           | 67/67           | 7/7                | RC=0     |
| M1 加 `apps/**` off 區塊 | 67/67           | 7/7                | RC=0     |
| M2 豁免 files 擴成全樹   | 67/67           | 6/7                | RC=0     |
| M3 豁免區塊多關一條      | 67/67           | 7/7                | RC=0     |
| C1 `ALL_RULES` 少一條    | 66/67           | 7/7                | RC=1     |

改後（同一套改法）：

| 變異                     | compliance 測試 | eslint-config 測試 | 閘門 CLI |
| ------------------------ | --------------- | ------------------ | -------- |
| 真設定（對照）           | 75/75           | 7/7                | RC=0     |
| M1 加 `apps/**` off 區塊 | 74/75           | 7/7                | RC=1     |
| M2 豁免 files 擴成全樹   | 72/75           | 6/7                | RC=1     |
| M3 豁免區塊多關一條      | 73/75           | 7/7                | RC=1     |
| C1 `ALL_RULES` 少一條    | 74/75           | 7/7                | RC=1     |

改後三顆的紅都含「跑完之後 COMPLIANCE.md 仍與映射一致」那條（它跑真閘門，baseline 不符）。M2 多紅的兩條是注入式的 M2／M3 本身：真樹已經是 `**/*.vue` 時，注入同一個值當然與真設定相同 —— 那兩條紅是絆線掛在被守的對象上的證據，不是缺陷。M3 多紅的一條同理。

`vpr ready`：第二段實作者三趟全紅（oxfmt、兩個型別錯、量測迴圈中間跑的）；監督者接手後四趟 —— 第一趟紅在 `decision-ids`（裁決表把對照組命名成「C 加一個零」，被讀成裁決編號），第二趟 oxfmt，第三趟又是 `decision-ids`（描述第一趟紅因的那句話裡又寫了那個字面），第四趟綠。對照組不要用 C 開頭命名，連散文裡也不要。

#### 五、與既有裁決的關係

- **C186**：同一條 baseline，這一則只是讓它讀得到第二個以後的區塊；§二 第 3 條「測試不改寫真樹的交付文件」是本則第 3、4 條的來源。
- **C168 §一**：同一顆變異兩邊都紅 —— 改後 M1／M3 在測試與閘門兩頭都紅；附錄 C 第 2 條由此關閉。
- **C43**：加的是反向測試，不減任何閘門。
- **C154 §三**：對象在外（`platform/eslint-config` 的設定）、壞法安靜（文件照印 23 條）—— 兩軸都填，所以絆線要有。
- **#282**：那張票改 `a11y.js:152` 那條 glob 的根錨定。本則刻意印字面不印命中數，讓它改完 `--update` 一次就跟上；不掛 blocking。
- **C173 §五**：本則編號的字面與本則同一支 commit。

### C188 — CSP 指令被刪兩邊都綠：期望與伺服器同源，而「不得縮水」那句寫在守不到它的測試上 —— 刪 `form-action`（沒有 fallback 的那一條）零紅（2026-09-06，#298）

票 #298（#295 地圖，C168 附錄 C 第 3 條），基準 `bcc753f`（C187 合併後的 `main`），worktree `csp-expectation`。

#### 一、事實

1. **消費鏈三處，期望與伺服器同源。** `platform/security-headers/src/policy.ts:25-57` 的 `BASE_DIRECTIVES`（13 條）→ `platform/bff-mock/src/server.ts:302` 用 `buildSecurityHeaders` 送標頭 → `tools/bff-check/tests/contract.test.ts:269` 拿 `Object.keys(BASE_DIRECTIVES)` 當期望逐條 `toContain`。刪一條指令，mock 少送一條、測試少查一條，綠。`BFF_ORIGIN` 指向真 gateway 那條跑法也一樣：期望值仍是同一份 `BASE_DIRECTIVES`。
2. **`contract.test.ts:268` 那句守不到自己。** 註解寫「指令的涵蓋範圍不得縮水」，而那條測試只在「伺服器送的少於期望」時紅；期望跟著政策縮，它永遠不紅。
3. **`policy.test.ts` 具名的只有七條的值**：`frame-ancestors`／`base-uri`／`object-src`（第 65 行 `it.each`）、`connect-src`（第 73 行）、`style-src`／`style-src-attr`／`script-src`（`'unsafe-inline'` 那組）。其餘六條 —— `default-src`、`img-src`、`font-src`、`form-action`、`worker-src`、`manifest-src` —— 刪掉零紅。
4. **刪 `form-action` 兩邊全綠，而它不是 `manifest-src` 那種等價刪法。** CSP 的 fetch 指令（`img-src`、`font-src`、`worker-src`、`manifest-src`…）缺席時退回 `default-src 'self'`，刪掉等價；`base-uri`、`form-action`、`frame-ancestors` **沒有 fallback**，刪掉的語意是「不設限」—— `form-action` 刪掉就是表單可以 POST 到任何來源。三條裡它是改前唯一沒具名的那條。實測刪它：`platform/security-headers` 39/39 綠、`tools/bff-check` 24/24 綠（含 `contract.test.ts` 15 條）。
5. **樹上沒有任何交付文件列 CSP 指令。** `tools/compliance/COMPLIANCE.md`、`src/map.ts`、`HANDOFF.md`、README 都只引 `@org/security-headers` 為單一來源，沒有一份印出清單。
6. **`bff-check` 在 `vpr ready` 鏈上有跑。** `vp run -r test` 的 log 有 `~/tools/bff-check$ vp test` 與 `tests/contract.test.ts (15 tests)`。第一段查證回報「不跑」，是錯的；本則不沿用。

#### 二、裁決

1. **期望值放進 `platform/security-headers/tests/policy.test.ts`，一條具名全清單：`expect(Object.keys(BASE_DIRECTIVES).sort()).toEqual([13 個名字])`。** 刪一條、加一條都恰好這一處紅，而 `toEqual` 的 diff 直接印出是哪一條。這是刻意的凍結：政策是這棵樹手寫的、減的方向零守，「加一條要來這裡登記」正是 review 會看到的動作 —— 與 `UNSAFE_INLINE_ALLOWED_IN` 存在的理由相同。
2. **三條沒有 fallback 的指令各自在場，另一條測試。** 全清單那條紅的時候 diff 是一整份清單；這一條紅的時候訊息只有一個名字，而且把「為什麼這三條刪不得」的理由釘在測試上，不是留在讀 CSP 規格的人腦裡。
3. **`bff-check` 的 `csp-on-document` 程式碼不動，只改註解。** 它的期望與政策同源是**對的**：那條 seam 的兩個 adapter（mock 與真 gateway）都該對同一份政策，它守的是傳輸那一半。拿掉「涵蓋範圍不得縮水」那句，改成「政策本身不得縮水由 `@org/security-headers` 自己的測試守」。
4. **`policy.ts` 的 `BASE_DIRECTIVES` 檔頭補一句**：加減指令要同步改測試的具名清單，減的方向沒有別的東西在守。

#### 三、不裁

- **把凍結清單放進 `bff-check`**：刪一條指令會紅在錯的 seam，而 `BFF_ORIGIN` 真 gateway 那條跑法會把「你們的 gateway 沒送 X」與「政策少了 X」混成同一個紅。
- **doc-facts 型（某份文件列指令、測試對文件）**：事實第 5 點 —— 沒有讀者的文件只是凍結清單換個地方放。
- **改成 baseline 檔比對（C186 那種）**：C186 那條有 `ACCESSIBILITY.md` 這個交付文件在讀 baseline；這裡沒有。
- **刪掉 `bff-check` 的同源比對**：它守的是傳輸那一半，C43。
- **與 C186 §二 否決的凍結清單是不是同一件事**：不是。a11y 那份清單從外掛推導（升級自動進新規則，凍結會逼每次升級改測試），而且減的方向已有 baseline 比對在守；`BASE_DIRECTIVES` 手寫、減的方向零守，具名清單就是那個釘子。

#### 四、實測

改前（基準 `bcc753f`，真的改 `policy.ts`、量完還原）：

| 變異                            | policy.test | bff-check |
| ------------------------------- | ----------- | --------- |
| 真設定（對照）                  | 39/39       | 24/24     |
| 刪 `form-action`                | 39/39       | 24/24     |
| 刪 `manifest-src`               | 39/39       | 24/24     |
| 刪 `frame-ancestors`            | 38/39       | 24/24     |
| `script-src` 加 `'unsafe-eval'` | 35/39       | 24/24     |
| 加 `media-src`                  | 39/39       | 24/24     |

改後：

| 變異                            | policy.test | 紅的是哪幾條                                    | bff-check |
| ------------------------------- | ----------- | ----------------------------------------------- | --------- |
| 真設定（對照）                  | 41/41       | —                                               | 24/24     |
| 刪 `form-action`                | 39/41       | 全清單 ＋ 三條無 fallback                       | 24/24     |
| 刪 `manifest-src`               | 40/41       | 全清單                                          | 24/24     |
| 刪 `frame-ancestors`            | 38/41       | 原本那條 `it.each` ＋ 全清單 ＋ 三條無 fallback | 24/24     |
| `script-src` 加 `'unsafe-eval'` | 37/41       | 原本那四條，位置不變                            | 24/24     |
| 加 `media-src`                  | 40/41       | 全清單                                          | 24/24     |

`bff-check` 六列全綠是預期的：它的期望與政策同源，這一則沒有改它的判準。

`vpr ready`：第二段實作者第一趟 oxfmt 紅、`vp check --fix` 後第二趟 `READY_RC=0`；監督者改完註解與本則之後再跑一趟，見 PR。

#### 五、與既有裁決的關係

- **C168 §一**：判準「刪一條要在恰好一處紅、那一處說得出少了哪條」—— 改後是 `policy.test` 的全清單一處，diff 印名字；附錄 C 第 3 條由此關閉。
- **C43**：加的是反向測試，不減任何閘門。
- **C174 §一**：`bff-check` 仍是 BFF seam 的唯一 test surface；政策內容不是那條 seam，它的 test surface 是 `security-headers` 自己的測試。
- **C186 §二**：凍結清單的差別見 §三 最後一點。
- **C154 §三**：對象在外（mock 與真 gateway 都讀同一份政策）、壞法安靜（刪一條兩邊一起少）—— 兩軸都填，絆線要有。
- **D11**：單一事實來源不變，三個消費端仍讀同一份 `BASE_DIRECTIVES`；加的是它的釘子，不是第二份來源。
- **C173 §五**：本則編號的字面與本則同一支 commit。

### C189 — 覆蓋率門檻的 `enabled: true` 四份副本：範本那份有守，樹上三片零守 —— 一片自己一行就能把自己的門檻關掉而 `vpr ready` 綠；「根層那份整塊不繼承」講的是一個從來沒放過的東西（2026-09-06，#299）

票 #299（#295 地圖，C168 附錄 C 第 4＋5 條），基準 `1577345`（C188 合併後的 `main`），worktree `coverage-enabled`。

#### 一、事實

1. **四份副本的位置。** `features/invoice/vite.config.ts:55`、`features/order/vite.config.ts:53`、`features/shipment/vite.config.ts:53`、範本 `tools/slice-gen/src/files.ts:79`。每一份上面都抄了同一段「`enabled` 這一行才是門檻真的會跑的原因」。
2. **只有範本那份有守。** `tools/slice-gen/tests/coverage-gate.test.ts:105`「★ 覆蓋率預設開著」對**產生器輸出**做文字斷言；`contract-alignment.test.ts` 也只讀產生器輸出。樹上三片沒有任何東西讀它們的 `vite.config.ts`：`conformance` 對切片讀 `REQUIRED_FILES` 四項（`package.json`／`tsconfig.json`／`README.md`／`src/index.ts`，`platform/slice-kit/src/contract.ts:23-28`）與 `SOURCE_EXTENSIONS` 五種副檔名的原始碼（`tools/conformance/src/scan.ts:49-55`），不含 `vite.config.ts`。
3. **一片自己一行就能把自己的門檻關掉。** 拿掉 `features/order/vite.config.ts:53` 那行：`vpr ready` `READY_RC=0`，log 裡「Coverage enabled with v8」從 3 段變 2 段 —— `features/order` 的門檻**沒有跑**，不是跑了剛好過。整個 `coverage` 區塊刪掉同樣綠。`npx vitest run` 在該 package 單獨跑：RC=0、覆蓋率零行（真設定是 2 行）。拿掉範本那一行則 slice-gen 2/106 紅（對照組正常）。
4. **這正是 AGENTS.md 規則二的形狀。** 「一個可以被受檢者自己調鬆的門檻，量到的只是它自己」—— `threshold-check` 守的是根層 lint 門檻不得調鬆，覆蓋率門檻住在切片自己的檔裡（C120 §一 刻意），而它的開關沒有人守。
5. **兩段散文講的是一個從來沒放過的東西。** `features/order/vite.config.ts:8-9`（`features/shipment` 同段）「根層那份的 `test` 區塊就整塊不繼承」、`apps/console/vite.config.ts:72-73`「根層 `vite.config.ts` 的 `test` 區塊整塊不繼承」—— 根層 `vite.config.ts` 現在沒有 `test` 區塊，`git log -S` 查歷史也從來沒有過。那兩段是 C120 §一 的機制理由（就算放了也不繼承），讀起來卻像在描述曾經存在的設定。同段的「100%」「13.20%」是 #130 當時的量測記錄，有出處，不是現況斷言。

#### 二、裁決

1. **`conformance` 的 `slice-shape` 加一條 `checkCoverageGate`：切片的 `vite.config.ts` 必須存在、去註解後要有 `coverage:` 區塊、`enabled: true`、並引用 `USECASE_COVERAGE_GLOB` 與 `USECASE_COVERAGE_MIN`。** 缺哪一項各自一條 finding，點名切片與缺項。放 `conformance` 而不是 slice-gen 的測試：slice-gen 的測試只在這棵樹的開發迴圈跑、只看產生器；`conformance` 是 tier1 閘門鏈上逐片判「這是不是一片合格的切片」的那一支，採用團隊 fork 後手改切片、在他們的樹上跑的是它。C120 §一 已裁門檻必須住在切片自己的檔裡，所以「那支檔在、門檻開著、數字從契約取」是切片形狀的一部分，與 `checkRequiredFiles`／`checkSliceTests` 同一類 —— 同一個對象多問一句，多讀一支檔。
2. **文字比對，不 import 設定物件。** `conformance` 跑在 bare node，import 切片的 `vite.config.ts` 會把 `vite-plus` 的 `defineConfig` 與 `@vitejs/plugin-vue` 拉進閘門的執行期；`coverage-gate.test.ts:105` 對範本也是文字比對，同一把尺。先去掉 `//` 與 `/* */` 註解再比對 —— 這幾支檔的註解裡本來就有 `enabled` 這個字。
3. **反向測試五條，掛在被守的對象上。** `enabled: true` 改 `false`、整個 `coverage` 區塊刪掉、沒有 `vite.config.ts`、`enabled: true` 只留在註解裡（去註解那步的反向測試）→ 各自紅、訊息含缺項與切片名；對照：把真樹 `features/order/vite.config.ts` 原文放進沙盒 → 綠 —— 規則要是嚴到連真的那份都過不了，前四條紅就沒有意義。
4. **散文三處各改一句。** 「根層那份的 `test` 區塊就整塊不繼承」改成「根層**刻意不放** `test` 區塊；就算放了，一個有自己 `vite.config.ts` 的 package 也整塊不繼承」。「100%」「13.20%」「#130」一字不動。
5. **`REQUIRED_FILES` 不動。** 它有 slice-gen `contract-shape.ts`、codemods 測試等消費者，加一項會漣漪到票外；缺檔由新規則自己回報。

#### 三、不裁

- **不另守、「靠範本生成就是答案」**：範本守的是產出那一刻，樹上的三片是人手改的；C154 §三 兩軸 —— 對象在外（採用團隊的切片）、壞法安靜（關掉門檻長得跟全綠一樣）—— 都填，絆線要有。第一段查證推薦不守的理由是「加規則等於新對象、成本大」，C137 §一 不准拿成本論證閘門增減，不採。
- **放 slice-gen 的 `contract-alignment` 讀真三片**：那是這棵樹的 package 測試，採用團隊的樹上不跑；而且它的職責是產生器。
- **加一條守「根層 `vite.config.ts` 沒有 `test` 區塊」**：對象在內（這棵樹自己的根層設定），C154 §三 一軸沒填；reviewer 讀 diff 看得到。
- **散文整段刪**：「100%」「13.20%」是有出處的量測記錄，刪了 C120 的理由就沒了。
- **散文原樣留**：「根層那份的 `test` 區塊」這個名詞指向一個不存在的東西，讀的人會去找它。

#### 四、實測

改前（基準 `1577345`，真的改檔、量完還原）：

| 變異                                   | `vpr ready` | 「Coverage enabled」段數 | conformance CLI | slice-gen |
| -------------------------------------- | ----------- | ------------------------ | --------------- | --------- |
| 真設定（對照）                         | RC=0        | 3                        | RC=0            | 106/106   |
| 刪 `features/order` 的 `enabled: true` | RC=0        | 2                        | RC=0            | 106/106   |
| 刪 `features/order` 整個 `coverage`    | RC=0        | 2                        | RC=0            | 106/106   |
| 刪範本 `files.ts:79`                   | RC=1        | 3                        | RC=0            | 104/106   |

改後（conformance 100 條測試全綠為對照）：

| 變異                                      | conformance CLI | 訊息                                     | slice-gen |
| ----------------------------------------- | --------------- | ---------------------------------------- | --------- |
| 真設定（對照）                            | RC=0            | —                                        | 106/106   |
| 刪 `features/order` 的 `enabled: true`    | RC=1            | `features/order` 沒有 `enabled: true`    | —         |
| 刪 `features/order` 整個 `coverage`       | RC=1            | 缺 `coverage` 區塊 ＋ 沒有 `enabled`     | —         |
| 刪 `features/invoice/vite.config.ts` 整支 | RC=1            | `features/invoice` 缺少 `vite.config.ts` | —         |
| `features/shipment` 門檻數字改字面 `90`   | RC=1            | 沒有引用 `USECASE_COVERAGE_MIN`          | —         |
| 刪範本 `files.ts:79`                      | RC=0            | 不是它的對象                             | 103/106   |

範本那顆改後 slice-gen 多紅一條（104 → 103），是 e2e 的「違規恰好一項」：產出的切片現在同時被 `conformance` 的新規則抓到，它的訊息說出了那一行。

`vpr ready`：第二段實作者第一趟 oxfmt 紅、第二趟綠；監督者改完規則、測試、散文與本則之後再跑一趟，見 PR。

#### 五、與既有裁決的關係

- **C120 §一**：門檻住在切片自己的檔裡，本則不搬它，守它的開關。
- **C119**：glob 與數字從契約取，本則把「有沒有引用契約常數」一起問。
- **C168 §一**：同一顆變異兩邊都紅 —— 拿掉 `enabled` 在 conformance CLI 與它的反向測試兩頭紅，訊息說得出哪一片；附錄 C 第 4、5 條由此關閉。
- **C154 §三**：對象在外、壞法安靜，兩軸都填。
- **C137 §一**：本則擴 `conformance` 射程（多讀一支檔）的理由是上面兩軸，不是成本；第一段用成本推薦不守，被本則否決。
- **C43**：新規則自帶五條反向測試。
- **AGENTS.md 規則二**：「可以被受檢者自己調鬆的門檻，量到的只是它自己」—— 本則是這句話在覆蓋率那一格的落地。
- **C173 §五**：本則編號的字面與本則同一支 commit。

### C190 — 兩支 eslint 走進鄰居工作樹：設定層只有黑名單可用，而讓它的不完整變吵的是**測試的期望值改問 git** —— 順帶量到票面沒寫的第三個紅（2026-09-06，#282）

票 #282 剩下的那一半（C182 §七 第一項「兩支 eslint 的機制」明寫另裁），基準 `c921578`。本則**不實作**（C169／C182 §九 的先例），實作留在 #282。

#### 一、事實

1. **兩支 eslint 都走進鄰居工作樹，而只有一支今天會紅。** 主 checkout 底下種一棵 `git worktree add --detach .claude/worktrees/<名>`，列舉數兩支都**恰好加倍**：根 `eslint.config.js` 285 → 570（鄰居貢獻 285，RC 仍 0），`a11y.js` 40 → 80（鄰居貢獻 40，**RC 0 → 1**）。a11y 那一條命中是 `.claude/worktrees/<名>/platform/ui/src/components/UiTextarea.vue:60` 的 `vuejs-accessibility/form-control-has-label` —— 與 #282 開票時完全同一條。根那支的 RC 0 就是 C182 §六 2「**綠不等於被排除**」的量測形式：它走進去了 285 個檔，只是內容沒有違規。
2. **成因是 `a11y.js:155` 的 `files: ["platform/ui/src/components/**/*.vue"]` 從 repo 根錨定。**（#282 票面寫的 `:148` 已過期，C187 在它上面加了行。）配得上 `platform/ui/…`，配不上 `.claude/worktrees/<名>/platform/ui/…`，於是 C84 刻意關掉的那條規則在副本上又被打開。
3. ⚠️ **票面沒寫的第三個紅：`platform/eslint-config` 自己的射程測試，有鄰居時就是紅的。** `tests/a11y.test.ts:120`「★ repo 裡每一個 `.vue` 都被掃到了」實跑 **70 vs 71**，差的**恰好一個檔**：鄰居樹裡那份 fixture 的複本。因為 `a11y.js:88` 的排除是 `**/tests/fixtures/a11y-violations.vue`（**萬用字元開頭**，連複本一起排除），而測試的期望值用 `path !== FIXTURE`（**絕對路徑相等**，排不掉複本）。#282 §二 那張 15 步表是對 `vpr gate` 的陳述而 `gate` 不含測試，所以它看不到這一格 —— 這個紅在 `vp test` 裡，也就是在 `vpr ready` 裡。
4. **同一個形狀第三次，而這次錨在路徑上的是「期望值」不是「豁免」。** #282 §三 把 `pii-check` 的 `EXEMPT`（完整路徑）與 a11y 的 `files:`（根錨定）並列成「路徑錨定的豁免對副本必然失效」；第三個實例是一條**期望值**：`vueFilesOnDisk` 走磁碟，`SKIP` 是手抄的 `node_modules`／`dist`／`coverage`／`.git`，**沒有 `.claude`**。C182 §六 1 讓 `pii-check` 的兩張手抄跳過清單退場，#290 落地；`a11y.test.ts:109` 是這棵樹的**第三份**。
5. **eslint 的設定層沒有「問 git」這個出口。** flat config 的 `ignores` 只吃字串樣式，不吃函式。所以 C182 對 `pii-check` 用的那個**結構性**答案（git 不走進另一個 repository，副本換位置也擋得住）在設定層買不到 —— 設定層能寫的只有一條路徑黑名單，而 C182 §五 正是以「黑名單每一列都要有人先被咬一次」否決過 `SKIP` 加一列 `.claude`。**這則裁決的重量全部落在這個差別上。**
6. **`.claude/` 在乾淨樹上零個檔被 lint。** 兩支設定的基線列舉裡 `.claude/` 底下都是 0；該目錄現在只有 `settings.local.json`、`launch.json` 與 `worktrees/`。排除它今天不會遮掉任何正在被檢查的東西。
7. **`tools/compliance` 把 a11y 設定當資料讀，而它對「多一個只有 `ignores` 的物件」不敏感。** `preFilterRules` 用 `.find()` 找第一個帶 `rules` 的區塊、`scopedOverrides` 從它之後往下掃並要求每個帶 `rules` 的區塊都有非空 `files`（C187 立的）。實測把 `{ ignores: [...] }` 插在陣列的**頭、中、尾**三個位置：`rules=23`、`overrides=1`，三次一致（§四）。
8. **package 自己宣告了「兩份互不相干」。** `platform/eslint-config/package.json` 的 `description` 與 `a11y.js:7–12` 的整段論證：a11y 不得併進 `src/index.js` 那個陣列，否則就把 Tier 2「與 oxlint 零重疊、亮起來就一定是真的」那個性質換掉了。`exports` 今天只有 `.` 與 `./a11y`。

#### 二、裁決

1. **兩支 eslint 的對象都改成「這個 checkout」，不是「這個目錄樹」。** 具體做法是在兩份設定各自的陣列裡排除巢狀工作樹的慣例位置，樣式 `**/.claude/worktrees/**`。⚠️ **這不是規則二說的「改門檻求綠燈」** —— 沿用 C182 §六 那句話：規則二 管的是**判準**，本則動的是**對象**。23 條規則、`label-has-for` 的選項、`files:` 的豁免範圍、根設定的安全規則集，一個字都不動。
2. **樣式定義一次，兩份設定各自 import。** 在 `platform/eslint-config` 新增一支只放這個邊界的模組與一條對應的 subpath export，兩份設定把它放進自己的陣列。**不得**塞進 `src/index.js` 的匯出陣列讓 `a11y` 去繼承，也**不得**從 `./a11y` 匯出給根設定用 —— 那兩個方向都在 §一 8 的宣告上開洞。理由不是整潔：C189 才量過同一個字面在四處、三處沒有東西比對它的下場，而這棵樹已經把「鄰居工作樹住在 `.claude/worktrees/`」寫在 `.gitignore:33`，本則會讓它變成第二、第三份。
3. ⚠️ **萬用字元開頭在這裡不是承重的，寫下來免得被讀成承重。** §四 的 basePath 表：從 package 那一端跑時，鄰居樹本來就落在 basePath 之外，`isPathIgnored` 現在就回 `true` —— 那**不是排除，是涵蓋**。所以 C182 §七 那條「兩個呼叫端都要成立」在本則是**由涵蓋滿足的**，不是由樣式形狀滿足的。前綴照寫（成本為零，且符合 `a11y.js:82–86` 記下的教訓），但不得拿它當「兩端都測過了」的證據。
4. **射程測試的期望值改問 git**，退掉 `vueFilesOnDisk` 的磁碟走訪與那張手抄 `SKIP`：`git ls-files` ∪ `git ls-files --others --exclude-standard`，濾 `.vue`，扣掉 fixture 那一支。⚠️ **這一步與第 1 點是同一種動作（換對象），而它不相依於第 1 點。** 測試現在的 70 vs 71 是**它自己判準下的真紅**（eslint 真的排除了複本的 fixture、磁碟走訪真的收了它），而那個紅在**設定一個字都沒改**的 `c921578` 上就量得到（§一 3）—— 壞的是它問的對象：「磁碟上每一個 `.vue`」而不是「這個 checkout 裡每一個 `.vue`」。⚠️ 第 1 點會讓它**更紅**（35 vs 71，見 §四），也就是把它從「該修」推成「非修不可」—— 但**不是它需要修的原因**，所以實作時不得因為第 1 點延後或省略這一步。
5. **`:143` 的 `expect(linted.length).toBeGreaterThan(0)` 留著，不換成任何下限數字。** 期望值改成一份會動的清單之後，「兩側同時歸零」照樣是相等的 —— 那一行是那個地板。不比照 `pii-check` 的 `MINIMUM_SCANNED` 另立一個數（C182 §七 也沒有動它）。
6. **零閘門增減**（C137 §一）。`vpr gate` 那條鏈一支不碰，`package.json:15` 與 CI 的 `eslint .` 那一行都不動。

#### 三、不裁

- **把 `files:` 改成萬用字元開頭** —— **明文否決**。那會讓豁免連副本一起涵蓋、症狀當場變綠，而它動的是**判準**（規則二 管的正是這一格），還直撞 `a11y.js:145–146` 自己寫的「範圍刻意只有這個目錄：切片與應用畫面裡的 `<input>`／`<textarea>` 仍然被這條規則守著」。而且它一點都不解決根設定那 285 個檔。
- **呼叫端 `--ignore-pattern`（由 `git worktree list` 算出）。** 它是「問 git」在**呼叫層**的版本，不是手抄黑名單，這一點比第二點強。不採用的理由是接不到全部呼叫端：`tests/a11y.test.ts` 走 `ESLint` API 不走 CLI，拿不到旗標；代價還包括動 `package.json:15` 與 `eslint.config.js:4` 記著的那行 CI 命令。**寫在這裡，是因為它是唯一一個結構性的候選，不要當作沒想過。**
- **在設定載入時由 `git worktree list` 算出 `ignores`。** 設定是 JS，技術上做得到。不採用：那會讓一份**被別人 import 當資料讀**的共用設定在 import 時跑 git 子行程（`tools/compliance/src/a11y.ts:1` 就是那個 import），並且讓設定的內容隨機器而變。
- **另外四支從子目錄走磁碟的**（theme-verify、vue-typecheck、supply-chain、threshold-check）維持 **C182 §七** 不裁。
- **#282 §四 第 1 點的「共用的樹的邊界」不由本則回答。** 本則換的是**一支測試的期望值**，不是替所有走磁碟的工具立一個共用邊界。那個問題留在 #282。
- **樹裡的純目錄副本要不要繼承豁免。** §四 量到：一份 `cp -r` 的副本落在版控裡（未追蹤、未 ignore），問 git 的期望值與 eslint 兩側**一致**（62 vs 62，射程測試綠），而 a11y 閘門 **RC 1**。按本則的判準那是**真紅** —— 那份副本確實在這個 checkout 裡，而豁免確實只給那一個目錄。沒有人裁過「副本要不要繼承豁免」，本則不裁，遇到再說。
- **禁止巢狀 worktree** 已由 **C182 §六 3** 裁過（不禁止，要改的是工具），本則不重議。

#### 四、實測

基準 `c921578`。探針是 `git worktree add --detach .claude/worktrees/<名> main`；「加排除」一律以包住真設定的探針設定跑，**樹一個字都沒改**。

**（1）列舉與閘門**

| 設定                  | 無鄰居 | 有一棵鄰居 | 其中在鄰居樹裡 | RC        |
| --------------------- | ------ | ---------- | -------------- | --------- |
| 根 `eslint.config.js` | 285    | 570        | 285            | 0 → **0** |
| `a11y.js`             | 40     | 80         | 40             | 0 → **1** |
| 根，加排除            | —      | 285        | **0**          | 0         |
| `a11y.js`，加排除     | —      | 40         | **0**          | 0         |

**（2）射程測試（`.vue` 條數，有一棵鄰居）**

| eslint 側 | 期望值側         | 值       | 差集                      | 測試     |
| --------- | ---------------- | -------- | ------------------------- | -------- |
| 現況      | 磁碟走訪（現況） | 70 vs 71 | 1（鄰居樹裡那份 fixture） | **紅**   |
| 加排除    | 磁碟走訪（現況） | 35 vs 71 | 36                        | **更紅** |
| 加排除    | **問 git**       | 35 vs 35 | **0**                     | 綠       |

**（3）三種副本形狀（都已加排除）**

| 副本形狀                     | 位置                 | eslint | 問 git | 射程測試          | a11y 閘門 |
| ---------------------------- | -------------------- | ------ | ------ | ----------------- | --------- |
| 巢狀樹（`git worktree add`） | `.claude/worktrees/` | 35     | 35     | 綠                | RC 0      |
| 巢狀樹（`git worktree add`） | **黑名單之外**       | 70     | 35     | **紅（差集 35）** | RC 1      |
| 純目錄副本（27 個 `.vue`）   | 樹裡任意位置         | 62     | 62     | 綠                | RC 1      |

⚠️ **這張表是第 2 點與 C182 §五 那個張力的答案。** 黑名單漏掉的位置（第二列）由問 git 的期望值變成**吵的**；純目錄副本（第三列）兩側一致而閘門紅，那是判準下的真紅（§三 最後一項）。也就是說**沒有一格是安靜的** —— 這正是 C182 §五 要求黑名單付出的東西，而它由**另一支機制**付，不是由黑名單自己付。

**（4）basePath：兩個呼叫端**

| 設定   | cwd     | fixture | `some-other.vue` | 鄰居樹裡的檔                |
| ------ | ------- | ------- | ---------------- | --------------------------- |
| 現況   | repo 根 | 排除    | 不排除           | **不排除** ← 症狀           |
| 現況   | package | 排除    | 不排除           | 排除 ← **是 basePath 之外** |
| 加排除 | repo 根 | 排除    | 不排除           | 排除                        |
| 加排除 | package | 排除    | 不排除           | 排除                        |

第二列是本則 §二 3 的來源：那一格今天就是「排除」，而它跟設定無關。

**（5）對照組**

| 方向     | 對照                                                | 讀數                                |
| -------- | --------------------------------------------------- | ----------------------------------- |
| 已知非零 | 種一個未追蹤的射程內 `.vue`（鄰居仍在）             | 兩側同時 35 → **36**，差集 0        |
| 已知為零 | 拿掉鄰居                                            | 兩側都留在 **35**，差集 0           |
| 已知非零 | compliance 解析器餵一個沒有 `files` 的 `rules` 區塊 | **丟例外**（C187 那條守衛還活著）   |
| 已知為零 | `{ ignores }` 物件插在陣列的頭／中／尾              | `rules=23`、`overrides=1`，三次一致 |
| 已知為零 | `.claude/` 底下有幾個檔在乾淨樹被 lint              | 兩支設定都 **0**                    |

`node tools/compliance/src/cli.ts` 在基準上 RC=0。量完後 `git worktree list` 回到一棵、`git status --porcelain` 回到 0 行（以 `rtk proxy` 取，見 §六 末項）。

#### 五、絆線（C154 §三 兩軸，本則先填，實作留在 #282）

**它要斷言什麼** —— 兩條，落在 `platform/eslint-config/tests/`，不依賴 `vp`（D2；`eslint.config.js:3` 自己就寫了這條）：

1. **行為，不是文字**：對**兩份**設定、在**兩個** cwd（repo 根與本 package）問 `isPathIgnored`，一個 `.claude/worktrees/…` 底下的路徑必須是排除的。不斷言陣列裡有那個字串 —— 那樣改一個字就能讓斷言與現實脫鉤。
2. **射程相等**：`a11y.js` 對 repo 根列舉到的 `.vue`，必須逐條等於問 git 的那份清單。⚠️ 這一條同時是第 1 條的守衛：黑名單漏掉一個副本位置時，它會紅（§四（3）第二列，差集 35）。

**對照組要三條**（少任何一條都會讓一個壞實作全綠）：種一個未追蹤的射程內 `.vue` → 兩側同時 +1（已知非零）；拿掉鄰居 → 數字不變（已知為零）；`:143` 的 `toBeGreaterThan(0)` 留著 → 兩側同時歸零時仍然紅。

**交付軸** —— 無。**迭代軸** —— ① 對象在外：它報的是兩道閘門的射程，不是它自己；② 壞法安靜：CI 是乾淨 checkout，這一整類結構性看不到，而發作的方向是「本機紅、CI 綠」—— 最容易教人忽略紅燈的方向。兩條都成立，與 C182 §九 同一組。

#### 六、與既有裁決的關係

- **C182**：本則是它 §七 第一項的續集，**不改它一個字**（C138：凍結不搬遷，寫續集）。§六 1 的「問 git」在本則只用在**期望值**那一側，設定那一側買不到（§一 5）。§六 2「綠不等於被排除」在 §四（1）拿到了讀數（根 570／RC 0）。§六 3「不禁止巢狀 worktree」本則沿用。**§五 的黑名單否決不被本則推翻** —— 本則的黑名單靠 §四（3）那張表付代價。
- **C84**：`form-control-has-label` 關掉的理由，本則不動它。
- **C187**：`scopedOverrides` 那條「沒有 `files` 就丟例外」是本則 §四（5）的已知非零對照組；本則的形狀提案不觸發它。
- **C189**：同一個字面在多處而沒有東西比對它 —— §二 2 拒絕製造第二、第三份的理由。
- **C137 §一**：零閘門增減；§二 2 的理由是 C189 的量測，不是可維護性。
- **C154 §三**：§五 兩軸。
- **C169／C182 §九**：裁決與實作分開，實作留在 #282。
- **C41**：「開發機紅、CI 綠」，本則是它在 eslint 上的實例。
- **AGENTS.md 規則二**：§二 1 動對象不動判準；§三 第一項是規則二 真正管得到的那一格。
- ⚠️ **量測工具**：本則所有 `git status` 讀數以 `rtk proxy` 取 —— rtk 在工作區乾淨時把 `--porcelain` 的空輸出改寫成一個字 `ok`，`| grep -c .` 因此回 1。

### C191 — 「這個 checkout 到哪裡為止」今天有四種機制在答，而其中兩種不是它們自稱的那一種：`supply-chain` 靠的是點規則不是起點，`vp lint` 靠的是 `.gitignore:33`（2026-09-06，#282）

⚠️ **量測基準 `fbdc901`**（C190 的實作 #305 合併後的 `main`）。本則回答 **#282 §四 1**「每一支掃磁碟的工具是不是要有一個共用的『樹的邊界』」—— C190 §三 明文把它留在票上。本則**不實作**（C169／C182 §九／C190 的先例：#304 裁、#305 做），實作與交回給人的兩項留在 #282。

⚠️ **量測條件比 #282 開票時更苛：主 checkout 底下有 3 棵鄰居工作樹**（`git worktree add` 的，各在自己的分支、都在 `fbdc901`），不是票面那 1 棵。

#### 一、先講結果：票面那兩個紅，在今天的樹上都不發作了

15 步逐支跑（不是 `vpr gate`：它是 `&&` 串的，第 9 步就中斷），**全部 RC 0**。其中票面點名的三步：

| 步驟                           | #282 開票時（1 棵鄰居） | 今天（3 棵鄰居）             |
| ------------------------------ | ----------------------- | ---------------------------- |
| 9 `pii-check`                  | **RC 1**（9 項）        | RC 0（`✓ 掃了 117 個檔案`）  |
| 14 `eslint .`                  | RC 0（走進去了）        | RC 0（**被排除**，C190 §四） |
| 15 `eslint --config a11y.js .` | **RC 1**（1 項）        | RC 0                         |

⚠️ **這不是本則的功勞，是 C182（#290）與 C190（#305）的。** 寫在這裡是因為 #282 的原始症狀該有一次收尾讀數，而它的條件比重現步驟寫的更強。

#### 二、事實：四種機制，逐支量過，而 C182 §三 的表有一格歸錯

C182 §三 把走磁碟的工具分成「從子目錄走（進不去）」與「從 repo 根走（會進去）」兩列。逐支短路它們自己的規則之後，實際上是**四種**機制，而且分類不同：

| 機制                       | 誰                                                                                                                         | 是什麼                                  |
| -------------------------- | -------------------------------------------------------------------------------------------------------------------------- | --------------------------------------- |
| **問 git**                 | scope-check、api-surface、doc-facts、conformance、gate-roster、promise-check、exit-drill、**pii-check**（C182 §六 1 之後） | **結構性**：git 不走進另一個 repository |
| **起點在子目錄**           | theme-verify、vue-typecheck                                                                                                | **結構性**：`.claude` 不在起點底下      |
| **手寫的點目錄規則**       | **supply-chain 的 `directDependencies`**                                                                                   | **判斷** —— 而它是**從 repo 根走的**    |
| **`.gitignore:33` 那一行** | **oxlint**（`vp lint` ＝ `vp check` 第 1 步，以及 threshold-check 的符號連結農場）                                         | **一條字面**，而樹上沒有一處記過這件事  |
| （設定層的黑名單）         | 兩支 eslint                                                                                                                | `worktrees.js:50`，C190 §二 2 裁的      |

三個新事實，逐條都有短路量測（§四）：

1. **`supply-chain` 的 `directDependencies()` 是 `walk(ROOT)`** —— 從 repo 根走。C182 §三 把它列在「從子目錄走磁碟 → 不會（`.claude` 不在它們的起點底下）」，**那個理由對它不成立**。今天擋住它的是 `entry.name.startsWith(".")` 那一行。⚠️ 照 C138 **不回頭改 C182 一個字**，本則是它的續集。
2. **而那條點規則的分佈，正是 C182 §五 否決 `skipDotDirs` 時量到的同一個 N=1**：短路它，走訪到的 `package.json` 由 **37 → 148**，多出來的 **111 個全部在 `.claude/worktrees/` 底下，別的點目錄一個都沒有**。
3. **`supply-chain` 今天答案不變，是因為它回的是「名字的集合」而鄰居是同一棵樹的副本。** 短路點規則之後，直接相依的名單兩邊都是 **33 個、差集空**。⚠️ 這是 C182 §六 2「**綠不等於被排除**」的第三個實例：它走進去了 111 份 manifest，只是那些名字它本來就有。已知非零對照組（fixture：鄰居的 `package.json` 多一個 `zeta-from-neighbour`）→ 短路版多收一個名字，而那個名字會走到 `health.ts:167` 的 `roster-drift`，那條檢查自己寫著「**必須是紅的，而不是靜靜略過**」。也就是**本機紅、CI 綠**，同一個形狀第四次 —— 但**今天不發作**（見 §三 第 2 點）。
4. **`.gitignore:33` 有第二個職務。** threshold-check 的農場**刻意不連 `.git`**（`probe.ts:56` 的 `SKIP`），而 oxlint 在**沒有 `.git`** 的情況下照樣讀 `.gitignore`。**只刪掉那一行**（其餘 76 行原封不動）：`vp lint --debug=files` 由 **298 → 1192**，其中 **895 條在鄰居樹裡**。對照組：那一行在的時候 298 條、`.claude` 命中 **0**。
5. **而那一行被拿掉時，threshold-check 不會紅。** 它比的是「農場掃到的」對「真樹掃到的」（`realFiles`／`probeFiles`），兩側**同時**膨脹就對得起來；而鄰居是同一棵樹的副本，各格的**實測 max 不變**，`門檻 == 實測 max` 照樣成立。會變的只有它印出來的「N 個測得到的單位」（861／1273／…），而**沒有東西在比對那些數字**。
6. **theme-verify 與 vue-typecheck 是真的靠起點。** 短路它們自己的點規則，`.vue` 條數一格不動（36→36、5→5，`.claude` 底下 0）。C182 §三 對這兩支是對的。
7. **邊界字面今天有兩份可執行的定義**：`.gitignore:33`（`.claude/worktrees/`）與 `platform/eslint-config/src/worktrees.js:50`（`**/.claude/worktrees/**`）。**沒有任何東西比對它們。**（測試裡的 `a11y.test.ts:257`、`enumeration.test.ts:68/93/100` 是探針不是定義。）
8. ⚠️ **C182 §二 那 13 步的對照組，threshold-check 在裡面。** 它印的是帶計數的行（「861 個測得到的單位」等 11 格），所以「13 步輸出逐位元組相同」對它是有內容的陳述，不是又一格「綠不等於被排除」。這一點查過了，免得留一個問號。

#### 三、裁決

1. **不立共用的「樹的邊界」模組 —— #282 §四 1 的答案是「否」。** 理由是 §二 那張表：四種機制**答的不是同一個問題**。「問 git」答的是「版控認得的這棵樹」、「起點」答的是「這個子目錄底下」、oxlint 那一格答的是「`.gitignore` 說什麼」。把它們收攏成一個「共用的邊界」，等於替三個不同的問題發明一個共同的答案 —— 而樹上已經有兩則裁決記過這個代價的形狀（`walk.ts:5–13` 的「給聯集會讓 conformance 悄悄變弱」、`pii-check/src/tracked.ts:80–84` 的「三支問 git 的問題其實不一樣」）。⚠️ **那兩則談的是「共用列舉工具」不是「共用邊界」**，所以它們是旁證不是主論證；主論證是 §二 那張逐支量過的表。⚠️ 也不得拿「過度設計」來論證（C137 §一，兩個方向都不准）。
2. **`supply-chain` 要不要改問 git —— 停下來交回給人。** 依 **AGENTS.md 規則二** 最後那一句（「如果你判斷某個門檻確實訂錯了，停下來告訴人，不要自己改」）。⚠️ 攤開來講清楚，免得被讀成含糊：
   - **已經賺到的**：C182 §七 給它 不裁 的**理由**（起點決定的）經量測**不成立**（§二 1）。這是事實，本則寫下來。
   - **還沒賺到的**：C182 §七 的**門檻**是「**沒有事故在後面，不裁**」，而本則**沒有帶來事故** —— 今天的答案兩邊都是 33 個名字、差集空，§二 3 那個紅是我用 fixture **造**出來的，不是這棵樹上發生的。理由沒了不等於門檻過了。
   - **代價已經量好，等人拍板**：改成問 git 之後 `package.json` 兩側都是 **37 份**（§四），也就是「換對象不換判準」的第二個實例（C182 §六 1）。
   - ⚠️ **一併交回去的是一條通則**：「某則裁決 不裁 的理由被推翻了」**不是**重開所有 不裁 事項的通行證。本則刻意只把這一格擺上桌。
3. **`.gitignore:33` 的第二個職務要有東西守 —— 這一項本則裁「要做」，實作留 #282。** 理由不是整潔：§二 4／5 量到那一行是 oxlint 列舉射程的**唯一**防線，而拿掉它**沒有任何東西會紅**（§二 5）。絆線的兩軸與形式見 §五。
4. **theme-verify、vue-typecheck 維持不裁** —— 而今天這句話背後是量測（§二 6），不是 C182 §三 那張表的推論。
5. **零閘門增減**（C137 §一）。`vpr gate` 那條鏈一支不碰，`package.json:15` 一個字不動，門檻一格不動。

#### 四、實測

基準 `fbdc901`，主 checkout 底下 3 棵鄰居工作樹。所有短路都在 scratchpad 的複製品上跑，**真樹一個字都沒改**；鄰居工作樹是別的 session 的，**只讀不碰**。

**（1）四種機制，短路它自己那條規則**

| 工具                              | 現況 | 短路後 | 多出來的在哪                                    | 判定                   |
| --------------------------------- | ---- | ------ | ----------------------------------------------- | ---------------------- |
| vue-typecheck（`.vue`）           | 36   | 36     | —                                               | 靠起點                 |
| theme-verify 消費端（`.vue`）     | 5    | 5      | —                                               | 靠起點                 |
| supply-chain（`package.json`）    | 37   | 148    | **111 全在 `.claude/worktrees/`；別的點目錄 0** | **靠點規則**           |
| oxlint（`vp lint --debug=files`） | 298  | 1192   | **895 在鄰居樹裡**                              | **靠 `.gitignore:33`** |

⚠️ oxlint 那一列的短路是「**只刪 `.gitignore` 第 33 行**」，其餘 76 行原封不動。整份刪掉的話是 346,212 條（`node_modules` 一起進來），那個數字**回答的是另一個問題**。

**（2）supply-chain：走進去了，而答案不變**

| 讀數                               | 有點規則 | 短路點規則       |
| ---------------------------------- | -------- | ---------------- |
| 走訪到的 `package.json`            | 37       | **148**          |
| 直接相依的名字                     | 33       | **33**（差集空） |
| **改問 git** 之後的 `package.json` | **37**   | —                |

**（3）對照組，兩個方向**

| 方向     | 對照                                                        | 讀數                                         |
| -------- | ----------------------------------------------------------- | -------------------------------------------- |
| 已知非零 | fixture：鄰居的 `package.json` 多一個 `zeta-from-neighbour` | 短路版多收該名字，現況版沒有                 |
| 已知非零 | 只刪 `.gitignore:33`                                        | oxlint 298 → **1192**                        |
| 已知為零 | `.gitignore:33` 在的時候，oxlint 掃到幾條 `.claude/…`       | **0**                                        |
| 已知為零 | 短路 theme-verify／vue-typecheck 的點規則                   | 數字一格不動                                 |
| 已知非零 | 15 步逐支跑（3 棵鄰居）                                     | 全 RC 0，而 #282 開票時同樣的步驟有兩步 RC 1 |

**（4）量測工具的兩個陷阱，本則踩到的**

- `git worktree list` **不經 `rtk proxy` 只回一行**（主 checkout），`rtk proxy` 回四行。**假的乾淨讀數**，而它決定了「這棵樹上有沒有鄰居」這個前提。
- `grep -v <樣式> 檔 > 新檔` 被 rtk 改寫成一句摘要，新檔只有 3 行（原檔 77 行）。第一版的「只刪第 33 行」因此是假的，改用 `sed '33d'` 才對。

#### 五、絆線（C154 §三 兩軸，本則先填，實作留 #282）

**守的對象**：`.gitignore:33` 對 **oxlint 列舉射程**的那個職務（§三 3）。

**它要斷言什麼**：**這條邊界對 oxlint 仍然有效**。⚠️ 形式上有兩個候選，本則裁**必須選在乾淨 checkout 上答得出來的那一種** —— 這是 C190 §五 1 立的標準（問的是 pattern 不是磁碟上的檔案），而 #282 §四 3 抱怨的正是「CI 結構性看不到這一整類」：

| 候選                                                                         | 掛在對象上？ | 乾淨 checkout 答得出來？                  |
| ---------------------------------------------------------------------------- | ------------ | ----------------------------------------- |
| 真樹跑 `vp lint --debug=files`，斷言 0 條在邊界內                            | **是**       | **否 —— 恆真**（CI 沒有鄰居，接不住刪除） |
| `git check-ignore` 問那條路徑                                                | 否，是代理   | **是**（pattern 問題，路徑不必存在）      |
| 合成樹：暫存目錄放這棵樹的 `.gitignore` ＋ 一個邊界底下的檔，在那裡跑 oxlint | **是**       | **是**                                    |

⚠️ **第一個候選是陷阱**：它看起來最紮實，而它在唯一會跑 CI 的那棵樹上**永遠是綠的** —— 就是 C182 §二 記的「綠不等於被排除」換一個位置再演一次。

**對照組要兩條**：拿掉那一行 → 紅（已知非零，§四（2）298 → 1192 是它的讀數）；那一行在 → 綠且**確實掃到東西**（已知為零的方向要配一個非零的總數，少了它，一支「永遠回 0 條」的實作也會通過）。

**交付軸** —— 無。**迭代軸** —— ① 對象在外：它報的是 `vp check` 與 threshold-check 的列舉射程，不是它自己；② 壞法安靜：§二 5 量到那一行被拿掉時 threshold-check **不會紅**（兩側同時膨脹、實測 max 不變），變的只有沒人比對的計數。兩條都成立，與 C182 §九／C190 §五 同一組。

⚠️ **這條絆線接不住的那一格，寫下來**：它守的是「這棵樹的 `.gitignore` 有沒有這條邊界」，接不住「**oxlint 哪天不再讀 `.gitignore`**」。那是上游行為變更，而今天樹上沒有任何東西在看它。

#### 六、#282 的去留

**不關。** 它現在守著三件具名的事：

1. **§三 2**：`supply-chain` 的 `directDependencies` 要不要改問 git —— **交回給人**（規則二）。
2. **§三 3**：`.gitignore:33` 那條絆線的實作。
3. **C190 §三** 留下的「純目錄副本要不要繼承豁免」（本則沿用不裁）。

⚠️ 而 **#282 §四 2、3 已經有答案了**，寫下來免得下一個人重跑：§四 2 由 **C182 §六 3** 答（不禁止巢狀 worktree，要改的是工具）；§四 3 由 `pii-check/tests/enumeration.test.ts` 與 C190 §五 那 4 條絆線答（它們問的是 pattern，乾淨 checkout 答得出來）—— 「**沒有任何閘門在守這件事**」今天不成立。

#### 七、與既有裁決的關係

- **C182**：本則是它 §三 那張表的續集，**不改它一個字**（C138）。§六 1 的「問 git」在本則是 §三 2 交回去的那一格；§六 2「綠不等於被排除」在 §二 3 拿到第三個實例（supply-chain 走進去了 111 份 manifest 而答案不變）；§五 對 `skipDotDirs` 的 N=1 判定在 §二 2 拿到第二個分佈。§七「另外四支不裁」本則只動其中一支的**理由**，不動它的**門檻**。
- **C190**：本則是它 §三 第五項（「#282 §四 1 不由本則回答」）的續集。§五 1 立的「乾淨 checkout 答得出來」標準是本則 §五 那張表的判準。
- **C41**：「開發機紅、CI 綠」；本則量到的第四個候選位置是 supply-chain，而它今天沒發作。
- **C137 §一**：零閘門增減；§三 1 明文不拿「過度設計」論證。
- **C138**：凍結不搬遷 —— C182 §三 歸錯的那一格寫在這裡，不回頭改。
- **C154 §三**：§五 兩軸。
- **C169／C182 §九／C190**：裁決與實作分開。
- **C189**：§二 7 的兩份可執行定義沒有東西比對它們 —— 而本則**不因此立共用模組**（§三 1），只讓其中一份被守（§三 3）。
- **D2**：§五 第一、三個候選都要跑 `vp lint`。D2 的條文管的是**CI 安全閘門**不經 `vp`，而 threshold-check 已經是本機閘門跑 `vp lint` 的先例（`probe.ts` 檔頭寫了為什麼非它不可：C150 §六 —— 繞過 `vp` 拿到的是預設集）。
- **AGENTS.md 規則二**：§三 2 是它最後那一句的直接適用。
- ⚠️ **量測工具**：§四（4）兩條。`git worktree list` 那一條是**假的乾淨讀數**，而它決定了本則所有量測的前提。

### C192 — `checkSliceTests` 零反向：`hasTestFile` 改成恆 true 時 conformance 自己 100 條全綠、對真樹 RC=0 分不出來 —— 五條反向各對一種壞法，`.spec.ts` 那條釘的是今天的射程（2026-09-06，#308）

C172 §五 登記給「下一次審查」的一件。⚠️ **量測基準 `fbdc901`，worktree `slice-tests-negative`**；行號與計數凍結在那一支，改後的數字另標。

#### 一、事實

- `tools/conformance/src/scan.ts:29` `hasTestFile(dir)` 三條分支：`dir/tests` 不存在 → false；遞迴走子目錄；檔名 `endsWith(".test.ts")` → true，否則走完回 false。`rules/slice-shape.ts:65` `checkSliceTests` 只叫它一次，finding 是「測試／找不到任何 tests/**/*.test.ts」；`rules/slice.ts:53` 接在每片切片的規則鏈上。
- `tools/conformance/tests/` 四支裡零處提到 `hasTestFile`／`checkSliceTests`（`git grep`）。`report.test.ts:31,55` 的「測試」是字串標籤，不是那條規則。
- 真樹三片（`git ls-files features/*/tests`）：invoice 一支 `.test.ts`＋`specs/invoice.spec.ts`＋`support/*.ts`；order 兩支 `.test.ts`；shipment 一支。全部在 `tests/` 第一層，沒有 `.test.tsx`／`.test.js`。

**改前變異表**（`tools/conformance` 的 vitest JSON `numTotalTests/numFailedTests`；CLI 對真樹的 RC 由監督者重量，第一段子代理把 M2 寫成 0）：

| 變異                                          | vitest                                     | CLI 對真樹 |
| --------------------------------------------- | ------------------------------------------ | ---------- |
| 對照（不動）                                  | 100／0                                     | 0          |
| M1 `hasTestFile` → `return true`              | 100／0                                     | 0          |
| M2 `hasTestFile` → `return false`             | 100／14（副本對照組與其他規則的 ★ 一起紅） | **1**      |
| M3 `endsWith(".test.ts")` → `endsWith(".ts")` | 100／0                                     | 0          |
| M4 拿掉遞迴（只看第一層）                     | 100／0                                     | 0          |
| M5 `checkSliceTests` → `[]`                   | 100／0                                     | 0          |

M2 是量測台的對照：它證明「規則會紅」這件事看得見；M1／M3／M4／M5 四顆零紅證明沒有東西在守規則**還活著**。真樹每片都有測試，所以 CLI 對真樹分不出 M1 與對照 —— 這條規則在版控的樹上永遠綠，壞掉也綠。

#### 二、裁決

1. **補反向，不動規則**（C43；C137 §一）。`tools/conformance/tests/negative.test.ts` 加一個 describe，沿用同檔的 `makeSandbox()`／`runConformance()`（C189 的形狀），副本裡只動 `features/order`，`shipment` 留著當同一趟的綠對照。
2. 五條各對一條分支：`tests/` 整個不在 → 紅、輸出含那句與 `order`；`tests/` 只剩一支非測試的 `helper.ts` → 紅（副檔名分支，M3 那種放寬在這裡綠）；**只有 `tests/specs/order.spec.ts` → 紅**；`.test.ts` 全搬進 `tests/nested/` → 綠（遞迴分支）；副本原樣 → 綠。
3. **`.spec.ts` 那條釘的是今天的射程，不是裁射程。** C172 §五 明文「認不認 `.spec.ts`」是閘門規則的射程、沒有相依方、不開票。這條測試存在是為了讓射程改變的那一天看得見 —— 到時它要一起改，註解寫了。

#### 三、不裁

- `hasTestFile` 認 `.spec.ts`：C172 §五 那個理由今天仍成立（`slice-gen` 產的切片同時有兩種）。
- 訊息「找不到任何 tests/**/*.test.ts」與行為一致，不動。
- `.test.tsx`／`.test.js` 要不要釘：`TESTING.md` 沒有定義層 2 的副檔名射程，樹上零支，不釘 —— 釘了是自己發明一條射程。

#### 四、實測（改後，`5d380ae` 之前的 `762be82`＋監督者改註解）

| 變異 | vitest  | 紅的是哪幾條                       |
| ---- | ------- | ---------------------------------- |
| 對照 | 105／0  | —                                  |
| M1   | 105／3  | 不在、只剩 helper、只有 `.spec.ts` |
| M2   | 105／16 | 改前那 14 ＋ 遞迴那條 ＋ 副本原樣  |
| M3   | 105／2  | 只剩 helper、只有 `.spec.ts`       |
| M4   | 105／1  | 遞迴那條                           |
| M5   | 105／3  | 同 M1                              |

四顆零紅的變異各至少一條紅，而且紅的正是設計上對應的那條。`vpr ready` 一趟綠（`READY_RC=0`，監督者跑）；子代理報告說的 `ready.log`／`after-M*.json` 在它說的路徑上不存在，本表是監督者重量的。

#### 五、與既有裁決的關係（C136 §八）

| 既有              | 本則                                                                    |
| ----------------- | ----------------------------------------------------------------------- |
| **C172 §五**      | 做掉它登記的第二件；第一件（`.spec.ts` 射程）與第三件（`NODE_ENV`）不動 |
| **C189**          | 同檔同形：沙盒五條＋真樹對照                                            |
| **C43／C137 §一** | 遵守 —— 只加測試碼，規則零改動                                          |
| **C154 §四**      | 對象在內（conformance 自己的規則），不填兩軸                            |
| **C168 §一**      | 判準照用：M1～M5 改前零紅、改後各紅，是「同一顆變異」那一格             |

### C193 — 示範切片的權限碼只靠字面釘著，第三片加進來那天 `invoice:read` 就缺了而零紅；契約特異性斷言的上界 15 從寫下那天就大於條數，恆真 —— 絆線改讀跑起來的 mock，斷言改成紅的集合恰等於預期（2026-09-06，#309）

C174 §五 記下不裁的兩件。⚠️ **量測基準 `fbdc901`，worktree `mock-permissions-tripwire`**；行號與計數凍結在那一支，改後另標。

#### 一、事實

**(a) 權限碼。** `platform/bff-mock/src/server.ts:41` `MOCK_PERMISSIONS = ["order:read", "shipment:read"]`；`apps/console/bff-routes.ts:87` `extraPermissions = ["order:cancel"]`。切片宣告（`features/*/src/index.ts:16`）：order 兩個、shipment 一個、**invoice `["invoice:read"]`** —— invoice 是 `0645fba`（#260）加的第三片，兩邊都沒有它。mock 也沒有 `/api/invoice`（`server.ts:437,448` 只有 orders／shipment）。跑起來的 console 裡那片到不了，是 C39 的形狀；`bff-routes.ts:84` 那句「只有兩片示範切片」寫的是 N=2 那天的事實（記憶「N=1 的時候整批與逐份長得一樣」，這次是 2 變 3）。

守它的只有 `platform/bff-mock/tests/routes.test.ts:206-208` 三個字面 `toContain`，註解說「示範切片改權限碼名時 `MOCK_PERMISSIONS` 必須跟著改」—— 靠人記得。

| 改前變異                                        | bff-mock | console | bff-check | `vpr gate` |
| ----------------------------------------------- | -------- | ------- | --------- | ---------- |
| 對照                                            | 11／0    | 6／0    | 24／0     | 0          |
| A1 `features/order` `order:read` → `order:view` | 11／0    | 6／0    | 24／0     | 0          |
| A2 `MOCK_PERMISSIONS` 拿掉 `shipment:read`      | 11／1    | 6／0    | 24／0     | 0          |
| A3 `extraPermissions = []`                      | 11／0    | 6／1    | 24／0     | 0          |

A1 就是票面說的安靜：切片改了名，三個 package 與閘門鏈全綠，本機那條路由從此 403。（第一段子代理量的；A2／A3 是對照。）

**(b) 契約特異性。** `tools/bff-check/tests/negative.test.ts:52` `CONTRACT_ITEM_COUNT = 15`，`:310` `expect(result.failed.length).toBeLessThan(15)`，意圖是「全紅代表 proxy 把整台伺服器弄壞了，不是這條 break 生效」。而 `failed` 是從輸出抓 `[id]` 的 Set，`contract.test.ts` 的 `contract()` 恰 **13** 次（`rtk proxy grep -c`），`CONTRACT_ITEMS` 13 條 —— `13 < 15` **恆真**。`git log -S` 找到它是 `33d93f1`（2026-08-16，#8）寫下的，那一天 `contract()` 12 次、條目 13 條：**15 從來沒對過**。

而把上界改成從契約取（13）也接不住：監督者實測一顆「全部回 500 ＋ 拿掉安全標頭 ＋ 清掉 cookie」的 break，紅的是 **12** 條 —— `same-origin` 對 proxy 永遠綠 —— `12 < 13` 照樣通過。所以問題不是數字抄錯，是「小於全部」這個形狀對這個接縫量不到東西。

#### 二、裁決

**(a)** 絆線住 `apps/console/tests/bff-routes.test.ts`，**來源是跑起來的 mock**：spawn 根 `package.json` 那條 `bff` script 同一條路（`node platform/bff-mock/src/cli.ts`，`BFF_MOCK_ROUTES` 取自 script、`BFF_MOCK_PORT=0` 讓 OS 配埠），打 `POST /api/session`，`registerFeatures(features).permissions` 每一個都要在回的 `permissions` 裡，缺的時候訊息說出那個碼。為什麼不 import `startBffMock`：這個 app 刻意不相依 `@org/bff-mock`（`bff-routes.ts` 檔頭：正式環境的 app 不該知道 mock 存在）；為什麼不抄一份 `MOCK_PERMISSIONS`：抄字面的話兩邊一起改錯仍綠 —— 第二段子代理第一版就是抄了三個字面，監督者改掉。

`invoice:read` 放 `MOCK_PERMISSIONS`：mock 的角色是「示範切片的唯讀權限」，invoice 是示範切片；`extraPermissions` 是 app 自己補的非唯讀權限（`order:cancel`）。`bff-routes.ts:84` 那句改掉數字。`routes.test.ts:206-208` 三個字面留（C174 §四：那是對的紅，守的是 mock 自己的預設），註解改指向新絆線。

**(b)** 刪 `CONTRACT_ITEM_COUNT`，`:310` 改成 `expect(sorted(failed)).toEqual(sorted(expected))` —— 紅的要**恰好**是這顆 break 該弄紅的那幾條。實測九顆 break 有八顆本來就恰好；「不送安全標頭」多紅 `csp-on-document`，CSP 本來就是安全標頭之一，把它加進那顆的 `expected`。等號同時擋住「proxy 弄壞整台」（12 ≠ 1）與「一顆 break 順手弄壞別條」兩種形狀，兩者都是原斷言接不住的。

兩件一則：同一個來源（C174 §五），同一支 PR，而且都是「靠字面／靠手抄」那一類。

#### 三、不裁

- **β**（`platform/bff-mock` 讀 `apps/console` 的 `extraPermissions`）：方向反了，platform 不能依賴 apps；第二個 app 出現時無解。
- **mock 沒有 `/api/invoice` 路由**：不是權限的事，是示範資料的事；**#311** 另問（放 mock 還是 console 注入、誰守「切片打的路徑 mock 都接得住」）。本則只把權限那一半補上，invoice 那片在本機仍然是 404 而不是 403 —— 症狀變了，還沒好。
- (a) 這條絆線是子行程型：Stryker 看不見它（`stryker.config.mjs` 檔頭），#307 那張票的排除名單要收它。

#### 四、實測（改後 `d1d0421`＋格式修正）

| 變異                                      | console | 紅的訊息                                            |
| ----------------------------------------- | ------- | --------------------------------------------------- |
| 對照                                      | 7／0    | —                                                   |
| A1 `order:read` → `order:view`            | 7／1    | 「切片宣告了 order:view，而 mock session 沒有它」   |
| A4 `MOCK_PERMISSIONS` 拿掉 `invoice:read` | 7／1    | 「切片宣告了 invoice:read，而 mock session 沒有它」 |

| (b) 探針（量完拿掉）                                                      | bff-check | 紅的訊息                                                 |
| ------------------------------------------------------------------------- | --------- | -------------------------------------------------------- |
| 全部回 500 ＋ 拿掉標頭 ＋ 清 cookie，`expected: ["401-unauthenticated"]`  | 紅        | 「預期紅的是 [401-unauthenticated]，實際紅的是：…12 條」 |
| `stripSecurityHeaders`，`expected: ["security-headers"]`（少寫 CSP 那條） | 紅        | 「實際紅的是：security-headers, csp-on-document」        |
| 改後正式九顆                                                              | 24／0     | —                                                        |

`vpr ready`：第一趟 oxfmt 紅（`vp check --fix`），第二趟綠。子代理第二段報告的 (b) 改後表寫「CONTRACT_ITEMS +1 → contract.test 新紅」—— 那是 README 同步那條，與特異性斷言無關；「全部回 500」它沒量，監督者量了才發現 13 也擋不住。

#### 五、與既有裁決的關係（C136 §八）

| 既有              | 本則                                                                               |
| ----------------- | ---------------------------------------------------------------------------------- |
| **C174 §四／§五** | §四 的「字面是對的紅」照留；§五 兩件本則做掉                                       |
| **C154 §三**      | (a) 對象在外（示範資料給採用團隊看腳手架怎麼跑）、壞法安靜（403 看起來像權限設定） |
| **C39**           | invoice 到不了是同一個形狀，路由那半交 #311                                        |
| **C173 §二**      | 「人抄的數字」的親戚，但這次抄對了也沒用 —— 形狀錯                                 |
| **C137 §一**      | 遵守：零閘門增減；(a) 加測試，(b) 改斷言形狀                                       |
| **C168 §一**      | 判準照用：A1 改前零紅、改後紅且說得出名字                                          |

### C194 — `stryker.config.mjs` 照原設定乾跑 3 秒就紅，而第一支紅的不是 C168 §八 點名的那兩支：讀真樹的測試有四支、`disableTypeChecks` 那一格是獨立的一件；四支改由版控內的 `vitest.stryker.config.ts` 排除、那一格關掉，`vue-typecheck` 回到射程（2026-09-07，#307）

C168 §五 裁「不排除」、§八 記下兩件沒修的紅。⚠️ **量測基準 `fbdc901`；最終驗證重接到 `291e2dc`（C193 之後）。clone 不是 worktree**（`inPlace` 會就地改寫，鄰居 worktree 共用 `.git`）；每一趟的 log 在票上列出。

#### 一、事實

**(a) 原設定的乾跑。** `pnpm exec stryker run --dryRunOnly`：`Found 136 of 451`，**3 秒**紅在 `tools/api-surface/tests/negative.test.ts`「對照組：沒動過的東西是綠的」—— `platform/` 的公開型別被插樁改寬（`Server<…>` → `Server<…> | undefined`、`"ForbiddenError"` → `"" | "ForbiddenError"`）。C168 §八 點名的是 `vue-typecheck` 與 `promise-check`；這一支是第三件，而 bail 之下先紅哪一支**不固定**（同一份設定另一趟先紅的是 `promise-check`）。

**(b) 讀真樹的測試有幾支 —— 盤的兩個方向都錯。** 「import `node:child_process` 或 `runCli`」盤出 22 支；實證（從零排除開始，紅一支加一支，直到綠）是 **4 支**，而 4 支裡有 1 支不在那 22 支裡：

| 排除                                         | 紅在哪                              | 為什麼                                                                                       |
| -------------------------------------------- | ----------------------------------- | -------------------------------------------------------------------------------------------- |
| `tools/api-surface/tests/negative.test.ts`   | 沒動過的東西是綠的                  | 插樁讓推導型別變寬                                                                           |
| `tools/promise-check/tests/negative.test.ts` | 真規格本身是綠的                    | 規格跑 `threshold-check`，插樁檔複雜度過門檻；**spawn 在產品碼裡，測試檔零 `child_process`** |
| `tools/promise-check/tests/cli.test.ts`      | 版控裡的承諾全部成立時回傳 0        | 同上，CLI 那一面                                                                             |
| `tools/gate-kit/tests/adoption.test.ts`      | `process.argv` 每支 cli.ts 只讀一次 | C180 的量法數原始碼字面；插樁把那個運算式複製進 mutant 開關                                  |

逐支拿掉重跑：四趟各紅回自己那一條（`dry-drop-1..4.log`）。22 支裡其餘 19 支對插樁**無感** —— 子行程讀到插樁檔，而沒有 mutant 被啟動，行為就是原版。

**(c) `disableTypeChecks`。** 四支排除後仍紅在 `vue-typecheck`「`<script setup>` 裡的型別錯誤：expected '' to contain 'TS2322'」—— 預設 `true` 往 fixture 的 `.vue` 插 `@ts-nocheck`。設 `false`：綠。⚠️ 檔頭原本寫「`false` 試過：跑超過十分鐘沒跑完」；實測 **49 秒**到紅、整趟綠的乾跑 **1 分 25 秒**。那句的量測條件不可考。Stryker 10 的 schema 只接 `boolean | string`，縮 glob 的路（C168 §八 提的）子代理試了兩種都照樣紅在 (a) —— 它接不住型別變寬。

**(d) `!tools/vue-typecheck/src/**/*.ts` 那條排除的理由不成立了。** 它擋的是 `@ts-nocheck`；(c) 關掉之後把它拿回射程，乾跑照樣綠：`Found 139`。`git ls-files` 照七條 glob 重數 144 − 1（config）− 4（security-headers）＝ 139，對得上；C168 §五 那天是 137 − 3 − 1 − 4 ＝ 129。

**(e) 副作用。** 紅掉的乾跑會在 `tools/vue-typecheck/tests/fixtures/` 留下 `.tmp-*` 沙盒（bail 殺掉 worker，`afterAll` 沒跑；這輪累積 45 個），而 C190 那條「這個 checkout 裡每一個 `.vue` 都被掃到了」會因此紅（125 vs 35）。綠的乾跑留 0 個。

**(f) 版控設定與 scratchpad 設定問的是不是同一個問題。** 同一個檔 `tools/gate-kit/src/testing.ts` 各跑一趟真變異：

| 設定                                                               | 跑的測試 | 殺  | 逾時 | 存活 | 分數  |
| ------------------------------------------------------------------ | -------- | --- | ---- | ---- | ----- |
| scratchpad（`tools/*/tests` 減 `promise-check/**`，C168 §八 那份） | 508      | 71  | 8    | 2    | 97.53 |
| 版控（全樹減四支）                                                 | 424      | 70  | 9    | 2    | 97.53 |

一顆在殺與逾時之間翻（時間相依），存活與分數相同。

#### 二、裁決

1. **`vitest.stryker.config.ts` 進版控**，`stryker.config.mjs` 以 `vitest.configFile` 指它；名單是 (b) 的四支，**每一條註明紅的形狀**。名單是實證的，檔頭寫明「盤不出來、只能跑出來」。
2. **`disableTypeChecks: false`。** 這裡沒有型別檢查器（`tsconfigFile` 指向不存在的路徑、沒有 `checkers`），那行註解沒有用途。
3. **`vue-typecheck` 回到射程**，兩條排除剩 `config`／`security-headers`（理由是 `proxy-target` 要 `process.chdir()`，沒動）。
4. **絆線 `tools/gate-kit/tests/stryker-config.test.ts`**：名單每一條存在且是 `tests/**/*.test.ts`；`vitest.configFile` 指到存在的檔；`disableTypeChecks === false`。C154 §三 兩軸：對象在外（Stryker 不在閘門鏈）、壞法安靜（vitest 對不存在的 `exclude` 路徑不報錯；改回預設只在乾跑紅）。乾跑本身不進 `vpr ready`：1 分 25 秒且**就地改寫產品碼**。
5. **C168 §八／C177／C178／C183／C185 用 scratchpad 設定量的數字不重量。** 依據是 (f)：同一個檔兩份設定同分；⚠️ 這是**一個檔**的實測，推到那五則是推導 —— 它們量的都在 `tools/gate-kit/src`，而兩份設定對那個目錄的差別只在 `promise-check` 的非 CLI 測試有沒有進來。
6. C168 標題下的標註區塊加一條指到本則（§五「不排除」、§八「兩件都沒修」）；原文不改（C136 §八）。

#### 三、不裁

- **「一支新測試讀真樹而沒列進名單」沒有東西在守** —— 只有乾跑紅。判準本身不可盤（(b)），絆線守的是反方向。
- (e) 的 `.tmp-*` 殘留：是沙盒 harness 在 bail 下的行為，不是本則的對象；`stryker.config.mjs` 檔頭「跑完看 `git diff --summary`」旁邊寫一句。
- `.claude/worktrees/**` 不在 vitest 的 `exclude` 裡：C191 剛裁「不立共用邊界」，本則沿用預設不加。
- C193 §三 要求的 console spawn 絆線進名單：**不進** —— 它 spawn 的是 `bff-mock` 而 `platform/bff-mock/src` 在射程裡，實測綠（子行程讀插樁檔而無 mutant 啟動）。那則的要求是在本則量出來之前寫的。

#### 四、實測

| 探針                                                  | 結果                                        |
| ----------------------------------------------------- | ------------------------------------------- |
| 名單一條改名 `adoption.test.ts` → `adoptionX.test.ts` | 只紅「每一支都在樹上」                      |
| `disableTypeChecks: true`                             | 只紅「`disableTypeChecks` 關著」            |
| `configFile` 指到 `vitest.nope.config.ts`             | 只紅「指到那份名單所在的檔」                |
| 名單一條換成 `src/root.ts`                            | 只紅「每一條都是 vitest 會收的 `.test.ts`」 |
| 對照（原樣）                                          | 5／0                                        |

最終設定 `--dryRunOnly`（`291e2dc` 上）：`Found 139`，`Ran 1364 tests in 1 minute and 22 seconds`，RC 0，`.tmp-*` 0 個 —— C193 那條 spawn `bff-mock` 的絆線在裡面而且綠。`vpr ready` READY_RC=0（clone）。SCOPE.md 加一列登記 `vitest.stryker.config.ts`，oxfmt 重排整張根層表（37 列，只有一列是新的 —— C146 那種形狀）。

#### 五、與既有裁決的關係

- **C168 §五**：「不排除」的判斷不變（射程反而變大）；§八 兩件由本則接住。
- **C154 §三**：絆線兩軸照填。
- **C180**：`adoption.test.ts` 的量法（數 `process.argv` 字面）是它在插樁下紅的原因，量法本身沒錯。
- **C190**：(e) 的紅是它的射程測試在做事，不是它的缺陷。
- **C193 §三**：那一條要求被本則的實測推翻（見 §三）。

### C195 — 第三片切片的權限碼 C193 補了，路由沒有：`/api/invoice` 在跑起來的 mock 裡 404 而規格全綠；示範資料補在 `bff-mock`、與出貨那條同形，絆線從 `features` 名單解析每片打的路徑不抄字面（2026-09-07，#311）

> ⚠️ **§三 兩件不裁已各開一張票；原文不改。**
>
> - §三「示範資料的形狀要不要對切片的型別檢查」→ 見 #318（訂單那片今天已漂：`Order` 要 `placedAt`，`DEMO_ORDERS` 沒有）
> - §三「(c) 1 那條路要不要開」→ 見 #319

地圖 #306 的第五張。C193 補的是 `MOCK_PERMISSIONS`，而同一天的 mock 對 `GET /api/invoice` 回的仍是 404 —— 兩半各守一半。基準 `716ca0e`。

#### 一、事實

**(a) 重現。** 起 `platform/bff-mock/src/cli.ts`（`BFF_MOCK_ROUTES=apps/console/bff-routes.ts`）、`POST /api/session` 拿 cookie 後三片各打一次：

| 路徑                | 改前 | 改後 | 角色       |
| ------------------- | ---- | ---- | ---------- |
| `GET /api/orders`   | 200  | 200  | 對照       |
| `GET /api/shipment` | 200  | 200  | 對照       |
| `GET /api/invoice`  | 404  | 200  | 本則的目標 |

`features/invoice/src/api.ts:19` 打的是 `` `/invoice${search}` ``，`search` 只有 `?page=`；前綴 `/api` 來自 `apps/console/.env.example` 的 `VITE_API_BASE_PATH`。畫面的症狀是列表停在 loading；規格餵 `tests/support/in-memory-gateway.ts`（C114 交付的形狀），全綠。

**(b) 誰補：兩個位置各自寫了自己的定位。** `platform/bff-mock/src/server.ts` 示範資料段：「**這不是契約的一部分**……少了它，腳手架的示範應用永遠停在 loading」，先例是 `/api/orders` 與 `/api/shipment`（唯讀、`?page=` 不理）。`apps/console/bff-routes.ts` 檔頭：「應用自己的 mock 資料端點」，先例是 `POST /api/orders/:id/cancel`（寫入端、帶權限碼、排在示範資料前面）。invoice 的 `src/` 零 POST／PUT／DELETE，列表與出貨那條同一個形狀 —— 落在前者。

**(c) 誰守：三種形狀。** 對象是「每片切片打的路徑 mock 都接得住」，C154 §三 兩軸都有分：對象在外（mock 在 `platform/`、路徑在 `features/`、測試在 `apps/console`）、壞法安靜（404 → loading，沒有規格看得見）。

1. **叫切片真的 gateway 打 spawn 起來的 mock** —— 最貼對象，而做不到不改設計：切片 package 只 export `./src/index.ts`（D7，一個 default），gateway 不在公開契約裡；資料端點在 mock 的 401 閘門之後，Node 的 `fetch` 不帶 cookie，`@org/http-client` 也沒有接縫塞它。要走這條得改 D7 的公開面加 stub `fetch`，超出票。
2. **字面清單 `["/api/orders", "/api/shipment", "/api/invoice"]`** —— 子代理交的版本。與 C193 拒絕的形狀同一個：第四片加進來時這裡照樣綠。
3. **從 `apps/console/src/features.ts` 的名單解析每片 `features/<name>/src/api.ts`，取 `http.get` 的路徑字面前綴**；前綴讀 `.env.example`。每片至少要解出一條，否則紅在「解析式失效了，不是切片沒在打」—— 沒有這一條，regex 對不上的那天絆線安靜地少守一片。

**(d) 子代理的草稿另外兩處。** 替 invoice 發明了 `pageSize=10` 的分頁並為此抽一個 `serveDemoInvoices`（理由寫「保持 `createBffMock` 行數在門檻以下」）；出貨那條先例對 `?page=` 是**不理**的，三筆假資料分頁沒有意義，照先例改回一行，`vp check` 綠、行數門檻沒有紅。絆線的 doc 註解寫「沒有 body 檢查」而程式碼有。兩處都重寫。

#### 二、裁決

1. **`/api/invoice` 補在 `platform/bff-mock/src/server.ts` 示範資料段**，緊接 `/api/shipment`，同形：`{ items, total }`，`?page=` 不理。`DEMO_INVOICES` 三筆，形狀對 `features/invoice/src/ports.ts` 的 `InvoiceItem`（目前只有 `id`）。
2. **絆線 `apps/console/tests/bff-routes.test.ts`「每一片切片的列表打的路徑，vpr bff 起來的 mock 都接得住」**，取 (c) 3 的形狀，spawn 走 C193 那條同一段。只取 `http.get`：寫入端點由 `bff-routes.ts` 注入，上面既有的幾條在守。
3. **C39 §二 的形狀第三次出現**（mock 缺那片切片的資料端點，畫面停在 loading：訂單 C39 §二、出貨 C41 §三、本則發票），前兩次各補一條路由沒有絆線，這次有；C193 與本則是同一件事的兩半，各自的絆線各守一半，不合併。

#### 三、不裁

- **示範資料的形狀要不要對切片的型別檢查。** `bff-mock` 在 `platform/`，依 D7 不能 import `features/`；`InvoiceItem` 本身還是 `// TODO: 補上這個切片實際的欄位`。絆線只驗 `items` 是陣列 —— 形狀漂了本則接不住，而那是 #188／C165 那道縫的另一段，不在這張票。
- **(c) 1 那條路要不要開**（切片 export gateway 或 http-client 開 cookie 接縫）。它牽 D7，留給有需要的票。

#### 四、實測

每一趟只改一處、跑完還原，對照組原樣 8／0（含 C193 那條）：

| 探針                                                        | 結果                                                                   |
| ----------------------------------------------------------- | ---------------------------------------------------------------------- |
| `server.ts` 的 `/api/invoice` 改成 `/api/invoice-OFF`       | 7／1，紅「invoice 打 /api/invoice，mock 回 404」                       |
| `server.ts` 的 `/api/shipment` 改成 `/api/shipment-OFF`     | 7／1，紅「shipment 打 /api/shipment，mock 回 404」                     |
| `features/invoice/src/api.ts` 改打 `/invoices`（mock 不動） | 7／1，紅「invoice 打 /api/invoices，mock 回 404」—— 讀的是切片不是字面 |
| 同檔把 `http.get<…>(` 拆成 `const get = http.get; get<…>(`  | 7／1，紅「invoice 的 api.ts 裡找不到任何 http.get —— 解析式失效了」    |
| 對照（原樣）                                                | 8／0                                                                   |

絆線本身 129 ms（spawn 那一段與 C193 同量級）。改動：`server.ts` +13、測試 +81，刪 0。`vpr ready` READY_RC=0（worktree）。

#### 五、與既有裁決的關係

- **C193**：同一天、同一片切片、另一半。它的絆線讀跑起來的 `/api/session`，本則讀跑起來的 `/api/<path>`，來源刻意都是跑起來的 mock。
- **C39 §二／C41 §三**：形狀的第一、二次，各自的處置是補路由並在程式碼裡寫明「不是契約」；本則是第三次，也是第一次有絆線。
- **C114／C165／#188**：規格走 in-memory gateway 是 C114 交付的形狀、刻意的；invoice 的規格在 C165 那一趟接上完成率（§九 說明那個 80% 是範本場景）。本則不動規格那一邊 —— 絆線補的是規格看不見的那一段，不是把規格接上 mock。
- **C168 §四**：spawn 的形狀沿用既有的那一段，沒有新設施。
- **C154 §三**：兩軸見 (c)。
- **D7**：(c) 1 走不通的原因；本則沒有動它。

### C196 — `features/order` 的 `maskName` 重測：C168 §二 列了候選、§六 沒帶上，實測四條各是 `platform/pii` 那組的嚴格子集，而抄過來的那條「不得包含原本的字」換了名字沒換字（2026-09-07）

⚠️ **量測基準 `436cefa`。** 本則的行號與計數凍結在那一支。

#### 一、問題：一列候選在報告自己的表與清單之間掉了

C168 §二 那張「真重複的八組」把 `maskName` 列成一列：「`platform/pii` mask.test:59 ↔
`features/order` masking.test:22（同一個字面值）」，處置欄寫「候選（§六）」。
§六 列了七項候選，**沒有這一列** —— 八組減 harness 那一組是七組，而 §六 的七項裡
`defineFeature` 與 `maskName` 被合成了第 3 項的一句話，那句話只寫了前者。之後
C170 到 C178 逐項落地，這一列沒有任何一則接手，也沒有票。

發現它的是一次「這份報告的東西都做完了嗎」的核對：逐列對樹，這一列的兩個字面值
（`"A○○ N○○○○○○○"`）在 `436cefa` 上仍然各在一邊。本則補裁，判準同 C168 §一。

#### 二、重盤：四條逐條比，每一條右邊都不弱於左邊

`features/order/tests/masking.test.ts:22-42` 的 describe「隱碼函式本身」四條，對
`platform/pii/tests/mask.test.ts:47-83` 的 `describe("maskName")` 八條加 :191-199 那條：

| masking.test（刪）                              | mask.test（留）                                                      | 關係                 |
| ----------------------------------------------- | -------------------------------------------------------------------- | -------------------- |
| :24 `"王曉明"` → `"王○○"`                       | :49-50 `"林佳蓉"`、`"黃詩涵"`；:56 複姓；:72 單字                    | 同形輸入，右邊多三種 |
| :30 `"Aya Nakamura"` → `"A○○ N○○○○○○○"`         | :60 **同一個字面值**；:67-68 空白原樣保留；:92／:96 字素叢集         | 逐字相同，右邊多四條 |
| :35 `not.toContain("佳")`、:36 `not.toBe(原值)` | :80-81 `not.toContain("佳"／"蓉")`；:197 五個函式都 `not.toBe(原值)` | 見下                 |
| :40 `""` → `""`                                 | :76 同                                                               | 逐字相同             |

第三列要單獨講。`:35` 對 `"王曉明"` 斷言「不含 `佳`」—— **`佳` 不在 `王曉明` 裡**，
那一行對任何實作恆真。它是從 mask.test:80 抄來的：那邊的輸入是 `"林佳蓉"`，`佳` 是
被遮掉的第二個字；抄到切片時輸入照檔頭「刻意不用示範資料裡的名字」換成了 `王曉明`，
而期望值沒跟著換。C168 §二 寫的「同一個字面值」指的是第二列；第三列是同一件事的另一種
樣子 —— **抄過來的斷言換了輸入沒換期望**，而它從 `#15` 進來那天起就沒守過任何東西。
剩下 `:36` 那半由 mask.test:197 守，五個函式一起。

實測（§六）：三個非等價變異兩邊同紅；一個等價變異兩邊同綠（mask.test 檔頭 #157 那段
早就判過它）；`keepHead` 的 `<=` → `<` **只有 mask.test 紅**。找不到任何只讓左邊紅的
變異。deletion test：刪掉那個 describe，`masking.test` 剩三條全綠，`mask.test` 一個字
不用加 —— 複雜度**消失**，不是搬家。真重複。

`:59-78` 那個 describe「渲染結果裡找不到完整姓名」是另一條接縫：它問的不是函式對不對，
是**宣告為個資的欄位渲染出來看不到完整值** —— `mask.test` 沒有 `mount()`，M4 之下它
紅的兩條是 `wrapper.html()` 不是 `maskName()` 的回傳值。切片的 test surface 就是它。**留。**

#### 三、裁決

1. **刪 `masking.test.ts:22-42` 整個 describe（4 條 `it`）。** 不留任何一條當「切片有
   呼叫到函式」的探針 —— 渲染那半的替身元件已經在呼叫 `maskName()`，M4 證明它會紅。
2. **`:59-78` 一字不動**，`FULL_NAME`／`LATIN_NAME` 與「刻意不用示範資料裡的名字」那句
   留著 —— 渲染那半還在用它們。
3. **檔頭加一段**：函式本身的行為歸 `platform/pii/tests/mask.test.ts`、曾經在這裡的四條
   為什麼是子集、切片的接縫是哪一條。少了它，下一次審查看到切片測試不驗函式會再問一次
   （C174 §四 第 7 項、C175 §三 第 3 項同一個理由）。
4. **`mask.test.ts` 不動。** 它自帶反向測試與 #157 的等價判定；唯一性由第 3 項單向指
   過去，不雙向互指。
5. **不動門檻**：`vite.config.ts` 對 `features/order/tests` 沒有逐檔覆寫，測試層那組
   （455／3／4／15）只減不增。

淨變化 **−4 條 `it`**，零斷言換形狀，零閘門增減（C137 §一）。

#### 四、順手撞到、本則不裁

- **「抄過來的斷言換了輸入沒換期望」沒有東西在守。** `:35` 那一行從 `2721ba8`（#15）到
  `436cefa` 一直恆真，四支閘門與 Stryker 都看不見它 —— Stryker 量的是產品碼的變異，
  一條對任何實作都綠的斷言在它眼裡與一條好的斷言沒有差別。本則把它刪了，形狀記在這裡；
  它是 C154 §四 那一側（對象在內、reviewer 讀 diff 看得到），不立絆線。
- **`OrderList.vue` 有沒有繼續呼叫 `maskName()` 靠 review**：`masking.test.ts:51-52` 檔頭
  自陳，HANDOFF.md:1290 登記為「宣告的欄位必須包 mask」的靜態檢查缺口。本則刪的四條
  沒有一條在守這件事（它們呼叫的是函式不是元件），缺口的大小不變。
- **`tools/exit-drill/src/dependencies.ts:18` 說 `happy-dom` 隨 `masking.test.ts` 進來**：
  仍然成立 —— 留下的那半就是 `mount()` 那半，`// @vitest-environment happy-dom` 不動。

#### 五、與既有裁決的關係（C136 §八）

| 既有                             | 本則做了什麼                                                                                                               |
| -------------------------------- | -------------------------------------------------------------------------------------------------------------------------- |
| **C168 §二 `maskName` 列**       | **裁了** —— §二 盤對了（同一個字面值），本則證實並多找到一條恆真的斷言；C168 頂端的標註區塊加一行指過來，原文不改          |
| **C168 §六**                     | **補它掉的那一列** —— 七項候選沒帶上 §二 的第五列；本則 §一 寫下掉法，下一次「表 → 清單」的升格要逐列對，不對數量          |
| **C168 §一**                     | **遵守** —— 同一把判準（同時紅＋deletion test），閘門鏈不動                                                                |
| **C168 §七「PII 偵測 vs 遮罩」** | **不是同一列** —— 那列是 `pii-check` detect.test ↔ `platform/pii`，判互補；本則是切片的重測 ↔ `platform/pii`，判重複       |
| **C172／C174／C175／C177／C178** | **同系列第六則**；C175 證實、C172／C174 各推翻一半，本則證實並加一條 —— 重盤的結論不預設方向                               |
| **C137 §一**                     | **遵守** —— 零閘門增減；不搬測試、不拆 describe 的理由就是它                                                               |
| **C154 §四**                     | **不適用**（零新增機械檢查；§四 第 1 項的形狀刻意不立絆線）                                                                |
| **C173 §五／§七**                | **引它** —— 測試檔先寫 `C196` 字面、本則接上前 `decision-ids` 紅，同一支 PR 內接上即綠                                     |
| **#15（§11 II ⑨）／#19**         | **不動** —— ⑨ 的證據機制 #19 拆掉了，這支檔留下的是渲染那半；函式本身的證據一直在 `platform/pii`，本則只是讓切片不再抄一份 |

#### 六、實測（基準 `436cefa`，worktree `order-mask-seam-93f5`）

每一趟只改 `platform/pii/src/index.ts` 一處，兩個 package 各自 `npx vitest run`
（不讀 `.vitest-results.json`，C177 §四 那個坑），跑完 `git checkout` 還原；對照組原樣
masking 7／0、mask 23／0：

| 變異                                         | masking.test（7 條）                       | mask.test（23 條）    | 讀法                             |
| -------------------------------------------- | ------------------------------------------ | --------------------- | -------------------------------- |
| M1 `keepHead(part, 1)` → `keepHead(part, 2)` | 紅 2（:24、:30）                           | 紅 6                  | 同時紅                           |
| M2 拿掉分段，整串 `keepHead(value, 1)`       | 紅 1（:30）                                | 紅 3（:60、:67、:96） | 同時紅                           |
| M3 拿掉 `value === ""` 短路                  | 綠                                         | 綠                    | 等價 —— mask.test 檔頭 #157 判過 |
| M4 `maskName` 回原值                         | 紅 5（:24、:30、:33 ＋ 渲染那半 :60、:73） | 紅 10                 | 同時紅；渲染那半紅的是 `html()`  |
| M5 `keepHead` 的 `<=` → `<`                  | **綠**                                     | 紅 3（:67、:72、:96） | **只有右邊紅** —— 嚴格子集的證據 |

- deletion test：刪掉 `:22-42` 後 `masking.test` 3／0、`order.test` 4／0；`mask.test` 沒改。
- 逐檔 `it` 數：`features/order` **11 → 7**（masking 7 → 3、order 4 不變）；`platform/pii` 23 不變。
- `:35` 的恆真：`"王曉明".includes("佳")` 是 `false`，不需要跑測試。
- `vpr ready` READY_RC=0（worktree，退出碼直接從命令取；log 裡 `features/order` 那一段是 7 passed）。

### C197 — 「選單指到的路由存在」不是獨一，是五份：C172 §二 的讀數少跑了 `apps/console`，而聯集那一份被三片的逐片比對嚴格蘊含（2026-09-08）

⚠️ **量測基準 `2326bc8`。** 本則的行號與計數凍結在那一支。

⚠️ 本則是 **C172 的續集**。依 C138，**C172 的原文一個字都不動** —— 錯的讀數留在那裡，
更正寫在這裡。

#### 一、問題：一個「只有」少跑了一個 package

C172 §二 那張七列的表，第五列「選單項目指向**實際存在**的路由」判「**可達、獨一**」，
§二 末尾的實測寫：

> invoice 選單 `routeName` 改成 `invoice/nope` → import 不炸，**只有**第五列那條紅。

那一趟只跑了 `features/*`。同一個變異在 `apps/console` 也紅 ——
`apps/console/tests/composition-root.test.ts:67-73` 用 `registerFeatures(features)`
的**聯集**問同一個問題，而它在 C172 的量測基準 `a318dc4` 上就已經在同一個行號存在
（`git show a318dc4:apps/console/tests/composition-root.test.ts` 的 `:67`）。

「獨一」這個字在這棵樹上是有後果的：它是「這一列不可刪」的**唯一**理由。
理由錯了而結論仍然對（見 §三 1），但這棵樹靠的正是數字對得上。

#### 二、事實：五份 implementation，一份是聯集

`rtk proxy grep -rn --include='*.ts' "實際存在的路由"`，五處：

| 處                                               | 形狀                                          | 射程                  |
| ------------------------------------------------ | --------------------------------------------- | --------------------- |
| `features/invoice/tests/invoice.test.ts:16`      | 逐片：`feature.routes`                        | invoice               |
| `features/order/tests/order.test.ts:16`          | 逐片                                          | order                 |
| `features/shipment/tests/shipment.test.ts:16`    | 逐片                                          | shipment              |
| `tools/slice-gen/src/files.ts:922`（範本）       | 逐片 —— 每產一片再複製一份                    | 未來每一片            |
| `apps/console/tests/composition-root.test.ts:67` | **聯集**：`registerFeatures(features).routes` | 註冊進 console 的全部 |

**聯集那一份問不出逐片問不出來的事**，理由在 `defineFeature`：

- `define-feature.ts:60` —— 路由 `name` 必須以 `${切片名}/` 開頭；
- `define-feature.ts:98` —— 選單 `routeName` 必須以 `${切片名}/` 開頭。

所以對切片 S 的任一選單項目 `r`（前綴必為 `S/`）：`r ∈ 全部路由名的聯集`
⟺ `r ∈ S 自己的路由名`。別片的路由名帶的是別片的前綴，接不住 `S/` 開頭的 `r`。
兩邊**同時紅、同時綠**，不是「大致相同」。

而逐片那三份**多守一件事**：它們不看 `apps/console/src/features.ts` 註冊了誰。
一片在樹上而沒被註冊時，逐片仍然紅、聯集看不到。今天三片都註冊了
（`features.ts:21` 的 `[invoice, order, shipment]`），所以差別是零；
**這正是刪聯集那一份、不是刪逐片的理由。**

#### 三、裁決

1. **更正 C172 §二 第五列與那句實測** —— 那一列是「**可達、五份**（三片逐片 ＋ 範本 ＋
   `apps/console` 聯集）」，不是「可達、獨一」；那句「只有第五列那條紅」少跑了
   `apps/console`，正確讀數是**兩個 package 各一條紅**（§四）。
   ⚠️ **C172 §四 1 的處置（刪那 10 條、留可達的 7 條）不受影響** —— 那 10 條的判準是
   「不可達」，與本列無關；第五列不刪，結論一個字不改，改的是理由。

2. **刪 `apps/console/tests/composition-root.test.ts:67-73`。** 切片的 seam 由切片自己守，
   聚合層只守聚合。那支檔剩下的四條（`registerFeatures` 自己的職務：路由／權限／i18n
   三個來源反推出的命名空間 ＝ `names`）一條不動。

   ⚠️ **這個無損是條件式的，條件寫在這裡：**
   - **三片各留著 `tests/*.test.ts:16` 那一條**；
   - **`slice-gen` 範本（`files.ts:922`）留著它**，所以第四片產出來時自帶。

   ⚠️ **沒有任何閘門在守這兩個條件。** `conformance` 的 `checkSliceTests`
   （`scan.ts:29` 的 `hasTestFile`，C192 加的五條反向）只保證「每片有一支
   `tests/**/*.test.ts`」，**不保證那支檔裡留著哪一條**。把三片的那一條一起刪掉，
   本則之後全樹零紅 —— 而在刪聯集那一份之前，`composition-root:67` 會接住它。
   **這是本則明知而接受的代價**，理由是 §二：聯集那一份接得住的，是「三片同時把
   自己的那條刪掉」這個情境，而它接住的方式是把三個切片的責任集中到 `apps/` ——
   與 D7「切片的契約住在切片內」相反。⚠️ 交回給人的不是這一項；本則不開票，
   因為沒有相依方（C172 §五 的同一個判準：沒有相依方的洞開票會變成又一個
   沒人主張的預設）。

3. **不動 `defineFeature`、不動 `conformance` 任何一條規則的射程、零閘門增減**
   （C137 §一）。`vpr gate` 那條鏈一支不碰。

4. ⚠️ **不得拿 C172 §四 2 改的 describe 名（「defineFeature 不驗、只有這裡在守的」）
   當排他宣告。** 那句話說的是「`defineFeature` 不驗這件事」，不是「全樹只有這裡在驗」——
   本則正是它讀成後者時會得到的錯誤結論的反例。名字不改：它對逐片那一份仍然成立。

#### 四、實測（基準 `2326bc8`，worktree `.wt-seam-round2`）

一處變異：`features/invoice/src/index.ts:42` 的 `routeName: "invoice/list"` →
`"invoice/nope"`。兩個 package 各自 `npx vitest run`（不讀 `.vitest-results.json`，
C177 §四 那個坑）：

| package            | 結果                     | 紅在                                                   |
| ------------------ | ------------------------ | ------------------------------------------------------ |
| `features/invoice` | 17 passed / **1 failed** | `tests/invoice.test.ts:19`（`:16` 那條 `it`）          |
| `apps/console`     | 18 passed / **1 failed** | `tests/composition-root.test.ts:71`（`:67` 那條 `it`） |

C172 §二 寫的是前者，漏的是後者。變異已還原（`index.ts.bak` 覆蓋回去，
`git status` 乾淨）。

對照組：`vpr ready` 在同一棵 worktree 上未改動時 **READY_RC=0**（退出碼以
`RC=$?; …; exit $RC` 保住，不讀背景通知摘要 —— 記憶裡那個坑）。

#### 五、順手撞到的兩件登記（本則不改程式碼）

**1. C191 §二 那張「四種機制」的表，「問 git」那列少一格。**

那列列了八個 `tools/*`（scope-check、api-surface、doc-facts、conformance、
gate-roster、promise-check、exit-drill、pii-check）。第九處在 `platform/`：
`platform/eslint-config/tests/a11y.test.ts:137-139` 的 `vueFilesInCheckout()`
把 `pii-check/src/tracked.ts:87-88` 的聯集（`ls-files` ∪ `ls-files --others
--exclude-standard`）逐字重建了一次。

它不是漏網，是**刻意的**：`a11y.test.ts:132-133` 自己引了 `tracked.ts` 的檔頭理由
（「跨工具相依要過 `conformance` 的邊界規則，而三支問 git 的問題不一樣」），
`harness-tripwire` 對 `spawnSync("git"` 的放行也涵蓋它。登記在這裡的理由只有一個：
**C191 §二 那張表是「今天有幾種機制在答同一個問題」的清單，而它少了一格，
而且少的那一格不在 `tools/` 底下** —— 下一個拿那張表當全集的人會漏掉 `platform/`。
⚠️ 依 C138 不回頭改 C191 一個字。⚠️ 本則**不**因此重開 C191 §三 1（不立共用邊界）：
多一格同型的實例不是新論證。

**2. C193 §三 (a) 預言了一件沒發生的事。**

C193 §三 (a) 寫「#307 那張票的排除名單要收它（`bff-routes.test.ts`）」。
C194 落地的 `vitest.stryker.config.ts:27-37` 只有四支，沒有它。

**不是缺口。** 那份名單依該檔 `:12-13` 自陳是**實證的**：從零排除開始、紅一支加一支，
四支各自在乾跑紅過。`bff-routes.test.ts` 的兩條 spawn 起的是 `bff-mock` 的 CLI，
子行程讀到的是插樁檔而沒有 mutant 被啟動，行為就是原版（同檔 `:14-17` 那 19 支的情形）——
乾跑不紅，所以名單裡沒有它是對的。

登記的是**預言與落地不一致**這件事本身：C193 寫下那句時是推論，C194 用量測否掉了它，
而兩則之間沒有一句話把它接起來。這一句就是。

⚠️ 同一格上還有一件已知而無人守的：`vitest.stryker.config.ts:23` 自陳「沒有閘門守
『一支新測試讀真樹而沒列在這裡』」，`tools/gate-kit/tests/stryker-config.test.ts:20-22`
是同一句話的第二份字面 —— **兩份沒有東西在比對**。本則只登記，不裁：守它要跑乾跑
（1 分 25 秒、就地改寫產品碼），而 C154 §三 兩軸已經把它判在 `vpr ready` 之外。

#### 六、與既有裁決的關係（C136 §八）

| 既有             | 本則做了什麼                                                                  |
| ---------------- | ----------------------------------------------------------------------------- |
| **C172 §二**     | **更正一個量測讀數**：「可達、獨一」→「可達、五份」；C138，原文不動           |
| **C172 §四 1**   | **不受影響** —— 刪那 10 條的判準是「不可達」，與第五列無關                    |
| **C172 §四 2**   | **遵守而且加註**：describe 名不得讀成排他宣告（§三 4）                        |
| **C138**         | **遵守** —— 更正寫續集，不回頭改原文                                          |
| **C137 §一**     | **遵守** —— 零閘門增減；刪的是測試碼，論證是「一條 seam、一處守」不是行數     |
| **C192**         | **引用** —— `checkSliceTests` 只保證測試檔存在，是 §三 2 那個條件無人守的依據 |
| **C191 §二**     | **登記一格**（§五 1），不重開 §三 1                                           |
| **C193 §三 (a)** | **收掉那句預言**（§五 2）                                                     |

### C198 — 一條 seam 上的三張卡片，三種答案：`spec-report` 的端對端刪一份、`slice-gen` 的 e2e 是射程借用不是重複、而範本那條字面**不刪**（C188 §二 2 已經替它定過價）（2026-09-08）

⚠️ **量測基準 `2326bc8`。** 本則的行號與計數凍結在那一支。

#### 一、三張卡片為什麼放在一起裁

第二輪審查的卡片 7 與卡片 8 形狀相同：**一條 seam 上有兩條斷言，其中一條被更深的
那一條蘊含**。一起裁省一次論證。裁下來三個答案不同，而不同的地方正是判準的內容。

| 卡片 | seam                                     | 兩條的關係                           | 裁       |
| ---- | ---------------------------------------- | ------------------------------------ | -------- |
| 8    | `render.ts:219-238` 的 `sameReport()`    | 兩條**都是端對端**                   | 刪一份   |
| 7(b) | `slice-gen` 產出的切片通過真的一致性檢查 | 不是重複，是**射程借用**             | 改射程   |
| 7(a) | `files.ts:79` 的 `enabled: true`         | 一條靜態、一條真的跑 —— **診斷不同** | **不刪** |

#### 二、卡片 8：刪 `report.test.ts:218-226`

`tools/spec-report/tests/report.test.ts` 那一組守的是 `normalizeTables` 的分支，
**一條樣本對一條分支**：`:191` 基本、`:204` 對齊冒號、`:212` 圍籬。唯獨 `:218`
不是樣本 —— 它 `renderReport()` 產一份**真的報表**再補 padding。

而那正是 `tests/cli.test.ts:294-300` 在做的事，只是更完整：spawn 真的 CLI、寫真的
檔案、`--check` 那一半（`cli.ts:214`）；`:302` 與 `:315` 把另一半（`cli.ts:236`
的寫檔守衛）與「內容真的變了」那個方向也走完了。

**實測**（拿掉 `render.ts:228` 的表格分支，`out.push(line)`）：

| 條                | 結果   | 它是什麼           |
| ----------------- | ------ | ------------------ |
| `report.test:191` | **紅** | 樣本（基本分支）   |
| `report.test:218` | **紅** | 端對端             |
| `cli.test:294`    | **紅** | 端對端（真 CLI）   |
| `cli.test:302`    | **紅** | 端對端（寫檔那半） |
| 其餘 37 條        | 綠     |                    |

⚠️ `cli.test.ts:267-269` 檔頭明文寫了分工：「逐格寬度對不對由 `report.test` 那組
樣本負責，這裡驗的是**兩半都要在**」。這句話成立，**而且它正好指出該刪的是哪一邊**：
樣本留在 `report.test`，端對端留在 `cli.test`。刪 `:218-226`，該處補一段指過去。

#### 三、卡片 7(b)：`e2e.test.ts:197-199` 是射程借用 —— 改射程，不刪

那條 it 叫「違規恰好一項 —— 不是一堆問題裡剛好有它」，斷言是
`expect(conformance.output).toContain("1 項違規")`。**名字說的是產出的那一片，
而 `run()` 打的是整棵真樹**：真樹三片任何一片多一項不相干的違規，這裡就紅，
而訊息說不出是哪一片。

**實測**（拿掉 `features/order/vite.config.ts` 的 `enabled: true`，製造一項與
`slice-gen` 完全無關的違規）：

```
× 違規恰好一項 —— 不是一堆問題裡剛好有它
  AssertionError: expected '…✗ 一致性檢查未通過：2 項違規…' to contain '1 項違規'
```

紅的是 `slice-gen` 的 e2e，壞的是 `features/order`。C189 §四 那次「104 → 103」
就是同一個東西。

**裁決**：改成從輸出解析出屬於 `features/<產出的那一片>` 那一段的違規列，斷言
恰好一項、而且是「擁有權」那一項。同一個變異下改後 **15/15 綠**；未變異時也是
15/15 綠。

⚠️ **這改變了這條 it 接得住什麼** —— 它不再為真樹的不相干違規紅。那不是損失：
真樹三片的覆蓋率門檻由 `conformance` 自己的 `checkCoverageGate` 守（C189），
而它是閘門、不是 `slice-gen` 的測試。**一條 seam 由一處守，而紅的時候說得出是誰。**

⚠️ 這一項**不動任何閘門規則**（C137 §一），只動測試碼；`specs/` 底下沒有場景在
背書「違規恰好一項」（已掃，零命中 —— AGENTS.md 規則四）。

#### 四、卡片 7(a)：`coverage-gate.test.ts:105-107` **不刪**

報告把它判成「`:139` 蘊含 `:105`，deletion test 消失」。**蘊含成立，而結論不成立。**

**實測**（拿掉 `files.ts:79` 的 `enabled: true`）：

| 條                       | 結果   | 訊息                                      | 耗時  |
| ------------------------ | ------ | ----------------------------------------- | ----- |
| `coverage-gate.test:105` | **紅** | 「★ 覆蓋率預設開著 —— 少了這一行…」       | 4ms   |
| `coverage-gate.test:139` | **紅** | 「應該紅卻是 0：」＋ 一整趟 vitest 的輸出 | 455ms |

兩條同紅，`:139` 確實更深。但**這正是 C188 §二 2 定過價的那個形狀**：

> 全清單那條紅的時候 diff 是一整份清單；這一條紅的時候訊息只有一個名字，
> 而且把「為什麼這三條刪不得」的理由釘在測試上。

`:105` 紅的時候訊息說的是「範本掉了 `enabled: true`」；`:139` 紅的時候是一趟真的
vitest，訊息講的是覆蓋率門檻。**診斷特異性不是零，而 C188 說那一半算數。**

第二個獨立理由：**這支檔的檔頭把兩層寫成設計**（`coverage-gate.test.ts:22-24`）：

> 1. 產生器**真的**產出那份設定（下面 §一，靜態）
> 2. 那份設定**真的**會在 usecase 沒被走完時紅（下面 §二，真的跑一次）

`:105-107` 是 §一 裡**唯一**釘 `enabled: true` 的一條。刪掉它，§一 就不再有它
自己宣稱有的那一層 —— 而那個檔頭會變成一句不是真的話，沒有東西會抗議。

⚠️ **本則因此與第二輪報告的判定相反，而理由不是成本**（C137 §一 兩個方向都不准）。
理由是：同一棵樹上，同一個「深的那條蘊含淺的那條」，C188 §二 2 留下了淺的那條。
拿走 7(a) 而留下 C188 那一條，是**同一個判準給兩個答案**。第二輪報告自己把卡片 14
（CSP 指令清單，同型）判成「沒有新事實，留給人」—— 本則把 7(a) 放進同一格。

#### 五、順手撞到、本則不裁

- **`report.test.ts:225` 那個負向（`padded.replace("75.0%","76.0%")` → false）
  跟著 `:218` 一起消失。** 它在 `:195`（樣本的負向）與 `cli.test:315`（端對端、
  內容真的變了）各有一份，兩份都比它強（後者連「重新產生會覆寫」都驗）。不補。
- **`coverage-gate.test.ts` §一 其餘五條的分工沒有重新盤過。** 本則只判 `:105-107`
  這一條，其餘（`:97` 契約取值、`:109` glob 不是空砲、`:116` task 快取、`:130`
  相依登記）原樣留 —— 它們各釘一件 `:139` 說不出來的事，而本則沒有量它們。

#### 六、與既有裁決的關係（C136 §八）

| 既有                 | 本則做了什麼                                                              |
| -------------------- | ------------------------------------------------------------------------- |
| **C188 §二 2**       | **遵守，並把它當判準用在 7(a)** —— 診斷特異性算數，所以淺的那條留         |
| **C189**             | **引用** —— 真樹三片的覆蓋率門檻歸 `checkCoverageGate`，e2e 不兼職（§三） |
| **C137 §一**         | **遵守** —— 零閘門增減；三項的論證都不是行數、不是成本                    |
| **C115／C126**       | **不碰** —— `spec-report` 的 CLI 行為與旗標處理一個字不動                 |
| **AGENTS.md 規則四** | **遵守** —— `specs/` 零命中、零修改                                       |

### C199 — a11y 三支：第一輪判「讀檔目標互不相交」而 C187 之後兩支同源，結論不變理由要換；而那三顆注入式變異的紅**訊息說不出真設定改了什麼**（2026-09-08）

⚠️ **量測基準 `2326bc8`。** 本則的行號與計數凍結在那一支。

⚠️ 本則更正的是**第一輪報告**（`reports/research/test-seam-review-2026-09-05.html`
附錄 A）的一列理由，不是某一則裁決。依 C138 的同一個規矩：**報告原文不動**，
更正寫在這裡。

#### 一、問題：那一列的理由在寫下來十四天後就不成立了

第一輪附錄 A 有這麼一列：

| seam      | 兩邊同紅？                 | deletion test | 處置       |
| --------- | -------------------------- | ------------- | ---------- |
| a11y 三支 | **否（讀檔目標互不相交）** | 消失          | 互補，不動 |

「讀檔目標互不相交」當時是真的：`eslint-config/tests` 讀 `a11y.js`、
`compliance/tests` 讀 `ACCESSIBILITY.md`、`ui/tests` 讀 `components/*.vue`。

**C187（#301）之後不是了。** 今天 `tools/compliance/src/a11y.ts:1` 與
`tools/compliance/tests/a11y.test.ts:3` 都是
`import a11yConfig from "@org/eslint-config/a11y"` —— 與 `eslint-config` 那一支
**同一份事實來源**。理由沒了。

#### 二、結論不變，而今天的理由是「判準不同」不是「來源不同」

同源之後兩支問的仍然不是同一件事：

- `eslint-config/tests/a11y.test.ts:66-76` 問「**被啟用的規則對 fixture 真的會開火**」——
  它跑真的 ESLint。
- `compliance/tests/a11y.test.ts:108-195` 問「**設定裡的範圍覆寫有沒有進交付文件**」——
  它跑 renderer。

兩個方向各有一顆只讓自己紅的變異（**兩邊都量過，不是推論**）：

| 變異                                                      | eslint-config | compliance   | 讀法       |
| --------------------------------------------------------- | ------------- | ------------ | ---------- |
| fixture 少一格（`a11y-violations.vue:56` 的 `autofocus`） | **1 紅**／11  | 20/20 綠     | 只有左邊紅 |
| `scopedOverrides()` 恆回空（C187 那個缺陷）               | 11/11 綠      | **5 紅**／20 | 只有右邊紅 |
| 豁免 `files` 擴成全樹的 `.vue`                            | **1 紅**／11  | **1 紅**／20 | 兩邊同紅   |

（後兩列與 C187 §四 改後那張表一致：C1／M1／M3 只有 compliance 紅、M2 兩邊都紅。）

**所以「互補、不動」這個結論不變，換掉的是理由**：不是「讀的東西不一樣」，
是「同一份設定，問的問題不一樣」。⚠️ 這一類最難抓：對照組全部正常、量測沒有壞，而**理由過期了**。

#### 三、而 M2 那兩條紅裡有一顆診斷缺陷

C187 §四 那段自陳：

> M2 多紅的兩條是注入式的 M2／M3 本身：真樹已經是 `**/*.vue` 時，注入同一個值
> 當然與真設定相同 —— 那兩條紅是絆線掛在被守的對象上的證據，不是缺陷。

**紅得對，而訊息不對。** `compliance/tests/a11y.test.ts:163-167`（改前）是

```ts
const mutated = render(withExemptionReplaced({ ...exemption, files: ["**/*.vue"] }));
expect(mutated).not.toBe(render());
```

真設定被改成全樹時，注入值 ＝ 真值，`mutated` 與 `render()` 逐位相同 —— 這顆變異
**自己變成 no-op**，而 vitest 印的是：

```
AssertionError: expected '# 無障礙：驗收端與開發端的分工\n\n> **這份檔案是產生的…'
             not to be '# 無障礙：驗收端與開發端的分工\n\n> **這份檔案是產生的…'
```

兩串**看起來一樣**的字串加一句 `not to be`。讀的人看不出真設定改了什麼、
也看不出這是變異失效而不是渲染壞了。C187 §四 說它「不是缺陷」——
**紅不紅那一半對，訊息那一半是缺陷**，本則只動後者。

順帶一顆同源的：M3（`:169-178`）把整列的字面寫死成
``label-has-for` | `platform/ui/src/components/**/*.vue``。**別人**把豁免的
`files` 改寬，M3 也會紅 —— 而它要問的是「多關一條會不會進交付文件」，
不是那個 glob 長什麼樣（那一格由 `:140` 的「印出每一列的規則名與 files glob 字面」守）。

#### 四、裁決

1. **不刪任何一邊** —— 三支 a11y 測試原樣留，第一輪的處置不變（§二）。

2. **第一輪附錄 A 那一列的理由更正為**：「否 —— **同源（C187 之後兩支都 import
   `@org/eslint-config/a11y`），而判準不同**」。⚠️ 報告原文不動（C138）。

3. **M1／M2／M3 三顆的 `not.toBe(render())` 換掉。** 每一顆改成：
   - 一句**具名的前置斷言**問「真設定是不是已經長成注入的樣子」，訊息直接說
     「注入值與真值相同，這顆變異變成 no-op」；
   - 對**注入結果**驗一次具名的 `toContain`（`:159-160`、`:166`、`:177` 本來就有的那一半）；
   - 對**真設定的產出**驗一次 `not.toContain`，訊息說「真設定的產出本來就印著它 ——
     上面那條沒有量到注入」。

   ⚠️ 這樣「兩份產出不同」仍然成立（一份含、一份不含 ⟹ 不同），而且是**具名**的。
   測試的真假值不再依賴被測設定的值。

4. **M3 的 glob 改成取自真設定**（`exemption.files[0]`），並在旁邊寫下為什麼不寫死。

5. **`:180` 那條對照（`render()` 兩次逐位相同）留**，標題裡「上面三顆的 `not.toBe`」
   改成「上面三顆拿 `render()` 當真值」—— 三顆現在靠的是它的決定性，不是它的不等。

6. **零閘門增減**（C137 §一）。`platform/eslint-config/src/a11y.js` 一個字不動，
   `tools/compliance/src/` 一個字不動，`ACCESSIBILITY.md` 不重產。

#### 五、實測（基準 `2326bc8`，worktree `.wt-seam-round2`）

每一趟只改一處，跑完 `cmp` 回原檔：

| 變異                                        | 改前                                       | 改後                                                                   |
| ------------------------------------------- | ------------------------------------------ | ---------------------------------------------------------------------- |
| 無（對照）                                  | 20/20 綠                                   | 20/20 綠                                                               |
| `a11y.js:160` 的 files 擴成全樹的 `.vue`    | **M2、M3 紅**；M2 訊息 `'…' not to be '…'` | **只有 M2 紅**；訊息「真設定的豁免 files 已經是 … 這顆變異變成 no-op」 |
| `scopedOverrides()` 恆回空（C187 那個缺陷） | —                                          | **5 條紅**（含 M1／M2／M3）—— 絆線還在守它要守的東西                   |
| fixture 少一格 `autofocus`                  | —                                          | `eslint-config` 1 紅／11，`compliance` **20/20 綠**                    |

第二列是本則的重點：改後 **M3 不再為別人的改動紅**，而 M2 紅的那一句話直接說出
真設定變成了什麼。第三列是 deletion test 的對照 —— 改寫沒有讓三顆變成恆真。

`vpr ready` **READY_RC=0**。

#### 六、與既有裁決的關係（C136 §八）

| 既有           | 本則做了什麼                                                                     |
| -------------- | -------------------------------------------------------------------------------- |
| **C187 §四**   | **一半遵守一半更正**：「紅是絆線的證據不是缺陷」成立；「訊息」那一半是缺陷，改掉 |
| **C186 §二 3** | **遵守** —— 測試仍然不改寫真樹的交付文件，三顆走參數注入                         |
| **C138**       | **遵守** —— 第一輪報告原文不動，更正寫在這裡                                     |
| **C137 §一**   | **遵守** —— 零閘門增減；動的是三顆斷言的寫法，不是任何規則的射程                 |
| **C168 §一**   | **遵守** —— 判準沒換（同一變異兩邊同時紅＋deletion test），換的是這一列的理由    |

### C200 — 第二輪最後三張卡片：`api-surface` 的紅燈整段刪掉全樹零紅（補上），而 `exit-drill` 那兩條 CLI 是全樹唯一釘住兩句使用者訊息的地方（不刪）（2026-09-08）

⚠️ **量測基準 `2326bc8`。** 本則的行號與計數凍結在那一支。

#### 一、三張一起裁的理由

卡片 6、11、12 是第二輪報告排在最後的三張（一張 Worth exploring、兩張 Speculative）。
三張的共同點是**報告對它們的「獨有斷言」判斷都需要重新量一次**，而重量之後
三個答案不同。

| 卡片 | 報告的判斷                           | 重量之後                                  | 裁       |
| ---- | ------------------------------------ | ----------------------------------------- | -------- |
| 11   | 紅分支兩條路都跑不到 → 加一條端對端  | **成立**，且比報告寫的更嚴重（全樹零紅）  | **補**   |
| 6    | `:80-88` 無獨有斷言、`:90-99` 已走過 | **不成立** —— 兩條各釘一句全樹唯一的訊息  | **不刪** |
| 12   | `:404-406` 嚴格弱，刪                | 弱是真的，而刪掉會少一份 Stryker 看得見的 | **改寫** |

#### 二、卡片 11：`cli.ts:277-284` 在此之前一條路都走不到

`tools/api-surface/src/cli.ts:277-284`（headline／dirs／remediation／`exit 1`）是
`checkIndexAgreement` 唯一的出口。走得到它的只有 `scopedToRepo`
（`cli.ts:259`，`PLATFORM === PLATFORM_DIR`）那條路，而：

- `negative.test.ts` 每一條會紅的案例都走 `--platform <tmpdir>` → **進不去**；
- 真樹那條（`negative.test.ts:213-228`）進得去，但這棵樹是乾淨的 → `problems` 恆空。

**實測**：把 `:277-284` 整段刪掉 → **`tools/api-surface` 97/97 全綠**。

⚠️ `cli.ts:275-276` 那句辯護（「少一個方向要動 `checkIndexAgreement`，而那支函式是
直接被測的」）**成立，而它守的是別的東西**：它守「少一個方向」，不守「這一段有沒有
被接上」。`:213-228` 只守綠燈那條路 —— `verifiedInIndex = entryDirs.length`
在那個 `if` **之外**，所以刪掉紅燈那段之後綠燈照樣宣稱「驗過在版控裡」。

**裁決**：加一條端對端。作法與這棵樹已有的形狀一致
（`conformance/tests/output.test.ts:93-106`「規則要真的被接進 cli.ts」）：

- 這支 CLI 的 `ROOT` 從 `import.meta.url` 推（`cli.ts:42`），**沒有 `--root`**。
  要讓 `PLATFORM === PLATFORM_DIR` 而內容又是壞的，只能複製一份樹再跑**副本的** CLI。
- 沙盒建在 `tools/api-surface` 底下（`sandbox({ within })`），node 才解析得到
  `@org/*` 的 workspace symlink —— 與 `tools/vue-typecheck` 的 fixture 同一個理由。
- 破壞法取 **vanished** 那個方向：`git add -A` 之後才刪掉副本的一個 `platform/*`
  目錄，於是「版控裡有、磁碟上沒有」。受害者的名字**不寫死**（改名之後寫死的那個
  會靜靜失效，而症狀是這條測試恆綠）。
- 斷言三件：RC=1、headline、**補救文字**（`兩條出路`）—— 最後一件是那段程式碼
  存在的理由，只驗 RC 的話刪掉補救文字仍然綠。

**改後**：新測試綠；把 `:277-284` 整段刪掉 → **只有這一條紅**（97 passed / 1 failed）。

⚠️ **不動 `cli.ts` 一個字**，不加 `--root`（那是 src 的變更，不在本則射程）。

#### 三、卡片 6：`exit-drill` 那兩條 CLI **不刪**

報告要刪 `tools/exit-drill/tests/cli.test.ts:80-88` 與 `:90-99`，理由是「一行傳遞
（`cli.ts:355` 的 `requireFresh: FLAGS["require-fresh"]`）只有一處，一條就殺得掉」。

**傳遞那一半是對的。** 實測把 `:355` 改成 `requireFresh: false`：三條 CLI 全紅、
`freshness.test` 十四列**全綠**（3 failed / 25 passed）。留 `:68-78` 一條確實
殺得掉那一行。

**而「無獨有斷言」不成立。** 全樹掃兩句訊息的字面：

| 訊息                   | 產生處                    | 斷言處                           |
| ---------------------- | ------------------------- | -------------------------------- |
| `尚未跑過完整退出演練` | `freshness.ts:85`         | **只有** `cli.test.ts:84`        |
| `沒有記樹的指紋`       | `tree-fingerprint.ts:102` | **只有** `cli.test.ts:94`、`:98` |

`freshness.test` 的十四列比的是 `[verdict.code, worst(verdict)]` —— **代號與等級，
不是文字**；下半張表只點名 `失敗的` 與 `548` 兩個字面。所以刪掉那兩條 it，
這兩句使用者看得到的訊息在全樹**零處**被釘住。

依同一輪 C198 §四 用過的判準（C188 §二 2：診斷特異性算數），以及第二輪報告
自己的判準表（「刪掉一邊複雜度**搬家** ＝ 留」—— 要刪就得把訊息斷言搬進
`freshness.test`），**本則裁不刪**。

⚠️ 順帶更正報告一句事實：`:80-88` 寫「無獨有斷言」，實際上 `:84` 是
`尚未跑過完整退出演練` 全樹唯一的斷言處。

#### 四、卡片 12：`facts.test.ts:404-406` 改寫，不刪

`handoffItemCount(真 HANDOFF) > 5` 確實比 `:381-389`（CLI 對真樹 RC=0）弱：
實測把 `:422` 的 `at <= to` 改成 `at < to` → **5 條紅**，兩條 CLI 端對端與三條
（含 `:404`）一起。

**而刪掉它有一個帳上的反面**：CLI 那兩條是 spawn 的，對 Stryker **完全不可見**
（`vitest.stryker.config.ts:14-17` 記的那件事）。刪掉 `:404` 等於把一份
Stryker 看得見的擊殺換成兩份它看不見的。報告自己因此把這張列成 Speculative。

**裁決**：不刪，**改寫**成一條說得出自己在幹嘛的前提檢查 —— 上面三條餵的是合成
字串，它們代表得了真的 `HANDOFF.md`，靠的是那份檔案還是 `## N.` 這個形狀；
形狀變了的時候那三條照樣全綠，量的是一個不存在的格式。標題與訊息照 `:385`
（`DECISIONS.md` 那條前提）的寫法，訊息直接說「這條測試失去意義」。

#### 五、順手撞到、本則不裁

- **`api-surface` 沒有 `--root`**，而這棵樹上八支 CLI 有（C126／C184）。本則用
  「複製整棵樹」繞過去，代價是那條測試要跑一次完整的 `extractSurface`。
  加 `--root` 是 src 的變更、要自己的裁決（C184 是它的先例），**本則不開票** ——
  沒有相依方，同 C172 §五 的判準。
- **`exit-drill` 四處 `ROOT` 手抄**（`freshness.test:22`、`plugin-accounting:20`、
  `counts:20`、`expected-failures:12` 都是 `join(import.meta.dirname, "../../..")`，
  而同 package 的 `cli.test:3` 已經用 `@org/gate-kit/testing` 的 `repoRoot`）——
  C168 那 17 處手抄的殘留。不是斷言，不會讓任何變異同紅，登記在這裡。

#### 五之一、把副本建在 repo 內會炸別人 —— 而它是間歇的

§二 那條測試的第一版用 `sandbox({ within: join(ROOT, "tools/api-surface") })`，
理由是 node 要解析得到 `@org/*` 的 workspace symlink，而 `tools/vue-typecheck`
的 fixture 是同一個 pattern。**那一版讓 `vpr ready` 紅了，而且是間歇的**：

```
✗ 門檻檢查的量測台自己壞了
  農場掃到 378 個檔，真樹掃到 300 個 —— 兩邊必須一樣。
```

`vp run -r test` 是**併行**的。副本存在的那幾秒裡，`threshold-check` 的符號連結
農場走過這棵樹，多看到 78 個檔（就是那份副本）。同一支 PR 的**上一趟是綠的** ——
兩趟之間唯一的差別是排程。

⚠️ **`vue-typecheck` 的 fixture 沒有這個問題，理由不是位置，是它在版控裡** ——
農場與真樹兩邊都看得到它。一份「跑到一半才存在」的副本不是那種東西。
**拿它當先例是錯的，而錯法只有在併行撞上的那一趟才看得見。**

改法：副本建在 `tmpdir()`，把這個 package 的 `node_modules` **連**過去
（不複製 —— 那是幾萬個檔）。

⚠️ **順帶量到的第二件，本則只登記**：副本建在 repo 內時 `pii-check` 走得進去 ——
實測在 `tools/api-surface/<副本>/tools/api-surface/tests/` 放一支帶通過校驗的
身分證字號的檔案 → `pii-check` **RC=1**，訊息指的是副本裡那條路徑。改成 `tmpdir`
之後這一格消失了，但**形狀留著給下一個人**：`scan.ts:63` 的 `EXEMPT` 是用
**完整路徑**當 key 的，哪天有人替一支會被複製到的檔案加豁免，副本裡的那一份就
對不上 —— **本機紅、CI 綠**（CI 是乾淨 clone）。同 #282 記過的那兩處是同一個形狀。
要守它得動 `pii-check` 的射程或 `EXEMPT` 的形狀，兩件都是閘門的變更（C137 §一）。

#### 六、與既有裁決的關係（C136 §八）

| 既有           | 本則做了什麼                                                         |
| -------------- | -------------------------------------------------------------------- |
| **C188 §二 2** | **第二次當判準用**（§三）—— 訊息的特異性算數，所以那兩條 CLI 留      |
| **C198 §四**   | **同一個判準的第三個實例** —— 蘊含成立不等於可以刪                   |
| **C183／C184** | **引用** —— `requireFresh` 那一行的零與 `--root` 的先例              |
| **C194**       | **引用** —— spawn 的測試對 Stryker 不可見，是 §四 不刪的理由         |
| **C137 §一**   | **遵守** —— 零閘門增減；`api-surface/src/cli.ts` 一個字不動          |
| **C168 §一**   | **遵守** —— 判準沒換；三張各自量過同一變異兩邊紅不紅與 deletion test |

### C201 — C189 §二 4 說「散文三處」而樹上是五處：漏的兩處裡有一處是**產生器**，所以那個更正不會傳給下一片切片（2026-09-09）

⚠️ **量測基準 `bcaf157`**（C200 與第二輪成果報告合併後的 `main`），worktree
`slice-config-prose-sweep`。⚠️ 依 **C138 不回頭改 C189 一個字**，本則是它的續集。

#### 一、事實

1. **帶那句假陳述的活檔是五處，不是三處。** 全樹掃
   `整塊不繼承`／`根層那份`（排除 `node_modules`）：

   | 位置                                                       | C189 之後 |
   | ---------------------------------------------------------- | --------- |
   | `apps/console/vite.config.ts:73`                           | ✅ 已改   |
   | `features/order/vite.config.ts:8-9`                        | ✅ 已改   |
   | `features/shipment/vite.config.ts:8-9`                     | ✅ 已改   |
   | `features/invoice/vite.config.ts:14-17`                    | ❌ 原樣   |
   | `tools/slice-gen/src/files.ts:38-41`（`VITE_CONFIG` 範本） | ❌ 原樣   |

   其餘命中都是歷史記錄裡的**引文**（`CHANGELOG.md:996`、`DECISIONS.md:10590`、
   `DECISIONS-2.md:5007`、第一輪審查報告的 HTML），不是活的斷言。

2. **漏的不是實作，是 C189 自己的枚舉。** C189 §一 5 寫「**兩段**散文」並點名
   `features/order`（`shipment` 同段）與 `apps/console`；§二 4 寫「散文**三處**
   各改一句」。實作 `c921578` 的 numstat 恰好是那三支 `vite.config.ts`
   （`apps/console` 3/2、`features/order` 2/2、`features/shipment` 2/2）——
   **說三處、改了三處，數量對得上，而枚舉少兩列。**

   ⚠️ 這是 **C196** 記過的形狀：C168 §二 列八組、§六 列七項，「八組減 harness
   那一組」讓數量自洽，漏掉的那一列要**逐列對樹**才看得見。兩次的發現方式也一樣
   —— 都是事後一次「這份報告／這則裁決的東西都做完了嗎」的核對。

3. **同一則裁決走過那兩支檔，只是問的是另一個字串。** C189 §一 1 把
   `features/invoice/vite.config.ts:55` 與 `tools/slice-gen/src/files.ts:79`
   列為四份 `enabled: true` 副本裡的兩份。**同一支檔、`:55` 進了枚舉、`:38` 沒有**
   —— 因為 §一 1 掃的是 `enabled: true`、§一 5 掃的是那句散文，而只有前者掃了全樹。

4. **漏的那兩處裡有一處是產生器，這是本則的重點。**
   `tools/slice-gen/src/files.ts:25` 的 `VITE_CONFIG` 是每一片新切片
   `vite.config.ts` 的**全部內容**（該檔 `:19` 註明它是本檔唯一被提到函式外面的
   模板）。所以 C189 更正的三處全是**已經手改過的既有切片**，而**產生下一片的
   那一份沒有改** —— `vp create slice` 今天產出的切片仍然帶著那句假陳述，
   採用團隊 fork 之後拿到的也是它。更正的射程停在存量，沒有到流量。

5. **`features/invoice` 那一份是流量已經跑出來的證據。** 它與範本逐字相同
   （差別只有逃脫的反引號），由 `0645fba`（#260，2026-09-05 02:45）產出 ——
   **早於 C189 落地的 `c921578`（2026-09-06）**。它不是 C189 之後長出來的新檔，
   是 C189 當時就在樹上而沒被枚舉到的第四份。

6. **沒有閘門在守，而且守不到。** `doc-facts` 不讀 `.ts` 的註解；C189 §二 1 自己
   加的 `checkCoverageGate` 比對的是**去掉註解之後**的 `coverage:` 區塊（§二 2
   明文「先去掉 `//` 與 `/* */` 註解再比對」）—— 註解正是它第一步就丟掉的東西。
   擋這一句的只有讀 diff 的人。

#### 二、裁決

1. **兩處各改一句，用 C189 §二 4 的原句。** `features/invoice/vite.config.ts`
   與 `tools/slice-gen/src/files.ts` 的 `VITE_CONFIG`，字面一致
   （範本那份的反引號照原樣逃脫）。「100%」「13.20%」「#130」比照 C189 §二 4
   一字不動 —— 這兩支檔的那一段本來就沒有那些數字。

2. **範本那一份與 `features/invoice` 一起改，不分兩次。** 只改 invoice 會讓
   下一片切片重新產出同一句話，而那正是 C189 §三 明文拒絕的「散文原樣留」。

3. **不加閘門守這句散文。** 對象在**內**（這棵樹自己的設定檔與範本註解），
   C154 §三 兩軸只填得出一軸；而註解品質靜態不可驗，AGENTS.md〈Commenting
   Standards〉已明文寫它 _not gated in CI, on purpose_。⚠️ 依 C137 §一，
   這裡不得也不需要拿成本論證 —— 理由是**判準本身**，不是划不划算。

#### 三、不裁

- **不改那四處歷史引文**（`CHANGELOG.md:996`、`DECISIONS.md:10590`、
  `DECISIONS-2.md:5007`、第一輪審查報告）。C138：不回頭改原文。
- **不把 `features/invoice` 的 docblock 對齊 `features/order` 的版本。**
  後者帶著 100%／7.84% → 14.28% 的實測值，是 C189 §二 4 明文「一字不動」的東西；
  兩支檔的分歧與本則要更正的假陳述無關，一起動會把一則更正變成一次改寫。
- **不開票查「範本與樹上三片的 docblock 何時分岔」。** 沒有相依方，同 C172 §五
  與 C200 §五 的判準：只登記。登記的內容是上面 §一 1 那張表。
- **不重開 C189 §二 1 的 `checkCoverageGate` 射程。** §一 6 說的是「它看不到註解」，
  那是它的設計（§二 2 裁過），不是缺口 —— 讓它讀註解會把一支文字比對的閘門
  變成一支散文審查器。

#### 四、實測

| 步驟                                          | 結果                                                                                       |
| --------------------------------------------- | ------------------------------------------------------------------------------------------ |
| 改前全樹掃（`git grep`，排除 `node_modules`） | 活檔 5 處，2 處未改                                                                        |
| 改後同一支掃（再排除歷史記錄四處）            | **0 處**                                                                                   |
| 對照：`features/order` 含舊字串               | **0**（早已改過）                                                                          |
| 兩支檔的 diff                                 | 各 4/4                                                                                     |
| 有沒有測試釘住這段註解                        | 沒有 —— `slice-gen` 對範本的文字斷言在 `coverage-gate.test.ts:105`，釘的是 `enabled: true` |
| `vpr ready`                                   | READY_RC=0                                                                                 |

#### 五、與既有裁決的關係（C136 §八）

| 既有           | 本則做了什麼                                                             |
| -------------- | ------------------------------------------------------------------------ |
| **C189 §二 4** | **續集更正射程** —— 「散文三處」的枚舉少兩列；原文一個字不動（C138）     |
| **C189 §三**   | **執行它** —— 「散文原樣留」是它拒絕的選項，而範本沒改等於對新切片保留它 |
| **C196**       | **同一個形狀第二次** —— 數量自洽而枚舉少列，只有逐列對樹看得見           |
| **C138**       | **遵守** —— 更正寫成續集，四處歷史引文不動                               |
| **C137 §一**   | **遵守** —— §二 3 不加閘門的理由是 C154 §三 的判準，不是成本             |
| **C154 §三**   | **判準用一次** —— 對象在內、壞法看得見，兩軸只填一軸 → 不立絆線          |
| **C120 §一**   | **引用** —— 被更正的那句話本來要講的機制（門檻必須住在切片自己的檔裡）   |

### C202 — 突變測試的乾跑紅了兩天而沒有東西看得見：#317 加的第五支真樹觀察者沒登記，整棵樹一顆 mutant 都不跑 —— 而那正是第二輪審查 14 張錯 3 張的成因（2026-09-09）

⚠️ **量測基準 `9093d92`**（C201 合併後的 `main`），worktree `stryker-real-tree-observer`。

#### 一、事實

1. **乾跑紅。** `./node_modules/.bin/stryker run` 在初始測試就中止：

   ```
   ERROR DryRunExecutor  One or more tests failed in the initial test run:
     apps/console 的 dev 資料端點 ★ 每一片切片的列表打的路徑，vpr bff 起來的 mock 都接得住
       invoice 的 api.ts 裡找不到任何 http.get —— 解析式失效了，不是切片沒在打
   ERROR Stryker  There were failed tests in the initial test run.
   ```

2. **它是第五支真樹觀察者，而名單只列四支。** `apps/console/tests/bff-routes.test.ts:227`
   從磁碟讀每片 `features/<片>/src/api.ts`，取 `http.get` 的路徑字面前綴
   （`:52`）。`api.ts` 落在 mutate 射程的 `features/*/src/**/*.ts` 裡，插樁把那個
   字面包進 mutant 開關 —— 解析式零命中。**紅的是工具造成的假紅，不是那條斷言寫錯。**

3. **紅了兩天。** 最後一次驗證乾跑綠是 `fbdc901`（#305／C190，2026-09-06 **22:32**）；
   加進那條斷言的是 `2e8890e`（#317／C195，2026-09-07 **01:15**）—— 相隔 **2 小時 43 分**。
   到本則為止**兩天**，期間沒有任何東西紅。

4. **後果不是「少一格數字」，是整棵樹一個數字都沒有。** Stryker 的乾跑只要一支測試紅，
   整趟一顆 mutant 都不跑（C168 §八、#307 已記過同一個機制）。

5. **`vitest.stryker.config.ts` 的檔頭自己預言過這件事：**

   > ⚠️ **沒有閘門守「一支新測試讀真樹而沒列在這裡」** —— 那只會在下一次跑 stryker 的乾跑紅。

   守的是反方向（列在這裡的每一支都要存在，`tools/gate-kit/tests/stryker-config.test.ts`）。
   本則是那句留白的**第一個實例**。

6. ⚠️⚠️ **這是第二輪測試接縫審查 14 張錯 3 張的成因。**
   `reports/research/test-seam-round2-outcome-2026-09-08.md` §九 1 記的是
   「每一張刪行卡片落地前手工做一次那個變異」，而那份報告自陳**沒跑 Stryker**。
   本則把那句話的性質改掉：**那不是方法上的選擇，是跑不起來。**
   ⚠️ 依 C138 不回頭改那份報告一個字。

7. **修掉之後一次就綠。** 名單加一列 `apps/console/tests/bff-routes.test.ts`：

   | 項目               | 值                                                           |
   | ------------------ | ------------------------------------------------------------ |
   | 射程對照           | `Found 139 of 558 file(s)` —— 跑前用七條 glob 獨立數也是 139 |
   | 乾跑               | 綠，1349 條測試，1 分 29 秒                                  |
   | mutant             | 13073 顆                                                     |
   | 全趟               | 8 分 55 秒，`STRYKER_RC=0`，log 零 `ERROR`                   |
   | 修完後 `vpr ready` | READY_RC=0（清掉 37 份 `.tmp-*` 之後）                       |
   | 存活 / 擊殺        | 1510 / 3789（覆蓋內 71.50%）                                 |

   **擋住整棵樹的是一支沒登記的測試。**

8. **跑完之後三個副作用，而 `ignoreStatic: true` 在位（`stryker.config.mjs:201`）。**

   | 症狀                                          | 實測                                                          |
   | --------------------------------------------- | ------------------------------------------------------------- |
   | index 被 `git add` 了 140 支插樁後的產品碼    | `git diff --cached --numstat` → +23034 −11837                 |
   | **本機 42 個 tag 全被刪光**                   | `git tag` 回 0，遠端去重後 42                                 |
   | 18 支 `cli.ts` 的執行位掉了                   | `100755 → 100644`，diff 0 插入 0 刪除                         |
   | **`vue-typecheck` 的 fixture 留下 37 份副本** | `tools/vue-typecheck/tests/fixtures/.tmp-*`，各含 2 支 `.vue` |

   `stryker.config.mjs:114` 寫的是「手工套上去時 `ignoreStatic` 保護不到你」——
   本次是**工具自己跑的**，一樣沒保護到。

   ⚠️ **第四列直接讓 `vpr ready` 紅。** `platform/eslint-config/tests/a11y.test.ts:164`
   （C190 那條射程斷言）比對「eslint 掃到的 `.vue`」與「git 說這個 checkout 裡有的
   `.vue`」—— eslint 掃磁碟，看到 **109**；git 看到 **35**。差的 74 恰好是
   37 份副本 × 2 支（**算式閉合**，這是這個診斷的對照）。副本被 `.gitignore` 掉了，
   所以 git 那一側是對的，紅的是磁碟那一側。刪掉 37 份之後 `vpr ready` READY_RC=0。

   ⚠️ 這是**測試自己在 repo 內建副本**的第三個實例（#282 留言、C200 §五之一、本則），
   而這一次連 `.gitignore` 都擋不住 —— 讀它的那支走的是磁碟，不是 git。

   ⚠️⚠️ **而 `git checkout -- .` 是錯的解藥：它從 index 還原，而 index 已經被污染。**
   實測跑了它一次，等於把插樁碼重新寫回工作區。正解是
   `git reset --hard <已推到遠端的 HEAD>` ＋ `git fetch --tags origin`。

#### 二、裁決

1. **`apps/console/tests/bff-routes.test.ts` 加進 `REAL_TREE_OBSERVERS`**，
   帶一句說明它為什麼讀真樹（解析 `api.ts` 的 `http.get` 字面）與為什麼插樁下必紅。
   ⚠️ 這**不是** AGENTS.md 規則二說的「改設定拿綠燈」：Stryker 不是閘門
   （`thresholds.break: null`、不在 `vpr ready` 的五步裡），而這份名單的維護程序是
   設定檔自己訂的（「名單是實證的，不是盤出來的：從零排除開始，紅一支加一支，
   直到乾跑綠」）。排除也不減少任何擊殺數 —— 那支測試對 instrumentation 本來就看不見。

2. **`apps/console/tests/bff-routes.test.ts` 本身一個字不動。** 它「不抄路徑字面、
   從 `features` 名單解析」的設計是 C195 刻意的，而且是對的（第四片加進來時抄字面
   那版照樣綠）。紅的是工具，不是它。

3. **不加閘門守「新的真樹觀察者要登記」。** 兩個理由，缺一不可：
   - **判準**：對象在**內**（這棵樹自己的量測台設定，採用團隊 fork 之後不一定跑
     Stryker），C154 §三 兩軸只填「壞法安靜」一軸。
   - **機制**：那個問題**在不跑 stryker 的情況下答不出來** —— 「這支測試會不會在插樁後紅」
     沒有靜態判準（設定檔檔頭已記：掃 `spawnSync`／`runCli` 兩個方向都不對，22 支直接
     spawn 的裡 19 支對插樁無感，而真正紅的四支裡有一支測試檔連 `child_process` 都沒 import）。
     ⚠️ 一道答不出來的檢查不是保險，是恆綠。

   ⚠️ 依 C137 §一，這裡不得拿成本論證；上面兩條都不是成本。

4. **登記那三個副作用，不修。** 它們是 Stryker 的行為，這棵樹改不動。能改的是**程序**：
   跑 stryker 之前 HEAD 必須已經推到遠端（那是唯一的還原點），跑完三件都要驗 ——
   `git status --porcelain` 的**第一欄**（index）、`git tag` 對遠端、`git diff --summary` 的模式。

#### 三、不裁

- **1510 顆存活的處置**：本則只讓量測跑得起來，不判任何一顆。`tools/compliance`
  一個 package 就 272 顆（覆蓋內 49.6%），前四支檔佔全部存活的 25% 且三支是渲染／訊息 ——
  那一層剛被 C188 §二 2 與 C200 §三 兩次裁定「訊息的特異性算數」。**另案。**
- **`tools/csp-verify` 整支零訊號**（0 殺 0 存活 100 未覆蓋）：只登記。51 支零訊號裡
  16 支是 `cli.ts`（子行程不可見，已知成因），`csp-verify` 是**兩支檔都零**，成因未查。
- **不重開 `ignoreStatic` 那一格。** C168／#307 已裁過它的代價，本則沒有新事實
  改變那個取捨 —— 新事實是「它擋不住 `git add` 與刪 tag」，而那是**程序**的事實，
  不是那個取捨的事實。

#### 四、與既有裁決的關係（C136 §八）

| 既有                     | 本則做了什麼                                                           |
| ------------------------ | ---------------------------------------------------------------------- |
| **C195／#317**           | **登記它的副作用** —— 那條斷言正確、設計也對，而它讓量測台停擺兩天     |
| **C194／C168 §八**       | **同一個機制的第二次** —— 乾跑一支紅則全樹零 mutant                    |
| **C190／#305**           | **引用** —— 最後一次驗證乾跑綠的基準                                   |
| **C154 §三**             | **判準用一次** —— 對象在內，兩軸只填一軸 → §二 3 不立絆線              |
| **C137 §一**             | **遵守** —— §二 3 的兩條理由是判準與機制，不是成本                     |
| **C138**                 | **遵守** —— 不回頭改第二輪報告 §九 1 一個字，成因寫在這裡              |
| **C188 §二 2／C200 §三** | **指向** —— 存活最集中的三支是渲染／訊息，正是那兩則判為有價值的那一層 |

### C203 — 突變帳本上的「存活」有三種意思：抽樣 24 顆、9 顆是假的，而假存活的第二個成因是「守衛在閘門裡，而 Stryker 只跑測試」（2026-09-09）

⚠️ **量測基準 `205c246`**（C202 合併後的 `main`），worktree `css-comment-stripper-tests`。
本則的帳本來自 C202 修好乾跑之後的**第一趟全樹突變測試**。

#### 一、事實

1. **帳本。** 13073 顆 mutant、139 支產品碼、8 分 55 秒：存活 **1510**、擊殺 3789、
   未覆蓋 5943、`ignoreStatic` 忽略 1831。覆蓋內 71.50%，total 33.70%。

2. **存活最集中的四支各抽 6 顆，真的套上去、真的跑一次該 package 的測試**
   （每一輪都有無變異的對照組，四輪的對照組都是 RC=0）。**結果逐檔完全不同：**

   | 檔案                                    | 存活 | 抽樣 | 假存活 | 真存活 | 真正的守衛在哪                       |
   | --------------------------------------- | ---: | ---: | -----: | -----: | ------------------------------------ |
   | `compliance/src/a11y-render.ts`         |   96 |    6 |  **5** |      1 | 一條 **spawn CLI** 的測試            |
   | `compliance/src/render.ts`              |  121 |    6 |      3 |      3 | 部分同上                             |
   | `spec-report/src/render.ts`             |   82 |    6 |      1 |  **5** | **閘門步驟 `--check`**，不在任何測試 |
   | `conformance/src/rules/phantom-deps.ts` |   83 |    6 |      0 |  **6** | **沒有**                             |

   合計 24 顆抽樣、**9 顆假存活（37.5%）**，而逐檔比例從 **0% 到 83%**。

3. **假存活成因一（已知的那個）。** 把 `a11y-render.ts:38` 的文件標題換成空字串：
   `compliance` 閘門 **RC=1**、該 package 測試 **1 failed / 75**。抓到它的是
   `negative.test.ts` 的「跑完之後 `COMPLIANCE.md` 仍與映射一致」，而那條用
   `runCli` 起子行程 —— `coverageAnalysis: "perTest"` 從來沒把 renderer 歸屬給它，
   Stryker 也就從來沒拿它去跑那顆 mutant。

4. ⚠️⚠️ **假存活成因二（本則新增，而且比第一個大）：守衛根本不在測試裡，在閘門步驟裡。**
   把 `spec-report/src/render.ts:133` 那句散文換成空字串：**該 package 的測試全綠**，
   而 `node tools/spec-report/src/cli.ts --check` **RC=1** —— 守它的是 `vpr ready`
   的第 5 步，逐位元組比對產出與版控裡那份文件。`compliance/src/cli.ts:229`／`:245`
   是同一個形狀。**Stryker 只跑測試、不跑閘門，所以這一整層保護在帳本上一律顯示為存活。**

5. **真缺口的形狀是「閘門規則自己的解析邏輯」。** `phantom-deps.ts` 抽 6 顆 6 顆都真：
   把 `specifier.split("/").slice(0, 2)` 改成 `specifier.split("/")` —— 測試沒紅，
   **`conformance` 閘門也 RC=0**。成因是閘門跑在**乾淨的樹**上，那條路徑走不到紅。

6. **`stripCssComments` 是 `export` 的，而整棵樹零消費者。** `git grep` 只有定義處
   （`phantom-deps.ts:72`）與同檔的呼叫（`:227`）。那個 `export` 買到的可測性
   一直沒有被用，而它身上有 **28 顆**存活 —— 正是第 5 點那一類（兩層都沒守）。

7. **抽樣撿到一顆等價變異。** `a11y-render.ts:112` 的 `override.files.join(", ")`
   換分隔符不影響輸出，**因為真設定的 `files` 只有一個元素**。它今天是等價變異，
   **而加第二個元素的那一刻就不是了，且那個轉變在 diff 上看不見**。

#### 二、裁決

1. ⚠️ **突變分數不得用來排優先順序 —— 逐檔先問「守衛在哪一層」。**
   四層：in-process 測試／spawn 的測試／閘門步驟／沒有。
   **判法是兩步，兩步都要跑**：把那顆變異真的套上去，(a) 跑一次該 package 的測試、
   (b) 跑一次那道閘門的 CLI。**兩個都綠才是真缺口。**
   ⚠️ 這不是拿成本論證（C137 §一）—— 是說整體分數在這棵樹上**量錯了對象**：
   它量的是「測試接住多少」，而這棵樹的保證有一大半放在閘門裡。

2. **補 `stripCssComments` 的直接單元測試，九條，放 `rules.test.ts`。**
   放那一支而不是 `negative.test.ts`：該檔檔頭已經裁過分工（「判定本身的測試 ——
   直接 import，不起行程」），而本則替那個分工加上第三個理由 ——
   **起行程的那一支對突變帳本不可見**。
   ⚠️ 每一條的輸入都對著 `stripCssComments` 檔頭那段警告（`@source "…/**\/*.{vue,ts}"`
   裡那個「長得像空註解」的東西），不是為了湊擊殺數而構造的。

3. **不一次補完 83 顆。** 其餘 55 顆分佈在 `isPackageName`、`packageOfSpecifier`、
   主流程與訊息字串上，**每一顆都要先各自過 §二 1 的兩步判定** ——
   本則只證明了 `stripCssComments` 那 28 顆是真的。只登記，不開票（同 C172 §五 判準）。

4. **`export` 留著。** 本則讓它第一次有消費者；反過來（收掉 export、改從
   `checkPhantomDependencies` 驅動）要在磁碟上擺出一整棵能通過前面所有規則的樹，
   而那正是 `rules.test.ts` 檔頭裁過不走的路。

#### 三、不裁

- **不動 `negative.test.ts` 一個字。** 它驗的是端對端行為，本則沒有改變那個分工的價值；
  「對突變帳本不可見」是工具的限制，不是那支測試的缺陷。
- **不改 Stryker 設定去「看見」閘門。** 那需要讓它跑閘門而不是跑測試，不在這個工具的
  能力範圍內。⚠️ 這一格登記，不重開 C121／C168 §八 的任何一則。
- **不替 1510 顆逐條裁。** 本則裁的是**怎麼讀這份帳本**，加上一支檔的處置。
- **不因為「假存活比例高」就否定帳本。** 37.5% 是抽樣值，而它指向的結論是
  「逐檔覆核」不是「不可用」—— `phantom-deps` 那 6/6 就是帳本指對了的地方。
- **不處置第 7 點那顆等價變異。** 它今天是等價的，處置要等 `files` 有第二個元素 ——
  而那一天沒有東西會提醒你，同 C144 記過的形狀。

#### 四、實測

| 步驟                                                   | 結果                         |
| ------------------------------------------------------ | ---------------------------- |
| 四支檔的抽樣對照組（無變異）                           | 四輪全部 RC=0                |
| 24 顆抽樣                                              | 假存活 9／真存活 15          |
| `a11y-render.ts:38` 換空字串 → `compliance` 閘門       | RC=1（測試 1 failed／75）    |
| `spec-report/render.ts:133` 換空字串 → 該 package 測試 | **全綠**                     |
| 同一顆 → `spec-report --check`                         | **RC=1**                     |
| `phantom-deps.ts:158` 變異 → 測試／`conformance` 閘門  | 全綠／**RC=0**（兩層都沒守） |
| 新增九條之後，`stripCssComments` L72–128 的 28 顆存活  | **28／28 全部轉紅**          |
| 對照：只跑新增那個 describe，無變異時                  | RC=0                         |
| `vpr ready`                                            | READY_RC=0                   |

⚠️ 第七列是逐顆量的：28 顆各自套上去、只跑新增的那個 describe、跑完還原，
**每一顆都要單獨紅**。前兩輪各有 2 顆不紅（`L99` 的 `&&`→`||` 與左運算元恆真），
第九條測試就是為它們寫的 —— 全稱選擇器的 `*` 前面不是 `/`、`grid-area` 的 `/`
後面不是 `*`，任何一邊放寬都會把那一段當成沒閉合的註解吃到檔尾。

#### 五、與既有裁決的關係（C136 §八）

| 既有               | 本則做了什麼                                                                   |
| ------------------ | ------------------------------------------------------------------------------ |
| **C202**           | **直接延續** —— 帳本是它修好乾跑之後跑出來的第一趟                             |
| **C168 §一**       | **判準沒換，加一道前置** —— 判「該不該補」之前先判「這個讀數算不算數」         |
| **C121／C168 §八** | **登記工具的第二個盲區** —— 已知子行程不可見；本則加上「閘門步驟不可見」       |
| **C137 §一**       | **遵守** —— §二 1 的理由是「量錯對象」，不是成本；零閘門增減                   |
| **C172 §五**       | **判準用一次** —— 其餘 55 顆沒有相依方，只登記不開票                           |
| **C144**           | **引用** —— §三 最後一項那顆等價變異是同一個形狀（條件成立時沒有東西會提醒你） |

### C204 — `USECASE_FORBIDDEN_IMPORTS` 從 C114 起就在契約裡，而整棵樹沒有東西讀它：usecase 裡 value import `vue` 並真的用到它，`vpr ready` 全綠（2026-09-09）

⚠️ **量測基準 `1987566`**（C203 合併後的 `main`），worktree `usecase-purity-tripwire`。

#### 一、事實

1. **契約宣告了那條邊界。** `platform/slice-kit/src/contract.ts:199` 的
   `USECASE_FORBIDDEN_IMPORTS = ["vue", "pinia", "vue-router", "vue-i18n", "@tanstack/vue-query"]`，
   檔頭寫著「usecase 層不得出現的 import —— 純度就是這一層的全部價值」。

2. **而沒有任何東西在讀那個宣告。** 全樹 `git grep`（排除 `surface.json` 與散文）：
   唯一的消費者是 `tools/slice-gen/tests/contract-alignment.test.ts:511`，
   **那支驗的是產生器的輸出，不是樹上的切片** —— 與 **C189** 記過的形狀一字不差
   （範本那份有守、樹上零守）。

3. **同一支閘門對兩個鄰居早就在做同一件事。** `tools/conformance/src/rules/layering.ts`：

   | 契約常數                    | `layering.ts` 讀幾次 |
   | --------------------------- | -------------------: |
   | `VIEW_FORBIDDEN_IMPORTS`    |                    2 |
   | `STORE_FORBIDDEN_IMPORTS`   |                    3 |
   | `USECASE_FORBIDDEN_IMPORTS` |                **0** |

   `src/usecases/` 是 C114 才長出來的那一層，`layering.ts` 沒有跟著擴。

4. **實測：全樹零紅。** 在 `features/invoice/src/usecases/query-invoice.ts` 頂端
   `import { ref } from "vue"` **並且真的用到它**（`const hits = ref(0); hits.value += 1;`）：

   | 量的東西            | 對照組（乾淨） | 帶著違規 |
   | ------------------- | -------------- | -------- |
   | `vpr ready`         | READY_RC=0     | **RC=0** |
   | `vpr gate`（15 道） | RC=0           | **RC=0** |
   | 該 package 的測試   | 21 passed      | **全綠** |

   ⚠️ **第一次量錯了，而對照組接住它。** 頭一版只 `import { ref }` 沒有用它，
   `vue-typecheck` 紅了 —— 那是 `TS6133 未使用的 import`，**與這條邊界無關**。
   改成真的用到才是乾淨的量測，而那一版全綠。

#### 二、裁決

1. **`layering.ts` 加第三層：`src/usecases/` 底下不得 value import
   `USECASE_FORBIDDEN_IMPORTS` 列的東西。** 判準與同檔 store 那段**逐字一致**
   （借型別不算耦合，`isTypeOnlyImportAt`）—— 三層用同一把尺，讀規則的人不必記三種例外。
   清單從契約取，不抄字面。

2. **⚠️ 這一條**不放**切片自己的單元測試裡，而那是量出來的，不是偏好。**
   先寫了一版放 `features/invoice/tests/invoice.test.ts`（掃 `src/usecases/` 的原始碼），
   撞到兩件：

   - **切片沒有 `@types/node`**（`features/invoice`、`features/order` 都沒有，
     `tools/conformance` 有）。掃原始碼要 `node:fs`／`node:path`，等於替**每一片
     產生的切片**加一個 `@types/node` 相依。⚠️ 那是一個訊號：**切片那一層本來就不該讀檔案系統。**
   - **產生器的行數門檻當場紅。** 把同一段加進 `slice-gen` 的模板，`vp check` 報
     `max-lines-per-function`：`buildSliceFiles` **904 行，上限 843**。
     照 `VITE_CONFIG` 的先例把靜態字串提到函式外之後仍是 852 —— 還超 9 行。
     ⚠️ **門檻不得調鬆（AGENTS.md 規則二）**，所以那條路要再搬一層才走得通。

   兩件都指向同一個位置：這條規則屬於**閘門**，不屬於切片。

3. **反向測試五條，掛在被守的對象上，放 `rules.test.ts`。**
   value import → 紅且訊息點名檔案與 specifier；`import type` → 綠；沒有
   `src/usecases/` 的切片 → 不紅（這一層是可選的，規則不得逼出它）；
   **清單裡每一項逐個跑**（只擋 `vue` 等於其餘四項是裝飾）；
   對照：真樹 `features/invoice` → 綠 ——
   **規則嚴到連真的那份都過不了，前四條紅就沒有意義。**

4. **不加端對端。** CLI 的回報格式與結束碼由同檔其餘規則的沙盒測試共用，
   再加一份是 C168 §一 的真重複。

#### 三、不裁

- **不動 `USECASE_FORBIDDEN_IMPORTS` 的內容。** 本則讓既有的宣告生效，不改它守什麼。
  ⚠️ 特別是**不加 `@org/http-client`** —— 契約檔頭明文寫過那不是遺漏
  （usecase 拿 `ports.ts` 的介面，禁的是框架不是資料存取）。
- **不替 `features/order`／`features/shipment` 造 `src/usecases/`。** 那一層是可選的，
  §二 3 第三條就是為它寫的；逼出它是新的框架承諾，要自己的裁決。
- **不重開「切片測試該不該讀磁碟」。** §二 2 只登記那兩個代價，沒有裁定切片永遠不得讀檔。
- **不因為 `slice-gen` 的行數門檻擋路就動門檻。** 規則二；而且那一格本來就在做它該做的事。

#### 四、實測

| 步驟                                    | 結果                                   |
| --------------------------------------- | -------------------------------------- |
| 改前：usecase value import `vue` 並用到 | `vpr ready` **RC=0**、15 道閘門全綠    |
| 改後：同一個違規 → `conformance`        | **RC=1**，訊息點名檔案與 `"vue"`       |
| 對照：乾淨的真樹 → `conformance`        | RC=0                                   |
| 五條反向測試（無變異）                  | 5 passed                               |
| **短路版**（規則整段不執行）            | **2 條紅**，訊息「`vue` 沒有被擋下來」 |
| `vpr ready`                             | READY_RC=0                             |

⚠️ 第五列是這一組測試的資格證明：**真的跑過一次短路版**，不是推論它會紅。

#### 五、與既有裁決的關係（C136 §八）

| 既有         | 本則做了什麼                                                                                 |
| ------------ | -------------------------------------------------------------------------------------------- |
| **C189**     | **同一個形狀第二次** —— 契約常數只有產生器在守、樹上的切片零守；處置也同（進 `conformance`） |
| **C114**     | **補上它留的洞** —— `src/usecases/` 那一層與它的禁用清單是 C114 立的                         |
| **C154 §三** | **兩軸都填** —— 對象在外（採用團隊的切片）、壞法安靜（全綠）→ 絆線要有                       |
| **C137 §一** | **遵守** —— §二 2 的兩個代價是實測的機械事實，不是成本論證；門檻一格未動                     |
| **C168 §一** | **遵守** —— §二 4 不加端對端，理由是與既有沙盒測試真重複                                     |
| **C201**     | **引用** —— §二 2 那條路要同時改範本（存量與流量），而它卡在行數門檻上                       |

### C205 — `features/order` 不是殘骸：逐欄查過，四樣示範全樹只有它有，而唯一的死欄位是宣告了卻零消費者的 `placedAt`（2026-09-09）

⚠️ **量測基準 `1987566`**，worktree `order-business-residue`。
本則起於一個直覺：「這是腳手架，`order` 應該只是當初有測試用業務需求時留下的狀況」。
**那個直覺對了一半，而錯的那一半只有逐欄量才看得見。**

#### 一、事實

1. **`features/order` 的影響面是 33 支檔**（排除裁決／CHANGELOG／reports）。
   `src/` 的引用幾乎全是**註解拿它當範例**，真正的程式碼只有
   `apps/console/src/features.ts:4` 的註冊。⚠️ 但註解不是無害的：
   `tools/conformance/src/rules/slice-shape.ts:98`／`:111` 是**閘門的修復指示**
   （「照 `features/order/vite.config.ts` 補一份」）—— 刪掉切片，這道閘門紅的時候
   會叫人去看一個不存在的檔案。

2. **逐欄查「有沒有機制在示範它」：**

   | 欄位           | 示範什麼                                   | 全樹別處有嗎                                             |
   | -------------- | ------------------------------------------ | -------------------------------------------------------- |
   | `status`       | D14 判準「客戶端是權威的篩選條件 → Pinia」 | **沒有** —— `invoice` 的 store 只有 `page`／`selectedId` |
   | `customerName` | `maskName()` 在**真畫面**裡的個資遮蔽      | **沒有** —— `maskName` 全樹只在 `OrderList.vue` 被用     |
   | `totalCents`   | `Intl.NumberFormat` 的貨幣格式化           | **沒有** —— 全樹只有 `OrderList.vue:53`                  |
   | `placedAt`     | **什麼都沒有**                             | —                                                        |

   `placedAt` 的實測：`git grep placedAt`（排除裁決／CHANGELOG／reports）只回
   **一行** —— `features/order/src/api.ts:16` 的宣告本身。沒有被渲染、`bff-mock` 的
   `DEMO_ORDERS` 也沒給它。**示範資料不滿足切片自己的型別，而沒有東西會紅**（#318 的實例）。

3. **`features/order` 還扛第四樣：全樹唯一的非唯讀權限碼。**
   `apps/console/bff-routes.ts:85`：它宣告了取消（`order:write`），mock 的預設 session
   沒有，所以那條路由的 403 路徑是這棵樹唯一在示範「權限碼擋得住寫入」的地方 ——
   另外兩片都只有 `:read`。

4. ⚠️ **`tests/masking.test.ts` 是 `platform/pii` 那個設計決定的舉證。**
   `platform/pii/src/index.ts:21` 明文：「現在的形狀有一道會紅的閘門與**一支斷言
   渲染結果的元件測試**」。而 `platform/pii` **刻意零框架相依** —— 沒有
   `vite.config.ts`、沒有 happy-dom、`dependencies` 是空的、`devDependencies`
   沒有 `vue`／`@vue/test-utils`。搬那支測試過去要替它加四個相依，並切斷那條舉證鏈。

5. **它讀起來像殘骸，是因為沒有東西記錄它扛什麼。** 上面四樣沒有一樣寫在
   `features/order/README.md` 裡 —— 那份 README 寫的是邊界、結構與命名空間，
   全部是**三片共通**的東西。三片各自扛什麼，樹上零處記載。

#### 二、裁決

1. **刪 `placedAt`（`api.ts:16`）。** 唯一的純殘骸：宣告成必填、全樹零消費者、
   示範資料也沒給。⚠️ 這**不是**在解 #318 —— #318 問的是「示範資料形狀要不要對切片
   型別檢查」，那道機制仍然沒有；本則只是拿掉它的一個實例。

2. **其餘三個欄位一個字不動。** 它們各自是全樹唯一的能力示範（§一 2）。
   ⚠️ 拿掉它們與整片刪掉是同一類損失，只是換個方式。

3. **`features/order/README.md` 補一節，寫明這一片扛的四樣示範、逐項的落點、
   以及「別處有沒有」。** 這一節才是本則真正的產出 —— 它讓下一個人（或下一個 agent）
   不必重跑一次這次的量測就答得出「order 是不是殘骸」。
   ⚠️ 同一節寫明 `Order` 的欄位是**示範用的詞彙不是業務需求**，換掉是採用團隊的預期動作。

4. **`masking.test.ts` 不搬也不刪**，理由寫進同一節（§一 4 的舉證鏈）。

#### 三、不裁

- **不整片刪 `features/order`。** §一 1 的閘門修復指示 ＋ §一 2、3 的四樣示範。
- **不動 `features/shipment`。** 它也沒有新架構、也帶著 `// TODO: 補上這個切片實際的欄位`，
  但本則沒有量過它扛什麼 —— **不拿 order 的結論套到它身上**（那正是本則證明會出錯的推論方式）。
- **不把 `masking.test.ts` 改成掛真的 `OrderList.vue`。** 要掛就要建 pinia、router、
  QueryClient，而 C114 明文裁過那層設施「貴到沒有專案組願意用」。
  「`OrderList.vue` 有沒有繼續呼叫 `maskName()`」仍然靠 review，登記在 `HANDOFF.md:1290`。
- **不替 `slice-shape.ts` 那兩條訊息改指向 `features/invoice`。** 它們今天指得到，
  而 §二 3 讓「為什麼指 order」有了答案。

#### 四、方法上值得記的

⚠️ **這一則有兩次是我自己的提案被自己的量測推翻。**

| 第幾版提案                                 | 被什麼推翻                                                         |
| ------------------------------------------ | ------------------------------------------------------------------ |
| ① 整片刪掉 `features/order`                | 閘門的修復指示會指向不存在的檔；三樣示範是全樹唯一                 |
| ② 抽掉「業務資料形狀」四個欄位             | 逐欄查之後只有 `placedAt` 是死的，另外三個各自扛一樣全樹唯一的示範 |
| ③ 把 `masking.test.ts` 搬去 `platform/pii` | 那支是 `platform/pii` 自己引用的舉證，而該 package 刻意零框架相依  |

三次的共通點：**「看起來像重複／殘骸」的判斷，只有逐項問「有沒有機制在用它」才站得住。**
與 C168 §一 的變異法、C203 §二 1 的「守衛在哪一層」是同一條紀律，換到「刪一片切片」的尺度上。

#### 五、與既有裁決的關係（C136 §八）

| 既有                     | 本則做了什麼                                                                                            |
| ------------------------ | ------------------------------------------------------------------------------------------------------- |
| **C137 §一**             | **遵守** —— `apps`／`features` 是示範不是產品；本則正是在問「示範什麼」                                 |
| **C196**                 | **接續** —— 它刪掉 `masking.test.ts` 重複的四條、留下渲染那半；本則說明留的理由在 `platform/pii` 那一頭 |
| **C114**                 | **引用** —— §三 不改 `masking.test.ts` 掛載方式的理由                                                   |
| **C168 §一／C203 §二 1** | **同一條紀律換尺度** —— 「重複」要逐項問「有沒有機制在用它」                                            |
| **#318**                 | **拿掉一個實例，不解那件事** —— 示範資料對型別檢查的機制仍然沒有                                        |

### C206 — `features/shipment` 移除：同一套逐項法、相反的答案 —— 它是 `features/invoice` 減掉架構，沒有扛任何全樹唯一的東西（2026-09-09）

⚠️ **量測基準 `753d025`**（C205 合併後的 `main`），worktree `drop-shipment-slice`。
⚠️ **C205 §三 明文「不拿 order 的結論套到 shipment 身上」** —— 本則是照同一套方法
獨立量它，而不是套用。答案相反，正好證明那條規矩有用。

#### 一、事實

1. **逐項查「它扛什麼、別處有沒有」：**

   | 東西                                   | shipment             | 別處有嗎                                |
   | -------------------------------------- | -------------------- | --------------------------------------- |
   | 設計系統元件（`UiButton`／`UiDialog`） | 有                   | **有** —— `order` 與 `invoice` 兩片都用 |
   | store 的內容                           | `page`＋`selectedId` | **有** —— 與 `invoice` 逐項相同         |
   | 資料形狀                               | `id` ＋ `// TODO`    | **有** —— 與 `invoice` 逐字相同         |
   | 測試                                   | 三個 describe        | **有** —— 產生器模板的原樣產出          |
   | 架構（`ports`／`usecases`／`specs`）   | **沒有**             | `invoice` 有                            |
   | 能力示範                               | **沒有**             | `order` 有四樣（C205 §一 2、3）         |

   **它是 `features/invoice` 減掉架構。**

2. **它的出身寫在 `tools/slice-gen/README.md:14`：**
   `vp create slice -- --directory=../features/shipment --slice=shipment …`
   —— 它就是「跑一次產生器會得到什麼」的樣本，而那個角色 `invoice` 已經在扮，
   還多帶了架構那一層。

3. **引用掃描：只有兩處是真相依。**

   | 位置                                       | 性質                                             | 處置              |
   | ------------------------------------------ | ------------------------------------------------ | ----------------- |
   | `conformance/tests/negative.test.ts:57`    | **複製真目錄**進沙盒（`:46` 說明要有第二片切片） | 換 `invoice`      |
   | `bff-mock/tests/routes.test.ts:208`        | 斷言 mock 預設權限含 `shipment:read`             | 換 `invoice:read` |
   | `doc-facts/tests/derive.test.ts:49`        | `workspace([...])` 合成字串                      | 不動              |
   | `pii-check/tests/enumeration.test.ts:85`   | `box.write(...)` 合成字串                        | 不動              |
   | `conformance/tests/rules.test.ts:109`      | 假的 `package.json` 物件                         | 不動              |
   | `conformance/src/rules/dependencies.ts:14` | 檔頭的假想例子                                   | 不動              |
   | `promise-check/src/breakage.ts:38`         | 檔頭在說「不要寫死 order／shipment」             | 不動              |

   ⚠️ **合成字串不需要真目錄存在** —— 這一格是逐項確認過的，不是推論。

4. **兩個寫死的數字被閘門抓到，而那正是刪除有沒有傳到位的證據。**
   刪掉 `CODEOWNERS` 一列之後 `doc-facts` **RC=1**：

   ```
   [mismatch] HANDOFF.md 寫著 22，而 CODEOWNERS 的條目數是 21
   [mismatch] README.md 寫著 35，而 workspace 內的 package 數是 34
   ```

   ⚠️ 兩處都是**人抄下來的數字**，而 `doc-facts` 存在的理由就是這個。改完 RC=0。

5. **根 `README.md` 的目錄樹一直沒有列 `invoice`。** 它從 `0645fba`（2026-09-05）
   進樹到今天四天，README 只列 `order/` 與 `shipment/` —— **沒有閘門在守目錄樹那一段**。
   本則順手補上。

6. ⚠️ **`git rm -r` 不夠 —— 目錄被 gitignore 掉的殘留檔撐著活下來。**
   刪完之後 `features/shipment/` 磁碟上還在（剩 `.DS_Store`、`.vitest-results.json`、
   `node_modules`），而 `conformance` **走磁碟不走版控**，於是它看到一片
   「缺 `package.json`／`tsconfig.json`／`README.md`／`src/index.ts`」的切片，**RC=1**。
   ⚠️ 這是「它判定要吃的資料，哪些不在版控裡」那一族的又一個實例 ——
   `rm -rf` 之後才綠。

7. ⚠️⚠️ **而本機全綠的那一版，CI 在 20 秒就掛了 —— `pnpm-lock.yaml` 沒跟著更新。**
   `vpr ready` 讀的是**已經裝好的** `node_modules`，而 CI 走
   `vp install --frozen-lockfile`：`ERR_PNPM_OUTDATED_LOCKFILE`，
   「specifiers in the lockfile don't match specifiers in package.json:
   \* 1 dependencies were removed: `@org/feature-shipment@workspace:*`」。
   ⚠️ **動任何 `package.json` 的相依都要跑一次 `vp install` 並把 lockfile 一起提交** ——
   本機的綠證明不了這一格，這是這棵樹記過的「`ready` 綠證明不了 CI 綠」的第六次。
   跑完的 diff 是 **−52／0**，唯一被移除的 importer 是 `features/shipment`，
   且 `git diff --summary` 為空（`pnpm install` 這次沒有翻動檔案模式）。

#### 二、裁決

1. **刪 `features/shipment`（13 支檔，外加 `rm -rf` 掉 gitignore 的殘留）**，連同 `apps/console` 的註冊與相依、
   `bff-mock` 的 `shipment:read` 權限碼／`DEMO_SHIPMENTS`／`/api/shipment` 路由、
   `CODEOWNERS` 那一列。

2. **`conformance/tests/negative.test.ts` 的第二片切片換成 `features/invoice`。**
   那組測試要有第二片切片才驗得了「切片不得相依切片」（該檔 `:46` 明說理由）——
   換對象，不是刪測試。

3. **`theme-verify/README.md` 那張表與「6 處」一個字不動。** 它們是 2026-08-17
   那次乾跑的**歷史記錄**（「乾跑撞到 4 處真陽性、0 偽陽性」），不是現況清單 ——
   照 C138 不回頭改。

4. **`slice-gen/README.md` 的那行指令留著，加一句說明。** 它記的是**那次已驗證的執行**，
   不是樹上的位置；產出的切片被本則移除這件事寫在旁邊。

5. **樹上剩兩片，各扛一件事**：`invoice` ＝ 架構範本、`order` ＝ 能力示範。
   根 `README.md` 的目錄樹照這個寫。

#### 三、不裁

- **不動 `features/order`。** C205 逐欄查過它扛四樣全樹唯一的示範。
- **不把 `invoice` 的架構補進 `order`，也不把 `order` 的示範搬進 `invoice`。**
  兩片各扛一件事是本則的結果，不是要繼續收斂的中間狀態；合併它們是新的決定。
- **不替「切片數量」立任何檢查。** 兩片、三片、一片都合法 —— 這棵樹沒有理由規定它。
- **不改 HANDOFF.md 裡關於 shipment 的歷史敘述**（`:1028` 的 C41 事件、`:1830`／`:1978`
  的計數記錄）。只改被 `doc-facts` 抓到的那兩個**現況數字**。

#### 四、實測

| 步驟                                    | 結果                                       |
| --------------------------------------- | ------------------------------------------ |
| 刪除後 `doc-facts`                      | **RC=1**，點名 HANDOFF 22→21、README 35→34 |
| 兩個數字改完                            | `doc-facts` **RC=0**                       |
| `vpr ready`                             | READY_RC=0                                 |
| 刪除後全樹掃 `shipment`（排除歷史記錄） | 只剩合成字串與檔頭的假想例子               |

#### 五、與既有裁決的關係（C136 §八）

| 既有           | 本則做了什麼                                                             |
| -------------- | ------------------------------------------------------------------------ |
| **C205**       | **同一套方法、相反的答案** —— 而 C205 §三 明文禁止套用結論，本則獨立量過 |
| **C138**       | **遵守** —— `theme-verify` 的歷史記錄與 HANDOFF 的歷史敘述一個字不動     |
| **C189 §二 3** | **換對象不刪測試** —— 沙盒的第二片切片改用 `invoice`                     |
| **D7**         | **引用** —— `apps/console/src/features.ts` 是唯一知道有哪些切片的檔案    |
| **D15**        | **不受影響** —— 設計系統的採用示範在 `order` 與 `invoice` 兩片都在       |

### C207 — C204 補了一條閘門規則而一句描述它的散文都沒改：四處裡有一處是產生器，而 C201 在九小時前才剛裁完同一件事（2026-09-09）

⚠️ **量測基準 `ff6e3d2`**（C206 合併後的 `main`）。
⚠️ 本則是 **C204 的續集**（C138：更正寫成續集，不改原文），同時是 **C201 §二 的第二個實例**。

#### 一、事實

1. **C204 的射程**：契約常數 → 規則實作 → 五條反向測試。**散文零處。**
   而 D14 那條界線在樹上有**六處**散文在描述它，六處在 C204 之後全部說得比實際少：

   | 位置                                       | 它說的                             | 少了什麼             | 處置       |
   | ------------------------------------------ | ---------------------------------- | -------------------- | ---------- |
   | `tools/conformance/src/rules/layering.ts`  | 「兩條規則」，列 1 命名 ＋ 1 views | `store` ＋ `usecase` | 改         |
   | `platform/slice-kit/src/contract.ts`       | views 那條是「D14 唯一有牙齒的」   | `store` ＋ `usecase` | 改         |
   | `README.md`（根層）                        | 第四層只列 `src/views/`            | `store` ＋ `usecase` | 改         |
   | `tools/slice-gen/src/files.ts`（**範本**） | 列 `views` ＋ `store.ts`           | **`usecase`**        | 改（流量） |
   | `features/invoice/README.md:18`            | 同上，逐字相同                     | **`usecase`**        | 改（存量） |
   | `features/order/README.md:18`              | 同上，逐字相同                     | —                    | **不改**   |

   ⚠️ 最後兩列**逐字相同而處置相反**，理由在〈三、不裁〉：`order` 那一片磁碟上沒有
   `src/usecases/`。**同一句話的對錯取決於它在描述哪一片。**

2. **⚠️ C204 只弄壞了後三處，前三處已經錯了 25 天。** 逐處查歷史（`git log -L`）：

   | 位置                      | 最後一次動它                       | 在 C204 之前                       |
   | ------------------------- | ---------------------------------- | ---------------------------------- |
   | 根 `README.md:359`        | `e7482f4`（2026-08-15，D14 初版）  | **已假** —— `a46ad47` 當天就讓它假 |
   | `contract.ts:68`          | 同上                               | **已假** —— 同上                   |
   | `layering.ts:30`          | `0e5de47`（2026-08-18，拆 cli.ts） | **已假** —— 搬過位置，沒重讀內容   |
   | `files.ts:191` 範本       | **`0b56217`（2026-08-15）**        | 正確 —— 當天就補上 `store`         |
   | `features/invoice/README` | 同 `0b56217` 那一批                | 正確 —— 同上                       |
   | `features/order/README`   | 同上                               | 正確，**而且今天仍然正確**         |

   ⚠️ **這件事有兩個獨立的失效來源，不是一個**：C204 讓後三處變短（其中一處是產生器），
   而前三處在 D14 落地當天就短了，25 天沒有人發現。

3. **⚠️⚠️ 而 `0b56217` 的 commit 標題是「D14 落地後對照文件，抓到三處漂移」。**
   那是一次**刻意的散文普查**，它掃到了產生器範本與兩片切片的 README，
   **漏掉的正是上面那三處** —— 規則自己的檔頭、契約自己的檔頭、根層 README。
   ⚠️ 掃的是「切片那一側」的文件，沒掃「規則那一側」的文件。
   **一次普查會漏掉哪一半，取決於掃的人當時站在哪一邊。**

4. **⚠️⚠️ 時間差：C201 在 `9093d92`（09:21）合併，C204 在 `4503c5a`（18:29）合併 —— 相隔 9 小時 8 分。**
   C201 §二 的處置逐字是「改任何會被複製／產生的文字之前先問『產生這份東西的那一份改了沒』」。
   C204 走的是反方向（先改規則、沒改任何文字），所以那條檢查**在它身上一次都沒有被觸發** ——
   ⚠️ **C201 的形狀是「改文字時要想到產生器」，而 C204 的形狀是「改規則時要想到文字」。**
   同一個產生器範本、同一天、隔九小時，第二次漏掉。

5. **⚠️ 第一次量 §四 那兩個數字時兩個都低估了，成因是 `head -20`。**
   `git grep -n '兩條規則'` 我讀成 13 行（真值 15）、`git grep -l 'D14'` 讀成 20 個檔
   （真值 21），而**被截掉的那一個檔正好是 `tools/slice-gen/tests/contract-alignment.test.ts`**，
   從它才找到 `features/*/README.md:18` 那兩處。⚠️ 這是本樹記過的「截斷」那一層：
   量測本身完全正確，壞的是我只讀了它的前綴。**量「有幾個」先取總數，再決定怎麼讀。**

6. **⚠️ 重刷 #318 的證據時順帶掃到三處，成因不是同一個。**
   那張票指著 `platform/bff-mock/src/server.ts`，於是連著讀了它與 `apps/console`
   那支絆線：

   | 位置                                        | 它說的                                                     | 真相                                                       | 成因                    |
   | ------------------------------------------- | ---------------------------------------------------------- | ---------------------------------------------------------- | ----------------------- |
   | `bff-mock/src/server.ts:441`                | 「`?page=` 與**出貨那條**一樣刻意不理」                    | 出貨那條路由已由 **C206** 移除                             | **C206 沒掃到**（今天） |
   | `bff-mock/src/server.ts:168`                | 「**第三片**切片…**而這一條路由沒有**」                    | 路由由 C195／#317 補在下面十行；切片今天兩片               | 兩件事各過期一次        |
   | `apps/console/tests/bff-routes.test.ts:220` | 「`features/invoice/src/api.ts` **檔頭警告的正是這件事**」 | ⚠️ **那個警告不存在** —— 查了 `2e8890e` 當時的檔案，也沒有 | **寫下的那天就是假的**  |

   ⚠️⚠️ 最後一列不是「過期」，是**引用從第一天就指不到** —— 而它與前兩列
   一起躺在同一輪掃描裡，長得一模一樣。C141 守的是「裁決編號指得到」，
   **指向檔案內容的引用沒有任何東西在守**。

7. **沒有任何閘門會紅。** `doc-facts` 比對的是**數字**與事實來源，不是「這句話列的規則齊不齊」；
   `conformance` 不讀自己的檔頭。四處全是 `AGENTS.md` 註解章節說的那一類
   ——「comment quality is not statically verifiable」。

#### 二、裁決

1. **五處改、一處刻意不改**，改法一致：說「幾條」的地方逐條列出來，說「唯一」的地方改成第幾條。
2. **⚠️ 三處不抄清單，改成指過去。** `USECASE_FORBIDDEN_IMPORTS` 有 **5 個成員**
   （`vue`／`pinia`／`vue-router`／`vue-i18n`／`@tanstack/vue-query`），
   而根 README 與產生器範本第一版的草稿只抄了 3 個 —— 抄的當下就短了兩個。
   定案改成指向契約常數。⚠️ **會長的清單能指過去就不要抄下來**，理由與
   C120 那條「glob 與數字都從契約取」同形：抄的那一份不會有東西告訴你它過期了。
3. **§一 6 那三處一併改**，理由同上：它們在同一輪掃描裡，分兩支 PR 送只會讓
   第二支失去「為什麼現在掃到它」的上下文。
4. **不替這件事立閘門。** 見〈三、不裁〉。

#### 三、不裁

- **不加「散文列舉必須完整」的檢查。** 它要語意分析，而 `AGENTS.md`
  的註解章節明文把註解品質留在 review 那一層（C201 §二 3 同一格）。
  ⚠️ 這不是拿成本論證（C137 §一 不准），是「這個問題沒有靜態判準」。
- **不改 `features/order/README.md:18`，而 `features/invoice/README.md:18` 要改。**
  ⚠️ 兩片的那一行**逐字相同**，處置不同的理由是逐項查過磁碟：
  `features/invoice/src/` 有 `usecases/`，`features/order/src/` **沒有**
  （C205：`order` 是能力示範、`invoice` 是架構範本）。切片 README 描述的是
  **那一片有什麼**，不是規則清單 —— 對 `order` 補一層它沒有的東西是寫假話。
  ⚠️ `layering.ts` 的 `existsSync(usecasesDir)` 讓那條規則在 `order` 上恆真，
  這也是為什麼沒有任何閘門分得出這兩行的差別。
- **不改 `0b56217` 的 commit 訊息。**
- **不把「改規則要順手掃散文」寫成規則。** 這棵樹已經有 C201 §二 那一句，
  本則加的是它的**反方向實例**，不是第二條規矩。

#### 四、實測

| 步驟                                    | 結果                                          |
| --------------------------------------- | --------------------------------------------- |
| `git grep '兩條規則'` 全樹（`ff6e3d2`） | **15** 處命中，只有 `layering.ts:30` 那處是它 |
| `git grep -l 'D14'` 全樹（`ff6e3d2`）   | **21** 個檔，逐檔讀，**六處**在描述那條界線   |
| ⚠️ 第一次量這兩個數字時寫成 12／20      | `head -20` 截斷了輸出，兩個都低估（見 §一 6） |
| `git log -L` 逐處查最後一次改動         | 見 §一 2 那張表                               |
| `vpr ready`                             | READY_RC=0                                    |

#### 五、與既有裁決的關係（C136 §八）

| 既有         | 本則做了什麼                                                                          |
| ------------ | ------------------------------------------------------------------------------------- |
| **C204**     | **續集** —— 補上它沒做的那一半：規則接上了，描述它的四處散文一處都沒改                |
| **C201 §二** | **第二個實例，方向相反** —— C201 是「改文字漏了產生器」，本則是「改規則漏了全部文字」 |
| **C196**     | **引用** —— 「兩條規則」數量自洽而枚舉少列，逐列對出來的，不是對數量對出來的          |
| **C120 §七** | **同形** —— 會長的清單指過去、不抄下來，理由一樣是「抄的那份不會告訴你它過期了」      |
| **C137 §一** | **遵守** —— 〈不裁〉那條的理由是「沒有靜態判準」，不是成本                            |
| **C138**     | **遵守** —— C204 的原文一個字不動                                                     |

### C208 — C203 的兩步判定有一個沒說出來的前提：閘門那一步要真的走到那支檔。第三輪 48 顆，而 `tools/supply-chain` 是目前量到真缺口密度最高的一包（2026-09-09）

⚠️ **帳本凍結於 `9093d92`**（C202 修好之後的第一趟，8 分 55 秒）；**樹在 `ff6e3d2`**。
兩者對 `tools/exit-drill` 與 `tools/supply-chain` 的 `git diff --numstat` 是**空的**，
所以報表裡的 `location` 偏移仍然指得到 —— ⚠️ **這是套任何一顆變異之前的第一道檢查**，
不驗它，「測試沒紅」量到的是錯的位元組。

#### 一、方法：C203 §二 3 加一步

C203 的判定是「套上變異 → 跑該 package 測試 → 跑閘門 CLI，兩者都綠＝真缺口」。
本輪照跑，而且**先補了一步**：

> ⚠️⚠️ **那個判定假設「閘門 CLI 會走到那支檔」，而這件事本身要量。**
> `tools/exit-drill/src/cli.ts:855` 是 `process.exit(FLAGS.full ? runFull() : runStatic())`
> —— 閘門鏈第 6 步不給旗標，走的是 `runStatic()`。⚠️ 有四支模組只有 `--full` 走得到，
> 而 `--full` 每季一次、還要先建置（C185）。**在那幾支上，「閘門也綠」的意思是
> 「我跑的那個模式看不見它」，不是「沒有東西在守」** —— 兩者長得一模一樣。

量法是一趟 `NODE_V8_COVERAGE=<dir> node tools/exit-drill/src/cli.ts`，逐模組數
執行到的 range：

| 模組                   | 閘門步驟執行到的 range | 讀法                                        |
| ---------------------- | ---------------------- | ------------------------------------------- |
| `plugins.ts`           | **45／53**             | 走得到 —— `runStatic` 呼叫 `accountPlugins` |
| `dependencies.ts`      | 10／12                 | 走得到                                      |
| `counts.ts`            | 10／14                 | 走得到                                      |
| `tree-fingerprint.ts`  | 5／11                  | 部分（`checkFreshness` 的回呼）             |
| `freshness.ts`         | 3／12                  | 部分                                        |
| `expected-failures.ts` | **1／4**               | **只有模組載入** —— `--full` 才走           |
| `drill-workspace.ts`   | **1／3**               | 同上                                        |
| `evidence.ts`          | **1／2**               | 同上                                        |

⚠️ **這張表推翻了一個看起來很合理的預期**：`plugins.ts`（53 顆，exit-drill 最大戶）
本來被我當成「只有 `--full` 才碰得到」，實測它在閘門步驟裡跑掉了 45 個 range。
**「這支檔屬於完整演練」是位置論證，不是量測。**

#### 二、對照組（兩個方向都放）

| 對照                                              | 結果                         |
| ------------------------------------------------- | ---------------------------- |
| 無變異的乾淨樹                                    | 測試 **RC=0**、閘門 **RC=0** |
| 已知 `Killed` 兩顆（`inventory.ts` 11663／11623） | 測試 **RC=1** —— 量測台會紅  |

⚠️ 少了第二列，「48 顆都沒紅」與「腳本根本沒把變異寫進去」是同一個讀數。

#### 三、結果：48 顆，10 顆假存活（20.8%）

**`tools/supply-chain`（160 顆存活，抽 24）**

| 檔案            | 存活 | 抽樣 | 假             | 真     | 備註                         |
| --------------- | ---- | ---- | -------------- | ------ | ---------------------------- |
| `inventory.ts`  | 52   | 6    | 3              | 3      | 三顆假的都由該包自己的測試紅 |
| `health.ts`     | 43   | 6    | 0              | 6      |                              |
| `lockfile.ts`   | 38   | 6    | 0              | 6      |                              |
| `provenance.ts` | 27   | 6    | 0              | 6      |                              |
| **合計**        | 160  | 24   | **3（12.5%）** | **21** |                              |

**`tools/exit-drill`（141 顆存活，抽 24，涵蓋其中 125 顆所在的四支）**

| 檔案                  | 存活 | 抽樣 | 假           | 真     | 閘門那一步的可信度      |
| --------------------- | ---- | ---- | ------------ | ------ | ----------------------- |
| `plugins.ts`          | 53   | 6    | 0            | 6      | 可信（45／53 range）    |
| `freshness.ts`        | 31   | 6    | 4            | 2      | 部分                    |
| `tree-fingerprint.ts` | 17   | 6    | 1            | 5      | 部分                    |
| `drill-workspace.ts`  | 24   | 6    | 2            | 4      | ⚠️ **不可信** —— 見 §一 |
| **合計**              | 125  | 24   | **7（29%）** | **17** |                         |

⚠️ `drill-workspace.ts` 那四顆「真存活」**只有測試那一半是量到的**；
閘門那一半在這個模式下是空轉，所以它們的正確標籤是**未判定**，不是真缺口。

**累計（C203 ＋ 本輪）：72 顆抽樣、19 顆假（26.4%）。**

#### 四、`tools/supply-chain` 是 `phantom-deps.ts` 的同形，而它更大

C203 §三 判 `conformance/src/rules/phantom-deps.ts` 是真缺口，形狀是
**「閘門規則自己的解析邏輯，而閘門跑在一棵乾淨的樹上，那條路徑走不到紅」**。
本輪 `supply-chain` 三支檔 **18／18 全真**，是同一個形狀而且量到的規模更大：

- 它的資料（`inventory.json`）**在版控裡**，閘門每一趟都真的讀它、真的比對；
- 所以那些規則**確實在執行**（V8 覆蓋：`inventory.ts` 52／59、`lockfile.ts` 37／48）；
- 而把判定改壞之後**兩邊都綠** —— 因為真實資料本來就是合法的，
  **改壞的是「它認不認得出不合法」，而樹上沒有不合法的東西可以拿來問它。**

⚠️ 這一格與 C203 §三 的差別只有一個：那裡是一支檔 83 顆，這裡是三支檔 108 顆。
**判準沒有變，量到的規模變了。**

#### 五、裁決

1. **本則只交方法與數字，不裁任何一格要不要補測試。** 判「某一條測試該不該補」
   走 C168 §一 的變異法，而那要逐顆做，不是看表。
2. **⚠️ 往後套 C203 §二 3 之前，先量「閘門走不走得到」。** 那一步是一趟
   `NODE_V8_COVERAGE`，成本比它接住的錯誤低兩個數量級 —— 本輪它救回的是
   `drill-workspace.ts` 那四顆（差一點被寫成真缺口）與 `plugins.ts` 那六顆
   （差一點被寫成「量不到」）。**兩個方向都錯過。**
3. **不拿累計的 26.4% 折算任何一包。** 逐檔仍然是 0%～83%（C203 §四 已記），
   本輪再添兩個極端：`health`／`lockfile`／`provenance` **0%**、`freshness` **67%**。

#### 六、不裁

- **不替「假存活」立自動偵測。** 它的定義是「工具說存活而實際有東西在守」，
  而「有東西在守」這一半要跑閘門 —— Stryker 只跑測試（C203 §二 2）。
  ⚠️ 這是機制上的界線，**不是**成本論證（C137 §一）。
- **不改 `ignoreStatic: true`。** 它是安全性的不是效能的（見記憶與 TESTING.md）。
- **不重跑 Stryker 來刷新總數。** 一趟 8 分 55 秒，而跑完會往 index 推 140 支插樁碼、
  刪光本機 tag；本則的結論（逐檔差異、supply-chain 的形狀）不依賴總數。

#### 七、與既有裁決的關係（C136 §八）

| 既有           | 本則做了什麼                                                                       |
| -------------- | ---------------------------------------------------------------------------------- |
| **C203 §二 3** | **補上它沒說的前提** —— 判定多一步：先量閘門走不走得到那支檔                       |
| **C203 §三**   | **同形、更大** —— `supply-chain` 三支 18／18 全真，與 `phantom-deps.ts` 同一個形狀 |
| **C202**       | **依賴** —— 沒有它修好乾跑，這份帳本不存在                                         |
| **C185**       | **引用** —— `--full` 在 CI 上空轉那件事，正是本則「模式決定量到什麼」的先例        |
| **C168 §一**   | **不越界** —— 本則交數字，不判任何一條測試該不該補                                 |
| **C137 §一**   | **遵守** —— §六 兩條的理由都是機制界線，不是成本                                   |

#### 八、順帶更正 C207 的標題數字（C138：更正寫成續集，不改原文）

⚠️ **C207 的 `###` 標題寫「四處裡有一處是產生器」，而它自己的 §一 1 那張表是六列。**
成因是我在同一支 PR 裡把 §一 1 從四處改成六處（掃到 `features/*/README.md:18` 那兩處），
**改了表、沒回頭改自己的標題** —— commit 訊息、PR 標題與 `CHANGELOG.md` 那一列都是「六處」，
只有 `###` 那一行留在舊值。

**以 §一 1 那張表為準：六處，五處改、`features/order/README.md:18` 刻意不改。**

⚠️⚠️ 而這件事本身是 C207 §一 5 記的那一格**在同一則裁決裡又發作一次**：
散文裡的計數沒有任何閘門在守，`decision-ids` 只驗編號指得到、`doc-facts` 只比對
事實來源的數字 —— **一則裁決的標題與它自己的內文對不上，全綠。**
（同 C196「數量自洽會掩蓋枚舉少列」的反面：**枚舉對了，而摘要那一行留在舊值**，
而這次兩個數字相隔 120 行。）

### C209 — 必填理由留在測試那一層：六條斷言現在全部量過，而三個手足裡有一個的問題本身是壞的 —— `ui-survey` 沒有閘門可以搬（2026-09-09，#222）

⚠️ **量測基準 `68ea095`。** 本則關掉 **#222**，那是 C155 §七 具名留下的留白：
**必填理由該落在閘門那一層還是測試那一層？** ⚠️ 它是**未決**不是**已決為不做**，
所以「不裁、關票」不是中性選項 —— 一條沒有人主張過的預設會自己取得效力。

#### 一、六條斷言，現在全部量過

C155 §三 量了 `gate-roster` 那三格；剩下三支它明文沒量（#222 開票時把那件事
列為關票前要答的第一項）。本則逐支量完，方法一字不改：把欄位改成 `"短"`
（門檻是 `> 20`），分別取 `vpr gate` 與**該 package 內**的 `npx vitest run` 結束碼。

| 工具             | 欄位                   | `vpr gate` | 該包 `vitest` | 誰量的         |
| ---------------- | ---------------------- | ---------- | ------------- | -------------- |
| 對照組（乾淨樹） | ——                     | **0**      | **0**         | 本則           |
| `gate-roster`    | `Gate.why`             | **1**      | 1             | C155 §三＋本則 |
| `gate-roster`    | `Ungated.why`          | 0          | **1**         | C155 §三       |
| `gate-roster`    | `Variant.why`          | 0          | **1**         | C155 §三       |
| `gate-roster`    | `notInGateScript`      | 0          | **1**         | C155 §三       |
| `pii-check`      | `rule.why`             | 0          | **1**         | **本則**       |
| `exit-drill`     | `entry.reason`         | 0          | **1**         | **本則**       |
| `ui-survey`      | `candidate.eliminated` | 0          | **1**         | **本則**       |

**六條全部會擋人**，五條擋在測試那一層、一條擋在閘門那一層。
⚠️ **沒有一條是「沒人守」** —— #222 票面已經寫明這一點，本則只是把它量完。

#### 二、⚠️ 而其中一個 `0` 不是量測，是定義

C208 §一 剛裁過：「閘門也綠」要能當證據，前提是**閘門那一步真的走到那支檔**。
把那一步套回本則：

| 工具         | 名冊位置                  | 閘門步驟走得到那支檔嗎（`NODE_V8_COVERAGE` 實測）      |
| ------------ | ------------------------- | ------------------------------------------------------ |
| `pii-check`  | `GATES`（`pii`）          | ✅ `scan.ts` **12／18** range —— 那個 `why` 真的被載入 |
| `exit-drill` | `GATES`（`exit-surface`） | ✅ `dependencies.ts` **10／12** range（C208 §一 已量） |
| `ui-survey`  | **`UNGATED`**             | ❌ **不存在** —— 見下                                  |

⚠️⚠️ **`ui-survey` 在 `.github/`、根 `package.json`、根 `vite.config.ts` 裡的命中數
是 0**（普查，非抽樣）。它唯一的執行面是自己 `package.json` 的 `test` script，
由 `vp run -r test` 帶起來。**所以它那一格的 `gate = 0` 不是量測，是定義** ——
沒有任何閘門步驟可以紅，因為沒有任何閘門步驟會跑到它。

**這件事改變了 #222 的第 2 項待答。** 那一項問：C155 §四 替 `Gate.why` 選閘門層的
理由（_「fork 團隊加 `tools/their-thing` 時撞到的應該是閘門，不是一支他們沒在讀的
測試」_）**對三個手足成不成立**。答案不是一個：

- 對 `pii-check`／`exit-drill`：**問得通**，兩支都在閘門鏈上，搬得過去。
- 對 `ui-survey`：**問題本身是壞的**。它刻意不是閘門（`UNGATED` 那一列寫著理由：
  選型市調會因為別人的專案改授權而紅）。**「搬到閘門層」要先讓它變成閘門**，
  而那是另一個提案、另一組兩軸。

⚠️ **三個手足不是三份同樣的問題，是 2 ＋ 1。** 本則若照 C155 §七 那張表逐字讀
（四支工具並列、處置同為「不動」），會把這件事讀丟 —— 同 C136 §八 要求逐則對關係
的理由：**並列不等於同形。**

#### 三、裁決：維持現狀，六條全部留在原地

1. **`Gate.why` 留在 `check.ts`**（C155 §四 的選擇不動）。
2. **另外五條留在測試那一層。** 不搬。
3. **這是一則裁決，不是「沒做」。** 往後要搬任何一條，得先推翻 §四 那三段。

#### 四、為什麼是維持現狀 —— 三段，一段都不是成本論證

1. **D16 兩軸皆零就丟，而這個提案兩軸都零。**
   交付軸：採購／資安／法務沒有收件人在意 `why` 寫在哪一層（C155 §四 已判）。
   迭代軸 ②：**§一 那張表逐支實測都是 1** —— 「在這次搬遷沒有發生的世界裡，
   那個壞法看起來是綠的」是假的，那個世界裡它們已經是紅的。
2. **支持搬遷的唯一論證是被禁止的那一類。** C155 §五 已經點名：
   「同一類要求不該有兩個執行位置」是可維護性論證，**C137 §一 明文不准拿它
   論證增減閘門，兩個方向都不准。** 本則多量了三支，那個論證一個字都沒變強。
3. **⚠️ 而 `ui-survey` 那一支連提案都構造不出來**（§二）。
   一個對三分之一的對象無法成立的統一提案，不是「打折執行」，是**射程寫錯了**。

#### 五、不裁

- **不動 `> 20` 這個門檻，也不動任何一條斷言的措辭。**
  ⚠️ C155 §四 記過它今天咬不到任何一列，而那是刻意的：「今天不痛是巧合，不是設計。」
- **不替 `ui-survey` 判它該不該變成閘門。** 它在 `UNGATED` 且寫了理由，
  要翻案得走 D16 兩軸，那是新提案不是本則的射程。
- **不碰 #219。** 本則只答「哪一層」；#219 問的是「兩軸有沒有被填」，
  ⚠️ 那張票的難題（`why` 是散文、沒有值域）本則一個字都沒有動到它。
- **不因為本則而動版號。** 同 C155 §八：本則不改變任何一條檢查的行為，
  一行程式碼都沒動。

#### 六、與既有裁決的關係（C136 §八）

| 既有         | 本則做了什麼                                                          |
| ------------ | --------------------------------------------------------------------- |
| **C155 §七** | **關掉它具名的留白** —— 六條斷言全部量完，裁定維持現狀                |
| **C155 §四** | **不推廣它** —— 那個理由對兩支成立、對 `ui-survey` 構造不出來         |
| **C208 §一** | **套用** —— 先問「閘門走不走得到」，於是分辨出一個 `0` 是定義不是量測 |
| **D16**      | **依它丟提案** —— 兩軸皆零                                            |
| **C137 §一** | **遵守** —— §四 三段都不是成本論證                                    |
| **C154**     | **不適用** —— 本則不新增任何機械檢查，所以不必報兩軸                  |
| **C136 §八** | **實例** —— 「四支工具並列」被讀成同形，而實際是 2 ＋ 1               |

### C210 — `.semgrep/rules.yml` 的檔頭說「零重疊」而實測是重疊：⚠️ 而正確的結論不是刪規則，是那份說明書寫的理由是假的（2026-09-09，#241）

⚠️ **量測基準 `68ea095`。** 本則關掉 **#241**。
⚠️⚠️ **本則最重要的一句話寫在最前面：#241 掃出的兩處重疊，`derived-redundancy-claims-need-mutation`
那個坑就在旁邊** —— 第二輪測試接縫審查 14 張錯 3 張，**全部錯在把「兩條都紅」讀成
「一條沒有獨有價值」**。本則兩條規則都逐條量過獨有貢獻，**兩條都留**。

#### 一、檔頭那句話，逐字對樹

檔頭原文兩句：

> 與 Tier 1／Tier 2 既有檢查**零重疊**是刻意的。
> （…）所以這裡只放 lint 做不到的那一類（taint），不重複 oxlint 已經擋掉的。

| 宣稱       | 實測                                                                                                                        |
| ---------- | --------------------------------------------------------------------------------------------------------------------------- |
| 零重疊     | ❌ **兩條規則都重疊**                                                                                                       |
| 只放 taint | ❌ `runtime-code-construction` **沒有 `mode: taint`**，它是 `pattern-either` 的單節點比對 —— 正好是那句話說「不放」的那一類 |

⚠️ 第二句尤其值得記：**那份說明書不只描述錯了射程，它描述錯了自己的形狀**，
而形狀是可以一眼看出來的（YAML 裡有沒有 `mode: taint`）。

#### 二、`runtime-code-construction`：三條 pattern 全被蓋掉，而它仍然不是多餘的

三個 pattern 逐條對 oxlint 實測（#241 §一，本則重驗 `new Function` 那條）：

| 探針                           | oxlint（Tier 1）                        |
| ------------------------------ | --------------------------------------- |
| `new Function("return 1")`     | **error** `typescript(no-implied-eval)` |
| `setTimeout("doThing()", 10)`  | **error** 同上                          |
| `setInterval("doThing()", 10)` | **error** 同上                          |

射程上 oxlint 是**嚴格超集**：路徑更寬（全樹，唯一關掉的 `.semgrep/**` 本來就不在
semgrep 的 include 裡）、語言更多（`.vue` 的 `<script setup>` 也掃）、一樣擋 PR。

**⚠️ 而它的獨有貢獻是抑制註解，兩向實測（2026-09-09）：**

| 探針                                                        | `vp lint`                        |
| ----------------------------------------------------------- | -------------------------------- |
| `new Function("return 1")`                                  | **RC=1**，點名 `no-implied-eval` |
| ＋ `// oxlint-disable-next-line typescript/no-implied-eval` | **RC=0**，完全安靜               |

semgrep 不認得 oxlint 的抑制註解。**寫下那一行的那天，只剩這一條在看。**
⚠️ 而全樹**沒有任何閘門在管抑制註解**（`reportUnusedDisableDirectives`／
`no-inline-config` 皆無）。今天普查：`oxlint-disable` **0**、`nosemgrep` **0**、
`eslint-disable-next-line` **3**（`bff-routes.test.ts:108`、`bff-mock/src/cli.ts:59`、
`codemods/run.ts:105`；另兩處命中是散文提及，不是抑制）。

⚠️⚠️ **而這個理由一個字都沒有寫在規則檔裡** —— 寫在那裡的是一個實測為假的理由。
**這就是本則要修的東西。**

#### 三、`tainted-route-input-to-dom-sink`：六個 sink 只有三個是獨有的

ESLint（Tier 2，`no-unsanitized`）逐格實測，本則重驗：

| sink                        | ESLint                              |
| --------------------------- | ----------------------------------- |
| `$EL.innerHTML = $X`        | **error** `no-unsanitized/property` |
| `$EL.outerHTML = $X`        | **error** `no-unsanitized/property` |
| `document.write($X)`        | **error** `no-unsanitized/method`   |
| `location.href = $X`        | 零命中                              |
| `window.location.href = $X` | 零命中                              |
| `window.open($X, ...)`      | 零命中                              |

前三格 ESLint **更寬**（不需要 taint，任何非字面值的指派都紅）—— 這條規則在那三格上
**嚴格較弱**。它實際買到的是**開放重導向**那三格。

⚠️ **而它的 `message` 與檔頭講的都是 DOM sink：買到的東西與說明書寫的不是同一件。**
⚠️ 反向測試（`.semgrep/rules.ts`）兩格都有：`innerHTML`（被蓋掉的那一類）與
`window.location.href`（獨有的那一類）—— **獨有貢獻是有測試守的**。

#### 四、裁決

1. **兩條規則都留，一個 pattern 都不刪。**
2. **改檔頭**：拿掉「零重疊」與「只放 taint」，換成實測結果，並指明第二條規則
   的理由寫在它自己那一條上。
3. **把兩條規則各自的獨有貢獻寫進規則檔** —— 抑制註解不對稱、開放重導向那三格。
4. **⚠️ 前三個 sink 不刪，理由寫下來**：留著是為了讓 taint 的示範完整
   （反向測試就用 `innerHTML` 那一格），**不是因為它們有獨有價值**。
   ⚠️ 不寫下這一句的話，下一個人會第三次做同一次量測。

#### 五、為什麼不刪 —— 而「不刪」的理由不可以是成本

- **`runtime-code-construction` 有量到的獨有貢獻**（§二）。刪它會在
  `oxlint-disable` 寫下去的那天靜默失守，**而那一格今天是 0，所以今天量不到差別**。
  ⚠️ 這正是 `N=1 的時候整批與逐份長得一樣` 那個形狀：**恆綠的檢查與任何東西都不
  衝突，衝突在恆綠消失的那一刻才現形。**
- **`tainted-route-input-to-dom-sink` 的開放重導向那三格零重疊**（§三）。
- ⚠️ **以上兩段都不是「刪掉太可惜」**。C137 §一 明文不准拿成本或過度設計論證
  增減檢查，**兩個方向都不准** —— 本則的理由是「量到了獨有貢獻」，不是「留著比較划算」。

#### 六、不裁

- **不替抑制註解立閘門。**（`reportUnusedDisableDirectives`／`no-inline-config`／
  禁止 `oxlint-disable` 之類。）⚠️ 它是一道**新的機械檢查**，依 C154 要在 D16 兩軸上
  各報一格，而本則沒有做那份報告 —— **本則刻意不順手把它塞進來。**
  ⚠️⚠️ 記下這一格是敞著的：`runtime-code-construction` 留下來的理由現在**承重**於
  「沒有東西在管抑制註解」這個事實，那件事變了它的理由就要重寫。
- **不動 `paths` 的前導斜線那一段。** 那是 #149 的結論，本則沒有重量它。
- **不動 `.semgrep/rules.ts` 的反向測試。** 兩格都在，而且正好各覆蓋一類。
- **不改 `COMPLIANCE.md` 那段對 semgrep 的敘述。** 逐字讀過：它寫「只有兩條規則、
  守的是汙點傳遞與執行期組程式碼」—— **兩條都點名了，沒有宣稱兩條都是 taint**，
  今天仍然正確。
- **不出版號。** 規則行為一個字沒改，改的全是註解。

#### 七、與既有裁決的關係（C136 §八）

| 既有           | 本則做了什麼                                                                       |
| -------------- | ---------------------------------------------------------------------------------- |
| **C201／C207** | **同一家族的第三、四例** —— 樹變了／量測翻案了，而描述它的散文留在原處             |
| **C137 §一**   | **遵守** —— §五 明文標出「不刪」的理由是量到獨有貢獻，不是划不划算                 |
| **C154**       | **遵守** —— §六 那道候選閘門要自己的兩軸報告，本則不代填                           |
| **C52**        | **相鄰不相同** —— 那次是**拆掉**一套機制並寫下代價；本則是**留下**規則並寫下真理由 |
| **#149**       | **不受影響** —— `paths` 前導斜線那一段本則沒重量，一個字不動                       |

### C211 — 名冊每一列要指得到判它的那則裁決：查的是**指針**不是副本，而它上線當天抓到兩列 —— 其中一列的理由從來沒有被獨立做過（2026-09-09，#219）

⚠️ **量測基準 `088a6dd`。** 本則交付 `#219` 提議的那道存在性檢查的**第一段**。
⚠️ **本則不關 `#219`** —— 那張票還按著三件沒解的事，見 §六。

#### 一、事實：23 列逐列普查

| 量測（`GATES` 17 ＋ `UNGATED` 6 ＝ 23 列） | 結果                                                        |
| ------------------------------------------ | ----------------------------------------------------------- |
| `why` 同時寫出「交付」與「迭代」的列       | **0**                                                       |
| `why` 只要出現其中一個詞的列               | **0**                                                       |
| `why` 帶著裁決編號（`C`／`D`／`R`）的列    | **21**                                                      |
| **不帶任何編號的列**                       | **2** —— `GATES` 的 `gate-roster`、`UNGATED` 的 `slice-gen` |

⚠️ 對照組：含「閘門」10 列、含編號 21 列，兩個方向都非零。
⚠️ **兩個獨立機制對出同一個答案**：上面那份是離線普查（python 逐列切），
而規則接上之後 `node tools/gate-roster/src/cli.ts` **RC=1，點名的正是那兩列**。

#### 二、為什麼查「指得到編號」，不查「有沒有寫出兩軸那兩句」

`#219` 票面提的是後者。**本則選前者，理由是 C154 §七 自己那一段：**

> ⚠️ **那一段是指針，不是副本。** （…）理由寫在那個檔頭自己的開場：
> **一份到處都有副本的清單，改動時只有人記得改其中幾處**（那是這份名冊存在的
> 全部理由）。在它裡面放一份 C154 的副本會是反諷的。

查「寫出兩軸」＝要求 23 列各抄一份 C154 的內容 —— **正是那句話說會是反諷的那件事**，
而且成本是 23 列的新散文（`#219` 坑 #2：「回填的內容有一部分沒有人判斷過」）。
查「指得到編號」＝ 2 列，而**回填的內容是去查編號，不是下判斷**。

⚠️ 兩軸的論證留在裁決裡，這一欄只負責**指得到它**。

#### 三、⚠️ 上線當天抓到的兩列，而第二列比第一列尖

**`gate-roster`（`GATES`）**：漏了編號而已。判它的是 C71（四份名冊必須對得上）
與 C132（接到 `main`，而接上的當場抓到三個真漏洞），兩則都在，補上即可。

**⚠️⚠️ `slice-gen`（`UNGATED`）不是同一回事。** 它的 `why` 開頭寫著「**同理**」，
而那個「理」指向的是**它上面那一列**（`codemods`）。往回查，判它的是 C71，
而 C71 那一句是這樣寫的：

> `codemods` 與 `slice-gen` 在 `tools/` 底下但不是閘門（一個改東西、一個產東西，
> 都不判定對錯）。

**那是一節在講 `codemods` 的段落，`slice-gen` 是被順帶帶上的。**
C132 的 `UNGATED` 判斷表列了 `gate-kit`／`csp-verify`／`ui-survey` 三列，
**`slice-gen` 與 `codemods` 都不在那張表上。**

> **這一列的理由從來沒有被獨立做過一次。**

⚠️ 這不是「所以該把它接成閘門」—— 本則**不重判它**（§八）。本則做的是把這件事
**寫進那一列**：編號補上去，並註明那個判斷是順帶帶上的、要翻案先讀 C71 那一節。
⚠️ 同 `measure-each-sibling-before-bundling` 記的形狀，只是方向相反：那次是
「四個同語意的欄位只有一個是靜默的」，這次是**兩個並列的對象只有一個被判過**。

⚠️ 而「上線當場抓到東西」在這棵樹有先例，C132 的標題就是：
**「接上的當場抓到三個真漏洞」**。

#### 四、本則自己的 D16 兩軸（C154 §三，三條逐條）

**第 1 條 —— 兩軸各報一格。**

| 軸       | 這一條                                                              |
| -------- | ------------------------------------------------------------------- |
| **交付** | **零。** 採購／資安／法務沒有一個收件人在意名冊某一列有沒有帶編號。 |
| **迭代** | **有分。** 見下。                                                   |

**第 2 條 —— 迭代軸照 C142 §二 兩條件填，並標明級別。**

- **① 對象在外** ✅ 它報的是**下一個人寫進去的登記內容**，不是這道檢查自己被改壞。
- **② 壞法安靜** ✅ **級別：發生過的實例。**
  證據就是 §三：`gate-roster` 與 `slice-gen` 兩列**今天**指不到任何裁決，
  而 `vpr gate`、`vpr ready`、四道 CI 全綠 —— **它已經發生了，而且沒有人發現。**
  ⚠️ 而 `slice-gen` 那一列更進一步：安靜的不只是「編號沒寫」，是**沒有人獨立判過它**。

⚠️⚠️ **本則刻意不拿 C204 當這一格的實例。** C204 是「兩軸報告填錯」的實例
（記在 `#219` 的留言裡），而**這條規則看不到它** —— 見 §六。
拿一個自己射程外的實例來填自己的 ②，正是本則批評 C204 的那件事。

**第 3 條 —— 自我防護的夾具另立一節，不計迭代軸的分。**

`roster.test.ts` 的 `describe("理由指不到裁決（C211）")` 整個 describe 都是守這條
規則自己的，走 `checkRoster` 的注入參數。**它們不填上面那一格。**
內容：兩側各一條紅（`GATES`／`UNGATED`）、一條 `toEqual([唯一規則名])`、
三種前綴各認一次、一條記錄代價的、一條「真的名冊每一列都通過」。

⚠️ **短路版真的跑過**（C144 記的是它沒被跑的那一次）：整段規則移除之後，
那三條斷言從 `["理由指不到裁決"]` 變成 `[]` —— **紅的確實是這一條，沒有別的規則
在同一格上蓋著它。**

#### 五、代價，寫下來免得被當成保證

1. **一個編號抄上去就過。** 這條規則分辨不了「指得對不對」——
   ⚠️ 一個**指不到任何一則**的三位數編號也會過（有一條斷言記錄這件事，不是要求它）。
   ⚠️⚠️ 那條斷言的假編號**必須用字串組出來、不能寫成字面** —— `decision-ids`（C141）
   掃全樹的編號字面並要求每一個都指得到，寫成字面的話**這一行自己就會讓那道閘門紅**。
   實測到它的是 `vpr ready`，而且**紅了兩輪**：第一輪點名兩處字面（裁決一處、測試一處）；
   ⚠️⚠️ 第二輪只剩一處，而那一處是**我在註解裡引用「對照組不可以叫某個編號字面」
   這條教訓時，把那個編號字面寫進了註解** —— 引用一條規則的那一行本身違反了它。
   **兩道閘門在這一格互相咬住，而它們都是對的** ——
   ⚠️ 而它的修法只有一種：改我的程式碼。那道閘門的紅燈訊息自己寫著
   「不要動本檔的 `ABSENT`（…）放寬它就是改閘門來換綠燈（AGENTS.md 規則二）」。
   同 `SCOPE.md` 桶欄的形狀（C143／C144：機器查填了沒有，填得對不對全綠）。
2. ⚠️ **它不驗編號指得到的那一則真的報過兩軸。** C204 引了 C154 的編號還是填錯了 ——
   **代理與它要分辨的東西同形**（C152 §二），這條規則沒有解決那件事。
3. `\b[CDR]\d{1,3}\b` 的**上界 3 位是刻意的**：沒有上界時 sha512 那種字串會餵出
   假的命中（`decision-ids` 踩過同一個坑）。⚠️ 代價是四位數編號那天要改這條 pattern。

#### 六、⚠️⚠️ 射程缺口：逐工具一列，而要求是逐規則

C154 §四 的射程明文是「**任何新增的機械檢查，包含在既有工具裡加一條規則**」。
**而這份名冊是逐工具一列。**

> **C204 在 `conformance` 裡加了一條規則，沒有新增任何名冊列 ——
> 所以今天唯一確認的「兩軸填錯」實例，這條規則結構性看不到。**

`conformance` 一支就有 `layering`／`slice`／`csp`／`phantom-deps`／`relative-escape`
等多條規則，名冊上是一列。**掛在名冊上的方案，射程上界就是「新增一支工具」。**

⚠️ 本則交付的是 `#219` 的**第一段**，不是全部。**那張票不關**，它還按著三件事：
「查什麼」的散文無值域問題、`delivery`／`iteration` 兩個結構化欄位那條路、
以及這一節的射程缺口。⚠️ 依上一輪的決定，那張欄位票**在 `#219` 關閉時才開**。

⚠️ 而 C154 §八 那條「填寫要求全部靠人讀」**只被兌現了一小塊** ——
兩軸報告本身仍然沒有任何東西在守。**這一句要留著**，否則 `#219` 關掉的那天
它會變成無人主張的預設（這棵樹記過六次同一個形狀）。

#### 七、順帶：兩條規則管同一格，而它當場被既有的絆線抓到

新規則與 C155 的長度規則管同一個欄位。接上的那一刻，`roster.test.ts` 既有的
**兩條 `toEqual` 立刻紅**：

```
expected [ '閘門沒寫理由', '理由指不到裁決' ] to deeply equal [ '閘門沒寫理由' ]
expected [ '理由指不到裁決' ] to deeply equal []
```

⚠️ **那正是 C143 §七 第 4 條 記的形狀**（兩條規則管同一格的話，拿掉一條仍然紅，
於是「紅的是這條規則」就證明不了），而 C155 §四 用 `toEqual` 寫那兩條斷言的
理由就是為了讓這件事被驗到。**它守住了。**

處置：把 C155 那兩條的夾具各加一個編號（短但指得到 → 只有長度那條會紅；
長且指得到 → 全綠），**兩條斷言的意思一個字都沒變弱**。

#### 八、不裁

- **不重判 `slice-gen` 該不該是閘門。** §三 只記錄「它的理由沒有被獨立做過」，
  ⚠️ 那是一個待答的問題，不是一個答案。要翻案走 D16 兩軸，那是新提案。
- **不加「編號必須真的存在」那條。** `decision-ids` 已經在守全樹的編號指得到
  （C141），在這裡再做一次是第二份斷言。⚠️ 而它守的是 `DECISIONS*.md`，
  **`gates.ts` 在不在它的射程裡本則沒有量** —— 沒量的事不寫成結論。
- **不動 C155 那條長度規則的門檻與措辭**（C209 剛裁過維持現狀）。
- **不追溯到 `#219` 以外的地方。** 本則只加名冊這一條。
- **不出版號。** ⚠️ 而它**會擋人**：一列 `why` 沒帶編號的登記升上來就會紅。
  同 C155 §八：版號要判的是 `main` 上積著的那一批合起來算什麼，射程比本則大。

#### 九、與既有裁決的關係（C136 §八）

| 既有               | 本則做了什麼                                                                                    |
| ------------------ | ----------------------------------------------------------------------------------------------- |
| **#219／C154 §八** | **交付第一段** —— 而 `#219` 不關，§六 列了它還按著的三件事                                      |
| **C154 §七**       | **依它選形狀** —— 指針不是副本，所以查編號不查兩軸                                              |
| **C154 §三**       | **逐條過** —— 三條都填了，②的級別是「發生過的實例」，夾具另立一節                               |
| **C155 §四**       | **同一層落點** —— 理由相同（fork 團隊撞到的應該是閘門）；⚠️ 而它的兩條 `toEqual` 當場接住了本則 |
| **C143／C144**     | **同形** —— 機器查「填了沒有」，填得對不對全綠                                                  |
| **C152 §二**       | **代價自陳** —— 代理與它要分辨的東西同形，本則沒有解決那件事                                    |
| **C141**           | **不重做** —— 「編號真的存在」由它守，本則只查「有沒有指」                                      |
| **C71／C132**      | **補上指針** —— 兩列缺的編號就是它們                                                            |
| **C137 §一**       | **遵守** —— 本則的理由是量到的（2 列指不到、1 列沒被獨立判過），不是成本                        |

### C212 — `#219` 剩下的三件：名冊掛點的實測是 0/2 而那是完整枚舉，交付它的那則裁決自己在射程外，而「兩軸那兩句」唯一有值域的設計寫下來但不建（2026-09-09，#219）

⚠️ **量測基準 `cd7f950`**（C211 合併後的 `main`）。本則結清 `#219`，並**關掉那張票**。
⚠️ 本則**不新增任何機械檢查**，所以 C154 §三 的三條填寫要求對它不適用 ——
**明寫這一句**，因為 C204 錯的地方正是把它留成沉默。

#### 一、三件的答案

| C211 §六 列的三件                            | 本則                                                                     |
| -------------------------------------------- | ------------------------------------------------------------------------ |
| 件 1：「查什麼」的散文沒有值域               | **確認，而且有解** —— 唯一給得出值域的設計寫在 §四，**但不建**，因為件 2 |
| 件 2：`delivery`／`iteration` 兩個結構化欄位 | **不做。** 實測 0/2，見 §二、§三                                         |
| 件 3：逐工具一列 vs 逐規則的射程缺口         | **關不掉**，而交付它的那則裁決自己就在缺口裡（§五）                      |

#### 二、⚠️⚠️ 先退掉兩個數字：逐則 grep 兩軸的詞，結構上低估

本則第一版打算普查「C154 之後每一則裁決有沒有報兩軸」（C211 §四 自陳沒做、
說它的實例是下界）。**那個量法是壞的**，而發現它的是一個對照組：

`C171` 的本文裡「交付」出現 **0** 次、`D16` 出現 **1** 次 —— 逐則 grep 會把它判成
「沒報」。**而它報了**：那份兩軸報告在 **C169 §六**，一張完整的表加兩個級別，
C171 §五 只補了 C154 §三 第 3 條 留給它的夾具那一格。

> **C154 §三 可以由「指向前一則」滿足。** 逐則 grep 對這種形狀恆假。

所以下面兩個數字**本則退掉**，它們量的不是「有沒有報」：

| 退掉的讀數                              | 值                           |
| --------------------------------------- | ---------------------------- |
| C155 起引用 `C154 §三` 或 `§四` 的則數  | 43／57 —— 引用不等於在射程內 |
| C155 起「交付」與「迭代」兩詞俱在的則數 | 15／57 —— 少算了指過去的那些 |

⚠️ 這是 `measurement-rig-needs-controls` 記的**第七層（射程借用）**：量測本身正確、
對照組正常，而那個數字回答的是另一個問題。⚠️ 而接住它的不是對照組，是**去讀了
那一則的原文**（同 C154 §三 對「歷史值是 0 時重現成功不算證據」的處置）。

#### 三、件 2 的實測：名冊掛點的整個射程，0/2

不必普查全部裁決 —— **這個掛點的射程是「名冊多一列」，而那是可以完整枚舉的。**

| 量測（基準 `10194f9` ＝ C154 落地那一刻 → `cd7f950` ＝ 今天） | 結果                                                          |
| ------------------------------------------------------------- | ------------------------------------------------------------- |
| 名冊列數                                                      | **21 → 23**（`GATES` 16→17、`UNGATED` 5→6）                   |
| 新增的列                                                      | `threshold-check`（`GATES`）、`release-distance`（`UNGATED`） |
| 其中兩軸報告**沒填**的                                        | **0**                                                         |

- `threshold-check` ← **C162**：交付「零」、迭代「有分，級別是**發生過的實例**」，
  §八 是自我防護的夾具那一節。三條逐條齊。
- `release-distance` ← **C171**：報告在 **C169 §六**（見 §二），
  ①② 齊、兩個主張兩個級別、判讀那一格連「要推翻它該做什麼」都寫了；夾具在 C171 §五。

> **這不是抽樣。這是那個掛點自 C154 以來看得到的全部東西 —— 兩列，兩列都合格。**

⚠️ **這兩列是「讀那兩則裁決的原文」驗的，不是 grep 出來的** —— 正是 §二 說 grep
做不到的那個動作（C171 那一份要翻到 C169 §六 才看得到）。

⚠️ 所以「n=2 太小」不是本則的問題：**分母就是 2**，因為八天裡名冊只長了 2 列。

#### 四、件 2 的裁決：不做 —— 而理由不是成本，是它自己的 ② 在自己的射程內是空的

依 D16 兩軸，逐格：

| 軸       | `delivery`／`iteration` 兩個結構化欄位                                         |
| -------- | ------------------------------------------------------------------------------ |
| **交付** | **零。** `#219` 票面自己填的，本則不改：沒有收件人在意名冊某一列有沒有兩句話。 |
| **迭代** | **① 對象在外 ✅；② 級別：判讀，而且是空的判讀** —— 見下                        |

**② 這一格的實情：這個掛點的整個射程裡，零個實例（§三）。**

⚠️⚠️ **不借 C204 當這一格的實例。** C211 §四 已經拒絕過同一個借用，理由一字不變：
C204 在既有工具裡加規則、沒有新增名冊列，**它落在這個掛點看不到的地方**（§五）。
拿一個自己射程外的實例來填自己的 ②，正是 C204 被判錯的那件事。

**⚠️ 判讀本身不等於出局 —— 而這一則與 C169 §六 的差別就在這裡。**

C169 §六 的承重牆也是判讀，而它被接受了。差別是它的 ② **拆成兩個主張、兩個級別**，
其中一個是**發生過的實例**（「25 支／8 天全程零紅燈零警告」）。

> **本條的 ② 沒有那一半。** 交付軸零 ＋ 迭代軸只剩一個空的判讀 ＝ **D16 判它出局。**

⚠️ 這不是「太貴所以不做」（C137 §一 不准，兩個方向都不准）。
代價那一欄本則一個字都沒算 —— 出局的理由是**它自己的射程內量不到東西**。

#### 五、件 1 的答案：唯一有值域的設計，寫下來但不建

件 1 是「兩軸那兩句沒有值域」（C152 §二 的同形問題）。**它有一個解**，而 `#219`
票面坑 #2 判它太貴的那個理由，可以拆掉：

**設計（不建，寫下來備用）**

1. `Gate` 與 `Ungated` 各加兩個欄位，**值域是列舉的**，不是散文：
   - `delivery: "有分" | "零"`
   - `iterationLevel: "判讀" | "探針" | "實例" | "未報（C154 之前）"`
2. 21 列既有的一律填 **`"未報（C154 之前）"`** —— ⚠️ **回填的是「沒有人報過」
   這個事實，不是替 21 列各裁一次。**

⚠️⚠️ 第 2 點是把 `#219` 坑 #2 那句「回填的內容有一部分沒有人判斷過」**變成填的內容
本身**，而這棵樹十小時前才做過同一個動作：**C211 §三 對 `slice-gen` 就是這樣處理的**
（不重判它，只把「它的理由從來沒有被獨立做過」寫進那一列）。

⚠️ 這個設計仍然只查「填了沒有、是不是那幾個之一」，填得對不對全綠 ——
同 C143／C144（`SCOPE.md` 桶欄）與 C211 §五 1。**它買不到件 1 的全部，只買到值域。**

**⚠️ 依 C154 §三，判讀那一格要寫「要推翻它，該做什麼」：**

> **下一次名冊多出一列，而判它進來的那則裁決沒有報兩軸 —— 那一刻 ② 從空的判讀
> 升到發生過的實例，這個設計就照著上面兩點建。** 分母是 2 而且完整枚舉，
> 所以那一刻**看得出來**：不需要普查，只要在加列的那支 PR 上讀一次。

#### 六、件 3：射程缺口關不掉 —— 而交付它的那則裁決自己在缺口裡

C154 §四 的射程原文：「任何新增的機械檢查，**包含在既有工具裡加一條規則**」。

**⚠️⚠️ C211 在 `gate-roster` 這支既有工具裡加了一條規則，名冊新增 0 列。**
實測：`6d93df8` 23 列 → `cd7f950` 23 列。

> **交付那道名冊規則的那一則裁決，自己就在 C154 §四 的射程內、而在它建的那個
> 掛點的射程外。** C204 是第一個實例，C211 是第二個 —— 而第二個是這道規則自己。

**為什麼往下一層做也關不掉：「一條規則」在這棵樹上沒有可枚舉的單位。**

`conformance` 的 `cli.ts` 分派 5 支 check 函式（`checkSlice`／`checkCspIncompatibleImports`／
`checkActionPinning`／`checkFileMode`／`checkPhantomDependencies`），而 `layering`／
`slice-shape`／`design-system`／`dependencies`／`relative-escape`／`ownership` 是
`checkSlice` **裡面**的子判定。⚠️ C204 加的是 `layering.ts` 裡**第三份禁止清單**——
按哪一種定義它都不是「多一支」。

⚠️ 而造一份規則登記表**正是 C136 §四 撤掉 `SCOPE.md` 那張 `tools/` 表的形狀**：
第二份手抄本，**兩道機制互不斷言**，漂移時兩邊都是綠的。
（`gates.ts` 檔頭那一段就是這麼寫的，本則不重述。）

> **裁決：名冊掛點的射程上界是「新增一支工具」，而這是它的性質，不是缺陷。**

⚠️⚠️ **本則不縮 C154 §四 的射程。** 差額（既有工具裡加規則）**繼續靠人讀** ——
把射程改成「掛得到的那一段」就是改要求來換綠燈（AGENTS.md 規則二 的形狀）。

#### 七、`#219` 票面坑 #3：`UNGATED` 那幾列要不要報兩軸

票面問的是「`UNGATED` 不是閘門，它們要不要填兩軸」。**判準不是它在哪一張表上。**

C154 §四 的射程詞是「機械檢查」，而 `UNGATED` 這 6 列**不是同一種東西**：

| `UNGATED` 列       | 是不是一道機械檢查                                                         |
| ------------------ | -------------------------------------------------------------------------- |
| `release-distance` | **是** —— 它印一個數字。C169 §六 為它報過兩軸                              |
| `csp-verify`       | ⚠️ **第三類，本則不判** —— 見表下                                          |
| `ui-survey`        | **不是** —— 名冊那一列自己寫著「**選型市調，不是判定**」（D15／`#49`）     |
| `codemods`         | **不是** —— C71：改東西，不判定對錯                                        |
| `slice-gen`        | **不是** —— C71：產東西，不判定對錯（⚠️ 而 C211 §三 記了它從沒被獨立判過） |
| `gate-kit`         | **不是** —— 它是被閘門用的函式庫                                           |

⚠️⚠️ **`csp-verify` 不歸這兩類，而本則刻意不替它歸類。** 它**會**判定 ——
起一台伺服器、用正式 CSP 服務正式建置產物 —— 而**判定的最後一步是人**
（名冊那一列的原文：「要人開瀏覽器」）。C52 拿掉的 `--record`／`--verify`
正是「有沒有人真的驗過」那個**機器**答案。所以「它是不是一道機械檢查」在它身上
不是一個是非題，而**本則沒有量它** —— 沒量的事不寫成結論（同 C211 §八 的處置）。
⚠️ 它是**「沒有東西守它」推不出「它被漏掉了」**那條通則的另一面：
一道被裁決**刻意**拿掉機器證據的檢查，既推不出「它被漏掉了」，
也推不出「它欠一份兩軸報告」。要判它，先去讀 C52 判了什麼。

⚠️ **而「它不會紅所以不算檢查」這條繞道 C169 §六 已經明文關掉了**（引 C137 §四：
判準套在描述上不是實質上）—— `release-distance` 就在 `UNGATED` 裡，而它報了。

> **答案：`UNGATED` 這張表本身既不豁免也不課予；問的是那一列是不是一道機械檢查。**

#### 八、關 `#219`，而三件事要明說

1. **`#219` 關。** 三件都有答案了（§一）。
2. **⚠️⚠️ `delivery`／`iteration` 那張票不開。** 上一輪的決定是「它在 `#219` 關閉時才開」，
   而 §四 判它出局 —— **所以是「不開，且理由記在 §五」，不是「忘了開」。**
   要開它的條件寫在 §五 最後那一段，那是一句可否證的話。
3. **⚠️⚠️ C154 §八 那句「這條要求全部靠人讀」留著、繼續生效。**
   本則否決的是**名冊這個掛點**，不是那條要求。
   「掛點不做」推不出「要求退休」—— 那正是 `unruled-rule-gains-force` 與
   `blanks-outlive-their-guardians` 記的那個形狀（相依方一張一張關掉之後，
   「不裁」就變成「無人主張的預設」）。**這一句本身就是那道防線。**

#### 九、不裁

- **不縮 C154 §四 的射程**（§六）。
- **不重判 `slice-gen`／`codemods` 該不該是閘門**（C211 §八 已經記過，本則不動）。
- **不建 §五 那個設計** —— 它是備用件，不是待辦。⚠️ 它**不進 `specs/`、不標 `@待辦`**。
- **不動 C211 那條規則的 pattern 與措辭。**
- **不出版號。** 本則零程式碼改動。

#### 十、與既有裁決的關係（C136 §八）

| 既有                 | 本則做了什麼                                                                        |
| -------------------- | ----------------------------------------------------------------------------------- |
| **`#219`／C211 §六** | **結清三件並關票** —— 件 1 有設計不建、件 2 出局、件 3 關不掉                       |
| **C154 §三**         | **不適用於本則**（零新機械檢查），而 §五 依它寫了「要推翻該做什麼」那一句           |
| **C154 §四**         | **不縮射程** —— 差額繼續靠人讀                                                      |
| **C154 §八**         | **留著** —— 明文拒絕「掛點不做 ⇒ 要求退休」這個推論                                 |
| **C169 §六**         | **判別依據** —— 同樣靠判讀，而它的 ② 有一半是實例；本條沒有                         |
| **C162／C171**       | **完整枚舉的兩列** —— 兩列的兩軸都齊，C171 那一份在 C169 §六                        |
| **C204／C211**       | **射程缺口的兩個實例** —— 第二個是建這道規則的那一則自己                            |
| **C211 §三**         | **借形狀** —— §五 的 sentinel 就是它對 `slice-gen` 的做法：記下「沒人判過」，不代判 |
| **C136 §四**         | **不重蹈** —— 規則登記表會是第二份互不斷言的手抄本                                  |
| **C143／C144**       | **同形** —— §五 那個設計仍然只查填了沒有                                            |
| **C152 §二**         | **只解一半** —— 值域有了，同形沒有解                                                |
| **C137 §一**         | **遵守** —— §四 出局的理由是射程內零實例，不是成本；代價那一欄一個字沒算            |

### C213 — `TESTING.md` 三處說「等 `#86`」而那個凍結十四天前就解除了：更正之後**擋路的理由換了一個，不是消失**（2026-09-09）

⚠️ **量測基準 `4a579d3`。** 本則**不新增任何機械檢查**，C154 §三 不適用（同 C212 的體例）。

#### 一、事實：全樹掃過，活的文件是三處

`git grep -F "#86"` 全樹 17 處，逐處分類：

| 類別                                                                           | 處數 | 處置               |
| ------------------------------------------------------------------------------ | ---- | ------------------ |
| **活的文件，而且是假的** —— `TESTING.md` :62／:133／:468                       | 3    | **本則更正**       |
| 裁決原文與有日期的日誌（真假都不動）—— `DECISIONS*.md`、`CHANGELOG` `[1.10.0]` | 13   | **不動**（§一 末） |
| 今天仍然為真 —— `.github/workflows/tier2-security.yml:62`                      | 1    | 不動               |

`#86` 於 **2026-08-26** 關閉（C133：`release/v1` 併回 `main` 並刪除），
所以那三句**過期了十四天**。

⚠️ **不改歷史那 13 處。** `CHANGELOG` 的 `[1.10.0] — 2026-08-24` 寫「實作等 #86 解凍」，
**那一天它是真的**；改它就是回頭改寫有日期的日誌 —— 同 `tools/doc-facts/src/cli.ts:25`
擋這件事的理由，以及 C138（第一卷凍結）。⚠️ 而 `DECISIONS.md` 那些是裁決原文，
C138 明文：更正寫成續集，不改原文。**本則就是那個續集。**

#### 二、⚠️⚠️ 更正之後，擋路的理由換了一個 —— 而它就寫在同一則裁決的同一條裡

C120 §七〈第一類那組的裁決〉，「數字」那一條的第一句：

> **數字：先不設，因為分母現在是壞的。** （…）走子行程跑自己的 CLI，
> v8 只看得到 in-process 的執行。

> **所以「等 `#86`」從來不是唯一的擋路理由，而今天它是一個假的。**

⚠️ **這一種過期最貴的地方不是它假，是它會遮住一句沒過期的話。**
讀到「等 `#86`」的人會去查 `#86` —— 查完發現它關了 —— 然後推出「所以可以做了」。
**真正的答案（分母是壞的）在同一段裡，而他不會再往下讀。**
⚠️ 同 `counts-in-prose-go-stale` 記的第四種形狀，方向相反：那次是一句真話被讀成
更強的宣稱，這次是**一句假話擋在一句真話前面**。

#### 三、C120 §七 自己點名的第三條路，以及它今天走到哪 —— ⚠️ 不寫成結論

C120 §七 那一條同時寫下：

> ⚠️ 值得先查的第三條路是用 `NODE_V8_COVERAGE` 收子行程的覆蓋率再合併，
> **但那是要實測的一格，不是可以直接寫進裁決的斷言**。

**那支儀器後來在這棵樹上被用起來了**：C208 拿 `NODE_V8_COVERAGE` 量「閘門那一步
到底有沒有走到某支檔」，一趟量測同時推翻了兩個方向的預測。

⚠️⚠️ **而本則到此為止 —— 那證明的是儀器跑得起來，不是那條路走完了。**
把子行程的 range 合併成**逐 package 的覆蓋率報表**沒有人做過，本則也沒有量。
**沒量的事不寫成結論**（同 C211 §八、C212 §七 對 `csp-verify` 的處置）。

#### 四、真正的狀態：已解鎖、有裁決、無人排程

| 問題           | 今天                                                   |
| -------------- | ------------------------------------------------------ |
| 凍結還在嗎     | **不在** —— `#86` 2026-08-26 關                        |
| 有裁決嗎       | **有** —— C120 §七（切法／數字／四種狀態／四個維度）   |
| 有票在追嗎     | **沒有** —— `#130`（量現況）、`#132`（訂下限）都已關閉 |
| 那為什麼還沒做 | C120 §七 的「先不設數字」，理由見 §二                  |

⚠️ **這正是 `blanks-outlive-their-guardians` 的形狀**：相依方一張一張關掉之後，
「先不做」就變成無人主張的預設。**本則不開票、不排程** —— 排不排是人的決定。
本則做的是把「**無人排程**」這四個字寫進 `TESTING.md`，
讓它不再看起來像「有票在追它」。

#### 五、⚠️ 刻意不重述那個數字 —— 而重數一次會給錯的答案

C120 §七 寫「**七支** tools 走子行程跑自己的 CLI」，並列了名字。
**本則不重數，而且更正稿裡明說不重述。**

理由是實測的：同一個天真數法（`git grep -E "spawnSync|execFileSync|execSync"`
掃 `tools/*/tests/*`）今天給 **8 支**，而**那份名單漏掉 `spec-report`** ——
它經 `@org/gate-kit/testing` 的 `runCli` spawn（`tools/spec-report/tests/cli.test.ts:5`），
字面上一個 `spawnSync` 都沒有。**C168 把各支的 harness 收攏之後，數它要換一個數法。**

> 週期性變動的事實**能指過去就不要抄下來**（C53 的形狀）。
> 更正一句過期的話時，順手抄一個新數字進去，就是替下一個人埋同一顆。

#### 六、⚠️ 這三處沒有任何機制在守

`doc-facts` 不讀 `TESTING.md` 的事實（全樹 `git grep -l "TESTING.md" -- tools/`
回的是 `layering.ts`／`gates.ts`／三份 README ／`slice-gen` 與 `spec-report` 的原始碼，
沒有一支把它當事實來源比對）。**擋這一類過期的只有人去掃字串。**

⚠️ 而本則自己就是證據：那三處**活了十四天**，`vpr ready` 與四道 CI 全程綠。

#### 七、不裁

- **不排程層 2 第一類**，也不開票（§四）。
- **不動 C120 §七 的任何一格**（切法／數字／四種狀態／四個維度）—— 本則只更正
  一句指向 `#86` 的話，不碰那則裁決的內容。
- **不改那 13 處歷史記錄**（§一）。
- **不宣稱 `NODE_V8_COVERAGE` 那條路可行**（§三）。
- **不出版號。** 本則零程式碼改動。

#### 八、與既有裁決的關係（C136 §八）

| 既有            | 本則做了什麼                                                                    |
| --------------- | ------------------------------------------------------------------------------- |
| **C120 §七**    | **續集，不改原文** —— 更正它的前提（`#86`），保留它的理由（分母是壞的）         |
| **C133／`#86`** | **兌現** —— 那條凍結 2026-08-26 就解除了，本則是它遲到十四天的下游更正          |
| **C138**        | **依它** —— 更正寫成續集；`DECISIONS.md` 那些原文一個字不動                     |
| **C208**        | **引為儀器可行的證據，而不是路已走完** —— C120 §七 標的「要實測的一格」仍未實測 |
| **C53**         | **同形** —— 不把會變動的數字抄進散文；§五 實測了重數會錯                        |
| **C154 §三**    | **不適用** —— 本則零新機械檢查（同 C212 的體例，明寫而非沉默）                  |
| **C211／C212**  | **同一句紀律** —— 沒量的事不寫成結論（§三）                                     |

### C214 — C120 §七 留了十六天的「要實測的一格」實測了：`NODE_V8_COVERAGE` 那條路走得通，而它只修兩個缺陷裡的一個；⚠️ 而第一版的偏移守衛對已知壞的輸入一份都沒拒絕（2026-09-09）

⚠️ **量測基準 `8ac58f4`。** ⚠️ 標題那個「十六天」的依據是 **C120／`v1.10.0` 的日期
（2026-08-24）到今天**，不是 `#86` 關閉那天（2026-08-26，那個差是十四天，
是 C213 的數字）—— **兩個數字量的是兩件事，不要互相借用。**

本則是 **C213 §三** 說「本則到此為止」的那一格的續集：
C120 §七 寫下「值得先查的第三條路是用 `NODE_V8_COVERAGE` 收子行程的覆蓋率再合併，
**但那是要實測的一格**」——**本則把它實測了。**

#### 一、結論：走得通，而且不必加任何相依

| 問題                                               | 答案                                                                                        |
| -------------------------------------------------- | ------------------------------------------------------------------------------------------- |
| 子行程的覆蓋率收得到嗎                             | **收得到。** `runCli` 用 `spawnSync("node", …)` 且不覆寫 `env`，`NODE_V8_COVERAGE` 直接繼承 |
| 偏移對得上原始碼嗎                                 | **對得上。** node 原生去型別是等長替換，具名函式宣告逐點命中                                |
| 要不要裝 `c8`／`v8-to-istanbul`                    | **本則沒裝** —— 用 C208 那支讀 `NODE_V8_COVERAGE` 的腳本改的                                |
| 那三支「整支不在分母裡」的檔案，收上來之後有讀數嗎 | **有。** 見 §二                                                                             |

#### 二、`tools/api-surface` 的兩個讀數 —— ⚠️ **而它們不可相減**

**(1) vitest 自己的報表，補上 `--coverage.include='src/**'`：**

| 檔           | 行覆蓋率            |
| ------------ | ------------------- |
| `cli.ts`     | **0**               |
| `compare.ts` | **0**               |
| `shape.ts`   | **0**               |
| `docs.ts`    | 100                 |
| `tracked.ts` | 100                 |
| **合計**     | **5.98%（35/585）** |

⚠️ **這重現了 `#130` 記的 5.91%**（差的是這段期間改過的程式碼）——
**尺沒換，問的是同一個問題。**

**(2) 同一套測試，`NODE_V8_COVERAGE` 收子行程之後（本則的腳本，逐檔行覆蓋率）：**

| 檔           | 行覆蓋率               |
| ------------ | ---------------------- |
| `cli.ts`     | **96.0%** (407/424)    |
| `compare.ts` | **94.6%** (244/258)    |
| `shape.ts`   | **95.1%** (1098/1154)  |
| `docs.ts`    | 100% (79/79)           |
| `tracked.ts` | 89.9% (160/178)        |
| **合計**     | **95.0%（1988/2093）** |

> ⚠️⚠️ **不可以寫成「5.98% → 95.0%」。** 兩個分母是不同的東西：vitest 那份是它
> 自己插樁算的行（585），本則腳本那份是**非空原始碼行**（2093）。
> **可以說的只有一句：那三支檔從「0，什麼都沒執行」變成「量得到」。**
> 那正是 C120 §七 問的那一格，不多也不少。

#### 三、⚠️⚠️ 兩個獨立的缺陷，而 `NODE_V8_COVERAGE` 只修其中一個

C120 §七 的原話是「分母是壞的」，而實測拆出來是**兩件**：

| 缺陷                                            | 實例                                                                                         | 修法                 |
| ----------------------------------------------- | -------------------------------------------------------------------------------------------- | -------------------- |
| **A. `include` 沒設** —— 未載入的檔整支不進分母 | `tools/theme-verify`：預設 **97.76%**，補上 `include` 之後 **41.06%**，而 `cli.ts` 是 **0%** | 設定一行（已知手法） |
| **B. 子行程看不見**                             | `tools/api-surface`                                                                          | 本則實測的那條路     |

⚠️ **A 不修，B 的結果讀不出來** —— 檔案根本不在分母裡的時候，「它有沒有被子行程走過」
沒有地方可以顯示。**次序是 A 先。**

⚠️⚠️ 而 A 造出來的假分數比 B 更難看見：`theme-verify` 的 **97.76%** 是一份
**看起來很好的報表**，而它整支 30KB 的 `cli.ts` 不在裡面。
（同 `coverage-default-scope-fake-high` 記的形狀，這是它第 N 次發作。）

#### 四、⚠️⚠️ 對照組：第一版的守衛對已知壞的輸入**一份都沒拒絕**

子行程的 profile 與 vitest worker 裡的模組**可以有完全一樣的 `file:` URL**，
而後者經過 vite 轉換、偏移對不上原檔。混進來就是一份看起來合理的假數字，
所以本則寫了一道守衛，並且**兩個方向都試**。

**第一版**：檢查具名函式的 `startOffset` **附近 ±160 字**有沒有那個名字。

| 方向                               | 第一版          | 第二版          |
| ---------------------------------- | --------------- | --------------- |
| 真的子行程 profile（已知好）       | 0 份被拒 ✅     | 0 份被拒 ✅     |
| **整份 offset 平移 +50**（已知壞） | **0 份被拒 ❌** | **5/5 被拒 ✅** |

> **第一版對已知壞的輸入照樣輸出，而且給的是 83.2% —— 一個看起來合理的數字。**

成因：視窗夠寬的時候，「名字在附近」對平移不敏感。
**第二版改成逐點相等**：具名函式宣告的 `startOffset` 必須**恰好等於**原始碼裡
`function NAME(` 的位置（名字在整份原始碼只出現一次的才判）。

⚠️ 而第一版的視窗**是被一個偽陽性逼寬的**：`const push = (name) => {}` 這種
具名箭頭函式的 range 起點在 `(`，名字在它**前面** —— 往前開視窗解決了偽陽性，
**同時把守衛弄瞎了**。⚠️ **放寬一道檢查去消偽陽性的時候，要回頭跑一次已知壞的輸入。**

#### 五、對照組二：不起子行程的 package，這個方法貢獻 **0**

`tools/theme-verify` 的測試不起任何子行程。同一套收集跑下來，
子行程法對它的 `src/` 得到 **0/0** —— **這個方法不會憑空灌水。**
⚠️ 同一份 profile 對 `tools/api-surface/src/` 也是 0/0（跨 package 不會串味）。

#### 六、⚠️⚠️ 這個數字量的是「執行到了」，不是「驗過了」

**單獨跑一次 `node tools/api-surface/src/cli.ts`（不帶任何引數、不做任何斷言），
本則的腳本就給 81.4%。**

> 起一支 CLI 對真樹跑一趟就會走過大部分的路徑。**這一類的覆蓋率特別便宜。**

所以修好分母之後，**不可以拿這個數字論證「測試夠好」** —— 那是元層（C121 的突變
測試）在答的問題，而這棵樹已經量過：假存活的第二個成因就是「守衛在閘門裡，
Stryker 只跑測試」（C203／C208）。⚠️ **本則只交付「分母修得好」，不交付「分子有意義」。**

#### 七、還沒做的，逐項寫下來

1. **合併進 vitest 的報表**沒有做，而**難的正是「合併」這兩個字**。
   本則是一支獨立腳本算出來的數字，不是 `vp test --coverage` 印出來的那張表。
   ⚠️⚠️ **兩邊的偏移不在同一個空間**：子行程那份是原檔的（node 原生去型別），
   vitest 行程內那份經過 vite 轉換 —— **§四 那道守衛拒絕後者，正是因為這件事。**
   要對起來就要吃 vite 那層轉換的 sourcemap，也就是 `v8-to-istanbul`／`c8` 那一類，
   **那是一個要加相依的決定**（本機 `vp install` 與 CI 的 `--frozen-lockfile`
   是兩條不同的路，這棵樹踩過六次）。⚠️ **可行性 ≠ 已落地**，而未解的核心是這一格。
2. **逐 package 分類沒有定論。** 「哪幾支的測試會起子行程」用兩個樣式數，
   得到 **12／21** 與 **17／21** —— ⚠️ 兩個都不可信，**同 C213 §五 記的形狀**
   （`spec-report` 字面上沒有 `spawnSync`，它經 `@org/gate-kit/testing` 的 `runCli`）。
   **正確的數法是收集之後看誰真的有 profile**，而那是實作的事。
3. ⚠️⚠️ **本則量到的全部是 `tools/*`，一支 `platform/*` 都沒量。**
   而 C120 §七 的第一類是 `platform/*` **加** `tools/*` 兩半。
   缺陷 A（`include` 沒設）是通用的；**缺陷 B 的形狀（`cli.ts` 只在子行程裡執行）
   是 CLI package 才有的**，`platform/*` 的成員是函式庫。
   ⚠️ 所以 **A／B 這個二分法不可以讀成「第一類的全貌」** ——
   `platform/ui` 已知的那個問題（`.vue` 的分母只算 `<script setup>`）
   **A 與 B 都不涵蓋**，它是第三種。
4. **C120 §七 那四件，修好分母一件都沒解**：逐 package 的切法、
   四種狀態不能都寫成 0%（`platform/bff-contract` 結構性 0/36、
   `platform/tsconfig` 與 `platform/eslint-config` 不適用而報表寫 100%＝0/0）、
   四個維度不互為代理、`platform/ui` 要單獨一格。
   ⚠️ **分母修好是「可以開始問門檻」的前提，不是門檻的答案**（C120：數字出來前不准問門檻）。

#### 八、排程

依使用者指示，層 2 第一類**排上去了**，票是 `#347`，本則是它的第一份證據。
⚠️ `TESTING.md` 那句「無人排程」同步改成指向那張票 —— **C213 §四 寫下那四個字
就是為了讓這一刻看得見**。

#### 九、不裁

- **不設任何門檻**（C120：數字出來前不准問門檻）。
- **不改任何 package 的 `vite.config.ts`** —— 本則零程式碼改動，`include` 那一行留給 `#347`。
- **不宣稱「合併進報表」可行**（§七 1）—— 沒做的事不寫成結論。
- **不用 95.0% 論證測試品質**（§六）。
- **不出版號。**

#### 十、與既有裁決的關係（C136 §八）

| 既有              | 本則做了什麼                                                                       |
| ----------------- | ---------------------------------------------------------------------------------- |
| **C120 §七**      | **實測它標的那一格** —— 那條路走得通；⚠️ 而它列的四件事一件都沒解                  |
| **C213 §三**      | **續集** —— 那則說「儀器跑得起來不等於路走完了」，本則把路走了一段，並標出還沒走的 |
| **C213 §四／§五** | **兌現** —— 「無人排程」改成 `#347`；分類數法分歧同它記的形狀，本則不重述數字      |
| **C208**          | **借儀器** —— 同一支讀 `NODE_V8_COVERAGE` 的腳本，改成算逐檔行覆蓋率               |
| **C121／C203**    | **劃界** —— 分母修好不回答「分子有沒有意義」，那在元層                             |
| **C154 §三**      | **不適用** —— 本則零新機械檢查（明寫，不留成沉默）                                 |
| **C137 §一**      | **遵守** —— 本則不論證增減任何檢查；§七 3 明文把門檻留在射程外                     |

### C215 — 閘門集合裂成「上游」與「下發」兩份：⚠️ 而「刪掉 12 支」的正確讀法是「不下發」，判準是兩條不是一條（2026-09-10）

起於一份清單：17 道閘門標成「必留 5、刪 12」，外加一支新增的腳手架 checksum。
逐題問完之後，清單一列都沒有改，**變的是它是什麼意思** —— 而那個轉換本身
才是這一則要寫下來的東西。

#### 一、判準是兩條，不是一條

目的（人給的）：**企業腳手架。讓公司各團隊保留自己的開發風格，不必每次重寫
屬於腳手架的內容，而腳手架升級迭代時各團隊無痛跟上，不影響手邊的業務邏輯。**

那份清單的「為什麼」欄逐列讀下來，會發現它其實是兩條判準在交替使用，而
兩條都自洽：

| #   | 判準                                     | 它砍掉的                                              |
| --- | ---------------------------------------- | ----------------------------------------------------- |
| 一  | **腳手架不規定團隊怎麼寫程式**           | `threshold-check`（複雜度門檻）、`a11y`               |
| 二  | **風險與法遵不是腳手架的事，是公司的事** | `pii`、`compliance`、`supply-chain`、`eslint`（安全） |

**兩條要分開寫，不可以壓成一句「非升級路徑」。** 壓成一句之後，將來有人拿它去
砍第三類東西，而那一類兩條都管不到 —— 這是 C134 §二 那句話的又一次應驗：
判準的效力依附在它的後果上，所以後果的射程要跟判準一樣寫死。

⚠️ **C137 §一 擋不住這個提案，本則明文寫下來，免得將來有人拿它當令箭。**
C137 §一 禁的是「拿過度設計／成本論證增減閘門」，兩個方向都禁。上面兩條都是
**射程**論證，不是成本論證。C137 §一 在本則的地位是**遵守**，不是**豁免**。

#### 二、對象是 fork 拿到的預設，不是這棵樹 —— 所以「刪除」是「不下發」

問到第二題就翻面了：要改的是**採用團隊 fork 之後拿到的預設**，不是上游這一棵。

這一個字的差別讓訪談前半段挖出來的四個衝突**當場消失三個**：

| 訪談中提出的衝突                                                                     | 對象確定之後                           |
| ------------------------------------------------------------------------------------ | -------------------------------------- |
| 刪 `threshold-check` 讓 `specs/gate-thresholds.feature` 不可滿足，而規則四不准改規格 | **消失** —— 上游照跑                   |
| 刪 `supply-chain` 連帶弄壞 SBOM／SCA 三個步驟（`--split-lockfile`／`--verify-sbom`） | **消失** —— 上游照跑                   |
| C52 §三「拿掉閘門而讓對照表繼續顯示已覆蓋，比沒有閘門更糟」                          | **轉形** —— 變成「下發的文件要跟著削」 |
| `scope-check`／`gate-roster`／`doc-facts` 那三列的理由寫反了                         | **只剩理由要改**（見 §三）             |

**上游跑全部，fork 拿子集。** 那 12 支不是被刪掉，是留在上游、不下發。

#### 三、⚠️ 三列的理由，事實是相反的 —— 而結論不變

清單上 `scope-check`／`gate-roster`／`doc-facts` 三列寫的是「不沿用治理文件
就是綠燈」。**逐支讀過原始碼，三支都是紅燈：**

- **`scope-check`** —— `SCOPE.md` 不存在的話是 `check.ts:200` 那個沒有保護的
  `readFileSync` **直接丟例外**。而節標題被改掉會紅，工具自己的修復訊息就寫著
  為什麼不能放過：「找不到就當成『這一層沒有清單』的話，**改個標題就能讓整層
  不再被檢查，而且是綠的**」。
- **`gate-roster`** —— 它第一條檢查是「每一支 `tools/*` 都要在 `GATES` 或
  `UNGATED` 裡登記」。採用團隊加一支自己的工具，當場紅。
- **`doc-facts`** —— 它有 `unguarded`／`never-cited` 兩種失敗形狀。README 被
  改寫、引用樣式對不上句子，紅。

**結論（三支都不下發）不變，換掉的是理由。** 記在這裡的原因是 C137 §四 那個
形狀已經在這棵樹上發生過四次：**判準套在東西的「描述」上，而不是它的「實質」
上。** 一個結論對而理由錯的決定，下一次有人拿那個理由去推別的東西時就會出事。

同一節順帶更正一個標籤：`threshold-check` 不是「防作弊」。它的實質是 C147 §二
**單向棘輪的「降」那一半**，而 `check.ts:74` 逐字寫著「**降不需要論證**……
**這也不是 AGENTS.md 規則二 禁的那種改動** —— 那一條禁的是調鬆」，`check.ts:19`
寫著「『抬』那一半今天仍然沒有機制在守，而且刻意」。

#### 四、機制：全部下發，只改接線

三條路上桌：(a) 上游發版時產出一份削減版的樹；(b) 一支 `eject` 命令；
(c) 全部下發，只改接線。

**裁決：(c)。** 唯一不需要新建「兩棵樹」這個概念的一條 —— 而「兩棵樹」正是
`gate-roster` 檔頭記的那一類問題（一份到處都有副本的清單，改動時只有人記得改
其中幾處）的放大版。附帶好處：團隊看得到全部工具，想自己接回來隨時接得上，
而那對「保留各團隊開發風格」是加分的。

分法記在**名冊**，加一個 `ship` 欄位，連理由一起寫 —— 與 `why`／
`notInGateScript` 同一個體例（C132：那個欄位是一句必填的理由，不是一個開關）。

⚠️ **`gate-roster` 因此留在上游而且變重要了。** 它那一列的結論（不下發接線）
成立，但理由要換成 §三 那一條。反過來說，若真把它整支移掉，這份分法就得手抄在
`scripts.gate` ＋ tier1 ＋ tier2 ＋ README ＋ 外加一份 fork 版共**五處**，
而沒有東西斷言它們一致。

#### 五、承諾五：退的是承諾，不是閘門

被砍的 `eslint`（安全規則）／`a11y`／`pii`／`compliance`／`supply-chain`
是 `HANDOFF.md:264`〈五、基礎資安在撰寫時就被發現〉的**全部守衛**。

**裁決：明說退掉承諾五，理由是公司有集中的 SAST／SCA 平台，腳手架不重複做。**

⚠️ **而「退掉承諾」與「刪掉閘門」是兩件事，本則把它們分開：上游仍然跑那五道，
只是不再對採用團隊承諾它。** 不分開寫的話，三個月後會有人讀成「這個腳手架不做
資安」。

這個決定同時是 `HANDOFF.md` 上一批**交辦給採用公司的未決項**的答案 ——
`## 2–3.`（原生二進位的 SCA 例外）、`## 10.`（SCA 掃描範圍）、`## 19.`
（§16 五年保存）、`## 20.`（§11 II ⑨ 隱碼）、`## 21.`（開發期 SAST）、
`## 22.`（無障礙）。**那些項目的收件人就是做這個決定的角色**，所以一併結掉。

散文射程（「五條」變「四條」）：`HANDOFF.md:144` 那一節與 `:152`
「守到了一條，還差四條」、`TESTING.md` 層 3、`tier2-security.yml` 兩處註解、
名冊裡 `promise-check` 的 `why`。⚠️ 依 C201，掃除要分「存量」與「流量」——
`slice-gen` 的範本若帶了同一句話，改存量沒有用。

#### 六、checksum：兩端不對稱，而那不是缺陷

新增的那一支是整份清單裡**唯一一道兩端不對稱**的閘門：它守「團隊不准改的檔」，
而**上游天天改那些檔**（上游就是生產者）。掛在上游的 PR 上要嘛恆紅要嘛恆綠，
兩種都沒有意義。

**裁決：上游在發版時「產生」章，fork 端在 PR 上「驗證」章。fork 端不得蓋章。**

這條路是這棵樹走過的 —— 而且走壞過一次，所以及格線是現成的：

| 前例                                 | 它給的約束                                                                                     |
| ------------------------------------ | ---------------------------------------------------------------------------------------------- |
| **C47**（建了 csp-verify 的指紋）    | 它**刻意不對建置產物取雜湊**：「任何切片的 PR 都會讓它變 —— 那道閘門會天天紅，一週內被刪掉」   |
| **C52**（把那套指紋拆掉）            | 判準是「**每次成本 × 觸發頻率**，不是有沒有價值」。checksum 要先估的是它多久紅一次、紅了誰付錢 |
| **C109 §七**（拒絕替根層清單加閘門） | ⚠️ **及格線：一張要被閘門守的清單必須是「活的」**，不能是有日期的過去式陳述                    |
| **C149**（樹指紋刻意只 warn）        | 基準率是量過的：最近 60 支 main commit 有 25 支動到涵蓋路徑（**42%**）。涵蓋路徑愈大愈快逼近它 |

因此 checksum 的清單用**目錄層級的規則**，不用逐檔名單：`platform/`、`tools/`、
以及根層幾支具名的 base 檔。逐檔名單第一天就開始腐爛（C109 §七 的形狀）。

而「腳手架的」與「團隊的」那條線，用**可延伸點**切，不整檔二選一：腳手架的部分
收進它自己的 base 檔由 checksum 守，根層 `vite.config.ts`／`eslint.config.js`／
`package.json` 只留 import／spread 給團隊擴充。理由是這條線同時是目的裡那句
「保留各團隊開發風格」的另一面 —— 團隊幾乎必然要改 lint 設定。

#### 七、C154 §三 的三份報備（checksum 是新增的機械檢查）

| 軸             | 報告                                                                                                                           |
| -------------- | ------------------------------------------------------------------------------------------------------------------------------ |
| **D16 交付軸** | **有分。** 採購與稽核在意「交付出去的腳手架完整性有沒有被動過」，那是一個在外面的收件人                                        |
| **D16 迭代軸** | **有分。** ① 對象在外：採用團隊的樹。② 壞法安靜：團隊改了受保護的檔，今天沒有任何東西會說話，症狀是下一次升級衝突              |
| **迭代軸級別** | **判讀。**「要推翻它該做什麼」：找出一次採用團隊改了受保護檔、而腳手架升級照樣無痛的實例 —— 有那個實例，這一支的迭代軸就是空的 |

自我保護的 fixture 獨立成節，**不計分**（C154 §三 第三項）。

#### 八、`AGENTS.md` 四條規則逐條處置

| 規則                               | 處置                                | 為什麼                                                                                                                                            |
| ---------------------------------- | ----------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------- |
| **一**（跑 `vpr ready` 全綠）      | **一個字不改**                      | 它的字面已經寫「＝ `vp check` ＋ 全套測試 ＋ 建置 ＋ `vpr gate`」，沒提 `spec-report`。要改的是 `scripts.ready`                                   |
| **二**（不得改設定或門檻達成綠燈） | **射程寫死，而它第一次有牙齒**      | 不准改的＝ checksum 守的那些檔，其餘是團隊的。今天這一條的「抬門檻」那一半沒有機制在守（`check.ts:19`，刻意）；新模型下它第一次指得到一個機械邊界 |
| **三**（完成的定義在驗收規格裡）   | **保留，而 `spec-report` 跟著下發** | 它是四條裡唯一講「人審什麼」的一條，抽掉報表之後變成一句沒有產出的話。它本來就不在 `gate` 裡，接進 `ready` 對團隊零阻擋成本                       |
| **四**（不得修改 `specs/`）        | **射程改寫**                        | fork 端剩團隊自己切片的規格；根層那兩份是上游專用（§九）                                                                                          |

⚠️ **精確地說：本則不改 `specs/` 底下任何檔案的「內容」。** 但規則四的射程是一句
關於 `specs/` 的陳述，而 `gate-thresholds.feature` 的**地位變了、位元組沒變** ——
這兩件事不一樣，寫在這裡免得將來被讀成「完全沒碰到」。

#### 九、根層 `specs/` 與 `promise-check` 是上游專用 —— ⚠️ 而這一節推翻了本則中途的一個結論

訪談中途曾把 `promise-check` 放進 fork 的必留，理由是它驗的承諾一由
`conformance` 守，而 `conformance` 是必留。**那個理由只涵蓋兩份根層規格裡的
一份，而它是錯的。**

`promise-check` 讀 `specs/` 底下全部，而 `gate-thresholds.feature` 指名
`tools/threshold-check`。(c) 之下那支工具**還在樹上**，所以 fork 端命中的不是
「閘門不存在」，是 `check.ts:189` 那一句：

> **「閘門沒有接上」**：`tools/threshold-check/src/cli.ts` 不在根 `package.json`
> 的 `scripts.gate` 裡

**採用團隊 fork 之後第一次跑 `vpr gate` 就紅** —— 而那不是缺陷，那正是
`promise-check` 存在的目的（C118 §二：「最實在的一件不是『弄壞了會不會紅』，
是規格指名的那道閘門必須真的接在 `scripts.gate` 上」）。

而中途提的解法「把 `gate-thresholds.feature` 標成上游專用」**是一個標籤，
不是一個機制** —— 沒有任何東西讀那個標籤。

**裁決：根層 `specs/` ＋ `promise-check` 整組是上游專用，fork 不接線。**
理由不是成本，是**歸屬**：根層 `specs/` 的定義就是「v1 對採用團隊的框架承諾」，
而**承諾是上游給團隊的，驗證承諾是上游的責任**。讓團隊在自己的 PR 上驗上游對
他們的承諾，本來就接錯邊了。

⚠️ 考慮過而**否決**的第三條路：讓 `ship: false` 的閘門，其規格自動判為「本樹
不判」。否決的理由是它給「**規格可以被設定關掉**」開了先例，而那離規則四禁的
那件事只有一步。

#### 十、oxlint 是例外：工具留下，數字歸團隊

`vite.config.ts:9` 逐字寫著：「**兩邊規則集刻意零重疊：oxlint 管對錯、風格與
複雜度，ESLint 只管安全與邊界。**」

那份清單的射程止於名冊那 17 列，而 oxlint 跑在 **`vp check`** 裡 —— 於是被砍的
`eslint` 是**安全與邊界**那一半，沒被碰到的 oxlint 是**風格與複雜度**那一半，
**正好與判準一相反**。

**裁決：oxlint 這個工具留下**（理由是 Vite+ 的完整性 —— 這棵樹踩過「繞過 `vp`
直接叫 oxlint 拿到的是預設集」），**而 `max` 值移到團隊那一半。**

理由是那組數字不是 Vite+ 的東西：`max-lines-per-function: 185`／`max-depth: 5`／
`max-params: 6`／`complexity: 39`（`vite.config.ts:104-107`）是**這棵樹用自己的
程式碼校準出來的**，對別的團隊的業務碼沒有意義；而維護它們的 `threshold-check`
不下發，所以它們只會腐化。C162 §八 量過腐化的樣子：`max-lines-per-function`
高了 40 行（239 而實測 199），`vp lint` exit 0、`vpr gate` 全綠、`pnpm ready`
全綠，**安靜了 15 支 commit**。

#### 十一、⚠️ 這棵樹跑過一次很像的實驗，而它被推翻了 —— 但本則的判準不是那一個

2026-08-17，`49b36da`「release(v1.0.0): 縮到五條承諾，其餘留在 main」用
**「不服務五條承諾就出局」**砍掉 8 樣：`bff-check`／`csp-verify`／`compliance`／
`exit-drill`／`pii-check`／`supply-chain`／`ui-survey` ＋ `UI-SURVEY.md`。
**隔天 C72 推翻了那個判準**，而 C72〈砍錯的〉表第一列逐字寫著：

> 「不服務五條承諾就出局」→ 連 `tools/doc-facts` 一起砍 —— 而它是 Tier 2 的
> 閘門之一，守的是 README／HANDOFF 的數字

而 C136 §二 記下了後續：**推翻之後只重判了 `gate-roster` 一支，沒有回頭問前一天
被砍的那一批**，那批是靠併線（#176）原封帶回來的，「而沒有任何東西說話」。

**本則的判準不是 C72 那一個，而且比它寬。** 差別要寫清楚：

- C72 推翻的判準保留五條承諾**全部**；本則的兩條判準**退掉承諾五**（§五）。
- C72 那次是**真刪**，而且刪在交付線上；本則是**不下發**，上游一支不少（§二）。

**所以本則不是 C72 的翻案，是一個 C72 沒有處理過的問題**（上游與下發是同一份
還是兩份）。⚠️ 而 C72 那張表仍然有效：**它擋下的那種錯是「拿一個判準去砍一批
沒有被逐支問過的東西」** —— 本則的 18 列是逐列問過的，這一節就是那個舉證。

#### 十二、本則沒有解的一件事，明文留白

(c) 之下上游與 fork 共用同一個 `package.json`，所以 `scripts.gate` 只有一份。
若上游把那 12 支移出 `scripts.gate`（讓它成為 fork 預設的 6 道），那麼上游自己
就得靠兩個 workflow 跑那 12 支 —— **而 `promise-check` 判「接上了沒有」讀的正是
`scripts.gate`**，於是 `gate-thresholds.feature` 會在**上游**紅。

**本則不裁這一格。** 它是批次 ② 的設計題，形狀已經寫在這裡，連同它的死結：
一支讀 `scripts.gate` 的檢查，遇到「`scripts.gate` 不再是閘門全集」這個新事實。
同 C148 §七 的體例：**只裁判準，不裁機制。**

#### 十三、本則不做什麼

- **零程式碼改動。** 名冊的 `ship` 欄位、接線、base 檔切法、checksum 本體、
  散文掃除，全部留給後續批次。
- **不改 `specs/` 底下任何檔案的內容**（§八 的但書）。
- **不動任何門檻**（AGENTS.md 規則二）—— §十 講的是把 `max` 值搬到哪一側由誰
  維護，不是調它。

#### 十四、與既有裁決的關係

| 裁決              | 關係                                                                               |
| ----------------- | ---------------------------------------------------------------------------------- |
| **C72／C136 §二** | **劃界** —— 最像的前例，而判準不同且更寬；§十一 逐條寫差在哪，並繼承它的舉證要求   |
| **C137 §一**      | **遵守** —— 兩條判準都是射程論證；明文寫下它擋不住本則，免得被當令箭               |
| **C134 §二**      | **應驗** —— 判準的效力依附在後果上，所以 §一 把後果的射程寫死                      |
| **C109 §七**      | **及格線** —— checksum 的清單必須是「活的」，所以走目錄規則不走逐檔名單            |
| **C47／C52**      | **前車** —— 指紋建了又拆的那一次；「每次成本 × 觸發頻率」直接決定 §六 的兩端不對稱 |
| **C149**          | **基準率** —— 42% 是 checksum 涵蓋路徑放大後會逼近的數字                           |
| **C154 §三／§四** | **兌現** —— checksum 是新增的機械檢查，三份報備在 §七                              |
| **C118 §二**      | **借判準** —— 「接上 ≠ 存在」正是 §九 那個紅燈的成因，而它證明機制在運作           |
| **C147 §二**      | **更正標籤** —— `threshold-check` 不是「防作弊」，是單向棘輪的降那一半（§三）      |
| **C162 §八**      | **借量測** —— 門檻腐化的實例：高了 40 行、全綠、安靜 15 支 commit（§十）           |
| **C132**          | **同體例** —— `ship` 與 `notInGateScript` 一樣是「一句必填的理由」，不是開關       |
| **C148 §七**      | **同體例** —— §十二 只裁判準不裁機制，留白明文寫下                                 |
| **C201**          | **提醒** —— §五 的散文掃除要分存量與流量，`slice-gen` 的範本另算                   |
| **C137 §四**      | **應驗第五次** —— §三 那三列是「判準套在描述上而不是實質上」                       |

### C216 — 名冊加上 `ship` 欄位（C215 批次 ①）：「下發」與「進不進 gate 鏈」是兩件事，而清單上第五列的理由也只對了一半（2026-09-11）

⚠️ **量測基準 `9ee257a`**（C215 合併後的 `main`）。

#### 一、做了什麼、不做什麼

**做：** `Gate` 加一個必填欄位 `ship: { to: "fork" | "upstream-only"; why: string }`，
17 列逐列填完 —— **fork 6 列**（`conformance`／`api-surface`／`exit-surface`／
`vue-typecheck`／`theme-verify`／`spec-report`）、**上游專用 11 列**。
`roster.test.ts`〈名冊本身的約束〉加一條 `ship.why` 非空斷言（`> 20`，與三個手足同門檻）。

**不做：**

- 不改 `scripts.gate`、兩個 workflow、README〈兩層檢查〉那張表 —— 那是批次 ② 的
  接線，連同 C215 §十二 的死結。
- 不碰 checksum（批次 ④）、不掃「五條→四條」的散文（批次 ⑤）。
- UNGATED 不加這個欄位（§四）。

⚠️ **本則不論證增減任何一道閘門**（C137 §一）。`GATES`／`UNGATED` 的成員一列都
沒動，`scripts.gate` 逐字不變。

#### 二、欄位形狀：必填，兩個方向都要理由

| 設計             | 會怎麼壞                                              |
| ---------------- | ----------------------------------------------------- |
| 預設下發         | 上游加一道閘門，每個 fork 開箱紅 —— C215 §九 那一種紅 |
| 預設不下發       | 新閘門安靜地永遠到不了團隊                            |
| **必填（採用）** | 第 18 列不做決定就是型別錯誤 —— 擋在它該擋人的那一刻  |

判別子用字面聯集不用布林，同 C132：那是一句必填的理由，不是一個開關。

⚠️ **它與 `notInGateScript` 正交，而 `spec-report` 就是交叉格**（下發 ∧ 不進
`scripts.gate`）。fork 的 gate 鏈因此今天是 **5 道**，C215 說的第 6 道要等批次 ④
的 checksum。批次 ② 若把 `to: "fork"` 讀成「進 fork 的 `scripts.gate`」，
`spec-report` 會被塞進 gate 鏈 —— 而它的 `notInGateScript` 寫的正是為什麼不能
（`vpr gate` 不跑測試，它會讀到上一次的 `.vitest-results.json`）。

#### 三、理由欄沒有照抄清單 —— 而第五列 C215 沒有問過

四列用 C215 §三 更正後的理由：`scope-check`／`gate-roster`／`doc-facts` 三列的
「不沿用治理文件就是綠燈」事實相反；`threshold-check` 不是「防作弊」。

⚠️ **第五列是 `bff-check`，清單的理由是「只在 CI，不在 vpr gate」。** C215 §三
沒有逐條更正它，§一 的兩條判準也都沒有收它 —— 那句話講的是接線位置，不是判準。
逐字對它自己的 `notInGateScript`：

- **前半成立**：它在 tier2 是獨立一步，不在 `scripts.gate` 裡。
- **後半要補**：它的測試由 `vp run -r test` 在本機跑到。所以它在 fork 的 `ready`
  裡**照跑**，`to: "upstream-only"` 拿掉的只有 tier2 那一步。

**結論不變**（不下發接線），寫下來的是它不下發的**是哪一半**。⚠️ C215 §十一 寫
「本則的 18 列是逐列問過的」—— 這一列的**結論**問過了，**理由**沒有。形狀同 C211
抓到的 `slice-gen`：一列的理由從來沒有被獨立做過。

#### 四、明文留白

1. **UNGATED 不加欄位。** C215 §四 的「分法」講的是閘門集合，UNGATED 六列本來就
   不在 gate 鏈或兩個 workflow 上。C215 只裁過其中兩列 —— `codemods` 下發、
   `release-distance` 不接進 fork 的 `ready` —— 另四列（`slice-gen`／`gate-kit`／
   `csp-verify`／`ui-survey`）**沒有被問過**。(c) 之下檔案全部下發，這四列的問題
   只剩接線，而它們今天沒有接線，所以不填不會錯。⚠️ 但將來若有人把 `ship` 推廣到
   UNGATED，**這四格要回去問人，不是拿判準一、二推出來** —— 那正是 C215 §十一
   繼承的那條舉證要求（不拿一個判準去砍一批沒有被逐支問過的東西）。
   `release-distance` 那一格是 `scripts.ready`（根 `vite.config.ts` 的 task）的
   接線，歸批次 ②。
2. **`exit-surface` 的 `--require-fresh` variant** 是排程才跑的。批次 ② 若從
   `ship` ＋ `variants` 推 fork 的 workflow，那個 variant 需要自己的答案；本則只在
   那一列寫一句留白。
3. ⚠️ **批次 ② 的一個新前提（判讀，非量測）：`ship` 管得到 `scripts.gate` 與兩個
   workflow，管不到 `vp run -r test`。** 上游專用的 11 列裡，至少 `gate-roster`／
   `doc-facts`／`scope-check`／`pii-check`／`compliance`／`supply-chain`／`bff-check`
   七支的 package 有 `test` script（`9ee257a` 逐支讀 `package.json`），而其中有讀
   真樹的測試 —— `gate-roster` 的「真的 repo 現在是一致的」就是
   `checkRoster(repoRoot())`。所以在 fork 上，團隊加一支自己的 `tools/*`，
   **`vpr gate` 不會紅，`vp run -r test` 會** —— 那是 C215 §三 替 `gate-roster`
   寫下的紅燈，換了一個入口。
   **要推翻它：** 在一份 fork 形狀的樹上加一支 `tools/x`、跑 `vp run -r test`，
   看那支測試是不是綠的。

#### 五、這條斷言自己的 D16 兩軸（C154）

依 C154 §四，射程含「在既有工具裡加一條規則」，所以 `ship.why` 那條非空斷言要報。

| 軸       | 這一條                                                          |
| -------- | --------------------------------------------------------------- |
| **交付** | **零。** 採購／資安／法務沒有一個收件人在意這一欄有沒有寫理由。 |
| **迭代** | **有分。** 見下。                                               |

- **① 對象在外** ✅ 它報的是**別人寫的登記內容** —— 同 C155 §四 對 `Gate.why` 的
  判法。這裡的「別人」是**下一個加閘門的上游作者**：第 18 列的去留由他決定，而這一格
  是 C215 之後唯一記得「fork 拿什麼」的地方。
- **② 壞法安靜** ✅ **級別：探針。** 把 `conformance` 的 `ship.why` 改成 `"短"`，
  逐格取結束碼（替換數逐格驗過恰好是 1）：

| 狀態                          | `gate-roster` CLI | `vitest` |
| ----------------------------- | ----------------- | -------- |
| 對照組（未變異）              | 0                 | 0        |
| `ship.why` → `"短"`，斷言在   | 0                 | **1**    |
| `ship.why` → `"短"`，斷言拿掉 | 0                 | **0**    |

第三列就是 ② 的反事實世界：**沒有這條斷言，一句說不出任何事的理由一路綠到底** ——
C155 §三 表上 `Gate.why` 那一列的原樣。跑前先 commit WIP，跑完 `git reset --hard`
回那個 commit，`gates.ts` 的 sha 與 commit 內容逐位相同。

⚠️ **這條斷言不是 C154 §三 第 3 條的自我防護夾具**：它吃的是真常數 `GATES`，
不走 `checkRoster` 的注入參數 —— 與 C155 §五 那三個手足同形，而 C155 §四 對同形的
東西判的是迭代軸有分。

⚠️ 門檻 `> 20` 今天咬不到任何一列（最短 57）。同 C155 §四：挑一個今天就會咬的
數字來證明它有用，量到的只會是那個數字。

#### 六、落在測試而不是 `check.ts` —— 而本則不裁 `#222`

C155 §四 把 `Gate.why` 放進閘門那一層，理由是「fork 團隊撞到的應該是閘門，不是
一支他們沒在讀的測試」。**`gate-roster` 自己是上游專用的**（本則那一列），所以
那條理由對 `ship` 不轉移 —— 寫 `ship` 的人是上游作者，而上游的 `ready` 跑測試。

⚠️ 這是對一個欄位的落點選擇，**不是對 C155 §七 那個留白的回答**：「必填理由該落在
閘門那一層還是測試那一層」仍然是 `#222`，本則沒有動它。

#### 七、⚠️ 這一欄今天只有一個讀者

批次 ① 之後，`ship` 的唯一讀者是 §五 那條斷言 —— 消費它的接線在批次 ②。
這是刻意的分步：先讓分法有一個可以被 review 的落點，再讓東西讀它。

寫在這裡，是因為那個形狀在這棵樹上有既定的讀法：「宣告成必填，而 `git grep` 只回
宣告那一行」長得像殘骸。**它不是殘骸，是還沒接上的第一半** —— 批次 ② 合併之前，
這一句是唯一分辨兩者的東西。

#### 八、與既有裁決的關係

| 裁決               | 關係                                                                            |
| ------------------ | ------------------------------------------------------------------------------- |
| **C215 §四**       | **兌現** —— 「分法記在名冊，加一個 `ship` 欄位，連理由一起寫」的那一步          |
| **C215 §三**       | **沿用** —— 四列的理由用更正後的版本                                            |
| **C215 §十一**     | **補一格** —— 「18 列逐列問過」對 `bff-check` 只涵蓋結論（§三）；舉證要求擋 §四 |
| **C215 §十二**     | **不碰** —— 死結留給批次 ②                                                      |
| **C132**           | **同體例** —— 字面聯集配必填理由，不是開關                                      |
| **C155 §四**       | **借判法** —— 吃真常數的必填理由斷言，迭代軸有分；落點理由不轉移（§六）         |
| **C155 §七／#222** | **不裁**                                                                        |
| **C154 §三／§四**  | **兌現** —— 兩軸在 §五                                                          |
| **C211**           | **同形** —— 一列的理由從來沒有被獨立做過（`slice-gen` → `bff-check`）           |
| **C137 §一**       | **遵守**                                                                        |

### C217 — fork 怎麼知道自己是 fork：一個由 fork 自己放的標記檔，而 `vpr gate` 變成選擇器 —— C215 §十二 的死結由「讓 `promise-check` 指向上游那一條」解開（2026-09-11）

⚠️ **量測基準 `cf259e3`**（C216 合併後的 `main`）。

#### 一、裁什麼

C215 §十二 把「fork 預設怎麼接線」明文留給批次 ②，並寫下它的死結。逐題問完：

| 題      | 裁決                                                                                           |
| ------- | ---------------------------------------------------------------------------------------------- |
| **Q22** | 升級管道：團隊把上游**合併**進自己的 fork（§二）                                               |
| **Q23** | 辨別子：fork 自己放一個標記檔 `.scaffold-fork`（§三）                                          |
| **Q24** | 上游專用工具的**測試**也在這一批處理，同一個辨別子（§五）                                      |
| **Q25** | tier2 不在名冊上的步驟全部上游專用，**含 gitleaks**（§六）                                     |
| **Q26** | `exit-drill.yml`、`supply-chain-recapture.yml`、`exit-surface --require-fresh` 上游專用（§六） |
| **Q27** | `scripts.gate` 變成選擇器，`promise-check` 改讀上游那一條（§四）                               |

**本則零程式碼。** 實作是下一支 PR，它自己的機械檢查在那一則報 C154 §三。

⚠️ **本則不論證增減任何一道閘門**（C137 §一）。上游實際執行的集合**一支都不少**（§七）。

#### 二、升級管道：合併，而 `HANDOFF.md:1138` 只寫了兩條裡的一條

`HANDOFF.md:1138` 寫著「**`platform/*` 發成內部套件，各案 fork 之後仍會升級它**」，
2026-08-16 從意圖升成既定做法。那一句是真的，但它**只講了 `platform/*`**。
`tools/`、根層設定與 workflow 在 fork 之後怎麼升級，樹上沒有任何一句話。

**裁決：團隊把上游合併進自己的 fork。** `platform/*` 另外照舊走內部套件。

理由是 C215 §六 已經預設了它：checksum 是「上游發版時產生章、fork 端在 PR 上驗章」，
**驗章的前提是 fork 手上的檔案來自上游** —— 不合併的話沒有東西可驗。
本則把那個前提寫成明文，而 `HANDOFF.md:1138` 那一句的補寫歸實作那支 PR。

#### 三、辨別子：一個由 fork 自己放的標記檔

**先量到的事實：這棵樹沒有任何東西分得出自己是上游還是 fork。** (c) 之下 fork 就是
整棵 clone，`package.json` 與兩個 workflow 在兩棵樹上逐字相同。所以任何接線方案
都只是「辨別子是什麼」的推論 —— 這一格要先定。

三個候選與它們的壞法：

| 候選                        | 忘了做的時候                                            | 方向     |
| --------------------------- | ------------------------------------------------------- | -------- |
| **fork 放標記檔（採用）**   | fork 忘了放 → 跑全部 15 道，等於今天的現況              | **大聲** |
| 上游設環境變數              | 上游本機忘了設 → **少跑 10 道而且全綠**，只有 CI 抓得到 | 安靜     |
| CI 只看 `github.repository` | 本機 `vpr ready` 分不出來 —— 只答得了一半               | —        |

**裁決：`.scaffold-fork` 存在＝這是 fork。** 由 fork 在「每 fork 一次做一次」那份
清單上多做一步放上去。

理由是**歸屬**，不是成本：「我是 fork」是一件關於 fork 自己的事，由 fork 自己聲明。
上游永遠沒有這個檔，所以 §二 的合併**不會在它上面衝突**；而上游的閘門集合在任何
情況下都不會因為它變少。

⚠️ **上游誤放的那一個方向是安靜的，所以要守**：上游 CI 加一步「上游不得有 fork
標記」。它是本則唯一新增的機械檢查，兩軸在 §八。

⚠️ **它與 C215 §四 的一處張力，已經交給人裁過**：C215 否決 (b) eject 的理由是
「造第二棵樹」，不是「一次性動作」。放一個檔不刪任何東西、不產生第二份樹形，
**判為不是 eject**。但它確實是 fork 要做的一步 —— 所以 `HANDOFF.md` 那一節的標題
〈開工前**唯一**必做的一件事〉會變成假的，改寫歸實作那支 PR。

⚠️ **辨別子只有一個。** CI 的上游專用步驟一律判 `.scaffold-fork`，不另外用
`github.repository` 判 —— 後者只出現在上面那一步「上游不得有 fork 標記」裡，
那是**檢查辨別子**，不是第二個辨別子。兩個辨別子會分岔，而分岔時兩邊都是綠的。

#### 四、接線形狀：`vpr gate` 變成選擇器 —— ⚠️ 而這一節推翻了訪談中的一格

| script（暫名）   | 內容                                                                                  |
| ---------------- | ------------------------------------------------------------------------------------- |
| `gate`           | 選擇器：沒有標記檔跑 `gate:upstream`，有就跑 `gate:fork`                              |
| `gate:upstream`  | **今天的 `scripts.gate`，逐字不變**（15 道）                                          |
| `gate:fork`      | 名冊 `ship.to === "fork"` 且不帶 `notInGateScript` 的那幾道（今天 5 道）              |
| `ready`          | 選擇器，同上                                                                          |
| `ready:upstream` | **今天的 `scripts.ready`，逐字不變**                                                  |
| `ready:fork`     | `vp check` ＋ 測試（排除上游專用，§五）＋ 建置 ＋ `vpr gate` ＋ `spec-report --check` |

三個約束：

1. **`gate:upstream` 與 `gate:fork` 都從名冊推導**，`gate-roster` ③ 逐字比對。
   分法的唯一事實來源留在名冊 —— 否則就是 C215 §四 那句「手抄五處，而沒有東西
   斷言它們一致」。
2. **`promise-check` 改讀 `gate:upstream`。** 在上游，`vpr gate` 交給 `gate:upstream`，
   所以它的判準「`vpr gate` 真的會執行它」（C118 §二）不變。這個「交給」由兩樣東西
   保證：選擇器自己的測試，與 §三 那一步「上游不得有 fork 標記」。
3. **選擇器不得住在上游專用的工具裡。** fork 每一次 `vpr ready` 都會跑它，而它的
   測試必須在 fork 的測試裡跑得到（§五 排除的是上游專用工具的 package）。

⚠️ **這一節推翻了 Q23 選項表上的一格。** 那張表寫著設計一「`scripts.gate` 逐字不變、
`promise-check` 不必改」。**那一格沒有拿 `AGENTS.md` 規則一的原文去對**：規則一寫著
「`vpr ready` ＝ `vp check` ＋ 全套測試 ＋ 建置 ＋ `vpr gate`」，而 C215 §八 裁它一個字
不改。`scripts.gate` 若維持 15 道，這句話在每一個 fork 裡都是假的，而且團隊或 agent
直接跑 `vpr gate` 會被 C215 決定不下發的那 10 道擋住。Q27 因此另立一題，裁 (y)。

**C215 §十二 的死結因此解開，解法不是讓 `scripts.gate` 保持全集，是讓讀它的那一支
明確指向上游那一條。** 死結的形狀 ——「一支讀 `scripts.gate` 的檢查，遇到
`scripts.gate` 不再是閘門全集」—— 在 (y) 之下不再成立：`promise-check` 讀的
`gate:upstream` 仍然是全集。

⚠️ **CI 不經過選擇器。** 兩個 workflow 仍然一步一格直接呼叫每支 CLI，
`gate-roster/src/check.ts` 檔頭否決 (a) 的兩個理由（D2 安全閘門不走自寫間接層、
CI 一步一格）都射在 CI 那一半，選擇器只在本機。

#### 五、上游專用工具的測試：同一個辨別子，排除清單從名冊推

`ready:fork` 的測試那一步排除 `ship.to === "upstream-only"` 那幾列的 package
（`vp run --filter '!<pattern>'`，`vp run --help` 實測有這個語法）。**排除清單從
名冊推，不手寫。** CI 的測試步驟同理，由同一個辨別子分流。

⚠️ 排除的是**名冊上有 `pkg` 的上游專用閘門**。`eslint`／`a11y` 沒有 package，
不在這一步的射程裡。

⚠️ UNGATED 六列的測試**照舊在 fork 裡跑** —— C216 §四 已經寫明那六列的去留大多
沒有被問過，本則不從判準推它們。`release-distance` 不在 `ready:fork` 裡，那是 C215
已經裁過的一格。

#### 六、CI：tier2 不在名冊上的步驟、兩個排程 workflow

**Q25 裁 (a)：** SAST 反向測試、Semgrep 掃描與上傳、拆 lockfile、SBOM 產出與完整性
檢查、Trivy SCA 掃描與上傳、**gitleaks**，在 fork 裡全部不跑。

判準是 C215 §一 **判準二**（風險與法遵是公司的事），不是 C215 §五 那張平台清單
（「公司有集中的 SAST／SCA 平台」）。⚠️ gitleaks 是唯一一列那句理由**字面上涵蓋
不到**的 —— 它既不是 SAST 也不是 SCA —— 所以單獨交給人確認過。拿平台清單逐項去比，
就是 C215 §三 更正過的那個形狀：判準套在描述上，不是實質上。

⚠️ 順帶一個事實：required check 裡的 `Semgrep OSS` 與 `Trivy` **不是獨立的 job**，
是 tier2 `gate` 那個 job 上傳 SARIF 時產生的檢查。fork 不跑那幾步，它們就不會出現；
fork 的分支保護由 fork 自己設。

**Q26：** `exit-drill.yml`（每季完整演練）、`supply-chain-recapture.yml`（手動重抓）、
tier2 排程裡的 `exit-surface --require-fresh`，**上游專用**。

理由：`evidence.json` 是**上游演練的證據**，fork 不重跑演練，`--require-fresh` 在 fork
裡遲早變成一個團隊沒做錯任何事的紅燈。`exit-surface` 的**主指令**（PR 上只驗退出面）
仍然下發 —— D2 保單在 fork 的 PR 上照樣有牙齒。`supply-chain-recapture.yml` 跟著
`supply-chain` 走，而那一列已經是上游專用。

#### 七、上游一支都不少 —— 這一條是 AGENTS.md 規則二的界線

重接 `scripts.gate` 讓某些東西不再紅，形狀上很接近規則二禁的動作。**分辨點是上游
實際執行的集合**：

- `gate:upstream`、`ready:upstream` 各自**逐字等於**今天的 `scripts.gate`、`scripts.ready`；
- 兩個 workflow 的每一步在上游照跑（上游沒有標記檔，所有條件為真）；
- 上游誤放標記檔會被 §三 那一步擋下。

實作那支 PR 要附**上游前後逐字相同**的比對，而不是「應該相同」。

#### 八、本則唯一新增的機械檢查：「上游不得有 fork 標記」的兩軸（C154 §三）

| 軸             | 報告                                                                                                                                                           |
| -------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **D16 交付軸** | **零。** 採購／資安／法務沒有一個收件人在意這個檔存不存在。                                                                                                    |
| **D16 迭代軸** | **有分。** ① 對象在外：上游那棵樹的狀態，被別人的 commit 改變。② 壞法安靜：少了它，上游誤放一個 `.scaffold-fork`，本機只跑 5 道、CI 跳過上游專用步驟，**全綠** |
| **迭代軸級別** | **判讀。**「要推翻它該做什麼」：在上游一支分支上放標記檔、不帶這一步，看 CI 是不是全綠 —— 若有任何一步紅，這一格的 ② 就不成立                                  |

⚠️ `gate-roster` 要擴充（比對 `gate:fork`／`ready:fork`／CI 步驟的條件），那是在
既有工具裡加規則，依 C154 §四 在射程內 —— **在實作那一則報**，本則不預填。

#### 九、對 C216 的更正（依 C136 §八，不改原文）

- **C216 §三**「`bff-check` 的測試在 fork 的 `ready` 裡**照跑**」—— 那一句在 C216 當時
  是真的；**批次 ② 合併後不再成立**，§五 把它排除了。
- **C216 §四.2**（`--require-fresh` 在 fork 排程的去留）—— 由 §六 答掉。
- **C216 §四.3**（`ship` 管不到 `vp run -r test`）—— 由 §五 答掉。

#### 十、明文留白

- **UNGATED 六列在 fork 裡的去留** —— 仍然只有 `codemods` 與 `release-distance` 被裁過（C216 §四）。
- **`renovate.json` 在 fork 裡怎麼處理** —— 沒有被問過。
- **「五條→四條」散文掃除** —— 批次 ⑤。
- **標記檔的內容** —— 本則只裁「存在」這一件事；要不要在裡面寫字，實作時定，但**不得讓內容影響判定**（否則辨別子變成兩個）。

#### 十一、本則不做什麼

- **零程式碼。**
- 不改 `specs/` 底下任何檔案的內容（規則四）。`gate-thresholds.feature` 在上游照樣被
  `promise-check` 執行。
- 不動任何門檻（規則二）。
- 不改 `AGENTS.md` —— §四 的全部目的就是讓規則一在兩棵樹上都是真的，而不是改它的字。

#### 十二、與既有裁決的關係

| 裁決              | 關係                                                                                   |
| ----------------- | -------------------------------------------------------------------------------------- |
| **C215 §十二**    | **解開** —— 讓 `promise-check` 指向上游那一條，而不是讓 `scripts.gate` 保持全集（§四） |
| **C215 §八**      | **遵守** —— 規則一一個字不改；(y) 讓它在兩棵樹上都是真的                               |
| **C215 §四**      | **兌現** —— (c) 全部下發、只改接線；標記檔判為不是 (b) eject（§三）                    |
| **C215 §六**      | **明文化其前提** —— checksum 預設了合併這條升級管道（§二）                             |
| **C215 §一**      | **套用** —— Q25 用判準二，不用平台清單（§六）                                          |
| **C216**          | **更正三處**（§九）                                                                    |
| **C118 §二**      | **保存** —— 「`vpr gate` 真的會執行它」在上游不變（§四）                               |
| **C154 §三／§四** | **兌現** —— 標記守衛的兩軸在 §八；`gate-roster` 的擴充在實作那一則報                   |
| **C136 §八**      | **體例** —— 更正不改原文                                                               |
| **C137 §一**      | **遵守** —— 論證是歸屬與射程；上游一支都不少（§七）                                    |

### C218 — 批次 ② 的實作：`vite-plus` 文件教的排除寫法會安靜地跑全部，而選擇器的第一版在 `vpr` 底下開不起巢狀的 `vp`（2026-09-11）

⚠️ **量測基準 `ee8b7ce`**（實作分支的最後一個 WIP）。

#### 一、做了什麼

照 C217 實作，逐項：

| 項            | 內容                                                                                                                                 |
| ------------- | ------------------------------------------------------------------------------------------------------------------------------------ |
| 選擇器        | 新工具 `tools/fork-select`（`UNGATED`、下發）；`gate`／`ready` ＝ `vp run --no-cache <名字>:select`，`<名字>:select` ＝ 選擇器       |
| 兩條鏈        | `gate:upstream`／`ready:upstream` **逐字等於** C217 之前的 `scripts.gate`／`scripts.ready`；`gate:fork`／`ready:fork` 從名冊推       |
| 名冊          | `Variant.ship`（選填、只准比主指令窄）；`exit-surface --require-fresh` 標上游專用（答掉 C216 §四.2）；`fork-select` 登記進 `UNGATED` |
| gate-roster   | `checkRoster` 拆成五段（§五 的第一條理由）；③ 改比對選擇器、兩條鏈與 fork 測試篩選；⑤b 新增辨別子那一步與每一步的條件                |
| promise-check | 讀 `gate:upstream`（C217 §四 第 2 條）                                                                                               |
| workflow      | 四支各加一步「這棵樹是上游還是 fork」（同一步守「上游不得有 fork 標記」）；上游專用步驟判它的輸出；tier1 測試拆兩步                  |
| 文件          | `HANDOFF.md`〈開工前必做的兩件事〉＋ `:1138` 補另一條升級管道（C217 §二）；README workspace 套件數 34 → 35                           |

#### 二、⚠️ 三件實測：兩件推翻了設計，一件推翻了量測本身

**（一）`vite-plus` 文件教的排除寫法，在這棵樹上安靜地跑全部。**

`node_modules/vite-plus/docs/guide/run.md` 的範例是 `vp run --filter "@my/*" --filter "!@my/utils" build`，
並寫明「多個 `--filter` 取聯集，排除在所有包含之後套用」。逐一實測（每一組都真的跑測試、數被選到的 package）：

| 寫法                                               | package | 被排除的兩支還在？ | 備註                          |
| -------------------------------------------------- | ------- | ------------------ | ----------------------------- |
| 對照組 `-r`                                        | 31      | —                  |                               |
| `-r` ＋ `--filter '!…'`                            | —       | —                  | **報錯**：兩者不能併用        |
| 只給排除（一個或兩個）                             | 31      | **在**             | task 61，比 package 多一倍    |
| **文件的寫法** `@org/*` ＋ 排除                    | 31      | **在**             | task 60                       |
| `*` ＋ 排除                                        | **0**   | —                  | ⚠️ **回 0** —— 什麼都沒跑而綠 |
| 目錄形式的排除 `!./tools/…`                        | 26      | **在**             |                               |
| `@org/*` ＋ `!@org/monorepo` ＋ 排除               | **29**  | 不在               | task 29                       |
| **採用**：`pnpm-workspace.yaml` 的四個目錄 ＋ 排除 | **29**  | 不在               | task 29                       |

**成因是根 package。** `@org/monorepo` 自己也是 workspace 成員，被 `@org/*` 與「除了某支以外」選中，
而它的 `test` 是 `vp run -r test` —— 被排除的那幾支從遞迴那一趟回來了。多出來的那一倍 task 就是它。
採用的寫法選不到根，而團隊新加的 package 不管取什麼名字都選得到；`--fail-if-no-match` 擋 `*` 那一列。
`gate-roster` 有一條測試專門釘住「`@org/*` 會紅」。

⚠️ 量測過程中我自己犯了一次：對照組與一組排除**並行**跑，兩趟的 `slice-gen` e2e 搶同一個
`features/zz-slice-gen-e2e`，那一組的 `RC=1` 是互搶不是篩選。之後每一組都單獨跑。

**（二）選擇器的第一版，在 `vpr` 底下開不起巢狀的 `vp`。**

第一版的 `gate` 直接是 `node tools/fork-select/src/cli.ts --script gate`。在一棵乾淨的 clone 裡，
`vpr ready`／`vpr gate` 每一趟都停在巢狀那條鏈的第一個子任務：
`Failed to spawn process: underlying os error: Invalid argument (os error 22)`。

帶鹽值防重播的量測（內層每次讀一個剛寫進去的值並印出；印出的值對得上才算真的執行）：

| 外層怎麼跑                      | 內層真的執行？ | `os error 22` |
| ------------------------------- | -------------- | ------------- |
| 直接從 shell（對照組）          | ✅             | 0             |
| `vpr` 跑、會被快取追蹤的 script | ❌             | **1**         |
| 包一層 `vp run --no-cache`      | ✅（兩趟）     | 0             |
| `pnpm run`                      | ✅             | 0             |

**被快取追蹤的行程裡，巢狀的 `vp` 只要必須真的執行就開不起子行程。** 修法是 `--no-cache` 那一層；
`gate-roster` 有一條測試專門釘住「少了那一層會紅」，選擇器的檔頭寫著為什麼。

**（三）⚠️ 那個錯被快取重播蓋住過，我的量測回報了全綠。**

找（二）的成因時，我先量了一組「在被追蹤的 script 裡開 `vp`」，**五組全綠**，差點寫下「巢狀本身沒問題」。
原因是內層第一趟是直接從 shell 跑的、結果進了快取，之後每一個「巢狀」都是 cache hit ——
**重播不開行程，所以不會出錯。** 真的選擇器那幾趟，內層每一段都是 cache miss（有檔案改過），所以每一趟都撞上。

這條錯的形狀是：**只在「有東西改過」的那一趟出現**。一個只在本機跑第二次的人永遠看不到它。
解法寫在上面那張表的第一句：量測要讓內層每次讀一個剛寫進去的值，並斷言印出來的就是那個值。

同一段路上還有兩次量測台壞在我自己：塞進 `vite.config.ts` 的暫時 task 寫了 `input: []`
（這一版不接受，整張 task 圖載不起來，六組全部同一個錯）、內層 `node -e console.log(1+41)` 沒加引號
（shell 語法錯，對照組就沒過）。**兩次都是對照組先抓到的。**

#### 三、上游一支都不少（C217 §七）

**本機那一半，逐字比對 `origin/main` 的 `package.json`：**

| 比對                                   | 結果                                      |
| -------------------------------------- | ----------------------------------------- |
| `gate:upstream` === `main` 的 `gate`   | **true**（583 字元）                      |
| `ready:upstream` === `main` 的 `ready` | **true**                                  |
| 其餘 script                            | 移除 0、內容改動只有 `gate`／`ready` 兩支 |

**快取行為與 C217 之前相同**（`vpr gate`，改一支 `conformance` 讀的檔再跑）：

| 趟                               | 判定               | 結果                                               |
| -------------------------------- | ------------------ | -------------------------------------------------- |
| 第二趟（沒改東西）               | 上游               | 14 段裡 13 段 cache hit                            |
| 改了 `features/order/src/api.ts` | 上游               | `conformance`：`cache miss: … modified, executing` |
| 放上 `.scaffold-fork`            | fork → `gate:fork` | 5 段，**`rc=0`**，沒有 `gate-roster`               |
| 拿掉                             | 回到上游           | —                                                  |

**CI 那一半，逐步驟對齊 #352 那一趟 PR**（同為 `pull_request` 觸發、全綠）：

| workflow | #352（基準）            | 本 PR（`97f2bef`）      | 差異                                                                              |
| -------- | ----------------------- | ----------------------- | --------------------------------------------------------------------------------- |
| tier1    | 20 success              | 21 success ＋ 1 skipped | 新增兩步：辨別子（success）、fork 專用的測試步驟（skipped —— 在上游本來就該跳過） |
| tier2    | 33 success ＋ 1 skipped | 34 success ＋ 1 skipped | 新增一步：辨別子（success）；那 1 步 skipped 仍是只在排程跑的 `--require-fresh`   |

**基準的每一步都在、結論逐一相同，沒有任何一步變成 skipped**（逐名比對，problems 0）。
⚠️ 基準不用 `main` 最近一趟：tier2 在 `main` 上只有排程觸發，而最近那一趟是修 CVE 之前的紅燈（`c77e497`）。

#### 四、fork 演練（乾淨 clone，`pnpm install --offline`）

| 趟            | 做法                                          | 結果                                                                                                                                                                                                                                                         |
| ------------- | --------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **R1**        | 放 `.scaffold-fork` 並 commit，跑 `vpr ready` | **rc=0**。判定 fork → `ready:fork`；`gate-roster`／`doc-facts` 的測試**沒有跑**；`gate:fork` 5 道；`spec-report --check` ✓「與現況一致」（完成率 4/5）；`os error 22` 0 次                                                                                   |
| **R2**        | 再加一支團隊自己的 `tools/x`，跑 `vpr ready`  | **rc=0**。`工具沒登記` 0 次、`gate-roster` 的測試沒跑、`spec-report --check` ✓。**C216 §四.3 那個紅燈解掉了**。⚠️ 第一趟紅在 `vp check` 的格式 —— 我寫進去的 `tools/x/package.json` 是單行 JSON；那是團隊那一半的東西，fork 的 `vp check` 照樣管它，這是對的 |
| **R3 對照組** | 拿掉標記檔，只跑 `vpr gate`                   | **rc=1**，紅在 `gate-roster`「工具沒登記：`tools/x`」—— C216 §四.3 那個紅燈，換回上游那一條就回來了。**它證明 R2 的綠不是因為那一步什麼都沒量到**                                                                                                            |

⚠️ **`spec-report --check` 那一格曾被預期會紅**（`SPEC-REPORT.md` 是上游產的，而 fork 的測試集合少了 9 支）。
實測是綠的：它只讀 `features/*/specs`，被排除的那 9 支裡沒有任何一份業務規格。

#### 五、C154 §三：逐條報備，不合報

`gate-roster` 這一批新增的每一條規則各報一格（C155 §五：一份報告蓋住幾條規則，就是 C70 那個形狀）。
四條的交付軸都是**零** —— 採購／資安／法務沒有一個收件人在意 fork 的接線 —— 所以下表只列迭代軸。

| 規則                                           | ① 對象在外                 | ② 壞法安靜（它不在的世界）                                               | 級別     | 要推翻它                                                         |
| ---------------------------------------------- | -------------------------- | ------------------------------------------------------------------------ | -------- | ---------------------------------------------------------------- |
| **選擇器的形狀**（`gate`／`ready`／`:select`） | ✅ 別人寫的 `package.json` | 上游有人把 `gate` 寫成 `gate:fork` 那一條：上游只跑 5 道，**全綠**       | 判讀     | 在上游那樣改、拿掉這條規則，看 `vpr ready` 是不是綠的            |
| **`gate:fork` 的推導比對**                     | ✅ 同上                    | `gate:fork` 少一道：fork 安靜地少跑那一道，**全綠**                      | 判讀     | 刪一道、拿掉規則，看 fork 的 `vpr gate` 是不是綠的               |
| **fork 測試篩選的比對**                        | ✅ 同上＋tier1 workflow    | 改成 `*` 的寫法：一個 package 都沒跑而**回 0**                           | **探針** | 見 §二（一）那張表的 `*` 列 —— 已經量過                          |
| **workflow 的辨別子與步驟條件**                | ✅ 別人寫的 workflow       | 辨別子那一步不見、或下發的步驟帶了上游條件：該跑的步驟被跳過，job **綠** | 判讀     | 在一支分支上拿掉那一步，看 CI 是不是綠而上游專用步驟全是 skipped |

⚠️ `Variant.ship`「只准比主指令窄」那一條落在 `roster.test.ts`，與 C216 那條同形（吃真常數）：
① ✅ 登記內容；② ✅ 判讀 —— 把上游專用閘門的某個 variant 標成下發，fork 就多跑一道 C215 決定不交給它的檢查，
而那一道多半是綠的，沒有人發現 fork 預設長大了。

⚠️ **自我防護的夾具另計，不計迭代軸的分**（C154 §三 第 3 條）：`describe("fork 預設的接線（C217）")`
那十三條走的是 `checkRoster` 的注入參數，守的是這幾條規則自己。

⚠️ **拆 `checkRoster` 的理由不是可維護性**（C137 §一 不准拿那一類論證任何東西）：是它加了這批規則之後
複雜度 47、行數 275，超過根層門檻 39／185，`vp check` 紅、`threshold-check` 與 `promise-check` 跟著紅
（AGENTS.md 規則二：要改的是程式碼）。拆完之後 `threshold-check` 仍然是「11 格一致」—— 撐著那兩個最大值的
是別的函式，**這一批沒有動任何門檻**（C217 §十一）。

#### 六、留白

- **四個 workflow 各有一份「辨別子那一步」的 shell。** tier1／tier2 那兩份由 `gate-roster` ⑤b 守「在不在」，
  **內容沒有東西在比對**；兩個排程 workflow 那兩份連「在不在」都沒有東西守 —— 與 `gates.ts` 檔頭寫的
  `exit-drill.yml` 那個洞是同一個洞。
- **tier2 不在名冊上的步驟**（SAST／SBOM／SCA／gitleaks）的上游條件，`gate-roster` 看不見 —— 同 `gates.ts`
  檔頭〈刻意不涵蓋什麼〉那一節。它們的去留由 C217 §六 裁定，寫在 workflow 裡。
- **`vp install` 印三條 `vitest-cucumber` 建 bin 失敗的 WARN。** 這一批沒有動那個 package；是不是既有的，沒有查。
- C217 §十 列的四格照舊留白。

#### 七、與既有裁決的關係

| 裁決                  | 關係                                                                                                        |
| --------------------- | ----------------------------------------------------------------------------------------------------------- |
| **C217**              | **實作** —— §三 辨別子、§四 選擇器與三個約束、§五 排除清單、§六 CI、§七 上游一支都不少                      |
| **C216 §四.2／§四.3** | **答掉** —— `--require-fresh` 的去留、`ship` 管不到的測試那一步                                             |
| **C215 §十二**        | **兌現** —— 死結的解法落在 `promise-check` 的一個常數上                                                     |
| **C171 §九**          | **同一族** —— `vp run` 的快取行為決定了能不能這樣寫；那一則是「輸入不是檔案」，本則是「巢狀 `vp` 開不起來」 |
| **C155 §五**          | **體例** —— 一條規則一份報備，不合報                                                                        |
| **C154 §三／§四**     | **兌現** —— §五                                                                                             |
| **C147 §二**          | **沒有觸發** —— 拆函式之後最大值沒有降，門檻不動                                                            |
| **C137 §一**          | **遵守** —— 拆函式的理由是門檻，不是可維護性                                                                |

### C219 — 批次 ③ 的實作：base 檔切在「誰的碼」上，而業務碼那一半的門檻是給 fork 的起始值、不入棘輪（2026-09-11）

⚠️ **量測基準 `733c73f`**（實作分支的 WIP）。

#### 一、做了什麼

照 C215 §六 實作，並依 Q28–Q31 的回答（四題都照建議）：

| 項                                         | 內容                                                                                                                                                                                                                                                                                                                     |
| ------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `vite.scaffold.ts`（新，根層）             | 腳手架那一半的 lint 設定：外掛、`no-eval`／`no-implied-eval`／`import/no-cycle`、選項，與 `scaffoldOverrides`（`tools/`／`platform/` 的產品碼與測試碼門檻、`slice-gen` 的 per-file 放行、`.semgrep/`、`features/*` 邊界）。**不 import `vite-plus`**，型別是本地結構型別                                                 |
| `vite.config.ts`                           | spread `scaffoldLint`；業務碼的門檻與測試碼 override 留在這裡；`...scaffoldOverrides` **排在 `overrides` 最後**；`create`、vite `plugins`、`run` 留在這裡（§三）                                                                                                                                                         |
| `@org/eslint-config/scaffold`（新 export） | 組 `.`＋`./worktrees`＋`.semgrep/` 的排除；根層 `eslint.config.js` 變成 `[...scaffold]`（Q30）。`.` 一個字沒動（C190 §二 2）                                                                                                                                                                                             |
| `threshold-check`                          | 兩份設定都讀、都放進農場、**格數都數**，只量 `vite.scaffold.ts`（§四）；新增 `pairPass`（只配被壓的那幾格）                                                                                                                                                                                                              |
| `promise-check`                            | 副本多一支 `vite.scaffold.ts`；`raiseFirstThreshold` 改抬它（不改的話 `gate-thresholds.feature` 場景一會報〈承諾沒有牙齒〉）                                                                                                                                                                                             |
| 文件與登記                                 | `CODEOWNERS` 加一列（21 → 22，`HANDOFF.md` 兩處跟著改）、`SCOPE.md` 根層表、README 目錄樹、`TESTING.md` 層 1、`HANDOFF.md` 升級管道那一段、`API.md`（13 個進入點、161 個 export）、名冊 `threshold-check` 的 `ship.why`。⚠️ `HANDOFF.md:939` 的 12／160 → 13／161 是**第一趟 `vpr ready` 由 `doc-facts` 抓到的**，我漏了 |

#### 二、四題的裁決

| #       | 問題                                                                                                                | 裁決                                                                                                                                                                                                                                                    |
| ------- | ------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Q28** | C215 §十「門檻數字歸團隊」，而同一組數字也管 `tools/`／`platform/` —— 團隊改不了、批次 ④ 之後還會被 checksum 守的碼 | **按「誰的碼」切**。業務碼的數字在根層歸團隊；腳手架自己的碼用 base 裡排在最後的 override，帶腳手架的數字。這是 C215 §十 的**射程說清楚**，不是推翻：§十 的理由是「那組數字用這棵樹的碼校準、對別的團隊的業務碼沒有意義」，那句話對腳手架自己的碼不成立 |
| **Q29** | C215 §六 說根層 `package.json`「只留 import／spread」—— JSON 沒有這個機制；`pnpm-workspace.yaml` 同形               | **③ 不拆，交給 ④**：checksum 改守 key 層級（`gate:*`／`ready:*`／工具別名、catalog 裡腳手架那幾列）。⚠️ **C215 §六 這一格照字面做不到**，依 C137 §四 的體例寫在這裡，不默默改成別的東西                                                                 |
| **Q30** | `eslint.config.js` 根層只剩兩條腳手架自己的（worktrees、`.semgrep/` 排除）                                          | **另開 `./scaffold` export**，`.` 不動                                                                                                                                                                                                                  |
| **Q31** | 拆完之後 `threshold-check` 要求把業務碼那一半降到上游示範碼的實測最大值，而那組數字就是每個 fork 的預設             | **B：業務碼那一半是給 fork 的起始值，不入棘輪**（§四）                                                                                                                                                                                                  |

我自己判、沒有問的三格，理由都是已經讀過的程式碼：

- **base 放根層，不放 `platform/`。** ⚠️ 這一格我中途翻過一次：先判 `platform/vite-config`（與 `@org/eslint-config` 同體例、checksum 的目錄規則涵蓋得到），讀完 `threshold-check` 的農場才改判 —— 農場把根層每一項連過去、只換根層的設定檔，base 放在 `platform/` 底下就得把一個被連過去的目錄拆開重建。放根層還有兩個附帶的好處：`exit-surface` 不掃根層；C215 §六 本來就寫著「根層幾支具名的 base 檔」。
- **vite 的 `plugins` 留在根層。** 退出演練的 plugin 帳目只讀退出面設定檔的文字（`tools/exit-drill/src/plugins.ts`）；搬走就是親手造出那支檔檔頭描述的洞。
- **`create` 留在根層。** `vp create vite:generator` 會直接改寫這一段的文字（vite-plus 的 create 文件）；而且 `vp create --list` 不列本機範本，這一格的 spread 量不到（§三）。

#### 三、實測（每一組都帶對照組，量完都還原，殘留 0）

**（一）spread 可行，三態。** 受測的是沒有數字選項的規則，所以不影響門檻的格數：

| 觀測                                       | A 原樣 | B 刪掉不補                    | C 從另一支檔 spread |
| ------------------------------------------ | ------ | ----------------------------- | ------------------- |
| `no-eval`（`--print-config` 頂層 `rules`） | `deny` | `warn`（oxlint 預設）         | `deny`              |
| `run.tasks` 的 `release-distance`          | RC 0   | RC 1                          | RC 0                |
| `vp create --list` 的本機範本              | 不列   | 不列（**與 A 逐位元組相同**） | —                   |

⚠️ 第一版的讀數是 `grep -c '"no-eval"'`，B 態讀出 2 —— 另一處是 `.semgrep/` override 裡的 `"allow"`。**對照組抓到的**，改成解析 JSON 只看頂層 `rules`。

**（二）門檻的數字不能搬家，除非 `threshold-check` 一起改。** 把 `max-params` 從根層搬到另一支檔再 spread 回來：`--print-config` 照樣讀出 6，而 `threshold-check` **RC 1**，「原始碼裡改寫了 10 格門檻，而 --print-config 讀出 11 格」。它只讀根層那一份的文字 —— 所以 Q28 的切法必須連 `threshold-check` 一起改。

**（三）⚠️ 兩份一起壓地板，量測台自己壞了。** 地板值是違規歸屬（同一條規則的第 n 格拿到 n），前提是每一格的地板值低於它的門檻。兩份合起來同一條規則出現四次，地板值排到 3，而 `max-depth` 測試碼那一格的門檻就是 3：「地板值 3 沒有低於門檻 3 —— 探針量不到分佈」。換排序救不了（拿到高地板值的那一格，真最大值不超過地板時會被報成「量不到」）。

改成**一份一趟**之後量出業務碼那一半：7 格過期、3 格量不到。那 3 格直接壓到 0 另量：

| 格                         | 量測台說         | 壓到 0 的真讀數      | 判讀                                                       |
| -------------------------- | ---------------- | -------------------- | ---------------------------------------------------------- |
| 業務碼測試 `max-depth`     | 量不到（地板 1） | **max 1**，16 個單位 | ⚠️ **量測台的假讀數** —— 真最大值等於地板值時看不見（§六） |
| 根層 `vue/max-props`       | 量不到（地板 0） | 0 個單位             | 真的：`apps/`、`features/` 沒有任何元件帶 props            |
| 業務碼測試 `vue/max-props` | 量不到（地板 1） | 0 個單位             | 真的                                                       |

**同一趟量測的副產品，而它是整個切法的地基：** 把業務碼的測試 override 壓到 0 之後，報進來的違規**沒有一條在 `tools/` 或 `platform/`**（0 條）。那證明「oxlint 的 override 後者蓋前者、腳手架那一串排最後」在這棵樹上成立 —— 少了這一條，§二 Q28 的切法不成立。

**（四）最終狀態。** `threshold-check` 綠：「複雜度門檻與實測最大值一致（11 格，vite.scaffold.ts）」，根層 10 格只數不量。**腳手架那 11 格的數字一個都沒變** —— 撐著舊值的極端值全在 `tools/`，所以搬過去就是實測最大值。業務碼那 10 格逐格等於批次 ③ 之前的值。**這一批沒有動任何一格門檻的值**（AGENTS.md 規則二）。

#### 四、Q31：業務碼那一半為什麼不入棘輪

兩個選項，fork 的業務碼**實際吃到的門檻**：

|                                | 產品碼（行數／參數／複雜度／深度）      | 測試碼（行數／深度） |
| ------------------------------ | --------------------------------------- | -------------------- |
| **A 照 C147 §二 降**           | 185→**104**、6→**2**、39→**5**、5→**2** | 455→**154**、3→**1** |
| **B 起始值、不入棘輪**（採用） | 與批次 ③ 之前逐格相同                   | 同左                 |

**判準是 C215 §一 判準一：腳手架不規定團隊怎麼寫程式** —— `threshold-check` 當初被判 upstream-only 就是這一條。A 等於把「不下發」的那件事用設定檔重新下發回去，而且比今天更緊：團隊在 `it()` 裡寫一個迴圈就是深度 2，第一天紅。

**沒有第三條路**：fork 繼承的就是同一支檔，所以不存在「上游示範碼的校準值」與「下發的預設」分開放的位置 —— 能分的（腳手架自己的碼）已經在 Q28 分出去了。

⚠️ **這一格縮了一道閘門的射程**：上游自己的 `apps/`、`features/` 從此沒有「降」的棘輪，C147 §二 在業務碼那一半不再適用。依 AGENTS.md 規則二，它由人裁（Q31），不是實作者的判斷。
⚠️ **理由不是工作量**（A 要先修量測台、要另裁兩格 `vue/max-props`）—— C137 §一 不准那一類論證，而它在這裡也不是決定性的。

#### 五、C154 §三

**這一批沒有新增機械規則。** `threshold-check` 的射程**縮了**（業務碼 10 格只數不量，§四），`pairPass` 與「兩份合計 = `--print-config`」是那支工具的自我防護夾具，依 C154 §三 第 3 條不計分。

#### 六、留白

- ⚠️ **`specs/gate-thresholds.feature` 的註解說「這裡被驗的對象是 `vite.config.ts` 裡那幾格門檻的數字」—— 現在不成立**：被量的是 `vite.scaffold.ts`，根層那份只數不量。**規則四不准我改 `specs/`**，交給人。規格的兩個場景本身仍然成立（`promise-check` 改抬 `vite.scaffold.ts` 之後，場景一照樣紅在「門檻過期」）。
- ⚠️ **排名地板的假讀數仍然潛伏在 `vite.scaffold.ts` 裡。** 同一條規則的第 n 格拿到地板值 n，而那一格的真最大值不超過 n 時，量測台報「量不到」而不是「過期」—— §三（三）第一列就是它。今天腳手架那 11 格全部量得到（最近的一格：測試碼 `vue/max-props` 真最大值 2、地板 1）；**哪天那兩支 fixture `.vue` 被刪掉，它會先以錯的名字紅**。這一批沒有修。
- `package.json`／`pnpm-workspace.yaml` 的切法交給 ④（Q29）。
- ⚠️ **checksum 守得住位元組，守不住生效的設定。** 團隊在根層 `rules` 裡寫在 `...scaffoldLint.rules` **之後**的同名規則，會蓋掉腳手架的（例如把 `no-eval` 關掉）；`vite.scaffold.ts` 一個位元組都沒動。這是 ④ 的設計題。
- `AGENTS.md` 規則二的字面仍然是「不得修改設定或門檻來達成綠燈」，沒有區分根層那一半是團隊的 —— 射程要寫死是 C215 §八 的事，不在這一批。
- `tier1-quality.yml` 那句「四趟 `vp lint`」與名冊的「五趟」本來就對不上；這一批兩處都只改了檔名，沒有修那個數字。

#### 七、與既有裁決的關係

| 裁決              | 關係                                                                                 |
| ----------------- | ------------------------------------------------------------------------------------ |
| **C215 §六**      | **實作** —— 可延伸點切在 import／spread；⚠️ `package.json` 那一格照字面做不到（Q29） |
| **C215 §十**      | **射程說清楚** —— 「數字歸團隊」只涵蓋業務碼（Q28）                                  |
| **C215 §一**      | **判準** —— 判準一決定了 Q31                                                         |
| **C147 §二**      | **射程縮小** —— 單向棘輪只在腳手架那一份（Q31，由人裁）                              |
| **C162／C163**    | **改寫** —— `threshold-check` 讀兩份、量一份；`--root` 換的是兩份                    |
| **C190 §二 2**    | **遵守** —— worktrees 不進 `@org/eslint-config` 的 `.`                               |
| **C217 §二**      | **兌現** —— 合併上游這條升級管道，根層設定從此不會撞在同一行                         |
| **C137 §一／§四** | **遵守** —— 理由是射程不是成本；做不到的那一格照實寫                                 |

#### 八、fork 演練（乾淨 clone，`--no-tags`，`pnpm install --offline`，基準 `d088cd6`）

| 趟            | 做法                                                                                             | 結果                                                                                              |
| ------------- | ------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------- |
| **R1**        | 放 `.scaffold-fork` 並 commit，跑 `vpr ready`                                                    | **rc 0**，選擇器判定 fork                                                                         |
| **R2**        | 團隊把**自己那一半**的 `max-lines-per-function` 收到 110（業務碼實測最大值 104），跑 `vpr ready` | **rc 0**，那條規則 0 條違規 —— 腳手架的碼沒有吃到 110                                             |
| **R3 對照組** | 同一個 110 套到 `vite.scaffold.ts` 的產品碼那一格，跑 `vp check`                                 | **rc 1**，11 條違規，全部在 `tools/`。**它證明 R2 的綠不是因為 110 太鬆**                         |
| **R4**        | 另開一支「上游」改 `vite.scaffold.ts`（`slice-gen` 那一格），合併進 R2 的 fork                   | **rc 0，衝突 0**                                                                                  |
| **R4 對照組** | 「上游」改根層 `vite.config.ts` 同一行，合併                                                     | **rc 1，衝突在 `vite.config.ts`**。**它證明 R4 的零衝突是切法買到的，不是那兩支分支本來就不相交** |

上游那一條：本機 `vpr ready` **READY_RC 0**（第二趟；第一趟紅在上面那格 `doc-facts`）。
