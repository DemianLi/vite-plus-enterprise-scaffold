# 版本沿革

格式參考 [Keep a Changelog](https://keepachangelog.com/zh-TW/1.1.0/)，
版號遵循 [Semantic Versioning](https://semver.org/lang/zh-TW/)。

> ⚠️ **這個 repo 的 SemVer 承諾對象是 `platform/*` 的型別形狀。**
> `tools/api-surface` 每次 CI 比對每個進入點的 props／slot／emit／成員形狀，
> **移除、改名、或改變形狀就讓閘門失敗**，而且必須附一份可執行的 codemod。
> 也就是說：major 版號不是宣告，是**閘門強制出來的**。

---

## [1.12.0] — 2026-08-25

**版控的檔案模式變更，逐行 diff 是零。** `git diff --stat` 印 `| 0`、
`git status --short` 印 ` M` —— 與任何一般修改長得一樣，而 `vp check`、全套測試、
每一道閘門、每一道 CI **一個都沒有在看那個位**。這一版補上那道檢查。

它已經發作過三次，源頭各不相同（`vp install`／`vp create`、`pnpm install` 建 bin
連結、突變測試的 `inPlace` 還原）—— ⚠️ **其中一次進了發出去的版本**（`v1.7.0`）。
每一次都是靠人在發版前用 `git diff --summary` 追出來的。

### ⚠️ 拉 v1 的團隊：升上來**可能**從綠變紅，而那正是重點

| 你的樹上如果…                                      | 會發生什麼                                                     |
| -------------------------------------------------- | -------------------------------------------------------------- |
| `package.json` 宣告的 `bin` 目標不是 `100755`      | **`vpr gate` 紅**，訊息附 `git update-index --chmod=+x <path>` |
| 有檔案是 `100755` 卻沒有任何 `package.json` 宣告它 | **也紅** —— 反方向同樣判，`--chmod=-x`                         |
| 兩邊都對                                           | 成功訊息多一行，帶「看了幾個版控檔案」的數字                   |
| 用 `--root` 跑 `conformance`（沙盒／反向測試）     | ⚠️ **這條規則不跑**，而且會明說它沒跑 —— 副本不是版控          |

**minor 不是 patch**：一道新的判定會讓原本綠的樹變紅，而判準是「升上來會不會壞」，
不是「改了什麼」。`platform/*` 的型別形狀一個位元組都沒動。

### 新增

- `tools/conformance` 多一條規則 `檔案模式`：**宣告了 `bin` 的目標一律 `100755`，
  其餘一律 `100644`**。事實來源是 `git ls-files -s`，不是檔案系統的 `stat`
  —— git 只記兩個值，而工作區的權限位比那多。
- ⚠️ 判準**不是**「有 shebang 就要 755」：那會把測試 fixture 與範例掃進來。
  `bin` 是宣告 —— 有人打算讓別人直接執行它，而那個意圖寫在 `package.json` 裡。
  兩種 `bin` 形狀都認得（物件八支、字串一支）。

### 已知不守的

- **`--root` 底下什麼都不說。** 副本不是版控，這條規則問的問題在那裡沒有答案 ——
  ⚠️ 不是「答案是綠的」。代價是 `conformance` 唯一的端對端反向測試機制照不到它，
  所以接線另外用一條測試守（斷言那個數字，不是那句話）。
- **只判 blob 的兩個模式** —— symlink 與 submodule 不判。
- ⚠️⚠️ **工作區單獨翻掉的模式，這道閘門看不到。** 事實來源是 `git ls-files -s`，
  讀的是 **index** —— 它守的是**進得了版控的**漂移（staged 或 committed），
  而 `chmod -x` 之後不 `git add`，閘門是綠的（實測）。
- ⚠️ 所以**突變測試那個缺口仍然隱形**（`v1.11.0` 的已知代價）：`stryker run` 的
  `inPlace` 還原翻的是工作區。**「跑完看一眼 `git diff --summary`」沒有被取代，
  仍然必要** —— 兩者互補：`git diff` 看工作區，這道閘門看 index。
  ⚠️ 本條在 `v1.12.0` 發出時寫反了（寫成「閘門會紅」），當日更正，見 C122 §七。

裁決見 `DECISIONS.md` 的 **C122**（含變異驗證表與第一版那個恆真的接線測試）。

---

## [1.11.0] — 2026-08-24

**這棵樹有十二道閘門，沒有一道在回答「那些沒有人審的測試是不是空頭支票」。**
這一版把突變測試從一次用完就丟的臨時量測，變成**設定進版控、跑得起來、產出一份清單**
的常設能力 —— 而它**不設任何門檻、不擋任何東西**。

⚠️ **它的產出是一份「哪些程式碼被改壞了而測試沒說話」的清單，給人看**；
不是一個會漂移的數字，給閘門比大小。

### ⚠️ 拉 v1 的團隊：升上來不會從綠變紅

| 你的樹上如果…                      | 會發生什麼                                                 |
| ---------------------------------- | ---------------------------------------------------------- |
| 什麼都不做                         | **什麼都不變** —— 沒有新閘門，`gate`／`ready` 一個字都沒改 |
| 跑 `pnpm exec stryker run`         | 拿到一份清單。⚠️ 跑完看一眼 `git diff --summary`（見下）   |
| 有巢狀的 `apps/`／`features/` 目錄 | SAST 的射程會**縮小到頂層那三個** —— 那是它一直宣稱的射程  |

**minor 不是 patch**：新增一個根層檔案、兩個 devDependency 與一組 catalog 條目
（判準見 `v1.3.0`：不是「改了什麼」，是**升上來會不會壞**）。

### 新增

- **`stryker.config.mjs`（根層）—— 突變測試的設定**（#150）。
  `thresholds.break` 是 `null`：`high`／`low` 只決定報表顏色，**沒有任何東西會因為
  分數變紅**。理由不是拖延 —— 分數下降的原因裡至少四種與程式碼品質無關
  （新程式碼讓分母變大、等價變異、結構性量不到的 package、時間相依的 timeout 判定），
  而**一道經常因為無關原因變紅的閘門，結局是被關掉，被關掉的閘門連清單都產不出來。**

  ```
  pnpm exec stryker run                          # 全樹一趟，1 分 29 秒
  pnpm exec stryker run -m 'platform/pii/src/**' # 只看一個 package，秒的量級
  ```

  第一次的基準：**78 支產品檔、6,125 個 mutant、0 errors，total 32.02%／covered 71.90%、
  692 個存活**。每一個非顯而易見的選項旁邊都寫著它的理由。

- **`TESTING.md`** 元層那一節改寫：怎麼跑、基準、三支照不到的 package、以及讀那份清單的
  兩條守則。**`SCOPE.md`** 根層表一列。

### 修正

- **`.semgrep/rules.yml` 的射程改成錨定寫法**（`apps/**` → `/apps/**`，#149）。
  semgrep 已經在警告不錨定的樣式「即將」改成錨定的 —— **射程會在某次升版時安靜改變，
  而規則檔一個字都不用動。**
  ⚠️ 兩種寫法**不等價**（實測，用一組已知位置的 fixture 比對命中清單）：
  不錨定的 `apps/**` 匹配「路徑裡任何一層叫 `apps` 的」，所以 `tools/*/apps/` 也被掃 ——
  而規則自己的註解寫著「tools/ 是建置期腳本，不看」。**那句話在舊寫法下本來就是漏的。**
  `exclude: "**/tests/**"` 不動：`**/` 開頭的樣式不受這個改版影響。

### ⚠️ 兩個已知代價，寫在這裡因為它們是已知的

**其一 —— 跑完會留下九個看不見的檔案模式變更。** `inPlace` 還原時不保留檔案模式，
九支命令列進入點的可執行位會掉，而**逐行 diff 是零**，沒有任何閘門在看檔案模式。
`git checkout -- tools/` 還原得掉。

**其二 —— `ignoreStatic: true` 是安全性的，不是效能的。** 靜態 mutant（模組載入時執行到的）
歸屬不到任何一條測試，所以工具對每一顆都**跑整套測試** —— 這棵樹有 827 顆（14%）。
而整套裡有一支會 spawn 真正的產生器 CLI，它底下的 bingo 會做
`git add -A` ＋ `git commit`，外加**刪掉本機全部的 tag**。
**實測：不開這一條跑一趟全樹，樹上多出一個 `feat: initialized repo ✨` 的 commit
（200 個檔、整棵被注入過的樹），而本機 33 個 tag 全部消失。**
⚠️ 兩者都是**零 diff、零紅燈**的損失 —— tag 不見了要到下一次發版才會發現。

代價：那 827 顆不再被測試，其中 491 顆本來是真的被殺掉的。要收回來的前置條件是先讓那支
測試在暫存目錄裡跑，**不是把旗標關掉**。

### 讀那份清單的兩條守則（都寫在 `TESTING.md` 與設定裡）

1. **存活未必是缺口** —— 等價變異沒有工具判得出來，比例逐 package 不同。
2. **存活未必真的存活** —— 「讓模組載不起來」的變異會被記成存活：工具數的是「有幾條
   測試失敗」，而一支載不起來的測試檔貢獻零條失敗。實測那一顆的 `testsCompleted` 是 17，
   **所以 `testsCompleted === 0` 不是它的過濾器**。

經過見 `DECISIONS.md` 的 **C121** —— 涵蓋範圍、`break: null` 為什麼是裁決
而不是拖延，以及它與 `v1.10.0` 那則「先不設」不是同一個東西。

---

## [1.10.2] — 2026-08-24

**一道 Tier 2 安全閘門在此之前完全靠兩道文字防呆站著，而那是一個字串巧合。**
`tier2-security.yml` 的「SAST 規則的反向測試」把 semgrep 的退出碼丟進 `| tee`，
而 GitHub Actions 的預設 shell 是 `bash -e {0}` —— 有 `-e`，**沒有 `pipefail`**。
管線取的是 `tee` 的退出碼，semgrep 自己的那個被丟掉了，`-e` 永遠不會為它開火。
經過與兩次探針的證據見 #144。

### ⚠️ 拉 v1 的團隊：你的樹上漏著同一個洞

這一步從 `v1.0.0` 就是這個寫法。**升上來會補掉它，不會讓任何既有的東西變紅** ——
`.semgrep/` 的規則與 fixture 一個字都沒改。

### 修正

- **`.github/workflows/tier2-security.yml` 加 `set -o pipefail`。**
  ⚠️ **兩道文字防呆留著，而實測證明它們不是重複**：`pipefail` 守 semgrep
  **回傳非 0** 卻被管線吃掉的那一類；兩道文字防呆守它**回傳 0** 的那一類
  （找不到 fixture 時 semgrep 印一行字然後回傳 0）。缺一不可。

### ⚠️ 兩次探針的對照 —— 這一版真正的產出

同一個退化（拿掉 `tainted-route-input-to-dom-sink` 的兩個 `location.href` sink）、
同一個釘死 sha256 的 semgrep、輸出逐字相同，唯一的變數是那一行：

```
沒有 pipefail：  No tests for fixes found.
                 ✗ 輸出裡找不到通過的測試數 —— 這一步不知道自己驗了什麼
                 ##[error]Process completed with exit code 1.

有   pipefail：  No tests for fixes found.
                 ##[error]Process completed with exit code 1.
```

紅的**位置**不同，而這互相印證兩件事：**semgrep 對「部分測試失敗」回傳非 0**
（探針 ② 沒走到防呆就紅了，而 `-e` 只能為非 0 開火），**所以探針 ① 那次那個非 0
真的被 `| tee` 吃掉了**。

⚠️ 修法之前擋住那次退化的，是防呆 ② 的一個字串巧合 ——
semgrep 印的是 `1 unit tests did not pass`，而 `[0-9]+/[0-9]+: .*tests passed`
要的是字面「tests **passed**」。**它今天是對的，沒有任何東西保證明天還是對的**：
升版是一個看起來無害的 sha256 改動，而訊息字串不在任何人的 review 視野裡。

---

## [1.10.1] — 2026-08-24

**`TESTING.md` §六 元層那一格的第一批 —— 而它是三支測試，不是一個工具。**
那一格的原話是「把既有的**手工反向測試**擴大到 `platform/*` 的產品碼」。
這一版補三處斷言，**產品碼一行都沒改**：缺的從頭到尾是斷言，不是實作。

⚠️ **三處都是機械跑一遍突變測試掉出來的，不是坐著想出來的**（#136 的全量量測）。
機械化本身（設定放哪、跑在哪、門檻怎麼定）**不在這一版**，見 #133。

### ⚠️ 升上來不會從綠變紅

| 你的樹上如果…                      | 會發生什麼                         |
| ---------------------------------- | ---------------------------------- |
| 沒有動過 `platform/pii` 的隱碼函式 | **什麼都不變**。產品碼一行都沒改   |
| 動過，而且動掉了 `<=` 那兩個邊界   | 會紅 —— 而那正是這一版補起來的洞   |
| 想知道這三條在守什麼               | 每一條的註解裡寫著它要殺的那個變異 |

**這一版是 patch 不是 minor**（判準見 `v1.3.0`：不是「改了什麼」，是**升上來會不會壞**）：
沒有新增任何能力、沒有 API 變更、沒有新閘門。

### 修正

- **`platform/pii` 的兩個邊界在此之前沒有測試站在上面，而錯的樣子是「完全不遮」。**
  `maskEmail` 的 `at <= 0` 寫成 `at < 0` 的話，`"@example.com"` **原樣輸出**；
  `maskNationalId` 的 `length <= 4` 寫成 `< 4` 的話，`"A123"` **原樣輸出**。
  非邊界的輸入（`wang@example.com`、`A100000000`）在兩種寫法下**完全等價** ——
  只有站在邊界上的輸入看得見差別。
  ⚠️ 這支檔案的覆蓋率是**四維 100%**（#130）。覆蓋率的門檻擋不下這一類。
- **`tools/spec-report` 的完成率百分比在此之前沒有任何斷言。**
  唯一相關的是 `toContain("完成率 3/4")` —— 只驗分數形式，括號裡的百分比一個字
  都沒驗，於是 `(完成 / total) * 100` 改成 `/ 100`（`75.0%` → `0.0%`）測試全綠。
  那是這份報表最外層、最多人只看那一眼的數字（`v1.7.0` 交付）。
- **`apps/console` 的絆線掛錯了對象。** 驗 `extraPermissions` 的測試是
  `for (const permission of extraPermissions)` —— **空陣列時迴圈跑 0 次而通過**。
  它守的是「宣告的都有人用」，不是「該宣告的都宣告了」，而後者才是那個常數
  存在的理由（少了它，取消那條路由在本機永遠 403）。新的斷言不比對寫死的字串，
  而是拿 **403 回應自己說的那個權限碼**去對，順帶守住被清空的 `body`。

### 文件

- **`TESTING.md`** 三處：§二 那一格不再說「無」；§六 那一格標明第一批已補、
  **是測試不是工具**；元層那一節加上這三處的出處表。

### ⚠️ 一個不要重新發現的東西

`{ granularity: "grapheme" }` → `{}` 看起來像缺口，其實是**等價變異** ——
`granularity` 預設就是 `"grapheme"`，而 `mask.test.ts` 本來就有一個
`describe("字素叢集：不能用碼點拆")`。**存活的 mutant 是缺口的上界，不是缺口**，
而等價變異沒有工具判得出來 —— 那一條對「門檻設多少」有直接影響（#133）。

---

## [1.10.0] — 2026-08-24

**層 2 的覆蓋率下限落地了一半 —— 而落地的那一半量的不是「測試寫夠了沒」。**
`TESTING.md` §六 的缺口表，「層 2 覆蓋率下限」那一格判過是**分裂**的（C117 §五）：
第二類（`features/*`、`apps/*`）落這條線，第一類（`platform/*`、`tools/*`）
送 `main`。這一版做第二類那半，第一類那半寫成裁決（切法 ＋ 數字 ＋ 理由），
實作等 #86 解凍。經過見 `DECISIONS.md` 的 **C120**。

門檻**只收在 `src/usecases/` 那一層**，四個維度都是 100。它問的問題是：
規格打的是 usecase（C114），所以**一行沒有被走過的 usecase，就是一個沒有規格
在驗的 usecase**。切片整體**不設數字** —— `src/views/**` 佔行分母的 40%，
而 `.vue` 只算 `<script setup>`（template 一行都不進分母），一個套在整包上的
數字會被那件事帶著走，不是被測試的多寡帶著走。

### ⚠️ 升上來不會從綠變紅，但**新產的切片**多守一條線

| 你的樹上如果…                       | 會發生什麼                                          |
| ----------------------------------- | --------------------------------------------------- |
| 既有的切片（沒有 `vite.config.ts`） | **什麼都不變**。這一版沒有動 `REQUIRED_FILES`       |
| 之後用 `vp create slice` 產的切片   | `vp test` 會順便跑覆蓋率，usecase 那層不滿 100 就紅 |
| 想把門檻也套到既有切片              | 把 `features/order/vite.config.ts` 抄過去即可       |

**這一版算 minor 不是 patch**（判準見 `v1.3.0`：不是「改了什麼」，是**升上來
會不會壞**）：它加了能力、沒有讓任何既有的東西變紅。

⚠️ **代價是明知而選的**：先寫 usecase、後寫規格的那段時間它必定紅。照〈校準〉
那三句話，那不是缺陷 —— **記一則裁決，收緊（或放寬）時拿它當論證。**

### 新增

- **切片自帶一支 `vite.config.ts`**，由 `slice-gen` 產出。裡面只有覆蓋率設定：
  射程 `src/**`、門檻掛在 `src/usecases/**`（glob 與數字都從
  `@org/slice-kit/contract` 取，不是字面值）、以及 **`enabled: true`**。
- **`@org/slice-kit/contract` 多兩個 export**：`USECASE_COVERAGE_MIN`（100）與
  `USECASE_COVERAGE_GLOB`。相容變更，`API.md` 已同步（158 個 export）。
- **`tools/slice-gen/tests/coverage-gate.test.ts`** —— 兩半的反向測試：靜態那半
  驗產生器真的產出那份設定、glob 不是空砲；執行那半把**產生器產出的原文**
  丟進一個暫存專案真的跑一次，只走一支分支 → 紅，兩支都走 → 綠。
- **`features/order`／`features/shipment` 各多一支 `vite.config.ts`**（同一份設定）。
- **catalog 多一條 `@vitest/coverage-v8: 4.1.10`** —— ⚠️ 與 `vitest` 同一句話：
  版本要逐位相同，用 `^` 範圍會裝出一份與 runner 版本不同的 provider，而症狀
  不是報錯，是數字看起來像那麼一回事。

### 修正

- **`apps/console` 的覆蓋率射程在此之前是錯的，而錯的樣子是滿分。**
  它有自己的 `vite.config.ts`，於是根層那份的 `test` 區塊**整塊不繼承**，
  覆蓋率退回 v8 的預設射程（只有測試載入過的檔案進分母）—— 報表寫 **100%**，
  一個 99% 的門檻照樣通過、exit 0。射程校正後是 13.20%。
  ⚠️ **這一格刻意不設門檻**：它 75% 的分母來自兩支「被編譯過、沒被執行過」的
  檔案，把線畫在量測產物上，一年後沒有人答得出「為什麼是這個數字」。
- **切片的 `test` 從 `package.json` 的 scripts 搬進 `vite.config.ts` 的 `run.tasks`。**
  ⚠️ **不搬的話那支 task 永遠不會 cache**：覆蓋率一開，v8 provider 每跑一次都
  讀寫 `coverage/.tmp/`，而 `vp run` 的自動追蹤看到「讀了自己寫的檔案」就判定
  不可快取 —— **而那會發生在每一個切片上**，任務快取是 Tier 1 的主要提速手段
  （D10）。換 `reportsDirectory` 沒有用（package 底下的 `node_modules/`、根層的
  `node_modules/.vite/` 都實測過），只有指到 repo 之外才會回來。
  處置是 `input: [{ auto: true }, "!coverage/**"]` ＋ `output: ["coverage/**"]`。
  ⚠️ 代價：切片目錄裡 `pnpm test` 不再有東西可跑，走 `vp run <pkg>#test`
  或 `vp run -r test`（產生器印出的後續步驟本來就是前者）。

- **`tools/slice-gen/src/files.ts` 的 per-file 放行 841 → 850。**
  ⚠️ **C119 那條唯一的放行第一次開火了。** 處置留在樹上：那份 `vite.config.ts`
  模板一個 `options` 的值都不用，所以被提到 `buildSliceFiles` 外面當 module 層
  常數，函式只長 8 行而不是 47 行。**這條線每被推高一次就是一次要寫下來的帳。**

### ⚠️ 兩個「不會報錯」的坑，記在這裡

- **設定放根層對「有自己 `vite.config.ts` 的 package」直接失效**，而失效的樣子
  是全綠。所以覆蓋率設定是**逐 package** 的，切片那一份由模板生成 ——
  形狀與 C111（規格 runner 的相依必須列在切片自己的相依裡）同一條。
- **覆蓋率門檻的 glob 命中零個檔案時靜默通過、exit 0**，與打錯字完全同形。
  這棵樹上正是這個狀態（兩支示範切片沒有 usecase 層），所以那條線靠**產生器
  輸出上的絆線**證明自己，不是靠 CI 的 exit 0。

### ⚠️ 量切片的覆蓋率要走 `vp run <pkg>#test`，不是 `vp test --root <pkg>`

兩者的 cwd 不同：`--root` 的 cwd 仍是 monorepo root，於是切片接線檔的
`loadFeature` 找不到 `.feature`，產生器輸出的 20 條測試只跑得到 5 條 ——
**usecase 那一層在那條路徑上讀出來是 0%**。`ready` 走的是前者，所以量測方法
與門檻執行走同一條路。

---

## [1.9.0] — 2026-08-23

**層 1 的複雜度那一格補上了，而它落在 Tier 1，不是安全閘門那一軌。**
`TESTING.md` §三 從第一版就寫著「缺的是複雜度那一格」，理由是「agent 會在
自己製造的糾結程式碼裡繞不出來，最後得人下場解」。這一版把它變成規則。

⚠️ **票上那個「真正的開放問題」——「複雜度塞進 Tier 2 安全閘門會不會稀釋
它的身分」——答案是不必塞。** 四個維度裡三個加一個替代品**已經在 oxlint 裡**，
而 oxlint 正是 `TESTING.md` 說的層 1。連帶：「與 oxlint 零重疊」那句話不用
重驗、紅燈分得出「有 XSS」與「函式太長」、`scripts.gate` 一個字都沒改。
經過見 `DECISIONS.md` 的 **C119**。

### ⚠️ 升上來可能從綠變紅 —— 先看這一段

| 你的樹上如果…                                    | 現在會紅                               | 怎麼辦                                                                 |
| ------------------------------------------------ | -------------------------------------- | ---------------------------------------------------------------------- |
| 有函式超過 **185 行**（測試碼 **455**）          | `max-lines-per-function`（`vp check`） | 拆函式；真的拆不動就在 `vite.config.ts` 加一條 per-file 放行並寫下理由 |
| 有 block 巢狀超過 **5 層**（測試碼 **3**）       | `max-depth`                            | 提早 return，或把內層抽成函式                                          |
| 有函式參數超過 **6 個**（測試碼 **4**）          | `max-params`                           | 收成一個具名的 options 物件                                            |
| 有函式循環複雜度超過 **36**（測試碼 **11**）     | `complexity`                           | 拆分支；查表法通常一次砍掉一半                                         |
| 有元件 props 超過 **5 個**（測試 fixture **2**） | `vue/max-props`                        | 拆元件，或把相關的 props 收成一個物件                                  |

**這是這一版算 minor 不是 patch 的理由**（判準見 `v1.3.0`：不是「改了什麼」，
是**升上來會不會壞**）。

⚠️ **而這些數字是從腳手架這棵樹量出來的，不是從你的樹。** 「不擋任何既有
程式碼」這句承諾的射程只到這個 repo —— 它在這裡實測零違規，對你的
`features/*` 則是別人家樹的形狀。第一次撞到就照〈校準〉那三句話走：
**記一則裁決，收緊時拿它當論證。**

### 新增

- **根層 `vite.config.ts` 的 `lint:` 區塊多五條規則**，產品碼與測試碼
  **兩組不同的數字**（`**/tests/**`、`**/*.test.*`、`**/*.spec.*`、
  `**/fixtures/**` 走測試碼那組）。

  ⚠️ **兩類分開不是為了寬鬆。** 測試碼在巢狀深度與循環複雜度上比產品碼
  **乾淨得多**（89% 的測試函式 depth 0），卻在函式大小上有一條產品碼沒有的
  長尾（`describe`／`it` 的 callback，最大 455 行）。同一組數字會在一個維度
  太鬆、另一個維度把人卡死。

- **一個 per-file 放行**：`tools/slice-gen/src/files.ts` 的 `buildSliceFiles`
  841 行（整個檔案的 98%，主體是回傳切片模板字串的一個 `return`）。
  ⚠️ 不把全域門檻抬到 841 的理由：那是次高值 185 的 4.5 倍，抬上去等於這條
  規則對其餘所有產品碼形同不存在。**那一行是債不是豁免。**

### ⚠️ 認知複雜度那一維是空的，而空著比補起來好

oxlint 沒有這條規則（三種寫法逐一用 `--print-config` 驗過）。唯一的來源是
`eslint-plugin-sonarjs`，而那要嘛把一條相依加進 Tier 2 安全閘門 —— 正是這一版
說不必做的事 —— 要嘛為了一個維度另養一軌。

⚠️ **`complexity` 是循環複雜度，是替代不是填滿。** `buildSliceFiles` 841 行、
**認知複雜度 0**：四個維度不互為代理，而用循環複雜度當認知複雜度的代理，
漏掉的正是那一類。

### 測試

反向測試四個方向，全部實測：

| 做什麼                      | 期待             | 結果                                                |
| --------------------------- | ---------------- | --------------------------------------------------- |
| 產品碼 `max-depth` 5 → 4    | 紅               | ✅ `doc-facts/src/facts.ts` 兩處，`vp check` exit 1 |
| 測試碼 `complexity` 11 → 10 | **只有測試碼**紅 | ✅ `platform/ui/tests/styles.test.ts`               |
| 拿掉 `slice-gen` 的放行     | 紅               | ✅ `files.ts:18` 841 行                             |
| 還原                        | 全綠             | ✅ 五條規則零違規，`Found 0 errors`                 |

⚠️ **最後一列不是裝飾** —— 前三列證明規則會說話，只有它證明此刻不擋任何東西。

⚠️ **`slice-gen` 的「開箱即綠」另外驗過一次。** 樹上零違規不等於**新產生的**
切片零違規 —— 那些檔不在版控裡。走真正的入口產一片，正式門檻下複雜度零違規；
而那個零自己也有對照組（門檻壓到 0／1 重跑，命中 36 條，證明規則確實在看）。
新切片的實際最大值是函式 72 行、depth 1。

⚠️ **量測用的述詞與設定裡的 glob 是兩份手抄本，已經機械化對過**：把測試碼
那條 override 壓到 1、產品碼抬到 99999，回報的檔案集合就是 glob 判定的測試
集合 —— 各 53 個檔，雙向差集為空。**glob 打錯不會報錯**（oxlint 靜默 exit 0），
症狀是測試碼安靜地套用產品碼門檻，而 `max-lines-per-function` 是唯一一個
產品碼比測試碼**緊**的維度。

### ⚠️ `.vue` 的盲點原樣存在

`UiDropdownMenu.vue`（script 275 行）、`UiDatePicker.vue`（186 行）在這五條
規則下**零違規** —— `<script setup>` 的 module 層不在任何函式裡，四個維度
只有 `max-depth` 看得見它。這不是 oxlint 特有的，ESLint 量出來一模一樣。
`vue/max-props` 補回「參數個數」那一格；區塊行數那一格 oxlint 沒有對應規則，
仍然空著。

---

## [1.8.0] — 2026-08-23

**十三道閘門在守五條承諾，而承諾與閘門對不對得上，只有人讀得出來。**
那句話是 `HANDOFF.md` 自己寫的（「這是這份文件最脆弱的一格」）。這一版把
承諾一寫成**人逐字讀的中文規格**，並且讓一道閘門**照著那份規格去執行**。

⚠️ **選的不是「寫一份規格描述閘門」** —— 那就是閘門的第二份手抄本，而
`SCOPE.md` 開頭警告的正是它（`v1.0.3` 付過代價）。選的是**閘門讀規格**：
`tools/promise-check` 照著「假設」那一句把一份切片副本弄壞、跑「當」指名的
閘門、比對「那麼」說的結果與訊息片段。經過見 `DECISIONS.md` 的 **C118**。

### ✅ 升上來不會從綠變紅 —— 但 `vpr gate` 會多一道閘門

`platform/*` 零變更，`surface.json`／`API.md` 零變更，相依只多一個 workspace
套件。**新增能力 → minor。** 升上來要知道的三件事：

1. **`vpr gate` 與 Tier 2 各多一步**（`node tools/promise-check/src/cli.ts`）。
   本機實測 0.7 秒：四個場景各建一份切片副本、各跑一次 `tools/conformance`。
2. ⚠️ **它要版控裡至少兩片切片當素材。** 少於兩片時它會紅，訊息說得出原因 ——
   承諾說的是「切片之間」的邊界，一片證明不了它。素材取的是 `git ls-files`
   排序後的前兩片，**不是寫死的 `order`／`shipment`**：fork 的樹上示範切片
   早就被換掉了，而那不是誰做錯了什麼。
3. **根層多了 `specs/`**，`CODEOWNERS` 多了 `/specs/` 一列。

⚠️ **repo 的原始碼一個位元組都不會被動到** —— 破壞發生在暫存目錄裡的副本上，
形狀沿用 `tools/conformance/tests/negative.test.ts`（改副本，不改 repo）。

### 新增

- **`specs/promise-1-architecture.feature`** —— 承諾一的原文，中文 Gherkin。
  打開它就知道「分工開發不受影響」具體指哪幾件事，**不必讀任何一支閘門的原始碼**。
- **`tools/promise-check`** —— 讀那份規格並執行它。接在 `scripts.gate` 與 Tier 2。
- **`AGENTS.md` 第 4 條的射程寫明涵蓋根層 `specs/`**。⚠️ 把一句「必須紅」
  改成「必須綠」是一行**合法而且全綠**的 diff —— 它把一條保證拿掉了。

### ⚠️ 三個方向都會紅，而第二個方向才是重點

「把承諾改成假的 → 紅」只證明它讀了那句話。**最常見的失效模式不是說謊，
是沒被讀到**：規格檔改名、場景被刪、「假設」句改寫之後接不上 ——
三種的共同症狀是**閘門全綠而承諾一條都沒驗**。

| 改壞規格的方式                 | 紅在哪一條     |
| ------------------------------ | -------------- |
| 「必須紅」改成「必須綠」       | 承諾誤擋       |
| 訊息片段改成不會出現的字       | 紅在別的地方   |
| 「假設」那一句改寫             | 沒有接線＋孤兒 |
| 整個場景刪掉                   | 孤兒接線       |
| 規格檔改名或刪掉               | 規格不見了     |
| 承諾綁到沒接在 `gate` 上的閘門 | 閘門沒有接上   |

⚠️ **最後一列是這一版買到的東西裡最實在的一塊。** 一道閘門存在、跑得動，
不代表它會被執行 —— `tools/spec-report` 就是現成的反例（它接在 `vpr ready`
的最後一步，**不在**閘門鏈上）。承諾綁到它，那條承諾在 CI 上是空的，
而「這支工具存在嗎」這種靜態比對看不出差別。

⚠️ 還有第三個方向：規格裡**必須有一條期待綠燈的對照組**，而且它**先跑**。
沙盒建壞掉的話每一條「必須紅」都會成功變紅 —— 那時閘門全綠，
而原因是環境壞了。

### 為什麼只有一條承諾

**不是估錯。** 九支閘門裡**只有 `tools/conformance` 能在一份副本上跑**（`--root`），
而那是這種驗法的前提：`theme-verify` 要真的建置兩次、`scope-check` 的事實來源是
`git ls-files`（副本不是 git repo）、`doc-facts` 吃的是整份 README／HANDOFF、
`eslint`／`.semgrep` 根本不是 `tools/*/src/cli.ts` 的形狀。

所以擴張的下一步**不是再寫四份規格**，是讓更多閘門能在一份副本上跑。
逐支的形狀與四條承諾的處置寫在 C118 §五。

### 測試

`promise-check` **24**（新）：解析層 10、反向 11、CLI 與分界 3。
變異逐一造過，**四條全部非零**：

| 變異                        | 閘門說了什麼 |
| --------------------------- | ------------ |
| 跨切片的破壞手法改成 noop   | 承諾沒有牙齒 |
| view 的破壞手法改成 noop    | 承諾沒有牙齒 |
| 幽靈依賴的破壞手法改成 noop | 承諾沒有牙齒 |
| 對照組改成真的弄壞一個檔    | 承諾誤擋     |

⚠️ **分界有測試守著**：`promise-check` 讀 `specs/*.feature`、`spec-report` 讀
`features/*/specs/*.feature`，兩者刻意不重疊 —— 混在一起的話，框架承諾會被
算進那份拿去對外報進度的**業務功能完成率**。那條測試**真的跑一次**
`spec-report --check`，不是比對兩個抄下來的 glob。

---

## [1.7.0] — 2026-08-23

**十二道閘門全綠，而沒有一道在回答「這個功能做完了沒有」。** 這一版補的是那一格：
專案組用中文寫下「這個功能叫做對是什麼樣子」，跑起來，**綠幾條就是完成幾條**。

覆蓋率量的是程式碼被跑過，回答不了前面那個問題。能回答的是驗收規格，
而這一版把那一層的設施、範本、與報表一次交付（`DECISIONS.md` 的 C114、C115）。

### ✅ 升上來不會從綠變紅 —— 但有三件事會變

`platform/*` 只有 `slice-kit/src/contract.ts` **新增** 78 行（五個常數），
零刪除零改名；`surface.json` **151 → 156**、進入點 **10 不變**、元件 **27 不變**。
所以**不是 major，而那一格照樣是閘門判的**。

新增能力 → **minor**。升上來要知道的三件事：

1. **根層 `ready` 多了一段。** `vp run -r test` 現在帶
   `--reporter=default --reporter=json --outputFile=.vitest-results.json`，
   而 `vpr gate` 之後多跑一次 `spec-report --check`。
   ⚠️ **某個 package 的 `test` 不是 vitest 的話，那兩個參數會被原樣傳進去** ——
   這是唯一一個會讓乾淨的樹升上來變紅的路徑。出路是把那個 package 的參數擋掉，
   或改回逐個 package 指定。
2. **根層多一個 `SPEC-REPORT.md`**（已登記在 `SCOPE.md` 的根層表）。
   它是產出物，`--check` 守著它與現況一致。
3. **`vp create slice` 產出的切片形狀變了**（多五個檔案）。**既有切片不受影響** ——
   `REQUIRED_FILES` 一個字沒動，所以 `order`／`shipment` 沒有規格也是綠的。

相依新增一筆：`@amiceli/vitest-cucumber@^7.0.0`（catalog）。⚠️ 純 JS、
沒有平台二進位、沒有 postinstall 下載 —— 與 C48 收 `happy-dom` 時同一條判準。

### 新增

- **`tools/spec-report`（第九支工具）** —— 業務功能完成率 ＝ 驗收規格的通過率。
  CLI 答「現在怎麼辦」（只印需要行動的），`SPEC-REPORT.md` 答「做到哪了」
  （完整台帳、進版控）。對照鍵 `<切片>/<功能>#<場景>[<例子>]` 把兩者接起來。
- **`slice-gen` 產出驗收規格的整條線** —— `specs/<切片>.feature`（需求，人寫的）、
  `src/ports.ts`、`src/usecases/`（純 TS 業務規則）、`tests/specs/<切片>.spec.ts`
  （接線）、`tests/support/in-memory-gateway.ts`。**產出的切片開箱即綠**
  （20 條測試，其中 15 條來自規格）。
- **`AGENTS.md` 的契約多一條**：agent 不得修改 `specs/` 底下的檔案，
  含不得自己加上 `@待辦`。⚠️ **這一條沒有機制在守**，擋它的只有那句話與人讀規格的 diff。
- **`platform/slice-kit/contract.ts`** 新增 `SPECS_DIR`／`USECASES_DIR`／
  `STEPS_GLOB`／`USECASE_FORBIDDEN_IMPORTS`／`TODO_TAG`。
- Tier 1 多一步「驗收規格完成率」，**在測試與建置之後**（結果檔跨 workflow 拿不到）。

### 三個實測推翻的假設

- ⚠️ **接線檔的副檔名 `.spec.ts` 不是可以換的。** vitest 的預設 include 只收
  `*.test.*` 與 `*.spec.*`，而這條線的根層沒有覆寫 `test.include`。草稿原本寫的
  `.steps.ts` 會讓整份規格**一條都不被收集** —— runner 靜默不跑、既有測試繼續
  全綠、完成率讀的是一個從來沒有被執行過的檔案。
- ⚠️ **三態不夠用。** 「有定義還沒做／該做了沒綠／完成」少了一格：
  **「規格解析得出來，但結果裡找不到它跑過」**。它的症狀與全綠一模一樣，
  而上面那個缺陷正好產生它。第四態「未執行」**擋**。
- ⚠️ **`--reporter=json` 會取代主控台輸出，不是加上去。** 一條紅測試在畫面上只剩
  `vp run: N failed`，連測試名都沒有。接 CI 的第一版就是這樣寫的，
  而 `vpr ready` exit=0 抓不到它 —— 一次全綠的執行沒有失敗輸出可以弄丟。

### 修正

- **`tools/conformance/src/cli.ts` 的檔案模式**從 `100755` 還原成 `100644`。
  ⚠️ 它是 `v1.6.1..HEAD` 之間一個非預期的改動，diff 是 **0 行**，
  **沒有任何閘門在看檔案模式**。C99 記過同一件事，這是第三次（C116）。

### 測試

`slice-gen` **81 → 96**（新增規格設施的斷言 ＋ 一支用真 parser 跑模板規格的測試），
`spec-report` **28**（新）。變異逐一造過，全部非零 —— C114 十一條、C115 十條（表在 `DECISIONS.md`）。

⚠️ **報表現在是空的**（既有兩個切片刻意沒有規格），所以 `--check` 目前恆綠。
**第一個真的寫規格的切片出現之前，這條閘門證明不了自己。**

---

## [1.6.1] — 2026-08-23

**閘門會對一個登記過的目錄報「沒登記」，只要那個目錄底下有中文檔名。**

`git ls-files` 對含非 ASCII 的路徑會加引號並做八進位轉義，於是 `scope-check`
讀到的第一段變成 `` `"features/ `` 而不是 `features/`。⚠️ **那種紅燈沒有合法
出口** —— 照訊息補一列 `"features/` 也不會綠。

### ✅ 升上來不會壞 —— 這一版是 patch

這條線目前的樹上**沒有任何非 ASCII 檔名**，所以三處的輸出一字不變；改動買到的
是「將來放進中文檔名時不會誤報」。`platform/*` 的型別形狀零變更，
`surface.json`／`API.md`／相依與 lockfile 零變更。

### 修正

- **`tools/scope-check/src/tree.ts`** 的兩個讀取點（`trackedDirectories`、
  `trackedRootEntries`）改用 `git ls-files -z` ＋ `split("\0")`。
- **`tools/doc-facts/src/derive.ts`** 的 `uiComponentCount` 同上 ——
  不改的話 `.vue` 的計數會漏掉中文檔名的元件。

⚠️ **為什麼是 `-z` 而不是 `core.quotepath=false`**：兩個都能關掉八進位轉義，
但後者對含引號或換行的檔名仍會加引號。`-z` 用 NUL 分隔、完全不轉義，
是唯一不用猜的形式。⚠️ 用了 `-z` 就**不要再 `.trim()`**，那會弄壞前後帶空白
的檔名 —— 三處一併拿掉。

### ⚠️ 這個修法在這個 repo 早就有先例，只是沒有傳播

`tools/api-surface/src/tracked.ts` **本來就寫著 `-z`**，所以一直有一支工具是
安全的 —— 但它的檔頭沒有寫下為什麼（整段在論證「為什麼是 `ls-files` 而不是
`readdirSync`」，一個字都沒提轉義）。

> **一個正確的作法如果沒有寫下理由，它就只是那一支工具的習慣，不會變成
> 這條線的作法。**

所以這次除了改程式碼，也把理由寫進 `tree.ts` 的檔頭。完整經過見 **C112**。

### ⚠️ 順帶修掉 `v1.6.0` 帶出去的格式問題

`v1.6.0` 發布時 **`vp check` 是紅的**（五個 markdown 檔的表格未對齊），而
`.github/workflows/tier1-quality.yml` 的第一步就是它 —— **Tier 1 CI 在那個
commit 上是失敗的**。

原因是驗證時跑了 `vpr gate` 七道、`eslint`、`vp run -r test`，**唯獨漏了
`vp check`**。`vpr ready` 是四件事：`vp check` ＋ 全套測試 ＋ 建置 ＋ `vpr gate`。

> **「七道閘門全綠」不等於「`ready` 全綠」。** 逐項跑閘門的時候，最容易漏掉
> 的正是不在 `tools/` 底下的那一項 —— 因為它不在那張表上。

本版一併 `vp check --fix`：改動全部是表格分隔線的長度與一個尾部空行，
`git diff -w` 確認**零語意改動**。

### 反向測試

`tools/scope-check/tests/scope.test.ts` 新增〈非 ASCII 的路徑〉兩條。
⚠️ **兩條都實測過「拿掉 `-z` 就會紅」**，還原後全過 —— 絆線掛在被守的對象上。

---

## [1.6.0] — 2026-08-23

**閘門全綠，而沒有一道在守「這東西做對了事」。** 這一版補的不是閘門，
是**座標** —— 一份把既有機制排進測試層級的文件，讓「還缺什麼」變成可以
回答的問題，而不是一種感覺。

### ✅ 升上來不會壞 —— 這一版是 minor

**只新增文件，一支工具、一條規則、一個門檻都沒有動。** `platform/*` 的型別
形狀零變更，`surface.json`、`API.md`、相依與 lockfile 零變更。新增的
`TESTING.md` 登記進 `SCOPE.md` 之後，`vpr gate` 七道全綠、`vp run -r test`
全數通過。

⚠️ **`scope-check` 的紅燈是實測過的，不是假設**：把 `TESTING.md` 那一列從
`SCOPE.md` 拿掉，它確實紅並指名該補哪一格 —— 這道閘門對新檔案真的會說話。

### 新增

- **`TESTING.md` —— v1 的測試層級模型。** 借 Robert C. Martin 2026 年 6–7 月
  在 X 描述的五層流水線（約束／單元／驗收／QA／週期性手動）加突變元層當座標，
  把既有機制排進去。

  ⚠️ **借的是層次，不是數值。** 他在同一批討論裡明說那些門檻出自他自己幾十年
  的判斷力，照抄數字是「性質完全不同的賭注」—— 所以本檔**一個門檻值都不定**。

### 這份文件掉出來的三件事

- **真正的缺口只有一個：層 3 驗收規格。** 它是五層裡唯一在回答「什麼叫做對」
  的一層，而 v1 完全沒有。既有的契約測試斷言的是**機制有沒有生效**，不是
  **這門生意認為什麼叫正確**，且沒有一份是人打算逐字讀的。
  ⚠️ **這一層缺席，正是「閘門全綠仍然不能讓人相信」的直接原因。**

- **元層不是空的，是手工的。** `.semgrep/rules.ts` 是故意寫壞的 fixture；
  `tools/api-surface/tests/negative.test.ts` 自稱反向測試、用兩種破壞法斷言
  閘門會紅；`tools/vue-typecheck/tests/negative.test.ts` 檔頭寫明「先讓
  fixture 壞掉，再證明這支工具會說話」。**刻意注入錯誤看閘門會不會紅，
  就是突變測試。**

- **依賴方向早就做完了。** 那組座標把「分層邊界」當成要新增的閘門；
  `tools/conformance` 不但有，而且更細（相對路徑逃逸、幽靈依賴都在管）。

### 變更

- **`SCOPE.md`〈兩份清單，兩條軸〉→〈三份清單，三條軸〉。** 加了第三份，
  那句標題當場變成假的 —— 這正是這個 repo 在 `v1.0.3` 付過代價的病。
  `TESTING.md` 同時登記進根層表，由 `tools/scope-check` 守著它的**存在**。

  ⚠️ **只守存在，不守內容。** `doc-facts` 的 `GUARDED` 仍然只有 `README`
  與 `HANDOFF`；`TESTING.md` 因此寫成「就算沒有守衛也不會腐爛成假數字」的
  形狀（全列名字、零導出數字），與 `SCOPE.md` 同一個作法。

### 兩類測試 —— 這一版最後才定下來的組織原則

**五層是借來的座標軸，兩類是這個腳手架的身分。**

|              | **第一類 · 框架測試**        | **第二類 · 交付的測試設施**                    |
| ------------ | ---------------------------- | ---------------------------------------------- |
| 誰維護       | 腳手架團隊                   | 腳手架團隊建，**專案組使用**                   |
| 守什麼       | 腳手架自身沒有壞             | 專案組交付的業務是對的                         |
| 成功長什麼樣 | 升級平台不會打斷任何一個案子 | **專案成員只要補 TDD，就量得出業務功能完成率** |

⚠️ **借來的座標整個只描述第一類。** 它在回答「一個團隊怎麼在不讀 diff 的前提下
相信自己的程式碼」，沒有在回答「我做的東西要交給一百個團隊用」。

⚠️ **而第二類幾乎整欄是空的** —— 只有 `slice-gen` 產生的測試骨架和 Tier 2 的
前置過濾，沒有 TDD 指引、沒有完成率的量法。**不安的來源可能不是第一類不夠好，
是第二類從來沒有被設計過。**

### ⚠️ 刪除裁決已反轉，`v2.0.0` 取消

初版裁決要在 `v2.0.0` 移除 `api-surface`、`codemods`、`slice-gen` 與 Tier 2
整條。用兩類的框架重量，**那四項全部是第二類**，裁決全數改判「留」——
判準從「對不對應五層」換成「它服務第一類還是第二類」。經過見 **C109**。

⚠️ **`slice-gen` 那一項最值得記**：「專案成員只需要補 TDD」這句話，目前唯一的
實現就是它產生的測試骨架（`files.ts` 註解：「沒有測試的切片＝沒有人能安全
重構的切片」）。**刪掉它等於把第二類的入口拆了。**

### 完成率設施的最小實測（C110）

動 `slice-gen` 模板之前先驗 —— 模板是第二類的入口，改壞了每個新切片都跟著壞。
中文關鍵字、中文步驟、場景大綱按例子列展開、背景資料表、`@待辦` 標籤過濾，
以及「該做了卻沒綠就紅」的反向測試，**全部成立**。

⚠️ **而草稿的三個假設有兩個是阻斷級的**：`.feature` 的 `# language:` 標頭
**本身不生效**（只寫標頭，parser 解析不出 Feature）；標了 `@待辦` 的場景
**不能沒有接線**（runner 會噴 `ScenarioNotCalledError`，正解是 `excludeTags`）。
兩件事靠同一行設定解決，而**那一行必須由 `slice-gen` 模板生成**。

⚠️ 這與 C108 是同一個形狀 —— 一個照方法來源的風險清單抄、一個照套件的常識
假設寫，**兩次都在文件即將定案前才驗，而兩次都驗出阻斷級的錯誤**。通則寫在
C110 §五：**一份會被別人照著做的文件，裡面每一個「照著做就會動」的斷言，
都要有一次真的跑過。**

### 三件前置驗完，其中一件撞出既有缺陷（C111）

`vp run <pkg>#test` **撿得到** `tests/specs/*.spec.ts`；`conformance` **接受**
`features/<切片>/specs/` 與 `src/usecases/` 兩個新目錄（反向測試證明它確實在看
新層）；`scope-check` 對新工具目錄照常紅、登記後綠。

⚠️ **兩個給 `slice-gen` 模板的硬需求**：`loadFeature` 的路徑是 **package 相對**
（`vp run <pkg>#test` 的 cwd 是 package 目錄）；runner 要列進**切片自己的**
`devDependencies` —— 幽靈依賴檢查只掃 `src/`，接線檔在 `tests/` 底下溜得過去，
而那種相依在乾淨重建時不成立。

### ⚠️ 撞出來的既有缺陷：`git ls-files` 會引號化非 ASCII 路徑

驗證過程中出現一個看不懂的違規：`` `"features/ `` —— 帶著一個引號。原因是
**git 對含非 ASCII 的路徑會加引號並八進位轉義**，於是第一段變成 `"features/`，
與表上的 `features/` 對不起來，**誤報違規**。

⚠️ **這與規格設施無關，是現存缺陷** —— 今天沒發作只因為樹上還沒有中文檔名的
檔案，**而這條線的規格檔會是中文的**。呼叫點在 `scope-check`（兩處）、
`api-surface`、`doc-facts`，三支都要修。

⚠️ **修法刻意不進這一版** —— 本版宣稱「只新增文件，一支工具都沒有動」，
把程式碼修進來那句話當場變假。**修在 `v1.6.1`。**

### 新增：`AGENTS.md` 的 agent 契約

標記之外新增一節，內容是**下發給採用團隊的三條規則**：任務結束前 `vpr ready`
全綠、**不得修改設定或門檻來達成綠燈**、「完成」的定義在驗收規格裡不在 diff 裡。

⚠️ **C93 撤回過標記外的手寫內容**，所以這次先履行它要求的舉證：受益者是拉 v1
的團隊 —— 這一節寫的是**他們的 agent** 在**他們的樹上**要遵守的規則，
它擋下的是「專案組的 agent 把門檻改掉讓測試變綠」。論證與 `#94` 的分界
寫在 C109 §七。

⚠️ **其中一個前置是阻斷級的**：刪除會把「元層非空」挖空，所以刪除的同時
必須把機械化突變測試升回必要 —— **跑不起來的話，刪除的前提就不成立。**

### 那個阻斷項已經實測解除（C108）

`platform/slice-kit` 跑完 Stryker：**58.66% 分數、6.0 秒、179 個 mutant、
零錯誤零逾時**（三次量測分數完全一致，時間取最小值）。

⚠️ **而阻斷不是方法來源警告的那個。** C107 初稿寫著「module-alias 會破壞
Stryker 的 sandbox，而這條線有要在多處同步的 path alias」—— **那句話是假的**，
這條線的 `tsconfig.json` 根本沒有 `paths`。本版已更正，經過記在 C108 §一。

真正撞到的兩個都有繞法：**TypeScript 7 移除了 Stryker 依賴的 compiler API**
（`parseConfigFileTextToJson` 等全部 `undefined` —— 任何依賴舊 compiler API 的
工具在這條線上都會這樣死），以及 **`node-linker=isolated` 讓 runner plugin
在 package 目錄裡看不見**（那不是 bug，是 D6 保 SBOM 不失真的設計代價）。

⚠️ **方法來源建議的 break 門檻是 60，實測 58.66 —— 照抄就是 CI 第一天紅。**
而存活的 mutant 裡有一個是真缺口：`register.ts:51` 的 `features.map((f) => f.name)`
被改成 `map(() => undefined)` **照樣全綠** —— 四個測試覆蓋到那一行，
沒有一條斷言 `names` 的內容。**這是元層存在理由的實物證據。**

---

## [1.5.2] — 2026-08-23

**`v1.5.1` 說跨分支斷言「歸零」，那句話掃的是兩個檔案，宣稱的是一整類。**

`HANDOFF.md` 18 → 0、`README.md` 7 → 0 都是真的。但全樹重掃，`` `main` ``
還有 **16 處**散在 `tools/` 的 `src/` 與 `tests/` 裡 —— 而讀者會把那句話
讀成「這件事處理完了」。

### ✅ 升上來不會壞 —— 這一版是 patch

只動了三個檔案的**註解**（`tools/codemods/`、`tools/doc-facts/tests/` 兩支）。
`surface.json`、`API.md`、`package.json` 的相依與 lockfile **零變更**，
測試 **966 條全數通過、零 skipped**。

### 修正

- **`tools/codemods/drop-a11y-config-for-v1.ts` 的檔頭不再叫人去 `main`**（2 處）。

  ⚠️ **這支是這個 repo 裡唯一會被複製到別人樹上執行的檔案** —— 位置斷言在
  那裡最貴：跑它的人手上沒有 `main`。改成判準（「需要無障礙靜態檢查的案子，
  要自己補一套」）。

- **`tools/doc-facts/tests/` 兩支測試註解裡的「`main` 那條線也沒有」**（2 處）。

### 刻意留下的 12 處，以及判別法

> **這句話會不會出現在別人的樹上？**

`tools/scope-check/src/check.ts` 有 6 處 `` `main` ``，其中兩處正是閘門在教
採用團隊「**『送 `main`』講的是這個 repo 的分支，跟你們無關**」—— 那是
C93／C96 的產出，拿掉會把訊息弱回去。另外 `derive.ts` 與 `tree.ts` 各 1 處
描述的是**情境**（「一台從 `main` 切到 `release/v1` 的機器」），`SCOPE.md:36`
是判準本身。

**談論那條分支** 與 **指路過去** 是兩件事。真殘跡的共同形狀是：
**它叫讀者去另一條分支拿東西，而讀者手上沒有那條分支。**

### 這一版真正的產出

> **報告一個數字歸零時，掃描的範圍必須與宣稱的範圍逐字相符。**

「`HANDOFF.md` 18 → 0」可驗證；「交付文件裡剩下的」是一個**沒有被界定過的
集合** —— 而那個集合沒有被掃完。完整經過見 `DECISIONS.md` 的 **C106**。

---

## [1.5.1] — 2026-08-23

**被拆掉的開發方向留下的殘跡清掉了 —— 其中四處不是過期的敘述，是壞掉的設定。**

`CODEOWNERS` 有 5 條 owner 規則指向這條線上不存在的目錄，**而 GitHub 真的會
拿它去指派審查者**；`renovate.json` 會貼一個叫人去跑「退出演練」的標籤，
而那個演練不在這一版；`platform/bff-contract/README.md` 給的是一行可以複製
貼上的 `vpr bff-check` —— 貼下去是 command not found。

### ✅ 升上來不會壞 —— 這一版是 patch

改了 21 個檔案、刪掉一整節缺口，但**沒有任何閘門讀那些東西**：`surface.json`、
`API.md`、`package.json` 的相依與 lockfile 全部零變更，測試 **966 條全數通過、
零 skipped**（與 `v1.5.0` 同數）。判準是**升上來會不會壞**，不是「改了什麼」——
一棵乾淨的樹升上來零紅燈。

### 移除

- **`CODEOWNERS` 的 5 條死條目**（`bff-check`、`supply-chain`、`compliance`、
  `csp-verify`、`pii-check`）。條目數 **20 → 15**。

  ⚠️ 那個數字由 `doc-facts` 守著，`HANDOFF.md` 的引用處在**同一個 commit**
  一起改 —— 分兩次就是一次紅燈。

- **`renovate.json` 的 `needs-exit-drill` 標籤**（3 處）、**`.gitignore` 裡
  指名某支缺席工具產物的那一行**、**`bff-contract/README.md` 那兩行跑不動的指令**。

- **〈已知的誠實缺口〉第一條與第六條整節**。第一條講的是個資遮罩的**強制機制**，
  第六條整條在講另一條分支缺什麼。其餘四條保留並重新編號。

  ⚠️ **`platform/pii` 本體沒有動。** `OrderList.vue` 正在跑 `maskName()`，
  7 個 export 一個都沒少 —— **v1 做遮罩，沒有的是強制你去用的那道閘門。**

- **交付文件裡剩下的跨分支位置斷言**：`HANDOFF.md` 歸零（原本 18 處），
  `README.md` 歸零（原本 7 處）。這是 C102 §三那條通則的收尾。

- **約 20 處註解裡指名缺席工具的句子**，散在 `src/`、`tests/` 與 `pnpm-workspace.yaml`。
  ⚠️ 只拿掉**指名**的部分，「這一版沒有東西在守這件事」那半句留著。

### 刻意沒有移除

| 沒動的                                | 為什麼                                                                                                              |
| ------------------------------------- | ------------------------------------------------------------------------------------------------------------------- |
| `DECISIONS.md`                        | 拆掉它 = **782 處 C／D／R 引用**變成死連結，**268 處在交付的程式碼自己裡**（`D8` 被引 54 次）                       |
| `AGENTS.md`／`.git-blame-ignore-revs` | 查完之後兩個都判在**內**（C105 §二）——「維護者面向」是我一開始的猜測，不是引用關係跑出來的結果                      |
| `tools/sast` 的引用                   | 那個目錄**從來沒存在過**，是 README 曾經列著一個不存在的東西 —— 那是 `scope-check` 存在的理由，也是它的紅燈訊息本體 |
| 測試裡的死工具名                      | 那是**反例測資**，不是紀錄                                                                                          |

### 修正

- **〈誠實缺口〉第五條的歸屬先前判反了**（C105 §三）。判它「維護者面向」的
  理由是內文寫「加一道閘門時四處要一起改」—— 但 `SCOPE.md` 開頭自己寫著
  **你在 `tools/` 底下加目錄時也會被送到那裡**。加閘門的人就包含你們。
  那一條留下，只拆掉裡面那段位置斷言。

### 這一版真正的產出

> **一個建議在還沒查完「誰在用它」之前，不是建議，是猜測。**

三個根層候選（`DECISIONS.md`、`AGENTS.md`、`.git-blame-ignore-revs`）我都先給
了「移除」的建議，查完引用之後**三個都翻盤**。同一個順序錯誤犯了三次：
把判準套在檔案的**描述**上，而不是套在**誰在用它**上。

---

## [1.5.0] — 2026-08-23

**`tools/theme-verify` 多擋一類違規：從 shadcn 抄元件時忘了翻譯的代幣。**

前兩類抓不到它 —— 它不是 Tailwind 內建色階，也不是色票層，
**它是一個「合法但不是我們的」名字**，而那一格顏色各案換不掉。

⚠️ 這也結束了 `v1.0.x` 那份 ①–⑥ 待辦清單（#57 是最後一項），
連同 `#95` 採用演練的 17 則 —— **v1 範疇內目前零欠項**。

### ⚠️ 升上來可能從綠變紅 —— 先看這一段

| 你的樹上如果…                                                                                          | 現在會紅       |
| ------------------------------------------------------------------------------------------------------ | -------------- |
| 元件裡留著 `bg-primary`、`text-muted-foreground`、`border-input`、`ring-ring` 這類 shadcn 原本的代幣名 | `theme-verify` |
| 或用任意屬性語法直接引用它們（前綴 ＋ 括號 ＋ 上游代幣名）                                             | 同上           |

**這是這一版算 minor 不是 patch 的理由**（判準見 `v1.3.0`：不是「改了什麼」，
是**升上來會不會壞**）。一個從 shadcn 抄了元件而沒翻譯代幣的 fork，
原本全綠，升上來會紅。

**兩條出路，都實測過：**

1. **翻譯成語意名。** 紅燈訊息直接寫是哪一個 —— `--primary` → `--color-accent`、
   `--muted-foreground` → `--color-fg-muted`。改完全綠。

2. **那個概念真的屬於你們的設計系統 → 宣告進 `@theme`。**
   判準是減法，所以宣告 `--color-primary` 之後 `bg-primary` **自動變合法**，
   不必改這支工具。實測：加進去之後產物從 200 格代幣宣告變成 **202** 格、
   連動代幣從 1 格變 2 格 —— 它是真的進了設計系統，不是繞過檢查。

⚠️ **紅燈訊息會勸阻第 2 條**（「不要把 `--primary` 加進 @theme：那會多一層
沒有人用的代幣」）。那句話擋的是**反射性地為了消紅燈而加**；
如果那是一個判斷過的決定，第 2 條是完全正當的出路 —— 而且判準的形狀
就是為了讓它不必改工具。

**沒有單一對應的那幾個**（`secondary`、`sidebar-*`、`chart-*`）訊息明說
**沒有一行改法**，不編一個出來：`secondary` 在這裡是一組 class 而不是一個
代幣，`sidebar-*`／`chart-*` 這個 repo 沒有那個概念。硬給對應比不給更糟。

### 新增

- **`palette.ts` 的第三類違規**（C104）。判準是 **shadcn 的詞彙 減去
  `index.css` 的 `@theme` 裡真的宣告過的名字**。

  ⚠️ **不能寫成「shadcn 的詞彙全部擋掉」** —— `accent` 兩邊剛好同名，而
  `bg-accent` 在元件裡用了十幾處。那樣寫會讓它們全紅，然後整條規則被關掉
  （`v1.0.6` 那批的 C41 就是這個形狀）。

  ⚠️ **減數讀 CSS，不寫死在偵測器裡。** 所以它是活的：加一個代幣進 `@theme`
  的那天，對應的 utility 自動變合法。寫死的話，改一個代幣名就會讓這道檢查
  靜靜地開始誤報或漏報 —— 而 `--color-muted` → `--color-fg-muted` 那次改名
  真的發生過。

### 沒有動的

`platform/*` 的公開形狀（**151 個 export、10 個進入點**）、**27 個元件**、
根層 `API.md`、相依 —— 全部零變更。**不是 major 是閘門判的**
（`surface.json` 零變更）。

### 這一版的證據

- **變異十條，零漏網。** 測試 `tools/theme-verify` **28 → 39**（實跑量的）。
- ⚠️ **其中五條的閘門是綠的，而那是這道規則的性質不是缺陷**：樹上今天一個
  未翻譯的代幣都沒有（量過），所以規則壞掉不會讓真的樹變紅。
  **這一類規則的守衛只有測試** —— 與前兩類（元件裡真的有顏色可以改壞）不同。
- **補救路徑 replay 過**：塞三種違規進元件、讀實際訊息、照它說的改 → 全綠；
  訊息指向的 `UiButton` 的 `VARIANTS` 真的在，`secondary` 真的是它的一個鍵
  （而且值真的是一組 class，那正是訊息說的）。

## [1.4.1] — 2026-08-22

**兩句寫在交付文件裡的話被撤掉了，因為它們宣稱的是另一條分支的狀態 ——
而那種話會過期，這條線上沒有任何東西看得到它過期。**

其中一句會把人送錯地方：因法遵而需要「新增個資欄位必須遮罩」的案子，
照〈已知的誠實缺口〉第一條會去 `main` 找那道檢查 —— **`main` 也沒有。**

### ✅ 升上來不會壞 —— 這一版是 patch

只動了兩份 markdown（`HANDOFF.md`、`DECISIONS.md`），**267 行新增、4 行刪除**，
刪的就是那句位置斷言。`surface.json`、`API.md`、`package.json` 與 lockfile
**全部零變更**，沒有新增任何「不存在就會紅」的檔案。

⚠️ 對照 `v1.4.0`：那一版算 minor，是因為根層少了 `API.md` 會讓一棵乾淨的樹
變紅。判準是**升上來會不會壞**，不是「改了什麼」——
這一版改的東西不少，仍然是 patch。

### 修正

- **〈已知的誠實缺口〉第一條「那道檢查在 `main`，不在 v1」是假的**（C102）。
  那道檢查是 `pii-check --masking`，**由 C52 移除**，連同切片契約那個必填的
  `Feature.personalData`。`main` 自己的三處紀錄講的都是這件事。

  ⚠️ **差別不是文字遊戲。**「在 `main`」的意思是**換一條分支就有** ——
  那正是〈不涵蓋什麼〉整節的承諾。實際上**沒有任何分支有它**：需要強制遮罩
  的案子得自己做，而機制的形狀與成本 C52 §一 記著。

- **〈不涵蓋什麼〉的表頭不再擔保「`main` 那邊有」**（C103）。
  原本那句「下面每一項在 `main` 分支都有完整實作（含閘門、反向測試與文件）」
  是**九列共用一句斷言**，而 C52 一次就打破兩列。

  **現在這一節只擔保一件事：那九項都是刻意不放進 v1.0.0 的，不是還沒做。**
  需要其中任何一項的案子，去 `main` 查它**現在**長什麼樣。

  ⚠️ **不會為此加閘門** —— 跨分支的存在性檢查會讓 v1 的閘門相依於另一條分支
  的內容，那比一句過期的話更糟（何況 `main` 目前是凍結的）。

### 補上一直缺的那段論證

**`SCOPE.md` 的判準是必要條件，不是充分條件。** 那份文件寫著「必須寫得出
受益者那一句，**寫不出來就不准進**」—— 那句話只擋得住寫不出來的，讀起來
卻像個充分測試。

`pii-masking` 是反例：

| 判準                               | 結果                                       |
| ---------------------------------- | ------------------------------------------ |
| `SCOPE.md`「它守的東西給誰看」     | **在內** —— 紅了擋下的是忘了遮罩的那個團隊 |
| C52 §一「**每次成本 × 觸發頻率**」 | **出局** —— 觸發時機是每加一個切片         |

C52 明說第二條是 D16 兩軸的**補充不是推翻**：**一道兩軸都有分的閘門，
仍然可能貴到不值得。** 判準給的是討論資格，不是門票。

⚠️ `SCOPE.md` 本身**沒有改**，那句話還在原地。

### 這一版真正的產出是一條通則

> **排除一件事的時候，寫下的必須是判準跑出來的結果，不是它現在放在哪。
> 位置會過期，判準不會。**

同一節的第五條（閘門名冊手抄）**把判準跑完了**，所以它沒事；第一條只寫了
一個位置，所以它壞了。差別就在這裡。

⚠️ **而這個病在寫這一版的時候又發作了一次。** 草稿寫著那句假話「在 `main`
寫了整整半年」—— 那個數字是編的。查了 git：`49b36da`（v1.0.0 發版）寫下
它的時候，C52 已經移除那道閘門**一天**。**它不是逐漸過期的，是出生就假的**，
存活 5 天。

⚠️ 而 `49b36da` 也是 `v1.4.0` 修的那個缺陷（C97 §三之三）的來源。
**一次大規模「把 X 留在 `main`」的範圍縮減，會產生一批當下就可能為假的
位置斷言，而它們讀起來全都像事實陳述。**

### 沒有動的

`platform/*` 的公開形狀（**151 個 export、10 個進入點**）、
**27 個元件**、相依、以及所有閘門的判定邏輯 —— 全部零變更。

## [1.4.0] — 2026-08-22

**`#95` 採用演練的六則非阻斷級全部處理完** —— 四則修掉、一則量完決定留著並
寫進〈已知的誠實缺口〉、一則順帶。而這一版真正的新東西是根層 **`API.md`**。

⚠️ 六則裡**有兩則的診斷跟票上寫的不一樣**，而**四則是靠讀那個檔案自己怎麼說
才沒有改錯**。詳見下面〈量完之後決定留著的〉。

### ⚠️ 升上來可能從綠變紅 —— 先看這一段

| 你的樹上如果…                                                                           | 現在會紅           | 怎麼辦                                                                          |
| --------------------------------------------------------------------------------------- | ------------------ | ------------------------------------------------------------------------------- |
| 根層沒有 `API.md`，或它與 `surface.json` 對不上                                         | `api-surface`      | `node tools/api-surface/src/cli.ts --update`（它會一起更新兩份）                |
| `README`／`CODEOWNERS`／`HANDOFF`／`SCOPE` 裡有「**見** HANDOFF 的 `R7`」這類編號式指路 | `doc-facts` 的測試 | 把內容寫出來，或拿掉那個指路 —— `HANDOFF.md` 沒有編號式章節，那種引用一定是死的 |

**這是這一版算 minor 不是 patch 的理由**（判準見 `v1.3.0`：不是「改了什麼」，
是**升上來會不會壞**）。

### 新增

- **根層 [`API.md`](API.md)** —— `platform/*` 全部 **151 個 export** 的形狀，
  逐個進入點、逐個 export 列出 props／slots／emits。

  ⚠️ **它是產生出來的，不是手寫的**：來源是 `tools/api-surface/surface.json`，
  而那份基準由原始碼推導、由 `api-surface` 守著。鏈條是
  **原始碼 → 基準（閘門守）→ `API.md`（同一道閘門守）**，
  `--update` 一次寫兩份。手抄 27 個元件的 prop 名字，正是這個腳手架
  一再栽跟頭的那件事。

  ⚠️ **它是形狀參考，不是使用說明** —— 檔案第一段就這麼說，並指回元件原始碼
  的檔頭註解。採用演練那個人說：「原始碼的註解品質很高」，
  但**要知道 `UiField` 收哪些 prop，唯一的路是打開 `.vue`**，
  而那一步花掉的時間比前面所有步驟加起來還多。

- `vp create slice` 的最後一步新增一句：鷹架接下來會印的
  `→ Next: cd features/<name> && vp run` **請忽略** —— 這個腳手架的切片不是
  獨立可跑的東西。⚠️ 那行印在我們控制範圍外，`suggestions` 是唯一講得到話的地方。

### 修正

- **`UiField` 包 `UiSelect` 會產生一個指向不存在元素的 `<label for>`**（C101）。
  `UiField` 交出的 `control`（`id`／`aria-describedby`／`aria-invalid`）落不到
  `UiSelect` 上 —— 它的根是 `SelectRoot`，一個無渲染的 context 元件。
  結果是那顆下拉**沒有任何程式可讀的名稱**，而三道相關閘門全綠。

  ⚠️ 修法是 `inheritAttrs: false` ＋ 把 `$attrs` 轉給觸發鈕，**不是加一個選填
  `id` prop** —— 前者三格一次接齊，而且 `platform/*` 的公開形狀**零變更**。
  ⚠️ **行為變化**：以前傳給 `<UiSelect>` 的屬性掉在無渲染的根上（等於消失），
  現在會落到觸發鈕。

- **兩處指向不存在章節的交叉引用**（C99）：`README` 的「見 HANDOFF 的 R3／R5」、
  `CODEOWNERS` 的「參照 HANDOFF #25」。⚠️ 它們**不是過期，是從寫下的那一刻就死的**
  —— 掃過 50 個動過 `HANDOFF.md` 的版本，那些編號**一次都沒出現過**。
  新增一道守擋住這一類（而它當場抓到第三處，然後那一處推翻了第一版的規則：
  「提到編號」與「叫你去看編號」是兩件事）。

- `CODEOWNERS` 檔頭那個 `22` 加了說明。⚠️ **不是把它改成 20** ——
  它量的是 `gh api …/codeowners/errors` 的 Unknown owner 數（GitHub 的判定），
  與條目數是**兩種東西**，只是共用量詞「條」。改掉它會毀掉一次歷史實測。

- `HANDOFF.md`〈已知的誠實缺口〉那段重複兩次的文字。

### ⚠️ 量完之後決定留著的

**使用端把 prop 名字打錯，不會有任何東西說話。** `UiButton` 是 `variant`，
`UiAlert`／`UiBadge` 是 `tone`；照前者的習慣寫 `<UiAlert variant="danger">`
全套閘門綠，而提示安靜地渲染成 info 色。

兩次量測都寫進〈已知的誠實缺口〉了（**不要再量第三次**）：

| 開什麼                   | 結果                                                                                  |
| ------------------------ | ------------------------------------------------------------------------------------- |
| `strictTemplates`        | 2 條誤報，而修法會關掉 fallthrough —— 是真的行為迴歸                                  |
| 只開 `checkUnknownProps` | **28 條，全部是 `data-slot`**；正名之後換成 `aria-invalid`／`aria-describedby` 那一批 |

最後那一批正是 `UiField` 的 `control` 靠 fallthrough 傳下去的東西。
**這個元件庫的設計整體建立在 fallthrough attrs 上**，而那顆旋鈕與那個設計衝突
—— 不是設定沒調對。⚠️ 已經好一點的一半：prop 名字現在查得到，在 `API.md` 裡。

### 相容性

**`platform/*` 的公開形狀零變更** —— `git diff v1.3.0..HEAD -- tools/api-surface/surface.json`
是**空的**。`api-surface` **151 個 export、10 個進入點**，元件 **27**，全部不變；
**相依零增減**。所以這**不可能是 major**，而那個判定是閘門做的。

⚠️ **「minor 不是 patch」一樣不是閘門判的** —— SemVer 承諾只釘 major。
算 minor 的理由寫在最前面那張表。

測試（實跑）：`api-surface` **91 → 100**、`doc-facts` **37 → 44**、
`slice-gen` **71 → 73**、`platform/ui` **397 → 399**。

---

## [1.3.0] — 2026-08-22

**這一版全部在 `tools/`，`platform/` 一個位元組都沒動。**

`#95` 的採用演練點名三道閘門「補救路徑假設你擁有這個 repo」。三格做完之後，
**那句話只對其中一格成立**（`scope-check`，已在 `v1.2.0`）。另外兩格的病是
另一種、而且更難看見：**跟著它們自己的紅燈指示走，會走進一個洞。**

### ⚠️ 升上來可能從綠變紅 —— 先看這一段

這一版加了**兩條新的閘門規則**。一個什麼都不改的樹，升上來可能當場紅：

| 你的樹上如果有…                                    | 現在會紅      | 怎麼辦                                                             |
| -------------------------------------------------- | ------------- | ------------------------------------------------------------------ |
| `platform/` 底下一個**沒 `git add`** 的套件目錄    | `api-surface` | `git add platform/<名字>`（staged 就算數），或把它移出 `platform/` |
| `platform/` 底下一個**版控裡有、磁碟上沒有**的套件 | `api-surface` | `git restore`，或 `git rm -r` 讓兩邊一致                           |
| `tools/doc-facts` 的某個事實 `citations: []`       | `doc-facts`   | 不再守它就**移除整個 `Fact`** —— diff 看得見，空陣列看不見         |

三條紅燈都印得出完整的出路。**這是這一版算 minor 不是 patch 的理由**（見〈相容性〉）。

### 新增

- **`doc-facts`：零引用樣式的事實會紅**（`kind: "unguarded"`，C97）。
  ⚠️ 在此之前，把某個事實的 `citations` 清成 `[]` 會讓閘門 **exit=0、全綠、
  測試全過** —— 唯一的痕跡是綠燈裡的樣式計數少一，而沒有人有那個基準。
  而**紅燈訊息當時正把人指向那條路**（「同步更新 `src/facts.ts` 的樣式」）。

- **`api-surface`：index 與磁碟不一致會紅**（C98），兩個方向都擋：
  - **磁碟有、版控沒有** —— 跑 `--update` 會把它烤進基準、當場變綠，
    而 CI 的乾淨 clone 上那些 export 全部變成「移除」，
    被判成破壞性變更、要求為一個**從來不存在於版控的 API** 寫 codemod。
  - **版控有、磁碟沒有** —— 鏡像的病理，要求為一個**仍然在版控裡**的套件寫 codemod。

  ⚠️ 兩種紅燈都**沒有合法出口**（沒有 codemod 可寫），所以改成在比對之前先擋。
  ⚠️ **好好地 `git rm` 不在其中** —— 擋的是兩邊不一致，不是移除本身。

### 修正

- **三道閘門的紅燈現在都對兩種讀者說話**，而**都不去偵測「這棵樹是不是上游」**
  （那是 `#91` 在問的，答案還沒有；猜錯的偵測比預設一種讀者更糟）。
  這一版補的是 `doc-facts`（「與上游分歧是預期的」）與 `api-surface`
  （「『下游』是誰取決於你是誰」—— 原文只寫了發布方那一種，而 fork 了 v1 的團隊
  **就是「各案」**）。

- **`doc-facts` 的 `action-refs` 一直沒有守著任何東西** —— `citations` 只有一行
  寫著「所以**只有它**被登記」的註解、**沒有 regex**。⚠️ 它不是生來就空的：
  樣式是在 `49b36da`（`release(v1.0.0)` 的範疇縮減）掉的，**regex 刪掉、註解留著**，
  因為它守的那句話在那次縮減裡被裁掉了。**空了八個發出去的版本，全套閘門全綠。**

- 訊息裡指錯對象的兩處：`doc-facts` 的「**這個 repo** 一再栽跟頭」（在 fork 的樹上
  那四個字指的是他們的 repo）、`surface.json` 的「論證在 **main 的** DECISIONS.md C69」
  （`main` 不是他們的分支）。

### ⚠️ 查過而刻意**沒有**修的

`tools/codemods` 的兩件：**空的 codemod 檔案照樣赦免那筆 removal**、**空的 `reason` 通過**。
`tools/codemods/README.md` 自己寫著這道閘門「保證的是**被看見並經過 review**，
不是 codemod 正確…用更多程式碼去驗證 codemod 的語意，是在自動化一件本來就該由人
判斷的事」。**明說了不保證，就不是宣稱假話** —— 與 `SCOPE.md` 那次（`v1.2.0` 的 C94）
剛好相反。

### 相容性

**`platform/` 零改動** —— `git diff v1.2.0..HEAD -- platform/` 是空的。
`api-surface` **151 個 export、10 個進入點，都不變**；元件 **27，不變**；
**相依零增減**（`pnpm-lock.yaml` 與 `pnpm-workspace.yaml` 一個字都沒動）。
所以這**不可能是 major**，而那個判定是閘門做的。

⚠️ **「minor 不是 patch」那一格一樣不是閘門判的** —— 上面那條 SemVer 承諾只釘 major。
這一版算 minor 的理由寫在最前面那張表：**一個什麼都不改的樹，升上來可能從綠變紅。**
patch 傳達的是「不可能壞」，而那句話在這裡是假的。

測試：`tools/api-surface` **66 → 91**、`tools/doc-facts` **29 → 37**（實跑，不是數 `it(`）。

---

## [1.2.0] — 2026-08-22

**兩件事：採用演練的阻斷級症狀修掉了，而一次發版前的範疇審查把「守範疇的
那道閘門」自己的三條縫翻了出來。**

⚠️ 第二件是這一版真正的主線。`v1.0.5` 起 `tools/scope-check` 就掛在
`vpr ready` 上，而它**宣稱的**與**做到的**中間有三條縫，三條都是同一種病：
**寫下來的東西與程式碼行為對不上，而沒有東西會說話。**

### 新增（C91）

- **`platform/bff-mock` 可以注入路由了** —— `BffMockOptions` 追加 `routes`
  與 `extraPermissions`。注入點在 **401 閘門與契約端點之後**、示範資料之前：
  沒有 session 一樣 401、契約端點蓋不掉、非安全方法一樣要過 CSRF。
  權限是**追加不是取代**，預設值仍然不含 `admin`。

- **`apps/console` 第一天就看得到資料了。** dev-only 的 session 橫幅
  （`import.meta.env.DEV` ＋ `defineAsyncComponent`，production 建置整段剝掉，
  有測試守著），proxy 目標改讀 `.env` 的 `BFF_ORIGIN`，
  而 `apps/console/bff-routes.ts` 宣告了 `POST /api/orders/:id/cancel` ——
  ⚠️ `features/order` 的 `cancelOrder()` **一直在呼叫它，而它一直 404**。

- `HANDOFF.md` 新增〈從這裡到第一個能操作的畫面〉。⚠️ 裡面的程式碼範例是
  **逐字存成檔案跑過 `vp check`** 的 —— 第一版就吃了 TS7031。

### 修正

- **深色模式下整個應用是黑底黑字**（C92）。⚠️ **代幣一直都在、而且一直是對的**
  —— 壞的是「入口沒有任何一條規則把它們用在文件上」。元件各自帶底色所以按鈕
  與對話框看起來正常，表格與頁面本體直接吃瀏覽器的深色畫布。
  修在 `platform/ui` 不是 `apps/console`，因為 `SCOPE.md` 說各案會把 `apps/`
  **整個換掉** —— 修在那裡等於沒修。

- **`sr-only` 的閘門分不出缺陷與非缺陷**（C89），順帶更正四處自己寫的論證。
  ⚠️ 其中一處是**建立在未量前提上的禁令** —— 一句錯話會被下一個讀者抓到，
  一條這樣的禁令會讓他不去查。

### 閘門 —— scope-check 自己的三條縫（C93–C96）

- **C94：那句保證是假的。** `SCOPE.md` 從 `v1.0.5` 起寫著「這道閘門保證的是
  **沒有人可以跳過那一格**」。實測：把某一列的後兩欄清空，解析照樣收下、
  閘門照樣綠。⚠️ 那句話把兩件事包成一句 ——「**寫得對不對**機器讀不出來」
  是真的，「**有沒有寫**機器讀不出來」是假的，於是可機械化的那一半也一起沒做。
  修的是程式碼不是那句話。

- **C96：射程只有 `tools/` 與 `platform/`。** 加一個新的頂層目錄不會紅。
  現在多一節〈根層 —— 准許存在的〉，管 7 個頂層目錄 ＋ 18 個根層檔，
  事實來源仍是同一條 `git ls-files`。
  ⚠️ 順帶補了第四個方向「**這一節沒有人在檢查**」—— 具名錨點擋住了
  「新增章節 = 無聲的治理範圍擴大」，卻放進了相反的洞。

- **C95：紅燈訊息預設讀者是維護者。** 而 `vpr ready` 正是 HANDOFF 叫
  **拉 v1 去做案子的團隊**第一個跑的東西 —— 原訊息叫他們寫出
  「受益者是拉 v1 的團隊」那一句，而那句話他們**依定義寫不出來**。
  訊息現在對兩種讀者各說一次，⚠️ **刻意不去偵測「這棵樹是不是上游」**：
  猜錯的偵測會給出看起來很確定的錯訊息，比預設一種讀者更糟。

`tools/scope-check` 的測試 **11 → 26**。

### ⚠️ 一批進了這條線又被撤掉的東西

`#94` 把 `AGENTS.md` 的一節、`CONTEXT.md`、`docs/agents/` 三份與
`docs/adr/0001` 放上 `release/v1`。發版前審查判定**超出 v1 範疇**，C93 全數撤回。

論證不是「`docs/` 面向開發流程」，而是 `SCOPE.md:28` 那條舉證責任：
「判定時必須寫得出『受益者是拉 v1 的團隊』那一句。」`#94` 寫的是另一句
（「`SCOPE.md` 把根層檔案排除在治理之外，所以不構成範疇衝突」）——
**「沒有被治理」不等於「准許進入」。**

⚠️ **沒有任何一個發出去的版本含過那些檔案**：`v1.1.0` 在它們進來之前，
這一版在它們被撤之後。寫在這裡，是因為在兩個 tag 之間讀 git log 會看到
`docs/` 出現又消失。內容保留在 `f240484`。

### 相容性

`platform/*` 的 API 形狀**零刪除、零改名**：`api-surface` 從 148 增為 **151** 個
export（10 個進入點不變），全部是新增。所以這是 **minor 不是 major** ——
而那個判定是閘門做的，不是人宣告的。

⚠️ **但「minor 不是 patch」那一格不是閘門判的。** 上面那條 SemVer 承諾只釘
major（移除、改名、改變形狀讓閘門失敗）。這一版算 minor 的理由是**新增了
採用端看得到的能力**，那是 SemVer 常規加上 `v1.1.0` 的前例 ——
把它講成閘門判的，會反過來把「版號是閘門強制出來的」那句話弄假。

元件數 **27，不變**。**相依零增減** —— `pnpm-lock.yaml` 與
`pnpm-workspace.yaml` 從 `v1.1.0` 到這一版**一個字都沒動**。

---

## [1.1.0] — 2026-08-20

**C81 判出的三個缺陷型元件補齊了：`Field`、`Alert Dialog`、`Dropdown Menu`。**

⚠️ **這一版放寬了 `v1.0.6` 才定下的元件範疇**（C78），而那是刻意的：
C81 §七 把候選分成「三個是缺陷、四個是方便」，這一版只補前三個。
**四個方便（`Card`／`Empty`／`Item`／`Spinner`）是待裁決，不是被裁掉** ——
把它們讀成「已經判出去了」，下一個人就會拿錯那把尺。

三個元件的共同形狀：**C81 給的理由是對的，但沒有一個足以決定 API 形狀。**
真正決定形狀的三件事都要讀上游原始碼才看得到，而三次都推翻了先寫好的設計。

### 新增（C84／C86／C88）

- **`UiField`** —— label ＋ 控制項 ＋ 說明 ＋ 錯誤訊息的版型**與接線**。
  ⚠️ 上游的 `Field` 是九個子元件的純版型家族，`aria-describedby` 全手動 ——
  照抄的話它過不了 C81 自己的層 2。這一個改成 **scoped slot 給一個 `control`
  物件**，id 產生與 `aria-invalid` 傳遞都在裡面。
  ⚠️ **使用端忘了 `v-bind="control"` 接線還是斷的，而且畫面正常。**

- **`UiAlertDialog`** —— 破壞性動作的確認框。⚠️ 它**不是** `UiDialog` 換兩顆
  按鈕：`role="alertdialog"`、點外面不關、而且**初始焦點落在「取消」**。

  > ⚠️ **刻意沒有 `footer` 槽**（`UiDialog` 有）。那個焦點保護不是
  > `AlertDialogContent` 做到的 —— `cancelElement` 是 `AlertDialogCancel`
  > **自己在 `onMounted` 註冊的**，沒渲染它 `?.focus()` 就是 no-op，
  > 而畫面完全正常。**覆寫是整條替換，所以能被覆寫的東西不能是保護。**

- **`UiDropdownMenu`** —— 表格每一列的「⋯」。方向鍵、Home/End、首字母跳轉、
  Esc 關閉並還原焦點。

  > ⚠️ **`label` 必填，因為選單的名字是借來的。** `DropdownMenuContent` 寫死
  > `:aria-labelledby="rootContext?.triggerId"` —— 觸發器沒有可及名稱，
  > `role="menu"` 也一起沒有。而「⋯」最自然的實作 `<button>⋯</button>`
  > 正好就是那個樣子。名字走 `sr-only` 的**文字**不走 `aria-label`：
  > 後者壞掉畫面完全正常，前者壞掉那行字直接顯示在按鈕上。
  > **把安靜的缺陷換成吵鬧的缺陷。**

  > ⚠️ **最後兩句已更正，見 `DECISIONS.md` C89 §五。** 「壞掉」只涵蓋**一種**
  > 壞法（`sr-only` 沒編進 CSS）—— `label` 這個字串本身寫錯、寫空、忘了翻譯，
  > 兩種寫法一樣安靜。而「把安靜的缺陷換成吵鬧的缺陷」這一招本 repo
  > **沒有用過**：先前那幾次的修法是補 prop、改上游預設、進 review 清單。
  > 真正撐住這個選擇的理由當時沒寫出來 —— **名字在內容裡，
  > 「選單真的拿到名字了」那條測試才走得通。**

### 修正（C85）

- **模板裡的 HTML 註解會進 SSR 產物。** `renderToString` **不移除**註解
  （用戶端 production build 會），所以只咬 SSR 使用端 —— 而本 repo 的
  `apps/console` 是 SPA，**自己的產物看不到這件事**。
  ⚠️ 不是 `UiDialog` 一個，是**四個元件五段**。

  > ⚠️ **C85 的標題點名的那一個是唯一不會的**（C86 §六 更正）：`UiDialog`
  > 那段寫在 `<DialogPortal>` 裡，SSR 下整個 portal 是 `<!--v-if-->`。
  > 掃描與絆線都對，錯的是**只量了一個就把結論推到另外四個身上**。

### 內部（C87）

- **兩支測試搶同一個真實 repo**，`tools/conformance` 與 `tools/slice-gen` 對
  同一個根斷言相反的狀態，並行跑是擲銅板。修在**排程層**，不弱化斷言。
  ⚠️ 連續三個 PR 的 CI 紅在同一行，而每次重跑都會過 —— 這種紅最貴的地方
  不是它本身，是它教人學會忽略紅燈。

### 閘門

- `platform/ui` 第一次有 **DOM 測試**（happy-dom）。⚠️ **供應鏈零變化**：
  `happy-dom` 與 `@vue/test-utils` 本來就在 catalog 裡，lockfile 只多兩行
  importer。環境用 `// @vitest-environment happy-dom` 限在單一檔案。

  > ⚠️ 非得是 DOM 不可：reka-ui 的 `Teleport` 是 `isMounted || forceMount`，
  > 而 `useMounted()` 在伺服器端是 false —— **包在 portal 裡的東西
  > `renderToString` 一個字都不渲染**。

- 三個元件的關鍵行為各自被變異驗過（C84 八個、C86 九個、C88 十五個）。
  ⚠️ **零紅的變異也記下來了**，沒有假裝有閘門在守 —— 見 DECISIONS 的
  C88 §九（`:text-value`、`data-[highlighted]`、以及 `sr-only` vs
  `aria-label` 三處只有慣例、沒有行為層檢查）。

### 相容性

`platform/*` 的 API 形狀**零刪除、零改名**：`api-surface` 從 142 增為 148 個
export（10 個進入點不變），全部是新增。所以這是 **minor 不是 major** ——
而那個判定是閘門做的，不是人宣告的。

元件數 24 → 27。**相依零增減。**

---

## [1.0.7] — 2026-08-19

**判準補了兩層、骨架的兩個無障礙缺陷修了、而測試檔的字面值一直在進使用者
下載的 CSS。**

三件事的共同形狀：**閘門全綠而東西是壞的** —— 這一版沒有新功能，全部是
把「看不出來的壞掉」變成看得出來的。

### 變更（C83）

- **交付的 CSS 少 0.84 kB。** `platform/ui` 的 `@source` 掃全 repo 的 `.ts`，
  而 Tailwind 的抽取器是字串比對不是解析器 —— **連註解裡長得像 utility 的
  字串都會變成規則**。實測 `apps/console`：27.31 → 28.15 kB，13 條選擇器
  全部來自測試檔，逐一查證過沒有一條是真的元件或切片在用。

  > ⚠️ **被汙染的不只 CSS 規則，還有 `@theme` 代幣表。** `--color-blue-500`
  > 與 `--font-weight-bold` 兩格由測試字串拉進去 —— 而 `theme-verify` 正是
  > **逐格驗代幣**的工具，它一直在驗一份含測試殘留的表。

  修法是 `@source not` 兩條（目錄式與並置式）。⚠️ 改窄這條 glob 正是它自己
  檔頭警告的動作，所以查了退出演練的佈局才動；而真正讓它安全的是**失敗方向
  相反** —— 原本那條失效會讓樣式少一半，這兩條失效只是產物變胖。

### 修正（C82）

- **`UiSkeleton` 對輔具是靜默的。** 一個純 `<div>`，沒有 `aria-hidden`，
  載入期間螢幕閱讀器什麼都不唸，然後內容突然出現。
  ⚠️ `aria-busy` **不在骨架上** —— WAI-ARIA 那條 MUST 是標在**容器**上的，
  骨架該拿的是 `aria-hidden`，busy 訊號歸消費端。
- **`animate-pulse` 的閃動關不掉。** Tailwind v4 不帶任何
  `prefers-reduced-motion` 保護（實測 `4.3.3` 四份 CSS 全檔沒有這個媒體查詢），
  對前庭障礙使用者是一個關不掉的動畫。加 `motion-reduce:animate-none`，
  而**驗收是編譯不是讀原始碼** —— C77／C80 已經栽過兩次「class 寫了但執行期
  被丟掉」。

### 文件（C81）

- **v1 的元件範疇判準補了兩層，而第二層一寫下來就被自己的存貨推翻。**
  C78 只寫了「典型 CRUD 第一天要不要自己寫」。實際在用的是三層：
  **層 0**（要不要應用外殼替它接線）、**層 1**（第一天要不要）、
  **層 2**（缺了消費端看不看得出來）。

  > ⚠️ 層 2 一開始是當**門檻**寫的。拿去跑既有 24 個元件時，
  > `UiBadge`／`UiSkeleton`／`UiTableCell` 全部過不了 —— 而那是 `v1.0.6`
  > 出的貨。**一條會把自己已經出貨的東西判成錯誤的判準，是被推翻了，
  > 不是更利了。** 改成排序：它決定「缺了算不算 bug」。

  上游型錄裡留白的 46 個名字整批裁完（扣掉 2 個別名 —— **型錄自己就有
  重複**）。三層全過的三個：`Alert Dialog`、`Field`、`Dropdown Menu`。
  ⚠️ 補它們是對 C78 範疇的**修改**，不在這一版裡。

- **`Sheet` 不是新元件，是 `UiDialog` 的 `content` 槽覆寫** —— 寫進該元件
  檔頭，否則下一個人會新增 `UiSheet`，然後兩份無障礙接線各自漂移。

### 閘門

新增兩道絆線，四個變異都驗過會紅：

- `platform/ui/tests/styles.test.ts` —— `@source not` 的宣告必須在，
  而且必須是能跨佈局的 glob。
- `tools/theme-verify` 的**反向探針** —— 與既有的 `fixtures/probe.ts` 成一對：
  一個守「`.ts` 必須進產物」，一個守「`tests/` 不准進產物」。
  ⚠️ 放這一邊是因為它有真的建置；退出演練**抓不到**這一種
  （它比 CSS 位元組比值 ≥ 80%，而多掃的方向是產物變大）。

### 沒有變的

`platform/*` 的 API 形狀零變更（142 個 export／10 個進入點）、元件數 24、
沒有新增或移除相依。

---

## [1.0.6] — 2026-08-19

**`platform/ui` 從 2 個元件補到 24 個，而範圍第一次被定義下來。**

`v1.0.0` 起 HANDOFF 的〈已知的誠實缺口〉第二條就寫著「元件還要補」。這一版把它
補完 —— 但補之前先回答了一個沒有人問過的問題：**補到哪裡算夠？**

> ⚠️ **查證結果：從來沒有任何文件列過 v1 要有哪些元件。** HANDOFF、SCOPE、
> DECISIONS、git 歷史都查過。唯一存在的依據是 C62 記下的產品要求原句
> （「一套基礎的 UI 版型和互動方式」）—— 那是**判準**不是清單。
>
> 範圍現在由 **C78** 定義，判準是「一個典型的 CRUD 案子，第一天要不要自己寫
> 這個元件」，而且**連刻意不進來的那幾個也寫下來了**。那是一個可以被反駁的
> 決定，不是一份被當成既定事實的記憶。

### 新增 21 個元件（2 → 24）

|                |                                                                                                                           |
| -------------- | ------------------------------------------------------------------------------------------------------------------------- |
| **表單**       | `UiInput`＊、`UiTextarea`、`UiSelect`、`UiCheckbox`、`UiRadioGroup`／`UiRadioItem`、`UiSwitch`、`UiLabel`、`UiDatePicker` |
| **資料**       | `UiTable`／`UiTableHead`／`UiTableBody`／`UiTableRow`／`UiTableHeadCell`／`UiTableCell`、`UiPagination`                   |
| **版型與回饋** | `UiBadge`、`UiSkeleton`、`UiTabs`／`UiTabsPanel`、`UiSeparator`、`UiAlert`                                                |

＊ `UiInput` 是從 `main` backport 的。

**刻意不在裡面的**：`Accordion`／`Tooltip`／`Popover`／`Combobox`／`Avatar`／
`Progress`／`Toast`。判準同上 —— 它們是**特定畫面需要時才加**的，不是每個案子
第一天都要有。⚠️ `Toast` 是最接近邊界的一個：它需要一組全域的 `Provider` ＋
`Viewport` 接線，而「掛在哪、疊在哪、同時最多幾個」是**應用外殼的決定**，
不是一個元件。所以用就地顯示的 `UiAlert` 取代。

### ⚠️ 修掉三個「畫面上看得見、而每一道閘門都是綠的」缺陷

這一版的三個 review 各抓到一個同一家族的東西：**寫下來的樣式在執行期
或渲染時消失，而所有檢查都通過。**

|                                                    | 症狀                                                                             | 為什麼閘門看不見                                             |
| -------------------------------------------------- | -------------------------------------------------------------------------------- | ------------------------------------------------------------ |
| **C77** `border-control` 被 `twMerge` 當成顏色丟掉 | **`UiButton` 的 `secondary`（預設 variant）與 `UiInput` 從落地那天起就沒有邊框** | CSS 產物完全正確 —— 那一格是**執行期**被丟掉的               |
| **C80** 同一個 bug 在方向性寫法上復發              | `UiTableHead` 的下邊框不見                                                       | C77 只登記了不帶方向的那一族，而守它的測試也只組得出基本形式 |
| **C79** `UiDatePicker` 的三條 `aria-invalid:*`     | 驗證失敗時日期欄不變色，而旁邊的 `UiInput` 會                                    | `DatePickerRoot` 不渲染元素，fallthrough 到不了 field        |

C77 的修法是 `extendTailwindMerge` 登記本 repo 的自訂代幣，而**守它的方式**是
`platform/ui/tests/cn.test.ts` 從 `styles/index.css` 的 `@theme` 推導代幣名
反過來問兩件事：同族的後者要贏（抓「不認得」）、不同族的兩個都要留
（抓「歸錯族」）。C80 補上八個邊與十四個角。

> ⚠️ **C80 的教訓值得單獨看：一道只檢查一半情況的閘門比沒有閘門更危險。**
> C77 的守衛存在、也真的會紅，但它探的形狀比實際會出現的窄，
> 於是同一個 bug 換一個 `-b-` 就整個溜過去。

### 新增 `tools/doc-facts` 的 `ui-components` 事實

「`platform/ui` 有 N 個元件」這句話寫在三個地方，而在這之前**沒有任何東西在守**
——`UiInput` 進來時三處都得手改。現在由 `git ls-files` 數出來（不是掃磁碟，
理由見 C73），兩個方向都變異驗過。

⚠️ 文件裡的中文數字跟著改成阿拉伯數字：**不是為了好看，是推導不出來的數字
守不住** —— 「三個元件」對 `\d+` 而言不存在。

### 新增 `tools/theme-verify` 的 `RUNTIME_PROVIDED`

`UiSelect` 用 reka-ui 在**執行期以 inline style 寫入**的
`--reka-select-trigger-width`（讓下拉面板與觸發器等寬），而懸空引用那道檢查
的模型是「我們的 CSS 宣告、我們的 CSS 引用」——它當場紅了。

**檢查是對的，模型是窄的。** 放行清單的**理由是必填字串**（C41），判準很窄：
只有「我們無法宣告、而且宣告了反而是錯的」才進去。三條反向測試各自變異驗過，
其中一條從**元件原始碼反查**，確保清單裡不會留下死掉的那一筆。

### 新增相依：`@internationalized/date`

⚠️ **不是新的供應鏈範圍。** 它從第一天就在樹裡（`reka-ui` 自己相依它）——
這裡只是把「本來就在用」寫成明的，因為 `platform/ui` 要直接 `import type
{ DateValue }`。**新增實際套件 0 個**，`pnpm-lock.yaml` 只多 6 行。

版本**逐字跟著 reka-ui 宣告的 `^3.5.0`**，不是抄目前解到的版本 ——
寫得比它窄會在升版時產生第二份副本（D15 記過的 `lightningcss` 形狀）。

### 決定：`cn()` 的位置不動（C75）

量完的結論是不做，而量的過程**推翻了問題本身的兩個前提**：

- 「每次 render 都重算」是假的 —— `<script setup>` 的本體是 `setup()`，
  每個實例跑一次；`computed()` 有快取。實測掛載後 10 次重繪都是 **0 次**。
- 成本的計量單位不是實例數，是**相異字串數** —— `tailwind-merge` 有 LRU
  （`cacheSize` 500，鍵是併好的 class 字串）。「一頁上百個 cell」是 **1 格**。

放進真的掛載裡量，`cn()` 的增量是 **0.26 – 0.29 µs**／實例，而實例掛載本身
約 2.8 µs（SSR）。

### API 形狀

`platform/*` 的 export 從 **98 → 142**，`@org/ui` 從 9 → 53。
**全部是新增** —— 沒有任何移除或形狀改變，所以這一版是 minor 不是 major。

### 契約測試零編輯撿到 21 個新元件

`platform/ui/tests/component-contract.test.ts` 是**掃目錄**的，加 21 個元件
**一個字都沒改**。`@org/ui` 整包從 **45 → 252 條**，而那 207 條的來源分得開：

|                              | v1.0.5 | 現在    | 來源                                    |
| ---------------------------- | ------ | ------- | --------------------------------------- |
| `component-contract.test.ts` | 33     | **189** | 掃目錄，每個元件 7 條 ＋ 固定的反向測試 |
| `cn.test.ts`                 | —      | **51**  | C77／C80 新增，從 `@theme` 推導         |
| `styles.test.ts`             | 12     | 12      | 不變                                    |

⚠️ 兩個數字要能分開才有意義：`45 → 252` 這個總數裡有 51 條**不是**元件帶來的，
把它整包說成「掃目錄撿到的」會高估那個機制。

---

## [1.0.5] — 2026-08-19

**讓 `SCOPE.md` 有牙齒。** `platform/*` 的型別形狀沒有改動 —— 新增的是一支
`tools/*` 閘門，不是 API。

`v1.0.4` 把「什麼准許出現在 `release/v1` 的樹上」寫成了 `SCOPE.md`，而那一版的
文末自己承認：**加一支工具而忘了在那裡加一列，不會有任何東西說話。**
這一版把那句話變成假的。

### 新增 `tools/scope-check`

```bash
./node_modules/.bin/vpr scope-check
```

比對 `SCOPE.md` 的〈准許存在的〉那兩張表與**版控裡真正存在的目錄**，
`tools/` 與 `platform/` 兩層，**兩個方向都會紅**：

| 情況                 | 為什麼要紅                                                                      |
| -------------------- | ------------------------------------------------------------------------------- |
| 版控裡有、表上沒列   | 範疇裡悄悄多了東西，而**沒有人被逼著寫那句「受益者是拉 v1 的團隊」**            |
| 表上列了、版控裡沒有 | **清單在說謊** —— README 的目錄樹曾經列著 `tools/sast/`，而那個目錄從來沒存在過 |

只驗前者，這份文件會慢慢長出一堆早就不存在的項目；只驗後者，加一支工具就再也
沒有人會被逼著寫那句受益者 —— 而那句話正是整條判準的執行方式。

### 事實來源改成 `git ls-files`（v1.0.4 預告的是 `git ls-tree`）

`readdirSync` 那一半沒有變：切分支會留下含 `node_modules` 殘骸的空目錄，
**用磁碟當事實來源的檢查會在開發機紅、在 CI 綠**。變的是另一半 ——
`ls-tree HEAD` 答的是「**上一個 commit** 裡有什麼」，於是新增一支工具、
`git add` 了、跑 `vpr ready` 會是**綠的**，要等 commit 完才紅。
而 `vpr ready` 存在的全部理由就是「推上去之前先知道」。
`ls-files` 讀的是 index，staged 的新目錄當場看得見。

⚠️ 找不到 git 時它**直接失敗，刻意不退回去掃磁碟**。一道「找不到就換個寬鬆
判準」的閘門，會在最需要它的環境裡安靜地變成另一件事，而且沒有人會發現。

### 它刻意不守的三件事

- **README 的目錄樹** —— 它只讀 `SCOPE.md`。忘了同步 README 一樣是全綠。
- **README〈兩層檢查〉那張表** —— 守它的機制在 `main`，見
  HANDOFF〈已知的誠實缺口〉第五條。
- **「受益者是拉 v1 的團隊」那一欄寫得對不對** —— 它只斷言那一欄**存在**。
  這道閘門買的是「沒有人可以跳過那一格」，不是「那句話是真的」。

### 接線

- `package.json`：`vpr scope-check` 別名，並接進 `scripts.gate`（`vpr ready` 涵蓋）
- `tier2-security.yml`：緊接在〈文件數字與事實來源一致〉之後 ——
  兩者中間就是那條縫，**`doc-facts` 守數字，這一步守清單**
- `README`：〈兩層檢查〉Tier 2 那格、CI 那張表、目錄樹、套件數 20 → 21

⚠️ 它放在 Tier 2 **不是**因為該檔頭那三條規則（那是給「會隨時間失效」的安全掃描
用的），而是與 `doc-facts` 同一條理由：跨整個 repo，不該被 affected 過濾跳過。
理由寫在 workflow 的註解裡，免得下一個人套一次判準就把它搬走。

### `DECISIONS.md`

- **C73** —— 判定為什麼建立在 git 上、為什麼只認表格第一欄（`SCOPE.md` 的散文
  用 `tools/gate-roster` 當反面教材，grep 全文會把它算成已登記）、以及那一節
  整個不見時為什麼是紅燈而不是「這一層沒有清單」。
  ⚠️ 這個編號在 `main` 上是另一件事，日後 backport 不要靠編號對齊。
- **C72 加標註**，不改原文：文末「沒有任何機制在守這份文件」已不成立。

---

## [1.0.4] — 2026-08-18

**純文件。** 一行程式碼都沒動，`platform/*` 的型別形狀當然也沒動。

把 v1.0.3 那一版**用來做判斷的那條判準本身**寫下來 —— 上一版是憑它擋掉了
`gate-roster`，但它只存在於幾個人的腦子裡與一則 issue 的內文中。

### 新增 `SCOPE.md`

回答一個問題：**什麼准許出現在 `release/v1` 的樹上。**判準是
**看它守的東西給誰看** —— 面向交付物（含文件）在內，受益者是拉 v1 去做案子的
團隊；面向開發流程在外，受益者是維護者。判定時**必須寫得出「受益者是拉 v1 的
團隊」那一句**，寫不出來就不准進。

它與 HANDOFF〈v1.0.0 **不**涵蓋什麼〉是**兩條不同的軸**，刻意不重述對方：

| 文件           | 軸       | 回答                                       |
| -------------- | -------- | ------------------------------------------ |
| HANDOFF 那一節 | **能力** | 「我的案子要無障礙／SBOM，該用哪條分支？」 |
| `SCOPE.md`     | **目錄** | 「這個目錄准不准出現在 v1 的樹上？」       |

⚠️ 加一份清單而不加守它的機制，就是在製造第五份手抄本 —— 這一版清楚知道自己
在做這件事，所以 `SCOPE.md` 裡**一個導出的數字都沒有**（全部列名字，不寫
「幾支工具」），而且文末就寫著「沒有任何機制在守這份文件」。掃 `git ls-tree`
比對的檢查排在 `v1.0.5`。

### 修正：README 的目錄樹列著一個不存在的目錄

`tools/sast/` **從來不在 `release/v1` 的樹上**。自寫的 SAST 汙點傳遞規則住在
`.semgrep/`，由 `tier2-security.yml` 用釘住 digest 的容器跑 —— 而 `.semgrep/`
在那棵目錄樹裡**根本沒出現**。一個假的項目、一個缺席的真項目，不知道多久了。

這正是這一版存在的理由：**`tools/doc-facts` 守的是數字，不是清單。**
一份被讀者當成「這個 repo 有什麼」的目錄樹，可以整行是假的而全套閘門照樣全綠。

### `HANDOFF.md` 承諾表補上 `tools/codemods`

承諾一的表裡 `api-surface` 那列一直寫著「破壞性變更必須附 codemod」，
但 `tools/codemods` 自己沒有一列。

⚠️ **`tools/doc-facts` 刻意沒有補進承諾表。** 原本的計畫是補，實際去補才發現
它不對應五條承諾中的任何一條 —— 它守的是那五條**寫下來的樣子可不可信**。
硬塞進某一條會重演 C70（同一份證據掛在兩條承諾上，而空著的那一條沒有人發現）。
它在 `SCOPE.md` 有獨立一列，理由寫在同一格。

### `DECISIONS.md`

- **C72** —— 判準怎麼定出來的：兩條看起來合理的判準各自砍錯東西，
  以及「守範疇的機制本身算不算範疇內」這個自我指涉的答案（算，因為它守的是
  交付物有哪些）。也記下搬運順序做反了的後果。
- **C71 加標註**，不改原文：那道閘門已於 v1.0.3 移出 v1，分析全部成立，
  只是解法住在 `main`。

---

## [1.0.3] — 2026-08-18

**移除 `tools/gate-roster`** —— 它在 40 分鐘前隨 v1.0.2 發布，而依這一版才定下來的
範疇判準，它不屬於 v1。

### 為什麼進來又出去

判準當時不存在。v1.0.2 發布之後才把它定下來：**看它守的東西給誰看** ——
面向交付物（含文件）在 v1 內，受益者是拉 v1 去做案子的團隊；面向開發流程的
在 v1 外，受益者是維護者。

`gate-roster` 守的是 CI 閘門清單四份手抄副本的一致性。**漂移傷的是維護者。**
所以它出局 —— 而它解決的問題是真的，工具會接到 `main`，那裡它更需要
（`main` 有 8 支 v1 沒有的工具，清單更長）。

⚠️ **v1.0.2 的 tag 與 release 不撤、歷史不改寫。** 這個 repo 不容忍的是
「歷史看起來比實際乾淨」—— v1.0.1 整版做的就是「把不實的敘述改成實的」。
它進來又出去這件事留在這裡，比抹掉它有用。

### 移除

- `tools/gate-roster` 整包，以及 `package.json` 的 `gate-roster` 別名與 `gate` 鏈裡那一段
- `tier1-quality.yml` 的「閘門名冊一致」那一步
- `tools/doc-facts` 的 `exports` 進入點，`workspacePackages()` 退回成 `derive.ts` 的內部函式
  —— 唯一的跨 package 消費者是 `gate-roster`。留一個沒有消費者的進入點，
  就是 `tools/gate-kit`（C73）拒絕先定義 `report()` 的同一個錯誤。
  函式本身留著，`workspacePackageCount()` 仍在用它。

### 保留

- **README〈兩層檢查〉Tier 2 那格的修正。** 那一格原本寫「一致性檢查 + ESLint
  安全規則」，而 Tier 2 實際上還跑 `api-surface`、`doc-facts`、SAST 與機密掃描。
  **那是一個真的錯誤被修好了，和 `gate-roster` 無關。**

### 新增

- **`HANDOFF.md`〈已知的誠實缺口〉第五條：四份閘門清單是手抄的。**
  移除之後這件事在 v1 回到零機制，所以它必須被寫下來 ——
  這個 repo 不容忍的不是「沒有閘門」，是「**沒有閘門卻讓人以為有**」。
  README 那張表上方的說明同步改掉：原本寫著「這張表不是手抄的清單」，
  移除之後那句話是假的。

---

## [1.0.2] — 2026-08-18

`platform/*` 的型別形狀一格沒動。這一版有兩件事：修掉 v1.0.1 修過的那個毛病的
**成因**，以及讓五條承諾裡最根本的那道閘門**第一次有辦法被測到**。

### 新增

- **`tools/gate-roster`：閘門名冊的單一事實來源，以及一道比對它的閘門。**

  同一份閘門清單被手抄在 `package.json`（`scripts.gate` 與各別名）、
  兩個 workflow、以及 README〈兩層檢查〉那張表，
  而在此之前**沒有任何東西在斷言它們一致**。

  這已經發作過兩次。v1.0.1 修的是第一次（`doc-facts` 只在 CI 跑，本機
  `vpr ready` 全綠而推上去 CI 紅）。那是修症狀：下一個人加一支工具，
  還是會漏在其中一份裡，而且不會有任何東西說話。

  第二次是寫這道閘門的當下抓到的，見〈修正〉。

  加一支 `tools/*` 而不登記會紅；登記了卻沒接進 workflow 會紅；
  接了卻沒寫進 README 那張表也會紅。刻意不當閘門的工具寫進 `UNGATED`，
  **理由必填**，這樣「漏接」與「刻意不接」才不會長得一樣（C41）。

### 修正

- **README〈兩層檢查〉那張表的 Tier 2 那格漏了兩道閘門。** 它寫著
  「一致性檢查 + ESLint 安全規則」，而 Tier 2 實際上還跑 `api-surface`
  與 `doc-facts`。這一格是讀者判斷「PR 會被什麼擋下來」的地方。
  現在它由 `vpr gate-roster` 守著。

### 變更

- **`tools/conformance` 的每一條規則現在都能被單獨 import 測到。輸出一字不差。**

  這支是 D4 邊界防護與 D9 防漂移的執行機制 —— 五條承諾裡最根本的那條。
  而在此之前它是 943 行的單一檔案，最後一行是 `process.exit(...)`：
  **一被 import 就跑完並結束行程**，所以裡面沒有任何一條判定測得到。
  能驗它的只有「起一個行程、掃整個 repo、比對 stdout」，
  而那種測試答得出「現在是綠的」，答不出「這條規則什麼時候會判錯」。

  判定搬進 `src/rules/*.ts`（8 支，每一條 `checkX(...): Finding[]`），
  `cli.ts` 縮到 117 行，只做「解析參數 → 收集 → 列印 → 離開」。
  既有的反向測試一條都沒改，仍然全過。

  **stdout 與結束碼一字不差** —— 六種情況逐位元組比對過，另外把改動前的 CLI
  抓回來做差分測試，補跑六個快照沒走到的分支（切片缺 `package.json`、
  空的 `features/`、非目錄檔案、`apps` 層…）。

  對要改這支工具的人，差別是：加一條自己的邊界規則（政府案很常見 ——
  某某目錄不准引用某某東西），現在可以寫一支不起行程的測試，
  而不是只能斷言「那個行程印了什麼」。

  ⚠️ **`Finding` 的形狀在這一版定下來**：`where`／`rule`／`detail`／`fix`。
  刻意沒有 severity —— 這支只有一種嚴重度，而 `main` 上的 `tools/gate-kit`
  當初拒絕先定義 `report()`，理由正是「沒有生產者就不要先發明接縫」（C73）。
  這支是第一個生產者，所以型別先住在生產者這一側。

- **`doc-facts` 匯出 `workspacePackages()`。** 「什麼算一個 workspace 套件」
  這條規則現在只有一份，`workspacePackageCount()` 改成呼叫它。
  判準是**目錄裡有 `package.json`**，不是「目錄存在」—— 差別在切過分支的
  開發機上是 16 比 7（見 C71）。

---

## [1.0.1] — 2026-08-18

`platform/*` 的型別形狀一格沒動 —— 這一版只把**不實的敘述改成實的**，
以及讓 `vpr ready` 名副其實。

### 修正

v1.0.0 是從 `main` **刪掉七支工具**做出來的，而刪掉的東西在文件與程式碼裡
留下了引用。歷史紀錄（本檔與 `DECISIONS.md`）保留原樣 —— 那是有日期的敘述；
其餘十三處改掉了：

- **`.claude/launch.json` 指向不存在的檔案。** 唯一的 preview 設定寫的是
  `tools/csp-verify/src/cli.ts`，而那支工具不在這一版 —— 任何團隊拉下來叫
  agent 跑預覽，第一步就失敗。改成 `apps/console` 的 dev server（port 5173）。
- **兩處「有東西在守」的假承諾。** `platform/pii/tests/mask.test.ts` 與
  `features/order/tests/masking.test.ts` 寫著 `tools/pii-check` 會掃到假資料
  並擋下 PR。這一版沒有那支工具 —— 改成寫明「靠約定，不靠閘門」。
- **兩處沒有東西在執行的規矩。** `apps/console/vite.config.ts` 與
  `renovate.json` 要求新的 vite plugin 登記進 `tools/exit-drill` 的
  `DRILL_PLUGINS`。改成寫明這一版要靠人記得。
- **四份文件叫人跑不存在的指令**（`vpr bff-check`、`vpr exit-drill`）。
  `platform/bff-contract` 的規格與 `platform/bff-mock` 這個參考實作都**還在**，
  不在的是把它跑成測試的驗收器 —— 兩份 README 都改成講清楚這件事。
- **四處指向不存在的程式當作參照**（`tools/compliance` 的對照表、
  `tools/exit-drill` 的 plugin 解析器、`tools/sast`）。
- **`tier2-security.yml` 有一整段 D8 契約的註解被留在 `doc-facts` 那一步上面** ——
  刪工具時刪了步驟、沒刪註解。

### 變更

- **`doc-facts` 加進 `scripts.gate`。** 在此之前它只跑在 CI 的 Tier 2，
  所以本機 `vpr ready` 可以全綠而推上去 CI 紅 —— 而 README 把 `vpr ready`
  講成「一次跑完所有檢查」。現在那句話是真的。

---

## [1.0.0] — 2026-08-17

第一個穩定版。**範圍是刻意縮小的** —— 五條承諾，其他能力留在 `main`。

### 這一版承諾什麼

1. **分工開發不受影響的系統架構** —— 垂直切片、三層邊界防護、切片產生器與
   一致性檢查共用同一份契約
2. **設計模板到前端工程的開發方式** —— 槽被宣告、元件真的讀到、宣告與預設表
   一致、各案覆寫得到：**每一段都有檢查**，而且是掃目錄的
3. **設計模板對應 vue component 的方式** —— 值→代幣、形狀→**具名槽**、結構→slot；
   槽名取自 reka-ui 的基元（也就是 shadcn 的 part 名）
4. **各案快速換配色與元件樣式** —— 代幣分兩層，`theme-verify` 真的建置兩次驗證
5. **基礎資安在撰寫時就被發現** —— 前置過濾器，不是交付的那份掃描報告

完整說明與**刻意不承諾什麼**：[HANDOFF.md](HANDOFF.md)。

### 變更（Changed）

- **`createUiTheme()` 的形狀**：`{ variants, sizes }` → `{ 元件名: { 槽名: class 字串 } }`
  - ⚠️ **breaking**：附 codemod `flatten-ui-theme-to-components`
  - 舊形狀是**按鈕的概念**長在一個全域 API 上。第二個元件 `UiDialog` 需要覆寫
    遮罩與內容框時沒有地方可去，於是它就沒有接縫 —— 寬度與位置寫死在模板裡，
    任何案子都換不掉，而**沒有任何閘門說話**
  - 在 tag 之前改，代價是一個呼叫端；tag 之後改，代價是每一個 fork、永遠
- **`platform/ui` 的元件契約改成掃目錄**：前一版的檢查 `readFileSync("UiButton.vue")`，
  寫死一個檔名 —— 它守的是一個檔案，不是一條規則

### 從 v0.7.1 移出（Removed）

以下能力**不是被刪除，是不放進這條產品線**。它們在 `main` 分支有完整實作
（含閘門、反向測試與文件），需要的案子請用 `main`：

- **無障礙**：`platform/eslint-config` 的 `./a11y` 匯出、Tier 1 的無障礙檢查步驟、
  以及無障礙驗收分工表
  - ⚠️ **breaking**：附 codemod `drop-a11y-config-for-v1`
- **法遵**：`tools/compliance`（個資法對照表、§16 證據清單）、`tools/pii-check`
- **供應鏈**：`tools/supply-chain`（盤點、SCA 例外申請書、鏡像清單、封閉網路
  前置條件）、Tier 2 的 SBOM 與 SCA 漏洞掃描
- **退出演練**：`tools/exit-drill`、`exit-drill.yml`
- **契約驗收**：`tools/bff-check`
- **CSP 實測**：`tools/csp-verify`
- **選型比較**：`tools/ui-survey` 與 `UI-SURVEY.md`

`tools/doc-facts` 隨之縮減：不再守供應鏈的五個數字，只守推導得出來的
API 進入點數、export 數、契約條目數、workspace 套件數、CI action 數、
CODEOWNERS 條目數。

### 已知缺口（Known issues）

不是 bug，是**已經知道而且刻意留著**的：

- `platform/ui` 只有 `UiButton` 與 `UiDialog` 兩個元件 —— 接縫通了，
  但能換的東西還不多
- `platform/pii` 的遮罩能力在，**強制它的東西不在**（那道檢查在 `main`）
- `bff-mock` 不是認證伺服器，正式環境請用組織的 gateway
- `tools/vue-typecheck` 用第二個 TypeScript（JS 版 5.x）

### 採用這一版的第一步

把 `CODEOWNERS` 的 `@org/*` 換成真的團隊。**在換掉之前，
擁有權治理是一份文字檔，完全沒有生效** —— GitHub 對不存在的團隊不會報錯。

---

## v0.1.0 – v0.7.1

腳手架的建構期。決策理由、實測校正與踩過的坑逐條記在
[DECISIONS.md](DECISIONS.md)（涵蓋範圍大於 v1.0.0）。
