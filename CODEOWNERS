# 擁有權對照（D12）。一致性檢查會驗證每個 features/* 都在此有條目 ——
# 沒有 owner 的切片＝沒人負責的切片。
#
# ⚠️ ── 採用時必須做的第一件事：把 @org 換成真的組織 ──────────────────
#
# 這份檔案裡的 `@org/*` **全部是佔位符**。在還沒替換之前，GitHub 判定
# 每一條都是 `Unknown owner`，也就是**這份 CODEOWNERS 一條都沒有生效** ——
# PR 不會自動指派審查者，保護分支的「需要 owner 核准」也沒有東西可要求。
#
# 而 `tools/conformance` 驗的是**條目存在**，不是**owner 有效** ——
# 存在不等於生效。這道落差本機看不出來，只有 GitHub 知道：
#
#     gh api repos/<owner>/<repo>/codeowners/errors
#
# 本 repo 2026-08-15 實測：**22 條全部 Unknown owner**（見 DECISIONS 的 C40）。
# 刻意不做成 CI 閘門 —— 那需要 GitHub API，會因為 token 權限與速率限制而變紅，
# 而不是因為真的有問題（與 tools/ui-survey 不進 gate 是同一條理由）。

# ── platform/ 由跨團隊抽調的 maintainer 把關（D12）────────────────────
# 任何人都能提 PR，但需 maintainer 核准。目的是既不製造瓶頸、也不放任無主。
# breaking change 必須附 codemod，並由提出者在同一 PR 跑完全 repo。
/platform/                  @org/platform-maintainers
/tools/                     @org/platform-maintainers

# 這三處定義的是安全行為本身（CSP 政策、認證與 CSRF 契約），不是一般平台程式碼。
# 放寬它們不會讓任何測試變紅 —— 只會讓標準變低，所以由資安共同把關。
/platform/security-headers/ @org/platform-maintainers @org/security
/platform/bff-contract/     @org/platform-maintainers @org/security
/tools/bff-check/           @org/platform-maintainers @org/security

# ── platform/ui 刻意**沒有**獨立條目 ─────────────────────────────────
#
# 它已經被上面的 `/platform/` 通則涵蓋（platform-maintainers）。
# 再寫一條 `/platform/ui/ @org/platform-maintainers` 是**完全等價的 no-op**，
# 而一個 no-op 配上一段說明，讀起來會像「這裡做過一個決定」—— 它沒有。
#（第一版就是那樣寫的，見 C40。）
#
# 真正待決的問題在 HANDOFF #14，而且它比「誰來當 owner」更具體：
#
#   **設計系統與應用外殼，是不是同一個團隊的事？**
#
# 因為候選已經在這份檔案裡了 —— 最下面的 `@org/team-platform-ui` 擁有
# `/apps/console/`。同一組人接手 `platform/ui` 是最省事的答案；
# 但那等於把「全公司產品長什麼樣」交給維護單一應用外殼的團隊。
#
# 要獨立 owner 的話，在這裡加一列就是了 —— 但**要加就要是真的決定**。

# supply-chain 的家族分級表刻意寫在原始碼裡（而不是 JSON），為的就是這一列：
# 一個新的原生工具鏈進到建置環境時，分類必須是一次**被資安看過**的變更。
# 這裡也放著 inventory.json / provenance.json 兩份基線 —— 它們是拿給稽核看的東西。
/tools/supply-chain/        @org/platform-maintainers @org/security

# 同一個理由，但更直接：map.ts 決定的是**這個組織對外宣稱自己遵循了什麼**。
# 把一條法規標成「已覆蓋」、或把一道閘門標成「已證明會紅」，是一次法遵聲明，
# 不是一次程式碼變更。COMPLIANCE.md 是產生的，改它要從這裡改。
/tools/compliance/          @org/platform-maintainers @org/security

# evidence.json 是一份「有人開瀏覽器驗過 CSP」的證據，而它的有效期由
# FINGERPRINT_PACKAGES 這一行決定 —— 從名單裡拿掉一個相依，等於讓一份
# 舊證據無限期續命。那是一次安全決定，不是一次重構。
/tools/csp-verify/          @org/platform-maintainers @org/security

# 個資：EXEMPT 一條就讓一個檔案不再被掃，personalData 少一個欄位就讓那個欄位
# 從此裸奔。兩者都是法遵決定，不是重構。platform/pii 的遮罩規則同理 ——
# 「留幾個字」是拿去回答稽核的那個數字。
/tools/pii-check/           @org/platform-maintainers @org/security
/platform/pii/              @org/platform-maintainers @org/security
# facts.ts 決定「哪些數字有人在守」。從登記表拿掉一條，那個數字就從此
# 可以自由漂移 —— 而它們是拿去跟採購與資安講的話。
/tools/doc-facts/           @org/platform-maintainers

# 規則放寬是靜默的：刪一條規則不會有任何東西變紅。
/.semgrep/                  @org/platform-maintainers @org/security
/tools/sast/                @org/platform-maintainers @org/security

/pnpm-workspace.yaml        @org/platform-maintainers
/vite.config.ts             @org/platform-maintainers
/eslint.config.js           @org/platform-maintainers @org/security
/.npmrc                     @org/platform-maintainers @org/security

# Tier 2 的三條規則（不快取、不過濾、有時間觸發）是刻意的，不是疏漏。
# 任何放寬都必須經資安核可 —— 理由寫在該檔開頭。
/.github/workflows/         @org/platform-maintainers @org/security

# ── 垂直切片：一片一個負責團隊 ─────────────────────────────────────────
/features/order/            @org/team-fulfillment
/features/shipment/         @org/team-logistics

# ── 應用外殼 ─────────────────────────────────────────────────────────
/apps/console/              @org/team-platform-ui
