/**
 * 巢狀工作樹的邊界 —— 兩份 ESLint 設定共用的那一列（C190 §二 1、2）。
 *
 * ── 換的是對象，不是判準 ─────────────────────────────────────────────
 *
 * 這棵樹的工作慣例是把並行 session 的 worktree 開在 `.claude/worktrees/`
 * （`.gitignore:33` 就是為它加的）。那底下躺的是**整棵樹的副本**，而兩份設定
 * 原本問的是「這個目錄樹裡的每一個檔案」—— 於是實測（C190 §四）主 checkout
 * 底下種一棵鄰居，列舉數兩支都恰好加倍：根設定 285 → 570、`a11y.js` 40 → 80，
 * 而 a11y 那一側的 RC 從 0 變 1，命中的是鄰居樹裡 `UiTextarea.vue` 的複本
 * —— 與主樹那一份逐位元組相同的檔案。CI 拿到的是乾淨 clone，所以它永遠是綠的：
 * **本機紅、CI 綠**（C41）。
 *
 * ⚠️ **這不是「改門檻求綠燈」（AGENTS.md 規則二）。** 規則二 管的是**判準**：
 * 23 條規則、`label-has-for` 的選項、`files:` 的豁免範圍、根設定的安全規則集，
 * 一個字都沒動。動的是**對象** —— 鄰居工作樹本來就不是「這個 checkout」。
 * 同一個處理在 `tools/pii-check/src/tracked.ts` 的檔頭（C182 §六）。
 *
 * ── 為什麼是黑名單，以及代價由誰付 ───────────────────────────────────
 *
 * `pii-check` 換到的是**結構性**答案：它問 git，而 git 不會走進另一個
 * repository —— 副本換個位置放照樣擋得住。**設定層買不到那個答案**：
 * flat config 的 `ignores` 只吃字串樣式，不吃函式。所以這裡只能是一條路徑
 * 黑名單，而 C182 §五 正是以「黑名單每一列都要有人先被咬一次」否決過
 * `pii-check` 的 `SKIP` 加一列 `.claude`。
 *
 * 代價由**另一支機制**付，不是由這條黑名單自己付：`tests/a11y.test.ts`
 * 的射程期望值改成問 git 之後，副本放在這條樣式**涵蓋不到**的位置時，
 * 兩側會對不起來而那支測試會紅（C190 §四（3）第二列，差集 35）。
 * 也就是說黑名單漏掉的位置是**吵的**，不是安靜的。
 *
 * ⚠️ **萬用字元開頭在這裡不是承重的。** 兩份設定各有兩個呼叫端（閘門從
 * repo 根跑、反向測試從 `platform/eslint-config/` 跑），而從 package 那一端
 * 跑時鄰居樹本來就落在 basePath 之外，`isPathIgnored` 加這一列之前就回 `true`
 * —— 那**是涵蓋，不是排除**（C190 §二 3、§四（4））。前綴照寫（成本為零，且
 * 符合 `a11y.js` 那段 basePath 教訓），但不要拿「兩端都綠」當成樣式形狀
 * 兩端都測過了的證據。
 *
 * ── 為什麼是獨立的 subpath，不塞進任何一份設定的身分裡 ────────────────
 *
 * 這一列**不得**放進 `src/index.js` 的匯出陣列讓 `a11y` 去繼承，也**不得**
 * 從 `./a11y` 匯出給根設定用：`package.json` 的 `description` 與 `a11y.js`
 * 的檔頭寫的是「兩份互不相干」—— Tier 2 的性質（只管安全與邊界、與 oxlint
 * 零重疊）正是它值得存在的原因，任一個方向都在那個宣告上開洞。
 *
 * 而樣式本身只寫一次：C189 才量過同一個字面散在四處、三處沒有東西比對它的
 * 下場。`.gitignore:33` 已經是這句話的第一份，這裡是第二份，不要有第三份。
 */
export default {
  ignores: ["**/.claude/worktrees/**"],
};
