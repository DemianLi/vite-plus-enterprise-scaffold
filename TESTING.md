# v1 的測試層級模型 —— 兩類測試，哪一層守什麼

> 這份文件給**兩種人**：**維護這個腳手架的人**，以及**拉 v1 去做案子的團隊**。
> 它回答一個問題：**這個腳手架的正確性，是靠哪幾層守的；哪幾層是你自己要補的。**
>
> 目錄該不該存在於 v1 的樹上 → [SCOPE.md](SCOPE.md)。
> v1 承諾什麼、刻意不承諾什麼 → [HANDOFF.md](HANDOFF.md)。
> 這三份各答各的問題，**不互相重述，只互相指過去**。

---

## 為什麼需要這份文件

`tools/` 底下每一支閘門，都是被一次具體的事故逼出來的，論證寫在 `DECISIONS.md`
的 C 編號裡。那個長法讓每一道都有理由，但也讓整體**沒有形狀** —— 全綠的時候，
它告訴你的是「沒有人破壞既有結構」，而不是「這東西做對了事」。

這份文件借用一組外部座標把既有的閘門排進去，好讓「還缺什麼」變成一個
可以回答的問題，而不是一種感覺。

**座標來源**：Robert C. Martin 於 2026 年 6–7 月在 X 的公開討論串所描述的五層
流水線（約束／單元測試／驗收測試／QA 程序／週期性手動測試）加一個檢查測試本身
的元層（突變測試）。

⚠️ **那是別人的座標，不是別人的門檻值** —— 他在同一批討論裡明說那些數值出自
他自己幾十年的判斷力，照抄數字是「下一個性質完全不同的賭注」。所以下面只借
層次，不借數字；本檔一個門檻值都不定，門檻值的定法見〈校準〉。

⚠️ **而且那組座標只描述了兩類測試裡的一類。** 他在回答的是「一個團隊怎麼在
不讀 diff 的前提下相信自己的程式碼」；**他沒有在回答「我做的東西要交給一百個
團隊用」。** 下一節那個二分，是這個腳手架的身分逼出來的，不在借來的座標裡。

---

## 一、兩類測試 —— 這份文件的組織原則

**五層是借來的座標軸，兩類是這個腳手架的身分。組織原則是後者。**

|                        | **第一類 · 框架測試**                                                         | **第二類 · 交付的測試設施**                    |
| ---------------------- | ----------------------------------------------------------------------------- | ---------------------------------------------- |
| 誰維護                 | 腳手架團隊                                                                    | 腳手架團隊建，**專案組使用**                   |
| 守什麼                 | 腳手架自身沒有壞                                                              | 專案組交付的業務是對的                         |
| 對象                   | `platform/*`、`tools/*` —— 那些每個案子都要重新搭一次、所以被抽出來共用的東西 | `features/*`、`apps/*` —— 各案自己的業務       |
| 它紅了，誰的問題被擋下 | 維護者的                                                                      | **拉 v1 的團隊的**                             |
| 成功長什麼樣           | 升級平台不會打斷任何一個案子                                                  | **專案成員只要補 TDD，就量得出業務功能完成率** |

⚠️ **第二類不是「腳手架的測試」，是「腳手架交付的測試能力」。** 這個分別決定
很多事：第一類的東西壞了，痛的是維護者；第二類的東西缺了，一百個專案組各自
用各自的方式補，而腳手架宣稱的「開箱即用」變成一句空話。

⚠️ **`SCOPE.md` 的判準對兩類的答案不同。** 那份文件問「這東西紅了，是誰的問題
被擋下來」—— 第二類**天然通過**（受益者就是拉 v1 的團隊），第一類要逐支論證。
`tools/gate-roster` 當年就是在這一格出局的（C72）。

---

## 二、五層 × 兩類

| 層        | 這一層問的問題                 | 第一類 · 框架測試                                                    | 第二類 · 交付的測試設施                                                         |
| --------- | ------------------------------ | -------------------------------------------------------------------- | ------------------------------------------------------------------------------- |
| 層 1 約束 | 程式碼有沒有纏成一團           | 有（C119）⚠️ 認知複雜度那一維沒有工具                                | 有（同一套設定隨腳手架下發），複雜度規則在根層 `vite.config.ts`                 |
| 層 2 單元 | 實作細節對不對                 | 有，**無下限門檻**（第一類那半等 #86）                               | 有下限，而它**只收在 usecase 那一層**（C120）—— 見〈門檻〉                      |
| 層 3 驗收 | **什麼叫做對**                 | **五條承諾守到一條**                                                 | 設施齊了，**第一片切片的規格還是產生器的範本**（#188／C165）                    |
| 層 4 QA   | 怎麼驗它真的能用               | 沒有                                                                 | 沒有，**且下發不了**（人做的事）                                                |
| 層 5 手動 | 規格本身有沒有寫錯             | 沒有                                                                 | 沒有，**且下發不了**（人做的事）                                                |
| 元層 突變 | 那些沒人審的測試是不是空頭支票 | 有（C121）⚠️ **不設門檻**，產出是清單不是分數；#145 那批手工斷言仍在 | 同一份設定涵蓋（`features/*`、`apps/*` 都在射程裡）；#145 那條在 `apps/console` |

⚠️ **看右邊那一欄。** 第一類做得相當紮實 —— 十幾道閘門，每一道附一支反向測試。
**第二類在 `v1.7.0` 之前幾乎整欄是空的**，而它才是「撐得住各個項目組拿去交付」這件事
的所在。

> **不安的來源可能不是第一類不夠好，是第二類從來沒有被設計過。**
>
> 那是這份文件初版最重要的一句話，也是 C114／C115 的起點。⚠️ **它現在描述的是
> 一段歷史，不是現況** —— 留著是因為那個發現比它的結論有用。

⚠️ **但「設施齊了」不等於「量得到」。** 這句話寫下時版控裡一份切片規格都沒有，
報表是空的、`tools/spec-report --check` 恆綠 —— **那條線證明不了自己。**
⚠️ `features/invoice` 進版控之後它跑起來了（#188／C165），而**恆綠那一段
一消失就撞出一個從 C115 就在的閘門互斥**：oxfmt 會排版這份產出物，
而空的報表沒有表格可以排。 ⚠️ 那不是欠項：**規格的內容由專案組自己寫**，
因為只有他們知道自己的生意認為什麼叫正確。分工的細節見〈四、第二類的現況〉，
這裡不重述。

---

## 三、第一類的現況

### 層 1 —— 約束

機械化擋下結構性問題，不靠提示詞、不靠人盯。落在這一層的：

| 機制                  | 擋什麼                                                  |
| --------------------- | ------------------------------------------------------- |
| `vp check`            | 格式（Oxfmt）、lint（Oxlint）、型別、**複雜度**（C119） |
| `tools/conformance`   | 切片契約、分層邊界、相對路徑逃逸、幽靈依賴              |
| `tools/vue-typecheck` | `.vue` 的型別（`vp check` 的 tsgolint 不看 SFC）        |

⚠️ **`eslint.config.js` 不在這一層**，雖然它形式上也是「機械化擋、不靠人」。
`platform/eslint-config` 檔頭寫得很清楚：它**只**管 oxlint 管不到的安全與邊界，
刻意零重疊、刻意不 extend 任何 recommended preset。層 1 問的是「程式碼有沒有
纏成一團」，安全規則不回答那個問題。

✅ **複雜度那一格已經補上（C119）** —— 函式大小、巢狀深度、參數個數各一條，
理由是「agent 會在自己製造的糾結程式碼裡繞不出來，最後得人下場解」。規則落在
根層 `vite.config.ts` 的 `lint:` 區塊（Tier 1），**不是** `platform/eslint-config`
—— 四條裡三條 oxlint 本來就有，而那一軌是安全閘門，複雜度不是安全。
產品碼與測試碼**兩組不同的數字**，另有一個 per-file 放行。

⚠️ **第四維「認知複雜度」是空的，不是漏掉。** oxlint 沒有這條規則，唯一的來源
會把一條相依加進 Tier 2 安全閘門。目前用**循環**複雜度（`complexity`）替代，
而它是替代不是填滿：`slice-gen` 的 `buildSliceFiles` 841 行、認知複雜度 0。

⚠️ **門檻是這棵樹此刻的觀測最大值** —— 依〈校準〉那三句話設的，此刻**不擋任何
既有程式碼**（實測：五條規則零違規）。它不是舒適區：第一次撞到就是一則 C 編號，
而那正是收緊的論證來源。

⚠️ **`<script setup>` 的 module 層在四維裡幾乎看不見**（只有 `max-depth` 例外）。
`platform/ui` 的 24 個零函式 `.vue`、合計 1992 行 script 在分佈裡是 0 ——
表上乾淨是因為量不到（#129 §六）。`vue/max-props` 補回「參數個數」那一格，
區塊行數那一格 oxlint 沒有對應規則，仍然空著。

⚠️ **`tools/conformance` 已經做完了依賴方向那一格。** 借來的座標把「分層邊界」
當成要新增的東西，在這個 repo 是**既有的**，而且做得更細。這一格不需要新工具。

### 層 2 —— 單元測試

「agent 寫、人不讀」的那一層。理由是這些測試驗的是 agent 自己端到端掌控的實作
細節，人讀它的邊際效益接近零。

`platform/*`、`tools/*` 底下都有測試，跑在 Vitest。
⚠️ **但沒有覆蓋率門檻**，所以「人不讀」在**第一類這一半**仍然沒有下限保護。
那一半的落點是 `main`（C117 §五），實作等 #86 解凍；切法、數字與理由已經寫成
裁決（C120 §七）—— ⚠️ **而它的第一句是「先不設數字」**：七支 tools 走子行程跑
自己的 CLI，v8 只看得到 in-process 的執行，分母現在是壞的。

第二類那一半已經落地，但它**不是這一格的翻版** —— 見〈門檻〉。

### 層 3 —— 驗收規格

它要的是：用自然語言寫、不夾帶 code-level 產物、**由人逐字或抽查**的規格。
它是唯一一層在回答「什麼叫做對」。

**五條承諾裡守到一條**（`v1.8.0` 起）：`specs/promise-1-architecture.feature`
用中文寫下承諾一具體指什麼，`tools/promise-check` 照著它把一份切片副本
真的弄壞、跑規格指名的閘門、比對結果。**規格是唯一來源，閘門讀它** ——
不是一份描述閘門行為的說明書（那會與閘門分岔，而分岔的那天沒有人會知道）。

⚠️ **這一格買到的是可讀層，不是新的驗證能力。** 那幾道閘門本來就在驗那些事；
規格買到的是「人不必讀閘門原始碼就知道承諾是什麼」，以及**承諾與閘門的綁定
從此有機制在斷言**（HANDOFF 那句「承諾與閘門對不對得上，只有人讀得出來」
就是這樣被改掉一部分的）。經過與剩下四條的處置見 `DECISIONS.md` 的 C118。

⚠️ **`specs/` 底下第二份規格不是第六條承諾。** `gate-thresholds.feature`（C163／C164）
寫的是「複雜度門檻不會悄悄過期」，由 `tools/threshold-check` 執行 ——
它守的是**量那五條用的尺**，不是第六種能力，所以檔名刻意不走 `promise-N-`，
上面那個「守到一條」也因此一個字都不用改。**判準與它為什麼沒有機制在守，見 C164。**

其餘四條承諾仍然只有人讀得出來。除此之外最接近這一層的是契約測試
（`platform/ui/tests/component-contract.test.ts` 掃目錄驗每一個元件、
`apps/console/tests` 真的建置一次再去產物裡找）。那些是好測試，但它們斷言的是
**機制有沒有生效**，不是**這門生意認為什麼叫正確**，而且沒有一份是人打算逐字讀的。

⚠️ 這一層**在第二類那一側**仍然缺席，而那是「全綠仍然不能讓人相信」的直接
原因 —— 前面兩層與元層全部建立在「規格寫對了」的前提上。

### 層 4 QA 程序 ／ 層 5 週期性手動測試

**這兩層下發不了**，兩類都一樣。它們是人做的事，不是可以打包進 `platform/*`
交付的東西。腳手架能交付的最多是層 1、層 2、層 3 與元層。

⚠️ 對拉 v1 去做案子的團隊來說，這兩層是**你們自己的責任**，而且借來的座標裡
最真實的那個破綻正落在你們身上：**規格如果是 AI 寫的，而它斷言了錯的行為
（不只是漏掉案例），抽查很可能抓不到。** 層 5 存在的唯一理由就是兜住這個。
這一段在 HANDOFF 有對應的一節，本檔不重述。

### 元層 —— 突變測試

問的是：那些沒有人審的測試，怎麼知道不是空頭支票。答案不是人工閱讀，是**刻意
在原始碼注入錯誤，看測試會不會失敗**。

⚠️ **已經在做，只是手工的、而且只對著閘門自己。** 支撐這句話的是三處，各自可查：

| 出處                                         | 它做什麼                                                                       |
| -------------------------------------------- | ------------------------------------------------------------------------------ |
| `.semgrep/rules.ts`                          | 故意寫壞的 fixture，存在的唯一目的是讓「規則到底有沒有在檢查」變成可執行的問題 |
| `tools/api-surface/tests/negative.test.ts`   | 自稱反向測試，用兩種破壞法（改基準檔副本、改 fixture 原始碼），斷言閘門會紅    |
| `tools/vue-typecheck/tests/negative.test.ts` | 檔頭寫明「每一條都先讓 fixture 壞掉，再證明這支工具會說話」                    |

HANDOFF 那句「一支只 grep `@theme` 有沒有寫的測試，量的是『有沒有寫』不是
『有沒有生效』」講的是同一件事。

⚠️ **覆蓋面已經跨出閘門了 —— 第一批三處（#145）**，而它們**不是**手寫想出來的，
是機械跑一遍掉出來的（#136）：

| 出處                                     | 被殺掉的那個變異                            | 它守什麼                             |
| ---------------------------------------- | ------------------------------------------- | ------------------------------------ |
| `platform/pii/tests/mask.test.ts`        | `at <= 0` → `at < 0`、`length <= 4` → `< 4` | 邊界上隱碼函式**完全不遮**           |
| `tools/spec-report/tests/report.test.ts` | `(完成 / total) * 100` → `/ 100`            | 完成率報表最外層那個數字             |
| `apps/console/tests/bff-routes.test.ts`  | `extraPermissions = [X]` → `[]`             | 絆線掛在被守的對象上，不是掛在自己上 |

⚠️ **`platform/pii` 那兩條是在四維 100% 覆蓋率底下掉出來的** —— 覆蓋率的門檻
（〈門檻〉那一節）擋不下這一類，而這就是元層存在的理由。

### ✅ 機械化已交付（C121／#150）—— 而它的產出是一份清單，不是一個數字

設定在樹上：根層的 **`stryker.config.mjs`**。

```
pnpm exec stryker run                          # 全樹一趟，1 分 30 秒
pnpm exec stryker run -m 'platform/pii/src/**' # 只看一個 package，秒的量級
```

⚠️ **它不擋任何東西，而那是一則有論證的裁決不是拖延。** `thresholds.break` 是
`null`（`high`／`low` 只決定報表顏色），閘門聚合腳本與發版前檢查一個字都沒改。
理由：分數下降的原因裡至少四種與程式碼品質無關 —— 新程式碼讓分母變大、等價變異、
結構性量不到的 package、時間相依的 timeout 判定。**一道經常因為無關原因變紅的閘門，
結局是被關掉，而被關掉的閘門連清單都產不出來。** 收緊的前置條件列在 #150。

第一次的基準（10.0.0，單趟全樹）：**78 支產品檔、6,125 個 mutant、0 errors、
1 分 30 秒**，total **32.02%**／covered **71.90%**，**692 個存活**。逐 package 的分佈在 #136。

⚠️ **`ignoreStatic: true` 開著，而那是安全性的不是效能的。** 靜態 mutant（在模組載入時
被執行到的）沒辦法歸屬到某一條測試，所以工具對每一顆都跑整套測試 —— 而整套裡有一支
會 spawn 真正的產生器 CLI，它底下的 bingo 在某些情況下會 `git add -A` ＋ `git commit`。
**實測：不開這一條跑一趟全樹，樹上多出一個 `feat: initialized repo ✨` 的 commit，
內容是整棵被注入過的樹（200 個檔），落在當時所在的分支上，而沒有任何閘門會看到它。**
開著它，那支測試在變異階段一次都不會跑（`(covered 0)`），順帶時間從 4 分 0 秒降到 1 分 30 秒。

⚠️ **代價**：827 顆靜態 mutant（全部的 14%）因此**不被測試**。關掉這條時它們裡面有
491 顆是真的被殺掉的 —— 那些訊號沒了。但同一批裡也有**假的存活**：`platform/pii` 的
`granularity: "grapheme"` → `""` 會讓模組載入時丟 `RangeError`，手工套上去是「整支測試檔
載不起來、零條測試跑」，而工具記成 **Survived**（17 條在別的檔案裡的測試通過、零條失敗）。
**「讓模組載不起來」的變異會被記成存活** —— 這是讀那份清單時的第二條守則。

⚠️ **同一條守則的第二個機制，而它的範圍大得多（#157）：有一整批測試，這個工具
一顆都算不進去。** 版控裡 49 支 `.test.ts`，報表的 `testFiles` 只看得見 35 支 ——
**14 支完全不可見**，而原因有三個不是一個：**子行程型**（4 支 —— mutant 靠行程內的
全域啟動，子行程繼承不到，於是那些測試對每一顆 mutant 都是綠的，無論它們多強）、
**在三個被排除的 package 裡**（5 支）、**檔案內容型**（5 支 —— 實查 `import` 產品碼
**零次**，它們把程式碼與文件當成資料讀，而這個工具改的是被 import 執行的東西）。

⚠️ 實測：`file-mode.ts` 有三顆記成 Survived，手工套上去跑，`output.test.ts` 當場紅；
`api-surface/src/docs.ts` 的三顆存活**全部**死在那支 45.5 KB 的 `negative.test.ts` 手上。
**三個原因不能混成一個** —— 混起來的下場是下一批用錯的理由重讀同一堆。

⚠️⚠️ **整包量過一次，而數字不是零頭**：`tools/conformance` 的 **237 顆存活裡，72 顆
（30%）其實被那兩支子行程測試殺得掉**。而且逐檔的假存活率從 4% 到 57% 不等
（`scan.ts` 8/14、`csp.ts` 1/28），**所以它不是一個可以拿平均值折算掉的東西** ——
要知道某一支的真實存活數，只能把變異套上去跑一次。

⚠️⚠️ **上一版這裡寫錯了一句，而錯的不是細節是那個推論本身（#163 撤回）。**
原句說報表上「覆蓋高、殺 0」的三支測試檔是**執行到了卻沒有一條斷言擋得住任何改動**。
量過之後：**三支沒有一支是那個形狀**，而**逐測試檔的擊殺數根本量不到「這支測試守得住
什麼」** —— 它有三個獨立的缺陷疊在一起：

1. **報表那兩個數字是「變異 × 測試」的配對數，不是變異數。** 唯一變異算下來是
   118→**59**、66→**22**、50→**1**。`cn.test.ts` 那個 50 是**同一顆變異**被
   五十條測試覆蓋到。
2. **`killedBy` 只記一支測試 —— 實測 1,820 顆被殺的變異，`killedBy` 長度
   全部是 1。** 所以逐檔的擊殺數量到的是**歸屬**，不是斷言強度：同一顆變異
   被記上的那支拿走全部功勞，其餘每一支都記 0，無論它們自己殺不殺得掉。
   （⚠️ 「被記上的是哪一支」沒有量 —— 量到的是「只有一支」。）
3. **`Ignored`（靜態）的變異一次都不會跑**（`ignoreStatic`，見上）。守的東西
   全部在模組載入期的測試檔，因此沒有任何一顆可殺。

**量得到那個問題的做法只有一個：把變異套到磁碟上，然後只跑那一支測試檔。**
（對照組齊備：未套變異必須全綠、已知會殺的必須紅、已知殺不掉的必須綠。）結果：

| 測試檔                                        | 報表 覆蓋／殺 | 唯一變異 | **單獨跑實際殺掉**         |
| --------------------------------------------- | ------------- | -------- | -------------------------- |
| `platform/ui/tests/cn.test.ts`                | 50／0         | 1        | **33／33**（`cn.ts` 全部） |
| `tools/promise-check/tests/cli.test.ts`       | 118／0        | 59       | **43／92**（`spec.ts`）    |
| `apps/console/tests/composition-root.test.ts` | 66／0         | 22       | **5／24**（`register.ts`） |

⚠️ **`cn.test.ts` 量到滿分，而報表給它 0。**（⚠️ 這裡只量了三支，
所以不寫「全樹最強」—— 那會是這一段自己在撤回的那種話。） `cn.ts` 的 33 顆有
**32 顆是 `Ignored`**（那張分族登記表是模組載入期的常數），手工套上去 **32 顆死在
斷言手上、第 33 顆讓模組整個載不起來**。這**不是**上面那 14 支「工具看不見」的
任何一種 —— 它在 `testFiles` 裡看得見。它的近親是那 27 個量不到的 SFC：
**被守的對象結構性地不在射程裡**，不是測試弱。
⚠️ 所以 #163 **沒有替它補任何斷言**：33／33 已經是滿的，補上去的只會是空頭手勢 ——
而空頭手勢正是這一層存在要抓的東西。

⚠️ **`cli.test.ts` 的「殺 0」是兩個機制疊起來的，而子行程只佔小的那一份。**
43 顆實際的擊殺裡，**39 顆同時死在純行程內那條斷言手上**（`parseSpec` 直接 import），
**只有 4 顆（9%）非得走子行程才碰得到**（`spec.ts:162` 的
`expectRed: thenMatch[1] === "紅"` 那四顆）。主因是上面第 2 條，不是子行程。
順帶：報表記成 `Ignored` 的 13 顆裡有 **7 顆**其實被這支殺得掉。

⚠️ **真正的缺口只有一個，而它在第三支。** `composition-root.test.ts` 單獨跑
只殺 5／24 —— 其餘 17 顆死在 `define-feature.test.ts` 手上（不是全樹缺口），
但 `register.ts:51` 的 `names: features.map((f) => f.name)` → `() => undefined`
**在全樹那一趟裡是活的**，而 `RegisteredFeatures.names` **整棵樹零處消費**：
它壞掉不會讓任何畫面出錯，也不會讓任何別的斷言變紅。#163 補的那一條**從聚合的
其餘三格反推命名空間**（刻意不抄 `features.map(...)` 當基準），把它殺掉，
順帶多殺兩顆 —— **5／24 → 8／24**。

⚠️ **這一條也用工具自己的判定收尾，而那是必要的**：手工套到磁碟上與這個工具跑
不是同一件事（見下），所以「全樹那一趟裡是活的」與「現在殺得掉」不能一半一半
各用一種方法。`stryker run -m 'platform/slice-kit/src/register.ts'` 重跑：
**24 顆全殺、0 存活**（原本 23 殺／1 存活），而 `killedBy` 記的正是新加的那一條。
⚠️ 同一支還揭出第三個機制：`apps/console/src/features.ts` 唯一那顆變異被記成
`Ignored`（靜態），**而它其實一直被這支測試殺著**（`至少註冊了一個切片` 那一條）。

⚠️ **三支 package 不在射程裡，而「沒有數字」不等於「沒有問題」**：`tools/vue-typecheck`
（Stryker 為了讓自己的 mutant 不製造型別錯誤而關掉型別檢查，正好關掉那支閘門存在的
理由）、`platform/config` 與 `platform/security-headers`（它們在射程裡會把一支需要
`process.chdir()` 的測試拉進 dry run，而 **dry run 一支紅就整趟中止** —— 留著它們等於
整棵樹一個數字都沒有）。三支的行覆蓋率分別是 58.25%／66.66%／70.58%。

⚠️ **`.vue` 不在射程裡**，所以設計系統那個 package 只被量到一小角（27 個 SFC
一個 mutant 都沒有）。這與〈層 1 約束〉那一格記的「`<script setup>` 在四條複雜度規則裡
不存在」是**同一個位置的第二次**。

⚠️⚠️ **跑完還有一件 `git status` 看不到的事（#163 實測）：根目錄多一支
`stryker-setup-<N>.js`。** 它在 `.gitignore` 裡（所以版控是乾淨的），但 ESLint
的忽略清單沒有它，於是 `pnpm gate` 會因為它裡面兩個「沒用到的 eslint-disable」
**在 `--max-warnings=0` 底下退出 1** —— 而那是一個看起來與你剛改的東西毫無關係的
紅燈。⚠️ 這一趟是**正常跑完**的（`Done in 3 seconds`、`Resetting your original
files`），不是中斷；工具就是不收這一支。`rm stryker-setup-*.js` 就好。
**刻意不把它加進 ESLint 的忽略清單** —— 理由與檔案模式那一條同一則（C121 §六）：
這一版不為量測工具的殘留放寬任何一道閘門。

⚠️ **跑完看一眼 `git diff --summary`**（⚠️ **只跑一個檔的 `-m` 也照樣掉** ——
sandbox 還原的是整棵樹，#163 實測）：`inPlace` 還原時不保留檔案模式，九支命令列
進入點的可執行位會掉，而**逐行 diff 是零**，沒有任何閘門在看檔案模式。
`git checkout -- tools/` 還原得掉。三個候選（跑完加一步檢查／改用非 inPlace／
接受並寫進文件）選了第三個：第二個被「範疇檢查的測試拿當前的樹當 fixture」擋死
（非 inPlace 會複製整棵樹，於是它對 sandbox 多出來的檔案報幾十個違規），
第一個等於新增一道閘門，而這一版明確不新增。

**剩下的是把第一批（#145）那三處擴到其餘的產品碼** —— 而那是這個能力的**用途**，
不是設定的交付。第二批（#157）讀完兩支檔案的全部存活：`platform/pii` 六顆與
`file-mode.ts` 二十四顆，補上四條斷言後後者掉到十二顆。第三批（#163）換了入口 ——
從報表的「覆蓋高、殺 0」進 —— 而交付的是**上面那則撤回**：那個入口量不到它宣稱
要量的東西，真缺口只有一條（`register.ts:51`）。

⚠️ **所以第四批的起點還沒有**，而缺的不是時間是方法：存活清單這條路第二批已經
走過，而報表另外那個入口在上面被撤回了。**在有新的挑法之前不要開下一批** ——
按著沒有依據的清單補斷言，正是這一層存在要抓的那個形狀。

⚠️ **重跑一趟是 717 個存活，而 692 + 24 = 716 —— 那第 25 顆沒有出處。** C122 讓
`file-mode.ts` 的 94 顆進了射程（其中 24 顆存活），解釋得了 24；中間隔了好幾個 PR，
別的檔案也可能漂了，而**這裡不把「大概是」寫成「是」**。要對得起來只能拿兩份
報告相減（`stryker.config.mjs` 記了相減的鍵不能含絕對位置）。

⚠️ **可行性已經實測過（C108）**：`platform/slice-kit` 跑得起來，
零錯誤零逾時，時間在秒的量級。兩個真阻斷（TypeScript 7 移除了 Stryker 依賴的
compiler API、isolated node_modules 讓 runner plugin 找不到）都有繞法。
⚠️ 實測順手掉出一個**真缺口**：`register.ts` 有一行被改壞之後測試照樣全綠 ——
覆蓋到了，但沒有任何一條斷言那個回傳值的內容。**手工反向測試守的是閘門自己，
守不到產品碼裡這種空頭斷言。**

⚠️ 這件事本身留下一條教訓：本檔初稿在這裡寫過「module-alias 會破壞 sandbox」——
那是照方法來源的風險清單抄的，而這條線根本沒有 path alias。
**借座標會連別人的風險清單一起借過來，那跟門檻值一樣是別人專案的產物。**

---

## 四、第二類的現況 —— 這一節是本檔真正的產出

第二類要成立的那句話是：**「專案成員只需要補 TDD，就能測出業務功能完成率。」**
逐段拆開看它現在靠什麼支撐：

| 那句話的哪一段       | 現在靠什麼                                                      | 夠不夠               |
| -------------------- | --------------------------------------------------------------- | -------------------- |
| 「腳手架內建安裝好」 | `slice-gen` 產生切片時附測試骨架＋驗收規格範本（C114）          | ✅ ⚠️ 入口只有這一個 |
| 「專案成員只需要補」 | 規格範本示範三態；`AGENTS.md` 的四條契約                        | ✅                   |
| 「測試出」           | Vitest 裝好了、跑得動；規格 runner 進切片自己的相依             | ✅                   |
| 「業務功能完成率」   | `tools/spec-report`：規格通過率 ＋ 四態 ＋ 進版控的台帳（C115） | ✅                   |

### 完成率的量法：驗收規格的通過率

**覆蓋率量的是「程式碼被跑過」，那回答不了「功能做完了沒有」。** 能回答後者的
是層 3：專案組用自然語言寫下「這個功能叫做對是什麼樣子」，跑起來，**綠幾條
就是完成幾條**。

⚠️ **分工在這裡必須講清楚，否則第二類會變成第一類的翻版**：

> **腳手架交付的是那一層的設施與範本** —— runner、testing API、`.feature` 骨架、
> 完成率的計算方式。**規格的內容由專案組自己寫**，因為只有他們知道自己的生意
> 認為什麼叫正確。

⚠️ 這一格同時也是「人與智能體協作」的落點：**規格是人寫給 agent 讀的需求，
通過率是 agent 交回給人的進度。** 人審的是規格（什麼叫做對），不是 diff
（它怎麼寫的）—— 那正是這組座標最初的主張。

### 完成率的分母怎麼算

`loadFeature` 載兩次：一次**不**過濾（算分母、列出待辦），一次過濾（跑該跑的）。
**兩次的差集就是待辦清單。** 場景大綱按例子的每一列各算一個實例 —— 這與
「應用場景和場景內的某一種流程通過就加一」的計數定義一致，而 runner 本身
就是這樣展開的（C110 §三）。

### `slice-gen` 是第二類的入口，這件事值得標明

它產生的切片自帶 `tests/` 與一支測試，`files.ts` 的註解寫著
「沒有測試的切片＝沒有人能安全重構的切片」。

⚠️ **一度有一個裁決要刪掉它**（理由：「一致性機制，不是測試層」）。那是用五層
的尺量出來的結果，而五層只描述第一類 —— 用兩類的框架重量，它是第二類目前
**唯一**的入口。裁決已反轉，經過見 `DECISIONS.md` 的 C109。

---

## 五、五層量不到的機制 —— 用兩類再量一次

這一節記的是一次判準的更換，因為那個更換本身比結論重要。

| 機制                 | 用五層的尺                       | 用兩類的框架                                                 |
| -------------------- | -------------------------------- | ------------------------------------------------------------ |
| `tools/api-surface`  | 「跨版本相容，五層沒這維度」     | **第二類** —— 平台一改就打斷每一個切片，這道閘門是他們的保險 |
| `tools/codemods`     | 「修復工具，不是閘門」           | **第二類** —— 升級平台時不必逐處手改                         |
| `tools/slice-gen`    | 「一致性機制，不是測試層」       | **第二類的入口** —— 專案組拿到測試骨架的唯一管道             |
| Tier 2 安全整條      | 「安全不在五層的題目裡」         | **第二類** —— 專案組寫業務時的前置過濾                       |
| `tools/theme-verify` | 「可替換性，不是程式碼對不對」   | **第二類** —— 換配色是各案的第一件事                         |
| `tools/doc-facts`    | 「文件斷言為真，沒有執行期後果」 | **第二類** —— 那兩份文件是交付物，他們照著做事               |
| `tools/scope-check`  | 「範疇治理，座標裡沒這格」       | 第一類 ＋ 第二類（fork 之後那張表就是他們自己的清單）        |

> **左欄那些「對應不上」，共同的原因只有一個：借來的座標整個只描述第一類。**
> 拿它去判第二類的機制的生死，等於拿尺去量溫度。

⚠️ **這不是說五層沒用。** 它把層 3 的缺席照出來了，而那是這份文件最重要的發現 ——
一維的座標找到了真缺口，二維的框架擋下了一次錯誤的刪除。兩件事都要記著。

### ⚠️ 而有一格是兩把尺都量不到的 —— 閘門回綠燈的方式

上面那張表問的是「這個機制為誰存在」。兩把尺都答得出來，因為兩把尺量的都是
**機制的用途**與**產品碼**。都沒有問的是另一件事：**這道閘門回綠燈的時候，
它有沒有真的把自己那個問題問出口。**

⚠️ **這一格不是〈元層〉的題目，儘管它們聽起來像。** 突變測試問的是「跑到的
程式碼，斷言夠不夠強」。而這裡壞掉的閘門**斷言強度是滿的** —— 它只是對著空氣
斷言：問題沒有被問出口，答案自然沒有反例。**強度滿分與問題不存在，在報表上
長得一模一樣。**

各自可查，而它們**不是**同一個成因：

| 出處                                    | 綠燈是怎麼回來的                                                   |
| --------------------------------------- | ------------------------------------------------------------------ |
| **C122**（檔案模式）                    | 守它的那條接線測試恆真 —— 斷言寫了，而它對任何輸入都成立           |
| **C124**（閘門看不看得見那份副本）      | 閘門讀的是真樹不是被弄壞的副本，於是「弄壞了會不會紅」問的是別的樹 |
| **C125／C126**（旗標）                  | 旗標打錯一個字母被靜默忽略，該跑的射程沒跑，退出碼照樣是零         |
| `gate-roster` 的 `UNGATED`（`e43f660`） | 名冊指向的票已經到期，而過期的是**指向**本身，不是被指的東西       |

⚠️ **上表的共同形狀是「問題沒有被問出口」，而不是「答案錯了」。** 五層問的是
「哪一層在守」，兩類問的是「為誰而守」；**兩個問法都預設了那道守衛真的執行過**。
預設不成立的時候，兩把尺同時失效，而失效的樣子是全綠。

⚠️ **補法目前是個案的，不是一層。** C125／C126 讓那批 CLI 一律拒絕不認得的旗標
（範圍以那則裁決為準，**這裡不重抄**），C124 留下一支兩趟 argv 對稱的差分探針 ——
前者修掉了一個成因，後者**偵測**另一個成因。**沒有一列是被同一件東西擋掉的**，
也沒有任何機制在問「這張表還缺哪一列」。所以這一格寫在這裡而不在〈六、缺口〉
的表裡：**那張表的每一列都答得出「補什麼」，這一格答不出來。**

---

## 六、缺口 —— 以及補法

按「補了之後改變什麼」排序，不按層數。

| 缺口                | 屬於哪一類 | 補什麼                                                                                                                                                                                                                                                                                                                                                                                   | 改變什麼                                                       |
| ------------------- | ---------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------- |
| **層 3 · 完成率**   | **第二類** | ✅ 設施與範本（C114）＋ 完成率報表（C115）                                                                                                                                                                                                                                                                                                                                               | 讓「業務功能完成率」第一次變成可量的東西                       |
| **層 3 · 框架承諾** | 第一類     | ✅ 承諾一（C118）：`specs/` 的規格 ＋ `promise-check` **照著它執行**；⚠️ 還有四條                                                                                                                                                                                                                                                                                                        | 新的**可讀層**（人讀規格不必讀閘門實作），⚠️ 不是新的驗證能力  |
| **層 1 複雜度**     | 兩類分開設 | ✅ 函式大小、巢狀深度、參數個數＋循環複雜度（C119）；⚠️ 認知複雜度那一維沒有工具                                                                                                                                                                                                                                                                                                         | 擋下 agent 把自己纏住，在它纏成一團之前                        |
| **層 2 覆蓋率下限** | 兩類分開設 | ✅ 第二類：`src/usecases/**` 四維 100，跟著 `vp test` 跑（C120）；⚠️ 第一類等 #86                                                                                                                                                                                                                                                                                                        | 讓「有 usecase 卻沒有規格在驗」這件事變成紅燈                  |
| **元層機械化**      | 第一類     | ✅ 第一批（#145）：三處真缺口的斷言，**是測試不是工具**；✅ 設定進版控（C121）：`stryker.config.mjs`，**不設門檻、不擋任何東西**；✅ 第二批（#157）：兩支檔案全讀 —— 四條斷言，外加**假存活的第二個機制（子行程，14 支測試檔不可見）**；✅ 第三批（#163）：**換掉的是挑入口的方法** —— 逐檔擊殺數量到的是歸屬（`killedBy` 只記一支），斷言只補了一條（`register.ts:51`，整棵樹零處消費） | 讓「不審 AI 寫的測試」有依據 —— 而依據是一份清單，不是一個分數 |

⚠️ **層 4 與層 5 不在這張表裡，因為它們補不了** —— 見〈層 4／層 5〉那一節。

⚠️ **第三格的「兩類共用」判錯了，而那是動手才發現的。** 原本寫共用，理由是
複雜度對兩類是同一件事；實測的分佈說不是 —— 測試碼在巢狀深度與循環複雜度上
比產品碼乾淨得多，卻在函式大小上有一條產品碼沒有的長尾（`describe`／`it` 的
callback，最大 455 行）。**共用一組數字會在一個維度太鬆、另一個維度把人卡死**，
而那正是本節最後一段對層 3 說過的同一句話。

⚠️ **第五格的進度不是「又補了幾條斷言」，是少掉一個挑入口的方法（#163）。**
第一、二批的入口是報表的存活清單；第三批改從報表的另一個入口進 ——「覆蓋高、
一顆都殺不動」的測試檔 —— 而量完之後撤回的是**那個入口本身**：逐測試檔的擊殺數
量到的是**歸屬**（`killedBy` 只記一支測試，實測 1,820 顆全部長度為 1），不是斷言
強度，報表指的三支**沒有一支**是它宣稱的那個形狀（33／33、43／92、5／24）。
真缺口只有一條，補在 `register.ts:51`。**所以這一格往下要先有新的挑法，才有
下一批** —— 三個缺陷與量法見〈✅ 機械化已交付（C121／#150）〉。

⚠️ **這一段原本寫的是「『屬於哪一類』不等於『落在哪一條分支』—— 剩下四格各自落
`release/v1` 還是送 `main`」。那個選擇不存在了**：兩條線 2026-08-26 併回一條、
`release/v1` 已刪（C133）。這張表往下不再有分支要判。

⚠️ **但 C117 沒有整則過期。** 判的時候**判準自己**出了什麼問題，見 `DECISIONS.md`
的 **C117** —— **這裡不重述**。

⚠️ **第一格排在最前面，是這一版改動最大的地方。** 先前的排序把「對腳手架自己的
承諾寫規格」放在第一位；用兩類重排之後，那一格買到的是**可讀層**而不是**驗證
能力**（閘門本來就在驗那些承諾），而第二類的完成率當時**沒有任何東西在做**。
⚠️ 那句話已經過期一半：`features/invoice` 讓分母第一次非零（#188／C165），
而它的規格內容今天仍然是範本 —— **規格由人寫，agent 只做骨架與接線**（規則四）。

### runner 與門檻

runner 用 `vitest-cucumber`，跑在既有的 Vitest 裡。⚠️ **不養第二套 runner** 的
理由不只是省事：跨套件測試在這條線上已經有排程相依（C87 用 `dependsOn` 讓開），
第二套 runner 會再撞一次同一個問題。

⚠️ **已經最小實測過（C110）**：中文關鍵字、中文步驟、場景大綱按例子列展開、
背景資料表、標籤過濾、以及「該做了卻沒綠就紅」的反向測試，全部成立。
**但有一行設定是必要條件**：`.feature` 裡的 `# language:` 標頭**本身不生效**，
而 `@待辦` 的場景若沒被 `excludeTags` 排除，runner 會要求它也要有接線。
兩件事都靠同一行程式解決，而**那一行必須由 `slice-gen` 模板生成** ——
靠專案組記得寫，第二類就會在第一個切片上壞掉。

⚠️ **模板還要負責另外兩件事（C111）**：`loadFeature` 的路徑是 **package 相對**
（`specs/…`，不是從 monorepo root 起算 —— `vp run <pkg>#test` 的 cwd 是 package
目錄）；runner 必須列進**切片自己的** `devDependencies`，不能靠 workspace 根目錄
提升 —— 幽靈依賴檢查只掃 `src/`，接線檔在 `tests/` 底下溜得過去，而那種相依
**在乾淨重建時不成立**。

⚠️ **`features/<切片>/specs/` 與 `src/usecases/` 兩個新目錄，`conformance` 都接受**，
而且反向測試證明它確實在看新層（相對路徑逃逸被抓、訊息指到正確的檔案）。

### ✅ 設施已交付（C114）—— 以及它買到什麼

`slice-gen` 現在產出的切片**開箱即綠**（實測：20 條測試，其中 15 條從規格來），
規格打的是新的 `src/usecases/` 層，而 **composable 呼叫的是同一份 usecase**。

⚠️ **最後那一句是整件事的關鍵。** usecase 若不在畫面真的會走到的路徑上，
規格驗的東西與畫面跑的東西就是兩條路 —— 規格全綠而畫面壞掉，沒有閘門看得見。
實測：把 usecase 裡的篩選拿掉 → 規格紅 5 條，而既有的切片契約測試那 5 條
**照樣全綠**。那個對照就是這一層買到的東西。

⚠️ **接線檔的副檔名是 `.spec.ts`，而它不是可以換的。** vitest 的預設 include
只收 `*.test.*` 與 `*.spec.*`，這條線的根層沒有覆寫 `test.include`。草稿原本
寫的 `.steps.ts` 會讓整份規格**一條都不被收集** —— runner 靜默不跑、既有測試
繼續全綠、完成率讀的是一個從來沒有被執行過的檔案。

### ✅ 報表也做完了（C115）—— 而它多長出一個「態」

`tools/spec-report` 讀規格與測試結果，產出 `SPEC-REPORT.md` 並印出需要行動的部分。

⚠️ **設計時多出一個原本沒有的第四態：「未執行」。** 三態（完成／擋下／待辦）
少了一格 —— 「規格解析得出來，但結果裡找不到它跑過」。它的症狀與全綠一模一樣，
而 C114 §二 那個 `.steps.ts` 正好會產生它。把「找不到」算成完成或安靜跳過，
就是把那個洞原樣重建在一份拿去對外報進度的文件裡。**它擋。**

⚠️ **「完成」這一態只從執行結果來。** 有一條看起來很划算的捷徑：「該做了沒綠就擋」
已經由 vitest 強制，所以樹綠的時候完成 ＝ 全部 − 待辦，純解析規格就算得出來。
走那條的話，報表會對每個非待辦場景寫「完成」，**包括此刻正在紅的那些**，
而且分不出「跑的時候是綠的」與「根本沒有人查過」。

⚠️ **它不自己跑測試** —— 上面那句「不養第二套 runner」也適用於「不重跑一次」。
結果由呼叫者提供，`--check` 接在 **Tier 1 的測試之後**（結果檔跨 workflow 拿不到）。

⚠️ **兩類的門檻要分開設，而且不是同一組數字。** 第一類是腳手架團隊對自己的
要求；第二類是下發給一百個不知道長什麼樣的業務場景的預設值。**用同一組數字，
不是太鬆就是把別人卡死。**

### ✅ 第二類那一半已交付（C120）—— 而它量的不是「測試寫夠了沒」

門檻**只收在 `src/usecases/` 那一層**，四個維度都是 100，跟著 `vp test` 跑
（設定裡的 `enabled: true`，不新增任何閘門）。

它問的問題與「覆蓋率」這個名字給人的印象不同：規格打的是 usecase（C114），
所以**一行沒有被走過的 usecase，就是一個沒有規格在驗的 usecase**。切片整體
不設數字 —— `src/views/**` 佔行分母的 40%，而 `.vue` 只算 `<script setup>`，
一個套在整包上的數字會被那件事帶著走，不是被測試的多寡帶著走。

⚠️ **100 是量產生器輸出量出來的**，不是喊出來的：`vp create slice` 產出的切片，
usecase 那一層四維皆滿（行 5/5、分支 5/5、函式 2/2、敘述 7/7）。
⚠️ **照樹上兩支示範切片的觀測值設門檻會設出一條假線** —— 它們是行 7.84% 與
9.30%，而一個帶 20 條測試的新切片是 22.22%，輕鬆飄過。

⚠️ **這棵樹上沒有一列會讓它開火**（兩支示範切片沒有 usecase 層），而覆蓋率
門檻的 glob 命中零個檔案時**靜默通過**。所以它靠**產生器輸出上的絆線**證明
自己，不是靠 CI 的 exit 0 —— 那條絆線與它擋下的東西寫在 C120 §五。

### 校準：先寬、先量、先記，再收緊

本檔一個門檻值都不定，這是刻意的（⚠️ **第一個真的落地的數字是 C120 的
usecase 覆蓋率 100，而它住在切片的 `vite.config.ts` 裡，不住在這裡** ——
本檔說的是定法，不是值）。定法沿用 `v1.0.4 → v1.0.5` 已經驗證過的順序：
**先用人工把判準跑一遍，再寫檢查。** 反過來做，補設定的動機會變成「讓閘門轉綠」
而不是「把判準真的跑一遍」—— 而人工那一遍掉出過兩件真的問題。

落到門檻值上就是三句話：**第一次先設寬到不擋任何既有程式碼；每一次它真的擋下
一件事就記一則 C 編號；收緊時附上那些 C 編號當論證。** 一年之後「為什麼是這個
數字」有答案，而那個答案不是「某某人說的」。

⚠️ 實測過的一個實例：方法來源建議的突變測試 break 門檻是 60，而這條線第一次
跑出來的分數在它下面 —— **照抄就是 CI 第一天紅，而紅的原因不是程式碼變差**
（C108）。

---

## 七、這份文件在三條軸裡的位置

本檔是**正確性**那條軸。另外兩條（目錄、能力）以及三者的分工表，寫在
`SCOPE.md` 開頭〈三份清單，三條軸〉那一節 —— **這裡不重抄**，那張表只有一份。

⚠️ **本檔刻意一個導出的數字都沒有**（全部列名字，不寫「幾支」「幾層有」）。
守哪幾份由 `tools/doc-facts` 的 `GUARDED` 決定，**這裡不重抄那份清單**。
所以本檔不進 `GUARDED`，而是寫成**就算沒有守衛也不會腐爛成假數字**的形狀
—— 與 `SCOPE.md` 同一個作法。

⚠️ **這裡原本寫著「`GUARDED` 只有 `README` 與 `HANDOFF`」，而那句話自己
預言了它怎麼死**（「加第三份的那一刻……會同時變成假的」）。第三份是
`UI-SURVEY.md`，加在 `main` 上，而本檔當時在 `release/v1` —— 那裡沒有那個檔案。
**同一句話在兩條線上一真一假**，併線把它變成假的，而沒有任何東西說話。
`SCOPE.md` 有一份逐字相同的手抄本，同時修掉。經過見 **C136 §七**。

本檔的**存在**由 `tools/scope-check` 守著（它登記在 `SCOPE.md` 的根層表裡）；
本檔的**內容對不對**，仍然只有人能判斷。
