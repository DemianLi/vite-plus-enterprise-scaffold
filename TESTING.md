# v1 的測試層級模型 —— 哪一層守什麼、哪一層你可以不讀

> 這份文件給**兩種人**：維護 `release/v1` 這條線的人，以及**拉 v1 去做案子的團隊**。
> 它回答一個問題：**這個腳手架的正確性，是靠哪幾層守的；哪幾層是你自己要補的。**
>
> 目錄該不該存在於 v1 的樹上 → [SCOPE.md](SCOPE.md)。
> v1 承諾什麼、刻意不承諾什麼 → [HANDOFF.md](HANDOFF.md)。
> 這三份各答各的問題，**不互相重述，只互相指過去**。

---

## 為什麼需要這份文件

`tools/` 底下每一支閘門，都是被一次具體的事故逼出來的，論證寫在 `DECISIONS.md`
的 C 編號裡。那個長法讓每一道都有理由，但也讓整體**沒有形狀** —— 全綠的時候，
它告訴你的是「沒有人破壞既有結構」，而不是「這東西做對了事」。

這份文件借用一組外部座標把既有的閘門排進去，好讓「還缺什麼」變成一個
可以回答的問題，而不是一種感覺。

**座標來源**：Robert C. Martin 於 2026 年 6–7 月在 X 的公開討論串所描述的五層
流水線（約束／單元測試／驗收測試／QA 程序／週期性手動測試）加一個檢查測試本身
的元層（突變測試）。⚠️ **那是別人的座標，不是別人的門檻值** —— 他在同一批討論裡
明說那些數值出自他自己幾十年的判斷力，照抄數字是「下一個性質完全不同的賭注」。
所以下面只借層次，不借數字；本檔一個門檻值都不定，門檻值的定法見〈校準〉。

---

## 一、五層 ＋ 元層，對應到 v1 的現況

「誰寫、誰審」是這組座標真正的切分，比層數重要。

| 層         | 這一層問的問題                   | 誰寫       | 誰審            | v1 的現況                     |
| ---------- | -------------------------------- | ---------- | --------------- | ----------------------------- |
| 層 1 約束  | 程式碼有沒有纏成一團             | 工具       | **沒有人**（機器擋） | 有，但不完整（見下）      |
| 層 2 單元  | 實作細節對不對                   | agent／人  | **刻意不審**    | 有，無下限門檻                |
| 層 3 驗收  | **什麼叫做對**                   | agent／人  | **人逐字或抽查** | **沒有**                     |
| 層 4 QA    | 怎麼驗它真的能用                 | agent／人  | 人              | 沒有，且下發不了              |
| 層 5 手動  | 規格本身有沒有寫錯               | —          | 人親手跑        | 沒有，且下發不了              |
| 元層 突變  | 那些沒人審的測試是不是空頭支票   | 工具       | 機器            | **有手工版，無機械版**        |

### 層 1 —— 約束

機械化擋下結構性問題，不靠提示詞、不靠人盯。v1 落在這一層的：

| 機制                    | 擋什麼                                             |
| ----------------------- | -------------------------------------------------- |
| `vp check`              | 格式（Oxfmt）、lint（Oxlint）、型別                |
| `tools/conformance`     | 切片契約、分層邊界、相對路徑逃逸、幽靈依賴         |
| `tools/vue-typecheck`   | `.vue` 的型別（`vp check` 的 tsgolint 不看 SFC）   |

⚠️ **`eslint.config.js` 不在這一層**，雖然它形式上也是「機械化擋、不靠人」。
`platform/eslint-config` 檔頭寫得很清楚：它**只**管 oxlint 管不到的安全與邊界，
刻意零重疊、刻意不 extend 任何 recommended preset。層 1 問的是「程式碼有沒有
纏成一團」，安全規則不回答那個問題 —— 它在〈對應不上這組座標的機制〉那張表。

⚠️ **缺的是複雜度那一格。** 函式大小、巢狀深度、參數個數、認知複雜度 —— 這正是
那組座標裡層 1 的主要內容，理由是「agent 會在自己製造的糾結程式碼裡繞不出來，
最後得人下場解」。v1 目前沒有任何一條這類規則。補法見〈缺口〉。

⚠️ **`tools/conformance` 已經做完了依賴方向那一格。** 借來的座標裡把「分層邊界」
當成要新增的東西，在這個 repo 是**既有的**，而且做得更細（相對路徑逃逸、幽靈依賴
都在管）。這一格不需要新工具。

### 層 2 —— 單元測試

「agent 寫、人不讀」的那一層。理由是這些測試驗的是 agent 自己端到端掌控的實作
細節，人讀它的邊際效益接近零。

v1 在 `platform/*`、`features/*`、`apps/*` 與 `tools/*` 底下都有測試，跑在 Vitest。
⚠️ **但沒有覆蓋率門檻**，所以「人不讀」目前沒有下限保護 —— 這一層的機械保證
現在是空的。要不要補、補到哪裡，見〈缺口〉。

### 層 3 —— 驗收規格

**v1 完全沒有這一層，而這是這份文件最重要的一句話。**

這一層要的是：用自然語言寫、不夾帶 code-level 產物、透過一層 testing API 直接
打進應用程式、**由人逐字或抽查**的規格。它是唯一一層在回答「什麼叫做對」。

v1 最接近的東西是契約測試（`platform/ui/tests/component-contract.test.ts`
掃目錄驗每一個元件、`apps/console/tests` 真的建置一次再去產物裡找）。那些是好
測試，但它們斷言的是**機制有沒有生效**，不是**這門生意認為什麼叫正確**，而且
沒有一份是人打算逐字讀的。

⚠️ 這一層缺席，是「全綠仍然不能讓人相信」的直接原因。前面兩層與元層全部建立在
「規格寫對了」的前提上，而 v1 目前沒有一份寫下來的規格。

### 層 4 QA 程序 ／ 層 5 週期性手動測試

**這兩層下發不了。** 它們是人做的事，不是可以打包進 `platform/*` 交付的東西。
腳手架能交付的最多是層 1、層 2、層 3 與元層。

⚠️ 對拉 v1 去做案子的團隊來說，這兩層是**你們自己的責任**，而且借來的座標裡
最真實的那個破綻正落在你們身上：**規格如果是 AI 寫的，而它斷言了錯的行為
（不只是漏掉案例），抽查很可能抓不到。** 層 5 存在的唯一理由就是兜住這個。
這一段在 HANDOFF 有對應的一節，本檔不重述。

### 元層 —— 突變測試

問的是：那些沒有人審的測試，怎麼知道不是空頭支票。答案不是人工閱讀，是**刻意
在原始碼注入錯誤，看測試會不會失敗**。

⚠️ **v1 已經在做這件事，只是手工的、而且只對著閘門自己。** 支撐這句話的是
三處，各自可查：

| 出處 | 它做什麼 |
| --- | --- |
| `.semgrep/rules.ts` | 故意寫壞的 fixture，存在的唯一目的是讓「規則到底有沒有在檢查」變成可執行的問題 |
| `tools/api-surface/tests/negative.test.ts` | 自稱反向測試，用兩種破壞法（改基準檔副本、改 fixture 原始碼），斷言閘門會紅 |
| `tools/vue-typecheck/tests/negative.test.ts` | 檔頭寫明「每一條都先讓 fixture 壞掉，再證明這支工具會說話」 |

HANDOFF 那句「一支只 grep `@theme` 有沒有寫的測試，量的是『有沒有寫』不是
『有沒有生效』」講的是同一件事。

**缺的是機械化，以及把覆蓋面從閘門擴大到 `platform/*` 的產品碼。**

⚠️ **上表前兩處都在 C107 的待刪清單上**（`.semgrep/` 屬 Tier 2、`api-surface`
整支）。刪除執行的那一刻，這一層的支撐只剩 `vue-typecheck` 一處 —— **而「元層
非空」正是把機械化突變測試從必要降為選配的唯一理由。**

**裁決（C107 §七）：`v2.0.0` 執行刪除的同時，機械化突變測試升回必要，
與刪除同一個 PR。** 用同一把尺刪掉手工版，就得補上機械版。
⚠️ 可行性實測因此是**阻斷項** —— **而它已經做完了（C108）：跑得起來，
`platform/slice-kit` 實測 58.66% 分數、6 秒、零錯誤零逾時。** 兩個真阻斷
（TypeScript 7 移除了 Stryker 依賴的 compiler API、isolated node_modules 讓
runner plugin 找不到）都有繞法，**阻斷項解除**。

⚠️ 但這件事本身留下一條教訓：本檔初稿在這裡寫過「module-alias 會破壞
sandbox」—— 那是照方法來源的風險清單抄的，而這條線根本沒有 path alias。
**借座標會連別人的風險清單一起借過來，那跟門檻值一樣是別人專案的產物。**

---

## 二、對應不上這組座標的機制 —— 以及為什麼

這一節是刪除決策的材料。**對應不上不等於該刪** —— 先看它守的是什麼，再判。

| 機制                  | 為什麼對應不上五層                                                     | 裁決（C107） |
| --------------------- | ---------------------------------------------------------------------- | ------------ |
| `tools/api-surface`   | 守的是**跨版本相容**：型別形狀改了就紅，逼出 codemod。那組座標假設單一 repo 單一團隊，沒有「交付給下游、下游會升級」這個維度 | **待刪** |
| `tools/codemods`      | 不是閘門，是**修復工具** —— 破壞性變更的遷移腳本。五層裡沒有「怎麼幫下游遷移」這一格 | **待刪** |
| `tools/slice-gen`     | 產生器與檢查器讀同一份契約。是**一致性機制**，不是測試層 | **待刪** |
| Tier 2 安全整條       | `eslint.config.js` ＋ `.semgrep/` ＋ gitleaks。那組座標在回答「怎麼在不讀 diff 的前提下相信程式碼是對的」，安全不在它的題目裡 | **待刪** ⚠️ 見下 |
| `tools/theme-verify`  | 守的是**可替換性**：真的建置兩次，證明各案覆寫得掉。這不是「程式碼對不對」，是「下游改不改得動」 | 留 |
| `tools/doc-facts`     | 守的是**文件斷言為真**。斷言寫錯沒有執行期後果，任何測試層都不會紅 | 留 |
| `tools/scope-check`   | 守的是**範疇治理**：樹上有什麼與 SCOPE.md 一致。程式碼正確性座標裡沒有這一格 | 留 |

⚠️ **「待刪」是裁決，不是現況 —— 這一版一支都還沒刪。** 刪除是破壞性變更
（拉 v1 的團隊會少掉數條承諾的機制），走 `v2.0.0`，論證與連帶清單見
`DECISIONS.md` 的 C107。**在那之前，上表那幾支全都還在樹上。**

⚠️ **Tier 2 待刪牽動的是 `gate` 鏈本身，不是層 1。** `eslint.config.js` 是
Tier 2 的進入點（D10），而 `package.json` 的 `gate` 鏈與
`.github/workflows/tier2-security.yml` 都指著它 —— 刪除要一起改，不能只刪工具。
⚠️ 本檔初稿曾把它誤列在〈層 1 約束〉，更正的經過記在 C107 §七。

⚠️ **這張表用的是一把測試的尺。** 上面每一支之所以對應不上，共同的原因只有一個：
借來的座標是為了回答「怎麼不讀 diff」而生的，它不處理安全、供應鏈、文件真實性、
跨版本相容、可替換性。**拿它去判這些機制的生死，等於拿尺去量溫度。**

⚠️ **`doc-facts` 與 `scope-check` 這兩支是不可逆的**（裁決保留了它們）**：** 它們守的正是「文件說的
跟樹上有的一致」—— 刪掉它們之後，**將失去發現自己刪錯了的能力**。

⚠️ **上面那幾個維度是「腳手架」這個身分特有的。** 一般專案不需要證明自己的 README
沒說謊，也不需要證明下游換得掉配色；交付給多個項目組的腳手架需要。借來的座標
沒有這些格子，不是因為它們不重要，是因為它回答的不是腳手架的問題。

---

## 三、缺口 —— 以及補法

按「補了之後改變什麼」排序，不按層數。

| 缺口             | 補什麼                                                     | 改變什麼                                     |
| ---------------- | ---------------------------------------------------------- | -------------------------------------------- |
| **層 3 驗收規格** | 一條承諾對一條 `.feature`，中文，打 `platform/*` 而不是 DOM，人逐字讀 | 這是唯一一道守「什麼叫做對」的閘門 |
| **層 1 複雜度**   | 函式大小、巢狀深度、參數個數、認知複雜度                   | 擋下 agent 把自己纏住，在它纏成一團之前       |
| **層 2 覆蓋率下限** | 分目錄的覆蓋率門檻，不設單一數字                         | 讓「人不讀單元測試」有一個機械下限            |
| **元層機械化**    | 把既有的手工反向測試擴大到 `platform/*` 的產品碼           | 讓「不審 AI 寫的測試」有依據。⚠️ 對**現況**是選配（手工版還在），對 `v2.0.0` 之後是**必要** |

⚠️ **層 4 與層 5 不在這張表裡，因為它們補不了** —— 見上面〈層 4／層 5〉那一節。

### 層 3 的規格對什麼寫

`README` 的〈v1.0.0 承諾的五件事〉那一節，就是這個腳手架寫下來的「什麼叫做對」，
所以第一批規格**一條承諾對一條規格**。⚠️ 承諾若被修訂（C107 §六 已排定其中一條
要改），規格跟著走 —— 這裡刻意不複述承諾的內容與數量。它們目前只被**間接**證明（`theme-verify` 真的建置兩次、元件契約
掃目錄），而那些斷言的是機制有沒有生效，不是承諾本身 —— 沒有一份是人打算
逐字讀的。

runner 用 `vitest-cucumber`，跑在既有的 Vitest 裡。⚠️ **不養第二套 runner** 的
理由不只是省事：跨套件測試在這條線上已經有排程相依（C87 用 `dependsOn` 讓開），
第二套 runner 會再撞一次同一個問題。

### 校準：先寬、先量、先記，再收緊

本檔一個門檻值都不定，這是刻意的。定法沿用 `v1.0.4 → v1.0.5` 已經驗證過的順序：
**先用人工把判準跑一遍，再寫檢查。** 反過來做，補設定的動機會變成「讓閘門轉綠」
而不是「把判準真的跑一遍」—— 而人工那一遍掉出過兩件真的問題。

落到門檻值上就是三句話：**第一次先設寬到不擋任何既有程式碼；每一次它真的擋下
一件事就記一則 C 編號；收緊時附上那些 C 編號當論證。** 一年之後「為什麼是這個
數字」有答案，而那個答案不是「某某人說的」。

---

## 四、這份文件在三條軸裡的位置

本檔是**正確性**那條軸。另外兩條（目錄、能力）以及三者的分工表，寫在
`SCOPE.md` 開頭〈三份清單，三條軸〉那一節 —— **這裡不重抄**，那張表只有一份。

⚠️ **本檔刻意一個導出的數字都沒有**（全部列名字，不寫「幾支」「幾層有」）。
`tools/doc-facts` 的 `GUARDED` 只有 `README` 與 `HANDOFF`，加第三份的那一刻，
好幾處寫著「只守那兩份」的句子會同時變成假的。所以本檔不進 `GUARDED`，
而是寫成**就算沒有守衛也不會腐爛成假數字**的形狀 —— 與 `SCOPE.md` 同一個作法。

本檔的**存在**由 `tools/scope-check` 守著（它登記在 `SCOPE.md` 的根層表裡）；
本檔的**內容對不對**，仍然只有人能判斷。
