# v1 的測試層級模型 —— 兩類測試，哪一層守什麼

> 這份文件給**兩種人**：維護 `release/v1` 這條線的人，以及**拉 v1 去做案子的團隊**。
> 它回答一個問題：**這個腳手架的正確性，是靠哪幾層守的；哪幾層是你自己要補的。**
>
> 目錄該不該存在於 v1 的樹上 → [SCOPE.md](SCOPE.md)。
> v1 承諾什麼、刻意不承諾什麼 → [HANDOFF.md](HANDOFF.md)。
> 這三份各答各的問題，**不互相重述，只互相指過去**。

---

## 為什麼需要這份文件

`tools/` 底下每一支閘門，都是被一次具體的事故逼出來的，論證寫在 `DECISIONS.md`
的 C 編號裡。那個長法讓每一道都有理由，但也讓整體**沒有形狀** —— 全綠的時候，
它告訴你的是「沒有人破壞既有結構」，而不是「這東西做對了事」。

這份文件借用一組外部座標把既有的閘門排進去，好讓「還缺什麼」變成一個
可以回答的問題，而不是一種感覺。

**座標來源**：Robert C. Martin 於 2026 年 6–7 月在 X 的公開討論串所描述的五層
流水線（約束／單元測試／驗收測試／QA 程序／週期性手動測試）加一個檢查測試本身
的元層（突變測試）。

⚠️ **那是別人的座標，不是別人的門檻值** —— 他在同一批討論裡明說那些數值出自
他自己幾十年的判斷力，照抄數字是「下一個性質完全不同的賭注」。所以下面只借
層次，不借數字；本檔一個門檻值都不定，門檻值的定法見〈校準〉。

⚠️ **而且那組座標只描述了兩類測試裡的一類。** 他在回答的是「一個團隊怎麼在
不讀 diff 的前提下相信自己的程式碼」；**他沒有在回答「我做的東西要交給一百個
團隊用」。** 下一節那個二分，是這個腳手架的身分逼出來的，不在借來的座標裡。

---

## 一、兩類測試 —— 這份文件的組織原則

**五層是借來的座標軸，兩類是這個腳手架的身分。組織原則是後者。**

| | **第一類 · 框架測試** | **第二類 · 交付的測試設施** |
| --- | --- | --- |
| 誰維護 | 腳手架團隊 | 腳手架團隊建，**專案組使用** |
| 守什麼 | 腳手架自身沒有壞 | 專案組交付的業務是對的 |
| 對象 | `platform/*`、`tools/*` —— 那些每個案子都要重新搭一次、所以被抽出來共用的東西 | `features/*`、`apps/*` —— 各案自己的業務 |
| 它紅了，誰的問題被擋下 | 維護者的 | **拉 v1 的團隊的** |
| 成功長什麼樣 | 升級平台不會打斷任何一個案子 | **專案成員只要補 TDD，就量得出業務功能完成率** |

⚠️ **第二類不是「腳手架的測試」，是「腳手架交付的測試能力」。** 這個分別決定
很多事：第一類的東西壞了，痛的是維護者；第二類的東西缺了，一百個專案組各自
用各自的方式補，而腳手架宣稱的「開箱即用」變成一句空話。

⚠️ **`SCOPE.md` 的判準對兩類的答案不同。** 那份文件問「這東西紅了，是誰的問題
被擋下來」—— 第二類**天然通過**（受益者就是拉 v1 的團隊），第一類要逐支論證。
`tools/gate-roster` 當年就是在這一格出局的（C72）。

---

## 二、五層 × 兩類

| 層 | 這一層問的問題 | 第一類 · 框架測試 | 第二類 · 交付的測試設施 |
| --- | --- | --- | --- |
| 層 1 約束 | 程式碼有沒有纏成一團 | 有，缺複雜度那一格 | 有（同一套設定隨腳手架下發），缺複雜度那一格 |
| 層 2 單元 | 實作細節對不對 | 有，無下限門檻 | **只有骨架**：`slice-gen` 產生的切片自帶一支測試，之後沒有任何機制 |
| 層 3 驗收 | **什麼叫做對** | **沒有** | **沒有** —— 而這一格正是「業務功能完成率」的所在 |
| 層 4 QA | 怎麼驗它真的能用 | 沒有 | 沒有，**且下發不了**（人做的事） |
| 層 5 手動 | 規格本身有沒有寫錯 | 沒有 | 沒有，**且下發不了**（人做的事） |
| 元層 突變 | 那些沒人審的測試是不是空頭支票 | **有手工版，無機械版** | **沒有** |

⚠️ **看右邊那一欄。** 第一類做得相當紮實 —— 十幾道閘門，每一道附一支反向測試。
**第二類幾乎整欄是空的**，而它才是「撐得住各個項目組拿去交付」這件事的所在。

> **不安的來源可能不是第一類不夠好，是第二類從來沒有被設計過。**

---

## 三、第一類的現況

### 層 1 —— 約束

機械化擋下結構性問題，不靠提示詞、不靠人盯。落在這一層的：

| 機制 | 擋什麼 |
| --- | --- |
| `vp check` | 格式（Oxfmt）、lint（Oxlint）、型別 |
| `tools/conformance` | 切片契約、分層邊界、相對路徑逃逸、幽靈依賴 |
| `tools/vue-typecheck` | `.vue` 的型別（`vp check` 的 tsgolint 不看 SFC） |

⚠️ **`eslint.config.js` 不在這一層**，雖然它形式上也是「機械化擋、不靠人」。
`platform/eslint-config` 檔頭寫得很清楚：它**只**管 oxlint 管不到的安全與邊界，
刻意零重疊、刻意不 extend 任何 recommended preset。層 1 問的是「程式碼有沒有
纏成一團」，安全規則不回答那個問題。

⚠️ **缺的是複雜度那一格。** 函式大小、巢狀深度、參數個數、認知複雜度 —— 這正是
那組座標裡層 1 的主要內容，理由是「agent 會在自己製造的糾結程式碼裡繞不出來，
最後得人下場解」。目前沒有任何一條這類規則。補法見〈缺口〉。

⚠️ **`tools/conformance` 已經做完了依賴方向那一格。** 借來的座標把「分層邊界」
當成要新增的東西，在這個 repo 是**既有的**，而且做得更細。這一格不需要新工具。

### 層 2 —— 單元測試

「agent 寫、人不讀」的那一層。理由是這些測試驗的是 agent 自己端到端掌控的實作
細節，人讀它的邊際效益接近零。

`platform/*`、`tools/*` 底下都有測試，跑在 Vitest。
⚠️ **但沒有覆蓋率門檻**，所以「人不讀」目前沒有下限保護。

### 層 3 —— 驗收規格

**沒有這一層。** 它要的是：用自然語言寫、不夾帶 code-level 產物、透過一層
testing API 直接打進應用程式、**由人逐字或抽查**的規格。它是唯一一層在回答
「什麼叫做對」。

最接近的東西是契約測試（`platform/ui/tests/component-contract.test.ts` 掃目錄
驗每一個元件、`apps/console/tests` 真的建置一次再去產物裡找）。那些是好測試，
但它們斷言的是**機制有沒有生效**，不是**這門生意認為什麼叫正確**，而且沒有
一份是人打算逐字讀的。

⚠️ 這一層缺席，是「全綠仍然不能讓人相信」的直接原因 —— 前面兩層與元層全部
建立在「規格寫對了」的前提上。

### 層 4 QA 程序 ／ 層 5 週期性手動測試

**這兩層下發不了**，兩類都一樣。它們是人做的事，不是可以打包進 `platform/*`
交付的東西。腳手架能交付的最多是層 1、層 2、層 3 與元層。

⚠️ 對拉 v1 去做案子的團隊來說，這兩層是**你們自己的責任**，而且借來的座標裡
最真實的那個破綻正落在你們身上：**規格如果是 AI 寫的，而它斷言了錯的行為
（不只是漏掉案例），抽查很可能抓不到。** 層 5 存在的唯一理由就是兜住這個。
這一段在 HANDOFF 有對應的一節，本檔不重述。

### 元層 —— 突變測試

問的是：那些沒有人審的測試，怎麼知道不是空頭支票。答案不是人工閱讀，是**刻意
在原始碼注入錯誤，看測試會不會失敗**。

⚠️ **已經在做，只是手工的、而且只對著閘門自己。** 支撐這句話的是三處，各自可查：

| 出處 | 它做什麼 |
| --- | --- |
| `.semgrep/rules.ts` | 故意寫壞的 fixture，存在的唯一目的是讓「規則到底有沒有在檢查」變成可執行的問題 |
| `tools/api-surface/tests/negative.test.ts` | 自稱反向測試，用兩種破壞法（改基準檔副本、改 fixture 原始碼），斷言閘門會紅 |
| `tools/vue-typecheck/tests/negative.test.ts` | 檔頭寫明「每一條都先讓 fixture 壞掉，再證明這支工具會說話」 |

HANDOFF 那句「一支只 grep `@theme` 有沒有寫的測試，量的是『有沒有寫』不是
『有沒有生效』」講的是同一件事。

**缺的是機械化，以及把覆蓋面從閘門擴大到 `platform/*` 的產品碼。**

⚠️ **可行性已經實測過（C108）**：`platform/slice-kit` 跑得起來，
零錯誤零逾時，時間在秒的量級。兩個真阻斷（TypeScript 7 移除了 Stryker 依賴的
compiler API、isolated node_modules 讓 runner plugin 找不到）都有繞法。
⚠️ 實測順手掉出一個**真缺口**：`register.ts` 有一行被改壞之後測試照樣全綠 ——
覆蓋到了，但沒有任何一條斷言那個回傳值的內容。**手工反向測試守的是閘門自己，
守不到產品碼裡這種空頭斷言。**

⚠️ 這件事本身留下一條教訓：本檔初稿在這裡寫過「module-alias 會破壞 sandbox」——
那是照方法來源的風險清單抄的，而這條線根本沒有 path alias。
**借座標會連別人的風險清單一起借過來，那跟門檻值一樣是別人專案的產物。**

---

## 四、第二類的現況 —— 這一節是本檔真正的產出

第二類要成立的那句話是：**「專案成員只需要補 TDD，就能測出業務功能完成率。」**
逐段拆開看它現在靠什麼支撐：

| 那句話的哪一段 | 現在靠什麼 | 夠不夠 |
| --- | --- | --- |
| 「腳手架內建安裝好」 | `slice-gen` 產生切片時附一支測試骨架；一致性檢查要求至少一支 | ⚠️ **入口只有這一個** |
| 「專案成員只需要補」 | 沒有 TDD 指引、沒有範本、`AGENTS.md` 只有 `vp` 的官方樣板 | ❌ 缺 |
| 「測試出」 | Vitest 裝好了、跑得動 | ✅ |
| 「業務功能完成率」 | **完全沒有** —— 沒有任何東西在量「這個功能做完了沒有」 | ❌ 缺，見下 |

### 完成率的量法：驗收規格的通過率

**覆蓋率量的是「程式碼被跑過」，那回答不了「功能做完了沒有」。** 能回答後者的
是層 3：專案組用自然語言寫下「這個功能叫做對是什麼樣子」，跑起來，**綠幾條
就是完成幾條**。

⚠️ **分工在這裡必須講清楚，否則第二類會變成第一類的翻版**：

> **腳手架交付的是那一層的設施與範本** —— runner、testing API、`.feature` 骨架、
> 完成率的計算方式。**規格的內容由專案組自己寫**，因為只有他們知道自己的生意
> 認為什麼叫正確。

⚠️ 這一格同時也是「人與智能體協作」的落點：**規格是人寫給 agent 讀的需求，
通過率是 agent 交回給人的進度。** 人審的是規格（什麼叫做對），不是 diff
（它怎麼寫的）—— 那正是這組座標最初的主張。

### `slice-gen` 是第二類的入口，這件事值得標明

它產生的切片自帶 `tests/` 與一支測試，`files.ts` 的註解寫著
「沒有測試的切片＝沒有人能安全重構的切片」。

⚠️ **一度有一個裁決要刪掉它**（理由：「一致性機制，不是測試層」）。那是用五層
的尺量出來的結果，而五層只描述第一類 —— 用兩類的框架重量，它是第二類目前
**唯一**的入口。裁決已反轉，經過見 `DECISIONS.md` 的 C109。

---

## 五、五層量不到的機制 —— 用兩類再量一次

這一節記的是一次判準的更換，因為那個更換本身比結論重要。

| 機制 | 用五層的尺 | 用兩類的框架 |
| --- | --- | --- |
| `tools/api-surface` | 「跨版本相容，五層沒這維度」 | **第二類** —— 平台一改就打斷每一個切片，這道閘門是他們的保險 |
| `tools/codemods` | 「修復工具，不是閘門」 | **第二類** —— 升級平台時不必逐處手改 |
| `tools/slice-gen` | 「一致性機制，不是測試層」 | **第二類的入口** —— 專案組拿到測試骨架的唯一管道 |
| Tier 2 安全整條 | 「安全不在五層的題目裡」 | **第二類** —— 專案組寫業務時的前置過濾 |
| `tools/theme-verify` | 「可替換性，不是程式碼對不對」 | **第二類** —— 換配色是各案的第一件事 |
| `tools/doc-facts` | 「文件斷言為真，沒有執行期後果」 | **第二類** —— 那兩份文件是交付物，他們照著做事 |
| `tools/scope-check` | 「範疇治理，座標裡沒這格」 | 第一類 ＋ 第二類（fork 之後那張表就是他們自己的清單） |

> **左欄那些「對應不上」，共同的原因只有一個：借來的座標整個只描述第一類。**
> 拿它去判第二類的機制的生死，等於拿尺去量溫度。

⚠️ **這不是說五層沒用。** 它把層 3 的缺席照出來了，而那是這份文件最重要的發現 ——
一維的座標找到了真缺口，二維的框架擋下了一次錯誤的刪除。兩件事都要記著。

---

## 六、缺口 —— 以及補法

按「補了之後改變什麼」排序，不按層數。

| 缺口 | 屬於哪一類 | 補什麼 | 改變什麼 |
| --- | --- | --- | --- |
| **層 3 · 完成率** | **第二類** | 驗收規格的設施與範本：runner、testing API、`.feature` 骨架、通過率報表 | 讓「業務功能完成率」第一次變成可量的東西 |
| **層 3 · 框架承諾** | 第一類 | 對腳手架自己的承諾寫規格，人逐字讀 | 新的**可讀層**（人讀規格不必讀閘門實作），⚠️ 不是新的驗證能力 |
| **層 1 複雜度** | 兩類共用 | 函式大小、巢狀深度、參數個數、認知複雜度 | 擋下 agent 把自己纏住，在它纏成一團之前 |
| **層 2 覆蓋率下限** | 兩類分開設 | 分目錄的門檻，不設單一數字 | 讓「人不讀單元測試」有一個機械下限 |
| **元層機械化** | 第一類 | 把既有的手工反向測試擴大到 `platform/*` 的產品碼 | 讓「不審 AI 寫的測試」有依據 |

⚠️ **層 4 與層 5 不在這張表裡，因為它們補不了** —— 見〈層 4／層 5〉那一節。

⚠️ **第一格排在最前面，是這一版改動最大的地方。** 先前的排序把「對腳手架自己的
承諾寫規格」放在第一位；用兩類重排之後，那一格買到的是**可讀層**而不是**驗證
能力**（閘門本來就在驗那些承諾），而第二類的完成率是**目前沒有任何東西在做**
的事。

### runner 與門檻

runner 用 `vitest-cucumber`，跑在既有的 Vitest 裡。⚠️ **不養第二套 runner** 的
理由不只是省事：跨套件測試在這條線上已經有排程相依（C87 用 `dependsOn` 讓開），
第二套 runner 會再撞一次同一個問題。

⚠️ **兩類的門檻要分開設，而且不是同一組數字。** 第一類是腳手架團隊對自己的
要求；第二類是下發給一百個不知道長什麼樣的業務場景的預設值。**用同一組數字，
不是太鬆就是把別人卡死。**

### 校準：先寬、先量、先記，再收緊

本檔一個門檻值都不定，這是刻意的。定法沿用 `v1.0.4 → v1.0.5` 已經驗證過的順序：
**先用人工把判準跑一遍，再寫檢查。** 反過來做，補設定的動機會變成「讓閘門轉綠」
而不是「把判準真的跑一遍」—— 而人工那一遍掉出過兩件真的問題。

落到門檻值上就是三句話：**第一次先設寬到不擋任何既有程式碼；每一次它真的擋下
一件事就記一則 C 編號；收緊時附上那些 C 編號當論證。** 一年之後「為什麼是這個
數字」有答案，而那個答案不是「某某人說的」。

⚠️ 實測過的一個實例：方法來源建議的突變測試 break 門檻是 60，而這條線第一次
跑出來的分數在它下面 —— **照抄就是 CI 第一天紅，而紅的原因不是程式碼變差**
（C108）。

---

## 七、這份文件在三條軸裡的位置

本檔是**正確性**那條軸。另外兩條（目錄、能力）以及三者的分工表，寫在
`SCOPE.md` 開頭〈三份清單，三條軸〉那一節 —— **這裡不重抄**，那張表只有一份。

⚠️ **本檔刻意一個導出的數字都沒有**（全部列名字，不寫「幾支」「幾層有」）。
`tools/doc-facts` 的 `GUARDED` 只有 `README` 與 `HANDOFF`，加第三份的那一刻，
好幾處寫著「只守那兩份」的句子會同時變成假的。所以本檔不進 `GUARDED`，
而是寫成**就算沒有守衛也不會腐爛成假數字**的形狀 —— 與 `SCOPE.md` 同一個作法。

本檔的**存在**由 `tools/scope-check` 守著（它登記在 `SCOPE.md` 的根層表裡）；
本檔的**內容對不對**，仍然只有人能判斷。
