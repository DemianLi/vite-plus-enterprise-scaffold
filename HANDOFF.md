# 交接清單：程式碼做不到的部分

> 腳手架本身已完成（`vpr ready` exit 0）。這份清單收的是**只有組織能做的決定** ——
> 從 `DECISIONS.md` 的風險登記簿、C13、以及各處置章節收攏而來，按**誰要做**排列，
> 因為它是拿來一段一段交出去的。
>
> 每一項都附「拿什麼去談」：那些東西**都已經備好**，不需要再做技術工作。
> 最後更新：2026-08-16（腳手架初版，送人工審查）。
>
> ── ⚠️ 這張表的評定是**逐案**的，不是腳手架的屬性（2026-08-16 重評）────
>
> 第一版把每一列評成「上線前必須 ✅／⬜」，而那個寫法假設有**一個**上線環境。
> 實際的使用情境是：**這份腳手架會被 fork 去接政府採購案**，`platform/*`
> 發成內部套件供各案升級。作為**受託者**，義務是逐案從委託機關繼承的 ——
> 同一列在不同標案下的答案不一樣，所以這份清單不可能有一個固定的結論。
>
> 因此〈上線前必須〉拆成兩欄，它原本混了兩種**會往相反方向走**的東西：
>
> - **東西會壞？** —— 技術硬性。不做的話建置或驗收在技術上出問題，與標案無關
> - **標案觸發** —— 契約條件。投標時就要確認，不是上線前才問
>
> 重評當天確認的四件事，它們決定了 11 列的理由：
>
> | 問的                                   | 答案                           | 改掉哪幾列               |
> | -------------------------------------- | ------------------------------ | ------------------------ |
> | CI／開發機能連公網？                   | 能                             | 但**交付端是另一個環境** |
> | 契約要求原始碼交付／機關端重建？       | **要**                         | 5／6／7／9／11／16       |
> | 源碼掃描、弱點掃描／滲透測試是交付物？ | **是**（第三方或機關指定工具） | 21／13                   |
> | 前端畫面上有個資？                     | **少數後台畫面有**             | 20                       |
>
> ⚠️ **不要照「無金流、無個資留存」這類描述去砍這張表。** 重評前的定位敘述
> 指向刪減，真實定位（政府採購、受託者義務逐案繼承）指向擴張 ——
> 照前者動手，會砍掉驗收要用的東西。這一段寫下來是因為它差點就發生了。
>
> ── 清單裡混了兩種東西，讀之前先分開 ────────────────────────────────
>
> **判斷題**（1、2–3、4、5–7、8、9、10、11–13、19、20）——
> 只有貴組織能裁決：要不要付這個錢、接不接受這個風險、由誰負責。
> 技術面能做的都做完了：事實查到底、成本量化、工具在失敗當下自己講清楚。
>
> **還沒做完的事**（15、17、18、22、25）—— 不是判斷題，是缺口。
> 其中 **15 是採用這份腳手架的第一個步驟**（把 `@org/*` 換成真的團隊），
> 其餘幾項是腳手架自己知道、也寫下來的盲點。
>
> ⚠️ 每一項的**編號固定不動**。有幾項已經在技術面處理掉或降級（4／7／9／12／14／16／19／21／23），
> 它們留在原位並標上結論，不重新編號 —— 這份清單是拿去給不同單位、
> 不同時間讀的，「第 12 項」在兩次會議之間變成別的東西比過期更糟。
>
> ⚠️ 這段刻意**不再手寫「N 項裡有 M 項…」的分項計數**。第一版寫了，
> 然後在項次長到 19 的時候整段都是錯的 —— 而那正是這份 repo 一路在防的形狀
> （見 `tools/doc-facts`）。總數由機器守著，分項計數沒有事實來源，所以拿掉。

## 先看這張表

| #   | 對象         | 事項                                                                          | 不做的後果                                                    | 東西會壞 | 標案觸發                                      |
| --- | ------------ | ----------------------------------------------------------------------------- | ------------------------------------------------------------- | -------- | --------------------------------------------- |
| 1   | 技術主管     | 核准 beta **版本流**、供應商登記 Cloudflare                                   | 元件不合規；修補 SLA 形同虛設                                 | ⬜       | 每案資安審查都會問「為什麼用 beta」           |
| 2   | 資安         | 原生工具鏈的**政策性**例外（144 個二進位）                                    | 機關端 SCA 判 fail                                            | ⬜       | **每一案**                                    |
| 3   | 資安         | 接受 43 個只有發佈簽章的佐證                                                  | 同上，且覆核時會被抓                                          | ⬜       | **每一案**                                    |
| 4   | 法務         | MPL-2.0 ＋ 22 個**完全無授權聲明**的套件                                      | 授權政策掃描標記，可能驗收前才爆                              | ⬜       | **每一案**                                    |
| 5   | 平台／IT     | 內部 registry 鏡像 **574 個**套件                                             | 機關端重建時抓不到套件                                        | ✅       | 原始碼交付／機關端重建                        |
| 6   | 平台／IT     | registry 設在**機器層級**，不是專案                                           | 封閉環境下 `vp` 第一步就往公網連                              | ✅       | 同上（收件人是**機關端**，不是你們 CI）       |
| 7   | 平台／IT     | 確認 `darwin-x64`／`win32-x64` 是否要支援                                     | 機關端那台裝不起來                                            | ✅       | 同上（問的是**機關端跑什麼平台**）            |
| 8   | 架構         | 指派 D8 同源中間層由誰提供                                                    | 登入、CSRF、401／403 整條路徑沒有著落                         | ✅       | **每一案**（有登入就有）                      |
| 9   | 發版流程     | 「`--capture` 在公網側完成」寫進流程                                          | 兩份檔案沒一起進機關端，閘門紅而現場修不了                    | ✅       | 原始碼交付／機關端重建                        |
| 10  | 資安         | SCA 掃描把 `bingo` 標為 dev-only                                              | 開發期工具被當成 runtime 相依計嚴重度                         | ⬜       | 每一案（5 分鐘）                              |
| 11  | 平台（CI）   | 內部 registry 下的 bootstrap ＋ SBOM 工具                                     | 私有網路下抓不到相依；SBOM 非稽核認可                         | ✅       | 前半：機關端重建／後半：**每一案**            |
| 12  | repo 管理者  | 自動核准要不要開（標籤已建）**安全決定**                                      | 開錯會讓 CODEOWNERS 整套失效                                  | ⬜       | 15 做完之後才有意義                           |
| 13  | 平台（上線） | CSP 由 report-only 切成 enforce                                               | 弱點掃描報告**必開一條**；report-only 等於沒有                | ⬜       | **有弱點掃描／滲透測試的案**                  |
| 14  | 架構／設計   | ~~樣式策略選型~~ ~~設計系統要不要獨立 owner~~ **全部已決策（D15／C62）**      | ~~沒有收件人~~ 收件人＝platform-maintainers（見 C62 五）      | ⬜       | 已結案；剩下的落在 24 與 25                   |
| 15  | repo 管理者  | **把 `@org/*` 換成真的團隊** —— 現在 20 條條目全是 `@org/*` 佔位符            | D12 的擁有權治理目前是一份文字檔，完全沒生效                  | ⬜       | **採用的第一步**，每 fork 一次做一次          |
| 16  | 平台（實作） | ~~補幽靈依賴檢查~~ **已實作（2026-08-16）**。剩：`tools/*` 與 `tests/` 不掃   | ~~機關端重建時才爆~~ 已守住 `src/` 那一半                     | ⬜       | 已不由標案觸發；剩下的是內部取捨              |
| 17  | 平台（實作） | ~~`api-surface` 看不見**型別形狀**~~ **已重做（2026-08-16）**。剩：純資料常數 | ~~升套件只拿到型別錯誤~~ 已守住成員、建構子、props            | ⬜       | 已做掉；剩下的是內部取捨                      |
| 18  | 平台（實作） | ~~守備範圍仍不完整~~ **已擴到 13 個事實（2026-08-16）**。剩：不可推導的那類   | ~~沒登記的數字會過期~~ 補登記的當下又抓到 2 處過期            | ⬜       | 內部工作，不由標案觸發                        |
| 19  | 法遵／IT     | §16 五年保存：**腳手架交出清單，保存排程是你們的**                            | 證據存在但沒進保存制度；`sbom.cdx.json` 90 天就沒了           | ⬜       | **契約寫了保存年限的案**（目前手上的案沒寫）  |
| 20  | 法遵／資安   | §11 II ⑨ 隱碼**現在是慣例，不是機制**（2026-08-16 拆掉閘門）                  | 後台新增個資欄位忘了遮罩時，沒有任何東西會說話                | ⬜       | **畫面上會出現個資的案**（→ 走白名單那條路）  |
| 21  | 資安         | ~~SAST 沒有接上~~ **已接上（2026-08-16）**。前置過濾器，不是交付的那份報告    | ~~汙點傳遞沒有東西在看~~ 已守住                               | ⬜       | 已接上；廠商掃描的範圍與時程不受影響          |
| 22  | 架構／設計   | 靜態閘門已裝；**行為面量過後決定不裝，改交付分工表（2026-08-17）**            | ~~驗收前才知道~~ 已知道哪幾格得靠人工／委外；欠規範原文       | ⬜       | **民眾端系統**（等級與規範版本仍以 RFP 為準） |
| 23  | 平台（CI）   | ~~17 處引用全用可移動的標籤~~ **已全部釘 SHA ＋ 加閘門（2026-08-16）**        | ~~CI 這一層是敞開的~~ 剩：`run:` 裡的容器映像沒被守           | ⬜       | 已釘住；剩下的是內部取捨                      |
| 24  | 架構／設計   | ~~三條軸只有一條有接縫~~ **三條軸全部接上並各有閘門（2026-08-17）**           | ~~各案只能改共用套件~~ 已結案，兩個殘留同日也關掉了           | ⬜       | 已擋掉；剩下的是內部取捨                      |
| 25  | repo 管理者  | CODEOWNERS 距離生效有**三層**，文件原本只寫了中間那層                         | 做完 15 會以為共簽生效了 —— 12 條資安共簽其實是任一核准       | ⬜       | 與 12、15 同批；**做 15 的時候一起看**        |
| 26  | 平台（實作） | ~~`.vue` 沒有型別檢查~~ **已接上（2026-08-17）**。乾跑撞出一個沒人守的相依    | ~~SFC 的型別斷言是裝飾品~~ 已守住，CSS `@import` 盲區同日補上 | ⬜       | 已做掉；剩下的是內部取捨                      |

---

## 1. 技術主管 — beta 版本流的事先核准

> **2026-08-16 換了對象。** 原本寫「採購／稽核」，那假設相依是由客戶核准的。
> 接案公司自己選相依，所以核准的人是你們的技術主管 —— 但**每一案的機關資安
> 審查都會問「為什麼用 beta」**，所以要準備的東西一個字都沒少，只是收件人變了。

**要決定的**

- 核准的對象是**版本流**（`vite-plus@0.2.x`），**不是逐版**。
  0.2.5→0.2.9 共 4 版、26 天，約 5–14 天一版。逐版核准會讓 D13 的
  **critical 3 天修補 SLA 一開始就是死的**
- 供應商紀錄登記 **Cloudflare**，不是 VoidZero（2026-06-04 併購）

**拿什麼去談**

授權疑慮已解除：`vite-plus`、core、8 個原生二進位的 `license` 欄位實測皆為 MIT。
但 **MIT 意味著無擔保、無支援義務**，官方文件沒有任何 SLA。所以緩解措施只能自己持有——
而它已經存在：

```bash
./node_modules/.bin/vpr exit-drill    # 每季一次，證據進版控
```

最後一次（2026-08-16，UTC）：退到上游 **Vite 8.2.1** 建置成功、上游 Vitest **155 個測試全過**、
**應用程式原始碼一字未改**、耗時 22 秒。證據在
[`tools/exit-drill/evidence.json`](tools/exit-drill/evidence.json)。

> 說法的差別：不是「我們押注在 beta 上，出事再說」，
> 而是「退出路徑每季實測，最後一次是 evidence.json 上的日期」。
> 第二種採購會接受 —— 差別不在風險大小，在於風險**是否被證明是有界的**。

---

## 2–3. 資安 — 原生二進位的 SCA 例外

**要決定的**

- 核准 **144 個平台原生二進位、12 個家族**的例外。建議按**政策**核准
  （「原生編譯的工具鏈套件」），不是逐廠商 —— 因為 `@typescript`（TypeScript 7 自己）
  與 `lightningcss` **都不是 vite-plus 帶來的**，是整個前端工具鏈原生化的結果
- 接受佐證**分兩級**：101 個有 SLSA provenance（可回推到來源 repo 的確切 commit），
  43 個只有 npm 發佈簽章（可驗發佈者，無法回推建置來源）

**拿什麼去談**

```bash
./node_modules/.bin/vpr sca-dossier
```

申請書全部由 `pnpm-lock.yaml` 推導，**不要手改**。內容含逐家族明細、佐證等級、
授權分佈、容量，以及四項**由閘門實際斷言**（非宣稱）的補償控制：

- 574 個套件全帶 sha512 integrity，CI 以 `--frozen-lockfile` 安裝
- `allowBuilds` 內沒有任何原生套件 → 那 144 個在安裝時不執行任何腳本
- lockfile 的 digest 與擷取當下一致，每次 gate 比對
- 家族清單進版控，新家族出現時閘門擋下並要求人工分類

> ⚠️ 43 個沒有 provenance 的那批含**全部 20 個 `@typescript/typescript-*`**。
> 若沿用舊說法「證據是 SLSA provenance」，覆核抽驗 TypeScript 時會當場破功。

---

## 4. 法務 — 兩件授權問題

| 事項                 | 範圍                                                                | 說明                                                                                       |
| -------------------- | ------------------------------------------------------------------- | ------------------------------------------------------------------------------------------ |
| **MPL-2.0**          | `lightningcss-*` 22 個（兩個版本）＋ `eslint-plugin-no-unsanitized` | 檔案層級弱著作權。三者皆僅作**建置期工具**、產物不含其原始碼，但多數企業授權政策會標記出來 |
| **完全沒有授權聲明** | `@yuku-parser`／`@yuku-codegen` 共 22 個                            | 見下方查證結果 —— 比「registry 上沒填」更嚴重                                              |

#### ⚠️ MPL-2.0 的範圍比申請書上看到的大（2026-08-16 發現）

`eslint-plugin-no-unsanitized@4.1.5`（Mozilla）也是 MPL-2.0，而**它不在
`vpr sca-dossier` 的〈授權分佈〉裡** —— 那一節只涵蓋 144 個**原生二進位**，
一個純 JS 的直接相依對它是隱形的。

這不是申請書寫錯，是它的取數範圍就是原生二進位（R2 的問題）。缺口由
`vpr capture-health` 補上：它掃的是 24 個**外部直接相依**，
判定與申請書共用同一份 `COMMON_LICENSES`。

> 談的時候把兩者一起講，否則法務核准了「11 個 lightningcss」之後，
> 覆核時會發現還有一個沒被提到的 MPL-2.0 —— 那比一開始就講出來難處理得多。

#### `@yuku-*` 的實際情況（2026-08-15 逐一查證，不是推測）

拆開 `@yuku-parser/binding-linux-x64-gnu@0.5.48` 的 tarball，裡面**只有兩個檔案**：

```
package/package.json      321 B   ← 沒有 license 欄位
package/yuku-parser.node  3.8 MB  ← 原生二進位
```

**沒有 LICENSE 檔。** 而且上層套件 `yuku-parser@0.5.48` 的 tarball 同樣**沒有 LICENSE 檔**
（只有 package.json／README.md／binding.js／decode.js／index.d.ts／index.js／walk.js），
它僅在 package.json 的 metadata 字串宣告 `MIT`。

所以要向法務問的問題是明確的，不是「請確認一下」：

> 22 個原生二進位（每個約 4 MB）進入我們的建置環境，**發佈的成品裡沒有任何授權文字**。
> 唯一的授權宣告是母套件 metadata 的一個字串。這樣可以接受嗎？
> 還是要求上游（`github.com/yuku-toolchain/yuku`）在 binding 套件補上 `license` 欄位與 LICENSE 檔？

順帶一提，這 22 個是 `@voidzero-dev/vite-plus-core` 拉進來的（Zig 寫的 JS/TS parser），
**不是我們直接選用的相依** —— 換句話說這是 vite-plus 的供應鏈帶進來的問題。

工具刻意**不**從母套件推斷授權填進申請書：**從別的套件推斷授權等於代發佈者做法律聲明。**

清單在 `vpr sca-dossier` 的〈授權分佈〉一節（記為 `UNKNOWN`，22 個）。

---

## 5–7. 平台／IT — 內部 registry 與封閉網路

> **2026-08-16 換了理由，結論反而變硬。** 原本這三列掛在一個假設上：
> 「貴組織可能是封閉網路」。重評時先確認你們的 **CI 與開發機能直連公網** ——
> 那一度讓這三列看起來可以拿掉。
>
> **問錯了軸。** 建置在你們這側，但**交付／驗收在機關端**，而契約要求
> 原始碼交付與機關端重建。所以這三列不但留著，而且從「可能用得到的預防措施」
> 變成**交付物**：`vpr mirror-manifest` 的清單、`vpr airgap` 的前置條件，
> 都是隨原始碼一起交出去的東西。
>
> 觸發條件因此是可驗證的，不再是猜測：**契約有沒有要求機關端重建或原始碼交付。**
> 沒有的話這三列不適用；有的話它們是硬的。

**要做的**

```bash
./node_modules/.bin/vpr mirror-manifest   # 574 筆，含 sha512，可直接餵給鏡像工具
./node_modules/.bin/vpr airgap            # 前置條件、平台矩陣、驗收方式
```

三個容易踩的點：

**① 鏡像清單不能照著安裝結果列。** `pnpm` 只裝符合當下平台的 optional dependency，
所以照 `node_modules` 或 lockfile 的 `snapshots:` 區產出的清單，
**必然只涵蓋產清單那台機器的平台** —— 那正是「mac 裝得起來、CI 的 linux-x64-gnu 直接爆」。
`vpr mirror-manifest` 讀的是 `packages:` 區（全平台中繼資料）。

**② 有一批不在專案的相依樹裡。** `pnpm`、`@pnpm/exe` 與 7 個平台變體、`@reflink`、
`detect-libc` 共 **19 個**，住在 `pnpm-lock.yaml` 的**第一份 YAML 文件**。
它們是 `vp` 為了啟動自己而取得的。漏掉的症狀不是某個套件裝不起來，
是 **`vp` 連跑都跑不起來**。

**③ registry 一定要設在機器／映像檔層級。** 實測四種設法：

| 設法                               | 涵蓋 `vp` 下載 pnpm？       |
| ---------------------------------- | --------------------------- |
| 專案 `.npmrc` 的 `registry=`       | ❌ **否**                   |
| 全域 `~/.npmrc` 的 `registry=`     | ✅ 是                       |
| `npm_config_registry` 環境變數     | ✅ 是（CI 用這個）          |
| `devEngines.packageManager.onFail` | ❌ **否**（不是下載的開關） |

第一列會製造一種很難察覺的假象：專案相依確實走了內部 mirror，於是全隊都相信
封閉網路沒問題 —— 但 `vp` 的第一步仍在往公網連，要到真的斷網那天才會發現。

**容量**：原生二進位部分 tarball 合計 **856 MB**（mirror 要存的），
解壓後 **2341 MB**（開發機與 CI 的 `node_modules` 佔的）。

**驗收**（在真的連不到公網的機器上，用乾淨的 HOME）：

```bash
HOME=$(mktemp -d) npm_config_registry=https://<內部位址>/ vp install --frozen-lockfile
```

乾淨的 HOME 是重點 —— 開發機上的 pnpm 快取會讓這個測試假性通過。

**待確認，而且現在有價目了**：四個目標平台裡，`linux-x64-gnu`（CI）與 `darwin-arm64`（開發機）
有依據，**`darwin-x64` 與 `win32-x64` 是假設**。`vpr airgap` 的〈要鏡像到哪些平台〉
現在會列出每個平台各要多存多少 —— 例如 `darwin-x64` 是 10 個套件、49 MB。

這樣「要不要支援 Intel Mac」就變成一個帶價目的決定，而不是憑印象回答的問題。
確認後請一併改 `tools/supply-chain/src/inventory.ts` 的 `TARGETS`（閘門會驗兩個方向）。

> ⚠️ **2026-08-16：這個問題的對象變了。** 原本問的是「你們的開發機有沒有 Intel Mac」，
> 那是一個內部盤點。現在問的是**機關端的重建環境跑什麼平台** —— 而政府機關的
> 建置機器是 `win32-x64` 的機率遠高於開發團隊。
>
> 而且它有牙齒：`TARGETS` 餵給供應鏈閘門，[`inventory.ts`](tools/supply-chain/src/inventory.ts)
> 要求 toolchain 家族**必須涵蓋全部 `TARGETS`**。少列一個平台，
> 申請書與閘門會一起在那個平台上失明 —— 而發現的時機是機關端重建那天。
>
> **這一格請在投標階段問，不是動工之後。**

---

## 8. 架構 — D8 同源中間層由誰提供

**要決定的**：那一層掛在哪、由誰維運。腳手架不提供它 —— 它是組織既有的 gateway。

**已經備好的**：它必須做到什麼（13 條可執行契約），以及怎麼證明做到了。

```bash
# 已經有 gateway：
BFF_ORIGIN=https://gateway.internal ./node_modules/.bin/vpr bff-check
# 全綠 = R6 關閉，不需要新程式碼

# 還沒有：照 platform/bff-contract 的規格蓋，platform/bff-mock 是已通過它的參考實作
```

**好消息是它比原本想的便宜一個量級**：建置產物**零個 inline script**，
所以 **CSP 不需要 nonce**。不需要 nonce 就不需要逐次請求改寫 HTML 的中間層 ——
組織端的要求從「準備一個會改寫 HTML 的東西」降到「多送幾個回應標頭」。
這個前提由 `assertStaticCspCompatible()` 在每次建置守著。

**誠實的限制**：`POST /api/session` 在真實環境是 OIDC 授權碼流程的終點，無法自動走完。
驗收時屬性面（HttpOnly／Secure／SameSite／Path）用 `BFF_SET_COOKIE_FILE` 貼上
gateway 實際回的 `Set-Cookie`，行為面用 `BFF_SESSION_VALUE` 帶一組真實 session。

---

## 9. 發版流程 — 一條硬性限制要寫進去

> **封閉環境裡無法就地升相依。**
> 改 `pnpm-lock.yaml` 與跑 `vpr supply-chain --capture` 必須在**還連得到公網的那一側**
> 做完，兩份檔案一起隨變更進到封閉環境。

原因：供應鏈閘門在 `provenance.json` 與 lockfile 對不上時直接失敗，
而重新擷取需要 `registry.npmjs.org`。

這是設計的結果不是缺陷 —— 反過來讓閘門自己連公網補資料，它就會在最需要它的環境裡失效。

**閘門現在會在紅燈當下自己講完這段**（2026-08-15 補）。原本它只說「跑 --capture
（需連得到 registry.npmjs.org）」，而在封閉網路裡看到那行的人會去跑一個永遠不可能
成功的指令，然後懷疑是網路設定壞了。現在三條失敗路徑都會附上「這件事不能在這裡做」。

**但流程文件仍該寫。** 錯誤訊息教的是「現在該怎麼辦」，流程要回答的是
「誰在什麼時候做、產出的兩個檔案怎麼一起進來」—— 那是工具講不了的。

> **2026-08-16：這一列從 ⬜ 升到硬性，因為它終於有了真實用例。**
> 原本它是一條寫給假想封閉環境的紀律。現在的形狀很具體：
> **你們在辦公室（連得到公網）跑 `--capture`，兩份檔案隨原始碼交付進機關端。**
> 漏掉其中一份，機關端重建時閘門會紅，而那是**現場修不了**的紅 ——
> 因為修它需要連 `registry.npmjs.org`，而那台機器連不到。
>
> 所以流程文件要寫的是**交付清單**：`pnpm-lock.yaml` 與 `provenance.json`
> 一起進交付包，而且是同一次擷取的產物。

---

## 10. 資安 — SCA 掃描範圍

`bingo`（產生器框架）由 `tools/slice-gen` 使用，屬**建置期／開發期**相依，
不進入任何交付產物。此分類**已由閘門斷言**：`apps`／`features`／`platform`
之下沒有任何 package 宣告 `bingo` 或 `@org/slice-gen`。

納入掃描範圍時請標為 **dev-only**，與 runtime 相依分開計算嚴重度。

---

## 11–13. 其餘待辦

**11 — CI 已在 GitHub Actions 上實跑過了**（2026-08-15，公開 repo）。
Tier 1 一次就綠；Tier 2 首跑紅，抓到三個本機看不到的問題，**都已修掉**
（根 `package.json` 漏宣告 `vitest`、`if: always()` 涵蓋範圍過大、
一個過時且寫著錯誤數字的 provenance 步驟）—— 詳見 `DECISIONS.md` 的 C32。

**剩下兩處仍需貴組織的環境才驗得了**：

1. **bootstrap 步驟**在內部 registry 下需設 `npm_config_registry` 與 `NODE_EXTRA_CA_CERTS`
   （公網環境驗不出來，因為它預設就通）
2. **SBOM／SCA 工具**目前用 Trivy。若貴組織用 Blackduck／Snyk，這兩步要換 ——
   交付稽核的必須是**稽核認可的那個工具**的輸出。
   **換工具時務必先驗這兩件事**（Trivy 兩件都中了，見 C33／C34）：

   | 要驗什麼                              | Trivy 的情況                           |
   | ------------------------------------- | -------------------------------------- |
   | 會不會把 dev 相依整批抑制掉？         | **會**。預設抑制 → SBOM 0 個 component |
   | 讀不讀得到多文件的 `pnpm-lock.yaml`？ | **讀不到**。只解第一份 → 只看到 19 個  |

   驗法不必靠人記得：閘門裡的 `vpr supply-chain --verify-sbom` 會比對
   SBOM 的 component 數與 lockfile 的套件數，任一種失明都會變紅。
   **換工具時保留這道檢查，比換對工具更重要。**

**12 — `platform-codemod` 標籤已建好**（2026-08-15）。剩下的是**自動核准要不要開、怎麼開**，
而那是一個**放寬 review 的安全決定**，不該由實作者單方面做。

要決定的是這件事：

> GitHub 的 **triage** 權限就能貼標籤。所以「貼上 `platform-codemod` 就自動核准」
> 等於**任何有 triage 權限的人都能繞過 CODEOWNERS**，包含繞過 `@org/security`
> 共同持有的那幾條路徑（`eslint.config.js`、`.npmrc`、`.github/workflows/`、
> `platform/security-headers/`、`tools/supply-chain/`）。

三個選項，成本與風險差很多：

| 做法                                       | 風險                                           |
| ------------------------------------------ | ---------------------------------------------- |
| 直接自動核准帶標籤的 PR                    | **等同把 CODEOWNERS 變成建議**。不建議         |
| 自動核准，但**排除資安共管的路徑**         | 可接受。需要在 workflow 裡維護一份路徑排除清單 |
| 不自動核准，改成把標籤當**優先審查的訊號** | 零風險，但沒解決核准地獄                       |

若選第二種，workflow 還必須確認**貼標籤的人不是 PR 作者**，否則作者自己貼自己過。

**這一項我刻意沒有實作**：它不是技術難題，是「要放寬多少 review」的組織決定，
而錯誤的版本會安靜地讓前面所有 CODEOWNERS 設計失效。

**13 — CSP 由 report-only 切成 enforce** 由 gateway 執行。目前 dev 已套用 report-only，
政策的單一事實來源在 [`@org/security-headers`](platform/security-headers)。
切換前請先看一段時間的 violation 報告。

---

## 14. 架構／設計 — 樣式策略與設計系統擁有權 ✅ 兩格都已決策（2026-08-15／08-16）

**決策：shadcn-vue（reka-ui ＋ Tailwind v4），元件原始碼住 `platform/ui`。**
完整理由見 [`DECISIONS.md`](DECISIONS.md) 的 **D15**，市調見
[`UI-SURVEY.md`](UI-SURVEY.md)，可重跑的工具在 [`tools/ui-survey`](tools/ui-survey)。

```bash
node tools/supply-chain/src/cli.ts --capture-health   # 授權與維護狀態（實際相依，非候選）
node tools/ui-survey/src/cli.ts --csp        # 執行期 <style> 注入探測
node tools/ui-survey/src/cli.ts --sca        # 供應鏈增量
```

### 最後一格也結案了 ✅（2026-08-16）

**問的是：設計系統與應用外殼，是不是同一個團隊的事？**
**答案：不是，而且維持 `/platform/` 通則 —— CODEOWNERS 零 diff。**
完整論證在 [`DECISIONS.md`](DECISIONS.md) 的 **C62**。

⚠️ **零 diff 不等於沒有決定。** 這一節與 `CODEOWNERS` 裡那段註解是同一件事的
兩面：決定的結果剛好是「檔案不變」，所以能分辨「決定不動」與「還沒決定」的
**只剩那段字**。C40 記的是相反方向的同一個錯（no-op 配說明，讀起來像做過決定）。

| 選項                          | 為什麼出局                                                                                   |
| ----------------------------- | -------------------------------------------------------------------------------------------- |
| 沿用 `@org/team-platform-ui`  | 方向反了：`platform/*` 跨案共用、會發成內部套件，`apps/console` 逐案。而且是**取代**不是疊加 |
| 另設設計系統小組              | 「要真的有人，否則就是掛一個不會回應的名字」—— 對會被 fork 的腳手架，這是最糟的預設值        |
| 雙簽（＋`@org/design`）       | **機制不成立**：同列多個 owner ＝ 任一核准即可（見第 25 項）。買不到共簽                     |
| **維持 `/platform/` 通則** ✅ | 它已經是業界講的 hybrid 模型 —— 見下                                                         |

**選它的正面理由，不是「懶得動」**：CODEOWNERS 那段
`── platform/ 由跨團隊抽調的 maintainer 把關（D12）──` 底下寫的
「任何人都能提 PR，但需 maintainer 核准；既不製造瓶頸、也不放任無主」，
就是設計系統治理裡 hybrid 模型的定義（核心團隊守基礎與治理、產品團隊貢獻擴充），
而 hybrid 是 10–50 人組織的業界預設。Nathan Curtis 的反面證據更硬：
純聯邦制的設計系統活下來的比例是 **0%** —— 聯邦的人被產品優先序驅動，
不會把系統工作排進去。**基礎必須有一個真的有人的中央擁有者，
而 `platform-maintainers` 已經是了。**

> **它買不到什麼（要讀，這是這一格真正的結論）**
>
> `platform/ui` 是唯一一個所有切片都依賴的 UI package，它的破壞性變更由
> `tools/api-surface` 擋下 —— 現在那道閘門**有收件人了**（platform-maintainers）。
>
> 但**無障礙仍然沒有具名負責人**。歸屬清楚了，職能沒有：platform-maintainers
> 是平台工程角色，不是設計角色。業界的 hybrid 分工把無障礙標準劃在核心團隊
> 那一側，而我們的核心團隊沒有那個職能。所以第 22 項那四個行為面缺口
> （焦點順序、對比度、標題階層、只靠顏色傳達狀態）現在**沒有一個具名的人在看**，
> 而靜態閘門對它們零命中。
>
> 這是本決定的**代價**，不是待補的工作。要買回來的方式是組織裡真的有設計職能，
> 不是在 CODEOWNERS 加一列 —— 那一列我們現在知道也買不到（第 25 項）。

### ⚠️ 但在那之前：整份 CODEOWNERS 現在一條都沒有生效

查證的時候發現的，而且與 `platform/ui` 無關 —— **每一條的 owner 都是
`@org/*` 佔位符，GitHub 因此判定全部 `Unknown owner`**：

```bash
gh api repos/<owner>/<repo>/codeowners/errors
```

> ⚠️ **這裡刻意不寫「幾條無效」。** 那個數字是 GitHub 的判定，不是這個 repo 裡
> 數得出來的東西 —— 2026-08-15 實測回報 22 條，而當時檔案是 14 條條目、
> 21 個 owner 引用，三個數字互不相等。
>
> 條目數（可推導）由 `tools/doc-facts` 守著；「幾條被 GitHub 判無效」
> 只能當場跑上面那行。第 18 項的判準：**不可推導的數字不要讓它看起來像被守著的。**

原因是這份腳手架裡的 `@org/*` **全部是佔位符**。在還沒替換成真的組織與團隊之前：

- PR 不會自動指派審查者
- 保護分支的「需要 owner 核准」沒有東西可要求
- 也就是說 **D12 的整套擁有權治理目前是一份文字檔**

而 `tools/conformance` 驗的是**條目存在**，不是 **owner 有效** ——
**存在不等於生效**，這道落差本機看不出來，只有 GitHub 知道。

**採用這份腳手架的第一件事**：把 `@org` 全部換掉，然後跑上面那行確認 0 錯誤。
已寫在 `CODEOWNERS` 的檔頭，因為那是會被讀到的地方。

> ⚠️ **2026-08-16 補充：換掉佔位符只是三層裡的一層，做完不會讓它生效。**
> 另外兩層（分支保護沒開、同列多個 owner ＝ 任一核准即可）在**第 25 項**，
> 論證在 C63。這一段原本讀起來像「拆掉這道就好了」—— 它不是。

### 實作待辦 ✅ **五項全部完成**（2026-08-15，見 C38／C39）

| #   | 事項                                     | 狀態                                                                                       |
| --- | ---------------------------------------- | ------------------------------------------------------------------------------------------ |
| 1   | `@tailwindcss/vite` 加進 `DRILL_PLUGINS` | ✅ 已登記，並補上**產物比對**（反向測試：拿掉 @source → 37%，紅）                          |
| 2   | 重跑 `vpr supply-chain --capture`        | ✅ 519 套件／144 原生／12 家族。新家族 `@tailwindcss` 已分類為 toolchain                   |
| 3   | 新增 `platform/ui` package               | ✅ CODEOWNERS（暫掛）、api-surface（11 個進入點／101 個 export）、退出演練 alias 都已接上  |
| 4   | 擋住「切片自己拼一套設計系統」           | ✅ 擋的是 **import 而非目錄**（理由見契約）；另加全 repo 禁用 reka-ui Splitter（CSP）      |
| 5   | 瀏覽器實測 CSP                           | ✅ 五個探針全對（Chrome 148）。⚠️ 證據檔與守它的閘門已於 2026-08-16 拆除（C52）—— 見下方註 |

> **第 5 項的現況（2026-08-16 更新）**：實測本身仍然成立 —— 五個探針在
> Chrome 148 上全對，論證留在 `DECISIONS.md` 的 C39／C47，重驗的步驟留在
> `tools/csp-verify/README.md`。**拆掉的是「把那次結論存成證據檔、由 CI 守其
> 指紋有效期」那套機制**（C52）：它每次失效都要人開一次瀏覽器，是所有閘門裡
> 每次成本最高的一道，而觸發它的是升 `reka-ui`／`vue`／`tailwindcss` 這種常見動作。
>
> 對審查者的意義：**「CSP 在 enforce 下實測過」是一份 2026-08-15 的紀錄，
> 不是一個持續成立的保證。** 升過相依之後要不要重驗，現在靠人記得。

### 會連動到本清單其他項的兩件事

- **第 4 項（法務）範圍會變大**：MPL-2.0 從「`lightningcss-*` 11 個」
  變成**兩個版本各一組**。`@tailwindcss/node` 釘死 exact `1.32.0`，
  而 `vite-plus-core` 要 `^1.33.0` —— 範圍不相交，無法合併。
- **第 5／6 項（封閉網路）**：新套件必須在**連得到公網那一側**跑
  `--capture` 再一起送進來。閘門刻意不自己連公網補資料。

### 為什麼沒有選比較便宜的那條

element-plus 只要 +21 套件、0 原生二進位、0 授權旗標，CSP 也已驗證零風險。
選了比較貴的一條，是因為成本花在**可以攤提的地方**：元件原始碼在自己手上。
完整的三方比較留在 `UI-SURVEY.md`，包含被淘汰者的理由 ——
其中 **PrimeVue 已於 2026-06-28 改為商業授權**，而市面上的比較文章
到今天仍然把它列為企業首選。

---

## 15. repo 管理者 — 把 `@org/*` 換成真的團隊（採用的第一步）

**`CODEOWNERS` 裡的 20 條條目全部是佔位符。在替換之前，這份檔案一條都沒有生效。**

GitHub 對每一條都會判 `Unknown owner`：PR 不會自動指派審查者，
保護分支的「需要 owner 核准」也沒有東西可要求。**D12 的擁有權治理目前
是一份文字檔。**

> ⚠️ **這句話的兩半，只有一半被守著。** `20` 這個數字由 `tools/doc-facts`
> 守（來源是 CODEOWNERS 的條目數）；但「**全部是佔位符**」這個斷言沒有
> 任何東西在守 —— 你們做完這一項之後，條目數還是 20，而那句話變成假的，
> 不會有紅燈。
>
> 補的方法很便宜（推導「owner 全是 `@org/*` 的條目數」，要求它等於條目數），
> 刻意先不做：這一項本來就是**要你們動手把它變假**的那一項，而做完之後
> 這整節都要重寫，不只那個斷言。**改的時候記得連這句一起改。**

危險的地方在於它**本機看不出來**：`tools/conformance` 驗的是「每個
`features/*` 在 CODEOWNERS 有條目」—— 條目存在，所以它綠。
存在不等於生效，而只有 GitHub 知道差別：

```bash
gh api repos/<owner>/<repo>/codeowners/errors
```

本 repo 2026-08-15 實測：**22 條全部 Unknown owner**（見 C40）。

**刻意沒做成閘門**：它需要 GitHub API，會因為 token 權限與速率限制而變紅，
而不是因為真的有問題 —— 那種紅燈會被關掉（與 `tools/ui-survey` 不進 gate
同一條理由）。替換的清單與每條的用意都寫在 `CODEOWNERS` 檔案本身，
採用時從頭讀一遍即可。

⚠️ 這一項與第 12 項（自動核准要不要開）是**同一套機制的兩端**。
15 沒做，12 就沒有意義 —— 沒生效的 CODEOWNERS 沒有東西可以繞過。

---

## 16. 平台（實作）— 幽靈依賴 ✅ 已實作（2026-08-16）

**`tools/conformance` 現在會擋「程式碼 import 了某個套件，但 `package.json` 沒宣告它」。**
掃 `features/*`、`platform/*`、`apps/*` 的每一個 package，比對該 package 自己的
`dependencies` ∪ `devDependencies` ∪ `peerDependencies`。

```bash
node tools/conformance/src/cli.ts
```

**⚠️ 剩下的兩個範圍取捨，是這一項現在唯一的內容**（都先乾跑量過，不是憑感覺）：

| 不掃什麼      | 為什麼                                                                                    | 代價                                         |
| ------------- | ----------------------------------------------------------------------------------------- | -------------------------------------------- |
| **`tools/*`** | 產生器與 codemod 的本職就是**把程式碼當資料拿著**。乾跑在那裡噴出 20 幾條，一條都不是真的 | 開發期工具的幽靈依賴看不到。它們不隨產物交付 |
| **`tests/`**  | 同一個理由的小號：測試用樣板字串**組出**假的原始碼餵給被測物                              | **測試檔裡的幽靈依賴看不到**                 |

第二個代價可以接受的理由是**失敗方向不同**：測試少一個相依會**當場跑不起來**，
不會安靜地混到驗收那天。真正致命的是 `src/` 那一半，而那一半守住了。

> ⚠️ 但**不要讓它看起來像被守著的**（第 18 項的判準）。所以那個取捨在
> `tests/negative.test.ts` 裡是一條**會執行的斷言**：它從綠變紅的那天，
> 代表有人改了取捨 —— 而不是靠人記得讀這一段。

**反向測試 6 條**，其中 4 條標 ★（驗「不該紅的時候不會紅」）。實測竄改：
把註解剝除改成 no-op → ★ 那條紅，**而且真正的 repo 也紅**（`contract.ts` 的
JSDoc 拿 `import { useQuery } from "@tanstack/vue-query"` 當反例）——
也就是說那個偽陽性是真的，不是假想的。

---

### 原本的缺口描述（保留，因為它解釋了規則為什麼長這樣）

**這一項原本寫著：全 repo 沒有一道閘門檢查「import 了但 `package.json` 沒宣告」。**

2026-08-15 做 C41 時從反面撞到：`features/shipment` 宣告了 `@org/ui`
卻還沒 `install`，rolldown 的嚴格解析當場擋下（訊息清楚、指到正確的檔案）。
但那是**宣告了卻沒裝**。反過來的**裝了卻沒宣告**沒有東西在看：

- pnpm 的嚴格 `node_modules` 通常會擋下來 —— 但 workspace 根目錄的提升、
  `vite.config.ts` 的 alias、以及被別的套件間接帶進來的相依，都是繞過它的路徑
- 症狀是**本機與 CI 全綠，退出演練或單獨發佈時才爆** ——
  而那正是最難回推原因的時機（D2 的整個論證建立在「應用原始碼零依賴」上）

**刻意沒有夾帶進 C41 那次改動。** 只補 `@org/ui` 一個套件會讓規則讀起來
比它解的問題還窄；這條缺口涵蓋 `@org/http-client`、`@org/slice-kit`、
`@tanstack/vue-query` 等每一個依賴。它該是一條**一般性**規則，
用 `IMPORT_SPECIFIER_PATTERN` 掃出所有裸模組名，比對 `package.json` 的
`dependencies` ∪ `devDependencies`，再排除 Node 內建與相對路徑。

放在 `tools/conformance`，與現有各條同一個位置。估計半天。

> **2026-08-16 重評時這一列升級最多，然後當天就做掉了。**
>
> 上面列的兩個失敗場景（退出演練、單獨發佈）**都是假設性的** —— 它們是
> 「有一天可能會做」的動作。重評時出現第三個，而且它寫在契約裡：
>
> > **機關端重建時才爆。而那是驗收現場。**
>
> 幽靈依賴的性質剛好最適合在那裡爆：你們的機器上它被提升、被 alias、
> 被別的套件間接帶進來，所以本機與 CI 全綠；機關端是一份乾淨的重建，
> 那些繞過路徑一個都不存在。
>
> 半天的估計是準的。**真正花時間的不是偵測，是量偽陽性** —— 先乾跑一次
> 看它會噴什麼，才決定範圍。那一步不做的話，這道閘門會噴 20 幾條、
> 上線第一週被加上例外，然後例外永遠不會拿掉。

---

## 17. 平台（實作）— ~~`api-surface` 看不見型別形狀的破壞性變更~~ 已重做

> **2026-08-16 做掉了。** 下面整段的現狀描述改用過去式讀 —— 保留是因為
> 它是這道閘門為什麼長成現在這樣的唯一說明，而**新增的兩條限制寫在最後**。

**~~`tools/api-surface` 只比對「模組 → export 名稱」。在一個匯出的 interface 上
加一個必填屬性，它完全不會說話。~~**

2026-08-16 做 §11 II ⑨ 時第一次撞到：`Feature` 加了必填的 `personalData`，
這對 `@org/slice-kit` 是破壞性變更 —— 下游**每一個既有切片都會編譯失敗** ——
而閘門只報了 `@org/pii` 那 7 個新 export。基準檔記的是：

> ⚠️ **那個欄位已於同日隨 C52 回退**，所以下面這個例子現在是歷史。
> **盲點沒有跟著消失** —— 它仍然是唯一一個實際撞到過的證據，
> 而且回退本身也是同一種變更（移除必填欄位），閘門同樣沒有說話。

```json
"@org/slice-kit": ["defineFeature", "registerFeatures"]
```

名稱一個都沒變，所以它是對的、也是瞎的。

D12 的規則是「改 platform 的破壞性變更必須附 codemod」，而這是**第一個那個
機制看不見的破壞性變更**。本 repo 的四個使用端都改好了，現況一致；
但這個腳手架是拿來被複製的 —— 下游團隊升級 `slice-kit` 只會拿到一個型別錯誤，
沒有遷移路徑，也沒有任何東西提醒作者該寫 codemod。

要修的話，基準檔得從「名稱清單」變成「名稱 → 型別形狀摘要」，
而摘要要穩定到不會因為換個型別別名就漂移。那是 api-surface 的一次重做，
不是一個補丁。估計 1–2 天。

⚠️ ~~**在修好之前，改 `platform/*` 的 interface 要靠 review。**~~
這一條之所以寫下來，是因為「機制看不見」與「沒有破壞性變更」在閘門輸出上
長得一模一樣 —— 而那正是這個 repo 一路在防的形狀。

### 做完之後的樣子（2026-08-16）

基準檔升到第 2 版：記的是「名稱 → 型別形狀」。跑一次會印出當前的
進入點數與 export 數（那兩個數字現在由 `doc-facts` 守著，不要手抄）。

新增被守住的東西：**型別 export 的每一個成員**（`Feature` 加一個必填欄位
會紅）、**class 的建構子與成員**（`HttpError` 建構子加一個參數會紅）、
**索引簽章**、以及 **`.vue` 元件的 props**。最後一項要特別講：
`declare module "*.vue"` 讓 checker 對每個元件都回報同一個
`DefineComponent<Record<string, unknown>, …>`，實測連 `UiButton` 與
`UiDialog` 都分不出來 —— 所以元件的形狀是另外直接解析 SFC 取得的。

合法出口跟著擴充：`surface.json` 的 codemod 登記多了 `changes` 欄位。
少了它，催生這次重做的那個變更（加一個必填欄位）**沒有任何辦法通過** ——
而一道對合法變更沒有出口的閘門，最後被拿掉的是閘門。

⚠️ **兩條新的限制，都是實測逼出來的：**

1. **公開簽章不得引用沒有 export 的型別。** 沒有任何 `NodeBuilderFlags`
   能把私有型別展開成結構（掃過一輪），所以形狀裡會留下它的名字 ——
   改個名就漂移，然後被判成破壞性。改成擋在前面，補救是加一個 `export`。
   TypeScript 自己**不禁止**這件事（`tsc --emitDeclarationOnly` 實測不報錯），
   所以這是本閘門的前置條件，不是語言規則。
2. **`platform/ui` 的元件不能用 `defineEmits` / `defineSlots` / `defineExpose`。**
   用了會直接丟例外 —— 因為那時 props 就不再是完整的公開面，而記下一份
   看起來完整的殘缺形狀，比不記更糟。要用的話先擴充那支解析。

**還沒守到的一格**：**字面量、陣列、tuple** 形態的常數，型別變更被判成
「相容」，只要求更新基準。它們的型別是內容的投影，判成破壞性等於每改一條
設定就要人寫一份不存在的 codemod。

⚠️ 這一格原本比現在寬得多：任何「沒有呼叫簽章」的 export 都算純資料，
於是 `@org/config` 的 `config` 物件拿掉一個欄位也算相容 —— 而每個讀
`config.appTitle` 的地方都編不過。那與上面那句「判準只有一條：下游會不會
編不過」直接矛盾，也是這個 PR 剛在 `UiButton.vue` 修掉的同一種毛病：
**一句不成立的保護聲明**。已收緊：匿名物件（`config`、`LAYERS`、`http`）
改記成員，走嚴格比對。

> **2026-08-16：從 ⬜ 升到 ✅，因為「下游」不再是假設。**
>
> 上面那句「這個腳手架是拿來被複製的」原本是一個意圖。現在它是既定的做法：
> **`platform/*` 發成內部套件，各案 fork 之後仍會升級它。**
>
> 所以那個「下游團隊只拿到一個型別錯誤」的場景有了具體的發生地點與時機：
> **某一案在驗收期升了內部套件，編不過，而沒有遷移路徑。** 那時候要接電話的
> 是你們自己的平台組。
>
> 1–2 天的重做在這個前提下划算了。原本排在第 16 項後面（那個半天、
> 而且失敗現場更早）—— **第 16 項已於同日做掉，所以這一項現在是隊首。**

## 18. 平台（實作）— 文件數字守衛的守備範圍仍然不完整

**被守的那幾份文件裡，還有別的數字沒有登記給 `tools/doc-facts`。**

登記了幾個事實、幾條引用樣式，**這裡刻意不寫** —— 那正是這道閘門在防的東西。
要當前的數字就跑一次，它自己會印：

```bash
./node_modules/.bin/vpr doc-facts
```

> ⚠️ **2026-08-16 這一項讓步驟自己示範了一次。** 出初版前重掃文件，
> 發現〈先看這張表〉裡三個數字過期得很嚴重（`121`→144、`32`→43、
> `467`→563），而這道閘門一直是綠的 —— 因為那三句話從來沒有被登記過。
>
> 那張表是拿去開會投影的一頁，也是採購與資安**唯一會讀完**的一頁。
> 已補登記（含新的 `slsa` 事實），並在 `facts.ts` 檔頭寫下修正後的順序：
> **新增引用時先問「這句話會不會被單獨拿出去用」，會的話優先登記。**
>
> ⚠️ **同一次掃描還發現守備清單本身漏了一份。** `UI-SURVEY.md` 從來不在
> 被守的文件裡 —— 理由很平庸：它不在根目錄那三份的直覺清單中。
> 但 README 與本檔第 14 項都把讀者**直接指過去**，而它當下就有一句過期的
> （`467` 套件／`121` 原生二進位）。已補進 `GUARDED`。
>
> 判準寫下來了：**被 README 或 HANDOFF 指過去、而且用現在式寫的，就要進去。**
> 讀者不會知道某一份文件的守備等級比較低。

沒守的分兩類，而只有第一類是待辦：

1. **有事實來源、還沒登記** —— 加一條登記就守得住，每條約十分鐘。

   > **2026-08-16：`platform/*` 的進入點數與 export 數已登記。**
   > 補的當下那句話兩個數字都是錯的（寫 9／65，實際 10／92）—— 也就是說
   > 「知道它沒守」這件事已經寫在這一格裡一段時間，而那段時間裡它一直是錯的。
   > **列在待辦裡不等於被守著。**

   > **2026-08-16（第二輪）：又補了五筆，而這一格自己的例子是錯的。**
   >
   > 這一格原本寫著「例如切片數（來源：`features/*`）」。掃過三份被守的文件，
   > **沒有任何一句在講切片數** —— 那個待辦指向一句不存在的話。
   > 登記一個零引用的事實只會讓閘門對著空氣紅。
   >
   > 改成先掃「所有計數式句子」再逐一分類，補上五筆：
   > 契約條目、workspace 套件數、workflow 的 `uses:` 引用處、不重複 action 數、
   > CODEOWNERS 條目數。**補的當下有兩筆是錯的**：
   >
   > - 「16 處引用」實際是 **17** —— 而那句話是**同一天**寫下的（第 23 項），
   >   寫的人（同一個）當時沒有數
   > - 「22 條 CODEOWNERS」實際是 **20 條條目** —— 見下面那條降級
   >
   > 登記還逼出一個歧義：**同一個詞「N 個 action」被用來指兩件事** ——
   > 一處指引用處（16），一處指不重複的 action（8）。兩個事實共用一個講法時，
   > 沒有任何樣式分得開它們。句子已改成「N 處引用」與「N 個 action」。

   > ⚠️ **一筆從第一類降到第二類：CODEOWNERS 的「22 條 Unknown owner」。**
   >
   > 那個 22 來自 `gh api …/codeowners/errors` —— **GitHub 的判定，不是 repo 裡
   > 數得出來的東西**。實測 C40 量到 22 的那個 commit，本地是 14 條條目、
   > 21 個 owner 引用：三個數字互不相等。
   >
   > 而它用現在式寫在〈先看這張表〉第 15 列，也就是**被單獨拿出去用得最多**
   > 的那一頁（第 15 項還是「採用的第一步」）。
   >
   > 處理照這一項自己的判準拆成兩半：**條目數**可推導 → 登記；
   > **「幾條被 GitHub 判無效」**不可推導 → 留在文件裡但標上量測日期，
   > 並明講它只能當場跑那行指令。

2. **沒有 repo 內部事實來源** —— 「四個目標平台」「每個約 4 MB」
   「實測 4/5 快取命中」。這一類**刻意不守**（A1）：硬守只能靠人再抄一次
   期望值進登記表，那是把同一個問題換到一個更少人會看的地方犯。

⚠️ 第二類要留在文件裡沒關係，但**不要讓它看起來像被守著的**。
判斷方法：那個數字改變時，有沒有一個 repo 內的檔案會跟著變？沒有就是第二類。

---

## 19. 法遵／IT — §16 五年保存：腳手架交出清單，保存排程是你們的

> **2026-08-16：這是重評裡唯一被降級的一列（✅ → ⬜）。**
> 你們目前手上的標案契約**沒有寫保存年限**，所以它不是契約義務。
>
> 但**不刪**，理由是這一列的答案是逐案的：作為受託者，義務從委託機關的
> 安全維護計畫繼承。某一案的契約寫了年限的那天，它立刻回到 ✅ ——
> 而下面第 1 點（`sbom.cdx.json` 只有 90 天）那時就是**硬傷**，
> 不是「未來要處理的事」。
>
> **投標時看到保存年限條款，先回來讀這一節。**

**這一條原本被寫成「腳手架欠一份保存期政策」。查完條文之後那句話是錯的。**

[§16](https://law.moj.gov.tw/LawClass/LawAll.aspx?PCODE=K0010162) 要求業者保存
五年的有**三類**，而前端只碰得到第三類：

| 保存什麼                       | 誰持有             | 腳手架碰得到嗎 |
| ------------------------------ | ------------------ | -------------- |
| 個資之蒐集、處理或利用紀錄     | 資料庫／後端       | ❌             |
| 自動化機器設備之軌跡資料       | 系統／基礎設施 log | ❌             |
| 落實執行安全維護計畫之**證據** | 依安全維護計畫而定 | ⚠️ 部分        |

誰歸檔、存哪、銷毀排程、負責人 —— 那是組織文件，不是 repo 裡的東西。
腳手架能做的是**產出證據**並**交出清單**：

```bash
./node_modules/.bin/vpr evidence
```

清單由 `tools/compliance/src/evidence.ts` 推導，CI 每次驗它沒有腐爛
（宣告的檔案都在、每一份都有閘門在維護）。**不要手抄那張表** ——
加新工具時它會變，而抄下來的版本不會。

**要決定的三件事**

1. **`sbom.cdx.json` 到不了五年。** 它是 CI artifact，GitHub 上限 90 天
   （公開 repo）、400 天（私有）—— **結構限制，腳手架修不了**。
   兩條出路都是你們的決定：進版控，或由組織的保存系統定期取走。

2. **版控的那幾份跟著 repo 走。** git 歷史本身就是保存機制，只要 repo 活著
   就超過五年 —— 但 repo 被刪、歷史被重寫（force push、filter-repo），
   證據就沒了。要不要備份、備份到哪，是你們的決定。

3. **清單上這幾份要不要寫進安全維護計畫。** §16 保存的是「落實執行**安全維護
   計畫**之證據」—— 如果計畫裡沒有提到這些閘門，這些檔案在法律上就不是
   那個「證據」。**先有計畫，這張清單才有法律意義。**

---

## 20. 法遵／資安 — §11 II ⑨ 隱碼現在是慣例，不是機制

**2026-08-16 拆掉了強制隱碼的閘門。遮罩的能力還在，強制它的東西沒了。**

原本的機制是兩層：切片契約有一個必填的 `personalData: readonly string[]`，
而宣告過的欄位在 `.vue` 模板裡必須包 `maskXxx()`，靜態檢查會比對；
渲染結果真的被遮住則由切片自己的元件測試證明。

拆掉的理由不是它沒價值，是**每次要人付的成本 × 觸發的頻率**（C52）：
它是「加一個切片」這個動作上最重的那一項摩擦，而加切片是這個腳手架
每天都在做的事。

**現在的狀態**

| 還在                                      | 沒了                                |
| ----------------------------------------- | ----------------------------------- |
| `platform/pii` 的 `maskName()` 等遮罩函式 | `Feature.personalData` 契約欄位     |
| `OrderList.vue` 仍然呼叫 `maskName()`     | 「宣告的欄位必須包 mask」的靜態檢查 |
| 切片自己的元件測試（跑在 Tier 1）         | CI 上的 `pii-masking` 這道閘門      |

法遵對照表上這一格**重新變成 🔴「無」**，這是刻意的：拿掉閘門而讓表繼續
顯示「已覆蓋」，比沒有閘門更糟 —— 那會讓組織以為有一個不存在的控制。

**要決定的**

新加的個資欄位忘了遮罩時，沒有任何東西會說話。三條路，成本差很多：

| 做法                                     | 成本                                                 |
| ---------------------------------------- | ---------------------------------------------------- |
| 維持慣例，靠 code review                 | 零工具成本。**取決於 review 的品質，會隨團隊變動**   |
| **只對「已知的個資切片」開白名單式檢查** | 中。要維護一份切片清單，但不影響其他切片的開發速度   |
| 復原原本的兩層機制                       | 高。回到每加一片都要回答一次的狀態 —— 就是被拆的原因 |

### 2026-08-16 重評：**選第二條**

重評時查到的事實把這三條路收斂成一條：**個資只出現在少數後台畫面**
（民眾端與一般使用者看不到）。

那正好是白名單式檢查的形狀，而且是三條裡代價最低的那條：

- 要維護的清單**很短**，而且**變動很慢** —— 後台切片不常新增
- **加一個切片的成本回到零**，那是 C52 拆掉原機制的唯一理由
- 遮罩的能力完全沒動過（`platform/pii` 還在），要接回去不必重新設計

⚠️ 但**觸發條件是逐案的**：這一列適用於「畫面上會出現個資」的案子。
接到純代號、純統計的案子時它不適用 —— 而那時 §11 II ⑥（測試環境不得用
真個資）仍然要，因為機關常常直接給真實資料做測試與資料移轉。
**⑥ 與 ⑨ 是兩件事，不要一起關掉。**

⚠️ **這是腳手架自己降級的一項控制，不是從來沒做過。** 實作與論證都留在
git 歷史裡（C50／C52），要復原不必重新設計。

---

## 21. 資安 — 開發期的源碼掃描（SAST）沒有接上

**目前 repo 裡的「源碼掃描」只有 lint 那一半，汙點傳遞類的問題沒有東西在看。**

ESLint 的 `security` 外掛與 `vue/no-v-html` 擋的是**單點樣式**
（`eval`、不安全的 regex、`v-html`）。它們看不見的是**跨函式的資料流** ——
一個從 `route.query` 進來的值，經過三層傳遞之後餵進 `innerHTML`，
每一個單點都合法。

已經動手做過一次（分支 **`feat/sast-semgrep`**，PR #18 **已於 2026-08-16 關閉**，
**分支刻意保留**），用 semgrep 開源版寫汙點規則。**卡住的原因寫在 PR 裡，簡述如下**：

1. `semgrep --test` 在找不到任何 fixture 時會印 `No unit tests found`
   然後**回傳 0** —— 也就是說 CI 顯示 4/4 綠，而實際上零條規則被驗證。
   這是這個 repo 一路在防的那個形狀。已加上防呆（`grep -q` 後 `exit 1`），
   所以那個 PR 最後是**正確地紅**。
2. 規則產生器讀的是 `Feature.personalData`，而那個欄位已隨第 20 項移除。

> **關閉不等於放棄，關的是做法**（理由見下一節與 C54 第六節）。
> 那個分支上有三樣東西要撿回新的實作裡，清單寫在 PR #18 的關閉留言，
> 因為**人不會去翻已關閉的 PR**。其中最要緊的是那道 `grep -q` 防呆 ——
> 換成公開規則集**一樣會踩**同一個坑。

### ✅ 已接上（2026-08-16）。但價值論證與做法**都跟重評當天說的不一樣**

**源碼掃描報告是寫死的交付物**，由第三方或機關指定的商用工具掃。
所以 repo 裡的 semgrep 是**前置過濾器**，不是報告本身。價值論證因此是：

> **廠商掃出來的東西，你們要在驗收期限內改完。改不完＝延遲驗收。**
> 前置過濾器把「驗收前才知道」變成「開發當下就知道」，而**分母是每一案**。

#### ⚠️ 「改用公開規則集」那個建議被實測推翻了

重評當天（C54 第六節）建議改用 `p/security-audit` 這類公開規則集，理由是
「自寫規則跟商用工具撞在一起的機率低」。**那個推理聽起來合理，而它是錯的。**

拿一份故意寫壞的 fixture 去測（`route.query` → `innerHTML`、open redirect、
`new Function`、字串型 `setTimeout`）：

| 規則集                  | 載入規則數 | 命中  |
| ----------------------- | ---------- | ----- |
| `p/xss`                 | 12         | **0** |
| `p/security-audit`      | 22         | **0** |
| `p/owasp-top-ten`       | 76         | **0** |
| `p/default`             | 210        | **0** |
| **本 repo 自寫的 2 條** | 2          | **2** |

原因不神祕：公開規則集的重心在**伺服器端**樣板與框架，Vue SPA 的
**瀏覽器端 DOM 汙點流**是它們覆蓋得最薄的一塊。（`p/default` 對本 repo
確實報了 26 條，但**全部是 CI 與供應鏈衛生**，一條程式碼層級的都沒有ーー
見第 23 項，那批發現值得單獨處理。）

所以規則留在 repo 裡。附帶的好處是它們**是釘住的**：註冊表的 `p/xxx` 是
會移動的指標，上游改規則不需要任何 commit，而那會讓一個沒改過的 PR 變紅。

#### 兩個 blocker 怎麼解的

- **blocker 2**（產生器讀已移除的 `Feature.personalData`）：產生器整個丟掉。
  它在解錯的問題 —— 規則不需要從切片契約長出來
- **blocker 1**（`semgrep --test` 找不到 fixture 卻回傳 0）：**兩件事**

  1. **配對方式**：必須 `cd` 進規則目錄、`--config` 指向**目錄**。從 repo
     根目錄下 `--test --config .semgrep` 配對不上，而它**不會報錯**
  2. **防呆留著，而且變成兩道**：`No unit tests found` 要紅，**且**輸出必須
     有真的「N/N tests passed」數字 —— 少了第二道，任何讓 semgrep 靜默的
     改動都會再度變成綠燈

  實測（semgrep 1.136.0）：刪掉 fixture → 印那行字、**exit 0**；
  把規則的 sink 拿掉一個 → **exit 1**。防呆不是防禦性寫法，是必要的。

**反向測試就是 `.semgrep/rules.ts` 本身**，含一條對照組：同一個汙點來源流進
`textContent` **不得**被報 —— 少了它，一個「看到 `route.query` 就報」的爛規則
會通過所有正向測試，然後在第一週被關掉。

⚠️ 加排除之前，**ESLint 的 `no-unsanitized/property` 與 oxlint 的
`no-implied-eval` 各自獨立地抓到了那份 fixture**。那是好消息並且值得寫下來：
兩個與 semgrep 無關的工具確認了它真的是壞程式碼，不是一個假想的壞例子。

---

## 22. 架構／設計 — 無障礙：能力在，強制它的東西沒有 ⚠️ 已裝閘門，但閘門看不見大部分（2026-08-16）

**已加 `vpr a11y`（`eslint-plugin-vuejs-accessibility` 全 23 條，跑在 Tier 1）。
它對本 repo 的正常結果是零個發現，而那**不是**因為模板沒問題。**

### 做完之後的樣子

| 東西                                       | 在哪                                                  |
| ------------------------------------------ | ----------------------------------------------------- |
| 閘門本體                                   | `platform/eslint-config/src/a11y.js`（`./a11y` 匯出） |
| 反向測試（故意寫壞的 SFC ＋ 掃描範圍比對） | `platform/eslint-config/tests/a11y.test.ts`           |
| CI                                         | `tier1-quality.yml` 的「無障礙靜態檢查」              |
| 本機                                       | `vpr a11y`，也在 `vpr gate` 裡                        |

**這一項最重要的產出不是那道閘門，是它的量測結果。**

23 條全開成 error 掃當時的 6 個 `.vue`：**一條都沒命中**。同一天用人眼讀
同一批檔案，讀出來的真缺陷有四個 —— **它一個都看不到**：

| 人眼讀到的                                      | 為什麼閘門看不到                                 | 已修 |
| ----------------------------------------------- | ------------------------------------------------ | ---- |
| 表格沒有 `<caption>`、`<th />` 是空的、沒 scope | 「這張表少一個說明」不是任何一個元素的屬性問題   | ✅   |
| 載入狀態沒有 live region（兩個切片）            | 同上，而且它牽涉**時序**（元素要先在，文字才變） | ✅   |
| `<nav>` 沒有可及名稱、沒有略過導覽的連結        | 同上                                             | ✅   |
| `UiButton` 只放 `<slot />`，圖示按鈕無可及名稱  | 要看的是**呼叫端**，不是元件本身                 | ❌   |

> ⚠️ 修法用到本 repo 第一次出現的 `sr-only` / `focus:not-sr-only`。
> **有去 `apps/console/dist/assets/*.css` 確認它們真的被產出來**，沒有靠
> 建置 exit 0 —— Tailwind 掃不到來源時建置會成功、CSS 還會變大，
> 但裡面一個 utility 都沒有（見 `platform/ui/src/styles/index.css` 的檔頭）。
> 那種失敗的樣子是表格上多出兩段本來該隱形的文字，而文件仍然寫著「已修」。

原因不神秘，而且是實測出來的：那 23 條比對的是**原生元素與屬性**，
而本 repo 的互動幾乎都包在元件裡（`UiButton`、`RouterLink`、`DialogRoot`）——
`<UiButton @click>` 與空的 `<RouterLink></RouterLink>` 實測都不會紅。

> **所以這道閘門的定位要講清楚：它守的是靜態可查的那一半，
> 而那一半在這種寫法下幾乎是空的。綠燈不代表頁面可用。**

### 「掃到零個」與「什麼都沒掃到」

這兩件事在 CI 上都是 exit 0。所以反向測試有兩條，各擋一種：

| 測的                                                                                | 少了它會怎樣                                                    |
| ----------------------------------------------------------------------------------- | --------------------------------------------------------------- |
| 故意寫壞的 SFC 要讓**每一條被啟用的規則**都開火（比集合，不比數量、不比 exit code） | 規則被關掉、被換掉、剖析器壞掉，全部以綠燈呈現                  |
| repo 裡有幾個 `.vue`，閘門就要掃到幾個                                              | `.vue` 搬去新目錄、`ignores` 被擴大一格，症狀只是「閘門變快了」 |

第二條是實測有用的：加 ignores 的第一版寫了從 repo 根算起的路徑，
而 flat config 的 `ignores` 是相對 basePath 比對的 —— 閘門那一側能排除、
反向測試那一側排不掉。兩邊都測才抓得到。

### 還沒做的，具名寫下來

1. **`UiButton` 的可及名稱守不住。** 圖示按鈕（`<UiButton><IconX /></UiButton>`）
   對輔具是一顆無名的按鈕，而**這件事只能在呼叫端判斷**，元件自己看不到。
   要守它得寫一條自訂規則（「`UiButton` 的內容不得只有元件」之類），
   那是新工具的支出，這一輪刻意不做。
2. **元件層的無障礙測試沒有做**（原本表格裡的「中」那一格）。理由同上：
   守得住共用元件，但守不住各切片自己拼的頁面，而後者才是缺陷的所在。
3. ~~**行為那一半完全沒有**~~ **已量過，並決定不裝那道閘門（2026-08-17，C69）。**
   量的結果是：想買的兩條在模擬 DOM 下**是壞的**，而買得到的那條在元件層級
   沒有意義。詳見下面〈量完之後決定不裝〉。
4. ~~**不在 `tools/compliance` 裡**~~ **現在有了，但是另一張表** ——
   `tools/compliance/ACCESSIBILITY.md`（產生的）。原本的理由仍然成立：
   `CONTROLS` 的條號全部來自個資辦法，無障礙不屬於它。所以做法不是塞進去，
   是**另立一個型別與一份產出**（`src/a11y.ts`）—— 兩套規範的判定者、流程、
   交付產出都不同，硬塞成同一個型別，兩邊都會變形。

### 量完之後決定不裝（2026-08-17）

三條路先量再決定（C55）。結果是**負面的**，而且比「買得少」更強：

| 植入（已知答案的 fixture）           | axe ＋ happy-dom 的結果             |
| ------------------------------------ | ----------------------------------- |
| 對比 **1.1:1** 的文字（AA 要 4.5:1） | `incomplete`，**不是** `violations` |
| 連結只靠顏色、沒有底線               | `incomplete`                        |
| 標題階層跳階 h1 → h4                 | ✅ `violations`                     |
| 正 `tabindex`                        | ✅ `violations`                     |

⚠️ **`incomplete` 是關鍵。** 幾乎每份教學寫的斷言是
`expect(violations).toHaveLength(0)` —— 那在一段對比爛到不可能通過驗收的
文字上**會亮綠燈**。也就是說那道閘門的失敗模式，正好是這個 repo 已經被騙過
五次的那一種，而這次會發生在一道專門用來守無障礙的閘門上。

另外兩件：`heading-order` 是**頁面級**性質，而實測每個畫面只有一個 `<h1>`，
掃孤立畫面時永遠不適用；DOM 裡有 `<iframe>` 時 axe **直接丟例外**，不是跳過。

> ⚠️ 這段量測自己也踩到同一個坑：掃 `UiDialog` 得到「0 violations」，
> 而它在 teleport stub 底下只 render 了 81 個字元 —— 掃描對象是空的。

**而真正決定的是：CI 裡的東西不是驗收。** 無障礙標章的驗收有三段
（Freego 掃已部署的 URL → 覆核 → 人工檢測 → 抽測），**沒有一段在 CI 裡**。
四個缺口對到成功準則之後：

| 缺口     | 成功準則           | 等級 | 誰判定         |
| -------- | ------------------ | ---- | -------------- |
| 對比度   | 1.4.3 對比（最低） | AA   | Freego ＋ 人工 |
| 只靠顏色 | 1.4.1 顏色的使用   | A    | Freego ＋ 人工 |
| 標題階層 | 2.4.6 標題和標籤   | AA   | Freego ＋ 人工 |
| 焦點順序 | 2.4.3 焦點順序     | A    | **只有人工**   |

⚠️ **焦點順序是 A 級，而連 Freego 都判定不了** —— 那一格無論在 CI 裡裝什麼
都買不到，**Playwright 也買不到**，它要的是人跑鍵盤。

所以 C47 不重開：付出瀏覽器二進位進 SCA 的代價，換到的是驗收端本來就會做、
而且做得更準的東西（Freego 掃的是真正要交付的網站，不是孤立元件）。
這與「源碼掃描由專業公司做、開發期只做開源前置過濾」是同一個形狀。

改成交付一張**分工表**：`vpr compliance --update` 會一併產出
`tools/compliance/ACCESSIBILITY.md`，內容是「哪幾格開發期擋得掉、
哪幾格必須人工或委外」，每一格都附上面那些量測。

### ⚠️ 這一項現在欠的是一份文件，不是程式

**規範原文。** 那張表刻意只收上面四條，不是 AA 的完整清單 ——
完整對照需要逐條的編號、名稱、等級，而官方頁面在寫的當下兩次都回 403，
次級來源彼此矛盾（12 指引 66 準則 vs 13 指引 78 準則）。
**沒有事實來源的計數不要寫**（C53），所以那份文件裡沒有任何「共 N 條」。

版本假設是 **110.07**，寫成欄位（`ACCESSIBILITY_STANDARD`）而不是註解，
理由是可查證的硬事實：Freego 2.0「已不受理此版申請」。

**要確認標案指定的是哪一版 —— 這是組織的動作，與第 15 項同一類。**
拿到規範原文之後，補完整張表是純粹的資料錄入。

### 仍然要人決定的

⚠️ 要哪個等級、驗收怎麼判、有沒有法定強制，**以 RFP 為準，這裡刻意不寫死**
（與第 21 項同一個處理：契約是事實來源，不是我們的印象）。閘門裝了不等於
這一格結案 —— 它只是把「驗收前才知道」的範圍縮小了一點點。

**建議在投標階段就問清楚等級**，因為這一列與第 14 項綁在一起：
無障礙的落點是**多案共用的設計系統**。

> **2026-08-16 更新：第 14 項已結案，而結果只解決了一半。**
>
> 這裡原本寫「沒有 owner 的設計系統，不會有人負責無障礙」。現在
> `platform/ui` 的 owner 是 `platform-maintainers`（C62），所以**歸屬**有了 ——
> 但那是一個平台工程角色，不是設計角色，**職能**還是沒有。
>
> 所以這一列的狀態要重寫成：下面那四個行為面缺口**歸屬清楚、沒有人在看**。
> 這比「沒有 owner」更難察覺，因為 CODEOWNERS 上看起來是有人的。

---

### 原本的缺口描述（保留，因為它解釋了為什麼值得做）

**2026-08-16 重評時發現的。它從來沒有進過這份清單，而它該進。**

查證到的事實只有兩件，兩件都很明確：

1. [`UI-SURVEY.md`](UI-SURVEY.md) 把它寫成**條件句**：「**若**組織有 WCAG
   要求，element-plus 與 reka-ui 的…」—— 然後就沒有再往下走
2. `eslint.config.js` 沒有 `vuejs-accessibility`，`tools/compliance` 的
   `CONTROLS` 與 `FUTURE` 一個字都沒有，本清單也沒有

**選型其實選對了**：reka-ui 的 WAI-ARIA 實作是它的賣點（見
[`DECISIONS.md`](DECISIONS.md) 的 D15／C39），焦點管理這類行為是它幫我們做掉的。

**但那不是同一件事**：

> **元件庫有無障礙 ≠ 你用它拼出來的頁面有無障礙。**
> 缺 label 的表單、只靠顏色傳達的狀態、跳號的標題階層、
> 自己寫的那些不經過 reka-ui 的互動 —— 元件庫一個都管不到。

而現在**沒有任何東西在守**。這是與第 20 項一模一樣的形狀：**能力還在，
強制它的東西沒有**，差別只在第 20 項是被拆掉的（有 git 歷史、有論證），
這一項是**從來沒有裝過**。

**當時列的三條路**

| 做法                                      | 成本                                                             |
| ----------------------------------------- | ---------------------------------------------------------------- |
| 什麼都不做，驗收前請廠商掃                | 零工具成本。**但無障礙的改動是全面性的**，驗收前才知道等於重做   |
| 加 `eslint-plugin-vuejs-accessibility`    | 低。擋得到靜態的那一半（缺 alt、缺 label、角色錯用），漏行為那半 |
| 上面再加 `platform/ui` 的元件層無障礙測試 | 中。守得住共用元件，但守不住各切片自己拼的頁面                   |

**走的是第二條，而實測把第二條的描述改小了一號。** 「擋得到靜態的那一半」
成立，但「靜態的那一半」在本 repo 的寫法下幾乎是空的 —— 見上面那張量測表。
第三條刻意沒做，理由寫在上面〈還沒做的〉第 2 點。

---

## 23. 平台（CI）— 供應鏈的論證停在 npm 邊界 ✅ 已補上（2026-08-16）

**8 個 action（17 處引用）全部改成 commit SHA ＋ 尾隨版本註解，容器映像改成 digest。**

但**改那 16 行不是重點** —— 它是一個快照，而快照會過期。真正的產出是三件
互相咬住的東西：

|                                                     | 做什麼                                                                                                                                  |
| --------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------- |
| `tools/conformance` 的新規則                        | 任何 `uses:` 不是 40 位 SHA 就紅。下一個人加一行 `uses: foo@v1` 會被擋                                                                  |
| `renovate.json` 的 `helpers:pinGitHubActionDigests` | 升級時把新版本也釘成 SHA。**少了它，Renovate 會提出把 SHA 換回標籤的 PR**，然後閘門紅、而紅的原因是我們自己的機器人 —— 那種紅燈會被關掉 |
| 6 條反向測試（4 條 ★）                              | 含「`@v0.36.0` 這種看起來很精確的版本號也要紅」，以及「沒有 workflow 目錄時不得紅」                                                     |

⚠️ **剩下的缺口，寫清楚不假裝**：那條檢查**只看 `uses:`**。`run:` 裡的容器
映像（SAST 兩步的 `semgrep/semgrep@sha256:...`）**不在範圍內** —— 要在 shell
腳本裡認出映像參考，任何做得到那件事的正則都會對路徑與網址誤報。
映像目前是**手動**用 digest 釘的，而「手動釘的東西」正是這條檢查存在的理由。

要補的話成本不低（得解析 `run:` 區塊而不是逐行比對）。在補上之前，
**改 workflow 裡的 `docker run` 要靠 review**。

---

### 原本的缺口描述（保留，因為它解釋了為什麼值得做）

**做 SAST 那一輪順手掃出來的，而它跟 SAST 無關。**

`p/default`（semgrep 的公開規則集）對本 repo 報了 26 條。**程式碼層級一條都沒有**
——全部是 CI 與供應鏈衛生，而其中最大的一群是這個：

```bash
grep -n "uses:" .github/workflows/*.yml
```

**修好之前，17 處引用全部以標籤釘住**：`actions/checkout@v7`、`actions/setup-node@v7`、
`gitleaks/gitleaks-action@v3`、`github/codeql-action/upload-sarif@v4`…
`aquasecurity/trivy-action@v0.36.0` 看起來很精確，但它同樣是標籤。

**標籤是可以被移動的指標。** 發佈者（或任何拿到那個帳號的人）可以把 `v7`
重指到別的 commit，而你們的 CI 下一次跑就會執行新的內容 —— 沒有任何 commit、
沒有任何 PR、沒有任何 diff。

為什麼這一項刺眼：

| 這個 repo 對 npm 相依做的             | 對 CI action 做的 |
| ------------------------------------- | ----------------- |
| 574 個套件全帶 sha512                 | 標籤              |
| `--frozen-lockfile` 安裝              | 標籤              |
| tarball digest 進版控、每次 gate 比對 | 標籤              |
| 144 個原生二進位逐一分佐證等級        | 標籤              |

而這些 action 跑在**產出 SBOM 與證據檔的那個 job 裡**，拿得到 repo 與 secrets。
也就是說：整套供應鏈論證的**執行環境本身**，是唯一沒有被論證的一層。

**要決定的**

| 做法                                                  | 代價                                                                       |
| ----------------------------------------------------- | -------------------------------------------------------------------------- |
| 全部改成 commit SHA ＋ 註解版本                       | 一次性改 16 行。之後升級要換 SHA，而 Renovate 支援這種釘法                 |
| 只釘第三方的（gitleaks／trivy），`actions/*` 維持標籤 | 折衷。理由是 GitHub 自家的帳號風險較低 —— **但那是一個信任假設，要寫下來** |
| 維持現狀                                              | 零成本。但機關資安審查問到 CI 完整性時沒有話講                             |

**選了第一個。** 第二個（只釘第三方、`actions/*` 維持標籤）看起來便宜，
但它要求寫下一句「我們相信 GitHub 自家帳號不會被盜」—— 那是一個
**沒有到期日的信任假設**，而全釘的成本只是 16 行加一個 Renovate preset。

⚠️ **同一個問題也在容器映像上**：SAST 那兩步原本用 `semgrep/semgrep:1.145.0`，
那也是標籤不是 digest。**已一起改成 digest** —— 只改一半會讓這一項讀起來
像已經解決，而它的 `run:` 那一半至今仍然沒有閘門在守（見上方）。

⚠️ 這一項**刻意沒有夾帶進 SAST 那個 PR**。它是 16 行 workflow 的改動、
影響每一次 CI 執行，而且第二個選項是一個**信任決定**——不該由實作者
在一個講 SAST 的 PR 裡順手做掉。

---

## 24. 架構／設計 — 三條軸都接上了 ✅（2026-08-17）

回答第 14 項時得到的產品要求是這一句：

> 公司會有一套基礎的 UI 版型和互動方式，但是各團隊可以依不同案件需求
> 更換**配色**或 **component 形狀**或**互動方式**。

這是接案公司很自然的形狀 —— 業界叫 multi-brand／white-label 設計系統：
一份程式碼、多套品牌，靠一層代幣（token）把「品牌表達」與「技術實作」隔開。
**三條軸都在 2026-08-17 接上並各自有東西守著。** 論證與過程見
`DECISIONS.md` 的 **C65**（配色與形狀）、**C66**（改名留下的洞）、
**C67**（互動）。

| 軸                 | 接縫在哪                                                        | 守它的                         |
| ------------------ | --------------------------------------------------------------- | ------------------------------ |
| **配色**           | `platform/ui/src/styles/index.css` 的兩層 `@theme`              | `theme-verify`                 |
| **component 形狀** | `createUiTheme({ 元件名: { 槽名: class } })`＋ 圓角／外框代幣   | `theme-verify` ＋ **元件契約** |
| **互動方式**       | `UiDialog` 的 `default`／`footer`／`close` slot（＋ emit 支援） | `api-surface`                  |

⚠️ **「三條軸都接上了」這句話在 2026-08-17 當天只對一個元件成立（C70）。**
形狀那條軸當時只做在 `UiButton` 上 —— `UiDialog` 的寬度與位置寫死在模板裡，
任何案子都換不掉。而沒有東西說話的原因很具體：**當時的檢查
`readFileSync("UiButton.vue")` 寫死一個檔名**，守的是一個檔案不是一條規則。

現在 `platform/ui/tests/component-contract.test.ts` 掃 `src/components/*.vue`，
五條條文（有沒有匯出／有沒有宣告槽／宣告與預設表一致／有沒有 `inject`／
模板有沒有綁到解析後的表），每條配反向測試。

⚠️ **「都接上了」仍然不等於「都一樣厚」。** 互動那條的接縫只有 `UiDialog`
真的用得上 —— `UiButton` 只有一個 `default` slot，它的互動就是「被點」。
而**「接縫夠不夠」刻意不做成靜態規則**：那是設計判斷，交給 `CODEOWNERS`
與 PR。做成規則會長出沒有人會覆寫的槽名，然後被加例外（C41）。

### 已經做掉的（2026-08-17）

- 代幣分兩層（色票 → 語意），而那個間接**實測是活的**：app 端只覆寫
  `--color-brand-600`，`--color-accent` 會跟著走
- 元件裡 15 處原始顏色歸零（`tools/theme-verify` 的靜態半守著）
- 非顏色代幣：圓角、外框寬度、陰影、字重
- `apps/console/src/styles.css` 是真的覆寫，`main.ts` 是真的用擴充點換掉
  預設的 `secondary` —— **各案 fork 之後第一個要改的就是這兩處**
- 反向測試：九條斷言八條實測會紅，逐條列在 `tools/theme-verify/README.md`

`apps/console` 的**實際產物**（2026-08-17，不是 fixture）：

```
--color-brand-500:oklch(62% .17 173)      ← 色票層覆寫
--color-brand-600:oklch(55% .18 173)
--color-brand-700:oklch(48% .17 173)
--color-line:oklch(85% .02 173)           ← 語意層覆寫
--radius-control:.5rem                    ← 形狀覆寫（預設 .375rem）
--color-accent:var(--color-brand-600)     ← 沒被覆寫，但跟著色票走
--color-focus:var(--color-brand-500)      ← 同上，焦點環也變青綠
.border-accent{border-color:var(--color-accent)}   ← 只寫在 main.ts 字串裡
.hover\:bg-surface-hover                            ← 同上，確實被編出來
```

最後兩行是這份證據裡最該看的：那兩個類別**只存在於 `main.ts` 的一個字串
字面值裡**，而它們出現在產物中 —— 也就是 `createUiTheme` 那條路是通的。
重跑：

```bash
./node_modules/.bin/vp -C apps/console build && \
  grep -oE '\-\-color-(brand-[0-9]{3}|line|accent|focus):[^;}]*|\-\-radius-control:[^;}]*' \
    apps/console/dist/assets/index-*.css
```

### 互動那條：擋著它的東西是一道裝飾品

當初以為要做的是「擴充 `SFC_UNSUPPORTED`，讓元件可以加 `defineSlots`」。
量了之後才知道**那道限制根本沒有在擋公開面**，它擋的是巨集的名字。
實測（fixture 元件，兩次都是閘門全綠）：

```
在 <template> 加 <slot name="header" /> 與 <slot />   → 閘門全綠
在 <template> 加 @click="$emit('picked', label)"      → 閘門全綠
```

也就是說 slot 與 emit **不需要巨集就能存在**，而 `UiDialog` 從落地那天起
就有三個沒有被記錄過的 slot（`default`／`footer`／`close`），
`features/order` 與 `features/shipment` 兩個切片一直在用 `#close`。

`UiButton.vue` 原本那句「加 `defineEmits`／`defineSlots`／`defineExpose`
會讓 `api-surface` 直接丟例外，因為那時 props 就不再是完整的公開面」——
**前半句是真的，後半句是假的**：props 早就不是完整的公開面了。

現在的狀態：

- `defineSlots` / `defineEmits` 都解析，記成 `[slot x]` / `[emit x]`
- **模板與宣告必須一致，兩個方向都會紅** —— 模板有而沒宣告（公開面比記錄大）、
  宣告有而模板沒有（消費端照著寫卻永遠不會渲染）
- `defineExpose` 仍然丟例外，而這次是**有理由的**：`<script setup>` 預設封閉，
  沒有那個巨集就洩不出任何實例成員 —— 它的絆線是真的
- 基準檔升到第 3 版（`platform/` 沒改，是工具開始記新東西，見 C67）
- **四個巨集都是「寫了卻讀不懂就紅」**（2026-08-17，見 C72）。只認
  `defineProps<{…}>()`／`defineSlots<{…}>()`／`defineEmits<{…}>()`／
  `defineModel<T>(…)`；執行期形式與具名型別別名一律丟例外，因為它們的
  失敗輪廓是**少算**（那一整組公開面從記錄裡消失，而基準檔看起來很完整）
- **零公開面的元件是合法的**（`Separator`／`Skeleton` 這種純版型元件）。
  ⚠️ 這條放行的前提是上一條已經補齊 —— 順序顛倒的話，寫錯形式的巨集會
  安靜地變成一個「合法的零公開面元件」

⚠️ **`theme-verify` 綠燈的意思仍然是「配色與形狀實測可換」，不是「設計系統可換」。**
互動那條軸由**兩道**閘門守，而且分工不同：`api-surface` 守 slot／emit 的
**名單**（改了會漂移），`tools/vue-typecheck` 守 slot payload 的**型別**
（宣告與消費端相符，2026-08-17 補上，見 C68）。

> ✅ **當天留下的第一個殘留已經關掉了。** 原本 `defineSlots` 裡的
> `VNode[]` 是「沒有人在驗的文字」；接上 `.vue` 型別檢查之後實測：
> slot payload 的型別用錯會紅。
>
> ✅ **第二個殘留也關掉了（同日）。** 原本記著「切片側用到的語意 utility
> 沒有人比對」，並猜它需要一份 Tailwind 內建 utility 清單 —— **那個猜測是多餘的**：
> `theme-verify` 早就有一支偵測器（`findPaletteUsage`），把它指向 `features`
> 與 `apps` 就好。乾跑撞到 **4 處真陽性、0 偽陽性**：兩個切片的 `<h1>` 寫著
> `text-gray-900`、`App.vue` 的略過導覽連結寫著 `focus:bg-white`，
> **而第四處在產生器模板裡** —— 也就是每個新切片天生帶著一個換不掉的顏色。
> 全部改成語意代幣，靜態那一段的範圍擴到消費端，產生器那一側由
> `slice-gen` 的測試用**同一支判定式**釘住。
>
> ⚠️ 還有一件**這兩道閘門都抓不到**的：`<template #不存在的slot>`。
> `@vue/language-core` 沒有 unknown slot 的旋鈕，而 `api-surface` 比對的是
> 元件自己的宣告與模板，看不到消費端寫了什麼。目前只能靠 code review。

### 重評時要重跑，不要重讀

```bash
# 元件裡的原始顏色（內建色階、white/black、色票層）。閘門在守的就是這個數字。
node tools/theme-verify/src/cli.ts
```

> ⚠️ 這一項第一次量的時候寫成 14／再修成 15，兩次都錯 —— 因為當時的 grep
> 認不得 Tailwind v4 的 `text-(--var)` 語法，**它漏掉的正好是唯一被正確
> 代幣化的那一類**。修正後是 16 處顏色宣告、13 個不重複類別、5 處走代幣。
>
> 現在那個 16 由 `tools/theme-verify` 接手，判準略有不同（它算的是**違規**，
> 而那 16 處裡有 1 處當時已經是語意層）—— 所以基線是 **15 → 0**。
> 兩個數字都對，關係是 15 ＝ 16 − 1。這個坑值得留著：
> **用來量「有沒有代幣化」的工具，第一版看不見代幣。**

### 三件當初具體點名的事，兩件已解

1. ~~**預設的那個 variant 完全換不掉。**~~ ✅ `secondary` 現在是
   `border-control border-line bg-surface text-fg hover:bg-surface-hover`，
   五個全是語意代幣。整條也能用 `createUiTheme` 換掉 —— `apps/console`
   就是這樣做的。

2. ~~**沒有任何一個 app 有自己的 CSS 檔。**~~ ✅ `apps/console/src/styles.css`
   是各案客製的入口，`main.ts` 改成 import 它。那條覆寫路徑當初**從來沒有
   被跑過**，現在每次 PR 都會跑兩次（`tools/theme-verify`）。

3. ~~**互動那條軸仍然被 `api-surface` 主動擋住。**~~ ✅ 擋著它的是一道
   裝飾品（見上一節）。`UiDialog` 的三個 slot 現在是**宣告過、被記錄、
   改了會紅**的公開面。

⚠️ ~~「配色」比字面上大~~ ✅ **圓角、外框寬度、陰影、字重**都已代幣化。
**間距刻意沒有** —— 那屬於「一套基礎的版型」那一半，代幣化它會長出
`--spacing-control-sm-padding` 這種名字，而真正要換尺寸的案子要換的是
整條規則（走 `createUiTheme({ sizes })`）。判準見 C65 第三節。

### ⚠️ 前兩條軸上線的那個 PR 自己留下一個缺陷（2026-08-17 當日修）

那個 PR 把 `--color-muted` 改名成 `--color-fg-muted`，而**使用端 6 處沒跟上**
（`features/order` ×4、`features/shipment` ×1、`tools/slice-gen` 的模板 ×1）。
產物裡因此有 `.text-\(--color-muted\){color:var(--color-muted)}` 指向一個
不存在的名字，五個 `<dt>` 標籤安靜地失去灰色 —— 而**四道閘門全綠**，
因為三道自訂檢查全部長在設計系統的**供給端**，改名的風險全部在**使用端**。

已修，並補上 `theme-verify` 的第三段（產物零懸空引用）。論證見 C66。

⚠️ **那一段有死角，而且這次的修法讓死角變大**：

| 寫法                  | 代幣不存在時 Tailwind 會…  | 抓得到嗎 |
| --------------------- | -------------------------- | -------- |
| `text-(--沒有的代幣)` | 照樣編出規則，`var()` 懸空 | ✅       |
| `text-沒有的代幣`     | **什麼都不產生**           | ❌       |

6 處都改成了正規工具類名，所以同一個錯誤下次會更安靜。產生器模板那一側
補了原始碼層的耦合（`tools/slice-gen/tests/contract-alignment.test.ts` 直接讀
`platform/ui` 的 `@theme` 清單，代幣改名會紅）；**切片那一側沒有**。
要補的話，判準是「切片用到的語意 utility 必須對得上一個已宣告的代幣」，
而它需要一份 Tailwind 內建 utility 的清單來排除誤判 —— 先量再決定（C55）。

### 做的過程撞出兩個新項目

- **#26**：`vp check` 不對 `.vue` 做型別檢查（實測 0 errors vs 1 error）
- conformance 的幽靈依賴檢查**看不到 CSS 的 `@import`** —— `platform/ui`
  一路靠 `apps/console` 剛好宣告了 `tailwindcss` 才解析得到。宣告已補，
  那條檢查的 CSS 盲區併在 #26 裡

> ✅ **#26 已於同日做掉，而互動這條軸的深度也跟著補上了。**
> `defineSlots` 宣告的回傳型別原本是**沒有被檢查過的文字**；
> 接上 `tools/vue-typecheck` 之後實測：把 slot payload 的型別用錯
> （`count: number` 當字串用）會紅。「宣告與實作相符」現在有人在保證。
> 論證見 C68。

另外兩個在 `api-surface` 上的洞已經**當場修掉**（見 C65 第八節）：
字面值聯集被展開成 `String.prototype`（那道守衛原本是裝飾品），
以及第一版修法把型別記成它自己的名字。第三個是互動這條軸自己撞到的：
`SFC_UNSUPPORTED` 那道三個巨集的絆線裡有兩個是裝飾品（見 C67）。

### 東西會壞的那一天

~~第二個案子開始共用 `platform/ui` 的那一天~~ —— 三條軸都有地方可以改了。

剩下會壞的是**兩件仍然沒有人守的事**：

1. **`defineSlots` 的型別與實際傳給 slot 的東西不符。** `.vue` 沒有型別檢查
   （#26），所以宣告 `footer(props: { busy: boolean })` 卻在模板寫
   `<slot name="footer" />`，不會有任何東西說話。目前兩個元件的 slot 都不帶
   payload，所以這個洞現在是零風險 —— **第一個帶 payload 的 slot 出現時
   就不是了**。
2. **切片端的語意 utility 對不上代幣**（見上一節那張死角表）。

---

## 25. repo 管理者 — CODEOWNERS 距離「生效」有三層，文件原本只寫了中間那層 ⚠️（2026-08-16）

查第 14 項的雙簽選項時查出來的。論證在 `DECISIONS.md` 的 **C63**。

| 層                                          | 現況            | 之前寫在哪                           |
| ------------------------------------------- | --------------- | ------------------------------------ |
| 1. 分支保護的 `require_code_owner_reviews`  | **沒開**        | ❌ 完全沒有                          |
| 2. owner 是 `@org/*` 佔位符 → Unknown owner | 已知            | ✅ 第 15 項、C40、COMPLIANCE §12 III |
| 3. **同一列多個 owner ＝ 任一核准即可**     | GitHub 原生行為 | ❌ 完全沒有                          |

```bash
# 第 1 層：本 repo 實測回 404 Branch not protected
gh api repos/<owner>/<repo>/branches/main/protection
```

第 3 層的官方原文（[GitHub Docs, About code owners](https://docs.github.com/en/repositories/managing-your-repositorys-settings-and-features/customizing-your-repository/about-code-owners)）：

> When reviews from code owners are required, an approval from **any** of the
> owners is sufficient to meet this requirement.

### 為什麼這比「少寫兩件事」嚴重

**12 條規則掛著 `@org/security` 當共同 owner**，而它們的註解寫的是
「所以由資安共同把關」「任何放寬都**必須經資安核可**」：`security-headers`、
`bff-contract`、`bff-check`、`supply-chain`、`compliance`、`csp-verify`、
`pii-check`、`platform/pii`、`.semgrep/`、`eslint.config.js`、`.npmrc`、
`.github/workflows/`。

在 GitHub 原生行為下那兩句是**假的**：platform-maintainers 一個人核准就過，
資安只會被自動加進 reviewer 清單、不會被要求。那 12 條寫的是**意圖**。

而時序讓它更糟：**第 15 項被寫成「採用這份腳手架的第一件事」**。
做完它、第 2 層拆掉之後，讀的人會非常合理地認為共簽開始生效了 —— 它不會，
而且原本沒有任何一句話會糾正這個推論。

### 三條出路（都要人決定，所以留在這裡）

| 做法                                           | 買到什麼                | 代價                                      |
| ---------------------------------------------- | ----------------------- | ----------------------------------------- |
| 開分支保護 ＋ 接受「任一核准」                 | 至少 owner 審查會被強制 | 共簽仍然不成立，但**文字改成誠實的**      |
| 把要共簽的檔案**拆到不同目錄**、各給不同 owner | 原生就能強制兩方        | 動的是檔案佈局，不是 CODEOWNERS           |
| 外掛檢查（多方核准的 Action）                  | 真的強制                | 多一個 CI 相依，而它會因 token 權限而變紅 |

⚠️ **「把 `@a @b` 拆成兩行」行不通** —— 最後一條匹配的規則獨贏，前面那條的
owner 被**取代**而不是合併。要拆的是目錄，不是行。

### 刻意不加閘門

判定「共簽有沒有生效」要打 GitHub API，會因為 token 權限與速率限制而變紅、
而不是因為真的有問題 —— 與 `CODEOWNERS` 檔頭拒絕把 `codeowners/errors`
做成閘門同一條理由，也與 C57 同一條理由（會隨機亮紅燈的閘門最後一定被拿掉）。

### 這**不會**推翻的東西

C44 的「Renovate 用 `workflow_dispatch` 而非自動觸發」把「`tools/supply-chain/`
與 `.github/` 都是 `@org/security` 共管」當成理由之一。那個決定**不受影響，
而且變得更站得住** —— 共簽本來就不強制的話，讓 bot 自主提交那些路徑只會更糟。

---

## 26. 平台（實作）— `.vue` 的型別檢查已接上，而乾跑撞出一個沒人看得見的相依 ✅（2026-08-17）

> **2026-08-17 結案。** 下面保留原本的問題描述（過去式讀），因為那個
> 「撞出來才知道」的過程本身是這一項的價值 —— 它是**沒有任何閘門會說話**
> 的那一類缺口，而這種缺口只會被下一次意外撞到。論證見 `DECISIONS.md` 的 C68。

### 現在的樣子

| 東西                              | 誰在看                                             |
| --------------------------------- | -------------------------------------------------- |
| `.ts` 的型別                      | `vp check`（tsgolint，原生 TS 7）                  |
| `.vue` 的 `<script setup>` 的型別 | **`tools/vue-typecheck`**（vue-tsc，JS 版 TS 5.x） |
| `.vue` 的模板運算式與跨元件型別   | 同上                                               |
| `.vue` 的 props／slot／emit 名單  | `tools/api-surface`（原文比對，不是型別）          |
| `.vue` 的模板無障礙               | `platform/eslint-config/src/a11y.js`               |

進 `vpr gate` 與 Tier 1，涵蓋 4 份 program、5 個非 fixture 的 SFC。

```bash
# 重評時重跑，不要重讀
node tools/vue-typecheck/src/cli.ts
```

⚠️ **這道閘門先證明自己有在看，才給判決。** 每一份 program 都用
`--listFiles` 比對「該讀的 `.vue`」與「vue-tsc 實際讀了哪些檔」，缺一個就紅 ——
「0 條錯誤」與「一個檔案都沒讀到」印出來長得一樣，而這個 repo 被那個形狀
騙過三次。

### 乾跑量到 16 條，而 16 條全部是同一件事

| 套件                | 錯誤數 |
| ------------------- | ------ |
| `platform/ui`       | 0      |
| `apps/console`      | 0      |
| `features/order`    | 10     |
| `features/shipment` | 6      |

⚠️ **`apps/console` 的 0 不是「另一半乾淨」。** 它的程式清單裡就有
`features/order/src/views/OrderList.vue` —— 同一個檔案在 console 的程式裡
0 條、在自己 package 的程式裡 10 條。缺陷不是「有幾條錯」，是
**切片單獨拿出來型別檢查不會過**，而這個腳手架的賣點就是切片會被 fork 走。

16 條全部是 `TS2339: Property '$t' does not exist`。`$t` 是 vue-i18n
augment 上去的全域屬性，兩個切片的模板都在用，而**兩個 `package.json` 都沒
宣告 vue-i18n**。執行期能動純粹因為 `apps/console` 的 `app.use(i18n)`。

> **這就是那個「沒人看得見」的相依。** `tools/conformance` 的幽靈相依檢查
> 讀的是 import，而全域屬性不是 import —— 不是那道檢查寫壞了，是這類相依
> 從來不經過它掃的形狀。找到它的只有 vue-tsc。

修法是兩處，而且實測過只有這一種組合有效（見 C68 的表）：`package.json`
宣告 ＋ 切片 `src/i18n.d.ts` 的一句 `import type {} from "vue-i18n";`。
產生器模板同步改掉，`slice-gen` 三條測試釘住。

⚠️ 那一行**不能**併進 `env.d.ts`：會讓它從全域腳本變成模組，於是
`declare module "*.vue"` 不再是環境宣告。而那個症狀**只有 tsgolint 看得到、
vue-tsc 全綠** —— 兩個 TypeScript 同時在場的第一個實例。

### 代價：這個 repo 現在有兩個 TypeScript

`typescript@7.0.2` 是原生 Go 版，`Object.keys()` 只有 2 個、沒有
`createProgram`；而 `vue-tsc` → `@volar/typescript` 需要 JS 版的 compiler API。
所以 `tools/vue-typecheck` 用具名 catalog `catalog:vue-typecheck` 拉一份 TS 5.x。

三個把成本框住的數字：

- ⚠️ ~~分歧上界 **0**，升級時要重跑比對~~ **那句話寫下當天就是錯的**：
  分歧不是風險，是這道閘門的**能力**。實測一支 `.ts` 寫
  `h(UiButton, { variant: "根本不是 variant" })` → `vp check` **0 errors**、
  這道閘門紅，因為 tsgolint 看的是 `declare module "*.vue"` 的萬用宣告。
  **「一邊紅一邊綠」多半是真陽性**，不是該重新評估決定的訊號。
  反方向也有一例（`env.d.ts` 變成模組，那次 tsgolint 對）——
  兩支各有對方看不見的東西。見 C68 的〈九〉。
- 供應鏈 **+9 個純 JS 套件**（565 → 574），
  **原生二進位 144 → 144、家族 12 → 12 不變**。
- 範圍收窄：只對含 `.vue` 的 package 跑，`.ts` 的判決仍然只有 `vp check`。

### 買到與沒買到

✅ 跨元件的 prop 型別、缺必填 prop、**slot payload 的型別** ——
最後一項把第 24 項留下的第一個殘留關掉了。

❌ **`<template #不存在的slot>` 不會紅。** `@vue/language-core` 3.x 只有
`checkUnknownProps`／`Events`／`Components`／`Directives`／`strictVModel`
五個旋鈕，**沒有 unknown slot 這一項** —— 是能力邊界，不是設定沒開。
那一半由 `api-surface` 守（名單），這裡守型別。

❌ **`strictTemplates` 沒開。** 乾跑量過：開了多 2 條，兩條都是
`<UiButton @click>`，而「修法」是加 `defineEmits` —— 那會關掉 fallthrough attr，
比病還糟。不開的代價是抓不到 prop 名字打錯（C41／C55）。

### CSS 的 `@import` 也守住了 ✅（2026-08-17 同日）

`platform/ui/src/styles/index.css` 寫著 `@import "tailwindcss"`，而
`platform/ui` 原本沒有宣告它 —— 解析成功純粹是因為 `apps/console` 剛好有。

乾跑：**四筆 `@import`，三個 package 全部都已宣告，0 違規**。
接它不是因為現在有東西可抓，是因為**它要抓的那個缺陷已經真的發生過一次**，
而當時沒有任何東西說話（D16 的迭代軸）。

`tools/conformance` 的幽靈依賴檢查現在同時掃 CSS，範圍與 JS/TS 那一半相同
（`features`／`platform`／`apps`，不含 `tests/`）。四條反向測試。

⚠️ 去註解器**必須先認得引號字串**：`@source "src/*.{vue,ts}"` 裡的 `/*`
是一個合法的 CSS 註解開頭，天真的正則會從那裡一路吃到下一個真註解的結尾，
把中間的 `@import` 一起吞掉 —— 兩邊都不報錯。同一個坑
`platform/ui/tests/styles.test.ts` 踩過一次。

> ⚠️ 那條反向測試的**第一版不會鑑別**：fixture 用的是 `**/*`，而 `/**/`
> 是自閉合的，天真版只吃掉那四個字元、吞不到下一行，於是兩種實作都綠。
> 改成 `src/*.{vue,ts}` 之後才真的會紅。**一條測到一半的測試與一條有效的
> 測試長得一模一樣** —— 這一輪第二次踩到同一件事。

### 原本的問題描述（保留）

做第 24 項時想用一個型別層的等式把兩份 union 釘在一起，**而它什麼都沒檢查**。
同一行 `const broken: number = "顯然是字串"` 放進 `.vue` 是 **0 errors**、
放進 `.ts` 是 **1 error**。當時 6 個 `.vue`（含 `platform/ui` 全部元件）
一行型別檢查都沒跑過。

**為什麼這件事跟 api-surface 是同一個根**：`tools/api-surface/src/shape.ts`
存在的理由，正是 `declare module "*.vue"` 讓 checker 對每個元件都回報同一個
泛型 —— 它自己去解析 SFC 才拿得到 props。那個 shim 讓 checker 看不見元件，
而型別檢查跳過 `.vue` 是同一件事的另一面。

**這也是為什麼第 24 項那個型別等式必須改成讀原始碼比對**
（`platform/ui/tests/theme.test.ts`）：在 SFC 裡寫型別斷言是裝飾品，
而裝飾品比沒有更糟 —— 它讓人以為有東西在守。

> ⚠️ 那個 shim 現在仍然在，而且**必須在** —— tsgolint 還是不認識 `.vue`。
> 反向測試的 fixture 也刻意帶著同一份，因為要證明的正是
> 「有了這個萬用宣告之後，跨元件的型別檢查還剩多少」（答案：還剩很多）。

---

## 拿去給誰、跑哪一行

| 對象          | 指令                                | 產出                                   |
| ------------- | ----------------------------------- | -------------------------------------- |
| 資安          | `vpr sca-dossier`                   | SCA 例外申請書（含補償控制與佐證分級） |
| 法務          | `vpr sca-dossier`                   | 同上的〈授權分佈〉一節                 |
| 平台／IT      | `vpr mirror-manifest`               | 574 筆鏡像清單，含 sha512              |
| 平台／IT      | `vpr airgap`                        | 封閉網路前置條件、平台矩陣、驗收方式   |
| 稽核          | `tools/exit-drill/evidence.json`    | 退出演練證據（進版控）                 |
| 架構／gateway | `vpr bff-check`                     | D8 中間層的 13 條驗收條目              |
| 法遵          | `vpr compliance`                    | 條號 ↔ 閘門 ↔ 證據對照表（含未覆蓋的） |
| 機關／無障礙  | `tools/compliance/ACCESSIBILITY.md` | 成功準則 ↔ 誰判定 ↔ 開發期擋不擋得掉   |
| 法遵／IT      | `vpr evidence`                      | §16 要放進保存排程的檔案清單           |

除了退出演練那一份之外都是**產生**的，不是寫的 —— 這份 repo 在同一個地方
一再犯錯（人抄下來的數字沒有人再推導一次），所以給外部團隊的數字一律由機器算。
細節見 `DECISIONS.md` 的 C17／C24／C25／C27／C31／C36，以及 §11 II ⑦ 那次註記。
守備範圍由 `tools/doc-facts` 涵蓋，**守哪幾份由它自己印出來**（見第 18 項的限制）：

```bash
./node_modules/.bin/vpr doc-facts
```
