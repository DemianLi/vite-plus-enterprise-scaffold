# 交接清單：程式碼做不到的部分

> 腳手架本身已完成（`vpr ready` exit 0）。這份清單收的是**只有組織能做的決定** ——
> 從 `DECISIONS.md` 的風險登記簿、C13、以及各處置章節收攏而來，按**誰要做**排列，
> 因為它是拿來一段一段交出去的。
>
> 每一項都附「拿什麼去談」：那些東西**都已經備好**，不需要再做技術工作。
> 最後更新：2026-08-16（腳手架初版，送人工審查）。
>
> ── ⚠️ 這張表的評定是**逐案**的，不是腳手架的屬性（2026-08-16 重評）────
>
> 第一版把每一列評成「上線前必須 ✅／⬜」，而那個寫法假設有**一個**上線環境。
> 實際的使用情境是：**這份腳手架會被 fork 去接政府採購案**，`platform/*`
> 發成內部套件供各案升級。作為**受託者**，義務是逐案從委託機關繼承的 ——
> 同一列在不同標案下的答案不一樣，所以這份清單不可能有一個固定的結論。
>
> 因此〈上線前必須〉拆成兩欄，它原本混了兩種**會往相反方向走**的東西：
>
> - **東西會壞？** —— 技術硬性。不做的話建置或驗收在技術上出問題，與標案無關
> - **標案觸發** —— 契約條件。投標時就要確認，不是上線前才問
>
> 重評當天確認的四件事，它們決定了 11 列的理由：
>
> | 問的                                   | 答案                           | 改掉哪幾列               |
> | -------------------------------------- | ------------------------------ | ------------------------ |
> | CI／開發機能連公網？                   | 能                             | 但**交付端是另一個環境** |
> | 契約要求原始碼交付／機關端重建？       | **要**                         | 5／6／7／9／11／16       |
> | 源碼掃描、弱點掃描／滲透測試是交付物？ | **是**（第三方或機關指定工具） | 21／13                   |
> | 前端畫面上有個資？                     | **少數後台畫面有**             | 20                       |
>
> ⚠️ **不要照「無金流、無個資留存」這類描述去砍這張表。** 重評前的定位敘述
> 指向刪減，真實定位（政府採購、受託者義務逐案繼承）指向擴張 ——
> 照前者動手，會砍掉驗收要用的東西。這一段寫下來是因為它差點就發生了。
>
> ── 清單裡混了兩種東西，讀之前先分開 ────────────────────────────────
>
> **判斷題**（1、2–3、4、5–7、8、9、10、11–13、14、19、20）——
> 只有貴組織能裁決：要不要付這個錢、接不接受這個風險、由誰負責。
> 技術面能做的都做完了：事實查到底、成本量化、工具在失敗當下自己講清楚。
>
> **還沒做完的事**（15、16、17、18、21、22）—— 不是判斷題，是缺口。
> 其中 **15 是採用這份腳手架的第一個步驟**（把 `@org/*` 換成真的團隊），
> 其餘五項是腳手架自己知道、也寫下來的盲點。
>
> ⚠️ 每一項的**編號固定不動**。有幾項已經在技術面處理掉或降級（4／7／9／12／14），
> 它們留在原位並標上結論，不重新編號 —— 這份清單是拿去給不同單位、
> 不同時間讀的，「第 12 項」在兩次會議之間變成別的東西比過期更糟。
>
> ⚠️ 這段刻意**不再手寫「N 項裡有 M 項…」的分項計數**。第一版寫了，
> 然後在項次長到 19 的時候整段都是錯的 —— 而那正是這份 repo 一路在防的形狀
> （見 `tools/doc-facts`）。總數由機器守著，分項計數沒有事實來源，所以拿掉。

## 先看這張表

| #   | 對象         | 事項                                                                         | 不做的後果                                           | 東西會壞 | 標案觸發                                         |
| --- | ------------ | ---------------------------------------------------------------------------- | ---------------------------------------------------- | -------- | ------------------------------------------------ |
| 1   | 技術主管     | 核准 beta **版本流**、供應商登記 Cloudflare                                  | 元件不合規；修補 SLA 形同虛設                        | ⬜       | 每案資安審查都會問「為什麼用 beta」              |
| 2   | 資安         | 原生工具鏈的**政策性**例外（144 個二進位）                                   | 機關端 SCA 判 fail                                   | ⬜       | **每一案**                                       |
| 3   | 資安         | 接受 43 個只有發佈簽章的佐證                                                 | 同上，且覆核時會被抓                                 | ⬜       | **每一案**                                       |
| 4   | 法務         | MPL-2.0 ＋ 22 個**完全無授權聲明**的套件                                     | 授權政策掃描標記，可能驗收前才爆                     | ⬜       | **每一案**                                       |
| 5   | 平台／IT     | 內部 registry 鏡像 **563 個**套件                                            | 機關端重建時抓不到套件                               | ✅       | 原始碼交付／機關端重建                           |
| 6   | 平台／IT     | registry 設在**機器層級**，不是專案                                          | 封閉環境下 `vp` 第一步就往公網連                     | ✅       | 同上（收件人是**機關端**，不是你們 CI）          |
| 7   | 平台／IT     | 確認 `darwin-x64`／`win32-x64` 是否要支援                                    | 機關端那台裝不起來                                   | ✅       | 同上（問的是**機關端跑什麼平台**）               |
| 8   | 架構         | 指派 D8 同源中間層由誰提供                                                   | 登入、CSRF、401／403 整條路徑沒有著落                | ✅       | **每一案**（有登入就有）                         |
| 9   | 發版流程     | 「`--capture` 在公網側完成」寫進流程                                         | 兩份檔案沒一起進機關端，閘門紅而現場修不了           | ✅       | 原始碼交付／機關端重建                           |
| 10  | 資安         | SCA 掃描把 `bingo` 標為 dev-only                                             | 開發期工具被當成 runtime 相依計嚴重度                | ⬜       | 每一案（5 分鐘）                                 |
| 11  | 平台（CI）   | 內部 registry 下的 bootstrap ＋ SBOM 工具                                    | 私有網路下抓不到相依；SBOM 非稽核認可                | ✅       | 前半：機關端重建／後半：**每一案**               |
| 12  | repo 管理者  | 自動核准要不要開（標籤已建）**安全決定**                                     | 開錯會讓 CODEOWNERS 整套失效                         | ⬜       | 15 做完之後才有意義                              |
| 13  | 平台（上線） | CSP 由 report-only 切成 enforce                                              | 弱點掃描報告**必開一條**；report-only 等於沒有       | ⬜       | **有弱點掃描／滲透測試的案**                     |
| 14  | 架構／設計   | ~~樣式策略選型~~ **已決策＋實作＋驗證（D15）**。剩：設計系統要不要獨立 owner | 破壞性變更被擋下來之後沒有收件人                     | ⬜       | 多案共用 `platform/ui` 之後（與 22 綁定）        |
| 15  | repo 管理者  | **把 `@org/*` 換成真的團隊** —— 現在 22 條全是 Unknown owner                 | D12 的擁有權治理目前是一份文字檔，完全沒生效         | ⬜       | **採用的第一步**，每 fork 一次做一次             |
| 16  | 平台（實作） | 補「import 了但 `package.json` 沒宣告」的一般性檢查                          | 幽靈依賴 —— **機關端重建時才爆，而那是驗收現場**     | ✅       | 原始碼交付／機關端重建（**半天，最便宜的一列**） |
| 17  | 平台（實作） | `api-surface` 看不見**型別形狀**的破壞性變更                                 | 各案升 `platform/*` 只拿到型別錯誤，沒有遷移路徑     | ✅       | `platform/*` 發成內部套件之後                    |
| 18  | 平台（實作） | 文件數字守衛的守備範圍仍不完整                                               | 沒被登記的數字仍會過期，而且沒有人會發現             | ⬜       | 內部工作，不由標案觸發                           |
| 19  | 法遵／IT     | §16 五年保存：**腳手架交出清單，保存排程是你們的**                           | 證據存在但沒進保存制度；`sbom.cdx.json` 90 天就沒了  | ⬜       | **契約寫了保存年限的案**（目前手上的案沒寫）     |
| 20  | 法遵／資安   | §11 II ⑨ 隱碼**現在是慣例，不是機制**（2026-08-16 拆掉閘門）                 | 後台新增個資欄位忘了遮罩時，沒有任何東西會說話       | ⬜       | **畫面上會出現個資的案**（→ 走白名單那條路）     |
| 21  | 資安         | 開發期**源碼掃描（SAST）沒有接上**，只有 lint 那一半                         | 廠商掃出來的東西要在驗收期限內改完，改不完＝延遲驗收 | ⬜       | **源碼掃描是交付物的案**                         |
| 22  | 架構／設計   | **無障礙沒有任何東西在守** —— 能力在 reka-ui，強制它的沒有                   | 驗收前才知道，而無障礙的改動是全面性的               | ⬜       | **民眾端系統**（等級以 RFP 為準）                |

---

## 1. 技術主管 — beta 版本流的事先核准

> **2026-08-16 換了對象。** 原本寫「採購／稽核」，那假設相依是由客戶核准的。
> 接案公司自己選相依，所以核准的人是你們的技術主管 —— 但**每一案的機關資安
> 審查都會問「為什麼用 beta」**，所以要準備的東西一個字都沒少，只是收件人變了。

**要決定的**

- 核准的對象是**版本流**（`vite-plus@0.2.x`），**不是逐版**。
  0.2.5→0.2.9 共 4 版、26 天，約 5–14 天一版。逐版核准會讓 D13 的
  **critical 3 天修補 SLA 一開始就是死的**
- 供應商紀錄登記 **Cloudflare**，不是 VoidZero（2026-06-04 併購）

**拿什麼去談**

授權疑慮已解除：`vite-plus`、core、8 個原生二進位的 `license` 欄位實測皆為 MIT。
但 **MIT 意味著無擔保、無支援義務**，官方文件沒有任何 SLA。所以緩解措施只能自己持有——
而它已經存在：

```bash
./node_modules/.bin/vpr exit-drill    # 每季一次，證據進版控
```

最後一次（2026-08-15）：退到上游 **Vite 8.2.1** 建置成功、上游 Vitest **108 個測試全過**、
**應用程式原始碼一字未改**、耗時 5 秒。證據在
[`tools/exit-drill/evidence.json`](tools/exit-drill/evidence.json)。

> 說法的差別：不是「我們押注在 beta 上，出事再說」，
> 而是「退出路徑每季實測，最後一次是 evidence.json 上的日期」。
> 第二種採購會接受 —— 差別不在風險大小，在於風險**是否被證明是有界的**。

---

## 2–3. 資安 — 原生二進位的 SCA 例外

**要決定的**

- 核准 **144 個平台原生二進位、12 個家族**的例外。建議按**政策**核准
  （「原生編譯的工具鏈套件」），不是逐廠商 —— 因為 `@typescript`（TypeScript 7 自己）
  與 `lightningcss` **都不是 vite-plus 帶來的**，是整個前端工具鏈原生化的結果
- 接受佐證**分兩級**：101 個有 SLSA provenance（可回推到來源 repo 的確切 commit），
  43 個只有 npm 發佈簽章（可驗發佈者，無法回推建置來源）

**拿什麼去談**

```bash
./node_modules/.bin/vpr sca-dossier
```

申請書全部由 `pnpm-lock.yaml` 推導，**不要手改**。內容含逐家族明細、佐證等級、
授權分佈、容量，以及四項**由閘門實際斷言**（非宣稱）的補償控制：

- 563 個套件全帶 sha512 integrity，CI 以 `--frozen-lockfile` 安裝
- `allowBuilds` 內沒有任何原生套件 → 那 144 個在安裝時不執行任何腳本
- lockfile 的 digest 與擷取當下一致，每次 gate 比對
- 家族清單進版控，新家族出現時閘門擋下並要求人工分類

> ⚠️ 43 個沒有 provenance 的那批含**全部 20 個 `@typescript/typescript-*`**。
> 若沿用舊說法「證據是 SLSA provenance」，覆核抽驗 TypeScript 時會當場破功。

---

## 4. 法務 — 兩件授權問題

| 事項                 | 範圍                                                                | 說明                                                                                       |
| -------------------- | ------------------------------------------------------------------- | ------------------------------------------------------------------------------------------ |
| **MPL-2.0**          | `lightningcss-*` 22 個（兩個版本）＋ `eslint-plugin-no-unsanitized` | 檔案層級弱著作權。三者皆僅作**建置期工具**、產物不含其原始碼，但多數企業授權政策會標記出來 |
| **完全沒有授權聲明** | `@yuku-parser`／`@yuku-codegen` 共 22 個                            | 見下方查證結果 —— 比「registry 上沒填」更嚴重                                              |

#### ⚠️ MPL-2.0 的範圍比申請書上看到的大（2026-08-16 發現）

`eslint-plugin-no-unsanitized@4.1.5`（Mozilla）也是 MPL-2.0，而**它不在
`vpr sca-dossier` 的〈授權分佈〉裡** —— 那一節只涵蓋 144 個**原生二進位**，
一個純 JS 的直接相依對它是隱形的。

這不是申請書寫錯，是它的取數範圍就是原生二進位（R2 的問題）。缺口由
`vpr capture-health` 補上：它掃的是 24 個**外部直接相依**，
判定與申請書共用同一份 `COMMON_LICENSES`。

> 談的時候把兩者一起講，否則法務核准了「11 個 lightningcss」之後，
> 覆核時會發現還有一個沒被提到的 MPL-2.0 —— 那比一開始就講出來難處理得多。

#### `@yuku-*` 的實際情況（2026-08-15 逐一查證，不是推測）

拆開 `@yuku-parser/binding-linux-x64-gnu@0.5.48` 的 tarball，裡面**只有兩個檔案**：

```
package/package.json      321 B   ← 沒有 license 欄位
package/yuku-parser.node  3.8 MB  ← 原生二進位
```

**沒有 LICENSE 檔。** 而且上層套件 `yuku-parser@0.5.48` 的 tarball 同樣**沒有 LICENSE 檔**
（只有 package.json／README.md／binding.js／decode.js／index.d.ts／index.js／walk.js），
它僅在 package.json 的 metadata 字串宣告 `MIT`。

所以要向法務問的問題是明確的，不是「請確認一下」：

> 22 個原生二進位（每個約 4 MB）進入我們的建置環境，**發佈的成品裡沒有任何授權文字**。
> 唯一的授權宣告是母套件 metadata 的一個字串。這樣可以接受嗎？
> 還是要求上游（`github.com/yuku-toolchain/yuku`）在 binding 套件補上 `license` 欄位與 LICENSE 檔？

順帶一提，這 22 個是 `@voidzero-dev/vite-plus-core` 拉進來的（Zig 寫的 JS/TS parser），
**不是我們直接選用的相依** —— 換句話說這是 vite-plus 的供應鏈帶進來的問題。

工具刻意**不**從母套件推斷授權填進申請書：**從別的套件推斷授權等於代發佈者做法律聲明。**

清單在 `vpr sca-dossier` 的〈授權分佈〉一節（記為 `UNKNOWN`，22 個）。

---

## 5–7. 平台／IT — 內部 registry 與封閉網路

> **2026-08-16 換了理由，結論反而變硬。** 原本這三列掛在一個假設上：
> 「貴組織可能是封閉網路」。重評時先確認你們的 **CI 與開發機能直連公網** ——
> 那一度讓這三列看起來可以拿掉。
>
> **問錯了軸。** 建置在你們這側，但**交付／驗收在機關端**，而契約要求
> 原始碼交付與機關端重建。所以這三列不但留著，而且從「可能用得到的預防措施」
> 變成**交付物**：`vpr mirror-manifest` 的清單、`vpr airgap` 的前置條件，
> 都是隨原始碼一起交出去的東西。
>
> 觸發條件因此是可驗證的，不再是猜測：**契約有沒有要求機關端重建或原始碼交付。**
> 沒有的話這三列不適用；有的話它們是硬的。

**要做的**

```bash
./node_modules/.bin/vpr mirror-manifest   # 563 筆，含 sha512，可直接餵給鏡像工具
./node_modules/.bin/vpr airgap            # 前置條件、平台矩陣、驗收方式
```

三個容易踩的點：

**① 鏡像清單不能照著安裝結果列。** `pnpm` 只裝符合當下平台的 optional dependency，
所以照 `node_modules` 或 lockfile 的 `snapshots:` 區產出的清單，
**必然只涵蓋產清單那台機器的平台** —— 那正是「mac 裝得起來、CI 的 linux-x64-gnu 直接爆」。
`vpr mirror-manifest` 讀的是 `packages:` 區（全平台中繼資料）。

**② 有一批不在專案的相依樹裡。** `pnpm`、`@pnpm/exe` 與 7 個平台變體、`@reflink`、
`detect-libc` 共 **19 個**，住在 `pnpm-lock.yaml` 的**第一份 YAML 文件**。
它們是 `vp` 為了啟動自己而取得的。漏掉的症狀不是某個套件裝不起來，
是 **`vp` 連跑都跑不起來**。

**③ registry 一定要設在機器／映像檔層級。** 實測四種設法：

| 設法                               | 涵蓋 `vp` 下載 pnpm？       |
| ---------------------------------- | --------------------------- |
| 專案 `.npmrc` 的 `registry=`       | ❌ **否**                   |
| 全域 `~/.npmrc` 的 `registry=`     | ✅ 是                       |
| `npm_config_registry` 環境變數     | ✅ 是（CI 用這個）          |
| `devEngines.packageManager.onFail` | ❌ **否**（不是下載的開關） |

第一列會製造一種很難察覺的假象：專案相依確實走了內部 mirror，於是全隊都相信
封閉網路沒問題 —— 但 `vp` 的第一步仍在往公網連，要到真的斷網那天才會發現。

**容量**：原生二進位部分 tarball 合計 **856 MB**（mirror 要存的），
解壓後 **2341 MB**（開發機與 CI 的 `node_modules` 佔的）。

**驗收**（在真的連不到公網的機器上，用乾淨的 HOME）：

```bash
HOME=$(mktemp -d) npm_config_registry=https://<內部位址>/ vp install --frozen-lockfile
```

乾淨的 HOME 是重點 —— 開發機上的 pnpm 快取會讓這個測試假性通過。

**待確認，而且現在有價目了**：四個目標平台裡，`linux-x64-gnu`（CI）與 `darwin-arm64`（開發機）
有依據，**`darwin-x64` 與 `win32-x64` 是假設**。`vpr airgap` 的〈要鏡像到哪些平台〉
現在會列出每個平台各要多存多少 —— 例如 `darwin-x64` 是 10 個套件、49 MB。

這樣「要不要支援 Intel Mac」就變成一個帶價目的決定，而不是憑印象回答的問題。
確認後請一併改 `tools/supply-chain/src/inventory.ts` 的 `TARGETS`（閘門會驗兩個方向）。

> ⚠️ **2026-08-16：這個問題的對象變了。** 原本問的是「你們的開發機有沒有 Intel Mac」，
> 那是一個內部盤點。現在問的是**機關端的重建環境跑什麼平台** —— 而政府機關的
> 建置機器是 `win32-x64` 的機率遠高於開發團隊。
>
> 而且它有牙齒：`TARGETS` 餵給供應鏈閘門，[`inventory.ts`](tools/supply-chain/src/inventory.ts)
> 要求 toolchain 家族**必須涵蓋全部 `TARGETS`**。少列一個平台，
> 申請書與閘門會一起在那個平台上失明 —— 而發現的時機是機關端重建那天。
>
> **這一格請在投標階段問，不是動工之後。**

---

## 8. 架構 — D8 同源中間層由誰提供

**要決定的**：那一層掛在哪、由誰維運。腳手架不提供它 —— 它是組織既有的 gateway。

**已經備好的**：它必須做到什麼（13 條可執行契約），以及怎麼證明做到了。

```bash
# 已經有 gateway：
BFF_ORIGIN=https://gateway.internal ./node_modules/.bin/vpr bff-check
# 全綠 = R6 關閉，不需要新程式碼

# 還沒有：照 platform/bff-contract 的規格蓋，platform/bff-mock 是已通過它的參考實作
```

**好消息是它比原本想的便宜一個量級**：建置產物**零個 inline script**，
所以 **CSP 不需要 nonce**。不需要 nonce 就不需要逐次請求改寫 HTML 的中間層 ——
組織端的要求從「準備一個會改寫 HTML 的東西」降到「多送幾個回應標頭」。
這個前提由 `assertStaticCspCompatible()` 在每次建置守著。

**誠實的限制**：`POST /api/session` 在真實環境是 OIDC 授權碼流程的終點，無法自動走完。
驗收時屬性面（HttpOnly／Secure／SameSite／Path）用 `BFF_SET_COOKIE_FILE` 貼上
gateway 實際回的 `Set-Cookie`，行為面用 `BFF_SESSION_VALUE` 帶一組真實 session。

---

## 9. 發版流程 — 一條硬性限制要寫進去

> **封閉環境裡無法就地升相依。**
> 改 `pnpm-lock.yaml` 與跑 `vpr supply-chain --capture` 必須在**還連得到公網的那一側**
> 做完，兩份檔案一起隨變更進到封閉環境。

原因：供應鏈閘門在 `provenance.json` 與 lockfile 對不上時直接失敗，
而重新擷取需要 `registry.npmjs.org`。

這是設計的結果不是缺陷 —— 反過來讓閘門自己連公網補資料，它就會在最需要它的環境裡失效。

**閘門現在會在紅燈當下自己講完這段**（2026-08-15 補）。原本它只說「跑 --capture
（需連得到 registry.npmjs.org）」，而在封閉網路裡看到那行的人會去跑一個永遠不可能
成功的指令，然後懷疑是網路設定壞了。現在三條失敗路徑都會附上「這件事不能在這裡做」。

**但流程文件仍該寫。** 錯誤訊息教的是「現在該怎麼辦」，流程要回答的是
「誰在什麼時候做、產出的兩個檔案怎麼一起進來」—— 那是工具講不了的。

> **2026-08-16：這一列從 ⬜ 升到硬性，因為它終於有了真實用例。**
> 原本它是一條寫給假想封閉環境的紀律。現在的形狀很具體：
> **你們在辦公室（連得到公網）跑 `--capture`，兩份檔案隨原始碼交付進機關端。**
> 漏掉其中一份，機關端重建時閘門會紅，而那是**現場修不了**的紅 ——
> 因為修它需要連 `registry.npmjs.org`，而那台機器連不到。
>
> 所以流程文件要寫的是**交付清單**：`pnpm-lock.yaml` 與 `provenance.json`
> 一起進交付包，而且是同一次擷取的產物。

---

## 10. 資安 — SCA 掃描範圍

`bingo`（產生器框架）由 `tools/slice-gen` 使用，屬**建置期／開發期**相依，
不進入任何交付產物。此分類**已由閘門斷言**：`apps`／`features`／`platform`
之下沒有任何 package 宣告 `bingo` 或 `@org/slice-gen`。

納入掃描範圍時請標為 **dev-only**，與 runtime 相依分開計算嚴重度。

---

## 11–13. 其餘待辦

**11 — CI 已在 GitHub Actions 上實跑過了**（2026-08-15，公開 repo）。
Tier 1 一次就綠；Tier 2 首跑紅，抓到三個本機看不到的問題，**都已修掉**
（根 `package.json` 漏宣告 `vitest`、`if: always()` 涵蓋範圍過大、
一個過時且寫著錯誤數字的 provenance 步驟）—— 詳見 `DECISIONS.md` 的 C32。

**剩下兩處仍需貴組織的環境才驗得了**：

1. **bootstrap 步驟**在內部 registry 下需設 `npm_config_registry` 與 `NODE_EXTRA_CA_CERTS`
   （公網環境驗不出來，因為它預設就通）
2. **SBOM／SCA 工具**目前用 Trivy。若貴組織用 Blackduck／Snyk，這兩步要換 ——
   交付稽核的必須是**稽核認可的那個工具**的輸出。
   **換工具時務必先驗這兩件事**（Trivy 兩件都中了，見 C33／C34）：

   | 要驗什麼                              | Trivy 的情況                           |
   | ------------------------------------- | -------------------------------------- |
   | 會不會把 dev 相依整批抑制掉？         | **會**。預設抑制 → SBOM 0 個 component |
   | 讀不讀得到多文件的 `pnpm-lock.yaml`？ | **讀不到**。只解第一份 → 只看到 19 個  |

   驗法不必靠人記得：閘門裡的 `vpr supply-chain --verify-sbom` 會比對
   SBOM 的 component 數與 lockfile 的套件數，任一種失明都會變紅。
   **換工具時保留這道檢查，比換對工具更重要。**

**12 — `platform-codemod` 標籤已建好**（2026-08-15）。剩下的是**自動核准要不要開、怎麼開**，
而那是一個**放寬 review 的安全決定**，不該由實作者單方面做。

要決定的是這件事：

> GitHub 的 **triage** 權限就能貼標籤。所以「貼上 `platform-codemod` 就自動核准」
> 等於**任何有 triage 權限的人都能繞過 CODEOWNERS**，包含繞過 `@org/security`
> 共同持有的那幾條路徑（`eslint.config.js`、`.npmrc`、`.github/workflows/`、
> `platform/security-headers/`、`tools/supply-chain/`）。

三個選項，成本與風險差很多：

| 做法                                       | 風險                                           |
| ------------------------------------------ | ---------------------------------------------- |
| 直接自動核准帶標籤的 PR                    | **等同把 CODEOWNERS 變成建議**。不建議         |
| 自動核准，但**排除資安共管的路徑**         | 可接受。需要在 workflow 裡維護一份路徑排除清單 |
| 不自動核准，改成把標籤當**優先審查的訊號** | 零風險，但沒解決核准地獄                       |

若選第二種，workflow 還必須確認**貼標籤的人不是 PR 作者**，否則作者自己貼自己過。

**這一項我刻意沒有實作**：它不是技術難題，是「要放寬多少 review」的組織決定，
而錯誤的版本會安靜地讓前面所有 CODEOWNERS 設計失效。

**13 — CSP 由 report-only 切成 enforce** 由 gateway 執行。目前 dev 已套用 report-only，
政策的單一事實來源在 [`@org/security-headers`](platform/security-headers)。
切換前請先看一段時間的 violation 報告。

---

## 14. 架構／設計 — 樣式策略 ✅ 已決策（2026-08-15）

**決策：shadcn-vue（reka-ui ＋ Tailwind v4），元件原始碼住 `platform/ui`。**
完整理由見 [`DECISIONS.md`](DECISIONS.md) 的 **D15**，市調見
[`UI-SURVEY.md`](UI-SURVEY.md)，可重跑的工具在 [`tools/ui-survey`](tools/ui-survey)。

```bash
node tools/supply-chain/src/cli.ts --capture-health   # 授權與維護狀態（實際相依，非候選）
node tools/ui-survey/src/cli.ts --csp        # 執行期 <style> 注入探測
node tools/ui-survey/src/cli.ts --sca        # 供應鏈增量
```

### 仍然要人決定的一格

**設計系統與應用外殼，是不是同一個團隊的事？**

這比「誰來當 owner」具體，因為**候選已經在 CODEOWNERS 裡了**：
最下面的 `@org/team-platform-ui` 擁有 `/apps/console/`。

| 選項                         | 意思                                      | 代價                                                                             |
| ---------------------------- | ----------------------------------------- | -------------------------------------------------------------------------------- |
| 沿用 `@org/team-platform-ui` | 維護應用外殼的人一併管設計系統            | 把「全公司產品長什麼樣」交給一個維護單一應用的團隊                               |
| 維持 `/platform/` 通則       | platform-maintainers 一體適用（**現況**） | 沒有人專門看設計一致性；審查會變成「程式碼對不對」而不是「這個元件該不該長這樣」 |
| 另設設計系統小組             | 獨立 owner                                | 要真的有人，否則就是掛一個不會回應的名字                                         |

**現況是第二個**：`platform/ui` 刻意**沒有**獨立條目 —— 它已被 `/platform/`
通則涵蓋，再寫一條相同 owner 的規則是完全等價的 no-op，
而 no-op 配上說明會讀起來像「這裡做過一個決定」。

要獨立 owner 的話在 CODEOWNERS 加一列即可，**但要加就要是真的決定**。

> 為什麼這一格不能拖：`platform/ui` 是**唯一一個所有切片都依賴的 UI package**。
> 它的破壞性變更由 `tools/api-surface` 擋下，但擋下來之後**要有人回應** ——
> 沒有 owner 的 platform 套件會讓那道閘門變成一個沒有收件人的警報。

### ⚠️ 但在那之前：整份 CODEOWNERS 現在一條都沒有生效

查證的時候發現的，而且與 `platform/ui` 無關 —— **22 條全部是 `Unknown owner`**：

```bash
gh api repos/<owner>/<repo>/codeowners/errors
```

原因是這份腳手架裡的 `@org/*` **全部是佔位符**。在還沒替換成真的組織與團隊之前：

- PR 不會自動指派審查者
- 保護分支的「需要 owner 核准」沒有東西可要求
- 也就是說 **D12 的整套擁有權治理目前是一份文字檔**

而 `tools/conformance` 驗的是**條目存在**，不是 **owner 有效** ——
**存在不等於生效**，這道落差本機看不出來，只有 GitHub 知道。

**採用這份腳手架的第一件事**：把 `@org` 全部換掉，然後跑上面那行確認 0 錯誤。
已寫在 `CODEOWNERS` 的檔頭，因為那是會被讀到的地方。

### 實作待辦 ✅ **五項全部完成**（2026-08-15，見 C38／C39）

| #   | 事項                                     | 狀態                                                                                       |
| --- | ---------------------------------------- | ------------------------------------------------------------------------------------------ |
| 1   | `@tailwindcss/vite` 加進 `DRILL_PLUGINS` | ✅ 已登記，並補上**產物比對**（反向測試：拿掉 @source → 37%，紅）                          |
| 2   | 重跑 `vpr supply-chain --capture`        | ✅ 519 套件／144 原生／12 家族。新家族 `@tailwindcss` 已分類為 toolchain                   |
| 3   | 新增 `platform/ui` package               | ✅ CODEOWNERS（暫掛）、api-surface（9 進入點／65 export）、退出演練 alias 都已接上         |
| 4   | 擋住「切片自己拼一套設計系統」           | ✅ 擋的是 **import 而非目錄**（理由見契約）；另加全 repo 禁用 reka-ui Splitter（CSP）      |
| 5   | 瀏覽器實測 CSP                           | ✅ 五個探針全對（Chrome 148）。⚠️ 證據檔與守它的閘門已於 2026-08-16 拆除（C52）—— 見下方註 |

> **第 5 項的現況（2026-08-16 更新）**：實測本身仍然成立 —— 五個探針在
> Chrome 148 上全對，論證留在 `DECISIONS.md` 的 C39／C47，重驗的步驟留在
> `tools/csp-verify/README.md`。**拆掉的是「把那次結論存成證據檔、由 CI 守其
> 指紋有效期」那套機制**（C52）：它每次失效都要人開一次瀏覽器，是所有閘門裡
> 每次成本最高的一道，而觸發它的是升 `reka-ui`／`vue`／`tailwindcss` 這種常見動作。
>
> 對審查者的意義：**「CSP 在 enforce 下實測過」是一份 2026-08-15 的紀錄，
> 不是一個持續成立的保證。** 升過相依之後要不要重驗，現在靠人記得。

### 會連動到本清單其他項的兩件事

- **第 4 項（法務）範圍會變大**：MPL-2.0 從「`lightningcss-*` 11 個」
  變成**兩個版本各一組**。`@tailwindcss/node` 釘死 exact `1.32.0`，
  而 `vite-plus-core` 要 `^1.33.0` —— 範圍不相交，無法合併。
- **第 5／6 項（封閉網路）**：新套件必須在**連得到公網那一側**跑
  `--capture` 再一起送進來。閘門刻意不自己連公網補資料。

### 為什麼沒有選比較便宜的那條

element-plus 只要 +21 套件、0 原生二進位、0 授權旗標，CSP 也已驗證零風險。
選了比較貴的一條，是因為成本花在**可以攤提的地方**：元件原始碼在自己手上。
完整的三方比較留在 `UI-SURVEY.md`，包含被淘汰者的理由 ——
其中 **PrimeVue 已於 2026-06-28 改為商業授權**，而市面上的比較文章
到今天仍然把它列為企業首選。

---

## 15. repo 管理者 — 把 `@org/*` 換成真的團隊（採用的第一步）

**`CODEOWNERS` 裡的 22 條全部是佔位符。在替換之前，這份檔案一條都沒有生效。**

GitHub 對每一條都會判 `Unknown owner`：PR 不會自動指派審查者，
保護分支的「需要 owner 核准」也沒有東西可要求。**D12 的擁有權治理目前
是一份文字檔。**

危險的地方在於它**本機看不出來**：`tools/conformance` 驗的是「每個
`features/*` 在 CODEOWNERS 有條目」—— 條目存在，所以它綠。
存在不等於生效，而只有 GitHub 知道差別：

```bash
gh api repos/<owner>/<repo>/codeowners/errors
```

本 repo 2026-08-15 實測：**22 條全部 Unknown owner**（見 C40）。

**刻意沒做成閘門**：它需要 GitHub API，會因為 token 權限與速率限制而變紅，
而不是因為真的有問題 —— 那種紅燈會被關掉（與 `tools/ui-survey` 不進 gate
同一條理由）。替換的清單與每條的用意都寫在 `CODEOWNERS` 檔案本身，
採用時從頭讀一遍即可。

⚠️ 這一項與第 12 項（自動核准要不要開）是**同一套機制的兩端**。
15 沒做，12 就沒有意義 —— 沒生效的 CODEOWNERS 沒有東西可以繞過。

---

## 16. 平台（實作）— 沒有任何檢查在看「幽靈依賴」

**全 repo 目前沒有一道閘門檢查「程式碼 import 了某個套件，但 `package.json` 沒宣告它」。**

2026-08-15 做 C41 時從反面撞到：`features/shipment` 宣告了 `@org/ui`
卻還沒 `install`，rolldown 的嚴格解析當場擋下（訊息清楚、指到正確的檔案）。
但那是**宣告了卻沒裝**。反過來的**裝了卻沒宣告**沒有東西在看：

- pnpm 的嚴格 `node_modules` 通常會擋下來 —— 但 workspace 根目錄的提升、
  `vite.config.ts` 的 alias、以及被別的套件間接帶進來的相依，都是繞過它的路徑
- 症狀是**本機與 CI 全綠，退出演練或單獨發佈時才爆** ——
  而那正是最難回推原因的時機（D2 的整個論證建立在「應用原始碼零依賴」上）

**刻意沒有夾帶進 C41 那次改動。** 只補 `@org/ui` 一個套件會讓規則讀起來
比它解的問題還窄；這條缺口涵蓋 `@org/http-client`、`@org/slice-kit`、
`@tanstack/vue-query` 等每一個依賴。它該是一條**一般性**規則，
用 `IMPORT_SPECIFIER_PATTERN` 掃出所有裸模組名，比對 `package.json` 的
`dependencies` ∪ `devDependencies`，再排除 Node 內建與相對路徑。

放在 `tools/conformance`，與現有各條同一個位置。估計半天。

> **2026-08-16：這一列這次升級最多，而它是全表最便宜的 ✅。**
>
> 上面列的兩個失敗場景（退出演練、單獨發佈）**都是假設性的** —— 它們是
> 「有一天可能會做」的動作。現在有第三個，而且它寫在契約裡：
>
> > **機關端重建時才爆。而那是驗收現場。**
>
> 幽靈依賴的性質剛好最適合在那裡爆：你們的機器上它被提升、被 alias、
> 被別的套件間接帶進來，所以本機與 CI 全綠；機關端是一份乾淨的重建，
> 那些繞過路徑一個都不存在。
>
> **半天的工作量，排在第 17 項（1–2 天）前面。**

---

## 17. 平台（實作）— `api-surface` 看不見型別形狀的破壞性變更

**`tools/api-surface` 只比對「模組 → export 名稱」。在一個匯出的 interface 上
加一個必填屬性，它完全不會說話。**

2026-08-16 做 §11 II ⑨ 時第一次撞到：`Feature` 加了必填的 `personalData`，
這對 `@org/slice-kit` 是破壞性變更 —— 下游**每一個既有切片都會編譯失敗** ——
而閘門只報了 `@org/pii` 那 7 個新 export。基準檔記的是：

> ⚠️ **那個欄位已於同日隨 C52 回退**，所以下面這個例子現在是歷史。
> **盲點沒有跟著消失** —— 它仍然是唯一一個實際撞到過的證據，
> 而且回退本身也是同一種變更（移除必填欄位），閘門同樣沒有說話。

```json
"@org/slice-kit": ["defineFeature", "registerFeatures"]
```

名稱一個都沒變，所以它是對的、也是瞎的。

D12 的規則是「改 platform 的破壞性變更必須附 codemod」，而這是**第一個那個
機制看不見的破壞性變更**。本 repo 的四個使用端都改好了，現況一致；
但這個腳手架是拿來被複製的 —— 下游團隊升級 `slice-kit` 只會拿到一個型別錯誤，
沒有遷移路徑，也沒有任何東西提醒作者該寫 codemod。

要修的話，基準檔得從「名稱清單」變成「名稱 → 型別形狀摘要」，
而摘要要穩定到不會因為換個型別別名就漂移。那是 api-surface 的一次重做，
不是一個補丁。估計 1–2 天。

⚠️ **在修好之前，改 `platform/*` 的 interface 要靠 review。**
這一條之所以寫下來，是因為「機制看不見」與「沒有破壞性變更」在閘門輸出上
長得一模一樣 —— 而那正是這個 repo 一路在防的形狀。

> **2026-08-16：從 ⬜ 升到 ✅，因為「下游」不再是假設。**
>
> 上面那句「這個腳手架是拿來被複製的」原本是一個意圖。現在它是既定的做法：
> **`platform/*` 發成內部套件，各案 fork 之後仍會升級它。**
>
> 所以那個「下游團隊只拿到一個型別錯誤」的場景有了具體的發生地點與時機：
> **某一案在驗收期升了內部套件，編不過，而沒有遷移路徑。** 那時候要接電話的
> 是你們自己的平台組。
>
> 1–2 天的重做在這個前提下划算了，但**排在第 16 項後面**（那個半天、
> 而且失敗現場更早）。

## 18. 平台（實作）— 文件數字守衛的守備範圍仍然不完整

**被守的那幾份文件裡，還有別的數字沒有登記給 `tools/doc-facts`。**

登記了幾個事實、幾個句子，**這裡刻意不寫** —— 那正是這道閘門在防的東西。
要當前的數字就跑一次，它自己會印：

```bash
./node_modules/.bin/vpr doc-facts
```

> ⚠️ **2026-08-16 這一項讓步驟自己示範了一次。** 出初版前重掃文件，
> 發現〈先看這張表〉裡三個數字過期得很嚴重（`121`→144、`32`→43、
> `467`→563），而這道閘門一直是綠的 —— 因為那三句話從來沒有被登記過。
>
> 那張表是拿去開會投影的一頁，也是採購與資安**唯一會讀完**的一頁。
> 已補登記（含新的 `slsa` 事實），並在 `facts.ts` 檔頭寫下修正後的順序：
> **新增引用時先問「這句話會不會被單獨拿出去用」，會的話優先登記。**
>
> ⚠️ **同一次掃描還發現守備清單本身漏了一份。** `UI-SURVEY.md` 從來不在
> 被守的文件裡 —— 理由很平庸：它不在根目錄那三份的直覺清單中。
> 但 README 與本檔第 14 項都把讀者**直接指過去**，而它當下就有一句過期的
> （`467` 套件／`121` 原生二進位）。已補進 `GUARDED`。
>
> 判準寫下來了：**被 README 或 HANDOFF 指過去、而且用現在式寫的，就要進去。**
> 讀者不會知道某一份文件的守備等級比較低。

沒守的分兩類，而只有第一類是待辦：

1. **有事實來源、還沒登記** —— 例如 `platform/*` 的進入點數與 export 數
   （來源：`tools/api-surface/surface.json`）、切片數（來源：`features/*`）。
   加一條登記就守得住，每條約十分鐘。

2. **沒有 repo 內部事實來源** —— 「四個目標平台」「每個約 4 MB」
   「實測 4/5 快取命中」。這一類**刻意不守**（A1）：硬守只能靠人再抄一次
   期望值進登記表，那是把同一個問題換到一個更少人會看的地方犯。

⚠️ 第二類要留在文件裡沒關係，但**不要讓它看起來像被守著的**。
判斷方法：那個數字改變時，有沒有一個 repo 內的檔案會跟著變？沒有就是第二類。

---

## 19. 法遵／IT — §16 五年保存：腳手架交出清單，保存排程是你們的

> **2026-08-16：這是重評裡唯一被降級的一列（✅ → ⬜）。**
> 你們目前手上的標案契約**沒有寫保存年限**，所以它不是契約義務。
>
> 但**不刪**，理由是這一列的答案是逐案的：作為受託者，義務從委託機關的
> 安全維護計畫繼承。某一案的契約寫了年限的那天，它立刻回到 ✅ ——
> 而下面第 1 點（`sbom.cdx.json` 只有 90 天）那時就是**硬傷**，
> 不是「未來要處理的事」。
>
> **投標時看到保存年限條款，先回來讀這一節。**

**這一條原本被寫成「腳手架欠一份保存期政策」。查完條文之後那句話是錯的。**

[§16](https://law.moj.gov.tw/LawClass/LawAll.aspx?PCODE=K0010162) 要求業者保存
五年的有**三類**，而前端只碰得到第三類：

| 保存什麼                       | 誰持有             | 腳手架碰得到嗎 |
| ------------------------------ | ------------------ | -------------- |
| 個資之蒐集、處理或利用紀錄     | 資料庫／後端       | ❌             |
| 自動化機器設備之軌跡資料       | 系統／基礎設施 log | ❌             |
| 落實執行安全維護計畫之**證據** | 依安全維護計畫而定 | ⚠️ 部分        |

誰歸檔、存哪、銷毀排程、負責人 —— 那是組織文件，不是 repo 裡的東西。
腳手架能做的是**產出證據**並**交出清單**：

```bash
./node_modules/.bin/vpr evidence
```

清單由 `tools/compliance/src/evidence.ts` 推導，CI 每次驗它沒有腐爛
（宣告的檔案都在、每一份都有閘門在維護）。**不要手抄那張表** ——
加新工具時它會變，而抄下來的版本不會。

**要決定的三件事**

1. **`sbom.cdx.json` 到不了五年。** 它是 CI artifact，GitHub 上限 90 天
   （公開 repo）、400 天（私有）—— **結構限制，腳手架修不了**。
   兩條出路都是你們的決定：進版控，或由組織的保存系統定期取走。

2. **版控的那幾份跟著 repo 走。** git 歷史本身就是保存機制，只要 repo 活著
   就超過五年 —— 但 repo 被刪、歷史被重寫（force push、filter-repo），
   證據就沒了。要不要備份、備份到哪，是你們的決定。

3. **清單上這幾份要不要寫進安全維護計畫。** §16 保存的是「落實執行**安全維護
   計畫**之證據」—— 如果計畫裡沒有提到這些閘門，這些檔案在法律上就不是
   那個「證據」。**先有計畫，這張清單才有法律意義。**

---

## 20. 法遵／資安 — §11 II ⑨ 隱碼現在是慣例，不是機制

**2026-08-16 拆掉了強制隱碼的閘門。遮罩的能力還在，強制它的東西沒了。**

原本的機制是兩層：切片契約有一個必填的 `personalData: readonly string[]`，
而宣告過的欄位在 `.vue` 模板裡必須包 `maskXxx()`，靜態檢查會比對；
渲染結果真的被遮住則由切片自己的元件測試證明。

拆掉的理由不是它沒價值，是**每次要人付的成本 × 觸發的頻率**（C52）：
它是「加一個切片」這個動作上最重的那一項摩擦，而加切片是這個腳手架
每天都在做的事。

**現在的狀態**

| 還在                                      | 沒了                                |
| ----------------------------------------- | ----------------------------------- |
| `platform/pii` 的 `maskName()` 等遮罩函式 | `Feature.personalData` 契約欄位     |
| `OrderList.vue` 仍然呼叫 `maskName()`     | 「宣告的欄位必須包 mask」的靜態檢查 |
| 切片自己的元件測試（跑在 Tier 1）         | CI 上的 `pii-masking` 這道閘門      |

法遵對照表上這一格**重新變成 🔴「無」**，這是刻意的：拿掉閘門而讓表繼續
顯示「已覆蓋」，比沒有閘門更糟 —— 那會讓組織以為有一個不存在的控制。

**要決定的**

新加的個資欄位忘了遮罩時，沒有任何東西會說話。三條路，成本差很多：

| 做法                                     | 成本                                                 |
| ---------------------------------------- | ---------------------------------------------------- |
| 維持慣例，靠 code review                 | 零工具成本。**取決於 review 的品質，會隨團隊變動**   |
| **只對「已知的個資切片」開白名單式檢查** | 中。要維護一份切片清單，但不影響其他切片的開發速度   |
| 復原原本的兩層機制                       | 高。回到每加一片都要回答一次的狀態 —— 就是被拆的原因 |

### 2026-08-16 重評：**選第二條**

重評時查到的事實把這三條路收斂成一條：**個資只出現在少數後台畫面**
（民眾端與一般使用者看不到）。

那正好是白名單式檢查的形狀，而且是三條裡代價最低的那條：

- 要維護的清單**很短**，而且**變動很慢** —— 後台切片不常新增
- **加一個切片的成本回到零**，那是 C52 拆掉原機制的唯一理由
- 遮罩的能力完全沒動過（`platform/pii` 還在），要接回去不必重新設計

⚠️ 但**觸發條件是逐案的**：這一列適用於「畫面上會出現個資」的案子。
接到純代號、純統計的案子時它不適用 —— 而那時 §11 II ⑥（測試環境不得用
真個資）仍然要，因為機關常常直接給真實資料做測試與資料移轉。
**⑥ 與 ⑨ 是兩件事，不要一起關掉。**

⚠️ **這是腳手架自己降級的一項控制，不是從來沒做過。** 實作與論證都留在
git 歷史裡（C50／C52），要復原不必重新設計。

---

## 21. 資安 — 開發期的源碼掃描（SAST）沒有接上

**目前 repo 裡的「源碼掃描」只有 lint 那一半，汙點傳遞類的問題沒有東西在看。**

ESLint 的 `security` 外掛與 `vue/no-v-html` 擋的是**單點樣式**
（`eval`、不安全的 regex、`v-html`）。它們看不見的是**跨函式的資料流** ——
一個從 `route.query` 進來的值，經過三層傳遞之後餵進 `innerHTML`，
每一個單點都合法。

已經動手做過一次（分支 **`feat/sast-semgrep`**，PR #18 **已於 2026-08-16 關閉**，
**分支刻意保留**），用 semgrep 開源版寫汙點規則。**卡住的原因寫在 PR 裡，簡述如下**：

1. `semgrep --test` 在找不到任何 fixture 時會印 `No unit tests found`
   然後**回傳 0** —— 也就是說 CI 顯示 4/4 綠，而實際上零條規則被驗證。
   這是這個 repo 一路在防的那個形狀。已加上防呆（`grep -q` 後 `exit 1`），
   所以那個 PR 最後是**正確地紅**。
2. 規則產生器讀的是 `Feature.personalData`，而那個欄位已隨第 20 項移除。

> **關閉不等於放棄，關的是做法**（理由見下一節與 C54 第六節）。
> 那個分支上有三樣東西要撿回新的實作裡，清單寫在 PR #18 的關閉留言，
> 因為**人不會去翻已關閉的 PR**。其中最要緊的是那道 `grep -q` 防呆 ——
> 換成公開規則集**一樣會踩**同一個坑。

### 2026-08-16 重評：⬜ → ✅，而且**修法要換一條路**

**源碼掃描報告是寫死的交付物**，由第三方或機關指定的商用工具掃。
所以 repo 裡的 semgrep 是**前置過濾器**，不是報告本身 —— 上面那句話沒錯，
但**價值論證要換掉**，因為原本的寫法（「讓廠商報告上少幾條」）聽起來像美觀問題：

> **廠商掃出來的東西，你們要在驗收期限內改完。改不完＝延遲驗收。**
> 前置過濾器把「驗收前才知道」變成「開發當下就知道」，而**分母是每一案**。

前置過濾器的成敗指標是**與商用工具的命中重疊率**。手寫汙點規則然後期待它
跟 Fortify／Checkmarx 那類工具撞在一起，是很差的賭注 —— 而這同時解掉上面
兩個 blocker：

- **blocker 2**：**不要從切片契約產生規則**。那個產生器在解錯的問題。
  改用 semgrep 的公開規則集（`p/security-audit`、`p/owasp-top-ten`、`p/xss`），
  它們是上游維護的，也是商用工具規則的同一批公開來源
- **blocker 1**：公開規則集**不需要**自己寫 `--test`（上游測過）。`--test`
  只留給你們保留的那幾條 repo 專屬規則，而 `grep -q` 那道防呆繼續留著

用 D16 兩軸看：**每次要人付**從「維護一套自寫汙點規則」降到一行 config，
**多久付一次**是每案一次。

⚠️ **誠實的但書**：公開規則集的誤報率高於自寫規則，第一次跑會噴一堆 ——
**而那正是閘門第一天就被關掉的典型死法**（`pii-check` 第一版把
`fsevents@2.3.3` 當成信箱那次）。要配一份 `.semgrepignore` 與一次人工分流，
那個成本要算進去，不要假裝它是零。

---

## 22. 架構／設計 — 無障礙：能力在，強制它的東西沒有

**2026-08-16 重評時發現的。它從來沒有進過這份清單，而它該進。**

查證到的事實只有兩件，兩件都很明確：

1. [`UI-SURVEY.md`](UI-SURVEY.md) 把它寫成**條件句**：「**若**組織有 WCAG
   要求，element-plus 與 reka-ui 的…」—— 然後就沒有再往下走
2. `eslint.config.js` 沒有 `vuejs-accessibility`，`tools/compliance` 的
   `CONTROLS` 與 `FUTURE` 一個字都沒有，本清單也沒有

**選型其實選對了**：reka-ui 的 WAI-ARIA 實作是它的賣點（見
[`DECISIONS.md`](DECISIONS.md) 的 D15／C39），焦點管理這類行為是它幫我們做掉的。

**但那不是同一件事**：

> **元件庫有無障礙 ≠ 你用它拼出來的頁面有無障礙。**
> 缺 label 的表單、只靠顏色傳達的狀態、跳號的標題階層、
> 自己寫的那些不經過 reka-ui 的互動 —— 元件庫一個都管不到。

而現在**沒有任何東西在守**。這是與第 20 項一模一樣的形狀：**能力還在，
強制它的東西沒有**，差別只在第 20 項是被拆掉的（有 git 歷史、有論證），
這一項是**從來沒有裝過**。

**要決定的**

⚠️ 要哪個等級、驗收怎麼判、有沒有法定強制，**以 RFP 為準，這裡刻意不寫死**
（與第 21 項同一個處理：契約是事實來源，不是我們的印象）。腳手架這一側要
決定的是**要不要裝一道閘門**，而那是一筆新工具的支出：

| 做法                                      | 成本                                                             |
| ----------------------------------------- | ---------------------------------------------------------------- |
| 什麼都不做，驗收前請廠商掃                | 零工具成本。**但無障礙的改動是全面性的**，驗收前才知道等於重做   |
| 加 `eslint-plugin-vuejs-accessibility`    | 低。擋得到靜態的那一半（缺 alt、缺 label、角色錯用），漏行為那半 |
| 上面再加 `platform/ui` 的元件層無障礙測試 | 中。守得住共用元件，但守不住各切片自己拼的頁面                   |

**建議在投標階段就問清楚等級**，因為這一列與第 14 項綁在一起：
無障礙的落點是**多案共用的設計系統**，而第 14 項正在問那個設計系統
要不要有獨立 owner。**沒有 owner 的設計系統，不會有人負責無障礙。**

---

## 拿去給誰、跑哪一行

| 對象          | 指令                             | 產出                                   |
| ------------- | -------------------------------- | -------------------------------------- |
| 資安          | `vpr sca-dossier`                | SCA 例外申請書（含補償控制與佐證分級） |
| 法務          | `vpr sca-dossier`                | 同上的〈授權分佈〉一節                 |
| 平台／IT      | `vpr mirror-manifest`            | 563 筆鏡像清單，含 sha512              |
| 平台／IT      | `vpr airgap`                     | 封閉網路前置條件、平台矩陣、驗收方式   |
| 稽核          | `tools/exit-drill/evidence.json` | 退出演練證據（進版控）                 |
| 架構／gateway | `vpr bff-check`                  | D8 中間層的 13 條驗收條目              |
| 法遵          | `vpr compliance`                 | 條號 ↔ 閘門 ↔ 證據對照表（含未覆蓋的） |
| 法遵／IT      | `vpr evidence`                   | §16 要放進保存排程的檔案清單           |

除了退出演練那一份之外都是**產生**的，不是寫的 —— 這份 repo 在同一個地方
一再犯錯（人抄下來的數字沒有人再推導一次），所以給外部團隊的數字一律由機器算。
細節見 `DECISIONS.md` 的 C17／C24／C25／C27／C31／C36，以及 §11 II ⑦ 那次註記。
守備範圍由 `tools/doc-facts` 涵蓋，**守哪幾份由它自己印出來**（見第 18 項的限制）：

```bash
./node_modules/.bin/vpr doc-facts
```
