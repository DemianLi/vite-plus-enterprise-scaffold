#!/usr/bin/env node

/**
 * 一支**故意忽略 `--root` 的假閘門**。⚠️ 這是 `src/probe.ts` 的已知非零對照組。
 *
 * ── 為什麼一定要有這一支 ────────────────────────────────────────────
 *
 * 這條線上今天唯一接進承諾規格的閘門（`tools/conformance`）**是**真的讀
 * `--root`。所以探針一寫出來就會全綠 —— 而全綠與「探針根本沒作用」長得
 * 一模一樣。這個 repo 在假的零上付過好幾次學費（`doc-facts` 的
 * `unguarded`、`spec-report` 的第四態、量測台裝在 repo 外面），每一次的
 * 解藥都是同一句：**表裡至少要有一列非零**。
 *
 * ── ⚠️ 它印出 argv，而那是刻意的 ────────────────────────────────────
 *
 * 這支假閘門把參數原樣印出來，卻完全不使用它 —— 也就是探針最容易在
 * **危險方向**上判錯的那個形狀（「輸出不同 → 它讀了」→ 放行一支在量真樹的
 * 閘門）。`probe.ts` 的兩趟對稱與遮罩就是為了接住它；拿掉那兩步的話，
 * 這一支會被判成「讀了 --root」。
 *
 * ⚠️ 它不在 `scripts.gate` 上，也不是任何 package 的 bin —— 它只被
 * `tests/probe.test.ts` 直接指名。
 */

console.log("✓ 假閘門通過（而它根本沒看那個目錄）");
console.log(`argv: ${process.argv.slice(2).join(" ")}`);
