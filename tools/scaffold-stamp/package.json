{
  "name": "@org/scaffold-stamp",
  "version": "0.0.0",
  "private": true,
  "description": "腳手架的章：fork 之後不准改的那一半，在 fork 的樹上還是不是原樣（C220，實作 C215 §六）",
  "bin": {
    "scaffold-stamp": "./src/cli.ts"
  },
  "type": "module",
  "scripts": {
    "test": "vp test",
    "check": "vp check"
  },
  "dependencies": {
    "@org/fork-select": "workspace:*",
    "@org/gate-kit": "workspace:*"
  },
  "devDependencies": {
    "@org/tsconfig": "workspace:*",
    "@types/node": "catalog:",
    "typescript": "catalog:",
    "vite-plus": "catalog:",
    "vitest": "catalog:"
  }
}
