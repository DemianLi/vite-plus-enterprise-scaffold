# 只有「可以被任何人看到」的值才准出現在這個檔案（D8）。
#
# VITE_ 前綴的值會被編譯進 production bundle 的明文 —— 它們躺在線上的 JS 檔裡，
# gitleaks 掃 build artifact 或按 F12 就撿得到。
#
# 機密一律不進前端：需要機密的呼叫走 BFF，由伺服器端持有。
#
# 這裡新增任何一筆，都必須同步加進 platform/config 的 PUBLIC_ENV_KEYS，
# 否則建置會失敗（assertNoUndeclaredEnv）。那個 diff 就是 review 的著力點。

VITE_APP_TITLE=Console

# BFF 的同源路徑前綴。必須是相對路徑 —— 換成完整 URL 會讓請求跨源，
# SameSite cookie 當場失效（D8）。
VITE_API_BASE_PATH=/api

# 由 CI 注入，用於錯誤追蹤時對應 sourcemap。
VITE_BUILD_SHA=local-dev

# ── 非 VITE_ 前綴：只在建置期使用，不會進入 bundle ──────────────────
# dev server 的 BFF 代理目標。
BFF_ORIGIN=http://localhost:8080
