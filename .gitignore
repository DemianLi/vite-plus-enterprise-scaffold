# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# dotenv environment variable files
.env
.env.*
!.env.example

# Editor directories and files
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# AI agent worktrees
.claude/worktrees/

# CI 產生的掃描產物
.scan/
sbom.cdx.json
trivy.sarif

# tools/vue-typecheck 反向測試的暫存副本（見 tests/fixtures/README.md）
tools/vue-typecheck/tests/fixtures/.tmp-*

# tools/slice-gen 覆蓋率門檻反向測試的暫存專案（見 tests/coverage-gate.test.ts）
tools/slice-gen/tests/fixtures/.tmp-*

# 覆蓋率報表的產物。切片的 vite.config.ts 預設開著 coverage（C120），
# 所以每一次 `vp test` 都會產生一份。
coverage/

# 驗收規格報表的輸入（每次跑測試都會變，不進版控）
.vitest-results.json

# 突變測試的產物（`stryker.config.mjs`）。每跑一次就整份重寫，不進版控 ——
# ⚠️ 增量檔尤其：它內嵌每支產品碼與每支測試檔的**完整原始碼**，加一個絕對路徑。
reports/
.stryker-tmp/
stryker.log
