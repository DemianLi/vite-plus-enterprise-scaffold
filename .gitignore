# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# dotenv environment variable files
.env
.env.*
!.env.example

# Editor directories and files
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# AI agent worktrees
.claude/worktrees/

# CI 產生的掃描用 lockfile（tools/supply-chain --split-lockfile）
.scan/
sbom.cdx.json
trivy.sarif

# tools/vue-typecheck 反向測試的暫存副本（見 tests/fixtures/README.md）
tools/vue-typecheck/tests/fixtures/.tmp-*

# tools/slice-gen 覆蓋率門檻反向測試的暫存專案（見 tests/coverage-gate.test.ts）
tools/slice-gen/tests/fixtures/.tmp-*

# 覆蓋率報表的產物。切片的 vite.config.ts 預設開著 coverage（C120），
# 所以每一次 `vp test` 都會產生一份。
coverage/

# 驗收規格報表的輸入（每次跑測試都會變，不進版控）
.vitest-results.json

# 突變測試的產物（`stryker.config.mjs`）。每跑一次就整份重寫，不進版控 ——
# ⚠️ 增量檔尤其：它內嵌每支產品碼與每支測試檔的**完整原始碼**，加一個絕對路徑。
# ⚠️ 忽略的是 `reports/` 的**內容**而不是目錄本身 —— 寫成 `reports/` 的話，
#    底下任何東西都再也 re-include 不回來（git 不會重納被排除父目錄底下的檔案）。
reports/*
!reports/research/
.stryker-tmp/
stryker.log
stryker-setup-*.js

# ⚠️ `.vite-plus` 刻意不加 —— 它不在這棵樹裡（#292）
#
# `tools/pii-check` 手抄的跳過清單有這一列。改成問 git 之後（C182 §六），
# 八列裡七列由這份檔案接手，只有它沒有 —— 而查下去的結論是**不該接手**：
# Vite+ 把受管執行期放在 `~/.vite-plus`（`VP_HOME`，見
# `node_modules/vite-plus/docs/guide/env.md`），不在 repo 裡，
# 而 `VP_HOME` 在本 repo 一處都沒設。
#
# 加一列會是一列**沒有受害者的黑名單** —— 正是 C182 §五 否決
# 「`SKIP` 加一列 `.claude`」的那個形狀。
#
# ⚠️ 萬一哪天真的長出來：它會以「未追蹤且未 ignore」的身分被掃到，
# 也就是 `git status` 上看得見 —— 壞法是**吵**（誤報），不是安靜地漏。
# 讓這段話失效的觸發條件是「有人把 `VP_HOME` 指到 repo 相對路徑」，
# 而**沒有任何閘門在看 `VP_HOME`**。
