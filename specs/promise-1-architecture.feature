# language: zh-TW
功能: v1 承諾一 —— 分工開發不受影響的系統架構

  一片功能 ＝ 一個 package，自帶 API／composables／views／store／測試。
  依賴方向單向（apps → features → platform），**切片之間一律禁止互相依賴**。

  這一段是寫給人讀的。打開這個檔案的人可能是接手維護的工程師、要評估
  這條線能不能撐住五個團隊並行的技術主管、或是驗收方 —— 他們讀完這幾個
  場景，就知道「分工開發不受影響」在這個腳手架裡**具體指什麼**，
  不必去讀任何一支閘門的原始碼。

  # ── 這份檔案是什麼 ──────────────────────────────────────────────────
  # 它是**承諾的唯一來源**（TESTING.md 層 3 · 框架承諾）。
  #
  # ⚠️ 它**不是**閘門行為的說明書。說明書會與閘門分岔，而分岔的那一天
  #    沒有人會知道 —— SCOPE.md 開頭警告的正是這件事，v1.0.3 付過代價。
  #
  # 這裡寫的每一個場景，`tools/promise-check` 都會**真的執行一次**：
  # 照著「假設」那句話把一份切片副本弄壞、跑「當」那一句指名的閘門、
  # 然後要求結果與「那麼」一致。⚠️ **repo 的原始碼一個位元組都不會被動到**。
  #
  # 所以這份規格改壞了，閘門就會紅：把「必須紅」改成「必須綠」、
  # 換一支抓不到這個違例的閘門、或是把訊息片段改成不會出現的字，
  # 三種都會當場被抓到。反過來，**刪掉一個場景也會紅** ——
  # 接線還在而沒有場景用它，那是「規格悄悄不再要求某件事」的樣子。
  #
  # ── 怎麼加一條 ──────────────────────────────────────────────────────
  # 先在這裡寫下承諾（人話），再去 tools/promise-check/src/breakage.ts
  # 用**逐字相同**的「假設」句補一段破壞手法。接不上就是紅燈，
  # 不會是安靜跳過。
  # ────────────────────────────────────────────────────────────────────

  場景: 一片切片相依另一片切片時，這道邊界會擋下來
    假設 一片切片的 package.json 宣告了對另一片切片的相依
    當 跑 tools/conformance
    那麼 它必須紅，訊息裡要出現 "跨切片依賴"

  場景: view 直接去拿資料時，分層邊界會擋下來
    假設 一片切片的 view 直接 import 了自己的資料層
    當 跑 tools/conformance
    那麼 它必須紅，訊息裡要出現 "元件直接取數"

  場景: 用了沒有宣告的套件時，幽靈依賴會擋下來
    假設 一片切片 import 了自己 package.json 沒有宣告的套件
    當 跑 tools/conformance
    那麼 它必須紅，訊息裡要出現 "幽靈依賴"

  # ⚠️ 這一條與上面三條的方向相反，而它比那三條更難寫、也更該在。
  # 一道會誤擋的規則，第一天就會被加上例外，然後例外永遠不會拿掉 ——
  # 規則等於廢了，而 CI 全綠。所以「沒有違規時不得紅」也是承諾的一部分。
  場景: 沒有人違規的時候，這道邊界不得說話
    假設 一份沒有被動過的切片副本
    當 跑 tools/conformance
    那麼 它必須綠，訊息裡要出現 "2 個切片"
