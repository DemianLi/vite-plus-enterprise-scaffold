# language: zh-TW
功能: 複雜度門檻不會悄悄過期

  程式碼被重構乾淨之後，守它的那格門檻要跟著降下來。不降的話，門檻留在
  舊高度，而**下一段同樣長的程式碼會安靜地通過** —— 那支極端值白省了，
  而 CI 全綠。

  這一段是寫給人讀的。打開這個檔案的人可能是接手維護的工程師、或是
  要判斷「這棵樹的複雜度門檻能不能信」的人 —— 他們讀完這兩個場景，
  就知道那道閘門具體擋下什麼，不必去讀它的原始碼。

  # ── ⚠️ 這**不是** v1 五條承諾中的任何一條 ─────────────────────────
  # `HANDOFF.md`〈v1.0.0 承諾什麼〉那五條講的是腳手架交給採用團隊的**能力**
  # （架構、樣式接縫、代幣、資安前置過濾）。這一條講的是那些能力背後
  # **量它們的那把尺會不會過期**，與 `tools/doc-facts` 同一類 ——
  # 它的登記理由裡明文寫著：硬把這種閘門塞進某一條承諾表，會重演 C70
  # 那個錯（同一份證據掛在兩條承諾上，而空著的那一條沒有人發現）。
  #
  # ⚠️ 所以檔名刻意**不走** `promise-N-`：`TESTING.md` 與 `HANDOFF.md` 裡
  # 那句「五條承諾守到一條」因此一個字都不用改，`promise-2` 到 `promise-5`
  # 也仍然留給 C118 §五 點名的那四條。**要把它編進五條裡，那是人的裁決。**
  #
  # ── 這份檔案怎麼被執行 ──────────────────────────────────────────────
  # 與 `promise-1-architecture.feature` 同一套：`tools/promise-check` 照著
  # 「假設」那句話把一份副本弄壞、跑「當」那一句指名的閘門、比對「那麼」。
  # ⚠️ **repo 的原始碼一個位元組都不會被動到。**
  #
  # ⚠️ 副本裡的 `platform/`、`apps/` 與那兩片切片，對這道閘門**沒有作用**，
  # 而那不是缺口：C127 §一 判過 `--root` 指的是**被驗的對象**。這裡被驗的
  # 對象是 `vite.config.ts` 裡那幾格門檻的數字；被 lint 的那棵樹是量它們
  # 用的素材，素材不跟著副本走（`tools/threshold-check/src/probe.ts` 的檔頭
  # 寫著為什麼 —— 換成「兩份副本互比」的話，那一招唯一的地基會變成恆真）。
  # ────────────────────────────────────────────────────────────────────

  場景: 門檻被留在沒有人碰得到的高度時，這道閘門會說話
    假設 有人把一格複雜度門檻調高到沒有人碰得到
    當 跑 tools/threshold-check
    那麼 它必須紅，訊息裡要出現 "門檻過期"

  # ⚠️ 這一條與上面那條方向相反，而它在這裡比在承諾一那份更關鍵：
  # 這道閘門所有的紅燈都是「零違規」形狀的，而「什麼都沒量到」也是零違規。
  # 副本裡少複製一個 `vite.config.ts`，上面那條照樣會紅 —— **原因卻是
  # 環境壞了**。少了這一條，那種紅燈會被當成「承諾成立」。
  場景: 沒有人動過門檻的時候，這道閘門不得說話
    假設 一份沒有被動過的門檻設定
    當 跑 tools/threshold-check
    那麼 它必須綠，訊息裡要出現 "複雜度門檻與實測最大值一致"
