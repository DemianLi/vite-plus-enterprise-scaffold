# CONTEXT —— 這個 repo 的領域詞彙與邊界

> ⚠️ **這是骨架，不是事實來源。** 立於 2026-08-20，內容待從既有文件抄錄。
> 在遷移完成之前，**每一個詞的權威定義都在右欄指的地方**，不在這份檔案裡。
> 遷移的範圍、約束與驗收條件見 [`docs/adr/0001-migrate-decisions-to-adr.md`](docs/adr/0001-migrate-decisions-to-adr.md)。

---

## 這個 repo 是什麼

一套 Vite+ 的企業級腳手架。兩條線：

- **`main`** —— 開發超集，什麼都可以有。
- **`release/v1`** —— 交付線，給拉這個腳手架去開案子的團隊。範疇由
  [`SCOPE.md`](SCOPE.md) 界定、`tools/scope-check` 守著。

四個 workspace：`apps/*`（外殼）、`features/*`（示範切片）、
`platform/*`（共用平台）、`tools/*`（閘門）。

---

## 詞彙表 —— **待抄錄**

下面每一列的定義**今天住在右欄**。遷移時把定義搬進中欄，右欄改成「出處」。

| 詞                             | 定義     | 今天的權威來源                                                                        | 查證狀態                    |
| ------------------------------ | -------- | ------------------------------------------------------------------------------------- | --------------------------- |
| 五條承諾 / 承諾五              | _待抄錄_ | `README.md:36`〈v1.0.0 承諾的五件事〉；承諾五的實作在 `SCOPE.md:115` 指的 `.semgrep/` | ✅ 找到列舉處               |
| 判準（「看它守的東西給誰看」） | _待抄錄_ | [`SCOPE.md`](SCOPE.md) §判準；來由見 `DECISIONS.md` C72                               | ✅ 找到定義                 |
| 交付線                         | _待抄錄_ | [`SCOPE.md`](SCOPE.md) 開頭                                                           | ✅ 找到定義                 |
| 切片（slice）                  | _待抄錄_ | 用例：`HANDOFF.md:46`（依賴方向）、`tools/slice-gen/`、`features/*`                   | ⚠️ **只找到用例，未見定義** |
| 閘門（gate）                   | _待抄錄_ | 用例：`tools/*` 各自的 README、`.github/workflows/`                                   | ⚠️ **只找到用例，未見定義** |
| ⭐ / ★ / 🔴 / ⚠️ 標記          | _待抄錄_ | 用例：`DECISIONS.md`、`platform/ui/tests/*`                                           | ⚠️ **只找到用例，未見定義** |

⚠️ 最後一欄是 2026-08-20 立骨架時**實際 grep 過**的結果，不是推測。標
「只找到用例」的那幾個，意思是這個 repo 到處在用這個詞、但**沒有任何一處
定義它** —— 遷移時該做的是去定義它，不是找一段看起來像定義的文字抄過來。

⚠️ 原本這裡把「承諾五」指向 `HANDOFF.md`，那是錯的 —— 那個詞在 `HANDOFF.md`
一次都沒出現。這正是上一段警告的東西：憑印象寫的指標，會把下一個人送到錯的檔案。

⚠️ **不要在這裡憑印象填定義。** 抄錄時要指得出行號；指不出來的，那個詞就還
沒有被定義過，該做的是去定義它，不是在這裡發明一個。

---

## 三份根層文件，三條正交的軸

抄錄的來源不只一份，各有各的問題要回答 —— 去錯地方會抄到不對的東西：

| 文件                           | 軸   | 回答什麼                                       |
| ------------------------------ | ---- | ---------------------------------------------- |
| [`SCOPE.md`](SCOPE.md)         | 目錄 | 這個目錄准不准出現在 `release/v1` 的樹上？     |
| [`HANDOFF.md`](HANDOFF.md)     | 能力 | 我的案子需要 X，該用哪條分支？                 |
| [`CHANGELOG.md`](CHANGELOG.md) | 時間 | 哪個版本動了什麼？                             |
| [`DECISIONS.md`](DECISIONS.md) | 決策 | 這件事**當初**為什麼這樣決定？（89 條 `C<n>`） |

⚠️ `README.md` 與 `HANDOFF.md` 由 `tools/doc-facts` 守著。把內容搬出這兩份，
可能讓閘門紅 —— 見 ADR-0001 的〈約束〉。
